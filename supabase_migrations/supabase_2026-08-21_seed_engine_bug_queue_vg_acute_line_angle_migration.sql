-- 2026-08-21 — the ACUTE LINE-ANGLE fold (founder_proxy Checkpoint B finding F1 on
-- lines_and_planes_in_space, engine half E2). 1 new row, filed already FIXED.
-- A Rule-40 platform change to src/lib/renderers/field_3d_renderer.ts, landing on master on its own branch.
-- Gate: check:vector-geometry-3d §30 (ALL SECTIONS PASSED, negative control fires).
-- Generated by src/scripts/_seed_engine_bug_queue_vg_acute_line_angle.ts from the SAME structure
-- the TS path applies. Idempotent, order-independent, never a downgrade.

INSERT INTO engine_bug_queue (bug_class, title, severity, owner_cluster, root_cause, prevention_rule, probe_type, probe_logic, status, concepts_affected, fixed_in_files, discovered_in_session, row_type, fixed_at) VALUES
('formula_surface_carries_an_absolute_value_the_readout_never_applies_so_a_state_prints_an_angle_its_own_equation_forbids', 'STATE_6 printed "angle = 115.0°" directly beneath cos θ = |d₁·d₂| ⁄ (‖d₁‖‖d₂‖), which gives 65.0° — the Δ5 line-line arc branch published the raw [0,180] vector angle and never applied the modulus its own formula surface displays', 'CRITICAL', 'peter_parker:field3d_surgeon', 'The angle-arcs pass of vgResolveLinesPlanes (src/lib/renderers/field_3d_renderer.ts, the Δ5 block, resolve by symbol) has three subject branches. The "<planeId>.normal" and "<planeId>" branches take their value from vgLinePlaneAngles, which folds inside itself with Math.abs and returns both of its angles in [0, 90], and each then folds its second leg onto the acute side with an explicit "if (vgDotVec(u0, u1) < 0) u1 = vgScaleVec(u1, -1);" under the comment "The arc is drawn on the acute side, which is the angle named." The third branch, ctx.lines[mB] — line versus line, the one subject for which the acute convention IS the definition — did neither: it read val = vgAngleDeg(u0, u1), the general undirected direction angle over the full [0, 180]. The helper''s own header argued for that, on the ground that a readout must track a continuous rotation past 90 or it would "disagree with the motion beside it" — reasoning that mistakes continuity of the number for a physics constraint. A line has no direction: d and -d name the same line, so the angle between two lines is the acute one by definition (NCERT Cl.12 §11.4; the same in IB HL and A-level FM), which is exactly what the modulus in the printed formula says. MEASURED on lines_and_planes_in_space STATE_6, whose formula_overlay is cos θ = |d₁·d₂| ⁄ (‖d₁‖‖d₂‖) and whose animate[] ramps theta_deg 69.3846 → 115 over 10500-19500 ms with control_ranges [25, 115]: the readout ended the state at "angle = 115.0°" (the equation gives 65.0°) and the frozen frame, pinned mid-rotation, read 92.2° where the equation gives 87.8°. A Class-12 student who copies the readout into a CBSE paper is marked wrong, and the sim contradicts itself on one screen. WHY EVERY WEAKER SAMPLING WAS GREEN: at the authored default (69.3846°) the fold is a no-op; the arithmetic of the raw angle was always right; the Δ2b/Δ2c reveal gating was right (the number waited correctly for its arc, then said a forbidden thing); THE EYE pins one frozen frame with no equation to compare it against, and D5/D6/D7 measure pixel motion, not meaning. The discriminating quantity is the readout at the FAR END of the state''s own knob range, evaluated against the equation on screen. FIXED 2026-08-21: the line-line branch folds u1 first and then takes val, so one decision corrects all three surfaces that read val — the inline publish, the generic o.readout publish after the arc push, and the arc''s own value_deg — and the drawn wedge comes down with the number instead of opening past it. vgAngleDeg stays the raw [0, 180] helper: the fold belongs to the SUBJECT, and only that branch knows the subject is two lines. BLAST RADIUS MEASURED on the real concept, all nine states, pre-fix build versus shipped build over each state''s whole authored ms span and its whole theta slider range: STATE_6 alone changes (75/221 authored frames, 51/181 slider positions, all of them past 90°); STATE_1-5 and STATE_7-9 are bit-identical, and below 90° STATE_6 itself is bit-identical at all 65 sampled knob values. Mode "products" cannot be reached: vgResolveLinesPlanes has ONE call site, behind d.mode === "lines_planes", and vector_products_in_space''s θ (a, b) is between VECTORS, legitimately obtuse, printed from its own theta_deg KNOB — verified in pixels at θ = 130° ("θ = 130°", "a·b = -3.86", obtuse arc) on run 20260821-025346. RESIDUAL, newly revealed by this fix and NOT dispatched (founder call): the theta_deg SLIDER ROW label is composed by vgThetaRowLabel as "θ (d₁, d₂)" from the labels of the reference and rotated lines, so past 90° that row reads "θ (d₁, d₂): 115°" while the readout two inches away reads "angle = 65.0°". The row label was correct before this fix and is a rotation-KNOB name, not a measured angle; the remedy is either an engine relabel of a knob that is no longer the angle it turns between, or an authoring relabel of that control. That layer has never been content-reviewed.', 'A FORMULA SURFACE AND THE READOUT THAT WOULD VERIFY IT MUST AGREE OVER THE WHOLE RANGE THE STATE CAN REACH, NOT ONLY AT THE AUTHORED DEFAULT. Where a surface prints |·|, a modulus, a norm, a max/min or any other range-restricting operator, drive the state''s own knob to BOTH bounds of its control_ranges and to the end of every animate[] ramp, and evaluate the printed equation at each — a spot check at the rest pose is structurally blind to this class, because the rest pose is where the two agree. Engine side: an angle token whose NAME says "lines" is folded to acute AT THE VALUE SOURCE, so the readout, the generic o.readout publish and the arc''s own value_deg cannot diverge; the arc''s second leg is folded in the same statement so the picture cannot contradict the number. The fold belongs to the SUBJECT, never to the shared direction-angle helper — vgAngleDeg stays [0, 180] for any future direction-versus-direction claim, and a caller that folds at the bottom would destroy information no caller could get back. Do not reason that a readout must be CONTINUOUS through a convention boundary: continuity of the number is not a physics constraint, agreement with the equation on screen is, and the arc turning around with it is what makes the picture honest.', 'js_eval', 'check:vector-geometry-3d §30 (src/scripts/check_vector_geometry_3d.ts). Build the shipped vgResolveLinesPlanes and run STATE_6''s verbatim vg block over θ ∈ [25, 115] (the state''s own control_ranges) at 1° steps, and over its 9 s theta ramp through the shipped vgAnimValue. Assert (1) the fixture actually crosses 90 (the raw angle reaches 115) so the claim cannot pass vacuously; (2) angle_lines_deg never exceeds 90; (3) at every sample it EQUALS acos(|d₁·d₂| / (‖d₁‖‖d₂‖)) evaluated by a second implementation written in the gate file — not a clamp, which would also satisfy (2) and be wrong everywhere past 90; (4) the arc''s value_deg equals the readout and its drawn legs satisfy u0·u1 ≥ 0 at every sample; (5) products mode is untouched, driven through the shipped frame driver at θ = 115 and 150 with the θ row read as TEXT off the panel and a·b asserted still negative. NEGATIVE CONTROL: rebuild the SHIPPED resolver body with the one fold statement deleted (guarded — a lost anchor throws rather than silently planting nothing) and watch it FAIL every claim: it reaches 115.0°, disagrees with the formula by 50°, renders the panel text "angle = 115.0°", reads 92.2° at the frozen pin instead of 87.8°, and draws the wedge at u0·u1 = -0.4226. BLAST-RADIUS CONTROL: with no line-line arc authored, and below 90° with one authored, the two builds must agree BIT FOR BIT; at θ = 90 exactly they differ by 3.8e-5° because the authored 6-dp rotation axis overshoots the crossing, and both render "angle = 90.0°".', 'FIXED', ARRAY['lines_and_planes_in_space']::text[], ARRAY['src/lib/renderers/field_3d_renderer.ts']::text[], 'session_2026-08-21_vg_acute_line_angle', 'incident', '2026-08-21T03:20:00.000Z')
ON CONFLICT (bug_class) DO UPDATE SET
  title = EXCLUDED.title, severity = EXCLUDED.severity, owner_cluster = EXCLUDED.owner_cluster,
  root_cause = EXCLUDED.root_cause, prevention_rule = EXCLUDED.prevention_rule,
  probe_logic = EXCLUDED.probe_logic, status = EXCLUDED.status, fixed_at = EXCLUDED.fixed_at,
  fixed_in_files = EXCLUDED.fixed_in_files, concepts_affected = EXCLUDED.concepts_affected
WHERE engine_bug_queue.root_cause NOT LIKE '%the frozen frame, pinned mid-rotation, read 92.2° where the equation gives 87.8°%'
  AND engine_bug_queue.status NOT IN ('FIXED', 'FALSE_POSITIVE');
