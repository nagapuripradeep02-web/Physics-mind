-- ============================================================================
-- 2026-06-28: moving_coil_galvanometer recording review (founder) — 3 defects.
--   1 (MAJOR) mcg_coil_rotation_opposes_bil_force_couple   — rotation/τ sign error
--   2 (MODERATE) mcg_uniform_field_force_arrow_shrinks_with_cosphi
--   3 (MODERATE) mcg_equilibrium_torque_balance_choreography_underplayed
-- All status OPEN. Generated by
-- src/scripts/_seed_engine_bug_queue_mcg_rotation_review.ts.
-- ============================================================================

INSERT INTO engine_bug_queue (bug_class, title, severity, owner_cluster, root_cause, prevention_rule, probe_type, probe_logic, status, concepts_affected, fixed_in_files, discovered_in_session, row_type) VALUES
('mcg_coil_rotation_opposes_bil_force_couple', 'Moving-coil galvanometer: the coil spins OPPOSITE to the BIL force couple it teaches — coil.rotation.y=+phiRad and the deflecting-torque arrow (+y) are reversed relative to the (correct) left-into-page / right-out-of-page forces, so each side visibly moves opposite its own force arrow', 'MAJOR', 'peter_parker:renderer_primitives', 'In field_3d_renderer.ts (moving_coil_galvanometer scenario) the field, current and forces are all PHYSICALLY CORRECT: N pole at x=-1.6, S at +1.6 (mcgBuildPole signs -1/+1, line ~8361), field drawn +x so B points N->S; current marches UP the left side (dotDefs, line ~8405); right-hand rule therefore puts the left-side force into the page (-z) and the right-side force out of the page (+z), exactly as the renderer draws them (mcg_force_left dir (0,0,-1) @ -x, mcg_force_right (0,0,+1) @ +x, lines ~8418-8421) and exactly as narration s2_3 / reveal_couple state. That couple = sum(r x F) = -y, so the coil MUST rotate -y (front +z sweeps toward -x). But the renderer applies coil.rotation.y = +phiRad (line ~8647) and draws the deflecting-torque arrow mcg_tau_deflect at +y (line ~8445) — both reversed. Net symptom: each coil side moves opposite to its own green force arrow, and the magenta deflecting-torque arrow agrees with the (wrong) spin instead of with the forces. The formulas/magnitudes are all correct; this is purely a rotation/torque-vector SIGN error.', 'The rendered rotation sense must be derived from (and validated against) the rendered force couple: the sign of sum(r x F) computed from the force-arrow directions and positions must equal the sign of coil.rotation.y, and the deflecting-torque vector must point along that same axis sense (restoring-torque opposite). Add a unit/visual probe asserting this for any current-loop-in-field sim. Never set the deflection sign independently of the forces.', 'manual', 'Play STATE_2 and watch the right edge of the coil at deflection onset: its green force arrow points OUT of the page (toward the viewer) but the edge swings INTO the page (away) => FAIL. After the fix, every side moves the same way its force arrow points, and the deflecting-torque arrow points along the actual spin axis. (js_eval form: assert sign(sum_i r_i x F_i over the two force arrows) === sign(coil.rotation.y).)', 'OPEN', ARRAY['moving_coil_galvanometer'], ARRAY[]::text[], 'session_2026-06-28_mcg_recording_review', 'incident'),
('mcg_uniform_field_force_arrow_shrinks_with_cosphi', 'Moving-coil galvanometer uniform-field states (S2/S3): the BIL force arrows are scaled x|cosφ| and parented to the coil so they shrink and rotate, but |F|=BIL is constant in magnitude AND fixed in world-direction as the coil turns about the vertical axis — only the torque (via the lever arm) falls as cosφ', 'MODERATE', 'peter_parker:renderer_primitives', 'In the uniform-field states the renderer sets the force-arrow length fLen = 0.85*|cos(phi)| (line ~8673) and the force arrows are children of the coil group (added at lines ~8419/8421) so they also rotate with the coil. Physically, when the coil turns about the vertical (y) axis its two vertical sides stay vertical and stay perpendicular to a uniform horizontal B, so |F|=BIL is constant in BOTH magnitude and world-direction (into/out of the page). What actually decreases as cos(phi) is the LEVER ARM (the x-position of each side), i.e. the TORQUE — which the deflecting-torque arrow already carries correctly (tau scaled by cos(phi), line ~8681). Drawing the force itself shrinking (and rotating with the coil) misrepresents the mechanism; narration s3_3 ("each force shrinks") reinforces the same misconception. Bites most in STATE_3 where phi reaches ~42deg.', 'In uniform-field states hold the force-arrow length AND world-direction constant (do not parent uniform-field force arrows to the coil rotation); express the cos(phi) fade ONLY through the torque arrow or a visibly shrinking lever arm. Reserve coil-child (co-rotating, tangential) force arrows for the RADIAL field, where the force direction genuinely rotates with position. Reword s3_3 to "the lever arm / torque shrinks" rather than "each force shrinks".', 'manual', 'Step STATE_3 to a large deflection: the green side-force arrows should keep full length and keep pointing straight into/out of the page; only the magenta deflecting-torque arrow (and/or a lever-arm guide) should shrink with cos(phi). If the force arrows themselves shrink or tilt with the coil => FAIL.', 'OPEN', ARRAY['moving_coil_galvanometer'], ARRAY[]::text[], 'session_2026-06-28_mcg_recording_review', 'incident'),
('mcg_equilibrium_torque_balance_choreography_underplayed', 'Moving-coil galvanometer equilibrium (S5/S6): the two opposing torques are drawn as collinear axial arrows on one line and the damped-settle overshoot is <1deg (effectively invisible), so the NIAB=kφ tug-of-war and the needle oscillate-and-settle do not read', 'MODERATE', 'peter_parker:renderer_primitives', 'STATE_6 settle uses overshoot 2deg * sin(u*PI) * exp(-2.5u) (line ~8631), which peaks below ~1deg for ~0.3s — so the needle reads as a smooth glide with no visible oscillation. The deflecting and restoring torque arrows share the same origin (0,0,1.6) and are collinear (+y and -y, lines ~8445/8450), so at equilibrium (equal length) they look like a single double-headed arrow rather than two opposing twists. There is no explicit "balanced" beat when NIAB=kphi is reached. The mechanism is all present and correct; it is just visually underplayed, which is why the equilibrium "both torques at once" reads as static/ambiguous on a recording.', 'Dramatize the equilibrium: render the deflecting twist as an arc ON the coil (in the rotation sense) and the restoring twist as an arc ON the hairspring (opposite sense) rather than two collinear axial vectors; during settle hold the deflecting arc fixed while the restoring arc grows until equal; use a visibly larger decaying oscillation (bigger overshoot, lighter damping) and add a brief lock/pulse (or "=") when NIAB=kphi. Equilibrium/balance beats need a perceptible (>~3-5deg) oscillation and a non-collinear opposing-twist depiction.', 'manual', 'Watch STATE_6: the needle should visibly overshoot and oscillate before settling, and the two torques should read as two distinct opposing twists that become equal at rest (with a clear "balanced" moment). If the settle looks like a static jump and the torques look like one axial arrow => FAIL.', 'OPEN', ARRAY['moving_coil_galvanometer'], ARRAY[]::text[], 'session_2026-06-28_mcg_recording_review', 'incident')
ON CONFLICT (bug_class) DO NOTHING;
