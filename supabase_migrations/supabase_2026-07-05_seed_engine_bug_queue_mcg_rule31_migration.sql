-- 2026-07-05: moving_coil_galvanometer Rule-31 rebuild — close 3 engine_bug_queue rows.
-- coil_rotation_opposes (stale, mcgSweep=-1 already correct), force_arrow_shrinks
-- (renderer fix: constant |F|, world-fixed direction in uniform field), equilibrium
-- choreography (verified addressed: non-collinear arcs + 7deg overshoot + balance pulse).
-- Generated by src/scripts/_seed_engine_bug_queue_mcg_rule31.ts — idempotent.

INSERT INTO engine_bug_queue (bug_class, title, severity, owner_cluster, root_cause, prevention_rule, probe_type, probe_logic, status, concepts_affected, fixed_in_files, discovered_in_session, row_type) VALUES
('mcg_coil_rotation_opposes_bil_force_couple', 'Moving-coil galvanometer: the coil spins OPPOSITE to the BIL force couple it teaches — coil.rotation.y=+phiRad and the deflecting-torque arrow (+y) are reversed relative to the (correct) left-into-page / right-out-of-page forces', 'MAJOR', 'peter_parker:renderer_primitives', 'VERIFIED STALE + marked FIXED 2026-07-05. The row describes coil.rotation.y=+phiRad / tau=+y, but the shipped renderer has mcgSweep=-1 (field_3d_renderer.ts:11855) since the file was first written (24f4bc3, 2026-06-29): coil.rotation.y = mcgSweep*phiRad = -phiRad and mcg_tau_deflect dir = (0,mcgSweep,0) = -y. The force couple from the drawn arrows (mcg_force_left into page @ local x=-h, mcg_force_right out @ x=+h) is sum(r x F) = -y, so the -y rotation and -y torque arrow are CONSISTENT with the forces — the coil rotates WITH its own force couple. The renderer comment at :11849-11854 documents this. eye-walker run 20260705-152326 confirmed independently via world-position + camera-projection-depth computation across phi=0..90 and frame cross-check (STATE_7 phi 11.5->68.8: the +h/out-of-page wire''s camera depth stays positive/in-front the whole sweep, the -h/into-page wire recedes) plus STATE_8 phi=85.9. No code change was required for the rotation sign; the row was stale relative to the current renderer.', 'The rendered rotation sense must be derived from (and validated against) the rendered force couple: sign(sum(r x F)) from the force-arrow directions+positions must equal sign(coil.rotation.y), and the deflecting-torque vector must point along that same axis sense (restoring opposite). A single scenario-wide sign constant (mcgSweep) must thread rotation + tau vectors + dial + spring so they stay mutually consistent. Before logging a rotation-sign incident, derive the couple sign from the actual arrow vectors in code — a diagonal/isometric camera makes raw screen-left/right unreliable for judging rotation sense.', 'manual', 'Compute sum(r x F) from mcg_force_left/right positions+directions; assert sign equals sign(coil.rotation.y = mcgSweep*phiRad) and sign(mcg_tau_deflect.y). With mcgSweep=-1 all three are -y → consistent. NOTE: on a 45-degree isometric camera a deflecting wire crosses screen-left<->right past phi=45 while staying in front — judge by camera-projection DEPTH, not raw screen x.', 'FIXED', ARRAY['moving_coil_galvanometer'], ARRAY['src/lib/renderers/field_3d_renderer.ts'], 'session_2026-07-05_mcg_rule31_rebuild_and_force_fix', 'incident'),
('mcg_uniform_field_force_arrow_shrinks_with_cosphi', 'Moving-coil galvanometer uniform-field states (S2/S3): the BIL force arrows are scaled x|cosphi| and parented to the coil so they shrink and rotate, but |F|=BIL is constant in magnitude AND fixed in world-direction as the coil turns — only the torque (via the lever arm) falls as cosphi', 'MODERATE', 'peter_parker:renderer_primitives', 'FIXED 2026-07-05 (field_3d_renderer.ts, updateMovingCoilGalvanometerFrame). Was: fLen = sd.radial ? 0.85 : 0.85*|cos(phi)| — the uniform-field force arrows shrank with cos(phi), wrongly implying |F| depends on phi. Fix: force-arrow length is now a constant 0.85 in BOTH fields (|F|=BIL is constant). Direction: in the UNIFORM field the two vertical sides stay vertical, so F = I L(+/-y) x B(+x) = -/+z is FIXED in world regardless of coil angle — the coil-child arrows are now counter-rotated by -coil.rotation.y so they hold world -/+z; in the RADIAL field the force is genuinely tangential and co-rotates, so the coil-local +/-z direction is kept. The cos(phi) dependence now reads only via the torque arrow (mcg_tau_deflect ∝ cos phi) + the crowded scale (S3). retrofit-surgeon reworded s3_3 in lockstep ("the lever arm / torque shrinks" not "each force shrinks").', 'In uniform-field current-loop sims hold BIL force-arrow length AND world-direction constant (do not parent uniform-field force arrows to the coil rotation); express the cos(phi) fade ONLY through the torque arrow or a visibly shrinking lever arm. Reserve coil-child (co-rotating, tangential) force arrows for the RADIAL field, where the force direction genuinely rotates with position.', 'manual', 'In S2/S3, across the deflection the two green force arrows must keep constant length and a world-fixed +/-z direction (they translate with their wire but do not shrink or tilt). Any cos(phi) fade must appear on the torque arrow / lever arm, not the force arrows.', 'FIXED', ARRAY['moving_coil_galvanometer'], ARRAY['src/lib/renderers/field_3d_renderer.ts', 'src/data/concepts/moving_coil_galvanometer.json'], 'session_2026-07-05_mcg_rule31_rebuild_and_force_fix', 'incident'),
('mcg_equilibrium_torque_balance_choreography_underplayed', 'Moving-coil galvanometer equilibrium (S5/S6): the two opposing torques are drawn as collinear axial arrows on one line and the damped-settle overshoot is <1deg (effectively invisible), so the NIAB=kphi tug-of-war and the needle oscillate-and-settle do not read', 'MODERATE', 'peter_parker:renderer_primitives', 'VERIFIED ADDRESSED + marked FIXED 2026-07-05. The row describes collinear axial arrows + <1deg overshoot, but the current renderer already draws the opposing twists as SEPARATE non-collinear arcs: mcgBuildTorqueArc places the deflecting twist as an arc ON the coil top (yLevel 0.95, sense mcgSweep) and the restoring twist as an arc ON the hairspring (yLevel -1.30, opposite sense -mcgSweep) — the code comment at :12085-12090 states "never the old collinear double-headed axial arrow". The deflecting arc span ∝ NIAB (held) while the restoring arc span ∝ k*phi (grows to equal it), and S6''s settle_phi authors overshoot_deg=7 (>5deg, perceptible) with a mcg_balance_marker "=" + ring brightness pulse as |NIAB - k*phi| -> 0. eye-walker run 20260705-152326 saw the two separate non-collinear arcs (magenta tau on coil, teal tauS on hairspring) and a multi-degree damped settle (phi 28.9->25.9->28.6) on-frame. Founder can confirm the settle drama at the S5/S6 review frames.', 'Equilibrium/balance beats need a perceptible (>~3-5deg) decaying oscillation and a NON-collinear opposing-twist depiction: deflecting twist as an arc on the moving body (rotation sense), restoring twist as an arc on the restoring element (opposite sense); during settle hold the deflecting arc while the restoring arc grows to equal, and add a brief lock/pulse ("=") at balance.', 'manual', 'In S5/S6 confirm two SEPARATE arcs at different heights/curls (coil vs hairspring), opposite sense, restoring arc growing to match the deflecting arc, a visible multi-degree damped oscillation, and an "=" / ring pulse at balance — not a single collinear axial double-arrow.', 'FIXED', ARRAY['moving_coil_galvanometer'], ARRAY['src/lib/renderers/field_3d_renderer.ts'], 'session_2026-07-05_mcg_rule31_rebuild_and_force_fix', 'incident')
ON CONFLICT (bug_class) DO UPDATE SET status = EXCLUDED.status, root_cause = EXCLUDED.root_cause,
  prevention_rule = EXCLUDED.prevention_rule, probe_logic = EXCLUDED.probe_logic,
  title = EXCLUDED.title, severity = EXCLUDED.severity, owner_cluster = EXCLUDED.owner_cluster,
  fixed_in_files = EXCLUDED.fixed_in_files;
