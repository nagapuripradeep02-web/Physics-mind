-- 2026-06-26: engine_bug_queue scar from the 2nd founder review of parallel_currents_force (RHR animation).
-- Generated by src/scripts/_seed_engine_bug_queue_pcf_rhr_animation.ts — idempotent.

INSERT INTO engine_bug_queue (bug_class, title, severity, owner_cluster, root_cause, prevention_rule, probe_type, probe_logic, status, concepts_affected, fixed_in_files, discovered_in_session, row_type) VALUES
('field3d_rhr_hand_static_no_curl_choreography', 'Force-rule right-hand was STATIC (rigid baked curl, thumb-tracks-F only) instead of an animated I→B→F demonstration that pauses and highlights at each beat (founder 2nd review)', 'MAJOR', 'peter_parker:renderer_primitives', 'parallel_currents_force STATE_3 built its right-hand with createRightHand() — the simple stylised hand whose four fingers are a baked, rigid quarter-circle curl; the per-frame block only re-oriented the whole group so the thumb pointed along F. It never animated the finger curl, never sequenced I→B→F, never paused/dwelled, never highlighted the matching scene quantity, and showed no step labels. A fully articulated, choreographed hand (rhrFingerJoints FK + the lorentz_hand v→B→F animate-loop choreography with SET_HAND_PHASE freeze) already existed but ran ONLY for elementType==="lorentz_hand", so the pcf hand (pcf_rhr_hand) was excluded. Founder (recording 26.06.2026_20.55.59) pen-annotated the hand asking it to slowly point fingers along the current (charge Q up), curl into the page toward B1, then thumb→F, pausing at each of I, B, F.', 'A state that teaches a cross-product DIRECTION (F = I L × B, F = q v × B, B = ...) must ship the ARTICULATED, choreographed right-hand (real finger-curl via rhrFingerJoints, a slow dwelling I→B→F cycle with a clear pause at each beat, per-beat brightness highlight of the matching scene vector per Rule 29, the embedded I/B/F arrows+labels, and short step captions), oriented from the scenario geometry — not a static mesh that only points its thumb. Reuse the buildArticulatedHandParts + the lorentz choreography pattern for every new RHR hand; do not fall back to the rigid createRightHand.', 'manual', 'On a cross-product force/field state, scrub the frozen/dense frames: the fingers must visibly transition flat-along-I → curled-into-B → thumb-along-F across the state clock (not a single fixed pose), with the matching scene element (current arrow / B vector / force arrow) brightened on its beat and a short step label present. A hand whose finger geometry is identical across all frames is the static-hand regression.', 'FIXED', ARRAY['parallel_currents_force'], ARRAY['src/lib/renderers/field_3d_renderer.ts'], 'session_2026-06-26_parallel_currents_force_rhr_animation', 'incident')
ON CONFLICT (bug_class) DO UPDATE SET status = EXCLUDED.status, root_cause = EXCLUDED.root_cause,
  prevention_rule = EXCLUDED.prevention_rule, fixed_in_files = EXCLUDED.fixed_in_files;
