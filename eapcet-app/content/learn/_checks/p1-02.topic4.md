# Second reading — p1-02 topic 4 "A body thrown straight up"

File checked: `content/learn/_parts/p1-02.topic4.json`. Brief: `content/learn/_BRIEF_LEARN.md`.
Every number worked independently on paper with g = 10 m/s². Every wrong route was executed as
written to see which option it produces. The JSON was not edited.

Totals: ok 23 · weak 9 · wrong 0 · harmful 2.

All six marked answers and both check answers are correct, and every route lands on the option it
names. The findings are about route wording, one sign convention, and repeated items.

## Subtopic 1 — throw_up ("Time up, height, and the return speed")

| item | verdict | note |
|---|---|---|
| concept card | weak | Four small things. (1) Line 2 "Its speed drops by g every second": g is an acceleration, so the speed drops by g × 1 s = 10 m/s each second — write "its speed drops by 10 m/s every second". (2) The formula field writes `h = u²/2g` while line 5 writes `h = u²/(2g)`; without the brackets it reads as (u²/2)g. Use `h = u²/(2g)` in both places. (3) h is the height above the throw point, not above the ground; say so. (4) Lines 6, 7 and 8 are true only with no air resistance, and the card never says that (the check stem does). Also: the card teaches h = u²/(2g) and the title promises height, but no check or apply question uses height — either drop it from the card or add a height question. |
| check | ok | |
| lq_p1-02_throw_up_01 | ok | Answer 2 = 3 s is right (30/10). |
| — r | ok | |
| — m0 | ok | 30/20 = 1.5 s = option 1. |
| — m1 | weak | Route names the correct operation: "...instead of dividing" tells the student the right method inside the phrase the brief says must be only what they did. Rewrite as "I took g away from the given speed". The type is also questionable: subtracting is a wrong method, not wrong arithmetic, so `concept` fits better than `calculation`. |
| — m2 | harmful | "I found the total time for the whole trip up and down" is a step a correct solver may honestly have taken: T = 2u/g = 6 s, then half = 3 s gives the right answer 3 s, and this route's own fix endorses that working ("Use half of the total flight time"). A student who answered 3 s correctly could say this sentence and be marked as having made the mistake. Rewrite so only the wrong solver can say it, e.g. "I doubled the time to the top". |
| — m2 fix | ok | Names the mistake and the fixing step. |
| lq_p1-02_throw_up_02 | ok | Answer 3 = 2 s is right (72 × 5/18 = 20 m/s; 20/10 = 2 s). |
| — r | ok | |
| — m0 | harmful | Same defect as Q01 m2. "I used the formula for the whole trip up and down" is honestly sayable by a student who found T = 4 s and halved it to the correct 2 s — the fix itself offers that route ("Halve the total flight time"). Rewrite as "I doubled the answer after dividing by g". |
| — m1 | ok | 20/20 = 1 s = option 2. |
| — m2 | ok | 72/10 = 7.2 = option 4; fix gives the 5/18 conversion correctly. |
| lq_p1-02_throw_up_03 | ok | Answer 1 = 20 m/s is right (4 s total → 2 s up → 10 × 2). |
| — r | ok | |
| — m0 | weak | "I skipped halving the time" is written from hindsight: a student who did u = g × 4 did not know halving was needed, so they would not describe it as skipping. It also names the missing correct step, which the brief's route rule excludes. Rewrite as "I multiplied g by the total time". |
| — m1 | ok | 10 + 2 = 12 = option 3; the fix correctly says adding gives wrong units. |
| — m2 | ok | 4/10 = 0.4 = option 4. |

## Subtopic 2 — zero_v_nonzero_a ("Zero velocity at the top, acceleration still g")

| item | verdict | note |
|---|---|---|
| concept card | weak | The formula line mixes two sign conventions: `v = u − gt (a = g throughout)`. In v = u − gt, up is positive and g is a positive number, so the acceleration in that equation is −g, not +g. Write it as "v = u − gt, with up taken positive; the acceleration is g downward at every instant" and let the meaning line say the direction (the worked example does say "downward", the formula field does not). All eight lines are physically true. |
| check | weak | Wording only. "Velocity being zero does not switch off gravity" and "nothing switches it on later" treat gravity as a device with a switch — a metaphor under Rule 41. The literal second half of each line already carries the meaning: "gravity never stops acting", "gravity acts from the moment the body is thrown". Answer 3 and all three why_wrong lines are true. |
| lq_p1-02_zero_v_nonzero_a_01 | weak | Answer 2 is right, but this item is a near-copy of the check above it: the same instant, the same belief, and its option 2 says what the check's correct option 3 says. It is also marked difficulty 1, which the brief defines as the formula used once with the numbers given, and there is no formula or number in it. Replace with a one-step numeric item (for example: thrown up at 15 m/s, what is the acceleration 1 s after release?). |
| — r | ok | |
| — m0 | ok | Produces option 1. |
| — m1 | ok | Swapping the two labels gives "velocity is g, acceleration is zero" = option 3. |
| — m2 | ok | Produces option 4. |
| lq_p1-02_zero_v_nonzero_a_02 | weak | Answer 3 is right and all three routes are clean, but this is the third item in a row on the single belief "the acceleration is g downward at every instant" (check, Q01, Q02). Difficulty 2 in the brief needs one conversion or one rearrangement; this item needs neither. Give it a rearrangement instead, for example: a ball reaches the top 1.5 s after release — find the throw speed and state the acceleration at the top. |
| — r | ok | |
| — m0 | ok | Acceleration along the motion gives option 1. |
| — m1 | ok | Produces option 2. |
| — m2 | ok | Produces option 4. |
| lq_p1-02_zero_v_nonzero_a_03 | weak | Answer 4 is right, but the numbers are exactly the worked example on the same card (u = 20 m/s, t = 2 s, v = 0, a = 10 m/s² downward). A student can copy the answer without computing. Change the numbers and keep the teaching point, for example u = 30 m/s at t = 3 s, which again gives v = 0 with a = 10 m/s² downward. |
| — r | ok | |
| — m0 | ok | Produces option 1. |
| — m1 | weak | "I subtracted g from the launch speed instead of g times t" names the correct quantity inside the route, which the brief's route rule excludes. Rewrite as "I took g away from the launch speed". The option it names is right: 20 − 10 = 10 m/s upward = option 2. |
| — m2 | ok | Produces option 3. |

## Checks that passed everywhere

- Both marked check answers and all six apply answers are correct.
- Every m route, executed as written, produces exactly the option it names. None produces a
  different option or a number that is not on the list.
- No route repeats another in its question, none exceeds twelve words, none contains a two-digit
  number, and no route option number equals the answer.
- Each question has one r route and one m route per wrong option; the correct option number varies
  (2, 3, 1 and 2, 3, 4).
- Every fix names the mistake and the step that repairs it.
- No claim of exam readiness anywhere; no "simply", "obviously" or "just".

## Round 2

Re-check of the reworked file against the same brief (rule 3 now also bans "instead of", "rather
than", and the hindsight verbs dropped / skipped / left out / missed / swapped in route phrases).
Every number worked again on paper with g = 10 m/s²; every wrong route executed as written to see
which option it lands on, then tested against the right answer's working. Round 1's text above is
unchanged and the JSON was not edited.

Totals: ok 31 · weak 5 · wrong 0 · harmful 0.

Both round-1 harmful routes and all six round-1 weak routes are fixed. Answers re-derived
independently: 3 s · 2 s · 20 m/s · 10 m/s² downward · u = 15 m/s with 10 m/s² downward · v = 0 with
10 m/s² downward; check answers 2 and 3. All six are correct.

Routes executed, subtopic 1: 30/(2×10) = 1.5 (opt 1) · 30 − 10 = 20 (opt 3) · 2 × 3 = 6 (opt 4) ·
2 × 2 = 4 (opt 1) · 20/(2×10) = 1 (opt 2) · 72/10 = 7.2 (opt 4) · 10 × 4 = 40 (opt 2) · 10 + 2 = 12
(opt 3) · 4/10 = 0.4 (opt 4). Subtopic 2: a = 0 (opt 1) · direction of motion at t = 1 s is up, so
10 m/s² upward (opt 3) · the given speed as the acceleration, 15 (opt 4) · 1.5/10 = 0.15 (opt 1) ·
a = 0 at the top (opt 2) · the two numbers exchanged, u = 10 and a = 15 (opt 4) · a = 0 (opt 1) ·
30 − 10 = 20 upward (opt 2) · 10 m/s² upward (opt 3). Each lands on exactly the option it names, and
none is sayable by a student who reached the right answer.

Difficulty order holds in both subtopics: 1 = one use with the numbers given, 2 = one conversion
(72 km/h → 20 m/s) or one rearrangement (u = g × t), 3 = two steps (halve then multiply; v = u − gt
then state a). No banned word appears in any route phrase; the file's single "instead of" is inside
a fix, where rule 3 does not reach. Every route starts with "I ", is ≤ 12 words, carries no
two-or-more-digit number, and differs from the others in its question. `zero_v_nonzero_a_01` picks
t = 1 s while the top is at 1.5 s, which is what keeps its "direction of motion" route on option 3
instead of on the answer.

| item | verdict | note |
|---|---|---|
| throw_up — concept card | weak | All four round-1 points are fixed: line 2 now says the speed falls by 10 m/s each second, the bracket problem is gone because the height formula was removed, line 5 says "above the throw point", and line 6 states "Ignoring air resistance". No sign convention is stated and none is needed — t = u/g uses magnitudes only. What is left over is height: the title still promises it and line 5 still mentions a maximum height, but nothing on the card computes one, no question uses one, and two fixes below still point at "the height formula". Cleanest repair is to put h = u²/(2g) back with a height apply question; otherwise retitle to "Time up and the return speed" and cut the two fix clauses. |
| throw_up — check | ok | |
| lq_p1-02_throw_up_01 | ok | |
| — r | ok | |
| — m0 | ok | |
| — m0 fix | weak | "that factor of two belongs to the height formula" sends the student to a formula the card no longer carries. Restore h = u²/(2g) to the card or cut the clause; the rest ("Redo using only g") is right. |
| — m1 | ok | |
| — m2 | ok | |
| lq_p1-02_throw_up_02 | ok | |
| — r | ok | |
| — m0 | ok | |
| — m1 | ok | |
| — m1 fix | weak | Same dangling reference: "that factor belongs to the height formula, not this one." There is no height formula on the card now. |
| — m2 | ok | |
| lq_p1-02_throw_up_03 | ok | |
| — r | ok | |
| — m0 | ok | |
| — m1 | ok | |
| — m2 | ok | |
| zero_v_nonzero_a — concept card | weak | The sign convention is now correct and plain — "up is positive, and the acceleration is g downward throughout" — so round 1's defect is fixed. But this card never says air resistance is ignored, and its central claims need that: with air resistance the acceleration would not stay 10 m/s² on the way up and v = u − gt would not hold, so both apply answers of 10 m/s² downward depend on an assumption the card does not state. The throw_up card states it (line 6) and both check stems state it. Add one line, for example "Air resistance is ignored, so the acceleration stays g downward for the whole flight." |
| zero_v_nonzero_a — check | ok | |
| lq_p1-02_zero_v_nonzero_a_01 | ok | |
| — r | ok | |
| — m0 | ok | |
| — m1 | weak | The phrase and the option are right: at t = 1 s the ball is still rising, so taking the direction of motion gives 10 m/s² upward = option 3. The type is wrong — reading the acceleration direction off the direction of motion is a wrong belief, so `concept`, not `careless` (misread or sign slip). The same mistake is typed `concept` at zero_v_nonzero_a_03 m2, so the file disagrees with itself. |
| — m2 | ok | |
| lq_p1-02_zero_v_nonzero_a_02 | ok | |
| — r | ok | |
| — m0 | ok | |
| — m1 | ok | |
| — m2 | ok | |
| lq_p1-02_zero_v_nonzero_a_03 | ok | |
| — r | ok | |
| — m0 | ok | |
| — m1 | ok | |
| — m2 | ok | |

### Checks that passed everywhere in round 2

- All six apply answers and both check answers are correct; every m route produces exactly the
  option it names, and no route is honestly sayable by a student who got the question right.
- No route phrase contains "instead of", "rather than", "dropped", "skipped", "left out", "missed"
  or "swapped", nor a verdict word. The replaced routes carry only what the student did.
- Sizes are inside the brief: concept lines 8 and 8 (max 99 and 94 characters), meanings 51 and 104,
  stems 103–162, options ≤ 75, every fix ≤ 240 characters.
- Formulas are Unicode: t = u/g and v = u − gt with the real minus sign U+2212, m/s² with the
  superscript. No LaTeX, no markdown.
- Each question has one r route and one m route per wrong option, no m route points at the answer,
  and the correct option number varies across the six questions (2, 3, 1 and 2, 3, 4).
- Both cards support their questions: throw_up's example now does the doubling that
  throw_up_03 rearranges, and zero_v_nonzero_a's v = u − gt with v = 0 at the top supports the
  rearrangement in zero_v_nonzero_a_02.
- Plain English throughout: round 1's "switch off gravity" metaphors are gone, replaced by "Gravity
  never stops acting" and "Gravity acts from the moment the body is thrown". No readiness claim, no
  "simply", "obviously" or "just" anywhere in the file.
