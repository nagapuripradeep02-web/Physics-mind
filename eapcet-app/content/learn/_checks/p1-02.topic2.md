# Second read — p1-02.topic2.json (Uniform acceleration)

Checked against `_BRIEF_LEARN.md`. Every number re-worked independently (g = 10 m/s²).
Verdicts: ok / weak / wrong / harmful. A blank note means the item is ok.

Totals: **39 ok · 9 weak · 1 wrong · 2 harmful** (51 items).

---

## Subtopic 1 — `three_equations` (The three equations of motion)

| item | verdict | note |
|---|---|---|
| concept card | ok | Example re-worked: v = 5 + 2×3 = 11 m/s. Correct. |
| check | ok | Answer 2 correct; both why_wrong lines true. |
| lq_p1-02_three_equations_01 | ok | v = 4 + 2×5 = 14 m/s = option 2. Correct. |
| — r | ok | |
| — m0 | weak | Route and option are right (a×t = 10 = option 1), but the fix ends "not just a×t". "just" is a banned word (brief rule 1). Use "not a×t alone". |
| — m1 | ok | (4+2)×5 = 30 = option 4. Correct. |
| — m2 | ok | s = 4×5 + ½×2×25 = 45 = option 3. Correct. |
| lq_p1-02_three_equations_02 | ok | s = (256−36)/10 = 22 m = option 3. Correct. |
| — r | ok | |
| — m0 | weak | Option is reachable ((256−36)/5 = 44 m = option 1), but "I left out a factor in the formula" does not name which factor, so a student cannot match it to what they did. Say "I divided by a instead of 2a". |
| — m1 | ok | (16−6)² = 100, 100/10 = 10 m = option 2. Correct. |
| — m2 | **harmful** | "I calculated how long the motion took" is honestly true of a student who reached the right answer: t = (16−6)/5 = 2 s, then s = 6×2 + ½×5×4 = 22 m is a valid solution. The route would mark that correct solver as wrong. It also only reaches option 4 ("2 m") if the time 2 s is then given as the answer. Rewrite as "I reported how long the motion took". |
| lq_p1-02_three_equations_03 | ok | v = 4 + 3×2 = 10 m/s; s = (100−16)/6 = 14 m = option 1. Correct (also 4×2 + ½×3×4 = 14 m). |
| — r | ok | |
| — m0 | weak | Option is reachable (10×2 = 20 m = option 2), but the fix says "Displacement uses average conditions", which is vague. Plain version: displacement = average velocity × time = ((4+10)/2)×2 = 14 m. |
| — m1 | ok | 84/3 = 28 m = option 3. Correct. |
| — m2 | ok | v = 10 = option 4; "I stopped after finding the speed" cannot be said by a correct solver. |

---

## Subtopic 2 — `nth_second` (Distance in the nth second)

| item | verdict | note |
|---|---|---|
| concept card | weak | Example is correct (S₄ = 3 + 4×7/2 = 3 + 14 = 17 m; check: 44 − 27 = 17 m). Line 6, "Sₙ increases by the same amount every second", is only true for positive acceleration; with a body slowing down Sₙ decreases. Correct statement: Sₙ changes by a each second. |
| check | ok | Answer 2 correct; both why_wrong lines true. |
| lq_p1-02_nth_second_01 | ok | S₃ = 2 + 4(5)/2 = 12 m = option 2. Correct. |
| — r | ok | |
| — m0 | **harmful** | "I found the total distance up to that second" is honestly true of a student who reached the right answer by the method the concept card itself teaches (line 4): s₃ = 24 m, s₂ = 12 m, 24 − 12 = 12 m. That student found the total up to the 3rd second and is correct, but this route calls it a concept error. Rewrite as "I reported the total distance up to that second" (the wording already used in `lq_p1-02_nth_second_03` m0). The option itself is reachable: s₃ = 6 + 18 = 24 m = option 3. |
| — m1 | ok | 2 + 4(3)/2 = 8 m = option 1. Correct. |
| — m2 | weak | Option is reachable (u and a swapped: 4 + 2(5)/2 = 9 m = option 4), but "I swapped which value was the acceleration" is written from after the student knows it was wrong; before that they would say "I took the second number as the initial velocity". Type is `application`; mixing up which given is which is a misread, so `careless` fits better. Fix ends "keep them in place", which is loose; say "use 2 as u and 4 as a". |
| lq_p1-02_nth_second_02 | ok | 8 = 2 + a(3)/2 → a = 4 m/s² = option 4. Correct (and consistent: u = 2, a = 4 does give S₂ = 8 m). |
| — r | ok | |
| — m0 | ok | 8 = 2×2 + ½a×4 → a = 2 m/s² = option 1. Correct. |
| — m1 | weak | Option is reachable (6 × 1.5 = 9 = option 2), but the route "…instead of dividing" names the correct operation inside the route phrase, which brief rule 3 forbids. Say "I multiplied the difference by the bracket term". |
| — m2 | ok | n = 1: 8 = 2 + a(1)/2 → a = 12 m/s² = option 3. Correct. |
| lq_p1-02_nth_second_03 | ok | s₄ = 20 + 16 = 36 m, s₃ = 15 + 9 = 24 m, difference = 12 m = option 3. Correct. |
| — r | ok | |
| — m0 | ok | 36 m = option 1. Correct. |
| — m1 | **wrong** | Making this mistake as stated does not give option 2. Leaving the half out of the acceleration term in both totals gives s₄ = 20 + 32 = 52 m and s₃ = 15 + 18 = 33 m, difference **19 m — not an option**. Option 2 ("3 m") needs the half kept for t = 4 s and dropped only for t = 3 s, which is not one mistake. Either change the option to 19 m, or reword the route to name the single subtraction it applies to. |
| — m2 | ok | s₅ = 25 + 25 = 50 m; 50 − 36 = 14 m = option 4. Correct. |

---

## Subtopic 3 — `two_stage_stopping` (Stopping in two parts)

| item | verdict | note |
|---|---|---|
| concept card | weak | Example is correct (64 = 144 + 16a → a = −5 m/s²). But the meaning line says "a = deceleration" while the example and the apply fixes use a = −5 and a = −4, i.e. a is the signed acceleration and the deceleration is the positive 5 or 4. Write "a = acceleration, negative when the body slows down". |
| check | weak | Option 1, "The time taken in that stage", is not actually false: with the start speed and the deceleration fixed, s = ut + ½at² means the time also decides the distance. The why_wrong line only says time is absent from v² = u² + 2as, which does not make option 1 wrong. Replace option 1 with something false, e.g. "The mass of the body". |
| lq_p1-02_two_stage_stopping_01 | ok | v² = 100 − 2×3×16 = 4, v = 2 m/s = option 2. Correct, and the car is still moving at 16 m (stopping distance 100/6 = 16.7 m). |
| — r | ok | |
| — m0 | ok | 100 + 96 = 196, v = 14 m/s = option 1. Correct. |
| — m1 | ok | v² = 4 read as v = option 3. Correct. |
| — m2 | ok | 0 m/s = option 4. Correct. |
| lq_p1-02_two_stage_stopping_02 | ok | a = (64−144)/20 = −4, deceleration 4 m/s² = option 4. Correct. |
| — r | ok | |
| — m0 | ok | −80/10 = −8, so 8 m/s² = option 1. Correct. |
| — m1 | ok | (8−12)² = 16, 16/20 = 0.8 m/s² = option 2. Correct. |
| — m2 | ok | 2s = 40: −80/40 = −2, so 2 m/s² = option 3. Correct. |
| lq_p1-02_two_stage_stopping_03 | ok | Stage 1: a = (256−400)/36 = −4 m/s². Stage 2: s₂ = 256/8 = 32 m = option 1. Correct. |
| — r | ok | |
| — m0 | ok | Scaling with the speed drop: 18 × (16/4) = 72 m = option 2. Correct. |
| — m1 | weak | Option 3 follows only if the 2 is dropped in step 1 alone (a = −144/18 = −8, then s₂ = 256/16 = 16 m). A student who believes the formula is v² = u² + as drops it in both steps and gets a = −8 then s₂ = 256/8 = **32 m — the marked answer** — so the same route describes a student who reached the right answer. Tie the route to the one step, e.g. "I divided by the distance alone to get the deceleration". |
| — m2 | ok | 0 = 400 − 8s₂ → s₂ = 50 m = option 4. Correct. |

---

## Notes across the file

- Structure checks pass: 3 subtopics, 3 apply slots each, difficulty 1→2→3, ids unique and in the
  `lq_p1-02_<subtopic>_<nn>` form, exactly one `r` plus one `m` route per wrong option, no route
  pointing at the marked answer, answer numbers varied (2,3,1 / 2,4,3 / 2,4,1), checks have no
  numbers in the stem and a why_wrong line for every wrong option.
- Plain English (Rule 41): no readiness claims, no personification, no metaphors. One banned word
  found — "just" in `lq_p1-02_three_equations_01` m0's fix. Two loose phrases: "average conditions"
  (`three_equations_03` m0 fix) and "keep them in place" (`nth_second_01` m2 fix).
- The two harmful routes share one cause: the route names a step a correct solver also performs,
  without a word that limits it to the wrong ending. `nth_second_03` m0 ("I **reported** the total
  distance…") shows the wording that avoids this and should be copied to both.

---

## Round 2

Re-read after the rework, against `_BRIEF_LEARN.md` with rule 3 extended (no "instead of" /
"rather than", no hindsight verbs dropped / skipped / left out / missed / swapped in route
phrases). Every number re-worked again from scratch (g = 10 m/s²); every one of the 36 route
phrases re-scanned; every named mistake re-made to confirm it lands on the option it points at.

Totals: **17 ok · 0 weak · 0 wrong · 1 harmful** (18 items).

| item | verdict | note |
|---|---|---|
| `three_equations_01` m0 fix | ok | "just" gone; fix now ends "not a×t alone". Route still gives 2×5 = 10 = option 1. |
| `three_equations_02` m0 | ok | "I divided by the acceleration alone" names the factor: 220/5 = 44 m = option 1. A correct solver divides by 2a, so cannot say it. |
| `three_equations_02` m2 | ok | "I reported how long the motion took" is honest (t = (16−6)/5 = 2 s, given as the answer → option 4 "2 m") and a correct solver reports 22 m, so cannot say it. |
| `three_equations_03` m0 fix | ok | "average conditions" replaced by "average velocity over time"; ((4+10)/2)×2 = 14 m checks out. Route still gives 10×2 = 20 m = option 2. |
| `nth_second` concept card | ok | Line 6 now "Sₙ changes by a fixed amount, a, from one second to the next" — true for negative a as well. Example still correct (S₄ = 3 + 14 = 17 m). |
| `nth_second_01` m0 | ok | "I reported the total distance up to that second" excludes the correct subtraction solver, who finds s₃ = 24 m but reports 12 m. Option 3 = 24 m reachable. |
| `nth_second_01` m2 | ok | "I took the second number as the initial velocity" — no hindsight verb; u = 4, a = 2 gives 4 + 2(5)/2 = 9 m = option 4. Type now `careless`; fix names "use 2 as u and 4 as a". |
| `nth_second_02` m1 | ok | "I multiplied the difference by the bracket term" — the banned "instead of dividing" is gone; 6 × 1.5 = 9 m/s² = option 2, and the fix names the bracket-over-two term. |
| `lq_p1-02_nth_second_03` | ok | Option 2 changed 3 m → 19 m, so re-solved in full: s₄ = 20 + 16 = 36 m, s₃ = 15 + 9 = 24 m, difference 12 m = option 3 (formula check: 5 + 2(7)/2 = 12 m). m0 → 36 m = option 1; m2 → s₅ = 50 m, 50 − 36 = 14 m = option 4. |
| `nth_second_03` m1 | ok | Now consistent: using at² in both totals gives 52 − 33 = 19 m, which is the new option 2. A correct solver keeps the half, so cannot say it. |
| `two_stage_stopping` concept card | ok | Meaning line now "a = acceleration (negative if slowing)", matching the example's a = −5 and the fixes' a = −4. 116 characters, inside the 120 limit. |
| `two_stage_stopping` check | ok | Option 1 replaced with "The mass of the body", which is genuinely false; why_wrong lines for options 1 and 3 both true; answer 2 correct; no numbers in the stem. |
| `two_stage_stopping_02` m2 | **harmful** | "I used twice the distance given for this stage" is what the CORRECT working does — it divides by 2s = 2×10 = 20. Its own fix then says "not using double the distance", contradicting the formula. Option 3 (2 m/s²) needs the distance doubled *before* the 2s step, i.e. −80/40. Reword to name that: "I divided by four times the distance". |
| `two_stage_stopping_03` m1 | ok | Route now tied to one step: a = −144/18 = −8 m/s², then s₂ = 256/16 = 16 m = option 3. The correct step divides by 2s = 36, so "the distance alone" excludes it. |
| route-phrase ban scan (all 36) | ok | No "instead of", "rather than", "dropped", "skipped", "left out", "missed", "swapped", and no verdict word. All start with "I ", all ≤ 12 words, none carries a two-digit number, all distinct within their question, none equals an option or concept line. |
| plain English (Rule 41) sweep | ok | Whole file re-scanned: no "just", "simply", "obviously", "the trick", no readiness claim ("master", "exam ready", "you will score"), no personification or metaphor. |
| concept cards vs apply questions | ok | `three_equations` card carries all three equations and the sign rule (Q1 v = u + at, Q2/Q3 v² = u² + 2as); `nth_second` card carries the formula and the subtraction method the difficulty-3 question is built on (line 4); `two_stage_stopping` card carries v² = u² + 2as, the negative-a rule, and the v²-drop comparison Q3 needs. |
| structure and sizes | ok | Re-checked mechanically: 3 subtopics × 3 slots, difficulty 1→2→3, unique ids, exactly one `r` plus one `m` per wrong option with full option coverage, no route pointing at the marked answer, stems ≤ 300, options ≤ 80, lines ≤ 160, fixes ≤ 240, meaning ≤ 120, formula ≤ 80. |

Notes: the two round-1 harmful routes are fixed — the "I reported …" wording names the ENDING, not
the step, so a correct solver who passes through the same intermediate value cannot claim it, while
the mistake it describes (handing in the intermediate value) is one students really make. The one
remaining harmful route is the mirror image of that failure in a route round 1 passed: it names an
operation that is part of the correct working (2s), so the correct solver can honestly claim it.
