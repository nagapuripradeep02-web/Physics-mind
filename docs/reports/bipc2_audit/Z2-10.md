# Z2-10 — zoology_2, Unit 5 Human Reproduction (Human Reproductive System · Reproductive Health) (part 2/2)

Cards audited: 9 / 9   Findings: HARMFUL 0 · WRONG 9 · WEAK 12

I answered all nine from NCERT Class 12 Ch.3 (Human Reproduction) and Ch.4 (Reproductive Health),
plus Class 11 Ch.22 for inhibin, before opening the steps — Sertoli = nutrition + inhibin (suppresses
FSH) vs Leydig = interstitial + androgens; sperm = head/neck/middle piece/tail with the acrosome
capping the anterior of the nucleus and mitochondria in the middle piece; spermiogenesis (spermatid →
spermatozoon) then spermiation (release from the seminiferous tubule); scrotum + tunica albuginea
inside tunica vaginalis; amniocentesis on foetal cells in amniotic fluid; lactational amenorrhea
(ovulation suppressed during intense lactation, NCERT's six-month limit, no side effects); MTP 1971;
the four population reasons (health care, death rate, MMR, IMR); the three STD measures.
Machine checks (`work/Z2-10/check.py`): mark sums vs `marks_total`, `mark_split` labels vs step
labels, one-boxed-step and `expected_time_min` against the 4/8/15 convention, an SVG path sampler
that computes every stroke's bounding box and arc length and measures each of the seven leader
lines against every drawn structure, an appearances census over the WHOLE 3330-card bank to test the
"more than any other two-mark question" claim, required-term and NCERT-omission scans of every
printed line, a cloned-prose diff of every prose field against the whole bank, a Rule-41
idiom/metaphor sweep, and a mark-scheme-claim sweep. **Every printed line and every boxed answer in
the group is NCERT-correct and all seven figure leaders land on their own structure (worst miss
4.2 px), so nothing is HARMFUL** — the defects are prose, plus one figure proportion that the card
itself forbids.

## HARMFUL — wrong content a student would copy into an exam
| question_id | step_id | what is wrong | what it should say |
| --- | --- | --- | --- |
| — | — | none | — |

## WRONG — incorrect but not exam-costing
| question_id | step_id | what is wrong | what it should say |
| --- | --- | --- | --- |
| ts_ipe_z2_hrs_sperm_diagram | s2_figure (`figure`, against s1_parts `why` + `common_mistakes[0]`) | The card prints "Drawing the head as long as the tail. **The tail is much longer**" and "the tail is **far longer** than the head" — then draws exactly the forbidden figure. Measured from the path data: head vertical extent 118.0 px, drawn tail arc length 120.2 px (vertical span 122.6 px) → **tail : head = 1.02 : 1**, and the head is 34 % of the whole cell. A real sperm is head ~4–5 µm against a tail ~40–50 µm. The student is told one thing and shown another. | Redraw with the tail dominant: on a 444 px canvas, head ≈ 55–60 px, neck ≈ 12 px, middle piece ≈ 45 px, tail ≈ 300 px (tail ≈ 5× the head), keeping the same label set and leader geometry. If the drawing must stay compact, drop the claim instead and print "the tail is the longest region; the diagram is not drawn to scale". |
| ts_ipe_z2_hrs_sperm_diagram | card (`insider_note`) | "one blob labelled 'head' **earns half**." We do not hold the board's mark scheme, and the note tells the student what a printed answer scores. | "Both marks are in the drawing. Draw the acrosome as a separate cap over the front of the nucleus, not one blob labelled 'head', and label the head, plasma membrane, acrosome, nucleus, neck, middle piece, mitochondria and tail." |
| ts_ipe_z2_hrs_spermiogenesis_spermiation | card (`insider_note`) | "The two words **differ by three letters** and are one step apart." Machine-checked: `spermiogenesis` (14 letters) vs `spermiation` (11), Levenshtein distance **8**; they share only the prefix `spermi`, then diverge completely (`ogenesis` vs `ation`). The one memorable fact the note offers is false. | "Both begin with 'spermi', then split: **spermi-o-genesis** is the change of shape, spermatid into spermatozoon; **spermi-ation** is the release of those sperm from the seminiferous tubules. Genesis = making, and the release comes one step later." |
| ts_ipe_z2_rh_amniocentesis | s2_disorders (`why` + `common_mistakes[0]`) | "All three are chromosome disorders, **which is exactly what a test on foetal cells can show**" and "Naming a disease that is **not chromosomal**" [is a mistake]. Amniocentesis tests foetal cells and foetal DNA, so it also detects Mendelian disorders — sickle-cell anaemia, thalassemia, phenylketonuria, cystic fibrosis, haemophilia — every one of which this same bank teaches as a genetic disorder in `ts_ipe_z2_gen_laq_genetic_disorders`. The card's own step 1 says "genetic defects", not "chromosomal defects". So the entry condemns a correct alternative. | `why`: "Foetal cells in the amniotic fluid can be karyotyped and their DNA tested, so both chromosomal disorders and single-gene disorders can be found before birth." `common_mistakes[0]`: "Naming a disease that is not genetic, such as an infection. Chromosomal disorders (Down, Edwards, Turner) and gene disorders (sickle-cell anaemia, thalassemia) are both detectable." |
| ts_ipe_z2_rh_lactational_amenorrhea | s1_conception (`memory_tip`) | "**No periods while breast feeding, so no ovulation.**" The causation is printed backwards, and it contradicts this step's own `why` ("Breast feeding suppresses ovulation, so no ovum is released"). Amenorrhea is the *result* of ovulation being suppressed, not its cause. | "Intense breast feeding suppresses ovulation, so there are no periods and almost no chance of conception." |
| ts_ipe_z2_rh_lactational_amenorrhea | s1_conception (`common_mistakes[0]`) | "Calling it a barrier method. **It works through hormones.**" NCERT lists lactational amenorrhea among the **natural methods** (with periodic abstinence and withdrawal); the "hormonal methods" of that chapter are the pills, injections and implants. As printed, the correction pushes the student into the wrong NCERT category. | "Calling it a barrier method. It is a natural method — intense breast feeding suppresses ovulation, and no device or drug is used." |
| ts_ipe_z2_rh_mtp_legalised | card (`insider_note`) | "The three reasons that follow are the answer, and **each is a separate tick**." The card's own `mark_split` gives the whole of step 2 a single mark, so three separate ticks contradict the card's own claim — and we do not hold the board's mark scheme. | "Expand MTP in the first line — Medical Termination of Pregnancy. Then give the three cases the law allows: unwanted pregnancy after unprotected intercourse, pregnancy from rape, and danger to the mother. All three belong in a complete answer." |
| ts_ipe_z2_rh_prevent_stds | card (`insider_note`) | "a single line about condoms **is marked as half an answer**." Same defect: a claim about what a printed answer scores. | "Four measures, two marks. Give at least three: avoid sex with an unknown partner or multiple partners, always use condoms, see a qualified doctor early, and take the complete course of treatment." |
| ts_ipe_z2_rh_prevent_stds | s1_avoid (`common_mistakes[1]`) | "**Naming a vaccine.** Most STDs have none." Hepatitis-B is on NCERT's own list of sexually transmitted infections and does have a vaccine, so naming vaccination is not an error — it is simply not one of NCERT's three measures. The correction has become a prohibition. | "Answering with vaccination alone. Only hepatitis-B among these infections has a vaccine, so the measures asked for are avoiding risky contact, using condoms, and early detection with complete treatment." |

## WEAK — quality, register, Rule 41
| question_id | step_id | what is wrong | what it should say |
| --- | --- | --- | --- |
| ts_ipe_z2_hrs_sertoli_leydig_cells | s1_sertoli (`lines[3]`) | "which **stops** the production of FSH". NCERT's wording is that inhibin **suppresses/inhibits** FSH synthesis — it does not stop it — and the line never says where FSH comes from, so the feedback loop is left half-drawn in a step that has room for it. | "They also produce a hormone called inhibin, / which suppresses FSH secretion by the / anterior pituitary." |
| ts_ipe_z2_hrs_sperm_diagram | s2_figure (`figure`, against `memory_tip` + `common_mistakes[1]`) | The tip says "**Acrosome caps the nucleus**", but the drawn acrosome ends at y = 119.5 and the nucleus begins at y = 132.0 — a 12.5 px gap — and the nucleus is a 25 px blob, only 21 % of the head's height. NCERT: "an elongated haploid nucleus, the anterior portion of which is covered by a cap-like structure, acrosome." Nothing in the drawing shows the capping relation the card insists on. | Draw the nucleus as an elongated body filling most of the head, with its anterior end running up into the acrosome so the cap sits directly on it (e.g. nucleus from y ≈ 112 to y ≈ 178, acrosome unchanged). |
| ts_ipe_z2_hrs_spermiogenesis_spermiation | s1_spermiogenesis (`memory_tip`) | "spermiation **sets it free**" — figurative register (Rule 41), and vaguer than the word the definition uses. | "Spermiogenesis changes the shape; spermiation is the release from the tubule." |
| ts_ipe_z2_hrs_spermiogenesis_spermiation | s2_spermiation (`why`) | "spermiation is the moment they are **let go**" — same figurative register, applied to the Sertoli cells. | "The developing sperm stay attached to the Sertoli cells until they are mature; spermiation is their release from the seminiferous tubule into its lumen." |
| ts_ipe_z2_rh_amniocentesis | card (`mark_split`) | `mark_split[1].label` is "Two disorders it detects" while the step it names is labelled "Two disorders" — machine-detected; the only split/step label mismatch in the group apart from the diagram card. | Make them identical: "Two disorders". |
| ts_ipe_z2_rh_amniocentesis | s2_disorders (`lines`) | The step's boxed (climax) line is the misuse sentence, "It is misused to learn the sex of the child", while the content the mark row names — the two disorders — is left unboxed above it. Every other card in the group boxes the marked content. | Box the disorders line ("The disorders detected include Down syndrome, Edwards syndrome and Turner's syndrome") and print the misuse sentence as the closing unboxed line. |
| ts_ipe_z2_rh_amniocentesis | s2_disorders (`lines[1]`) | "**Edward's** syndrome" here against "**Edwards** syndrome" in this bank's own `ts_ipe_z2_gen_laq_genetic_disorders` s8_rest. The syndrome is named after John H. Edwards, so the possessive apostrophe is not standard; either way the two cards must not disagree. | "Edwards syndrome" in both cards (see the teacher-gate question on eponym spelling). |
| ts_ipe_z2_rh_lactational_amenorrhea | s2_baby (`common_mistakes[1]`) | "**Leaving out the protection against allergies**" is listed as a mistake, but the mark row it sits under is "It helps the baby 1M" and the same step's first line — better immunity — already answers that row. A complete answer is condemned for missing one optional extra. | "Writing only that breast milk is good food. Say what it does: it gives the baby the mother's antibodies, so the baby has better immunity, and it also protects against allergies." |
| ts_ipe_z2_rh_population_explosion | card (`mark_split`) | The rows read "Better health care 1M · Falling death rates 1M", but the general fall in death rate is printed in **step 1** ("2. Decline in the death rate") while step 2 carries only MMR and IMR — as that step's own `mark_note` ("MMR and IMR") already says. The split labels do not bound the content they name. | "Health care and death rate 1M · MMR and IMR 1M". |
| ts_ipe_z2_rh_population_explosion | card (`insider_note`) | "those two abbreviations are **what the examiner looks for**" — a claim about the board's marking that we do not hold (the same register as the two `insider_note`s filed WRONG above, but here without a scoring claim). | "The answer is about the death rate falling, not the birth rate rising. Write MMR and IMR with their full forms — maternal mortality rate and infant mortality rate." |
| ts_ipe_z2_rh_prevent_stds | s1_avoid (`margin_note`) | "Number the measures. **The examiner ticks each one.**" Same marking claim. | "Number the measures so each one is separate." |
| ts_ipe_z2_rh_prevent_stds | s2_treatment (`why`) | "Many of these infections are **silent** early on, so only a check-up finds them **while they can still be cured**." "Silent" is figurative where "without symptoms" is the plain word, and the curability clause is loose: NCERT says that *except* for hepatitis-B, genital herpes and HIV, these diseases are completely curable if detected early. | "Many of these infections show no symptoms in the early stages, so only a check-up finds them. Most of them are completely curable if they are detected early and treated fully." |

## Teacher-gate questions
- `ts_ipe_z2_hrs_testes_location_coverings`: does the Telangana key expect **two** coverings (tunica vaginalis outside, tunica albuginea inside), or **three** with the tunica vasculosa? The card's `common_mistakes[0]` states flatly "Two are asked", which would read as a prohibition if the key lists three.
- `ts_ipe_z2_rh_amniocentesis`: does the key accept non-chromosomal (Mendelian) disorders — sickle-cell anaemia, thalassemia, haemophilia — as "disorders detected by amniocentesis"? My WRONG finding above assumes it does, since the bank's own genetic-disorders card teaches them as genetic disorders.
- Eponym spelling across the bank: "Edward's" (this card) vs "Edwards" (`ts_ipe_z2_gen_laq_genetic_disorders`); also Down / Down's and Turner / Turner's, where NCERT prints "Down's syndrome", "Turner's syndrome", "Klinefelter's syndrome". Which forms does the key print?
- `ts_ipe_z2_hrs_sertoli_leydig_cells`: NCERT (Class 11, Ch.22) says inhibin "suppresses the FSH synthesis". Confirm the Telangana textbook's verb before the printed line is changed from "stops" to "suppresses".
- `ts_ipe_z2_rh_lactational_amenorrhea`: NCERT gives two advantages the card does not print — "no side effects, as no medicines or devices are used" and the method being reliable only **up to about six months** after parturition. Does the key's two-mark answer require either? (The card's second mark is the benefit to the baby instead.)
- `ts_ipe_z2_rh_mtp_legalised`: does the key require the year **1971** and the first-trimester / twelve-week safety limit in a two-mark answer, and does it count "failure of the contraceptive used" (NCERT) as one of the allowed cases? The card prints three cases and none of these.
- `ts_ipe_z2_rh_population_explosion`: NCERT gives a fourth reason — "increase in number of people in reproducible age". Does the key expect it? The card's `insider_note` currently rules the birth side of the equation out altogether.
- `ts_ipe_z2_hrs_sperm_diagram` has no `appearances` and no `boxed_final` step; 12 of the 13 zoology_2 cards with a diagram step have no boxed step, so I treated that as the bank convention rather than a defect. Confirm.

## Cards checked clean
ts_ipe_z2_hrs_testes_location_coverings

## Notes
- **Frequency claim verified, not assumed.** `ts_ipe_z2_rh_amniocentesis`'s "Asked eight times in printed papers, more than any other two-mark question" is exactly right: its `appearances` array holds 8 entries, it is the only 2-mark zoology_2 card with ≥ 8 (next is 6), and across all 3330 cards in the bank only one other 2-mark card ties it at 8 (`ts_ipe_m2a_rv_binomial_mean_4_variance_3`, a different subject). Left unfiled.
- **Every figure label is on the right structure.** Leader-endpoint distance to its own named stroke: mitochondria 0.0 px, nucleus 1.2, tail 1.7, middle piece 2.3, plasma membrane 2.4, neck 3.1, acrosome 4.2 — and in each case the named structure is the *nearest* stroke of all 17. The `head` bracket (y 62→194) brackets the head (y 66→184) correctly. Draw order is teacher order: head + acrosome + membrane → nucleus → neck + middle piece + mitochondria → tail → labels, in five captioned phases.
- The figure draws the plasma-membrane outline around the head only, while step 1's line says "The whole sperm is covered by a plasma membrane". This matches the usual textbook line diagram (NCERT's own figure labels the membrane at the head), so I did **not** file it — recording it here in case the figure reader sees it differently on the rendered PNG.
- **Structure passed everywhere else**: all nine cards VSAQ · 2 marks · `expected_time_min` 4 · step marks sum to 2 · `mark_split` sums to 2 · exactly one `boxed_final` step (the diagram card excepted, per the convention above). `mark_split` labels match step labels on 7 of 9; the two mismatches are filed (amniocentesis) or explained (the diagram card, whose two split rows are two halves of one 2-mark figure step — legitimate, since the marks are both inside the drawing).
- **Cloned prose census**: four group strings also appear elsewhere in the bank — the Leydig `memory_tip` (also in `ts_ipe_z2_end_androgens`), "Placing Leydig cells inside the tubules…" (also in `ts_ipe_z2_hrs_laq_male_reproductive_system`), "Keep the contrast in the same words." (also in `ts_ipe_z2_abio_primary_vs_secondary_antibodies`) and "Giving only one disorder when two are asked." (also in `ts_ipe_z2_beg_occupational_disorders`). Each is **true where it stands**, so none is filed; only the generic margin note is worth a rewrite on a future pass.
- **Pattern across the group — marking claims in `insider_note`s.** Five of the nine cards make a claim about the board's marking: "earns half", "marked as half an answer", "each is a separate tick", "what the examiner looks for", "the examiner ticks each one". The two that tell a student what a printed answer *scores* are filed WRONG; the three softer ones are filed WEAK. A single sweep over the whole Zoology-II bank for `earn|tick|examiner|half a mark` would clear the class in one pass.
- **Correct physiology confirmed** (not "corrected back"): Sertoli = nutrition + inhibin, Leydig = interstitial + androgens; spermiogenesis before spermiation; tunica vaginalis outside tunica albuginea; amniotic fluid carrying foetal cells; MTP legalised with restrictions and not a contraceptive; health care / death rate / MMR / IMR as the population reasons; the three NCERT STD measures printed as four numbered points. `ts_ipe_z2_rh_amniocentesis`'s boxed misuse line (sex determination) matches NCERT, which records the same misuse and the legal ban.
