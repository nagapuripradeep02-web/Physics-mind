# P2-09 — physics_2, Unit 9 Electromagnetic Induction (part 1/1)

Cards audited: 13 / 13   Findings: HARMFUL 0 · WRONG 4 · WEAK 2

Re-derived every card by hand before reading its steps, then machine-checked with one Python
script (sympy + numeric substitution): the three numericals (`self_inductance_numerical` →
20 μH, `self_inductance_from_emf` → 4 H, `flux_linkage_numerical` → 30 Wb, all match the book);
the coaxial-solenoid mutual-inductance derivation symbolically (M₂₁ = M₁₂ = μ₀N₁N₂A/l, and the
μ₀n₁n₂Al form is algebraically identical); the motional-emf derivation symbolically
(ε = −d(Blx)/dt = −Blv). Both places where the source book is recorded as having dropped the
Lenz's-law minus sign (`faradays_law` s2, `motional_emf` s4) were checked line by line: the card
keeps ε = −N dΦ/dt and ε = −dΦ/dt respectively, in the boxed line and in every surrounding LINE —
the correction is right and consistent everywhere, no HARMFUL findings.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_emi_faraday_henry_experiments | s1_observation | `mark_note`: "Moving magnet, or changing current" reads as either one being enough for the mark. That contradicts the same step's own `margin_note` ("Name both experiments: a moving magnet, and a changing current in a nearby coil") and `common_mistakes[1]` ("Naming only the moving-magnet experiment [is a mistake]") — both say the mark needs BOTH. | `mark_note`: "Both: moving magnet, and changing current" (or drop the "or" so it reads as a two-item list, not an alternative). |
| ts_ipe_p2_emi_self_inductance | s2_emf_unit | `memory_tip`: "One henry = one volt per ampere per second." Read the ordinary English way (the same "per...per" chaining as "metres per second per second" = m/s²), this parses as (V/A)/s = V/(A·s), which is NOT the henry — it differs from the correct V·s/A by a factor of s² (sympy-checked). The card's own `verification.note` avoids the trap with explicit grouping: "1 H = 1 V per (A s⁻¹)". | "One henry = one volt per (ampere per second)." or "One henry = one volt divided by one ampere-per-second." |
| ts_ipe_p2_emi_mutual_inductance_solenoids | s1_figure | `memory_tip`: "draw the long outer rails first, then the **shorter** pair of rails inside." This contradicts the card's own physics: `s2_setup`'s LINE states the two solenoids are "each of length l" (i.e. the SAME length), and this very step's own `margin_note` says "Mark the **common** length l." The figure/geometry itself is out of scope here (being repaired separately), but this memory_tip is prose, and it asserts a different length where the card elsewhere asserts an equal one. | "draw the long outer rails first, then the inner pair of rails between them, same length l." |
| ts_ipe_p2_emi_motional_emf | card (`mark_split`) | Two of the four `mark_split` labels don't match any step's own `label` field: "Setup with the figure" matches neither `s1_figure`'s label ("Figure: rod sliding on rails", 0 marks) nor `s2_setup`'s label ("Setup and the symbols", 1 mark) — it blends a 0-mark step and a 1-mark step into one phrase; "The result ε = Blv" doesn't match `s5_result`'s label ("The result: motional emf"). Every other card in this group (including the structurally identical `mutual_inductance_solenoids`, which also has a 0-mark figure step) has exact label↔label matches throughout. | `mark_split[0].label`: "Setup and the symbols" (matching `s2_setup`); `mark_split[3].label`: "The result: motional emf" (matching `s5_result`). |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_emi_faraday_henry_experiments | s1_observation | `memory_tip`: "move the magnet, or change the current **next door**." "Next door" is a residential metaphor for "in a nearby coil" (Rule 41a). | "move the magnet, or change the current in a nearby coil." |
| ts_ipe_p2_emi_mutual_inductance_solenoids | s2_setup | `memory_tip`: "Same l, same A, turns N₁ and N₂ — **set the stage** in one sentence before any formula." "Set the stage" is a theatrical idiom (Rule 41a). | "Same l, same A, turns N₁ and N₂ — declare all four in one sentence before any formula." |

## Teacher-gate questions

- `ts_ipe_p2_emi_faraday_henry_experiments` `common_mistakes[1]` names the two-coil, changing-current
  experiment specifically as "Henry's ... experiment." NCERT (and this card's own `verification.note`)
  credits Faraday and Henry jointly for electromagnetic induction without splitting which named
  experiment belongs to which physicist, and Henry's best-known individual result is self-induction
  (a single coil), not the two-coil case described here. Low-stakes (it's inside a `common_mistakes`
  clause, not a line the student writes), but a board teacher should confirm whether this specific
  attribution is the source book's own framing before it's trusted.

## Cards checked clean

ts_ipe_p2_emi_magnetic_flux_definition, ts_ipe_p2_emi_faradays_law, ts_ipe_p2_emi_lenzs_law,
ts_ipe_p2_emi_eddy_currents, ts_ipe_p2_emi_inductance_definition,
ts_ipe_p2_emi_self_inductance_numerical, ts_ipe_p2_emi_self_inductance_from_emf,
ts_ipe_p2_emi_flux_linkage_numerical, ts_ipe_p2_emi_eddy_currents_uses

(9 fully clean; the other 4 carry the findings above.)

## Notes

- **The minus sign is right everywhere it appears.** `faradays_law` s2 (ε = −N dΦ/dt) and
  `motional_emf` s4/s5 (ε = −dΦ/dt → ε = −Bl·dx/dt → ε = −Blv, magnitude Blv) both keep the sign in
  the LINE, the boxed answer, the WHY, the MISTAKE warning against dropping it, and the MARGIN_NOTE —
  no card in this unit drops it or misexplains what it means. `lenzs_law` and `self_inductance` /
  `inductance_definition` also state it correctly and consistently wherever it recurs.
- **Two self-documented source-book corrections both check out**: `self_inductance_from_emf`
  (book's data line "di = 0.1S" for what is actually Δt = 0.1 s — the card's L = 4 H is right either
  way, arithmetically, but the card's own relabelling to Δt is correct) and `eddy_currents_uses`
  (book heading "Magnetic breaking trains" → card correctly writes "braking").
- **No `common_mistakes` line condemns correct alternative physics** (the commonest defect the brief
  warns about) — `flux_linkage_numerical`'s "60 V" and `self_inductance_numerical`/`self_inductance_
  from_emf`'s cross-referenced sibling answers (20 μH vs 4 H) are each correctly framed as *answers to
  a different question*, not as wrong arithmetic, and are used as deliberate RECALL `reject` entries
  to catch a student who confuses the two sibling numericals — verified this is intentional pairing,
  not an error.
- **`mark_split` sums and label-pairing were checked on all 13 cards**; only `motional_emf` (above)
  has a labelling mismatch. All `margin_note` "First/Second/Third/Fourth mark" ordinals match their
  step's running mark total on every card.
- The mutual-inductance figure (`mutual_inductance_solenoids`, `coaxial_solenoids`) was NOT
  re-reported per the brief — only its prose (which surfaced the length-mismatch finding above).
  `motional_emf`'s figure (`rod_on_rails`) was in scope and checked clean: closed end, rod position,
  x-dimension line, and the rightward v-arrow all agree with the physics text.
