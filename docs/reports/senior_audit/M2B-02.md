# M2B-02 — mathematics_2b, Unit 1 "Circle (Maths-2B)" (part 2/3)

Cards audited: 19 / 19   Findings: HARMFUL 0 · WRONG 1 · WEAK 0

Every centre, radius, tangent, normal, pole and conjugate-lines condition was re-derived
independently with a sympy script (`verify.py`, one file, run once) — symbolic S₁/polar expansion,
solving the proportionality system for each pole, the conjugate-lines identity `r²(l₁l₂+m₁m₂) =
(l₁g+m₁f−n₁)(l₂g+m₂f−n₂)` for both k-cards, and every boxed radius/centre/parametric pair. All 19
cards' printed lines and boxed answers check out exactly (two apparent script mismatches on Card 1's
±-tangent pair and Card 6's tangent/normal were my own comparison bugs — sign/scale artifacts, not
card errors — confirmed by re-deriving those two by hand). The four field_3d-style diagrams (Cards 1,
2, 6, 7) were checked coordinate-by-coordinate: stroke endpoints were substituted into each line's
equation and each tangent's distance from its circle's centre computed, confirming true tangency,
correct relative position (opposite sides of the centre for the parallel-tangent pair; radius
perpendicular to tangent at the touch point for Card 6; angle bisected by OC for Card 7) — not just
schematically plausible.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_cir_through_minus2_3_centre_origin | s1_radius (`margin_note`) + card (`insider_note`) | The printed `LINE`s of s1 explicitly take the square root and write `r = √13`. But `margin_note` says "Keep r², not r; the equation of the circle needs the square," and `insider_note` says "taking the square root to write r = √13 and then squaring it back is a wasted step that often introduces an error." Both notes describe the card avoiding exactly the step its own printed working performs — the card contradicts itself about its own method. | Either change the printed working to stop at r² = 4 + 9 = 13 (never writing √13), or change the notes to match the printed method, e.g. margin_note: "One mark. r = √13 is written out here for clarity, then squared back to 13 in the next line — both r and r² are acceptable, but going straight to r² = 13 is one line shorter." insider_note similarly should stop calling the shown method "a wasted step" or "an error." |

## WEAK — quality, register, Rule 41

_(none)_

## Teacher-gate questions

None. The conjugate-lines-condition formula `r²(l₁l₂+m₁m₂) = (l₁g+m₁f−n₁)(l₂g+m₂f−n₂)` and the
pole-by-comparison method are standard board-accepted derivations; both k-cards' arithmetic checks
out under it, so there is nothing here needing a curriculum-convention ruling.

## Cards checked clean

ts_ipe_m2b_cir_tangent_at_3_minus1_and_parallel_tangent, ts_ipe_m2b_cir_tangents_parallel_to_x_y_minus8, ts_ipe_m2b_cir_pole_of_x_y_2_wrt_4x_6y_minus12, ts_ipe_m2b_cir_pole_of_3x_4y_minus45_wrt_6x_8y_5, ts_ipe_m2b_cir_conjugate_lines_find_k_kx_3y_minus1, ts_ipe_m2b_cir_tangent_and_normal_at_3_2, ts_ipe_m2b_cir_perpendicular_tangents_from_origin_condition, ts_ipe_m2b_cir_conjugate_lines_find_k_2x_ky_minus8, ts_ipe_m2b_cir_radius_4_find_a, ts_ipe_m2b_cir_radius_6_find_c, ts_ipe_m2b_cir_centre_minus4_minus3_find_g_f_radius, ts_ipe_m2b_cir_centre_2_3_find_g_f_radius, ts_ipe_m2b_cir_find_a_2x2_ay2_and_radius, ts_ipe_m2b_cir_find_a_b_ax2_bxy_3y2, ts_ipe_m2b_cir_parametric_6x_4y_minus12, ts_ipe_m2b_cir_parametric_6x_8y_minus96, ts_ipe_m2b_cir_parametric_centre_3_4_radius_8, ts_ipe_m2b_cir_parametric_4x2_4y2_9

(18 clean; the 1 remaining carries the finding above.)

## Notes

- **Mathematically this group is in excellent shape.** All 19 boxed answers, all intermediate
  algebraic lines, all four figures, and every `common_mistakes` entry were re-derived and none
  condemns mathematically correct alternative working (the commonest defect class named in the
  brief) — every `MISTAKE[i]` across all 19 cards describes a genuine, verifiable error (a real sign
  slip, a real dropped factor, a real swapped ratio, a real "not defined" trap on `tan 90°`), several
  of which I hand-checked reproduce the exact wrong numeric value a student would actually get (e.g.
  Card 5 s3 `MISTAKE[0]`: flipping the sign on n₂ gives exactly `−2 − 2 + 5 = 1`, matching the
  printed text).
- **`RECALL`: 0 / 19** — no card in this group carries a `recall` field (confirmed against the raw
  JSON directly, not just the dump), so check 3's `RECALL` item is N/A for this entire group, same
  as A01 found for `insider_note` in its unit.
- **`INSIDER_NOTE`: 19 / 19** — every card has one; all read true against the card's own steps except
  the one flagged above.
- **Marks:** every card's `mark_split` sums to `marks_total` (4 for the seven SAQ cards, 2 for the
  twelve VSAQ cards); every label and mark count in `mark_split` matches its step's `label` and
  `marks` exactly; every `margin_note`'s ordinal ("One mark" / "Second mark" / "The last N marks")
  matches the step's running-total position. No LAQ (7-mark) cards fall in this group, so the
  7-vs-8-mark note in the brief does not apply here.
- **No card in this group has a diagram whose figure is generic/schematic-only in a way that hides an
  error** — the four diagrammed cards (tangent+parallel-tangent, tangents-parallel-to-a-line, tangent+normal,
  perpendicular-tangents-from-origin) all have stroke coordinates that are numerically consistent
  with their own circle's centre/radius and the claimed tangency/perpendicularity/bisection, not just
  plausible-looking.
- **`expected_time_min`** is a clean 9 for every 4-mark SAQ and 4 for every 2-mark VSAQ — no outliers.
- Minor, not filed as a finding: `ts_ipe_m2b_cir_pole_of_x_y_2_wrt_4x_6y_minus12` s1 `MISTAKE[1]`
  ("Leaving the polar ungrouped, which makes the comparison step much harder") labels a stylistic
  choice rather than a mathematical error as a "mistake" — the ungrouped form is not wrong, just
  harder to use in the next step. Borderline, but it does not condemn the working as incorrect the
  way the brief's target pattern does, so it is left unfiled.
- No Rule-41 idiom/metaphor/personification found anywhere in this group's prose.
