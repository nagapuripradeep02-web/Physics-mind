# M2B-09 — mathematics_2b, Unit 6 "Integration (Maths-2B)" (part 2/2)

Cards audited: 25 / 25   Findings: HARMFUL 0 · WRONG 0 · WEAK 1

All 25 group cards are VSAQ (2 marks each; no LAQ cards here, so the 7-vs-8-mark note does not
apply to this group). Every boxed antiderivative was verified with sympy by differentiating it and
confirming the result equals the printed integrand — 20/25 matched symbolically outright; the other
5 (all `log|f(x)|` forms: partial fractions, `log|logx|`, `log|log(logx)|`, `log|sin(logx)|`, and the
`sec²x·csc²x = sec²x+csc²x` identity) hit sympy's `Abs()`/branch-cut artifacts on symbolic
simplification and were confirmed instead by 5-point numeric evaluation of the derivative against
the integrand (all 5/5 passed, diffs ~1e-15). Every intermediate `LINE` (substitution derivatives,
partial-fraction splits, double-angle/hyperbolic identities, ILATE by-parts choice, the `eˣsinx`
reduction-formula algebra) was hand-traced against the line above it. `mark_split` label/sum ↔ step
label/marks pairing was cross-checked programmatically for all 25 cards (0 mismatches). Structural
grep confirmed 0 `RECALL` fields and 0 `FIGURE` lines in this group (pure computation, no diagrams),
exactly 2 `MISTAKE[i]` entries per step throughout, and uniform `expected_time_min=4` /
`marks_total=2`.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

_(none)_

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_int_vsaq_dx_over_x_plus_1_x_plus_2 | s2_final | `MISTAKE[1]`: "Combining the two logs into log\|(x+1)/(x+2)\| without checking that this is genuinely equal (it is, but only one form should be written, not a mix of both)." The parenthetical itself concedes the combined form is correct, so the entry never names an actual error — it is self-cancelling and gives the student nothing concrete to avoid (the step already has a real mistake in `MISTAKE[0]`, the term-order swap). | Replace with a genuine error, e.g. "Writing log\|(x+2)/(x+1)\| with the ratio inverted", or "Dropping the modulus bars on either log", or drop the entry — `MISTAKE[0]` already covers this step. |

## Teacher-gate questions

none

## Cards checked clean

ts_ipe_m2b_int_vsaq_ex_1_plus_xlogx_over_x, ts_ipe_m2b_int_vsaq_xex_over_x_plus_1_squared, ts_ipe_m2b_int_vsaq_1_over_1_minus_x2_plus_1_over_1_plus_x2, ts_ipe_m2b_int_vsaq_sec2x_csc2x, ts_ipe_m2b_int_vsaq_dx_over_coshx_plus_sinhx, ts_ipe_m2b_int_vsaq_dx_over_1_plus_cosx, ts_ipe_m2b_int_vsaq_1_plus_cos2x_over_1_minus_cos2x, ts_ipe_m2b_int_vsaq_sin_logx_over_x, ts_ipe_m2b_int_vsaq_cot_logx_over_x, ts_ipe_m2b_int_vsaq_dx_over_xlogx, ts_ipe_m2b_int_vsaq_dx_over_xlogx_loglogx, ts_ipe_m2b_int_vsaq_log_1_plus_x_over_1_plus_x, ts_ipe_m2b_int_vsaq_logx_squared_over_x, ts_ipe_m2b_int_vsaq_sin_tan_inv_x_over_1_plus_x2, ts_ipe_m2b_int_vsaq_1_over_sqrt_sin_inv_x_sqrt_1_minus_x2, ts_ipe_m2b_int_vsaq_ex_1_plus_x_over_cos2_xex, ts_ipe_m2b_int_vsaq_ex_sin_ex, ts_ipe_m2b_int_vsaq_cos3x_sinx, ts_ipe_m2b_int_vsaq_sin4x_over_cos6x, ts_ipe_m2b_int_vsaq_x8_over_1_plus_x18, ts_ipe_m2b_int_vsaq_dx_over_1_plus_ex, ts_ipe_m2b_int_vsaq_logx_over_x2, ts_ipe_m2b_int_vsaq_ex_sinx, ts_ipe_m2b_int_vsaq_cos_sqrt_x

(24 clean; the 25th carries the finding above.)

## Notes

- **Mathematically this group is clean.** 25/25 boxed antiderivatives re-derived independently (via
  differentiation, the correct check for indefinite-integral cards) with zero sign errors, zero
  wrong constant, zero wrong power, zero mis-picked standard form. Alternate-form identities cited
  in the prose were also independently verified: `Tanh⁻¹x = ½log|(1+x)/(1−x)|`,
  `cscx − cotx = tan(x/2)`, the `1/[(x+a)(x+b)] = [1/(x+a) − 1/(x+b)]/(b−a)` shortcut (a=1,b=2),
  `cosh²x − sinh²x = 1`, and the non-obvious `sec²x·csc²x ≡ sec²x + csc²x` identity underlying the
  Card 5 split — all confirmed true.
- **No `MISTAKE` entry condemns mathematically correct working as wrong anywhere in this group** —
  the check the brief flags as the bank's commonest defect. The one WEAK finding above is the
  opposite failure mode: a `MISTAKE` entry that explicitly ADMITS the alternative form is correct
  and therefore fails to identify any error at all (still filed, since it gives the student no real
  guidance — see brief's "vague or unhelpful tips" WEAK criterion).
- **No false or cloned prose found.** Every `INSIDER_NOTE` (25/25, one per card) checked true against
  its own card, including sequencing claims that are independently verifiable from `VERIFICATION_NOTE`
  answer numbers (e.g. Card 11's "third question in a row using t=logx" — answers 149.1, 149.2, 150
  are indeed three consecutive `t=logx` substitutions). `MEMORY_TIP` and `WHY` fields were checked
  card-by-card; repeated tips (e.g. "∫sint dt = −cost" on Cards 9/15/18) are true every time they
  recur, not false clones.
- **Rule 41:** no idioms of the "by heart"/"nail it" class found. A few borderline colloquial verbs
  ("hides the pairing", "vanish into dt", "not a fresh power to fear") are standard, non-personifying
  math-pedagogy phrasing (consistent with the A01 precedent of not filing ordinary English as Rule 41),
  so none filed.
- **Structure:** `mark_split` label/sum ↔ step label/marks pairing verified programmatically for all
  25 cards — 100% match, `marks_total=2` throughout, `expected_time_min=4` uniform across the group
  (no LAQ cards, so the 7-vs-8-mark reform note is not applicable here). No `FIGURE` or `RECALL`
  fields in this group (pure computation VSAQs) — nothing to check under checks 5/RECALL.
- Scratch scripts: `verify.py` (symbolic sympy differentiation, 25 cards), `verify_numeric.py`
  (5-point numeric fallback for the 5 `log|·|`-form cases), `check_marks.py` (mark_split/step
  structural cross-check) — all under
  `...\98cbf4ad-9e7c-46d3-834e-139ecd3b20af\scratchpad\M2B-09\`.
