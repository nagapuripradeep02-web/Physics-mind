# M2B-11 — mathematics_2b, Unit 7 Definite Integrals (part 2/2)

Cards audited: 23 / 23   Findings: HARMFUL 1 · WRONG 8 · WEAK 2

Every integral was evaluated independently with one sympy script (numeric + symbolic, including a
parametrised `a`) and cross-checked against a from-scratch implementation of Walli's/the mixed-power
formula, run against sympy's own `integrate()` for ten (m,n) pairs — all matched. Antiderivatives were
verified by differentiation (`sqrt(3-2x)`, `x-atan(x)`, the `x²/2-x+log(1+x)` split, the `(1/a)atan(x/a)`
and `√(a²-x²)` standard forms, `2sin(θ/2)`). All 23 boxed final answers are correct; every substitution's
limit transform (θ→θ/2, x→y=tanθ, x→u=3-2x) is applied correctly; every sign-critical step (cos(θ/2)≥0
on [0,π], the modulus split points, the (−1)ⁿ evaluations, the −I / 2I algebra) checks out. All defects
found are in prose (mistakes/memory tips/margin notes/an insider note), not in the printed working.

## HARMFUL — wrong content a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_di_vsaq_sin4xcos6x_0_to_2pi | card (insider_note) | Claims "a chain of separate fractions (3/10)(1/8)(5/6)(3/4)(1/2)(π/2) — both give the same 3π/128" as the card's own boxed answer. Computed exactly: (3/10)(1/8)(5/6)(3/4)(1/2)(π/2) = **3π/512**, not 3π/128 (3π/512 is only the *inner* 0-to-π/2 Walli's value, before the ×4 range-reduction the card's own s1/s2 steps correctly apply). The insider note directly contradicts the card's own WHY and boxed final line. | "…via a chain of separate fractions (3/10)(1/8)(5/6)(3/4)(1/2)(π/2) for the inner 0-to-π/2 integral, then multiplied by the range-reduction factor 4 (3π/512 × 4) — both give the same 3π/128." |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_di_vsaq_x2_over_1plusx2_0_to_1 | s1_split | `common_mistakes[0]`: "Attempting a substitution instead of the simpler add-and-subtract trick." Substitution (e.g. x=tanφ) is a fully valid, correct method for ∫x²/(1+x²)dx — just longer. Listing a correct alternative method under "mistakes" wrongly tells a student who used it that their working was an error. | Replace with a genuine error, e.g. "Trying to integrate x²/(1+x²) directly as a single unfamiliar fraction, without adding and subtracting 1 in the numerator first." |
| ts_ipe_m2b_di_vsaq_x2_over_1plusx_0_to_4 | s1_split | `common_mistakes[0]`: "Trying long division instead of the faster add-and-subtract-1 trick." Long division of x² by (1+x) is a valid, correct method — same defect as above. | "Leaving x²/(1+x) as an unfamiliar improper fraction without adding and subtracting 1 to expose the (x−1)(x+1) factor." |
| ts_ipe_m2b_di_vsaq_x2_over_1plusx_0_to_4 | s2_final | `common_mistakes[1]`: "An arithmetic slip in 16/2−4, e.g. getting 4 instead of the correct value." 16/2−4 = 8−4 = **4 is the correct value** — it is exactly what the step's own line computes and what the step's own `memory_tip` ("16/2 − 4 = 8 − 4 = 4") confirms one line later. The mistake entry contradicts the card's own working. | "An arithmetic slip in 16/2−4, e.g. computing it as 6 or 12 instead of the correct value 4." |
| ts_ipe_m2b_di_vsaq_sin2xcos4x_0_to_2pi | s1_reduce | `common_mistakes[0]`: "Re-deriving from scratch instead of using the reduction ∫₀^(2π) = 4∫₀^(π/2) that both even powers allow." Re-deriving directly (e.g. via power-reduction identities) is a valid, correct method, just longer — same defect as the two entries above. | "Assuming the ×4 range reduction applies without separately checking BOTH f(π−x)=f(x) and f(2π−x)=f(x)." |
| ts_ipe_m2b_di_vsaq_sin3xcos3x_0_to_pi | s2_final | `common_mistakes[1]`: "Subtracting I from both sides instead of adding it, which loses the information." False — from I = −I, subtracting I from both sides gives 0 = −2I, i.e. I = 0, the identical correct conclusion the card reaches by adding I to both sides. Nothing is "lost." | "Dividing both sides of I = −I by I to get 1 = −1 and wrongly concluding a contradiction, instead of adding (or subtracting) I to isolate I = 0 directly." |
| ts_ipe_m2b_di_vsaq_xsin7xcos6x_0_to_pi | s2_final | `memory_tip`: "The denominator product for m+n=13 has SEVEN descending terms, one more than the six-term products seen earlier in this chapter." No card anywhere in this VSAQ/Star-Q+ Definite-Integrals set (this group or the sibling VSAQ files) has a six-term denominator product — the largest earlier ones are five-term (cos¹¹x: 11·9·7·5·3; the m+n=10 cards: 10·8·6·4·2). The comparison is false. | "The denominator product for m+n=13 has SEVEN descending terms — the longest Walli's-formula product in this chapter." |
| ts_ipe_m2b_di_saq_area_y_eq_x3plus3_neg1_to_2 | s3_final | `margin_note`: "Last two marks. …" but the step header is `marks=1 (running 1→2)` — this step earns exactly ONE mark (the second/last of the two), not two. | "Second mark. Subtracting a negative value at x=−1 turns the subtraction into an addition." |
| ts_ipe_m2b_di_saq_area_y_eq_x2_neg1_to_2 | s3_final | `margin_note`: "Last two marks. …" — same mismatch: `marks=1 (running 1→2)`, one mark, not two. | "Second mark. (−1)³ = −1 — an odd power keeps the negative sign, so subtracting it adds 1/3." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_di_saq_area_y_eq_x3plus3_neg1_to_2 | card (mark_split) | `mark_split`'s second label is "Evaluate and simplify" but the scored step it maps to (`s3_final`) carries `label="Apply the limits and simplify"` — the two do not match (checked programmatically against all 23 cards; this pair is the only mismatch in the group). | Set `mark_split[1].label` to "Apply the limits and simplify" (or rename the step label to "Evaluate and simplify") so the two agree. |
| ts_ipe_m2b_di_saq_area_y_eq_x2_neg1_to_2 | card (mark_split) | Same mismatch: `mark_split` says "Evaluate and simplify", step `s3_final.label` says "Apply the limits and simplify". | Same fix as above. |

## Teacher-gate questions

None. No board-convention ambiguities were found in this group (all cards are single-method evaluations
or standard-formula applications; the two AREA cards' VSAQ/2-mark re-classification is already resolved
and explained in their own `verification.note`, and is provenance, not something this audit grades).

## Cards checked clean

ts_ipe_m2b_di_vsaq_sqrt_2plus2costheta_0_to_pi, ts_ipe_m2b_di_vsaq_dx_over_x2_plusa2_0_to_a, ts_ipe_m2b_di_vsaq_sqrt_a2_minusx2_0_to_a, ts_ipe_m2b_di_vsaq_abs_1_minusx_0_to_2, ts_ipe_m2b_di_vsaq_abs_2_minusx_0_to_4, ts_ipe_m2b_di_vsaq_sin4x_0_to_pi2, ts_ipe_m2b_di_vsaq_cos11x_0_to_pi2, ts_ipe_m2b_di_vsaq_practice_sin7x_0_to_pi2, ts_ipe_m2b_di_vsaq_sin5xcos4x_0_to_pi2, ts_ipe_m2b_di_vsaq_practice_cos7xsin2x_0_to_pi2, ts_ipe_m2b_di_vsaq_sin6xcos4x_0_to_pi2, ts_ipe_m2b_di_vsaq_sin2xcos4x_neg_pi2_to_pi2, ts_ipe_m2b_di_vsaq_dx_over_sqrt_3_minus2x_0_to_1, ts_ipe_m2b_di_vsaq_sec4theta_0_to_pi4, ts_ipe_m2b_di_vsaq_sin2thetacos7theta_neg_pi2_to_pi2

(15 clean; the other 8 carry the findings above.)

## Notes

- **The book-error correction on `practice_cos7xsin2x_0_to_pi2` is genuinely right.** Re-derived
  independently two ways (Walli's mixed formula and the substitution u=sinx, ∫₀¹u²(1−u²)³du =
  1/3−3/5+3/7−1/9 = 16/315): the printed book value 8/315 for this Practice Question really is wrong
  (it is answer 176's value copied verbatim), and the card's correction to 16/315 is correct. Its sibling
  `sin5xcos4x_0_to_pi2`'s insider note about the same book error is consistent and correct.
- **`sin2thetacos7theta_neg_pi2_to_pi2` (32/315) cross-checks exactly against
  `practice_cos7xsin2x_0_to_pi2` (16/315)** — the former is 2× the latter, as required (doubling a
  −π/2..π/2 even-function integral over the same integrand's 0..π/2 half-range). `sin2xcos4x_0_to_2pi`
  (π/8) is likewise exactly 2× `sin2xcos4x_neg_pi2_to_pi2` (π/16), matching the "0-to-2π companion" claim
  in the former's own insider note.
- **`ts_ipe_m2b_di_saq_area_y_eq_x3plus3…` / `…y_eq_x2…` s1's "Setup" mark_note (vs. every other card's
  "Method") and their 0-mark `s2_antideriv` step are deliberate, not defects** — the 0-mark step's own
  margin_note correctly says "No mark of its own" in both cards, and mark_split still sums to 2 = marks_total.
- **No `insider_note`, `WHY`, or `MEMORY_TIP` elsewhere quotes a number wrong for its own card** — every
  other numeric aside (Tan⁻¹1=π/4, Sin⁻¹1=π/2, the 3840/10395→256/693 and 45/3840→3/256 reductions, the
  −π/2..π/2 vs 0..2π factor-of-2/4 comparisons) was independently recomputed and is correct.
- **No `RECALL` field and no `FIGURE` lines exist anywhere in this group** (confirmed against the raw
  JSON schema for all 23 cards, not just the dump) — nothing to audit under those two checks, matching
  this unit's convention (no ASR ally ambiguity, no diagrams for pure evaluation VSAQs).
- **No Rule 41 idiom/metaphor found** — "worth memorising," "always know," "simply," "straightforward"
  etc. are literal register, not idiom or personification.
- Two entries flagged similarly worded but NOT filed as findings, for contrast: `saq_area_y_eq_x3plus3…`
  s1 `common_mistakes[0]` ("Splitting the integral at x=0 out of habit, which is **unnecessary** here…")
  and the `…y_eq_x2…` sibling's equivalent — these honestly label the alternative as *unnecessary extra
  work*, not as incorrect, so they don't misrepresent a valid method the way the three filed WRONG entries
  above do.
