# M2B-12 — mathematics_2b, Unit 8 Differential Equations (part 1/2)

Cards audited: 18 / 18   Findings: HARMFUL 0 · WRONG 1 · WEAK 1

Every one of the 18 ODEs was solved independently, then every boxed relation was machine-verified
by one sympy script two ways: implicit differentiation of the boxed relation (arbitrary constant
fully isolated on one side, so no algebra is hidden inside a residual) checked against the ODE's own
dy/dx for the 12 homogeneous/reducible/separable/substitution cards, and `sympy.dsolve()` (plus a
direct substitution-and-differentiate check for the one case dsolve would not close, card 16) for the
6 linear cards. Both initial-value cards were also checked at their given point (1, π/4). Every
`MARK_SPLIT` label/mark pairing and every step-mark sum was checked against `marks_total` (7 for the
ten LAQ cards, 4 for the eight SAQ cards — this is the pre-reform 7-mark Section-C paper, not the
2026-27 8-mark reform, as the brief flags). All 18 `insider_note`s were checked against their own
card's boxed answer and steps — all 18 are true here (a sibling group flagged a HARMFUL
`insider_note` contradicting its own card; none found in this group).

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_de_curve_xsin2_yx_dx_ydx_minus_xdy | s5_apply_point | `common_mistakes[1]`: "Writing the final answer with log\|x\| when the working already used log x for a positive x = 1 point, which is consistent but should not be swapped mid-answer." This condemns a mathematically correct — in fact more general — alternative: log\|x\| is the true antiderivative of ∫dx/x for all x≠0, and at x=1>0 it equals log x exactly, so writing log\|x\| is not an error. The entry admits "which is consistent" in the same breath as calling it a mistake to avoid. | Drop the entry, or replace it with a genuine error for this step, e.g. "Substituting x=1, y=π/4 into the un-rearranged line cot v = log x + c − (a stray term) instead of the stated family (2)," or simply reuse a different genuine slip (this step already carries one real mistake, MISTAKE[0], substituting into equation (1) instead of (2)). |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_de_linear_y_2y_over_x_eq_2x2 | card (insider_note) | "…a good check that the answer **is on track** is that ∫(2/x)dx = 2 log x = log x² collapses to a power of x…" — "on track" is a metaphor (Rule 41a), not literal textbook English. | "…a good check that the working is correct is that ∫(2/x)dx = 2 log x = log x² collapses to a power of x, not a fraction or a log left standing." |

## Teacher-gate questions

The task prompt's flagged reviewed-exception card, `ts_ipe_m2b_di_vsaq_xsin7xcos6x_0_to_pi` (carrying
the phrase "the most involved VSAQ in the whole Definite Integrals chapter"), is **not a member of
this group**. M2B-12 is Unit 8 "Differential Equations (Maths-2B)"; that id's `_di_` infix and its own
phrase both name the Definite Integrals unit, a different chapter/group. No wording change is
proposed here, and the cross-reference gate's exception list needs no update from this report.

## Cards checked clean

ts_ipe_m2b_de_homogeneous_x2y2_dx_2xydy, ts_ipe_m2b_de_homogeneous_x2_minus_y2_dx_xydy, ts_ipe_m2b_de_curve_gradient_yx_minus_cos2_yx, ts_ipe_m2b_de_homogeneous_x2y_minus_2xy2_dx, ts_ipe_m2b_de_homogeneous_x3_minus_3xy2_dx, ts_ipe_m2b_de_homogeneous_y2_minus_2xy_dx, ts_ipe_m2b_de_homogeneous_y2_minus_2xy_over_x2_minus_xy, ts_ipe_m2b_de_reducible_2x_y_1_4x_2y_minus1, ts_ipe_m2b_de_reducible_x_minus_y_plus3_over_2x_minus2y_plus5, ts_ipe_m2b_de_separable_xy2_plus_x_dx, ts_ipe_m2b_de_substitution_y_minus_x_tan, ts_ipe_m2b_de_linear_y_tanx_eq_cos3x, ts_ipe_m2b_de_linear_y_tanx_eq_sinx, ts_ipe_m2b_de_linear_cosx_dy_ysinx_sec2x, ts_ipe_m2b_de_linear_y_secx_eq_tanx, ts_ipe_m2b_de_linear_1plusx2_dy_y_etaninvx

(16 clean; the other 2 carry the findings above.)

## Notes

- **Both prior corrections in this group re-verified independently and hold.**
  `ts_ipe_m2b_de_homogeneous_x3_minus_3xy2_dx` (the C=−2, D=0 partial-fraction swap) and
  `ts_ipe_m2b_de_homogeneous_y2_minus_2xy_over_x2_minus_xy` (the x³→x⁶ power-clearing fix) both now
  check out exactly: solving each card's own partial-fraction system by hand reproduces its stated
  A/B/C/D, and sympy's implicit-differentiation residual against the original ODE is identically 0
  for both boxed relations. Nothing further to flag on either.
- **Sign discipline (the brief's named failure mode) held across all 18 cards.** Every homogeneous
  substitution, every "flip the sign to separate," and every partial-fraction sign was independently
  re-derived (by hand for the two corrected cards, by sympy residual for all twelve non-linear cards)
  and matched the printed line every time. No dropped leading minus found.
- **`ts_ipe_m2b_de_reducible_x_minus_y_plus3_over_2x_minus2y_plus5`'s `VERIFICATION_NOTE`** (provenance
  metadata, not a reader-facing field the chatbot speaks — `verification` is a separate top-level JSON
  key from `insider_note`) contains a raw, unedited self-correction mid-sentence: "…which answers
  39.1, 40.1, 41.1 and 44.2 — **wait**, 44.2 IS one of those four…". Not filed as a graded finding
  since it is outside the six audited reader-facing fields, but it is a genuine editorial artifact
  (a scratch reasoning trace left in a shipped field) worth a cleanup pass.
- **The constant-relabelling convention** (c → 2c → c, c → c⁴ → c, c → c⁶ → c, etc., used in cards
  x2_minus_y2_dx_xydy, x2y_minus_2xy2_dx, y2_minus_2xy_over_x2_minus_xy, reducible_2x_y_1,
  linear_y_tanx_eq_cos3x, linear_y_2y_over_x_eq_2x2) is mathematically valid — an arbitrary constant
  raised to a fixed power, or scaled by a fixed factor, is still an arbitrary constant — and is
  self-documented in a `common_mistakes` entry on the affected step every time it appears. Not a
  defect anywhere it occurs.
- **No `recall` field exists in this chapter's card schema at all** (checked the raw JSON:
  `mark_split`, `verification`, `insider_note`, and per-step `why`/`margin_note`/`memory_tip`/
  `common_mistakes` are the only prose-bearing keys). Check 3's RECALL sub-item is N/A for this whole
  group, not merely absent by chance.
- **No `FIGURE` lines anywhere in the group** — differential-equation proofs carry no diagram in this
  bank. Check 5 (figures) is N/A for this group.
- Every `MARK_SPLIT` label matches its step's `label` verbatim and every card's marks sum to
  `marks_total` (7 or 4); every `expected_time_min` is 15 on the ten 7-mark LAQ cards and 9 on the
  eight 4-mark SAQ cards, with no outlier.
