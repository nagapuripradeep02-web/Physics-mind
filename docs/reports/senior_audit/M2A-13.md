# M2A-13 — mathematics_2a, Unit 10 Random Variables and Probability Distributions (part 1/1)

Cards audited: 14 / 14   Findings: HARMFUL 0 · WRONG 1 · WEAK 0

Every distribution, mean, variance and probability in the group was recomputed independently with
one Python script (fractions + sympy, exact rational/symbolic arithmetic — no floats except a
sanity-check float series sum). All 14 boxed final answers match exactly: k = 1/15, μ = 11/3,
σ² = 14/9 · k = 0.1, μ = 0.8, σ² = 2.16 · k = 1/10 (root of 10k²+9k−1=0, k=−1 rejected), μ = 3.66,
P(0<X<5) = 4/5 · die μ = 7/2, σ² = 35/12 · two-dice μ = 7 (count pattern 1,2,3,4,5,6,5,4,3,2,1
verified to sum to 36) · cubic roots {1,2,1/3} with c=1,2 shown to give P(X=2)=4,9 (invalid), c=1/3
giving valid probabilities 1/9,2/9,2/3 summing to 1, P(0<X<3)=8/9, P(1<X≤2)=2/3, P(X<1)=1/9 ·
c = logₑ2 from the exponential series, P(0<X<3) = logₑ2+(logₑ2)²/2 · c = 1/4 from Σ(k+1)xᵏ =
(1−x)⁻², P(1<X<3) = 3/16 · Poisson λ = 2, P(X=5) = 4e⁻²/15 · four binomial cards (n,p,q) = (16,
1/4,3/4), (9,2/3,1/3), (10,1/10,9/10), (7,2/5,3/5), (6,2/5,3/5) all satisfy np/npq exactly, and
every quoted probability (P(X≥1)=1−(3/4)¹⁶; P(X=0)=1/3⁹, P(X=1)=2/3⁷; P(X=1)=(9/10)⁹; P(X=3)=
4536/15625; P(1<X≤4)=2268/3125) reproduces bit-for-bit. Every `mark_split` entry sums to 7 (LAQ) or
2 (VSAQ) and its labels/running totals (0→…→marks_total) match the step labels and marks exactly,
card by card. No `FIGURE` lines exist in this group (a probability/statistics chapter — check 5 is
N/A). No Rule-41 idiom/metaphor/personification found (grepped the group for the flagged terms).

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_rv_left_handed_one_in_ten | s2_required_probability | `MISTAKE[2]`: "Answering 1 − (9/10)¹⁰, which is 'at least one'." lists this as a flat mistake. But the same card's own `INSIDER_NOTE` says: "The English also allows 'at least one', which would be 1 − (9/10)¹⁰ and a very different number — write which reading you are taking." The card contradicts itself — one field calls the "at least one" computation wrong outright, the other treats it as a legitimate alternate reading of genuinely ambiguous English ("there is one who is left-handed") that only needs to be stated, not avoided. 1 − (9/10)¹⁰ is itself correct arithmetic for that reading. | Reword `MISTAKE[2]` to match the `INSIDER_NOTE`'s own position, e.g. "Answering 1 − (9/10)¹⁰ ('at least one') without stating that reading — this book's convention reads the question as EXACTLY one, so state which reading you use rather than switching silently." Do not present a self-consistent alternate computation as a bare error. |

## WEAK — quality, register, Rule 41

_(none)_

## Teacher-gate questions

- `ts_ipe_m2a_rv_left_handed_one_in_ten`: the stem "there is one who is left-handed" is genuinely
  ambiguous between "exactly one" and "at least one" in ordinary English; the source book and this
  card both adopt "exactly one" by convention. A Telangana IPE teacher should confirm this is the
  board's expected reading (and hence the WRONG-finding fix above, which keeps the "state your
  reading" framing rather than flatly rejecting the "at least one" reading).

## Cards checked clean

ts_ipe_m2a_rv_dist_table_1_to_5_k_mean_variance, ts_ipe_m2a_rv_dist_table_minus2_to_3_k_variance,
ts_ipe_m2a_rv_dist_table_0_to_7_k_mean_p_0_to_5, ts_ipe_m2a_rv_die_mean_variance,
ts_ipe_m2a_rv_two_dice_sum_distribution_mean, ts_ipe_m2a_rv_cubic_in_c_range_0_1_2,
ts_ipe_m2a_rv_c_power_k_by_k_factorial, ts_ipe_m2a_rv_k_plus_1_c_by_2_power_k,
ts_ipe_m2a_rv_poisson_p1_eq_p2_find_p5, ts_ipe_m2a_rv_binomial_mean_4_variance_3,
ts_ipe_m2a_rv_binomial_mean_6_variance_2_first_two_terms, ts_ipe_m2a_rv_rain_three_days_in_a_week,
ts_ipe_m2a_rv_binomial_mean_2_4_variance_1_44

(13 clean; the 14th carries the one WRONG finding above.)

## Notes

- **Mathematically this group is in excellent shape.** All 14 boxed answers, every intermediate
  distribution table (probabilities checked to sum to 1 on every card that gives a full table:
  cards 1–3, 6), and every binomial/Poisson parameter derivation reproduce exactly under
  independent script recomputation.
- **Binomial/Poisson conditions match what the stems grant**: cards 9 (Poisson) and 14 give the
  distribution directly; cards 10–13 model "n independent trials with a fixed success probability"
  from word problems (a die-throw week, 10 random people, a week of rain days) — this is the
  book's own standard textbook convention for this question type, not a bank-authored assumption,
  so it is not filed as an error.
- **No cross-card cloning contamination found** despite five binomial VSAQs (174–178) sharing step
  ids `s1_find_n_p_q`/`s2_required_probability` and three distribution-table LAQs (41.1/41.2/42)
  sharing `s1_find_k`/`s2_mean`/`s3_variance`/`s3_prob`: every `WHY`, `MEMORY_TIP` and `MARGIN_NOTE`
  in these families was checked and is specific to its own card's numbers (n, p, q, and the
  resulting powers/coefficients differ correctly card to card).
- **`MARK_SPLIT` structure is clean across all 14 cards**: every label matches its step's `label`
  word-for-word, every running total chains 0→…→marks_total without gaps, and every sum is 7 (LAQ)
  or 2 (VSAQ) as `marks_total` requires.
- **`INSIDER_NOTE` is true on all 14 cards** — checked each against its own card's steps and boxed
  answer; the only issue found is the `MISTAKE`-vs-`INSIDER_NOTE` tension on the left-handed card
  above (the `INSIDER_NOTE` itself is correct).
- `VERIFICATION_NOTE`s carry the "mark split is ours, not the book's" disclaimer honestly on 12 of
  14 cards, and the one card where the book does print `[N Marks]` tags
  (`ts_ipe_m2a_rv_cubic_in_c_range_0_1_2`) was checked against those book tags and matches (1+1+1+2+1+1=7).
