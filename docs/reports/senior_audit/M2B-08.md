# M2B-08 — mathematics_2b, Unit 6 Integration (Maths-2B) (part 1/2)

Cards audited: 26 / 26   Findings: HARMFUL 0 · WRONG 5 · WEAK 2

Every one of the 26 boxed final answers was independently re-derived and checked by differentiating
the printed antiderivative and comparing it to the printed integrand at 3–4 points each, in 40-digit
precision (`mpmath`, one script, `scratchpad/M2B-08/verify.py`) — all 26 PASS, including the two
cards the authoring pass itself corrected (`3x_minus_2_sqrt_2x2_minus_x_plus_1`,
`6x_plus_5_sqrt_6_minus_2x2_plus_x`: both re-derived by hand from `3x−2 = A·Dr′+B` and
`6x+5 = A·Dr′+B` respectively, matching the card's corrected coefficients exactly). I also ran the
BOOK's original printed (uncorrected) forms through the same differentiation check as a negative
control on four cards — the sin⁴x/csc⁵x dropped-minus claims and both sqrt-times-linear-factor
coefficient claims (`5/32` not `5√2/16`; `13√2/16` not `13√2/32`) all independently reconfirmed as
real book errors, correctly fixed by the card. `MARK_SPLIT` sums to 7 (LAQ) / 2 (VSAQ) on every card,
every label matches its step, and running totals are internally consistent throughout.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_int_laq_reduction_cosn_hence_cos4 | s4_evaluate | `memory_tip`: "sin⁴x and cos⁴x are structural twins — same coefficients (1/4, 3/8, 3/8), opposite name, same sign (all +)." This claims the two answers share "same sign (all +)". They do not: sin⁴x's boxed answer is `−sin³x cosx/4 − 3/8 sinx cosx + 3x/8 + c` (two negative terms), cos⁴x's is all positive. | "sin⁴x and cos⁴x are structural twins — same coefficients (1/4, 3/8, 3/8) — but the SIGNS differ: sin⁴x's answer keeps two negative terms, cos⁴x's is all positive." |
| ts_ipe_m2b_int_laq_reduction_cscn_hence_csc5 | s2_apply | `memory_tip`: "Copy secⁿx's working, then put a minus sign in front of every single term — that is the whole difference." False: comparing the two boxed equations two lines above (`I_n = sec^{n-2}x tanx − (n−2)(I_n−I_{n−2})` vs `I_n = −csc^{n-2}x cotx − (n−2)(I_n−I_{n−2})`), only the FIRST term's sign flips — the `−(n−2)(I_n−I_{n−2})` part is identical in both. This card contradicts itself two steps later: s3_reduction's own `why`/`memory_tip` correctly says only "the first term keeps the minus sign". | "Copy secⁿx's working, then put a minus sign in front of ONLY the leading power term (−cosecⁿ⁻²x cotx instead of +secⁿ⁻²x tanx) — the −(n−2)(I_n−I_{n−2}) part stays exactly the same." |
| ts_ipe_m2b_int_laq_2sinx_plus_3cosx_plus_4_over_3sinx_plus_4cosx_plus_5 | s3_weierstrass | `common_mistakes[1]`: "Sign slip differentiating −1/(t+3) into 1/(t+3)² instead of −1/(t+3)²." This is backwards. d/dt[−1/(t+3)] = +1/(t+3)² (verified numerically), which is exactly why the card's own line above it, `∫2dt/(t+3)² = −2/(t+3)+c`, is correct. The mistake field labels the correct derivative as wrong and asserts a genuinely wrong one as correct. | "Believing d/dt[−1/(t+3)] = −1/(t+3)² — it is actually +1/(t+3)², which is exactly why ∫2dt/(t+3)² integrates to −2/(t+3)+c, not +2/(t+3)+c." |
| ts_ipe_m2b_int_laq_cosx_plus_3sinx_plus_7_over_cosx_plus_sinx_plus_1 | s4_final | `common_mistakes[1]` and `memory_tip` both claim the two logs "cannot be [combined] since their arguments are different" / "never combine into a single log term." This reasoning is false: a·logP + b·logQ = log(Qᵇ/Pᵃ) regardless of differing arguments — logs combine via the product/quotient rule irrespective of what's inside them. The real reason this answer stays as two logs is that combining gives nothing simpler (coefficients −1 and 5 share no common base), not that it's mathematically blocked. | `common_mistakes[1]`: "Believing these two logs cannot combine because their arguments differ — they CAN combine via a·logP+b·logQ=log(Qᵇ/Pᵃ); they are left separate here only because combining does not simplify anything." `memory_tip`: "Two logs appear in this answer, one in x, one in tan(x/2) — they are left separate only because combining would not simplify anything, not because they cannot combine." |
| ts_ipe_m2b_int_laq_x_plus_1_over_x2_plus_3x_plus_12 | s3_square | `common_mistakes[0]`: "Not simplifying −39/4 to a clean form before matching it to a²." No such quantity as −39/4 ever appears in this card's correct derivation — the leftover constant is always +39/4 (confirmed by the card's own next line and by `common_mistakes[1]` on the same step, which correctly says the value is "39/4, not −39/4 or 9/4"). This reads as prose cloned from a card whose completed-square constant genuinely comes out negative (e.g. this group's own `dx_over_4x2_minus_4x_minus_7`), pasted in unmodified. | "Using a = 39/4 instead of a = √39/2 — a is the square root of the leftover constant, not the constant itself." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_int_laq_dx_over_x2_plus_x_plus_1 | s3_formula | `common_mistakes[0]` prints "the log-form standard integral **∩**dx/(x²−a²)" — a corrupted symbol (intersection ∩, not the integral sign ∫). Confirmed present in the raw JSON, not a dump artifact. | "Confusing this with the log-form standard integral **∫**dx/(x²−a²), which is used for a DIFFERENCE of squares, not a sum." |
| ts_ipe_m2b_int_laq_cosx_plus_3sinx_plus_7_over_cosx_plus_sinx_plus_1 | s1_setup | `common_mistakes[1]`: "Sign slip solving A+B=1 and −A+B=3 together (adding gives 2B=4, so B=2, then A=1−B=−1)." This doesn't describe an error — it just narrates the correct method in parentheses without saying what a wrong result would look like. | "Subtracting instead of adding the two equations, e.g. (A+B)−(−A+B)=2A instead of 2B, isolating the wrong variable first." |

## Teacher-gate questions

Four cards in this group (`dx_over_x2_plus_x_plus_1`, `practice_dx_over_3x2_plus_x_plus_1`,
`dx_over_4x2_minus_4x_minus_7`, `practice_dx_over_5_minus_2x2_plus_4x`) sit under the book's own
printed sub-heading "INTEGRATION-II **[to be deleted]**" (book p.29) — the book's own
syllabus-deletion marker, already flagged inside each card's `verification_note`. A board teacher
needs to confirm whether this completing-the-square-standard-form sub-section is still examinable
under the 2026-27 syllabus before these four cards are assigned to students. This is not a math or
prose defect — it is already surfaced by the cards themselves, and I'm not re-filing it as a finding,
just relaying it as unresolved.

## Cards checked clean

ts_ipe_m2b_int_laq_reduction_sinn_hence_sin4, ts_ipe_m2b_int_laq_reduction_tann_hence_tan5_tan6, ts_ipe_m2b_int_laq_reduction_cotn_hence_cot4, ts_ipe_m2b_int_laq_reduction_secn_hence_sec5, ts_ipe_m2b_int_laq_practice_dx_over_3x2_plus_x_plus_1, ts_ipe_m2b_int_laq_dx_over_4x2_minus_4x_minus_7, ts_ipe_m2b_int_laq_practice_dx_over_5_minus_2x2_plus_4x, ts_ipe_m2b_int_laq_dx_over_4cosx_plus_3sinx, ts_ipe_m2b_int_laq_dx_over_1_plus_sinx_plus_cosx, ts_ipe_m2b_int_laq_dx_over_3cosx_plus_4sinx_plus_6, ts_ipe_m2b_int_laq_9cosx_minus_sinx_over_4sinx_plus_5cosx, ts_ipe_m2b_int_laq_2cosx_plus_3sinx_over_4cosx_plus_5sinx, ts_ipe_m2b_int_laq_2x_plus_5_over_sqrt_x2_minus_2x_plus_10, ts_ipe_m2b_int_laq_3x_minus_2_sqrt_2x2_minus_x_plus_1, ts_ipe_m2b_int_laq_6x_plus_5_sqrt_6_minus_2x2_plus_x, ts_ipe_m2b_int_laq_sqrt_5_minus_x_over_x_minus_2, ts_ipe_m2b_int_vsaq_ex_sinx_plus_cosx, ts_ipe_m2b_int_vsaq_ex_secx_plus_secx_tanx, ts_ipe_m2b_int_vsaq_ex_tanx_plus_log_secx, ts_ipe_m2b_int_vsaq_ex_1_plus_tan2x_plus_tanx

(20 clean; the other 6 carry the findings above — `cosx_plus_3sinx_plus_7_over_cosx_plus_sinx_plus_1`
carries both a WRONG and a WEAK finding on two different steps.)

## Notes

- **`ts_ipe_m2b_di_vsaq_xsin7xcos6x_0_to_pi` is NOT a card in this group** (it belongs to Definite
  Integrals, not this Unit-6 Integration part). I did not touch it, propose no wording for it, and
  have nothing to add to its reviewed cross-reference-gate exception.
- **Mathematically this group is in very good shape.** All 26 boxed answers re-derived and
  differentiation-checked independently; every reduction-formula chain (sin⁴, cos⁴, tan⁵/tan⁶, cot⁴,
  sec⁵, csc⁵) re-walked step by step and matches. The five WRONG findings are all in explanatory
  prose (`common_mistakes`, `memory_tip`) — none are in a `LINE` a student would copy onto an answer
  sheet, and none touch a boxed final answer.
- **The two already-corrected cards are genuinely fixed.** `3x_minus_2_sqrt_2x2_minus_x_plus_1`
  (coefficient 5/32, not the book's 5√2/16) and `6x_plus_5_sqrt_6_minus_2x2_plus_x` (coefficient
  13√2/16, not the book's 13√2/32) — I re-derived both independently from `A·Dr′+B` and the standard
  `∫√(u²±a²)du` result, matching the cards' corrected coefficients and confirming the book's
  originals fail to differentiate back to the integrand.
- **The book's dropped-minus-sign claims on `reduction_sinn_hence_sin4` and `reduction_cscn_hence_csc5`
  are independently reconfirmed real.** Differentiating the book's printed (unsigned) final lines
  does not return sin⁴x / cosec⁵x; the cards' signed forms do.
- **No `MARK_SPLIT`/step-label/running-total mismatch found anywhere in the group** — every label
  pairs correctly, every sum is 7 (LAQ) or 2 (VSAQ), and no `MARGIN_NOTE` claims a specific numbered
  mark it doesn't earn (this group's margin notes are generic step-type tags — Setup/Method/Result/
  Solution/Standard result/Identify — never numbered mark claims).
- **No `RECALL` field exists anywhere in this group's dump** (grep-confirmed) — nothing to check
  under that part of the method.
- **Rule 41 register is clean** — no idioms, no personification of forces/equations found in any
  reader-facing field. Two borderline informal phrases ("closes the loop," "structural twins") read
  as ordinary technical shorthand rather than idiom/metaphor and were not filed.
- Minor, not filed as findings: `reduction_tann_hence_tan5_tan6` s1_setup `common_mistakes[0]`
  frames integration-by-parts as merely "the slow route" for tanⁿx, when in fact the natural IBP
  split (`u=tan^{n-1}x, dv=tanx dx`) introduces a `log|secx|` inside the remaining integral and does
  not yield a usable reduction at all, not just a slow one — too soft a claim to be confident is
  "false" rather than loosely worded, so left unfiled per the brief's caution against shaky findings.
