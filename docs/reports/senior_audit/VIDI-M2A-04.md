# Vidi audit — slice 4 of 11 — Maths-2A (TS IPE second year)

Slice file: `C:\Tutor\physics-mind\.answerbook_logs\audit_r1_ts_ipe_m2a.slice-04.md`
Subject: Maths-2A. LAQ = 7 marks (75-mark pre-reform paper, per SUBJECT note).
24 question cards graded, all 10 templates present on every card (marks, whystep, remember,
explain, mistakes, important, skiplast, why, outofbank, telugu) = 240 replies. Every reply graded;
none sampled or skipped.

Cards covered: chapter *De Moivre's Theorem* (22 cards: `dm_alpha_n_beta_n_x2_2x_4`,
`dm_cos_sum_zero_cos_3alpha`, `dm_cos_sum_zero_cos_squares_3_2`, `dm_one_plus_cos_theta_n`,
`dm_one_plus_i_2n_cos_n_pi_2`, `dm_one_plus_i_n_cos_n_pi_4`, `dm_p_iq_nth_root_sum`,
`dm_sin_pi_8_power_8_3`, `dm_vsaq_cis_abc_triangle_xyz`, `dm_vsaq_cis_pi_6_fifth_power_i`,
`dm_vsaq_omega_2_minus_omega_49`, `dm_vsaq_omega_a3_plus_b3`, `dm_vsaq_omega_cube_minus_8`,
`dm_vsaq_omega_product_9`, `dm_vsaq_omega_reciprocal_sum`, `dm_vsaq_omega_sixth_powers_128`,
`dm_vsaq_one_minus_i_8`, `dm_vsaq_one_plus_i_16`, `dm_vsaq_x6_plus_1_by_x6`,
`dm_x_minus_1_cubed_plus_8`, `dm_x_m_y_n_cos_sin_identity`, `dm_z_2n_i_tan_n_theta`) and chapter
*Measures of Dispersion* (2 cards: `md_laq_md_mean_step_dev_0_50`, `md_laq_md_mean_step_dev_0_70`).

Every model-answer identity, mark split (checked to sum to 7 on every LAQ card and 2 on every
VSAQ card), and worked arithmetic step was independently re-derived and verified correct across
all 24 cards. Every Vidi reply was checked line-by-line against its card's ANSWER FACTS block —
mark values, step ids, exam years, star ratings, and every quoted number/identity all matched the
source exactly, with no invented content, no misattributed marks, and no hallucinated facts.

## 1. Per-template table

| Template | Mean | n=0 | n=1 | n=2 | n=3 |
|---|---|---|---|---|---|
| marks | 3.000 | 0 | 0 | 0 | 24 |
| whystep | 3.000 | 0 | 0 | 0 | 24 |
| remember | 3.000 | 0 | 0 | 0 | 24 |
| explain | 3.000 | 0 | 0 | 0 | 24 |
| mistakes | 3.000 | 0 | 0 | 0 | 24 |
| important | 3.000 | 0 | 0 | 0 | 24 |
| skiplast | 3.000 | 0 | 0 | 0 | 24 |
| why | 3.000 | 0 | 0 | 0 | 24 |
| outofbank | 3.000 | 0 | 0 | 0 | 24 |
| telugu | 3.000 | 0 | 0 | 0 | 24 |

## 2. Overall mean

**Overall mean: 3.000** across **240** replies. **Guard replies excluded from the mean: 0** — no
"I could not answer just now" / "Give me a short moment" replies appeared anywhere in this slice.

## 3. Replies scored 0 or 1

None. Every reply in this slice was graded 3 (precise, correct and grounded in the ANSWER FACTS).

## 4. Four explicit counts

- **WRONG-STEP** (whystep replies explaining a different step than the one named, or misattributing
  that step's marks): **0 of 24**. Every `whystep` reply addressed exactly the step id/description
  named in the ask and cited that step's own mark value correctly.
- **SCOPE-CREEP** (outofbank replies that decline correctly but then volunteer steps/formulas/marks
  of the off-bank question): **0 of 24**. Every `outofbank` reply declined the unrelated
  "integration by parts" question cleanly, offered no content about it, and only bare-offered to
  help with the open question.
- **LITERAL-MARKDOWN** (`**`, leading `- ` bullets, `#` headings, or backticks in any reply):
  **0 of 240**. No reply anywhere in the slice contained markdown syntax; all replies are plain
  prose (the `>` marks are the transcript's own quoting convention, not Vidi output).
- **TRUNCATED** (any reply ending mid-sentence or mid-formula): **0 of 240**. Every reply in the
  slice ends on a complete sentence/formula.

## 5. Mechanical flags on replies judged wrong

No reply in this slice was judged wrong, so no flag falls under a "wrong reply" by the letter of
the rubric. However 3 flags fired in this slice, and in all 3 cases the flagged reply is itself
correct and appropriately scoped for the ask — each is a false positive worth reporting per the
rubric's own callout ("a flag that fires on a correct reply is itself a finding"):

1. `ts_ipe_m2a_dm_p_iq_nth_root_sum` — `[why]` reply — flag `OVER_BUDGET(165w/150)`. Content is
   accurate, matches the WHY fields for all four steps including the p>0 caveat, and the length is
   appropriate for an open-ended "explain the idea, not just the steps" ask. Scored 3.
2. `ts_ipe_m2a_dm_sin_pi_8_power_8_3` — `[why]` reply — flag `IDIOM:the whole trick`. "The whole
   trick is..." is casual phrasing but not a wrong or misleading claim; content is fully accurate.
   Scored 3.
3. `ts_ipe_m2a_md_laq_md_mean_step_dev_0_50` — `[why]` reply — flag `OVER_BUDGET(152w/150)`.
   Content is accurate and appropriately thorough for the "why" ask. Scored 3.

All three flags fired on `[why]` replies that ran a few words past a fixed length budget while
staying accurate and on-topic — the 150-word budget looks tuned tight for this open-ended template.

## 6. Cards whose ANSWER FACTS are themselves wrong, self-contradictory, or ambiguous

**One finding**, in `ts_ipe_m2a_dm_alpha_n_beta_n_x2_2x_4`:

> `Asked: TS 2016 · AP 2019, 2017`
> `INSIDER POINT: Five printed mark tags and a four-times-asked question. The one mark most students drop is the first: the roots must be written as 1 ± i√3 before anything else.`

The `Asked` line lists exactly three exam appearances (TS 2016, AP 2019, AP 2017), but the
INSIDER POINT claims the question is "four-times-asked" — a direct self-contradiction within the
same ANSWER FACTS block. ("Five printed mark tags" is correct — the MARK SPLIT does list five
tagged steps — only the exam-appearance count is wrong.) No Vidi reply in this card propagated the
error: the `[important]` reply correctly states "It did appear... TS 2016 and AP 2019, 2017"
without repeating the "four times" claim, so this defect did not poison any graded reply, but it
would poison a future reply that trusted the INSIDER POINT literally.

No other bank defect was found across the remaining 23 cards — every other INSIDER POINT count
claim (e.g. "Five printed year tags" in `dm_cos_sum_zero_cos_squares_3_2`, "Four of the seven
marks..." in `dm_x_m_y_n_cos_sin_identity` and `md_laq_md_mean_step_dev_0_50`) was independently
verified correct against its own MARK SPLIT/Asked line, and every worked identity/arithmetic chain
in every model answer was independently re-derived and found correct.
