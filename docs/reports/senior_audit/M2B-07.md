# M2B-07 — mathematics_2b, Unit 5 Hyperbola (Maths-2B) (part 1/1)

Cards audited: 16 / 16   Findings: HARMFUL 0 · WRONG 2 · WEAK 2

Every quantity in every card was re-derived independently with sympy (`verify.py`): e, foci,
directrices and latus rectum for the three centre/eccentricity/foci/LR cards (incl. the shifted-centre
and the y²-positive a↔b-swap case); the tangency-condition discriminant for both tangent-parallel/
perpendicular cards and the find-k card (cross-checked by direct substitution into the hyperbola,
not just the formula); the director-circle and angle-sum loci by extracting the quadratic-in-m
polynomial coefficients symbolically and taking product/sum of roots; the conjugate-eccentricity
identity and its numeric instance; the asymptote-angle formula and the exact surd sec15° = √6−√2;
the rectangular-hyperbola e = √2; the product-of-perpendiculars formula (and independently verified
it at the vertex (4,0)); and the parametric normal line at θ = π/3. All 16 boxed answers are correct;
this unit has no LAQ/7-mark cards (SAQ=4 marks, VSAQ=2 marks only, per the book's own blueprint), so
the 7-mark note does not apply here.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_hyp_conjugate_eccentricity_given_5_by_4 | s1_setup | `MISTAKE[0]`: "Squaring 5/4 incorrectly as 5/16 or 25/16 with the wrong denominator." — 25/16 **is** the correct value of (5/4)² (it is the card's own implicit intermediate en route to 1/e² = 1/(25/16) = 16/25, which the printed LINE computes). The item condemns correct arithmetic as if it were an error. | Replace with a genuine slip, e.g. "Squaring 5/4 as 25/4 (forgetting to square the denominator) instead of the correct 25/16." |
| ts_ipe_m2b_hyp_product_perpendiculars_to_asymptotes | s1_setup | `MISTAKE[1]`: "Trying to substitute a specific point (x₁,y₁) instead of using the ready-made formula a²b²/(a²+b²)." — this condemns a valid alternative method: the vertex (4,0) lies on x²/16−y²/9=1, and substituting it directly into the two perpendicular-distance formulas gives (12/5)(12/5) = 144/25 — the same correct answer, confirmed with sympy. Specific-point substitution is not an error on this card. | Replace with a genuine slip, e.g. "Computing a² + b² = 25 and stopping there instead of forming the ratio a²b²/(a² + b²)." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_hyp_tangents_parallel_perp_line_x_plus2y_x2_minus4y2_4 | card (insider_note) | "This question is a **trap** by design" — "trap" is a metaphor/idiom (Rule 41a), not literal textbook English, in text the chatbot speaks first to the student. | "This question is built around a special case: the given line x + 2y = 0 is exactly one of the hyperbola's own asymptotes, so a tangent 'parallel' to it does not really exist — write the examiner-marked mechanical result down for the marks, but be ready to explain in words that it is the asymptote itself, not a true tangent." |
| ts_ipe_m2b_hyp_conjugate_eccentricity_given_5_by_4 | s2_solve | `MEMORY_TIP`: "e₁ = 5/3 is bigger than e = 5/4, since the conjugate here is the 'steeper' branch shape." — "steeper branch shape" is a vague, undefined descriptor (not a standard property of a hyperbola branch) and is not needed to justify e₁ > e, which the reciprocal identity in s1 already guarantees. | Drop the "steeper" clause, e.g. "e₁ = 5/3 is bigger than e = 5/4 — both correctly exceed 1, as the reciprocal identity 1/e² + 1/e₁² = 1 requires." |

## Teacher-gate questions

None. The unit's a↔b labelling convention (a² is always the x²-term's denominator and b² always the
y²-term's denominator, regardless of which axis is transverse — so formulas swap to use b in place of
a whenever the y-term is positive) is applied 100% consistently across every card that needs it
(`_16y2_minus9x2_144`, `_conjugate_eccentricity_*`, `_shifted_centre`) and matches the source book's
own printed values, so it is not an ambiguity to route to a teacher — just an authored choice worth
knowing about if cross-checking against a book that instead always calls the transverse semi-axis "a".

## Cards checked clean

ts_ipe_m2b_hyp_centre_ecc_foci_lr_directrices_x2_minus4y2_4, ts_ipe_m2b_hyp_centre_ecc_foci_lr_directrices_16y2_minus9x2_144, ts_ipe_m2b_hyp_tangents_parallel_perp_line_y_eq_x_minus7_3x2_minus4y2_12, ts_ipe_m2b_hyp_director_circle_perpendicular_tangents, ts_ipe_m2b_hyp_locus_2xy_k_x2_minus_a2_tan_theta_sum, ts_ipe_m2b_hyp_conjugate_eccentricity_reciprocal_identity, ts_ipe_m2b_hyp_angle_between_asymptotes_general, ts_ipe_m2b_hyp_eccentricity_given_asymptote_angle_30deg, ts_ipe_m2b_hyp_rectangular_hyperbola_define_eccentricity, ts_ipe_m2b_hyp_equation_given_foci_and_transverse_axis_length, ts_ipe_m2b_hyp_tangent_find_k_3x_minus4y_plus_k_x2_minus4y2_5, ts_ipe_m2b_hyp_normal_at_theta_pi_by_3_3x2_minus4y2_12, ts_ipe_m2b_hyp_centre_ecc_foci_lr_directrices_shifted_centre

(13 clean; the other 3 carry the findings above — `_conjugate_eccentricity_given_5_by_4` carries both
a WRONG and a WEAK finding.)

## Notes

- **`ts_ipe_m2b_hyp_tangents_parallel_perp_line_x_plus2y_x2_minus4y2_4` (card 3) is a genuinely
  degenerate question, correctly handled.** The given line x + 2y = 0 IS one of the hyperbola's
  asymptotes (slope −1/2 = −b/a); the tangent-with-slope-m formula's root vanishes there
  (a²m²−b² = 4·¼−1 = 0), so no real tangent parallel to it exists — the printed boxed line
  reproduces the source book's mechanically-derived (but degenerate) result, and the card's own
  `why`/`common_mistakes`/`insider_note` all correctly flag it as not a genuine tangent. This
  matches the card's own `VERIFICATION_NOTE` FINDING, already corrected — not a new defect.
- **Every `mark_split` sums to `marks_total`, every entry's label matches its step's label, and
  every 0-mark step (the standard-form setup steps in the four centre/ecc/foci/LR cards, and the
  diagram step in the director-circle card) correctly carries "No mark(s) of its own" rather than
  claiming one.** `expected_time_min` is internally consistent: 9 for every 4-mark SAQ, 4 for every
  2-mark VSAQ.
- **No `RECALL` field appears anywhere in this group's dump** — nothing to audit under that check
  for M2B-07.
- **No prose was found cloned from a sibling card that is false here.** The near-identical sibling
  pairs (78.1/78.2 centre-ecc-foci-LR; 79.1/79.2 tangent-parallel/perp; 80/81 director-
  circle/angle-sum locus) each carry their own distinct `why`/`mistake`/`memory_tip` text tailored
  to their own numbers and axis orientation — checked line by line, none misapplies a sibling's
  values.
- The figure on `ts_ipe_m2b_hyp_director_circle_perpendicular_tangents` was checked for geometric
  sense: the two tangent strokes from the labelled point are numerically near-perpendicular (dot
  product ≈ 0 relative to their lengths), and the drawn circle (radius 57 px) is smaller than the
  vertex-to-centre distance (≈70 px) on both branches, consistent with the "director circle sits
  inside the hyperbola's own vertices" claim in the card's own prose.
