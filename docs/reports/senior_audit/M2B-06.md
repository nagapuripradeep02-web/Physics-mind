# M2B-06 — mathematics_2b, Unit 4 Ellipse (Maths-2B) (part 1/1)

Cards audited: 21 / 21   Findings: HARMFUL 1 · WRONG 3 · WEAK 2

Every card's chain of algebra was re-derived independently with a single sympy script
(`verify.py`/`verify2.py` in the scratch dir): all four completing-the-square identities (cards
1–4), the tangency discriminant condition for every tangent line in cards 5–6 and 21, the
foci/latus-rectum/directrix reverse-engineering quadratics in cards 7–9 (including the rejected
root in each), the two full focus-directrix conic expansions in cards 10–11 (matched term-by-term
against the printed intermediate lines), the two-point substitution in card 12, both S₁=0
tangent/normal pairs in cards 13–14 (confirming the given points lie on their ellipses first), and
the three general proofs (director circle, auxiliary circle, `lx+my+n` tangent condition, `CM·CN`
identity, and the `e⁴+e²=1` / equilateral-triangle identities in cards 17–20) symbolically. Every
boxed final answer matches. Mark-split sums, label pairing and running-total margin-note claims
("First mark" … "Fourth mark") were checked mechanically for all 21 cards — 100% consistent, no
structural defects. `RECALL` is not a field in this unit's schema (confirmed against the raw JSON)
so check 3's `RECALL` bullet does not apply here.

## HARMFUL — wrong content a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_ell_ecc_foci_lr_directrices_shifted_center_2_neg1 | card (insider_note) | Says "the foci, directrices **and latus rectum** all shift with the new centre" — grouping the latus-rectum LENGTH with the two position quantities. This directly contradicts the card's own step `s4_latus_rectum`, whose `why` says "The latus rectum is a length, and lengths are unchanged by shifting the centre — only positions move." Length of latus rectum (2b²/a = 9/2) is unaffected by the centre shift, exactly like eccentricity — grouping it with foci/directrices primes the reader toward `s4`'s own listed error ("Trying to add the centre coordinates into a length calculation"). Spoken first to the student, before the correcting text in `s4`. | "Completing the square turns this into the standard ellipse x²/16 + y²/9 = 1 moved to the centre (2, −1) — so the eccentricity and the length of the latus rectum come straight from a = 4, b = 3 and need no re-deriving; only the foci and directrices need the centre (2, −1) added in, since those are positions, not lengths." |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_ell_ecc_foci_lr_directrices_9x2_16y2_144 | s5_directrices, `common_mistakes[1]` | "Writing both directrices as one line with a single ± sign in front of 16, which does not correctly pair a sign with each line." Checked: `√7x ± 16 = 0` and `√7x ∓ 16 = 0` are algebraically the identical pair of lines `{√7x−16=0, √7x+16=0}` — there is only one ± symbol in the expression, so there is nothing to "mis-pair" with. This condemns a standard, correct compact notation as an error. (The same false claim, verbatim in structure, is cloned into `ts_ipe_m2b_ell_ecc_foci_directrices_vertical_4x2_y2` s4 below.) | "Leaving the directrix equations un-rationalised as x = ±16/√7 instead of clearing the √7 from the denominator to reach √7x − 16 = 0 and √7x + 16 = 0." |
| ts_ipe_m2b_ell_ecc_foci_directrices_vertical_4x2_y2 | s4_directrices, `common_mistakes[1]` | Same false claim, cloned: "Combining both directrices onto one line with a single ± sign in front of 4, which does not pair a sign correctly with each line." Same check applies — `√3y ± 4 = 0`-style compact forms are not in error. | "Leaving the directrix equations un-rationalised as y = −1 ± 4/√3 instead of clearing the √3 from the denominator to reach √3y + √3 − 4 = 0 and √3y + √3 + 4 = 0." |
| ts_ipe_m2b_ell_normal_latus_rectum_minor_axis_e4_e2 | s3_simplify, `common_mistakes[1]` | "Forgetting to divide both sides by a at all — the a on the left must stay to match a²e² on the right." No division occurs anywhere in this step (`ab = a² − b² = a² − a²(1−e²) ⇒ ab = a²e²` is pure substitution); this item describes an operation that cannot happen here. It duplicates/misplaces the genuine "divide by a vs a²" issue that `s4_conclude`'s `common_mistakes[0]` already correctly owns one step later. | "Writing the substitution as b² = a²(1 + e²) instead of a²(1 − e²), flipping the sign inside the bracket before it is even expanded." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_ell_ecc_foci_directrices_vertical_4x2_y2 | s2_eccentricity, `memory_tip` | "When the y-denominator is bigger, use b² − a² over b² — the bigger axis is always on the bottom." Omits "under one square root" — without it the tip literally reads as e = (b²−a²)/b² (no root), which is numerically wrong (3/4 instead of √3/2). Its sibling card's identical-concept tip does include the qualifier. | Match the sibling: "When the y-denominator is bigger, use b² − a² over b², all under one square root — the bigger axis is always on the bottom." (see `ts_ipe_m2b_ell_ecc_foci_vertical_3x2_y2` s2's tip, which already says this correctly) |
| ts_ipe_m2b_ell_ecc_foci_vertical_3x2_y2 | card (insider_note) | "…so most of the four marks are actually earned in the completing-the-square step…" — the mark_split gives that step 2 of the 4 marks, exactly half, not a majority. | "…so half of the four marks are actually earned in the completing-the-square step alone — get the centre (1, 1) right and the rest follows quickly." |

## Teacher-gate questions

None. This is a self-contained Maths-2B unit with no board-dialect or convention ambiguity — every
mark-split is the bank's own invented (`OURS`) apportionment (recorded honestly in each
`verification.note`), and its internal consistency was checked and holds on all 21 cards.

## Cards checked clean

ts_ipe_m2b_ell_tangents_parallel_perp_45deg_2x2_y2_8, ts_ipe_m2b_ell_tangents_equal_intercepts_9x2_16y2_144, ts_ipe_m2b_ell_standard_form_foci_dist_2_lr_15by2, ts_ipe_m2b_ell_standard_form_foci_dist_8_directrix_dist_32, ts_ipe_m2b_ell_standard_form_lr_4_foci_dist_4root2, ts_ipe_m2b_ell_focus_1_neg1_e_2by3_directrix_xyplus2, ts_ipe_m2b_ell_focus_1_2_e_2by3_directrix_oblique, ts_ipe_m2b_ell_standard_form_through_neg2_2_and_3_neg1, ts_ipe_m2b_ell_tangent_normal_at_neg1_2, ts_ipe_m2b_ell_tangent_normal_end_latus_rectum, ts_ipe_m2b_ell_director_circle_perpendicular_tangents, ts_ipe_m2b_ell_auxiliary_circle_foot_of_perpendicular, ts_ipe_m2b_ell_condition_line_tangent_lmx_n, ts_ipe_m2b_ell_tangent_intercepts_cm_cn_identity, ts_ipe_m2b_ell_stb_equilateral_triangle_eccentricity, ts_ipe_m2b_ell_tangent_condition_find_k

(16 clean; the other 5 carry the findings above — `ts_ipe_m2b_ell_ecc_foci_directrices_vertical_4x2_y2` carries two, one WRONG and one WEAK.)

## Notes

- **Mathematically this group is in very good shape.** All 21 boxed final answers, every
  intermediate printed line, and every rejected-root justification (card 7's a=−1/4, card 9's
  a=−2, card 19/20's negative-e rejections) were independently re-derived and matched exactly —
  including the two "oblique ellipse" focus-directrix expansions (cards 10, 11), which were
  checked term-by-term against the printed intermediate expansion lines, not just the final boxed
  equation.
- **`RECALL` does not exist in this unit's schema** — confirmed by inspecting the raw JSON
  (`answer_book_v1` schema for this card has no `recall` key at any level). Check 3's `RECALL`
  bullet is therefore N/A for this whole group, not a gap.
- **Figures** (5 diagram steps, all 0-mark): labels and stroke geometry were checked for
  structural correctness — the director-circle figure (`ts_ipe_m2b_ell_director_circle_...`) draws
  the circle strictly larger than the ellipse as required; the auxiliary-circle figure shares the
  ellipse's semi-major-axis radius on the x-extent while being larger on the y-extent (touches only
  at the major-axis ends, as its own mistake-list requires); the equilateral-triangle figure
  (`..._stb_equilateral_triangle_...`) has its three drawn side lengths genuinely equal (≈90 px each
  in the authored stroke coordinates) rather than merely labelled equal. No figure contradicts its
  own card's answer.
- **One legitimate duplicate found, not filed as a defect**: the `memory_tip` "Latus rectum equals
  2 times the smaller square, over the bigger axis length." appears verbatim in both
  `ts_ipe_m2b_ell_ecc_foci_lr_directrices_9x2_16y2_144` and its shifted-centre twin — correct in
  both, since both share the same a=4, b=3.
- **Rule 41 (plain language)**: no idioms, metaphors or personification found anywhere in the
  group's reader-facing prose; the "answers"/"asks" hits are all either inside
  `verification_note` (not reader-facing) or ordinary usage ("the question asks for", "the two
  answers coincide") explicitly exempted by the brief's own trap list.
- The two directrix-notation WRONG findings and the one misplaced-mistake WRONG finding in card 19
  are all in `common_mistakes` items, not in printed working lines — none of them changes a boxed
  answer a student would copy onto the exam sheet.
