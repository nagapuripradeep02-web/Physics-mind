# M2A-02 — mathematics_2a, Unit 1 Complex Numbers (part 2/2)

Cards audited: 19 / 19   Findings: HARMFUL 1 · WRONG 3 · WEAK 0

Every modulus, argument, conjugate, square root and locus was re-derived independently (Python
`cmath`/`math`, then confirmed with exact `sympy`) and matched every boxed answer, including the
two flagged book corrections (`modamp_minus_1_minus_i`: book's θ=3π/4 gives −1+i, not −1−i;
`sqp_locus_arg_z_minus_1_pi_2`: book's x−1=0 omits y>0). Every `mark_split` sums to 2 and pairs
correctly to its step's label and running total. Every `tan θ fits two angles` / quadrant-selection
claim was checked by periodicity (`tan(θ±π)=tan θ`). The two locus cards in this group
(`sqp_locus_mod_z_1`, `sqp_locus_arg_z_minus_1_pi_2`) handle the excluded-point question correctly
and consistently with each other: the circle |z|=1 has no excluded point (correctly stated as a
full circle) and the half-line has one real excluded point z=1, which the card correctly excludes
with a strict `y > 0` — this is not the same defect pattern the sibling group flagged, because here
the two cards' math genuinely differs (one needs no exclusion, one does), not a case of the same
situation handled two ways.

## HARMFUL — wrong content a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_cn_z_2_minus_3i_quadratic | card (insider_note) | "Squaring z = 2 − 3i as it stands keeps an i term that never cancels." This is false — verified by direct computation: z² = −5−12i, −4z = −8+12i, and z²+(−4z)+13 = 0 exactly (the −12i and +12i cancel). Substituting z=2−3i straight into z²−4z+13 is a valid, complete method that reaches 0; it is not a dead end. The step's own `why` field even says so ("Substituting 2 − 3i into z² − 4z + 13 also works"), so the insider_note — spoken first to the student — contradicts the card's own reasoning. | "Squaring z = 2 − 3i as it stands still works — substituting into z² − 4z + 13 gives 0 — but the i term only cancels at the very last step (−12i from z² against +12i from −4z), so it takes one line more than moving the 2 across first." |
| ts_ipe_m2a_cn_z_2_minus_3i_quadratic | s1_square (common_mistakes[1]) | "Squaring z = 2 − 3i as it stands, which keeps an i term." This is listed as a common mistake, but the same step's `why` field says this exact approach "also works" (confirmed above). Labelling a valid, working method as a mistake is the bank's commonest defect. | Drop this item — direct substitution of z = 2 − 3i into z² − 4z + 13 is a valid (if longer) method, not an error; the step already carries one genuine mistake `common_mistakes[0]` (the (−3i)²=+9 sign slip). |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_cn_sqp_least_n_1_plus_i_over_1_minus_i | s1_simplify (common_mistakes[1]) | "Multiplying by 1 − i and getting (1 − i)²/2 = −i, the wrong base." Verified by exact computation: multiplying the *actual* fraction (1+i)/(1−i) by (1−i)/(1−i) gives numerator (1+i)(1−i)=2, denominator (1−i)(1−i)=−2i, so the result is 2/(−2i) = i — the CORRECT base, not −i. (1−i)²/2 = −i is instead what you get from rationalising the reciprocal fraction (1−i)/(1+i). The described operation does not produce the claimed wrong answer. | "Writing the fraction upside down as (1 − i)/(1 + i) and rationalising correctly still gives the wrong base: (1 − i)²/[(1+i)(1−i)] = −2i/2 = −i." |
| ts_ipe_m2a_cn_sqp_root3_plus_i_power_100 | s1_modulus (common_mistakes[0]) | "Trying to expand (√3 + i)¹⁰⁰ or to use De Moivre when only a² + b² is asked." De Moivre is a fully valid, exact alternative here: (√3+i)¹⁰⁰ = 2¹⁰⁰cis(2π/3) = 2⁹⁹(−1+i√3), giving a²+b²=4, the same correct answer — confirmed by exact `sympy`. Listing it alongside "trying to expand" with no qualifier condemns a correct method, unlike the sibling cards in this same unit (`a_plus_ib_squared_x2_plus_y2`, `cis_alpha_cis_beta_x2_plus_y2`) which explicitly mark their own longer-but-valid alternative "correct but ... work". | "Trying to expand (√3 + i)¹⁰⁰ term by term — correct but far longer than taking the modulus. (Using De Moivre — (√3+i)¹⁰⁰ = 2¹⁰⁰cis(2π/3) — also reaches a²+b²=4, just with more steps.)" |

## WEAK — quality, register, Rule 41

_(none)_

## Teacher-gate questions

none

## Cards checked clean

ts_ipe_m2a_cn_sqrt_minus_5_plus_12i, ts_ipe_m2a_cn_polar_minus_root7_plus_i_root21, ts_ipe_m2a_cn_modamp_1_plus_i_root3, ts_ipe_m2a_cn_modamp_minus_1_minus_i, ts_ipe_m2a_cn_modamp_1_minus_i, ts_ipe_m2a_cn_modamp_minus_root3_plus_i, ts_ipe_m2a_cn_modamp_minus_1_minus_i_root3, ts_ipe_m2a_cn_arg_product_minus_1_minus_i, ts_ipe_m2a_cn_arg_quotient_minus_1_over_i, ts_ipe_m2a_cn_arg_sum_conjugate_pi_5_pi_3, ts_ipe_m2a_cn_a_plus_ib_squared_x2_plus_y2, ts_ipe_m2a_cn_cis_alpha_cis_beta_x2_plus_y2, ts_ipe_m2a_cn_sqp_conjugate_5i_over_7_plus_i, ts_ipe_m2a_cn_sqp_locus_mod_z_1, ts_ipe_m2a_cn_sqp_z_minus_1_over_z_cos_sin, ts_ipe_m2a_cn_sqp_locus_arg_z_minus_1_pi_2

(16 clean; the other 3 carry the findings above.)

## Notes

- **Mathematically this group is in good shape.** 38/38 steps re-derived independently (numeric
  Python + exact sympy for the six most delicate: the two book-correction cards, the two De
  Moivre/modulus-shortcut cards, and the `(1+i)/(1-i)` base-simplification card); zero wrong final
  answer, zero wrong modulus, zero wrong argument, zero wrong conjugate, zero wrong locus.
- **Both book corrections recorded in `VERIFICATION_NOTE` are genuinely right.** `modamp_minus_1_minus_i`:
  the book's printed θ=3π/4 evaluates to −1+i (confirmed exact), not −1−i; the card's −3π/4 (equiv.
  cos3π/4 − i sin3π/4) correctly gives −1−i. `sqp_locus_arg_z_minus_1_pi_2`: the book's x−1=0 alone
  includes the lower half of the line, where Arg(z−1)=−π/2, not π/2; the card's `x = 1, y > 0` is the
  complete, correct locus.
- **The two locus cards are internally consistent with each other** on the excluded-point question
  (see summary paragraph above) — this group does not repeat the sibling group's split-convention issue.
- **All six modulus-amplitude/polar VSAQs of this unit (book answers 110.1–110.6) are present in this
  group** (`polar_minus_root7...`, `modamp_1_plus_i_root3`, `modamp_minus_1_minus_i`, `modamp_1_minus_i`,
  `modamp_minus_root3_plus_i`, `modamp_minus_1_minus_i_root3`) and every quadrant-selection / reference-angle
  claim in each was checked against `tan(θ±π)=tan θ` — all six are correct and mutually consistent in
  method and in their `RECALL.reject` lists (each correctly rejects the bare reference angle without
  the quadrant adjustment, e.g. "theta is pi by four" for the card whose real answer is −π/4).
- **No `RECALL.reject` entry rejects a correct answer; no `RECALL.accept` entry contains a wrong value**
  — every accept string's stated final number/angle matches the boxed answer for its own card.
- **`MARK_SPLIT` ↔ step-label ↔ running-mark-total triples are consistent in all 19 cards** (1+1=2
  throughout); no `MARGIN_NOTE` claims a mark its step does not earn.
- No Rule-41 register issues found (no idioms/metaphors/personification of numbers, angles, or
  arguments) across all 38 steps' prose fields.
- Minor pattern worth naming even though not filed as a finding: three sibling "shortcut vs. full
  expansion" cards in this same unit (`a_plus_ib_squared_x2_plus_y2`, `cis_alpha_cis_beta_x2_plus_y2`,
  `sqp_conjugate_5i_over_7_plus_i`) correctly disclose their longer alternative method as "correct
  but ... work" / "not wrong"; `sqp_root3_plus_i_power_100` and `z_2_minus_3i_quadratic` break that
  convention (filed above) — a fleet-wide pass over other units' modulus-shortcut VSAQs for the same
  omission may be worth a founder call, since the pattern recurs by family.
