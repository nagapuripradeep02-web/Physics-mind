# P2-06 — physics_2, Unit 6 Current Electricity (part 1/1)

Cards audited: 26 / 26   Findings: HARMFUL 0 · WRONG 2 · WEAK 3

Every boxed answer and every symbolic derivation was recomputed independently with one Python
script (`fractions.Fraction` for the arithmetic cards, `sympy` for the Wheatstone-bridge and
potentiometer symbolic derivations): all three colour-code conversions, the resistivity
dimensional formula, the 100 W/200 V bulb (confirming the book's own 220 V/484 Ω slip is wrong and
the card's 400 Ω at the stated 200 V is right), both meter-bridge cards, both potentiometer-emf
numerics, the battery/terminal-voltage card, the parallel/series algebra cards (three identical
resistors, wire bent into a circle, three 10 Ω resistors), the mixed-grouping maximum-current
condition (I = nε/2R and I = mε/2r verified algebraically equal at R = nr/m), the stretched-wire
4× resistance factor, and the Kirchhoff/Wheatstone and both potentiometer LAQ derivations (junction
+ loop equations solved symbolically down to P/Q = R/S and to r = R(l₁−l₂)/l₂). All match the
cards' printed values and boxed answers exactly — zero wrong numbers, zero wrong equations, zero
wrong sign anywhere in the group.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_cur_stretched_wire_resistivity | s2_resistance | `MEMORY_TIP`: "Twice as long and half as thick: 2 × 2 = 4." This conflates cross-sectional AREA (what the step's own `LINE` correctly says halves — "doubling the length halves the area") with wire thickness/diameter, a linear dimension. Re-derived: if a student instead reads "half as thick" literally as the diameter halving, A ∝ d² makes the area drop to 1/4, giving R(2l, A/4)/R(l,A) = 8, not the card's own boxed 4R. | "Twice as long and half the area: 2 × 2 = 4." (matches the step's own printed line and keeps the mnemonic's arithmetic valid without implying a diameter change.) |
| ts_ipe_p2_cur_potentiometer_internal_resistance | s5_closed_key | The symbol I is silently reused for two different currents within the same step. `[eq] V = I ρ l₂` (eq 2) uses I as the PRIMARY potentiometer-wire current — the same I set up in s1/s4 ("ε = I ρ l₁"). Two lines later `[eq] V = IR` and `[eq] ε = I(R + r)` (eq 4) use I for the SEPARATE current the test cell drives through the resistance box once K₁ is closed — a different physical current in a different loop. Nothing on the card flags the switch. The boxed result r = R(l₁ − l₂)/l₂ is still correct (re-derived and confirmed symbolically), but a student following the symbol literally would treat both as one current. The sibling card `ts_ipe_p2_cur_potentiometer_compare_emfs` has no such collision because no second circuit-current is ever needed there. | Give the secondary-loop current its own symbol, e.g. "V = i R and ε = i(R + r), where i is the current the resistance box draws from the cell — not the potentiometer-wire current I," then divide as before to reach ε/V = (R + r)/R. |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_cur_potentiometer_internal_resistance | s1_principle (card-level mark shape vs its twin `ts_ipe_p2_cur_potentiometer_compare_emfs`) | Mark shape is out of line with its near-twin card. Here the "principle (2)" step bundles BOTH the principle statement AND the wire/primary-circuit description into one 2-mark step — its own `margin_note` says so explicitly: "First two marks. The principle in full, then the wire and its primary circuit." The twin card keeps those as two SEPARATE 2-mark steps ("The principle" then "The arrangement") and instead gives both balance readings TOGETHER as one 2-mark step; this card instead splits the first and second balance readings into two SEPARATE 2-mark steps. A student working the two near-identical LAQs side by side sees the same kind of content marked in two different shapes. | Re-cut to match the twin: split s1 into a "principle" step and a separate "arrangement" (wire + primary circuit) step, 2 marks each, and fold the two balance-point readings back into one combined 2-mark step — or leave as is only if a teacher confirms the two questions are genuinely marked differently. Both cards still sum to 8 either way. |
| ts_ipe_p2_cur_copper_manganin_thicker | s2_answer | `MEMORY_TIP`: "The worse conductor must be fatter to keep up — manganin is thicker." "must … to keep up" personifies a passive wire (Rule 41a idiom/personification) — the wire does not keep up with anything. | "The higher-resistivity material needs the larger area to give the same resistance — manganin is thicker." |
| ts_ipe_p2_cur_colour_code_350_mega_ohm | s1_split | `MISTAKE[1]`: "Writing 3.5 × 10⁸ — the code wants two whole digits, 35, not 3.5." "the code wants" personifies the colour-code convention (Rule 41a). | "Writing 3.5 × 10⁸ — the colour code has only two digit bands, so it needs two whole digits, 35, not 3.5." |

## Teacher-gate questions

- `ts_ipe_p2_cur_three_ten_ohm_parallel`: the card's own `VERIFICATION_NOTE` already flags that the
  book prints this two-line parallel-resistance calculation under Long-Answer/8-mark numbering with
  no natural precedent for spreading 8 marks over so short a calculation, and that its 1+2+2+2+1
  split is an outright invented guess. A Telangana IPE teacher needs to confirm both the split and
  whether this question is genuinely examined at 8 marks at all — recorded here rather than filed as
  a finding, since the split is internally consistent (sums to 8, labels match) and the brief scopes
  mark-scheme correctness to the teacher, not this audit.

## Cards checked clean

ts_ipe_p2_cur_mean_free_path, ts_ipe_p2_cur_ohms_law_statement, ts_ipe_p2_cur_resistivity_define, ts_ipe_p2_cur_temp_coefficient_resistance, ts_ipe_p2_cur_manganin_standard_resistors, ts_ipe_p2_cur_household_parallel, ts_ipe_p2_cur_colour_code_red_red_red_silver, ts_ipe_p2_cur_colour_code_23_kilo_ohm, ts_ipe_p2_cur_bulb_100w_200v_resistance, ts_ipe_p2_cur_meter_bridge_left_gap, ts_ipe_p2_cur_meter_bridge_60cm_ratio, ts_ipe_p2_cur_potentiometer_emf_180cm, ts_ipe_p2_cur_potentiometer_second_cell_emf, ts_ipe_p2_cur_battery_internal_resistance_terminal_voltage, ts_ipe_p2_cur_three_identical_resistors_parallel, ts_ipe_p2_cur_wire_bent_circle_diameter, ts_ipe_p2_cur_mixed_grouping_maximum_current, ts_ipe_p2_cur_household_parallel_independent_control, ts_ipe_p2_cur_colour_code_350_milli_ohm, ts_ipe_p2_cur_kirchhoff_laws_wheatstone_bridge, ts_ipe_p2_cur_three_ten_ohm_parallel

(21 clean; the other 5 carry the findings above — `ts_ipe_p2_cur_potentiometer_compare_emfs` is
named only as the comparison twin in the mark-shape WEAK row, it carries no finding of its own.)

## Notes

- **Two duplicate-question pairs are handled correctly.** `ts_ipe_p2_cur_household_parallel`
  (VSAQ 6, same-voltage + independent-branch reasons) and
  `ts_ipe_p2_cur_household_parallel_independent_control` (VSAQ 20, switching + full-voltage reasons,
  in that order) are both genuinely printed by the book with different answer wording; both cards
  disclose the duplication in their `VERIFICATION_NOTE`s and neither contradicts the other.
- **Two source-book printing errors are correctly caught and corrected**, and the corrections were
  re-derived and confirmed right: the 100 W/200 V bulb (book substitutes 220 V and prints 484 Ω; the
  card answers the question as printed, 200 V → 400 Ω) and the 60 cm meter-bridge ratio (book prints
  the reduced fraction as 6/2 and appends a stray Ω to a unit-less ratio; the card gives 60/40 = 3/2 =
  1.5 with no unit).
- **The Wheatstone-bridge card avoids the bank's commonest defect pattern well.** Its
  `s7_balance` `MISTAKE[0]` — "Writing P/R = Q/S" — explicitly says "Both forms are true, but the
  asked form pairs the arms as P with Q and R with S," i.e. it does NOT condemn the mathematically
  equivalent alternative as wrong. Checked and confirmed both forms are algebraically identical
  (cross-multiplication of one gives the other); this is advisory, not a defect, and worth noting as
  the bank getting the exact trap the brief warns about right.
- **No `MISTAKE` in this group condemns correct alternative working** — the commonest defect
  class named in the brief. Every `MISTAKE` entry checked names a genuine physics/arithmetic error.
- **Mark-split sums and label pairing are 100% consistent**: every VSAQ card's two 1-mark steps sum
  to 2, every LAQ card's steps sum to 8, and every `MARK_SPLIT` label matches its step's `label`
  string exactly, including the 0-mark diagram steps correctly carrying no `MARK_SPLIT` row.
  `expected_time_min` is uniform within qtype (4 min for all 22 VSAQs, 15 min for all 4 LAQs).
- **`INSIDER_NOTE`s all check out** against their card's own content and mark split (0 false
  insider notes in the group).
