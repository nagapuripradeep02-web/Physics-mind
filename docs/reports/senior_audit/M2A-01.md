# M2A-01 — Maths-2A, Unit 1 Complex Numbers (part 1/2)

Cards audited: 19 / 19   Findings: HARMFUL 0 · WRONG 1 · WEAK 2

Every boxed answer, every intermediate `LINE`, and the three Argand figures were re-derived
independently with a `sympy`/numeric script (`verify.py`): all six Argand distances in each of the
three polygon SAQs, the general `(a−ib)³` identity (`x/a+y/b=4(a²−b²)` reduces to 0 symbolically),
the `z=3−5i` cubic factorisation, the half-angle reciprocal identity (checked symbolically and at
4 numeric θ, `4x²−1=0` every time), both loci (numerator of the real part matches the boxed
equation, and the two "excluded points" z=1 / z=−i were confirmed as genuine zero-denominator
points that also lie algebraically on their respective loci), the x/y simultaneous-equation solve,
all four products/conjugates, both `A+iB` quotient identities, both multiplicative inverses, and
the `√(7+24i)=±(4+3i)` check-by-squaring. The three Argand figures' pixel stroke coordinates were
also reconstructed against the stated points (linear fit of pixel↔coordinate) and are internally
consistent — no mislabelled quadrant or wrong stroke pairing in any of the three diagrams.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_cn_locus_z_minus_i_over_z_minus_1_imaginary | s3_locus | `MISTAKE[2]`: "Writing the answer as an inequality or as x² + y² = x + y with no '= 0' form — either is fine, but the standard form is expected." Inside a list of mistakes, this calls one of the listed items ("an inequality") *fine*. No inequality is ever a correct answer here — the condition "purely imaginary" is an equality (real part = 0), and the true locus is a specific circle, not a region. Only the second alternative (terms not yet moved to one side) is genuinely a harmless variant. | Split the item and drop the false half, e.g.: "MISTAKE[2]: Leaving the answer as x² + y² = x + y instead of moving every term to one side — this is the same locus, so it is not wrong, but the standard '= 0' form is expected." An inequality answer should not be listed as an acceptable variant anywhere in this step. |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_cn_cube_root_x_over_a_plus_y_over_b | card (insider_note) | `INSIDER_NOTE` opens "The first mark is **the opening move**: cube both sides, and the cube root never appears again." "Opening move" is a game metaphor (chess/opening move), not literal textbook English (Rule 41a bans idiom/metaphor). | "The first mark is the first step: cube both sides, and the cube root never appears again." |
| ts_ipe_m2a_cn_locus_real_part_z_plus_1_over_z_plus_i | s3_locus | The boxed final line reads "∴ Locus of z: x − y = 1, **z ≠ −i**" — the excluded point is folded into the box. Its sibling card `ts_ipe_m2a_cn_locus_z_minus_i_over_z_minus_1_imaginary` (explicitly built as "one method over different points," same 1+2+1 split, same step ids) faces the identical situation — a point where the original quotient is undefined and which also lies on the algebraic locus — and its own `VERIFICATION_NOTE`/`WHY` record a deliberate choice to leave that point OUT of the box, matching "the board-expected locus," noting the exclusion only in `WHY`. The two near-identical cards resolve the same situation two different ways with nothing in either card explaining why they differ. | Either move "z ≠ −i" out of the box into the `WHY` line to match the sibling's documented convention, or add a one-line note here (as the sibling has) explaining why this card's box keeps the exclusion. See also the teacher-gate question below. |

## Teacher-gate questions

- The `z ≠ −i` / `z ≠ 1` exclusion-point convention above: is a Telangana IPE marking scheme
  expected to dock a mark for stating the locus equation alone (without the excluded point), for
  including it, or is either accepted? Whichever a teacher confirms, the same convention should be
  applied to both of this unit's locus cards (currently split, see the WEAK row above).
- No chemistry-style naming/convention ambiguities apply to this unit (pure complex-number algebra);
  none found otherwise.

## Cards checked clean

ts_ipe_m2a_cn_argand_equilateral_triangle, ts_ipe_m2a_cn_argand_square, ts_ipe_m2a_cn_argand_rhombus, ts_ipe_m2a_cn_conjugate_pair_2_plus_11i_over_25, ts_ipe_m2a_cn_z_3_minus_5i_cubic, ts_ipe_m2a_cn_reciprocal_1_plus_cos_theta_4x2_minus_1, ts_ipe_m2a_cn_real_x_y_3_plus_i_3_minus_i, ts_ipe_m2a_cn_product_2_minus_3i_3_plus_4i, ts_ipe_m2a_cn_1_minus_i_cubed_times_1_plus_i, ts_ipe_m2a_cn_conjugate_3_plus_4i_2_minus_3i, ts_ipe_m2a_cn_conjugate_2_plus_5i_minus_4_plus_6i, ts_ipe_m2a_cn_a_minus_ib_over_a_plus_ib, ts_ipe_m2a_cn_real_imag_a_plus_ib_over_a_minus_ib, ts_ipe_m2a_cn_mult_inverse_7_plus_24i, ts_ipe_m2a_cn_mult_inverse_root5_plus_3i, ts_ipe_m2a_cn_sqrt_7_plus_24i

(16 clean; the other 3 carry the findings above — `cube_root_x_over_a_plus_y_over_b` WEAK only,
`locus_z_minus_i_over_z_minus_1_imaginary` WRONG + named in the second WEAK row,
`locus_real_part_z_plus_1_over_z_plus_i` the second WEAK row.)

## Notes

- **Mathematically this group is in very good shape.** All 19 boxed answers, and every intermediate
  `LINE`, re-derive correctly by independent computation; zero wrong final answers, zero wrong sign,
  zero wrong figure.
- **No `MISTAKE` condemns mathematically correct alternative working** anywhere in this group — the
  bank's commonest defect pattern (per the brief) does not appear here. The one WRONG finding above
  is the *opposite* shape: a mistakes-list item wrongly calling a genuinely wrong form ("an
  inequality") acceptable.
- **`MARK_SPLIT` structure is clean across all 19 cards**: every split sums to `marks_total` (4 for
  the ten SAQ cards, 2 for the nine VSAQ cards); every step's `marks=`/running total matches its
  `MARK_SPLIT` entry; every `MARGIN_NOTE` mark-position label ("First mark"/"Half the marks"/"Last
  mark" for the 1+2+1 and 1+1+1+1 SAQ cards; "First mark"/"Second mark" for the 1+1 VSAQ cards) is
  used consistently within its own split shape and consistently across all sibling cards of the same
  shape — no split-shape drift between siblings like the one found in the Maths-1A audit.
  `expected_time_min` is uniformly 8 for the 4-mark/SAQ/Section-B cards and 4 for the 2-mark/
  VSAQ/Section-A cards, matching `qtype`/`marks_total`/`section` throughout.
- **0-mark diagram steps never claim a mark**: all three Argand figure steps (`s2_figure`) correctly
  read "Worth no mark" in their `MARGIN_NOTE`.
- **`RECALL accept`/`reject` lists were read against each step's actual boxed content**; no list
  would reject a correct spoken answer or accept a wrong one. `reject` entries are uniformly
  under-specified restatements of the instruction ("find the sides", "simplify"), never a correct
  paraphrase.
- **`VERIFICATION_NOTE` provenance cross-checked for internal consistency**: the "this question IS
  Q11/Q1 of Model Paper N" claims across the 19 cards never collide (each of Model Papers 1–5 and the
  Bullet Model Paper is claimed by at most one SAQ card and, separately, at most one VSAQ card as its
  Section-A slot) — no two siblings claim to be the same paper's same slot with different content.
  Per the brief, this provenance metadata is recorded, not graded.
- **Figures**: all three Argand sketches (equilateral triangle, square, rhombus) were reconstructed
  from their stroke coordinates against the stated complex numbers (linear pixel-to-coordinate fit on
  both axes) and are geometrically faithful — correct quadrants, correct side/diagonal pairing, no
  contradiction with the boxed distances.
- Rule 41 (plain language): swept the group for idiom/metaphor patterns; only the one "opening move"
  instance above. Phrases like "sit on" (marks), "breaks" (a distance), "stands on" (a vertex) were
  considered and left alone as ordinary technical English, consistent with the previous audit's
  treatment of similarly mild collocations.
