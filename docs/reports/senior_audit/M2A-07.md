# M2A-07 — mathematics_2a, Unit 5 Permutations and Combinations (part 2/2)

Cards audited: 24 / 24   Findings: HARMFUL 0 · WRONG 2 · WEAK 1

Every boxed answer was re-derived independently in Python (`math.comb`/`math.perm`/`factorial` plus
brute force for the checkable counts: full enumeration of all 4! permutations for the derangement
D₄=9, full divide-check of 1..1080 for the 32 divisors, and full enumeration of all 6!/7! circular
arrangements modulo rotation+reflection for the two bead-chain counts, 60 and 360) and matched the
card in all 24 cases. The two general permutation/combination identities the cards invoke (Pascal
n Cr + n C(r+1) = (n+1)C(r+1); the permutation form (n−1)Pr + r·(n−1)P(r−1) = nPr, and its
reindexed twin nPr + r·nP(r−1) = (n+1)Pr used in one insider_note) were verified to hold for all
tested n, r, not just the printed instance. r = 4 or 6 (card 2) and r = 5 (card 4) both check
numerically against the identity.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_pc_vsaq_prove_10c3_10c6_11c4 | s1_rewrite | `common_mistakes[1]`: "Rewriting ¹⁰C₃ as ¹⁰C₇ instead, which moves further away." This condemns a mathematically correct alternative. Rewriting ¹⁰C₃ = ¹⁰C₇ (via ⁿCᵣ=ⁿCₙ₋ᵣ) also makes Pascal directly applicable, because 7 and 6 differ by exactly one: ¹⁰C₇ + ¹⁰C₆ = ¹¹C₇ = ¹¹C₄ = 330, the same one-line result the card's own route reaches. It does not "move further away" — it is an equally valid pairing choice, just not the one the mark scheme names. | Either drop the item, or reword to state the true reason it isn't credited: "Rewriting ¹⁰C₃ as ¹⁰C₇ instead of ¹⁰C₆ as ¹⁰C₄ also reaches ¹¹C₄ (via ¹⁰C₇+¹⁰C₆=¹¹C₇=¹¹C₄) — a valid alternative, but not the step this mark scheme names." |
| ts_ipe_m2a_pc_vsaq_arrange_a4b3c5 | s1_count_letters | `common_mistakes[1]`: "Adding the exponents wrongly: 4 + 3 + 5 = 12, not 11." As written this states the CORRECT sum (12 — confirmed correct, and stated as correct three other times in this same card: the step's own `LINE`, `MARGIN_NOTE`, and `INSIDER_NOTE` all say "12 letters") under a "wrongly" heading, with an unexplained "11" implied as the right answer. It self-contradicts the rest of the card. | Match the sibling cards' pattern (e.g. `arrange_mathematics` MISTAKE[0]: "Counting 12 letters — MATHEMATICS has 11."): state the wrong count as the headline, e.g. "Miscounting the total as 11 instead of 4 + 3 + 5 = 12." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_pc_vsaq_chess_games_30_students | s1_setup | `common_mistakes[0]` and `[1]` are the same error stated twice: "Using ³⁰P₂ = 870, double-counting every game" and "Answering 30 × 29 = 870 for the same reason" — ³⁰P₂ IS 30 × 29, so both items name the identical computation and the identical cause, giving no distinct second thing to watch for. | Replace `common_mistakes[1]` with a genuinely different error, e.g. "Adding instead of choosing pairs: 30 + 29, or otherwise not recognising 'each pair plays once' as ³⁰C₂." |

## Teacher-gate questions

None specific to this group beyond the bank-wide, already-flagged item: the 1+1 mark split on every
card in this unit is the bank's own construction (no `[N Marks]` tag is printed in the source book),
so it stays unverified until a Telangana IPE teacher confirms it — this is disclosed in every card's
own `VERIFICATION_NOTE` and is not a new finding.

## Cards checked clean

ts_ipe_m2a_pc_vsaq_9c3_9c5_10cr_find_r, ts_ipe_m2a_pc_vsaq_10c5_2_10c4_10c3, ts_ipe_m2a_pc_vsaq_12p5_5_12p4_13pr, ts_ipe_m2a_pc_vsaq_arrange_intermediate, ts_ipe_m2a_pc_vsaq_arrange_independence, ts_ipe_m2a_pc_vsaq_arrange_mathematics, ts_ipe_m2a_pc_vsaq_injections_5_to_7, ts_ipe_m2a_pc_vsaq_bijections_7_onto_itself, ts_ipe_m2a_pc_vsaq_functions_5_to_4, ts_ipe_m2a_pc_vsaq_committee_5_from_6_men_3_ladies, ts_ipe_m2a_pc_vsaq_select_4_boys_3_girls, ts_ipe_m2a_pc_vsaq_equation_3_vowels_2_consonants, ts_ipe_m2a_pc_vsaq_chains_6_beads, ts_ipe_m2a_pc_vsaq_chains_7_beads, ts_ipe_m2a_pc_vsaq_diagonals_12_sides, ts_ipe_m2a_pc_vsaq_divisors_of_1080, ts_ipe_m2a_pc_vsaq_4_sons_5_schools, ts_ipe_m2a_pc_vsaq_seven_digit_numbers_2223344, ts_ipe_m2a_pc_vsaq_12_books_4_alike_of_3_kinds, ts_ipe_m2a_pc_vsaq_select_7_from_10_soldiers, ts_ipe_m2a_pc_vsaq_derangements_4_letters

(21 clean; the other 3 carry the findings above.)

## Notes

- **Mathematically this group is in very good shape.** 24/24 boxed answers re-derived independently
  (combinatorial formula + brute force where feasible) and all correct, including the two "book is
  incomplete" cards the bank deliberately corrects: `9c3_9c5_10cr_find_r` (book stops at r = 4; the
  card correctly adds r = 6 since ¹⁰C₄ = ¹⁰C₆) and the derangement card (the book's own working is
  also confirmed right, D₄ = 9 matches a full listing of all 24 permutations of 4 items).
- **`insider_note`: 24 / 24**, and every one re-checked against its card — all true, all consistent
  with the boxed answer, none cloned falsely from a sibling. This is the highest-density insider-note
  group audited so far in this book and it held up.
- **Marks:** every card sums 1 + 1 = 2 = `marks_total`; every `mark_split` label matches its step's
  `label` field exactly (checked all 48 steps); every `margin_note` "First mark"/"Second mark" matches
  the step's running total; `expected_time_min = 4` uniformly across all 24 cards (unit convention).
- **No `RECALL reject` list rejects a correct student answer** — every reject entry is a genuinely
  under-specified phrase ("use the formula", "evaluate", "add them"), never a correct final value or
  a correct paraphrase of the method.
- **No figures in this group** (all 24 cards are pure counting VSAQs) — check 5 (figure geometry) is
  N/A.
- **Rule 41 (plain language): clean.** No idioms, metaphors, or personification found anywhere in the
  group's `why`/`memory_tip`/`margin_note`/`insider_note` text.
- **Sibling-clone risk was specifically checked** across the two near-identical clusters this group
  contains — the 4 "arrange the letters of a word/expanded form" cards (`intermediate`,
  `independence`, `mathematics`, `a4b3c5`) and the 2 "chain of n beads" cards (6-bead, 7-bead) — for
  any prose accidentally carrying a neighbour's number or letter count. None found; every count
  (E×3/I×2/T×2 for INTERMEDIATE, E×4/N×3/D×2 for INDEPENDENCE including the exact N-position claim
  "positions 2, 7, 10" — verified letter-by-letter, M×2/A×2/T×2 for MATHEMATICS, 4/3/5 for a⁴b³c⁵) is
  correct for its own card.
- Per the brief, the two WRONG findings above sit in `common_mistakes` (advisory prose), not in a
  `LINE` a student copies onto an answer sheet — consistent with sibling group M2A-06's clean result,
  this half of the chapter is also mathematically solid; its defects are both isolated wording
  problems in the "what NOT to do" guidance, not in the taught method or the boxed answers.
