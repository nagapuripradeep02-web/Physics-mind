# M2A-04 — mathematics_2a, Unit 3 Quadratic Expressions (part 1/1)

Cards audited: 24 / 24   Findings: HARMFUL 0 · WRONG 0 · WEAK 0

Solved every card from `QUESTION:` first, then checked every printed `LINE:` against a fresh
sympy re-derivation (one script, `verify.py`): all six range/maximum/"lies between"/"does not lie
between" discriminant chains (cards 1–6), the symbolic equal-roots identity `a³+b³+c³=3abc or a=0`
(card 7), nature-of-roots (card 8), the `(x−a)(x−b)=h²` always-real proof (card 9), sum/product
equation-forming with surd and complex conjugate roots (cards 10–11), the four symmetric-function
VSAQs (cards 12, 13, 22 and the `1/α+1/β` pair), max/min via `(4ac−b²)/(4a)` (cards 14–15), equal-roots-in-m
(cards 16–17), common-root systems (cards 18–21), and the two sign-of-a/interval VSAQs (cards 23–24).
Every discriminant simplification, every boundary-value attainment claim (checked by solving for the
x that reaches each endpoint and substituting back), every common-root/coefficient system, and every
symmetric-function identity matched the card exactly — including the negative-maximum card
(`max_value_2x_minus_7_minus_5x2`, answer −34/5, correctly printed negative and correctly not
"corrected" to positive) and the two cards whose own `VERIFICATION_NOTE` records a correction to the
source book (`roots_of_x_minus_a_x_minus_b_h2_real`: book said "Δ is positive", card correctly says
Δ ≥ 0 with the a=b, h=0 equality case; `max_value_2x_minus_7_minus_5x2`: a scan-lost minus sign in
the book, card correctly keeps it).

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

_(none)_

## WEAK — quality, register, Rule 41

_(none)_

## Teacher-gate questions

- `ts_ipe_m2a_qe_equal_roots_a3_b3_c3_3abc` sits on the book's unmarked "Star Questions Plus" p.91
  and is classed here as a 4-mark SAQ by shape (four written moves matching the chapter-8 SAQ
  pattern). The card's own `VERIFICATION_NOTE` records genuine counter-evidence: book p.75 routes
  readers to p.91 from the END of the VSAQ chapter ("Few More VSAQ are in Page 91"), which argues for
  a 2-mark VSAQ classification instead. `needs_teacher_verification` is already `true` on this card —
  flagging here only because it is the one open structural question in an otherwise closed group; no
  new finding, the card already discloses both readings.
- Every card in the group carries the same disclosure: the 1+1+1+1 / 1+1 mark split is the bank's own
  construction, not printed in the source book, and stays unverified until a Telangana IPE teacher
  confirms it (per the brief, this is not independently auditable beyond internal consistency, which
  holds on every card).

## Cards checked clean

ts_ipe_m2a_qe_range_x_plus_2_over_2x2_3x_6, ts_ipe_m2a_qe_range_x2_x_1_over_x2_minus_x_1, ts_ipe_m2a_qe_max_x2_14x_9_over_x2_2x_3, ts_ipe_m2a_qe_x_over_x2_5x_9_between_1_and_minus_1_11, ts_ipe_m2a_qe_sum_of_fractions_not_between_1_and_4, ts_ipe_m2a_qe_bounds_for_p_x_minus_p_over_x2_3x_2, ts_ipe_m2a_qe_equal_roots_a3_b3_c3_3abc, ts_ipe_m2a_qe_nature_of_roots_3x2_7x_2, ts_ipe_m2a_qe_roots_of_x_minus_a_x_minus_b_h2_real, ts_ipe_m2a_qe_form_equation_roots_7_plus_minus_2_root_5, ts_ipe_m2a_qe_form_equation_roots_minus_3_plus_minus_5i, ts_ipe_m2a_qe_one_by_alpha_plus_one_by_beta, ts_ipe_m2a_qe_one_by_alpha2_plus_one_by_beta2, ts_ipe_m2a_qe_min_value_x2_minus_x_7, ts_ipe_m2a_qe_max_value_2x_minus_7_minus_5x2, ts_ipe_m2a_qe_equal_roots_x2_15_m_2x_8, ts_ipe_m2a_qe_equal_roots_x2_m_plus_3_x_m_plus_6, ts_ipe_m2a_qe_common_root_find_p, ts_ipe_m2a_qe_common_root_find_a, ts_ipe_m2a_qe_common_root_b_plus_c_plus_1_zero, ts_ipe_m2a_qe_common_root_2_b_plus_d_equals_ac, ts_ipe_m2a_qe_alpha2_plus_beta2, ts_ipe_m2a_qe_x2_5x_6_positive, ts_ipe_m2a_qe_15_4x_3x2_negative

(all 24 clean; zero findings in any category.)

## Notes

- **Strict-vs-weak inequality watch (the brief's named trap): passed on every card.** All discriminant
  conditions in this unit correctly use ≥ 0 (existence of real x) or ≤ 0 (holds for all y), never a
  wrongly-strict `>`/`<`, and every place a boundary value is claimed reached, the card shows the actual
  x that reaches it (checked independently in the script for all 12 such endpoints across cards 1–6).
  The one place the group's own source material had this exact bug (card 9, the book's "Δ is
  positive") is already corrected on the card, with the a=b, h=0 equal-real-roots case preserved.
- **Interval bracket discipline verified by substitution, not just claim:** every closed-vs-open
  bracket claim (square brackets when Δ=0 is attained, round brackets when strict "positive"/"negative"
  excludes the roots) was tested against a live x-value in the script and came out correct in every
  case, including the two "outside the roots" sign cards (23, 24) confirmed with test points inside
  and outside each interval.
- **No MISTAKE entry condemns correct alternative working** — the brief's commonest defect pattern was
  checked against all ~140 `MISTAKE[i]`/`MEMORY_TIP` entries in the group; every one describes a
  genuine, reproducible error (a dropped sign, a mis-expanded square, a wrong root pairing, a lost
  case). Two entries were checked closely for false condemnation and cleared: card 16 s1's "harmless
  here" sign-identification note (which explicitly says it does not break this card's answer) and card
  21 s2's α²-substitution note (describes skipping the squaring step, not condemning a valid
  alternate route).
- **No prose cloned from a sibling reads false here.** The five range/maximum/lies-between SAQs (cards
  1–5) and the bounds-for-p SAQ (card 6) intentionally share step ids and method shape; each card's
  numbers, MEMORY_TIP and MARGIN_NOTE were checked to match ITS OWN coefficients, not a neighbour's —
  no stray quantity from another card's problem was found. Same check on the near-duplicate VSAQ pairs
  (12/22 both derive symmetric functions of α, β; 18/19 and their common-root method; 13 vs 22 —
  1/α²+1/β² vs α²+β², confirmed genuinely different targets, not a duplicate).
- **Rule 41 (plain language):** scanned every `WHY`/`MISTAKE`/`MEMORY_TIP`/`MARGIN_NOTE`/`INSIDER_NOTE`
  string in the group for idiom/metaphor/personification; none found. Physics/maths vocabulary
  (discriminant, perfect square, conjugate, sign of a) is the plain word, not jargon.
- **Structure:** every `MARK_SPLIT` label matches its step's `label` exactly and the running mark total
  in every step header advances 0→1→2→(3→4); every `mark_split` sums to `marks_total` (4 for the seven
  SAQs, 2 for the seventeen VSAQs); every `MARGIN_NOTE` "First/Second/Third/Last mark" tag matches the
  step's own running-mark position. `expected_time_min` is uniform within each qtype (8 min / 4 marks
  for SAQ, 4 min / 2 marks for VSAQ) — the group's own convention, held on every card.
- No `insider_note` was found false against its card; each was checked against the card's own working
  (e.g. the "expect the maximum to come out negative" note on card 15 matches the actual computed
  value −34/5).
