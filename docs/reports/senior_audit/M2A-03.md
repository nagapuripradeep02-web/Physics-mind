# M2A-03 — mathematics_2a, Unit 2 De Moivre's Theorem (part 1/1)

Cards audited: 22 / 22   Findings: HARMFUL 0 · WRONG 1 · WEAK 0

Every printed line was re-derived by hand from the QUESTION alone, then checked against the card.
One Python script (cmath/math, no sympy needed) numerically verified all 22 boxed final results —
116 checks, 0 failures — including the priority card at n = 1..6 with both real and imaginary parts
pinned to zero/target. Every `mark_split` sum, step-label pairing, and running-total (a→b) tag in
every `margin_note` was checked by hand; all 22 cards sum to their `marks_total` (7 for the ten LAQs,
2 for the twelve VSAQs) with correct step pairing. No `FIGURE` lines exist in this group (De Moivre
algebra has no diagrams), so check 5 is N/A. Rule 41 grep for idiom/metaphor markers returned zero
hits.

## Priority card — `ts_ipe_m2a_dm_alpha_n_beta_n_x2_2x_4`

Checked first and hardest, per instruction. **The grader was mistaken about the mathematics: every
printed line, the boxed final answer, and the mark-split are all correct.** Roots re-derived
independently: x² − 2x + 4 = 0 ⇒ x = 1 ± i√3 (discriminant −12, matches Vieta's α+β=2, αβ=4); r = 2,
θ = π/3; αⁿ + βⁿ = 2ⁿ⁺¹cos(nπ/3) verified by direct computation for n = 1..6 (e.g. n=3: α³+β³ = −16 =
2⁴cos(π) ✓; n=6: 128 = 2⁷cos(2π) ✓). `mark_split` 1+2+1+1+2 = 7 matches the five steps exactly, and the
VERIFICATION_NOTE's disclosed placement call (moving the book's 4th tag from equation (1) to the
conjugate step so it isn't a 0-mark step) is reflected correctly in the actual step marks.

There **is** one real, findable defect, and it plausibly explains why a chatbot reading might sound
"self-contradictory": step `s2_mod_amp`'s `MEMORY_TIP` and `MARGIN_NOTE` both say "r = 2 ... is where
the **2ⁿ⁺¹** comes from" — but at that point in the proof only r = 2 has been established; raising it
in `s3_de_moivre` produces only **2ⁿ**, and the extra factor of 2 (the "+1") comes only from
`s5_add`, which correctly says "2ⁿ · 2 = 2ⁿ⁺¹". So s2 pre-attributes the whole exponent to a step that
hasn't happened yet, while s5 later gives the actually-correct account — two different explanations of
where the same "+1" comes from inside one card. The four sibling cards in the same book family
(`one_plus_i_2n`, `one_plus_i_n`, `one_plus_cos_theta_n`, `p_iq_nth_root_sum`) all keep this generic
("r is what makes the power of 2") and never name the specific final exponent at the mod-amp stage —
confirming s2's phrasing is the outlier, not the house style. Graded WRONG, not HARMFUL: the printed
derivation lines themselves are all correct: a student copying the card's LINEs gets full, correct
marks; only the explanatory aside overclaims.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_dm_alpha_n_beta_n_x2_2x_4 | s2_mod_amp | `memory_tip` and `margin_note` both claim "r = 2 ... is where the 2ⁿ⁺¹ comes from." At this stage only r = 2 is known; raising it (s3) gives 2ⁿ, and the "+1" comes only from doubling in the addition step (s5), which itself correctly attributes it there. The two explanations of the same exponent, inside one card, disagree. | Match the sibling-card convention (e.g. card `one_plus_i_2n_cos_n_pi_2`'s s1): "r = √(1 + 3) = 2 — it is what makes the power of 2 in the answer" (drop the specific "ⁿ⁺¹", which isn't earned until the addition step). |

## WEAK — quality, register, Rule 41

_(none)_

## Teacher-gate questions

- `ts_ipe_m2a_dm_alpha_n_beta_n_x2_2x_4`'s `VERIFICATION_NOTE` discloses a specific editorial mark-placement
  call: the book prints five tags summing 1+2+1+1+2=7 with (per the note) two separate 1-mark tags
  inside the raise-to-n / equation-(1) region and none on the "Similarly … (2)" conjugate line; the
  card's author moved one of those two 1-mark tags onto the conjugate step so it isn't a 0-mark step.
  The resulting split (s3_de_moivre=1, s4_conjugate=1) is internally consistent and sums correctly,
  but the placement itself — whether the conjugate line earns a mark of its own on a Telangana IPE
  answer sheet, versus the book's apparent intent to double-tag the De Moivre step — is a real marking
  judgment call, not just the generic "coaching book, unverified" disclaimer repeated on every card in
  the bank. Worth a specific teacher ruling.
- No other group-specific conventions or ambiguities surfaced; the remaining 21 cards' `VERIFICATION_NOTE`
  provenance/placement disclosures are the same boilerplate-unverified-until-teacher-confirms pattern
  seen bank-wide and not something this group adds new information about.

## Cards checked clean

ts_ipe_m2a_dm_one_plus_i_2n_cos_n_pi_2, ts_ipe_m2a_dm_one_plus_i_n_cos_n_pi_4, ts_ipe_m2a_dm_one_plus_cos_theta_n, ts_ipe_m2a_dm_x_m_y_n_cos_sin_identity, ts_ipe_m2a_dm_p_iq_nth_root_sum, ts_ipe_m2a_dm_sin_pi_8_power_8_3, ts_ipe_m2a_dm_z_2n_i_tan_n_theta, ts_ipe_m2a_dm_cos_sum_zero_cos_squares_3_2, ts_ipe_m2a_dm_cos_sum_zero_cos_3alpha, ts_ipe_m2a_dm_vsaq_cis_abc_triangle_xyz, ts_ipe_m2a_dm_vsaq_x6_plus_1_by_x6, ts_ipe_m2a_dm_vsaq_omega_sixth_powers_128, ts_ipe_m2a_dm_vsaq_omega_2_minus_omega_49, ts_ipe_m2a_dm_vsaq_one_minus_i_8, ts_ipe_m2a_dm_vsaq_one_plus_i_16, ts_ipe_m2a_dm_vsaq_omega_cube_minus_8, ts_ipe_m2a_dm_vsaq_omega_product_9, ts_ipe_m2a_dm_vsaq_omega_reciprocal_sum, ts_ipe_m2a_dm_vsaq_x_minus_1_cubed_plus_8, ts_ipe_m2a_dm_vsaq_omega_a3_plus_b3, ts_ipe_m2a_dm_vsaq_cis_pi_6_fifth_power_i

(21 clean; `ts_ipe_m2a_dm_alpha_n_beta_n_x2_2x_4` carries the one finding above but is otherwise
correct — its steps s1, s3, s4, s5 and its boxed answer are all clean.)

## Notes

- **This is a mathematically airtight group.** 22/22 boxed final answers verified numerically
  (script covered every LAQ identity for multiple n/angle values and every VSAQ evaluation), plus the
  ω-family identities (1+ω+ω²=0, ω³=1) and the two "prove-two-equal-sides" VSAQs
  (`omega_reciprocal_sum`, the p+iq root-sum family). Zero wrong final answers, zero wrong signs,
  zero wrong quadrant reads, zero wrong roots.
- **`p_iq_nth_root_sum`'s p > 0 hypothesis is correctly carried and correctly justified.** The card's
  `WHY` gives a concrete counterexample (p = −3, q = 4, n = 2: true sum is 2, the unconditioned
  formula would give 4) — re-derived and confirmed exactly right (principal square roots of
  −3±4i are 1±2i, summing to 2; the naive Tan⁻¹(q/p) branch gives 4). This is the "identity stated
  as general carries the hypothesis it needs" check (method step 3) passing cleanly, and it is the
  one card in the group that most needed it.
- **No MISTAKE entry condemns valid alternative working.** The closest candidate,
  `cos_sum_zero_cos_squares_3_2`'s s5 `MISTAKE[0]` ("deriving the sin² result by subtracting the cos²
  result from 3, which is fine mathematics but skips the marked step"), explicitly says the shortcut
  is mathematically fine and only costs the METHOD mark — correctly handled, not a defect.
- **No sibling-clone contamination found.** The five-card mod-amp-form family (`one_plus_i_2n`,
  `one_plus_i_n`, `one_plus_cos_theta_n`, `alpha_n_beta_n`, `p_iq_nth_root_sum`) and the two-card
  `cos_sum_zero` family share step ids and prose shape deliberately (as their `VERIFICATION_NOTE`s
  say), but every number, sign and quadrant read in each was checked against THAT card's own numbers,
  not copied wrongly from a neighbour — except the one exponent-attribution slip filed above.
- `Q1`–`Q4` quadrant trap does not arise in this group (no quadrant-numbered content); flagging its
  absence per the brief's trap list.
