# M2B-10 — mathematics_2b, Unit 7 "Definite Integrals (Maths-2B)" (part 1/2)

Cards audited: 24 / 24   Findings: HARMFUL 0 · WRONG 6 · WEAK 2

All 24 boxed final answers were re-derived independently with `mpmath.quad` (40-digit precision,
numeric definite integration matched against the symbolic claimed value; diff < 1e-15 on every card)
and every algebraic identity flagged below (King's-property swaps, `(1−t²)/(1+t²)` splits, the
`4^(3/2)` and `log(a²)=2log(a)` steps, the Wallis-formula coefficients, both parabola-area
intersections at a=2,b=3) was checked with `sympy`. Every printed LINE follows correctly from the one
above it and every boxed answer is right — the six defects below are all in prose (`MISTAKE`,
`MEMORY_TIP`, `WHY`) and in two cloned FIGURE stroke paths, never in a line a student would copy.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_di_laq_xsin3x_over_1plus_cos2x_0_to_pi | s4_final | `MISTAKE[1]`: "Writing the final answer as π²/2 − π/2 instead of the equivalent π(π−2)/2." These are NOT equivalent: π(π−2)/2 = π²/2 − π ≈ 1.793, but π²/2 − π/2 ≈ 3.364. The card asserts two unequal expressions are the same value. | "Writing the final answer as π²/2 − π instead of the equivalent π(π−2)/2." (π²/2 − π genuinely equals π(π−2)/2 — the un-simplified vs. simplified form, matching the pattern used in the sibling card's own step-4 mistake.) |
| ts_ipe_m2b_di_laq_xsinx_over_1plus_sinx_0_to_pi | s2_add | `MEMORY_TIP`: "1/(1+sinx) always splits into 1 minus 1/(1+sinx)…" — false as written (1/(1+sinx) ≠ 1 − 1/(1+sinx); verified with sympy, the difference simplifies to (1−sinx)/(1+sinx) ≠ 0). It contradicts the step's own `WHY` ("Writing sinx as (1+sinx) − 1…") and its own `LINE`s, which split **sinx**/(1+sinx), not 1/(1+sinx). | "sinx/(1+sinx) always splits into 1 minus 1/(1+sinx) — write it as [1 + sinx − 1]/(1 + sinx)." |
| ts_ipe_m2b_di_laq_sin2x_over_cosx_plus_sinx | s4_final | `WHY` ends "…leaving a perfect square inside the log, whose **square root** cancels the outer 1/2 exactly." No square root is taken anywhere in this step's `LINE`s — the mechanism actually used is log(a²) = 2log(a) (the sibling card `..._x_over_sinx_plus_cosx`'s own s4 `WHY` states this correctly for the identical algebra). | "…leaving a perfect square inside the log; log(a²) = 2log(a) turns that square into a factor of 2, which cancels the outer 1/2 exactly." |
| ts_ipe_m2b_di_saq_area_y2_eq_4x_x2_eq_4y | s3_final | `MISTAKE[0]`: "Computing 4^(3/2) incorrectly — it is (√4)³ = 2³ = 8, not 4×√4." But 4×√4 = 4×2 = 8 — the same value (verified with sympy: 4√4 = 4^(3/2) = (√4)³ = 8 identically). The line condemns a correct alternative computation of the same number as if it were wrong. | "Computing 4^(3/2) incorrectly, e.g. as 4³ = 64 (forgetting the power is 3/2, not 3) instead of the correct (√4)³ = 2³ = 8." |
| ts_ipe_m2b_di_saq_area_y2_eq_4x_x2_eq_4y | s2_boundary | FIGURE `area_two_parabolas_4x_4y`: the two curve strokes are swapped relative to their labels. `curve_y2_4x`'s path ("M 60 200 Q 105 200 128 152…") has a horizontal start tangent — it hugs the x-axis at the origin — but y²=4x's true tangent at the vertex is vertical (perpendicular to its horizontal axis of symmetry; dy/dx→∞ as x→0). `curve_x2_4y`'s path ("M 60 200 Q 62 145 92 122…") has a near-vertical start tangent — it hugs the y-axis — but x²=4y's tangent at the vertex is horizontal (dy/dx=x/2→0 as x→0). Verified by sampling both Bézier curves at t=0.05–0.2: the "y²=4x" stroke moves mostly in x near the origin, the "x²=4y" stroke moves mostly in y. Confirmed by the card's own text elsewhere ("y²=4x opens sideways… x²=4y opens upward"), which the drawn strokes contradict. | Swap the two `d` path strings between the stroke ids: give `curve_y2_4x` the path currently on `curve_x2_4y` ("M 60 200 Q 62 145 92 122 Q 122 104 150 100") and vice versa, so the y-axis-hugging curve is labeled y²=4x and the x-axis-hugging curve is labeled x²=4y. |
| ts_ipe_m2b_di_saq_area_y2_eq_4ax_x2_eq_4by | s2_boundary | FIGURE `area_two_parabolas_4ax_4by`: identical stroke paths to the sibling card above (`curve2_y2_4ax`/`curve2_x2_4by` are a verbatim clone of `curve_y2_4x`/`curve_x2_4y`), so the same tangent-direction swap is present here too. | Same fix as the sibling card: swap the two `d` path strings between `curve2_y2_4ax` and `curve2_x2_4by`. |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_di_saq_x2_a2_minus_x2_3by2_neg_a_to_a | s4_final | `MARGIN_NOTE`: "sin²θcos⁴θ has one odd-looking piece (the overall product is even·even = even), so the π/2 factor is present." Confusing/unhelpful — "one odd-looking piece" and "even·even = even" describe nothing precise (the Wallis π/2 criterion is that BOTH exponents, 2 and 4, are even numbers, not a product of "even·even"). The step's own `WHY` states the correct criterion clearly. | "Both exponents in sin²θcos⁴θ are even (2 and 4), so Walli's formula includes the extra π/2 factor." |
| ts_ipe_m2b_di_vsaq_2x_over_1plusx2_2_to_3 | s1_recognise | `MARGIN_NOTE`: "…a formula worth recognising **on sight**." Rule 41 idiom (one of the brief's own named examples). | "…a formula worth recognising immediately." |

## Teacher-gate questions

- `ts_ipe_m2b_di_saq_area_ellipse_deduce_circle` — the card's own `VERIFICATION_NOTE` records a
  deliberate SECTION OVERRULE: the book's printed page banner reads "DEFINITE INTEGRALS-VSAQ" and
  this answer's two page-neighbours are authored VSAQ/2, but this card is authored SAQ/4 because the
  "show, hence deduce" two-part derivation doesn't fit a 2-mark slot. The note explicitly asks a
  teacher to settle it. Flagging per the note's own request — not an error, both readings are on
  record.

## Cards checked clean

ts_ipe_m2b_di_laq_xsinx_over_1plus_cos2x_0_to_pi, ts_ipe_m2b_di_laq_x_over_sinx_plus_cosx, ts_ipe_m2b_di_laq_log_1plus_tanx_0_to_pi4, ts_ipe_m2b_di_laq_log_1plusx_over_1plusx2_0_to_1, ts_ipe_m2b_di_laq_sqrt_x_minus_a_b_minus_x, ts_ipe_m2b_di_laq_sinx_plus_cosx_over_9plus16sin2x, ts_ipe_m2b_di_saq_a_sinx_plus_b_cosx_over_sinx_plus_cosx, ts_ipe_m2b_di_saq_cos_5by2x_over_sin_plus_cos_5by2x, ts_ipe_m2b_di_saq_sin5x_over_sin5x_plus_cos5x, ts_ipe_m2b_di_saq_practice_cos5x_over_sin5x_plus_cos5x, ts_ipe_m2b_di_saq_sqrt_sinx_over_sqrt_sinx_plus_sqrt_cosx, ts_ipe_m2b_di_saq_dx_over_4plus5cosx, ts_ipe_m2b_di_saq_16_minus_x2_5by2_0_to_4, ts_ipe_m2b_di_saq_area_y_eq_x2_and_y_eq_x3, ts_ipe_m2b_di_saq_area_ellipse_deduce_circle, ts_ipe_m2b_di_vsaq_dx_over_sqrt_2x_minus1_1_to_5, ts_ipe_m2b_di_vsaq_x_over_sqrt_x2_plus16_0_to_3

(17 clean; the other 7 carry the findings above — two of which, the parabola-area pair, share the
same cloned figure defect.)

## Notes

- **Mathematically this group is in very good shape.** 24/24 boxed final answers verified numerically
  (`mpmath.quad`, 40-digit precision, diff < 1e-15 against the symbolic claimed value, including the
  two general-parameter cards `a=2,b=3` numeric instantiations). Every King's-property swap
  (sin↔cos, sin↔cos under π/2−x, the general a+b−x form used on the π/6–π/3 card), every Wallis-formula
  coefficient (cos⁶θ, sin²θcos⁴θ), and the completing-the-square/√(k²−u²) semicircle-area card were
  independently re-derived and matched.
- **`ts_ipe_m2b_di_saq_sin5x_over_sin5x_plus_cos5x`'s BOOK ERROR is real and correctly caught**: the
  card's `VERIFICATION_NOTE` and `MISTAKE[0]` both flag that the printed book repeats sin⁵x instead of
  turning it into cos⁵x on the second King's-property line — verified: sin(π/2−x)=cosx, so the second
  form's numerator must be cos⁵x, exactly as the card's own (corrected) `LINE`s show. The book's final
  answer π/4 is unaffected and correct.
- **Sibling-card cross-check on the general-parameter area pair**: `..._area_y2_eq_4ax_x2_eq_4by`'s own
  `INSIDER_NOTE` says setting a=b=1 in its 16ab/3 result must reduce to `..._area_y2_eq_4x_x2_eq_4y`'s
  16/3 — confirmed true, and confirmed the two cards' figures share the same cloned tangent-swap defect
  (filed once per card above since each is independently reader-facing).
- **No `RECALL` fields exist in this group's cards** (grep confirmed 0 occurrences) — nothing to check
  under that part of the method.
- **No card in this group contradicts its own `mark_split`**: every `MARGIN_NOTE`'s stated mark count
  matches its step's `marks`/running total, and every `MARK_SPLIT` sums to `marks_total` (7 for the 9
  LAQs, 4 for the 12 SAQs, 2 for the 3 VSAQs).
- Three SAQ cards (answers 82/83/88) carry genuinely PRINTED margin-mark tags from the source book,
  transcribed verbatim per their `VERIFICATION_NOTE`s — not authored splits; these were not
  second-guessed.
