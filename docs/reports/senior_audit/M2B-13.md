# M2B-13 — mathematics_2b, Unit 8 "Differential Equations (Maths-2B)" (part 2/2)

Cards audited: 18 / 18   Findings: HARMFUL 1 · WRONG 2 · WEAK 0

One sympy script substituted every boxed solution back into its differential equation (implicit
differentiation for the eight separable/linear "solve" cards, direct differentiation for the four
"form the D.E." cards, and symbolic exponent/isolation checks for the five order-and-degree cards),
and independently recomputed every integrating factor by differentiating its exponent. All 18 boxed
answers, all 4 integrating factors, and all 5 order/degree verdicts check out exactly — every
`simplify()` residual came back `0`. The two defects below are both in prose that contradicts the
card's own printed working, not in the mathematics a student would copy onto the answer sheet.

## HARMFUL — wrong content a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_de_orderdegree_x12_d2y13 | card (`insider_note`) | "The fractional power sits on the highest derivative alone, so it is cleared by cubing the WHOLE equation, **not by touching the x·dy/dx + y terms that already sit outside the bracket**." This is false on two counts, both provable from the card's own text. (1) The printed working isolates `x^(1/2)(d²y/dx²)^(1/3)` on one side FIRST, then cubes both sides — and the resulting line is `x^(3/2)·d²y/dx² = −(x·dy/dx + y)³`, i.e. the `x·dy/dx + y` term is explicitly cubed, not left untouched. (2) The card's own `s1_cube` `MISTAKE[0]` names exactly the method this insider_note recommends as the error: "Cubing the original three-term equation directly without isolating the fractional-power term first — cubing does not distribute over addition… the bracket must be isolated before cubing." The insider_note — spoken to the student BEFORE they attempt the problem — tells them to do the thing the card's own mistake list warns against. | "The fractional power sits on the highest derivative alone, so the bracket must be isolated on one side FIRST — cubing does not distribute over a sum, so (A+B+C)³ ≠ A³+B³+C³. Once x^(1/2)(d²y/dx²)^(1/3) is alone on one side, cube both sides; the x·dy/dx + y term does get cubed as a result, becoming (x·dy/dx + y)³." |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_de_orderdegree_d2y_eq_1plusdy2_53 | s2_state_order_degree | `WHY` ends: "…the order still counts only how many times the equation was differentiated, which stays at 2." This is a given differential equation (not one being formed from a family of curves) — nothing was differentiated anywhere in this card's solution (only cubing was used to clear the fractional power). Order here is simply "the order of the highest-order derivative appearing in the equation," full stop; it has nothing to do with a differentiation count. Taken literally, "how many times the equation was differentiated" would be 0 in this solution, not 2 — the sentence imports language that correctly describes the sibling "form the D.E." cards in this same unit (`ts_ipe_m2b_de_formde_acos3x_bsin3x`, `..._order_circles_centre_origin`, etc., where order genuinely does equal the differentiation count needed to eliminate arbitrary constants) into a card where it doesn't apply. | "…the order is simply the order of the highest-order derivative appearing in the equation, d²y/dx², which is 2 — order and degree here are read directly off the given equation, not from any differentiation process." |
| ts_ipe_m2b_de_separable_x_plus_ydydx_eq0 | s1_separate | `MARGIN_NOTE`: "One mark. Move x across and **divide by y** to separate the two variables," echoed by `MISTAKE[1]`: "Trying to divide by x instead of by y." Neither operation appears in the printed working: from `y·dy/dx = −x` the card goes straight to `y dy = −x dx` by multiplying through by dx — y is already correctly placed with dy, nothing is divided by y. This contradicts the step's own `WHY`, which correctly says the separation is done "moving x to the other side and **multiplying through by dx**." | `MARGIN_NOTE`: "One mark. Move x across, then multiply through by dx to separate the two variables." `MISTAKE[1]`: "Multiplying only one side by dx (e.g. keeping y·dy/dx = −x·dx), which does not separate the two variables correctly." |

## WEAK — quality, register, Rule 41

_(none)_

## Teacher-gate questions

None specific to this group's mathematics or prose. (The bank-wide ★★ vs ★★★ chapter-star discrepancy
and the Bullet Model Paper's `+`/`−` sign misprint on `ts_ipe_m2b_de_orderdegree_d2y_dy3_pow65` are
recorded in `VERIFICATION_NOTE`, not spoken to the student, and were independently confirmed not to
affect any boxed answer — see Notes.)

## Cards checked clean

ts_ipe_m2b_de_linear_x_in_y_taninvy, ts_ipe_m2b_de_linear_xplusyplus1_dydx_eq1, ts_ipe_m2b_de_linear_xlogx_dy_y_eq_2logx, ts_ipe_m2b_de_orderdegree_d2y_dy3_pow65, ts_ipe_m2b_de_formde_acos3x_bsin3x, ts_ipe_m2b_de_formde_y_cx_minus2c2, ts_ipe_m2b_de_order_circles_centre_origin, ts_ipe_m2b_de_formde_acos_nxplusb, ts_ipe_m2b_de_separable_dydx_exy, ts_ipe_m2b_de_substitution_dydx_plus1_exy, ts_ipe_m2b_de_separable_1plusy2_over_1plusx2, ts_ipe_m2b_de_separable_sqrt1minusx2_sqrt1minusy2, ts_ipe_m2b_de_separable_dydx_eq_2y_over_x, ts_ipe_m2b_de_findif_cosx_dy_ysinx_tanx, ts_ipe_m2b_de_findif_xdy_minusy_2x2sec22x

(15 clean; the other 3 carry the findings above.)

## Notes

- **Machine checks run (sympy, one script):** implicit-differentiation verification of all 8 separable/
  linear "solve" boxed answers against their original D.E.s (cards 1–3, 11–18 minus the two find-IF-only
  cards, which were checked by re-deriving P, simplifying Q, and re-integrating P independently); direct
  differentiation of all 4 "form the D.E." answers (cards 7, 8, 9, 10) back to the boxed result; symbolic
  exponent-clearing checks for all 5 order/degree cards (4, 5, 6, plus the two solved above); and
  independent recomputation of both integrating factors (cards 17, 18) by differentiating P from
  scratch and simplifying `e^(∫P dx)`. Every check returned exactly 0 / the expected closed form —
  **zero wrong boxed answers, zero wrong integrating factors, zero sign errors** in this group (the
  paper's recorded failure mode — a dropped leading minus — was hunted for specifically on every
  "Losing the minus sign…" `MISTAKE` entry and every implicit-differentiation check; none reproduced).
- **The source-book minus/plus discrepancy on `ts_ipe_m2b_de_orderdegree_d2y_dy3_pow65`** (Model Paper
  prints `[d²y/dx² − (dy/dx)³]^(6/5)`, this VSAQ page and the Model Paper's own worked line both use
  `+`) does not affect order or degree either way — both signs give the same cleared-equation exponent
  structure, so the card's choice to follow the `+` form (matching its own working) is a safe,
  independently-confirmed non-issue, not a teacher-gate question.
- **No `RECALL` field appears anywhere in this group** — these 18 cards carry no drill-down trigger
  phrases, so check 3's `RECALL must_convey/accept/reject` item and check 5 (Figures) are both N/A for
  this group (no `FIGURE` lines either — every card here is pure algebra/calculus, no diagrams).
- **No prose cloned from a sibling that is false here.** The six separable-equation cards (11, 13, 14,
  15, 16 plus 12's substitution) share a near-identical `INSIDER_NOTE`/`WHY` template ("both sides are
  already standard … no substitution needed" / "separates immediately"), but each instance was checked
  against its OWN equation and is true for that card — this is intentional template reuse, not a
  cloning defect.
- **`ts_ipe_m2b_de_orderdegree_x12_d2y13` and `..._d2y_eq_1plusdy2_53` are a deliberately contrastive
  pair** (fractional power buried inside a sum vs. sitting alone on one side) — the HARMFUL finding
  above is exactly the point this pairing exists to teach, which makes the wrong insider_note on the
  harder of the two especially costly: a student who reads it learns the WRONG general rule right
  before hitting the case where it matters.
- Every `mark_split` label matches its step's `label` exactly and every card's marks sum to
  `marks_total` (4 for the three SAQ cards, 2 for the fifteen VSAQ cards); `expected_time_min` is
  uniform within qtype (9 for SAQ, 4 for VSAQ) across the whole group.
