# P2-08 — physics_2, Unit 8 Magnetism and Matter (part 1/1)

Cards audited: 13 / 13   Findings: HARMFUL 0 · WRONG 0 · WEAK 3

Every numeric card was recomputed with a Python script (`scratchpad/P2-08/verify.py`): torque
τ = MB sinθ = 5×0.4×sin60° = √3 ≈ 1.7320508 N·m (matches boxed answer); earth field
B = H/cos60° = 2.6×10⁻⁵/0.5 = 5.2×10⁻⁵ T (matches, and the flagged trap 1.3×10⁻⁵ = H·cos60°
reproduces exactly); pole-force distance d₂ = d/√2 = 0.70710678 d (matches, and halving d gives
a ×4 force factor as the card's mistake-line claims). The 1 gauss = 10⁻⁴ T conversion (Card 1)
is in the correct direction — no inversion, matching the background flag. Every set/dip/declination
fact, every para/dia/ferro property (χ sign, μᵣ = 1+χ, motion in a non-uniform field, rod
orientation, examples) and Gauss's law for magnetism (Δφ = B·ΔS cosθ, Σ B·ΔS = 0, contrast with
Σ E·ΔS = q/ε₀) were checked by hand against standard NCERT-level physics and found correct. All
13 `mark_split` tables sum correctly and match their step labels; all `RECALL reject` lists reject
only content that belongs to a sibling step, never content correct for the step it is attached to.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

_(none)_

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_mag_torque_on_bar_magnet | card (insider_note) | "The 0.1 m length is **planted data** — the students who lose time here are the ones trying to use it." "Planted" is a metaphor (Rule 41a). | "The 0.1 m length is not needed — the students who lose time here are the ones trying to use it." |
| ts_ipe_p2_mag_earth_field_from_h | card (insider_note) | "The whole numerical **turns on** one choice — divide by cos 60°, not multiply — and the halved answer 1.3×10⁻⁵ is the **wrong turn** the examiner sees most." Two idioms in one sentence: "turns on" (= depends on, the same family as the explicitly banned "rides on") and "wrong turn" (= mistake). | "The whole numerical depends on one choice — divide by cos 60°, not multiply — and the halved answer 1.3×10⁻⁵ is the mistake the examiner sees most." |
| ts_ipe_p2_mag_compass_at_poles | card (insider_note) | "The asked-in-2017 phrasing **wants** the mechanism, not just the fact — zero horizontal component, therefore no torque, therefore no direction." Personifies the question phrasing. | "The question asked in 2017 needs the mechanism, not just the fact — zero horizontal component, therefore no torque, therefore no direction." |

## Teacher-gate questions

None.

## Cards checked clean

ts_ipe_p2_mag_units_moment_induction_field, ts_ipe_p2_mag_magnetic_declination, ts_ipe_p2_mag_angle_of_dip, ts_ipe_p2_mag_classify_materials, ts_ipe_p2_mag_solenoid_moment, ts_ipe_p2_mag_pole_force_doubled, ts_ipe_p2_mag_closed_loops, ts_ipe_p2_mag_needle_at_poles, ts_ipe_p2_mag_compare_para_dia_ferro, ts_ipe_p2_mag_gauss_law_magnetism

(10 clean; the other 3 carry the WEAK findings above.)

## Notes

- **Physics is clean across the group.** No wrong final answer, no false printed line, no wrong
  formula, no wrong classification, no wrong sign. The background-flagged gauss/tesla conversion
  (Card 1: "1 gauss = 10⁻⁴ T") is in the correct direction; checked again explicitly per the
  audit instruction and found sound everywhere it appears (only Card 1 states it).
- **No `MISTAKE` entry condemns correct physics** anywhere in the group (checked all 13 cards,
  ~30 mistake entries) — the closest near-miss is `ts_ipe_p2_mag_compass_at_poles` s1
  `MISTAKE[0]` ("Saying the field is strongest at the poles instead of saying it is vertical"),
  which is factually true (the field IS stronger near the poles) but is correctly framed as an
  answer-relevance note, not a claim that the fact itself is false — not a finding.
- **No prose cloned from a sibling that is false here.** `ts_ipe_p2_mag_compass_at_poles` and
  `ts_ipe_p2_mag_needle_at_poles` cover the same physics (H = 0 at the poles) from two angles and
  are mutually consistent, not duplicated; `ts_ipe_p2_mag_closed_loops` and
  `ts_ipe_p2_mag_gauss_law_magnetism` share the "monopoles do not exist" reasoning by design
  (the `insider_note` on the VSAQ explicitly calls out this reuse) and both state it correctly.
- **No `MARGIN_NOTE` claims a mark its step does not earn.** Every margin note's mark count
  matches the step's `marks=` value, including the 0-mark diagram step in
  `ts_ipe_p2_mag_gauss_law_magnetism` (s2), whose margin note explicitly says "No diagram mark is
  asked."
- **No `RECALL reject` entry would reject a correct answer for its own step** — every reject
  list names content that is correct but belongs to a *different* step of the same card (e.g.
  Card 5 s1 rejects the final boxed value "root three newton metre", which is s2's job); checked
  all 26 recall blocks across the 13 cards.
- Figure in `ts_ipe_p2_mag_gauss_law_magnetism` (closed_surface_flux): outward normal is
  perpendicular to the local surface tangent at the element patch, the θ arc sits between the
  normal and field-arrow directions, and all five labels sit next to their strokes — no
  mislabeling or wrong-direction arrow found.
- `expected_time_min` is consistent within qtype across the group (4 min for every 2-mark VSAQ,
  8 min for every 4-mark SAQ).
