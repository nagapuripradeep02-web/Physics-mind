# M2A-06 — mathematics_2a, Unit 5 Permutations and Combinations (part 1/2)

Cards audited: 25 / 25   Findings: HARMFUL 0 · WRONG 0 · WEAK 0

Every boxed answer was independently re-derived: brute-force enumeration (Python `itertools`) for
the three dictionary-rank cards (MASTER=257, PRISON=438, EAMCET=133) and both digit-sum cards
(1,3,5,7,9→666600; 0,2,4,7,8→545958, including the 559944-all-arrangements and 13986-leading-zero
sub-sums the card itself states) and the PISTON at-least-one-repeat card (936); direct combinatorial
identity checks for the two telescoping sums (34C5 case, 25C4 case) and the ⁴ⁿC₂ₙ/²ⁿCₙ proof (checked
symbolically for n=1..5); case-by-case enumeration for the three "list compositions, count, add" cards
(cricket 63, cricket-with-keepers 560, committee 281); and exhaustive/algebraic search for all 13
VSAQ "solve for n or r" cards. All 25 match the card's boxed answer.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

_(none)_

## WEAK — quality, register, Rule 41

_(none)_

## Teacher-gate questions

None. The nationalities in `ts_ipe_m2a_pc_saq_committee_indians_majority` ("6 Indians and 5
Americans") are flagged by the card's own `VERIFICATION_NOTE` as the printed question's own wording,
kept because changing them would change the question — not a portability defect to rule on here.

## Cards checked clean

ts_ipe_m2a_pc_saq_sum_34c5_telescoping, ts_ipe_m2a_pc_saq_sum_25c4_equals_30c4, ts_ipe_m2a_pc_saq_prove_4nc2n_by_2ncn, ts_ipe_m2a_pc_saq_cricket_team_atleast_5_bowlers, ts_ipe_m2a_pc_saq_cricket_team_2_keepers_atleast_4_bowlers, ts_ipe_m2a_pc_saq_committee_indians_majority, ts_ipe_m2a_pc_saq_rank_master, ts_ipe_m2a_pc_saq_rank_prison, ts_ipe_m2a_pc_saq_rank_eamcet, ts_ipe_m2a_pc_saq_sum_four_digit_numbers_1_3_5_7_9, ts_ipe_m2a_pc_saq_sum_four_digit_numbers_0_2_4_7_8, ts_ipe_m2a_pc_saq_piston_four_letter_words_repeated, ts_ipe_m2a_pc_vsaq_np3_1320_find_n, ts_ipe_m2a_pc_vsaq_12pr_1320_find_r, ts_ipe_m2a_pc_vsaq_np4_1680_find_n, ts_ipe_m2a_pc_vsaq_np7_42_np5_find_n, ts_ipe_m2a_pc_vsaq_n1p5_np5_ratio_3_2, ts_ipe_m2a_pc_vsaq_n1p5_np6_ratio_2_7, ts_ipe_m2a_pc_vsaq_nc4_210_find_n, ts_ipe_m2a_pc_vsaq_10nc2_3n1c3_find_n, ts_ipe_m2a_pc_vsaq_npr_5040_ncr_210, ts_ipe_m2a_pc_vsaq_nc5_nc6_find_13cn, ts_ipe_m2a_pc_vsaq_nc21_nc27_find_50cn, ts_ipe_m2a_pc_vsaq_15c2r1_15c2r4_find_r, ts_ipe_m2a_pc_vsaq_12cr1_12c3r5_find_r

## Notes

- **Every `INSIDER_NOTE` checked against its own card's numbers and marks** — all 25 correctly name
  which step carries which mark and correctly state the trap they describe (e.g.
  `ts_ipe_m2a_pc_vsaq_np3_1320_find_n`'s note that 20·66=1320 is "correct arithmetic" but not
  consecutive factors — a properly-scoped caution that does NOT condemn a valid alternative
  factoring as wrong, unlike the bank's commonest defect pattern this audit specifically hunted for).
- **No `MISTAKE` entry condemns mathematically correct alternative working.** The one borderline case
  — `ts_ipe_m2a_pc_vsaq_nc5_nc6_find_13cn` s1 `MISTAKE[1]`, "Trying to solve nC5 = nC6 by expanding
  both — it works but takes far longer than two marks allow" — explicitly states the method "works"
  (i.e. is correct) and only warns it is too slow for a 2-mark VSAQ; it does not call correct math
  wrong, so it was not filed.
- **No cross-contamination between sibling cards.** This group is built from five families that
  share step ids and method shape by design (per each card's own `VERIFICATION_NOTE`): the two
  telescoping sums, the three case-list-count-add cards (cricket/cricket-with-keepers/committee),
  the three dictionary-rank cards, the two digit-sum cards, and the "solve for n or r" VSAQ family.
  Every number in every `LINE`, `WHY`, `MISTAKE`, `MEMORY_TIP`, `MARGIN_NOTE` and `RECALL` field was
  checked against THIS card's own question, not a sibling's — none carried a stray value left over
  from another card in its family.
- **`MARK_SPLIT` sums to `marks_total` on all 25 cards** (4 for every SAQ, 2 for every VSAQ), and
  every step's own `marks=` running total lines up with its `MARK_SPLIT` entry and `MARGIN_NOTE`.
  The two documented split-shape differences — `ts_ipe_m2a_pc_saq_sum_four_digit_numbers_0_2_4_7_8`
  runs 1+1+2 instead of its twin's 1+2+1 because it carries an extra correction step, and the two
  telescoping SAQs run 1+1+2 rather than the bank's general 1+2+1 four-mark shape — are both
  explained in-card and internally consistent, not unexplained drift.
- **This group has no LAQ cards** (all 12 are SAQ at 4 marks, all 13 are VSAQ at 2 marks), so the
  task brief's "long answers are worth 7 marks, not 8" caveat for this pre-reform 75-mark paper does
  not apply to anything in this file.
- **No `FIGURE` lines exist in this group** (pure algebra/counting unit) — the figures check (brief
  item 5) is not applicable.
- **No Rule-41 idiom/metaphor/personification found** in any `WHY`/`MISTAKE`/`MEMORY_TIP`/
  `MARGIN_NOTE`/`RECALL` string (grepped for common idiom patterns across all 25 cards; the only
  hits were in `VERIFICATION_NOTE` provenance text, which is not reader-facing).
