# M2A-09 — Mathematics-2A, Unit 6 Binomial Theorem (part 2/2)

Cards audited: 17 / 17   Findings: HARMFUL 0 · WRONG 4 · WEAK 1

Every boxed answer was independently re-derived (by hand, then checked with one Python script —
`fractions`/`math.comb` — covering both identities, the product-of-C-sums identity for n=3–7, a
term-ratio numeric summation of the infinite series to convergence, and every combinatorial/power
value used in the 14 VSAQ cards). All 17 boxed answers are correct. All findings below are in
explanatory prose (`MISTAKE`/`MARGIN_NOTE`/`MEMORY_TIP`), never a printed `LINE` or boxed answer.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_bt_series_3_by_4_8_alternating_sum | s1_rewrite | `margin_note` opens "Each denominator is a product like 5·10·15 = 5³·(1·2·3), so every term becomes (a product)/(k!) times (1/4)ᵏ." This series' denominators are all multiples of 4 (4·8, 4·8·12, 4·8·12·16…) — the very next clause ("(1/4)ᵏ") and this step's own `memory_tip` ("4·8·12 = 4³·3!") both use base 4. The 5·10·15 example is unrelated to this card and self-contradicts within the same sentence. | "Each denominator is a product like 4·8·12 = 4³·(1·2·3), so every term becomes (a product)/(k!) times (1/4)ᵏ." (drop the 5·10·15 example; the rest of the note is unchanged and correct.) |
| ts_ipe_m2a_bt_vsaq_middle_terms_4x2_5x3 | s2_evaluate | `mistake[0]`: "Adding the powers as 2·9 + 3·8 = 40." 2·9+3·8 = 42 (verified), which is the CORRECT power of x for T₉ — it matches this same step's own `margin_note` ("2·9 + 3·8 = 42") and the boxed line's x⁴². The mistake entry condemns a correct computation and states a false numeric result for it. | Replace with a real mistake, e.g. "Computing T₉'s power as 9 + 8 = 17 by forgetting to multiply by the exponents 2 and 3 in 4x² and 5x³ (the correct power is 2·9 + 3·8 = 42)." |
| ts_ipe_m2a_bt_vsaq_equal_coefficients_find_r | s2_evaluate | `mistake[1]`: "Adding the indices as 5r + 6 = 21 and getting r = 3, then writing 5r = 21." 5r = 21 gives r = 21/5 = 4.2 (verified), not r = 3 — the sentence claims a wrong intermediate step ("5r = 21") produces the right answer, which is false. | "Writing 5r = 21 (forgetting to subtract the 6 from both sides) instead of 5r = 15, giving the non-integer r = 21/5." |
| ts_ipe_m2a_bt_vsaq_c0_2c1_4c2_3n | s1_setup | `mistake[1]`: "Trying to prove it by induction." Induction is a mathematically valid alternative proof of this identity (via Pascal's rule Cₖ + Cₖ₋₁ = C_{n+1,k}) — listing it as a mistake condemns correct alternative working, the pattern the brief flags as the bank's commonest defect. | Replace with a genuine error, e.g. "Substituting x = −1 instead of x = 2, which proves C₀ − C₁ + C₂ − … = 0, not this identity." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_bt_vsaq_6th_term_2x_by_3_3y_by_2 | s1_setup | `memory_tip` ends "the index runs one behind" — personifies the index r (Rule 41). | "The 6th term is Tᵣ₊₁ with r = 5, not 6 — the term number is always one more than r." |

## Teacher-gate questions

Would TSBIE markers give full credit for an induction proof of C₀+2C₁+4C₂+…+2ⁿCₙ = 3ⁿ, or does
the mark scheme reward only the (1+x)ⁿ-substitution method the two steps name? (Bears on the
`ts_ipe_m2a_bt_vsaq_c0_2c1_4c2_3n` finding above — the underlying math is not in question, only
whether "induction" belongs in a `mistake` list at all for a 2-mark, 4-minute VSAQ.)

## Cards checked clean

ts_ipe_m2a_bt_odd_even_terms_p_q, ts_ipe_m2a_bt_product_c_sums_n_plus_1_power_n, ts_ipe_m2a_bt_vsaq_7th_term_4_by_x3, ts_ipe_m2a_bt_vsaq_4th_term_from_end_2a_5b, ts_ipe_m2a_bt_vsaq_middle_term_3x_by_7_2y, ts_ipe_m2a_bt_vsaq_term_independent_x_sqrt, ts_ipe_m2a_bt_vsaq_coefficient_x11_2x2, ts_ipe_m2a_bt_vsaq_largest_coefficient_24_19, ts_ipe_m2a_bt_vsaq_largest_22cr_find_13cr, ts_ipe_m2a_bt_vsaq_number_of_terms_trinomial, ts_ipe_m2a_bt_vsaq_validity_3_minus_4x, ts_ipe_m2a_bt_vsaq_validity_2_plus_3x

(12 clean; the other 5 carry the findings above.)

## Notes

- **Machine checks run** (one script, `fractions`/`math.comb`, no heredoc): P² − Q² = (x² − a²)ⁿ
  and 4PQ = (x+a)²ⁿ − (x−a)²ⁿ for n ∈ {3,4,5,7} across three (x,a) pairs each; the product identity
  (C₀+C₁)(C₁+C₂)…(Cₙ₋₁+Cₙ) = ((n+1)ⁿ/n!)·C₁…Cₙ for n = 3–7; the infinite alternating series summed
  via its term-to-term ratio to convergence (57 terms) confirming S = √(2/3) − 3/4 to machine
  precision, and each of the three printed terms (3/(4·8), (3·5)/(4·8·12), (3·5·7)/(4·8·12·16))
  matched the general-term formula exactly; every VSAQ combinatorial value (¹⁴C₆=3003, ⁸C₃=56,
  ¹⁰C₅=252, ¹⁷C₈=¹⁷C₉=24310, ¹⁰C₂=45, ¹³C₃=286⇒coefficient 7907328, ²⁴C₁₂=2704156,
  ¹⁹C₉=¹⁹C₁₀=92378, ¹³C₁₁=¹³C₂=78, ⁹C₅=126⇒189) and every x-power/validity-interval computation
  matched the card's boxed answer.
- **Hypotheses carried correctly**: both validity cards (`..._validity_3_minus_4x`,
  `..._validity_2_plus_3x`) state the open interval from `|y| < 1` and explicitly decline to claim
  divergence at the endpoints ("What the series does at |y| = 1 is outside this syllabus…") — the
  correct, careful hypothesis-scoped statement the brief's method step 3 asks to check for.
- **`mark_split` sums and label pairing**: all 17 cards — every `MARK_SPLIT` label matches its
  step's own label, every step's marks match the running-total annotation on its header, and every
  card's marks sum to its `marks_total` (7 for the three LAQ cards, 2 for the fourteen VSAQ cards).
  `expected_time_min` is uniform within each type (12 for LAQ, 4 for VSAQ) — in line with the
  group's convention.
- **Pattern worth flagging**: two of the four WRONG findings (cards `..._middle_terms_4x2_5x3` and
  `..._equal_coefficients_find_r`) share the same defect shape — a `mistake[i]` entry embeds an
  arithmetic equation that is simply false as printed (2·9+3·8 ≠ 40; 5r=21 does not give r=3),
  in both cases directly contradicted by the SAME step's own `margin_note` or boxed line. Worth a
  bank-wide sweep for other `mistake` entries whose printed arithmetic doesn't check out.
- No `INSIDER_NOTE` in this group was found false; all three LAQ cards' insider notes and all
  fourteen VSAQ insider notes were checked against their card's own lines/boxed answer and hold.
- No `RECALL accept`/`reject` list was found that would reject a correct paraphrase or accept a
  wrong one — all `reject` entries are generic non-answers ("write the formula", "simplify",
  "that is the answer"), never a specific wrong value.
