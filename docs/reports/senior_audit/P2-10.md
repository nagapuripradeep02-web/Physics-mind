# P2-10 — physics_2, Unit 10 Alternating Current (part 1/1)

Cards audited: 11 / 11   Findings: HARMFUL 0 · WRONG 0 · WEAK 2

Every formula was re-derived with one sympy script: Nₛ = Vₛ Nₚ/Vₚ = 100 (and the "upside-down"
mistake line independently checked to give Nₛ = 1, matching what the card itself claims that error
produces); XL = ωL and XC = 1/ωC dimension-checked to Ω; V = V₀ sin ωt integrated/differentiated
through R, L, C confirms I in phase (R), lagging by π/2 (L), leading by π/2 (C); cos φ = R/Z with
Z = √(R²+X²) ≥ R proves φ ∈ [0°, 90°) — the exact claim used to justify "cos φ = 1 ⇒ φ = 0° and no
other value"; the wattless-component power split (V·I cos φ · cos 0 + V·I sin φ · cos 90°) confirms
the sin φ term is identically zero; Z = √(R²+(ωL−1/ωC)²) minimized gives ω = 1/√(LC) and Zₘᵢₙ = R;
and eₛ = −Nₛ dφ/dt, eₚ = −Nₚ dφ/dt divide to vₛ/vₚ = Nₛ/Nₚ, with iₛvₛ = iₚvₚ rearranging to
vₛ/vₚ = iₚ/iₛ, both matching the card's boxed line. Also grepped every card's JSON for "50 Hz" /
"230 V" / "220 V" / "60 Hz" / "110 V" / "120 V" — none present; the bed-lamp card explicitly
declines to state a mains number, and no other card in the unit names one, so this chapter's
country-specific risk (flagged in the brief) does not fire here.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

_(none)_

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_ac_transformer_turns_numerical | s2_result / card | `MARK_SPLIT` names the second step "Substitution and result", but the step's own `label` is "Substitute and solve" — the only label mismatch in the group (all other 10 cards, including the 4-part `transformer_principle_working`, have exact `mark_split`↔step-label matches). | Change `mark_split`'s second entry to "Substitute and solve" (or rename the step to "Substitution and result" — either way, make the two strings identical). |
| ts_ipe_p2_ac_wattless_current | s2_component | `MEMORY_TIP`: "cos carries, sin sits idle: I cos φ carries the power, I sin φ is wattless." — "sits idle" personifies the current component (Rule 41a); the physics itself is correct. | "cos carries, sin is wattless: I cos φ carries the power, I sin φ is wattless." |

## Teacher-gate questions

none

## Cards checked clean

ts_ipe_p2_ac_bed_lamp_transformer, ts_ipe_p2_ac_transformer_phenomenon, ts_ipe_p2_ac_transformer_ratio, ts_ipe_p2_ac_reactance_expressions, ts_ipe_p2_ac_phase_difference_r_l_c, ts_ipe_p2_ac_power_factor_define, ts_ipe_p2_ac_lcr_minimum_impedance, ts_ipe_p2_ac_unity_power_factor_phase, ts_ipe_p2_ac_transformer_principle_working

(9 fully clean; the other 2 carry only the WEAK findings above — their physics content is otherwise correct.)

## Notes

- **This group is unusually clean.** All 11 boxed answers/final relations re-derived correct; zero
  false `MISTAKE` entries found anywhere (every `MISTAKE[i]` in the group condemns a genuinely wrong
  line — none of them punish a valid alternative form, e.g. the "upside-down ratio" mistake in
  `transformer_turns_numerical` was checked numerically and does give a different, wrong answer
  (Nₛ = 1 vs the true 100), so it is a real error, not a false condemnation).
- **`INSIDER_NOTE` (10/11 cards; qualitative cards named it explicitly, the numerical one too) — all
  ten checked true and internally consistent with their card's own `mark_split`/steps.** None
  overclaim a mark, none contradict the boxed answer.
- **`RECALL accept`/`reject` lists are all correctly scoped per-step** — every `reject` entry names
  content that belongs to a *different* step of the same card (or is a genuinely wrong physics
  statement, e.g. `unity_power_factor_phase` s1 rejects "ninety degrees" which is simply false),
  never a correct answer to the step it's attached to.
- **`transformer_principle_working` (the one SAQ, 4 marks, 5 steps incl. a 0-mark figure step)**:
  running marks 0→1→1→2→3→4 sum correctly to `marks_total = 4`; all four `mark_split` labels match
  their steps exactly; the figure's stroke coordinates were traced by hand — primary coil/leads
  close into a loop through the a.c.-source symbol, secondary coil/leads close into a loop through
  the load box, and the lamination strokes sit in the core's top yoke — a physically sensible,
  self-consistent topology (no wrong-structure labels, no dangling leads) as far as coordinate
  geometry alone can establish without rendering the SVG.
- **Considered and NOT filed**: card 4's `MISTAKE[0]` on the emf-ratio step ("holds for the ideal,
  100% efficient case") conflates two textbook idealizations (no winding resistance/leakage vs. no
  core loss) that a rigorous treatment would separate — but NCERT-level physics treats "ideal
  transformer" as bundling both, matching `transformer_principle_working`'s own derivation, so this
  is standard textbook register, not an error. Card 6's "ELI the ICE man" mnemonic was weighed
  against Rule 41 (personification) and kept clean — it is a real, standard, widely-used AC-circuits
  mnemonic naming a device rather than attributing agency to a physical quantity, unlike "sits idle"
  above.
