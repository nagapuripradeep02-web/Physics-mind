# P2-14 — physics_2, Unit 14 Nuclei (part 1/1)

Cards audited: 26 / 26   Findings: HARMFUL 0 · WRONG 0 · WEAK 3

One Python script recomputed every number in the group: E = uc² for 1 u (931.54 MeV), the cube-root
radius ratio (27/8)^(1/3) = 3/2, ln 2 = 0.6931 for T = 0.693/λ, E = mc² for 1 μg (9×10⁷ J), the
radium half-life count (n = 3, t = 4800 y, cross-checked against N = N₀e^(−λt)), atom-number and
charge-number balance on all 12 nuclear equations in the group (Bothe–Becker ×2, neutron β-decay,
the 6-reaction CN cycle + its net, the 4-reaction p-p cycle + its net), the p-p cycle energy sum
(26.72 MeV), kT at 300 K (0.0259 eV, confirming eV not MeV), and every mark_split sum against its
card's marks_total. All passed. Every MARGIN_NOTE mark-ordinal ("First mark" … "Sixth mark") was
checked against its step's running total; all 26 cards are internally correct.

All five source-book errors named in the brief were checked and are correctly repaired, consistently
across every card that touches them: **K > 1 for "controlled"** → both `..._multiplication_factor_controlled_uncontrolled`
and `..._chain_reaction_and_multiplication_factor` correctly print K = 1 controlled / K > 1
uncontrolled, and `..._control_rods_role` / `..._nuclear_reactor_principle_working` both hold K at 1
for the reactor — no card anywhere in the group prints K > 1 for a controlled reaction.
**"fusion" for "fission"** → `..._chain_reaction_and_multiplication_factor` correctly prints FISSION
for the 3→9→27 neutron-growth sequence. **Beryllium as a control-rod absorber** →
`..._control_rods_role` correctly drops beryllium and names cadmium/boron only (physically right:
beryllium is a moderator/reflector, not an absorber), and `..._nuclear_reactor_principle_working`
independently and consistently uses cadmium/boron for the rods and heavy water/graphite for the
moderator. **Thermal neutrons at 0.025 eV, not MeV** → both `..._thermal_neutrons` and
`..._moderator_function` correctly print eV (kT ≈ 0.026 eV at 300 K, confirmed above).
**"1u = 931.5 MeV"** → `..._amu_equivalent_energy` deliberately writes "1 u ≡ 931.5 MeV" (≡, not
the book's =) after first deriving it as "a mass of 1 u is equivalent to the energy E = …", and its
own MISTAKE entry explicitly warns against saying mass and energy are identical — a real, working
fix, not a copy of the book's error. The CN cycle is credited "(suggested by Bethe)"; nowhere does
"Bothe" appear except correctly, for Bothe and Becker's 1930 beryllium-radiation observation in the
neutron-discovery card — the two names are never confused.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

_(none)_

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_nuc_neutrons_best_projectiles | s2_charged_repelled | `memory_tip`: "Charged bullets must climb a hill; the neutron walks in on level ground." Personifies the projectiles (climbing, walking) — Rule 41a bans metaphor/personification. | "A charged particle must first cross the Coulomb barrier; a neutron meets no such barrier." |
| ts_ipe_p2_nuc_chain_reaction_definition | s1_mechanism | `memory_tip`: "Fission needs a neutron and makes three — that is why it keeps itself alive." Personifies the reaction as a living thing. | "Fission needs a neutron and makes about three — that is why the reaction is self-sustained." |
| ts_ipe_p2_nuc_chain_reaction_and_multiplication_factor | s2_definition | `memory_tip`: "Fission needs a neutron and makes neutrons — the reaction feeds itself." Personifies the reaction ("feeds itself") — same defect as the sibling card above, repeated. | "Fission needs a neutron and makes neutrons — the reaction is self-sustained." |

## Teacher-gate questions

- `ts_ipe_p2_nuc_energy_from_one_microgram_u235` and `ts_ipe_p2_nuc_radium_half_life_1600_years` each
  carry a stronger self-flag than the group's standard invented-mark-split boilerplate: both
  `VERIFICATION_NOTE`s say the 2+2+2+2 split for what is really "three or four lines of arithmetic"
  "needs the teacher pass more than any other card in this chapter," and that authoring them as
  8-mark LAQs (because the book prints them under Long Answer numbering with no Problems banner)
  is itself a judgment call. Both are mathematically correct (9×10⁷ J; 4800 years) — the open
  question is purely whether a Telangana IPE teacher agrees these are 8-mark questions at all, not
  whether the physics is right.
- No chemistry-style naming-convention ambiguity applies to this chapter; no other teacher-gate items
  found.

## Cards checked clean

ts_ipe_p2_nuc_isotopes_isobars, ts_ipe_p2_nuc_isotones_isomers, ts_ipe_p2_nuc_amu_equivalent_energy, ts_ipe_p2_nuc_ratio_of_nuclear_radii, ts_ipe_p2_nuc_disintegration_constant_units_dimensions, ts_ipe_p2_nuc_neutrons_cannot_ionize, ts_ipe_p2_nuc_thermal_neutrons, ts_ipe_p2_nuc_multiplication_factor_controlled_uncontrolled, ts_ipe_p2_nuc_control_rods_role, ts_ipe_p2_nuc_becquerel_and_curie, ts_ipe_p2_nuc_moderator_function, ts_ipe_p2_nuc_delayed_neutrons, ts_ipe_p2_nuc_thermonuclear_reactions, ts_ipe_p2_nuc_discovery_of_neutron, ts_ipe_p2_nuc_properties_of_neutron, ts_ipe_p2_nuc_nuclear_forces_properties, ts_ipe_p2_nuc_half_life_and_decay_constant, ts_ipe_p2_nuc_fission_vs_fusion, ts_ipe_p2_nuc_radioactive_decay_law_exponential, ts_ipe_p2_nuc_nuclear_reactor_principle_working, ts_ipe_p2_nuc_stellar_energy_carbon_nitrogen_proton_proton, ts_ipe_p2_nuc_energy_from_one_microgram_u235, ts_ipe_p2_nuc_radium_half_life_1600_years

(23 clean; the other 3 carry the WEAK findings above.)

## Notes

- **Physics/mathematics in this group is fully sound.** Every boxed answer, every printed LINE, and
  every reaction equation re-derives correctly; the numeric script above found zero discrepancies.
- **The reactor figure** in `ts_ipe_p2_nuc_nuclear_reactor_principle_working` (`s2_diagram`) was left
  untouched per instructions — its label collisions are tracked separately. Its prose (WHY, MISTAKE,
  MEMORY_TIP, MARGIN_NOTE, RECALL) was checked and is consistent with the figure's five named parts
  (core/fuel rods, control rod, moderator, coolant, shielding); the MARGIN_NOTE lists all five even
  though the WHY sentence discusses only three (fuel rods, moderator, control rod) — not a factual
  error, just a narrower explanatory scope, not filed.
- **No MISTAKE entry condemns correct physics** anywhere in the group — the commonest defect class
  named in the brief does not occur here. Every MISTAKE checked is a genuine error a student could
  make (wrong unit, swapped definition, dropped sign, wrong exponent, etc.).
- **No RECALL `accept`/`reject` item is wrong** — every accept phrase matches the card's own correct
  content, and every reject phrase is content that belongs to a sibling step, not a correct answer to
  this one.
- **No prose is falsely cloned from a sibling.** Overlapping cards (`chain_reaction_definition` vs
  `chain_reaction_and_multiplication_factor`; `multiplication_factor_controlled_uncontrolled` vs
  `chain_reaction_and_multiplication_factor`; `moderator_function` vs `nuclear_reactor_principle_working`;
  `neutrons_cannot_ionize`/`neutrons_best_projectiles` vs `properties_of_neutron`) were compared
  line-for-line; all agree with each other and with each card's own numbers.
- **mark_split labels and sums, and margin_note mark-ordinals, are all internally correct** across
  all 26 cards (2/2/2/2/2/2/2/2/2/2/2/2/2/2/2 for the 15 VSAQs, 4/4/4/4/4/4 for the 6 SAQs, 8/8/8/8/8
  for the 5 LAQs) — checked mechanically against each card's `marks_total` and each step's running
  total.
- The Telugu phrases in `recall_prompt`/`recall.accept` were left alone, per the group's own bank-wide
  scope convention (confirmed in the maths-audit template) — outside this audit's reader-facing scope.
