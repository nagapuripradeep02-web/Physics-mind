# P2-07 — physics_2, Unit 7 Moving Charges and Magnetism (part 1/1)

Cards audited: 16 / 16   Findings: HARMFUL 0 · WRONG 0 · WEAK 0

Re-derived every formula and both numericals independently in Python (numpy for the two numericals,
sympy for the cyclotron period/frequency/KE identities), and — because of the background flag on this
unit — extracted the actual stroke coordinates of the two figures that claim a specific field-out-of-plane
direction and computed the real cross product from them, rather than trusting the card's prose.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

_(none)_

## WEAK — quality, register, Rule 41

_(none)_

## Teacher-gate questions

none

## Cards checked clean

ts_ipe_p2_mcm_oersted_experiment_importance, ts_ipe_p2_mcm_circular_coil_magnetic_moment, ts_ipe_p2_mcm_force_on_conductor_in_field, ts_ipe_p2_mcm_force_on_charged_particle, ts_ipe_p2_mcm_ammeter_vs_voltmeter, ts_ipe_p2_mcm_galvanometer_to_ammeter, ts_ipe_p2_mcm_galvanometer_to_voltmeter, ts_ipe_p2_mcm_moving_coil_galvanometer_principle, ts_ipe_p2_mcm_biot_savart_law, ts_ipe_p2_mcm_amperes_law_state_prove, ts_ipe_p2_mcm_cyclotron_components_uses, ts_ipe_p2_mcm_field_long_straight_conductor, ts_ipe_p2_mcm_coil_field_and_moment_numerical, ts_ipe_p2_mcm_parallel_wires_force_numerical, ts_ipe_p2_mcm_force_conductor_and_parallel_wires, ts_ipe_p2_mcm_torque_on_loop_and_galvanometer

## Notes

- **The background-flagged correction is confirmed right, not harmful.** `ts_ipe_p2_mcm_force_conductor_and_parallel_wires`'s `VERIFICATION_NOTE` says the source book drew F in the plane of the page even though the current and B are both drawn in that plane, and that this card corrects it to F = ⊙ (out of the page). I pulled the actual stroke coordinates for the wire (`i_arrowhead`, tip at (282,149)) and the field (`b_line`/`b_arrowhead`, tip at (250,180)) from the figure text, converted to a right-handed math frame, and computed l × B numerically: z-component +12740 → out of the page. The card's ⊙ is correct; the correction went the right way. I ran the same check on the Biot–Savart figure in `ts_ipe_p2_mcm_biot_savart_law` (`dl × r`, current drawn upward, P to the right and below), which the card separately claims gives dB into the page (⊗): z-component −4300 → into the page, also confirmed correct. Script: `verify.py` in the scratch folder.
- **Both numericals recomputed and match to the stated precision.** `coil_field_and_moment_numerical`: B = μ₀ni/2r = 2.0106×10⁻³ T ≈ 2×10⁻³ T; M = niπr² = 10.053 ≈ 10 A·m². `parallel_wires_force_numerical`: F/l = μ₀i₁i₂/2πr = 2×10⁻⁵ N/m exactly. The insider_note's claim that leaving r = 10 (cm, unconverted) in the field formula gives an answer wrong by a factor of 100 is itself arithmetically correct (100/0.2 vs 320/20 — ratio is exactly 100).
- **Cyclotron formulas verified symbolically** (sympy): half-period πm/(qB) from πr/v with r = mv/qB; frequency f = qB/2πm; KE at the exit radius = q²r²B²/2m. All match the card.
- **Every MARK_SPLIT sums to marks_total, and every non-diagram step's running mark total matches its own MARGIN_NOTE ordinal** ("First mark" / "Second mark" / … / "Last mark", including the two 2-mark steps — `s5_torque` "Two marks" running 3→5 and `s9_final` "Last two marks" running 6→8) across all 16 cards, including the two 8-mark LAQ cards with 7 marked sub-steps each. No 0-mark diagram step names a mark. `expected_time_min` is uniform within its qtype band (VSAQ 4 / SAQ 8 / LAQ 15) with no outlier.
- **No MISTAKE entry condemns correct physics** — checked all 68 MISTAKE items in the group; every one flags a genuine error (wrong angle reference, wrong trig function, dropped constant, swapped resistance value, wrong direction, etc.), including subtle-but-correct distinctions the bank gets right (e.g. "the forces on the b-sides are not zero, they cancel"; "a radial field gives maximum torque, not maximum flux — the flux is zero"; the note that B = μ₀i/2πr used inside the Ampère's-law proof comes from Biot–Savart, not from Ampère's law itself, so the derivation is not circular).
- **RECALL reject lists never reject content required by their own step** — several reject entries are true statements, but in every case they are the content of a *different* step of the same card (e.g. rejecting "deflection proportional to current" in the step that only earns the torque mark), which is this bank's established convention for keeping per-step recall on-topic, not a defect.
- **Sibling pairs checked for cross-contamination**: `galvanometer_to_ammeter` vs `galvanometer_to_voltmeter` (low-resistance-parallel vs high-resistance-series kept correctly separate throughout, including MISTAKE/MEMORY_TIP/RECALL) and `force_on_conductor_in_field` vs `force_on_charged_particle` (i·l vs q·v kept correctly separate) — no swapped or cloned-wrong content found.
- Did not flag ordinary mnemonic phrasing ("a shunt is a low-resistance side road in parallel", "a brake in the line") as Rule 41 — these are teaching analogies for circuit topology, not personification giving a force or equation agency/desire, and are consistent throughout the unit's memory tips.
- Per instructions, the torque-on-loop figure's missing b/b sin θ labels in `ts_ipe_p2_mcm_torque_on_loop_and_galvanometer` were not re-reported; its prose fields were audited and are clean.
