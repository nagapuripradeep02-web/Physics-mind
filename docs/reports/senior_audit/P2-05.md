# P2-05 — physics_2, Unit 5 Electric Potential and Capacitance (part 1/1)

Cards audited: 17 / 17   Findings: HARMFUL 0 · WRONG 0 · WEAK 2

Solved every card independently before reading its steps, then mechanically re-derived every
symbolic and numeric result in one sympy script: the point-charge potential integral (W = qq₀/4πε₀r,
V = q/4πε₀r), the dipole-energy rotation integral (W = pE(cosθ₁ − cosθ₂), U = −pE cosθ from θ₁ = 90°),
the parallel-plate C = ε₀A/d (+ Kε₀A/d), the average-V energy derivation and its Q²/2C integral form,
and every numeric card (2/3/4 pF parallel at 100 V → 200/300/400 pC, three 9 pF in series at 120 V →
C = 3 pF, Q = 360 pC, V = 40 V each, including both named wrong-path checks: 9 pF×120 V = 1080 pC and
360 pC/3 pF = 120 V) and the dielectric energy-with-battery pair (U₀/K disconnected, KU₀ connected).
All matched the cards exactly — zero arithmetic or algebraic error anywhere in the group. `mark_split`
labels/sums/step-label pairing were checked on all 17 cards (all correct); figure stroke coordinates
were checked against their physics (field directions, induced-charge faces, torque-arm construction,
O–P–B–A integration-path geometry) — all consistent.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

_(none)_

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_epc_potential_zero_intensity | s2_example | `memory_tip`: "Hollow sphere: the field stops at the surface, the potential walks in unchanged." — "walks in" personifies the potential (Rule 41a: no personification of an abstract physical quantity performing a human action). | "Hollow sphere: the field ends at the surface; the potential inside stays equal to the surface value." |
| ts_ipe_p2_epc_three_capacitors_parallel_ratio | s2_pds | `memory_tip`: "Parallel means the same two terminals for all — one V for everyone." — "everyone" is a word reserved for people; applying it to capacitors is personification (Rule 41a). Its own sibling cards in this group phrase the identical idea correctly (`..._parallel_100v` s2: "Parallel: one V for all, the charge splits"; `parallel_combination_derive` s1: "Parallel: one V for all, the Q splits"). | "Parallel means the same two terminals for all — one V for every capacitor." |

## Teacher-gate questions

None beyond what every card already discloses itself: all `mark_split` weightings in this group are
INVENTED per `VERIFICATION_NOTE` and remain a claim until a Telangana IPE teacher confirms them — this
is a standing, bank-wide caveat, not a new finding from this audit.

## Cards checked clean

ts_ipe_p2_epc_intensity_zero_potential, ts_ipe_p2_epc_equipotential_surfaces, ts_ipe_p2_epc_field_perpendicular_equipotential, ts_ipe_p2_epc_three_capacitors_series_ratio, ts_ipe_p2_epc_capacitance_area_doubled, ts_ipe_p2_epc_potential_point_charge_derive, ts_ipe_p2_epc_dipole_potential_energy_derive, ts_ipe_p2_epc_parallel_plate_capacitance_derive, ts_ipe_p2_epc_series_combination_derive, ts_ipe_p2_epc_parallel_combination_derive, ts_ipe_p2_epc_energy_stored_capacitor, ts_ipe_p2_epc_three_capacitors_parallel_100v, ts_ipe_p2_epc_dielectrics_in_electric_field, ts_ipe_p2_epc_three_capacitors_series_120v, ts_ipe_p2_epc_energy_stored_with_dielectric

(15 clean; the other 2 carry the findings above — each carries exactly one WEAK item, everything
else on both cards checked clean.)

## Notes

- **This is the strongest group audited so far in this pass**: 17/17 cards mathematically and
  physically correct, zero `MISTAKE` items condemning correct alternative working (the commonest
  defect class per the brief), zero prose cloned falsely from a sibling, zero self-contradicting
  card, zero `MARGIN_NOTE` naming a mark its step does not earn, zero `RECALL` rubric that would
  reject a correct answer or accept a wrong one.
- **The sibling-pair RECALL design is intentional, not a bug.** In the two zero-intensity/
  zero-potential cards and the two field-perpendicular/equipotential steps, each step's `reject`
  list is deliberately either (a) its own card's OTHER step's content (so a bare repeat of the
  wrong step doesn't earn credit) or (b) the sibling card's wrong example (hollow conductor vs.
  dipole equatorial line) — verified correct in every direction, not a cross-card contamination.
- **Every series/parallel WHY and MEMORY_TIP pair (arrangement, charges-vs-voltages, combination
  formula) was checked for accidental swap between the series and parallel derive cards and the two
  numeric cards** — all four consistently keep "series → common Q, V splits" and "parallel → common
  V, Q splits" the right way round, including in the worked-example mistake illustrations (e.g. the
  1080 pC and 120 V wrong-path numbers in the series-120V card are the actual results of the named
  wrong method, confirmed by script).
- **Figures**: all seven diagrammed steps were checked geometrically against their physics — the
  point-charge O–P–B–A integration path (B closer to O than A, matching "moving toward O"), the
  dipole torque-arm construction (vertical dashed segment between the two parallel force-lines
  correctly equals 2a sinθ), the parallel-plate and dielectric-slab field arrows (pointing + → −,
  induced field opposing applied field, induced charge signs on the correct faces), and the series/
  parallel capacitor-bank circuit sketches (chain vs. ladder) — all correct, no mislabeled structure.
- **`expected_time_min`** follows a clean 2 min/mark convention for all 16 VSAQ/SAQ cards (4 min for
  2-mark, 8 min for 4-mark); the one LAQ (`energy_stored_with_dielectric`, 8 marks) is listed at 15
  min rather than the linearly-extrapolated 16 — with no second LAQ in this group to compare against,
  this 1-minute gap is not filed as a finding, only noted.
- Two `MEMORY_TIP` personification instances (above) are the only Rule 41 issues found; every other
  `WHY`/`MISTAKE`/`MEMORY_TIP`/`MARGIN_NOTE` in the group reads as plain, literal textbook English.
