# P2-04 — physics_2, Unit 4 Electric Charges and Fields (part 1/1)

Cards audited: 13 / 13   Findings: HARMFUL 0 · WRONG 1 · WEAK 6

Re-derived every result independently (sympy + numeric Coulomb-vector sums, one script,
`scratchpad\P2-04\verify.py`): n = 1/(1.6×10⁻¹⁹) = 6.25×10¹⁸; F(d/2)=4F, F(2d)=F/4; k = 1/4πε₀ =
8.9876×10⁹ ≈ 9×10⁹; the axial-field algebra (r+a)²−(r−a)²=4ra and the full simplification to
2pr/(r²−a²)²; a direct two-charge Coulomb vector sum at an equatorial point confirming the
perpendicular components cancel and the axial components give p/(r²+a²)^³⁄² (matches the card's
cosθ = a/√(r²+a²) derivation exactly); axial/equatorial short-dipole ratio = 2, as claimed. Also
checked every `mark_split` sum against `marks_total`, every step's running total, and every
"First/Second/Third/Last mark" `margin_note` against its step's actual mark position — all 13 cards
clean on that front. Read every `FIGURE` stroke path and cross-checked coordinates numerically
(e.g. the dipole-couple figure's dashed "2a sin θ" segment measures exactly 120 px, matching
2a·sinθ computed from the drawn charge positions).

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_ecf_equatorial_plane_dipole_field | s2_figure | `FIGURE equatorial_dipole_field` draws the E₁ arrow with both a shaft and a head (`stroke e1` + `stroke e1_head`), but the E₂ arrow has ONLY `stroke e2_head` — there is no `stroke e2` shaft line in the dump. The arrowhead's own geometry still points the right way (down-left, towards −q, matching the card's text "directed along PA, towards −q"; verified by direction cosines), but as authored the E₂ arrow would render as a floating chevron near (139,116) with no line connecting it back to P(170,70). | Add a shaft stroke pairing with the existing head, e.g. `stroke e2: M 170 70 L 138 121` (ending where `e2_head`'s tip already sits, mirroring `stroke e1: M 170 70 L 138 19`), so E₂ renders as a complete arrow from P towards −q. |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_ecf_gauss_law_and_importance | card (structure) | No step in this 4-mark SAQ is `kind=boxed_final` or carries a `[boxed]` line — s3_equation prints the same `∮ E · dS = q / ε₀` that the sibling card `ts_ipe_p2_ecf_gauss_law_statement` marks `[boxed]`/`boxed_final`, but here it is left as plain `[eq]`, and the card ends on two `kind=text` importance steps. Every other SAQ card in this group (coulombs_law, intensity_point_charge, couple_on_dipole, axial_line_dipole_field, equatorial_plane_dipole_field) ends with a `boxed_final` step. | Mark s3_equation's line `[boxed] ∮ E · dS = q / ε₀` (and its step `kind=boxed_final`) to match the group's own convention and its sibling card — or, if a multi-part law-plus-importance answer is deliberately left without a single boxed takeaway, that should be a stated exception, not a silent gap. |
| ts_ipe_p2_ecf_charge_quantized | s1_meaning | `MEMORY_TIP`: "Charge is counted, not poured — it comes in whole steps of e." "Poured" is a metaphor (Rule 41) — charge is not literally poured in any other context, and an ESL Class-11 reader would need it explained. | "Charge is discrete, not continuous — it comes in whole steps of e." |
| ts_ipe_p2_ecf_repulsion_sure_test | s1_attraction | `MEMORY_TIP`: "Attraction has two suspects — an opposite charge, or no charge at all." "Suspects" personifies the two physical causes as a detective-story device (Rule 41 metaphor/personification). | "Attraction has two causes — an opposite charge, or an uncharged body by induction." |
| ts_ipe_p2_ecf_repulsion_sure_test | s2_repulsion | `MEMORY_TIP`: "Repulsion has one suspect only — like charge on both bodies." Same metaphor repeated. | "Repulsion has only one cause — like charge on both bodies." |
| ts_ipe_p2_ecf_repulsion_sure_test | card (insider_note) | `INSIDER_NOTE`: "...the one the book buries..." — "buries" is a metaphor for "understates/places briefly" (Rule 41). This is spoken first to the student by the chatbot. | "...the one the book states only briefly..." |
| ts_ipe_p2_ecf_gauss_law_statement | s1_statement | `MEMORY_TIP`: "Closed surface, net charge, enclosed — the three words the mark hangs on." "Hangs on" is an idiom for "depends on" (Rule 41). | "...the three words the mark depends on." |

## Teacher-gate questions

None. No board-convention ambiguity, alternate-notation dispute, or curriculum-variant question
arose in this group — every discrepancy found was either a physics/derivation check (all passed)
or a Rule-41/structure issue resolvable without a teacher ruling.

## Cards checked clean

ts_ipe_p2_ecf_electrons_in_one_coulomb, ts_ipe_p2_ecf_weight_positively_charged,
ts_ipe_p2_ecf_field_lines_never_intersect, ts_ipe_p2_ecf_force_distance_halved_doubled,
ts_ipe_p2_ecf_coulombs_law, ts_ipe_p2_ecf_intensity_point_charge, ts_ipe_p2_ecf_couple_on_dipole,
ts_ipe_p2_ecf_axial_line_dipole_field

(8 fully clean; the other 5 carry the findings above.)

## Notes

- **Every boxed/final answer in the group is correct** — re-derived and machine-checked: Q=±ne,
  n=6.25×10¹⁸, 4F/F÷4, k=9×10⁹ N m² C⁻², F=(1/4πε)Q₁Q₂/r² in a medium, E=(1/4πε₀)Q/d² with unit
  N C⁻¹, τ=pE sinθ (=p×E), E_axial=(1/4πε₀)2p/r³ along p, E_equatorial=(1/4πε₀)p/r³ opposite to p
  (verified as exactly half the axial value at equal r, with the correct opposite sign), and both
  Gauss statements ∮E·dS=q/ε₀.
- **No `MISTAKE` entry in this group condemns physically or mathematically correct working** — the
  commonest defect class per the brief was searched for explicitly across all 26 `MISTAKE` items
  and none were found; every one genuinely names a wrong operation, wrong sign, wrong factor, or a
  genuinely incomplete answer.
- **No `RECALL reject` entry rejects a valid restatement of its own step's answer.** The group has a
  consistent, intentional design where each step's `reject` list is drawn from the *other* mark
  step's content (e.g. card `repulsion_sure_test` s1 rejects "repulsion only between like charges" —
  a true statement, but it is step 2's content, not step 1's) — this enforces per-step specificity
  and is not an error.
- **Figures**: all field/force arrow directions were checked against the physics and against each
  card's own text (axial E₁ due to +q pointing outward / E₂ due to −q pointing inward, matching
  magnitude ordering by arrow length; dipole-couple +qE and −qE correctly opposite; Gauss surface
  closed with charge inside and outward E through the patch). Only the one missing shaft above.
- **Book-typo corrections** (`consititute`→`constitute`, `seperated`→`separated`,
  `equitorial`→`equatorial`, `permitivity`/`permittivity of free spaced`→`permittivity of free
  space`, `neglisible`, `a electric dipole`→`an electric dipole`, capital P→lowercase p for dipole
  moment) were all spot-checked against the card's own printed lines and are consistently applied.
- **Book-error correction confirmed**: `axial_line_dipole_field`'s `VERIFICATION_NOTE` claims the
  source book's intermediate line carries an extra factor of q and drops a factor of 2; the note's
  own claimed correct grouping (q·4ra = 2(q·2a)r = 2pr) checks out algebraically and matches the
  card's own printed derivation, which is internally consistent end to end.
- Mark-split sums, step running totals, and every "First/Second/Third/Last mark" `margin_note`
  were checked against actual step mark positions for all 13 cards — all consistent (no repeat of
  the A01-style margin-note/mark mismatch here).
