# P2-03 — Physics-II, Unit 3 Wave Optics (part 1/1)

Cards audited: 10 / 10   Findings: HARMFUL 0 · WRONG 2 · WEAK 3

Re-derived every card from `QUESTION:` alone before reading its steps, then machine-checked the
algebra with one sympy/python script (`P2-03/verify.py`): Malus law limits, the Brewster identity
sin θₚ = μ sin(90°−θₚ) ⇒ μ = tan θₚ, Fresnel distance Zλ/a = a ⇒ Z = a²/λ, the full two-source
interference derivation (y-sum → R² → half-angle → I = 4I₀cos²(φ/2) → average = 1/2), the
three-polaroid identity I₀cos²θ·cos²(90°−θ) = (I₀/4)sin²2θ with its θ=45° maximum, and Snell's law
at r=90° giving sin C = μ₂/μ₁. All confirmed correct. Also numerically re-derived the incidence/
refraction angles implied by the `huygens_refraction_rarer` figure's own stroke coordinates
(i≈35°, r≈60°) to confirm the drawn geometry matches its own "r > i" claim.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_wop_polarization_of_light | s2_polaroid | `margin_note` states the second mark is earned by naming "the device (a polaroid) and the result (plane polarised light)," but the step's own `[boxed]` line — the line that stands as this step's graded, exam-sheet content — is "Vibrations restricted to one direction," which repeats step 1's general definition and names neither the polaroid nor plane polarised light. Every sibling VSAQ 2-step card in this group (`malus_law`, `brewsters_law`, `fresnel_distance`) boxes the step's OWN new content (a formula), not a restatement of step 1; this card is the outlier. | `[boxed] Unpolarised light through a polaroid → plane polarised light` (or similar wording naming the polaroid and the result), so the boxed line matches what `margin_note` says the mark requires. |
| ts_ipe_p2_wop_resolving_power_of_eye | card (`mark_split`) | `MARK_SPLIT` reads "The stripe pattern (1)" — but the step actually labelled "The stripe pattern" is `s1_figure`, a 0-mark diagram step. The step that carries mark 1 is `s2_pattern`, labelled only "The pattern." The mark-split entry therefore names the wrong step by its exact label. | Change the mark-split entry to "The pattern (1)" to match `s2_pattern`'s own label (or rename `s2_pattern`'s label to "The stripe pattern" and rename `s1_figure` to something distinct, e.g. "The figure"). |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_wop_polaroid_between_crossed_polaroids | s3_after_p2 | `MARK_SPLIT` reads "Intensity after P₂ (1)" but the step's own label is "Intensity after P₂ — Malus law" (extra suffix not present in the mark split). Unambiguous since no other step shares the base name, but it's label drift the brief's structure check calls out. | Either drop " — Malus law" from the step label, or add it to the `MARK_SPLIT` entry, so the two strings match exactly. |
| ts_ipe_p2_wop_interference_intensity_conditions | s3_amplitude | `MEMORY_TIP`: "Square and add kills α; the half-angle identity gives cos(φ/2)." — "kills" personifies/uses violent-metaphor register for a mathematical variable being eliminated (Rule 41a); the step's own `WHY` already says this correctly with "removes." | "Square and add removes α; the half-angle identity gives cos(φ/2)." |
| ts_ipe_p2_wop_total_internal_reflection_huygens | s4_beyond | `MEMORY_TIP`: "A sine cannot pass 1. When Snell's law asks for more, refraction stops and everything reflects." — "Snell's law asks for more" personifies the law (Rule 41a explicitly bans "answers/asks" for forces/equations). | "A sine cannot pass 1. When Snell's law would require sin r greater than 1, refraction stops and everything reflects." |

## Teacher-gate questions

None specific to this chapter's content or conventions. Every card in the group carries the same
group-wide disclaimer (already stated in each `VERIFICATION_NOTE`) that the mark split is an
INVENTED claim pending Telangana IPE teacher confirmation, because Physics-II has no first-year BLM
overlap and no Paper-I-only PYQ corpus coverage for back-testing — this is systemic to the whole
Physics-II paper, not an ambiguity local to Wave Optics, so it is not re-listed per card here.

## Cards checked clean

ts_ipe_p2_wop_malus_law, ts_ipe_p2_wop_brewsters_law, ts_ipe_p2_wop_fresnel_distance,
ts_ipe_p2_wop_doppler_effect_in_light, ts_ipe_p2_wop_energy_conservation_interference

(5 clean; the other 5 each carry one finding above — `interference_intensity_conditions`,
`polaroid_between_crossed_polaroids`, and `total_internal_reflection_huygens` are otherwise fully
correct derivations, each flagged for a single WEAK register/label item only.)

## Notes

- **All ten boxed final answers and every intermediate algebra line are correct** — confirmed
  independently (by hand and by sympy) for: Malus law and its θ=0°/90° limits; Brewster's
  μ = tan θₚ from Snell's law + perpendicularity; Fresnel distance a²/λ; the full y₁+y₂ →
  R cos α/R sin α → R² → half-angle → I = 4I₀cos²(φ/2) → maxima/minima derivation (5-step,
  4-mark SAQ); the energy-conservation average-intensity argument (Iₐᵥ = 2I₀ = I₀+I₀); the
  three-polaroid I₃ = (I₀/4)sin²2θ derivation and its θ=45° maximum; the Huygens TIR construction
  sin C = μ₂/μ₁ with r > i confirmed even in the figure's own drawn coordinates; the d/D resolving-
  power formula and the ≈1′ normal-eye value.
- **Two book-error corrections were independently re-verified and are themselves correct**: the
  `interference_intensity_conditions` card's note that the source book's printed equation (2) has
  a `cos ωt cos φ` typo where the correct expansion of `a sin(ωt+φ)` gives `cos ωt sin φ` — confirmed
  by direct expansion; and the `total_internal_reflection_huygens` card's normalisation of the
  question's missing question mark and "Huygen's" → "Huygens" spelling — cosmetic, not a physics
  claim.
- **No `common_mistakes`/`MISTAKE` item condemns correct physics** anywhere in the group — every
  mistake entry checked names a genuinely wrong statement (wrong sign, wrong angle, dropped
  condition, swapped symbol), the commonest defect class the brief warns about was not found here.
- **No `RECALL accept`/`reject` list would mark a correct student answer wrong or a wrong one
  right** — every `reject` phrase belongs to a DIFFERENT step's content in the same card (checked
  all 10 cards' recall blocks against their sibling steps), so the rejects correctly enforce the
  mark split rather than penalising valid content.
- **No `INSIDER_NOTE` is false** — all 10 checked true against their card's own derivation.
- **`mark_split` sums correct on all 10 cards** (2,2,2,2,4,4,4,4,4,4 matching each `marks_total`);
  0-mark diagram steps correctly carry no mark-split row except the two labelling issues above.
- **Figures**: all stroke geometry checked against physics — the `two_source_interference`,
  `three_polaroids`, and `huygens_refraction_rarer`/eye-resolution diagrams all draw the physically
  correct arrangement (crossed vs tilted polaroid axes, r > i bending away from the normal into the
  rarer medium — verified numerically from the actual pixel coordinates, not just visually).
- Rule 41 plain-language sweep covered every `WHY`/`MISTAKE`/`MEMORY_TIP`/`MARGIN_NOTE` in the
  group; only the two personification/informal-register items above were found.
