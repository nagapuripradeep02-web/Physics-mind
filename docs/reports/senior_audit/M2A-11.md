# M2A-11 — mathematics_2a, Unit 8 Measures of Dispersion (part 1/1)

Cards audited: 14 / 14   Findings: HARMFUL 0 · WRONG 3 · WEAK 0

One Python script (`verify.py`) independently recomputed every mean, median, mean deviation,
variance and standard deviation in the group — every intermediate table column (midpoints, dᵢ,
dᵢ², fᵢdᵢ, fᵢdᵢ², cumulative frequency, |xᵢ − x̄|/|xᵢ − M|, fᵢ|xᵢ − x̄|/fᵢ|xᵢ − M|) and every running
sum, N, and boxed final answer. All 14 boxed answers are correct. Both step-deviation-vs-direct
cross-checks the brief requires were run: card 1/2's step-deviation mean (27, 33.4) matches the
direct Σfᵢxᵢ/N mean exactly; card 5's step-deviation variance (201) matches the direct
Σfᵢxᵢ²/N − (Σfᵢxᵢ/N)² variance exactly. Card 3's script run first showed a false mismatch on
Σfᵢ|xᵢ − M|; re-running with the card's own stated rounded M = 27.86 (not the unrounded
27.857142…) reproduces 517.16 exactly — the card is internally consistent on its own rounding, not
an error.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_md_laq_md_mean_step_dev_0_50 | s1_table | `MISTAKE[2]`: "Adding fᵢdᵢ without the signs: −10 − 8 + 0 + 16 + 12 = 10, not 46." The label names the mistake as *dropping* the signs, but the arithmetic shown keeps every sign (−10, −8, …) — it is the CORRECT signed computation, not a demonstration of the described error. As printed this reads as self-contradictory (or as if 10, the table's own correct Σfᵢdᵢ, were the wrong answer). | "Adding fᵢdᵢ without the signs: 10 + 8 + 0 + 16 + 12 = 46, not the correct −10 − 8 + 0 + 16 + 12 = 10." (verified: dropping the signs on −10, −8 does give 46; the signed sum is 10, matching the table's own Σfᵢdᵢ.) |
| ts_ipe_m2a_md_laq_md_mean_step_dev_0_70 | s1_table | `MISTAKE[1]`: "Adding fᵢdᵢ without the signs: −18 − 10 − 8 + 0 + 7 + 12 + 9 = −8, not 64." Same defect as the twin card above: the label says "without the signs," the demonstration keeps them. | "Adding fᵢdᵢ without the signs: 18 + 10 + 8 + 0 + 7 + 12 + 9 = 64, not the correct −18 − 10 − 8 + 0 + 7 + 12 + 9 = −8." (verified: unsigned sum 64, signed sum −8, matching the table's own Σfᵢdᵢ.) |
| ts_ipe_m2a_md_laq_variance_sd_grouped_30_100 | s1_table | `MISTAKE[2]`: "Adding fᵢdᵢ without the signs: −9 − 14 − 12 + 0 + 8 + 6 + 6 = −15." Same mislabeling a third time — the demonstrated arithmetic is the signed (correct) computation, not an illustration of adding "without the signs." | "Adding fᵢdᵢ without the signs: 9 + 14 + 12 + 0 + 8 + 6 + 6 = 55, not the correct −9 − 14 − 12 + 0 + 8 + 6 + 6 = −15." (verified: unsigned sum 55, signed sum −15, matching the table's own Σfᵢdᵢ.) |

## WEAK — quality, register, Rule 41

_(none)_

## Teacher-gate questions

Every card's `VERIFICATION_NOTE` states the 2+2+2+1 / 3+3+1 / 1+1 mark splits are the bank's OWN
pattern (modelled on a 1A house weighting), not printed in the source book (no `[N Marks]` tags
exist in this chapter), and explicitly "stays unverified until a Telangana IPE teacher confirms
it." That is a genuine, group-wide open item, not a defect I can resolve by re-deriving math —
flagging per the brief rather than re-asserting it as a finding on every card.

## Cards checked clean

ts_ipe_m2a_md_laq_md_median_grouped_0_60, ts_ipe_m2a_md_laq_md_median_discrete_n30,
ts_ipe_m2a_md_laq_variance_sd_discrete_n30, ts_ipe_m2a_md_vsaq_md_mean_3_6_10_4_9_10,
ts_ipe_m2a_md_vsaq_md_mean_6_7_10_12_13_4_12_16, ts_ipe_m2a_md_vsaq_md_median_4_6_9_3_10_13_2,
ts_ipe_m2a_md_vsaq_md_median_6_7_10_12_13_4_12_16, ts_ipe_m2a_md_vsaq_md_mean_38_70_48_ten_values,
ts_ipe_m2a_md_vsaq_md_median_13_17_16_eleven_values, ts_ipe_m2a_md_vsaq_variance_6_7_10_12_13_4_8_12,
ts_ipe_m2a_md_vsaq_variance_sd_5_12_3_18_6_8_2_10

(11 clean; the other 3 carry the findings above — each on the SAME step, `s1_table`, of the three
grouped step-deviation LAQs, so they are not spread across the group.)

## Notes

- **Mathematically this group is fully correct.** All 52 LAQ table cells + all 8 VSAQ datasets
  were independently recomputed; every boxed final answer (9.44, 13.18, 27.86→10.34, 4.97, 201→14.18,
  45.8→6.77, 2.67, 3.25, 3.29, 3.25, 8.4, 2.45, 9.25, 24.25→4.92) is right.
- **Book misprint correctly caught by the card, not silently propagated**:
  `ts_ipe_m2a_md_vsaq_variance_sd_5_12_3_18_6_8_2_10` s2_variance states the source book prints
  σ = 4.95 for σ² = 24.25 and shows why that is wrong (4.95² = 24.5025 ≠ 24.25, while the card's own
  4.92² = 24.21 is the correct rounding of √24.25 = 4.9244…) — recomputed and confirmed the card's
  correction, not the book's print, is right.
- **No `MISTAKE` entry condemns mathematically correct alternative working** (the commonest defect
  type per the brief) — checked every `MISTAKE[i]` across all 14 cards against its own card's
  numbers; all describe genuine errors (wrong divisor, dropped sign, wrong centre, unsorted data,
  formula misuse), none mislabel a valid method as wrong.
- **Every `INSIDER_NOTE` (14/14) is true on its own card** — each was checked against the
  recomputation (e.g. the claimed zero-deviation values, the claimed mean/median mismatch, the
  claimed book misprint) and all hold.
- **`MARK_SPLIT` sums, labels and `MARGIN_NOTE` running-mark claims all check out**: every card's
  split sums to `marks_total` (7 for the six LAQs, 2 for the eight VSAQs); every step's
  `MARGIN_NOTE` "First N marks / Next N marks / Last mark" phrase matches its own `(a→b)` running
  total; no 0-mark step claims a mark.
- **Repetition across the eight near-identical VSAQ mean/median/variance cards is genuinely
  deliberate**, per the brief's framing — each twin's `WHY`/`MEMORY_TIP`/`INSIDER_NOTE` was checked
  against ITS OWN numbers (not assumed correct because a sibling is), and all are correct where
  repeated.
- **No `FIGURE` lines exist in this group** — Measures of Dispersion is table/formula content only,
  so check 5 (figures) is not applicable here.
- **No Rule 41 (plain-language) violations found** — no idioms, metaphors or personification in any
  `WHY`/`MEMORY_TIP`/`MARGIN_NOTE`/`INSIDER_NOTE` across the group.
- Minor, not filed as a finding: `ts_ipe_m2a_md_laq_md_mean_step_dev_0_70` s2_mean `MISTAKE[1]`
  ("Forgetting to multiply by C, which gives 35 − 0.16.") leaves the expression unevaluated, while
  its twin card's parallel `MISTAKE[0]` completes to a final number (25.2) — a harmless
  completeness inconsistency, not a false statement (35 − 0.16 = 34.84 either way).
