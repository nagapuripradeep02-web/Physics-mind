# M2B-04 — mathematics_2b, Unit 2 System of Circles (part 1/1)

Cards audited: 23 / 23   Findings: HARMFUL 2 · WRONG 1 · WEAK 1

Every radical axis, radical centre, common-chord length, family-circle diameter condition,
three-unknown orthogonal-circle system (g, f, c), orthogonal-k value, and angle-between-circles
computation was independently re-derived with a sympy script and checked against every printed
line and boxed answer — all 23 boxed final answers are numerically correct, including two general
proofs (angle = 3π/4 identically in a; the S+λS′ orthogonality holding for all l, m, g). The
angle-between-circles sign convention used in the three angle cards (17-19) was independently
verified against the well-known canonical result for the `x²+y²=a², x²+y²=ax+ay ⇒ 3π/4` problem —
it is self-consistent across all three cards and correct, not a defect.

## HARMFUL — wrong content a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_sc_radical_centre_three_circles_two | card (insider_note) | `insider_note`: "...divide it by 2 before doing anything else, or every radical-axis coefficient downstream comes out exactly double what it should be." Verified by direct computation: subtracting S − S′_as-printed (leading coeff 2) WITHOUT dividing first gives −x²+x−y²−5y+2 — the x² and y² terms do not cancel at all, so the result is not even a straight line, let alone one with "doubled" coefficients. The claim is false. | "The middle circle is printed with a leading coefficient of 2 — divide it by 2 before doing anything else, or the x² and y² terms will not cancel when you subtract, and the result will not be a straight line at all." |
| ts_ipe_m2b_sc_circle_through_intersection_and_point | card (insider_note) | `insider_note`: "...writing S + λS′ = 0 by mistake is the single most common slip here." This directly contradicts the card's OWN step `s2_family_through_sp_and_l` `common_mistakes[0]`, which correctly says S + λS′ = 0 "is a valid family too, but not the standard method." Verified by computation: substituting the given point (1, 2) into S + λS′ = 0 and solving gives λ = 1/2, and the resulting circle simplifies to x² + y² − 6x − 4y + 9 = 0 — the IDENTICAL correct final answer. Calling a mathematically valid alternative method "a mistake" that a student must avoid is false. | "This looks like a two-circle family question; the working below builds the family from S′ and the RADICAL AXIS L rather than from S and S′ directly, which is the standard method — but S + λS′ = 0 is also a valid family through the same two points (see the mistake note on the next step) and gives the same final circle if carried through correctly." |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_sc_radical_centre_three_circles_two | s1_standard_form | `common_mistakes[0]`: "Subtracting the circles before dividing the middle one by 2, which doubles every coefficient in the radical axis." Same false "doubling" claim as the card's insider_note (see HARMFUL row) — verified false by the same computation. | "Subtracting the circles before dividing the middle one by 2 — the x² and y² terms will not cancel, so the result is not a valid straight line at all." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_sc_circle_through_two_points_orthogonal | s3_orthogonal_condition | Three consecutive `LINE` entries render as "⇒ orthogonal to x² + y² + 5/4 · 2x − 3/2 ·" / "2y + 2" / "= 0" — the coefficient is shown as "5/4 · 2x" instead of the simplified 5/2 x, and the single equation is broken mid-expression across three separate lines. Not mathematically wrong (5/4·2 = 5/2, matching 2g′), but confusing to read. | "⇒ orthogonal to x² + y² + 5/2 x − 3y + 2 = 0" (one line, simplified coefficients) |

## Teacher-gate questions

None. Every `⚠ THE MARK SPLIT IS OURS` disclosure in this chapter's cards is already transparent
about the split being unverified against a TSBIE document — not a new ambiguity raised by this
audit.

## Cards checked clean

ts_ipe_m2b_sc_radical_centre_three_circles_one, ts_ipe_m2b_sc_common_chord_length_one, ts_ipe_m2b_sc_common_chord_length_two, ts_ipe_m2b_sc_circle_on_chord_as_diameter_one, ts_ipe_m2b_sc_circle_on_chord_as_diameter_two, ts_ipe_m2b_sc_orthogonal_circle_through_origin, ts_ipe_m2b_sc_orthogonal_circle_through_given_point, ts_ipe_m2b_sc_orthogonal_circle_through_1_1, ts_ipe_m2b_sc_orthogonal_find_k_one, ts_ipe_m2b_sc_orthogonal_find_k_two, ts_ipe_m2b_sc_show_orthogonal_one, ts_ipe_m2b_sc_show_orthogonal_general, ts_ipe_m2b_sc_radical_axis_one, ts_ipe_m2b_sc_radical_axis_two, ts_ipe_m2b_sc_angle_between_circles_proof, ts_ipe_m2b_sc_angle_between_circles_find, ts_ipe_m2b_sc_find_k_given_angle_45, ts_ipe_m2b_sc_common_chord_general_ab, ts_ipe_m2b_sc_orthogonal_find_k_general, ts_ipe_m2b_sc_radical_axis_three

(20 clean; the other 3 carry the findings above.)

## Notes

- **Mathematically this group is in very good shape.** All 23 boxed final answers, and every
  intermediate line feeding them, were independently re-derived with sympy and matched exactly —
  zero wrong radical axis, zero wrong radical centre, zero wrong chord length, zero wrong g/f/c
  solve, zero wrong k, zero wrong angle. The two HARMFUL findings are both in `insider_note` prose
  making a false auxiliary claim, not in a printed working line or a boxed answer.
- **Angle-between-circles sign convention (cards 17-19), specifically checked and NOT a finding:**
  the formula used, cos θ = (d² − r₁² − r₂²)/(2r₁r₂) [equivalently (c+c′−2gg′−2ff′)/(2r₁r₂)], gives
  the OPPOSITE sign from the naive law-of-cosines angle at the intersection point's triangle vertex.
  I verified this is the textbook's actual intended convention, not an error: it is applied
  identically across all three angle cards, and card 17's result (3π/4) matches the famous, widely
  reproduced textbook answer for this exact classic problem (x²+y²=a², x²+y²=ax+ay). Since angle
  between two circles is only geometrically defined up to a supplementary-angle choice, and this
  convention reproduces the canonical known answer consistently, it is correct as authored.
- **MARK_SPLIT structure:** every card's mark_split sums to marks_total, every label matches its
  step's label, and every step's running-total margin note ("First/Second/.../Last two marks")
  matches its own `marks` value. No mismatches found.
- **No Rule 41 idiom/metaphor hits** — grepped the whole group for the standard idiom list
  (by heart, nail it, grip, budge, fate, ceiling-as-limit, personifying "wants/knows/cares"); no
  genuine hits (the few "answers"/"asks" matches are the literal verb "the question asks").
- **The one figure in this group** (`radical_centre_three_circles`, card 1) was checked: its labels
  and the meeting point of the two drawn radical-axis strokes are consistent with the card's own
  explicit disclaimer that it is schematic, not to true scale.
- **This is System of Circles, not a conics chapter** — the ellipse/hyperbola a²(1∓e²) trap named in
  the dispatch brief does not apply to any card in this group (no eccentricity/foci/directrix/
  asymptote/latus-rectum parameter appears anywhere in these 23 cards).
- All source-JSON checks for the WEAK finding's raw `lines` array were done by reading
  `answer-book/questions/ts_ipe_m2b_sc_circle_through_two_points_orthogonal.json` directly, confirming
  the odd typesetting is authored as-is, not a text-dump artifact.
