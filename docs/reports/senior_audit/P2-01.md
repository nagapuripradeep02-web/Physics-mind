# P2-01 — physics_2, Unit 1 Waves (part 1/1)

Cards audited: 23 / 23   Findings: HARMFUL 0 · WRONG 2 · WEAK 2

Every dimensional formula, every derivation (V=nλ; Y=2A sin kx cos ωt; open-pipe n=(p/2l)√(T/μ)-family
via V; closed/open pipe λ-fits; both Doppler crest-timing derivations) and all three numerical answers
were recomputed with one `python` script (sqrt/exponent arithmetic, unit conversions, and a symbolic
dimension-exponent check for T/μ and B/ρ). Confirmed: 118.2 Hz (closed pipe, 70 cm), 93 m/s (steel
wire), and 523/1046/1569 Hz (string) all reproduce exactly; the book's own √70 = 8.27 in the string
card is independently confirmed wrong (√70 = 8.3666…) and the card's correction to 8.37 is right.
Also spot-checked three "erroneous alternative" values MISTAKE entries claim a student would get
(110 m/s, 236 Hz, 473 Hz) — all three are numerically correct as printed.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_wav_steel_wire_wave_speed | s4_substitution | `MISTAKE[1]`: "Taking the root of the tension and the density separately." This condemns a mathematically CORRECT alternative: √T/√μ = √(T/μ) is an identity, not an error. Verified numerically: √60/√(6.9×10⁻³) = 7.7460/0.083066 = 93.2505, exactly matching √(60/6.9×10⁻³) = √8700 = 93.2505 to 4 decimal places — the two routes give the identical answer. (Contrast with the genuinely error-prone split rooting flagged correctly two cards later, in `string_three_lowest_frequencies` s3, where the power of ten is odd and DOES trip students up — that one is a real mistake and is not flagged here.) | Drop the item (MISTAKE[0], the power-of-ten slip, already covers the step's real pitfall), or replace it with a genuine one, e.g. "Rounding √60 or √μ too early, before dividing, and compounding the rounding error." |
| ts_ipe_p2_wav_resonance_velocity_of_sound | s3_two_lengths | `LINE`: "Second resonance: l₂ = 3λ/4 = 3l₁" and its `MARGIN_NOTE`: "...note l₂ is three times l₁." This equality holds only when the tube's end correction e = 0 (with e ≠ 0, l₁ = λ/4 − e and l₂ = 3λ/4 − e, so l₂ = 3l₁ + 2e ≠ 3l₁ — verified by substitution). The very next step, `s4_wavelength`, contradicts it directly in its own `WHY`: "Using the DIFFERENCE of two measured lengths, instead of l₁ alone, cancels the small end correction of the tube — **both lengths carry the same error** and subtraction removes it" — which only makes sense if e ≠ 0. The card asserts e = 0 in one step and relies on e ≠ 0 to justify its method in the next. (The graded formula, V = 2n(l₂ − l₁), does not depend on l₂ = 3l₁, so this is not exam-costing on its own.) | Drop "= 3l₁" from the LINE (keep "Second resonance: l₂ = 3λ/4"); in the MARGIN_NOTE replace "note l₂ is three times l₁" with "Two loudest lengths, and their λ values." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_wav_dimensional_speed_string | card (insider_note) | "...v = K(T/μ) without the root is the standard way this answer **dies**." Rule 41: "dies" personifies an inanimate written answer — a metaphor, not literal textbook English. | "...v = K(T/μ) without the root is the standard way this answer loses the mark." |
| ts_ipe_p2_wav_beats_occurrence_uses | s3_beat_frequency | `WHY`: "...so the pair **comes back into step** n₁ − n₂ times — one loud instant each time." Rule 41: "in step" is an idiom borrowed from marching, applied here to two waves' phase relationship. | "...so the pair returns to being in phase n₁ − n₂ times — one loud instant each time." |

## Teacher-gate questions

- Three numerical LAQ cards (`closed_pipe_70cm_fundamental`, `steel_wire_wave_speed`,
  `string_three_lowest_frequencies`) each carry a `VERIFICATION_NOTE` admitting the 5-way, 8-mark split
  is this bank's own invention — the source book files these numericals under Long-Answer numbering
  with no separate "Problems" banner, so there is no printed scheme to match against, and a teacher may
  reasonably mark any of the three as a shorter 4-mark question instead. This is disclosed by the cards
  themselves, not a new finding — flagging here only because it recurs three times in one group and a
  teacher should rule on it once, for all three.
- Chapter-convention note, not an error: the bank consistently uses `n` for BOTH "frequency" (physics
  symbol) and, inside RECALL/accept strings, spoken renderings like "n one" — this is the standard
  Telugu-board notation (n rather than f for frequency) and is applied consistently across every card
  in this group, so it is not flagged as an inconsistency.

## Cards checked clean

ts_ipe_p2_wav_transverse_vs_longitudinal, ts_ipe_p2_wav_dimensional_speed_sound, ts_ipe_p2_wav_phase_reversal_rigid_boundary, ts_ipe_p2_wav_node_and_antinode, ts_ipe_p2_wav_node_antinode_distance, ts_ipe_p2_wav_what_wave_represents, ts_ipe_p2_wav_wave_velocity_expression, ts_ipe_p2_wav_principle_of_superposition, ts_ipe_p2_wav_doppler_applications, ts_ipe_p2_wav_conditions_for_reflection, ts_ipe_p2_wav_doppler_effect_example, ts_ipe_p2_wav_doppler_effect_illustrative, ts_ipe_p2_wav_stationary_waves_string_laws, ts_ipe_p2_wav_open_pipe_harmonics, ts_ipe_p2_wav_closed_pipe_harmonics, ts_ipe_p2_wav_doppler_source_moving, ts_ipe_p2_wav_doppler_observer_moving, ts_ipe_p2_wav_closed_pipe_70cm_fundamental, ts_ipe_p2_wav_string_three_lowest_frequencies

(19 clean; the other 4 carry the findings above.)

## Notes

- **Physics/maths is in very good shape.** All four derivations (transverse-wave dimensional analysis,
  sound-speed dimensional analysis, standing-wave superposition Y = 2A sin kx cos ωt, both Doppler
  crest-timing derivations) check out exactly; every open-pipe vs closed-pipe fit (λ = 2l/p family vs
  λ = 4l/(2k−1) family) and every harmonic ratio (1:2:3 open, 1:3:5 closed) is applied consistently and
  never crossed between the two twin cards.
- **The open/closed-pipe and source/observer-moving twin pairs are clean of cross-contamination.**
  `open_pipe_harmonics` vs `closed_pipe_harmonics`, and `doppler_source_moving` vs
  `doppler_observer_moving`, were checked line-by-line for a sibling's variable or formula leaking into
  the other card — none found; in fact several `RECALL reject` entries deliberately quote the SIBLING
  card's formula to test that a student doesn't confuse the two, which is a real design strength, not a
  clone defect.
- **Every MARK_SPLIT sums to marks_total, every step label pairs correctly with its mark_split entry,
  and every 0-mark diagram step correctly carries no "Nth mark" claim** in its MARGIN_NOTE — checked
  mechanically across all 23 cards (52+ steps total).
- **Figures** (7 diagram steps: resonance columns, string loops, open/closed pipe fundamentals, two
  Doppler geometries): antinode/node placement is correct at every open/closed/fixed end in every
  figure (open pipe has no end-cap stroke; closed pipe does; string-loop fixed supports both sit on the
  axis line). No arrow or label was found pointing to the physically wrong place.
- **No `MISTAKE` entry condemns a mathematically valid alternative** except the one flagged above
  (steel_wire_wave_speed) — a nearby, superficially similar entry in `string_three_lowest_frequencies`
  s3 ("rooting 6.4 and the power separately" → 2.5×10⁻³) was checked and is a genuine, distinct pitfall
  (the exponent there is odd, so splitting the root without first rewriting 6.4×10⁻⁵ as 64×10⁻⁶ really
  does trip students up) — not the same defect, correctly left unflagged.
- Minor, not filed as findings: "protects the mark" (appears in `node_antinode_distance`,
  `principle_of_superposition`, `conditions_for_reflection` insider_notes) reads as ordinary
  exam-prep English (protect = don't lose), not personification, matching the sibling group's precedent
  that "loses the order" is fine, plain register.
- Two source-book corrections were checked and are legitimate, not new errors: `doppler_applications`
  s2 corrects "speed of rotation of earth" to "the Sun and stars" (the standard Doppler astronomy
  application; acoustic/optical Doppler is not how Earth's own rotation is measured), and
  `string_three_lowest_frequencies` corrects the book's √70 = 8.27 to 8.37 (independently verified: √70
  = 8.3666…, and 62.5 × 8.27 = 517 while 62.5 × 8.37 = 523.1, matching the book's own printed final
  answer — the book's intermediate step, not its answer, was wrong).
