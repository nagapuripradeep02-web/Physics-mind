# M2A-08 — mathematics_2a, Unit 6 "Binomial Theorem" (part 1/2)

Cards audited: 17 / 17   Findings: HARMFUL 0 · WRONG 9 · WEAK 0

Every boxed answer, every printed algebraic step, and both general AP-coefficient identities (card
`three_coefficients_ap_general`, `coefficients_x9_x10_x11_ap`) were re-derived by hand and cross-checked
with one Python script (`sympy` + `mpmath`, 60-digit precision): the 4-consecutive-coefficient identity,
the `2nCn+r` sum identity plus both deductions, the two `((n+1)/(r+1))·nCr` series-sum cards, the
`ab = −1` relation, and all eight infinite series were summed to 400 terms against their claimed closed
forms (`√3`, `5∛5/16`, `√2`, `5√15/9 − 8/5`, etc.) — every one matched to machine precision. `36, 84, 126`
was confirmed as the UNIQUE `n, r` solution by brute-force search. Zero wrong mathematics found anywhere
in the group; every finding below is in cloned/boilerplate `MISTAKE` prose, not in a printed line.

## HARMFUL — wrong content a student would copy into an exam

_(none)_

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2a_bt_series_1_by_3_3_6_sum | s1_rewrite | `common_mistakes[0]`: "Forgetting to add the missing terms on both sides when the series starts late, so the comparison has no 1 to match." This series (1 + 1/3 + …) already starts with the "1" term — nothing is ever added in this card's own working, and this very step's own `memory_tip`/`margin_note` say so ("This series already starts with 1 and 1/3 — add nothing"). `common_mistakes[2]`: "Losing the alternating sign when the terms alternate." This series has no alternating signs (all terms are +), confirmed by the next step's own `margin_note` ("All plus signs mean the (1 − x) form"). Both entries are boilerplate cloned from sibling cards where they are true. | Delete both. `MISTAKE[1]` ("Writing 3·6·9 as 3·3! instead of 3³·3!") is the one genuine risk for this card and is sufficient on its own. |
| ts_ipe_m2a_bt_series_4_by_5_alternating_sum | s1_rewrite | `common_mistakes[0]`: "Forgetting to add the missing terms on both sides when the series starts late, so the comparison has no 1 to match." This series (1 − 4/5 + …) already starts with 1 — this step's own `memory_tip` says "This series already starts with 1, so add nothing." | Delete `MISTAKE[0]`. `MISTAKE[1]` (factorial rewrite) and `MISTAKE[2]` (losing the alternating sign — genuinely applicable here, this IS an alternating series) are both real and sufficient. |
| ts_ipe_m2a_bt_series_1_by_5_find_3x2_6x | s1_rewrite | `common_mistakes[2]`: "Losing the alternating sign when the terms alternate." This series (1/5 + (1·3)/(5·10) + …) has no alternating signs — every term is +, confirmed by the very next step's own comparison line, the all-plus `(1 − y)^{−p/q}` form. | Delete `MISTAKE[2]`. `MISTAKE[0]` (add the missing 1) and `MISTAKE[1]` (factorial rewrite) are both genuine and already tailored to this card. |
| ts_ipe_m2a_bt_series_1_3_by_3_6_prove_9x2_24x | s1_rewrite | `common_mistakes[2]`: "Losing the alternating sign when the terms alternate." This series ((1·3)/(3·6) + (1·3·5)/(3·6·9) + …) is all-positive; no alternation occurs anywhere in the card. | Delete `MISTAKE[2]`. |
| ts_ipe_m2a_bt_series_5_by_2fact_3_find_x2_4x | s1_rewrite | `common_mistakes[1]`: "Writing 3·6·9 as 3·3! instead of 3³·3! — every factor carries a 3." This card's denominators are 2!·3, 3!·3², 4!·3³ — the literal product "3·6·9" never appears anywhere in this problem (unlike the sibling cards it was cloned from). `common_mistakes[2]`: "Losing the alternating sign when the terms alternate." — this series is also all-positive. | Delete both. `MISTAKE[0]` (forgetting to add 1 + (3/1!)(1/3) = 2) is the genuine, card-specific risk. If a second entry is wanted, promote the card's own `memory_tip` insight into one: "Reading p as the printed first numerator 5 instead of rewriting 5/(2!·3) as (3·5)/(2!·3²) first, so p is taken as 5 instead of 3." |
| ts_ipe_m2a_bt_series_7_by_5_1_by_100_sum | s1_rewrite | BOTH of this step's `common_mistakes` are false for this card. `[0]`: "Forgetting to add the missing terms on both sides when the series starts late…" — this bracket S = 1 + 1/10² + … already starts with 1; the card's own working never adds anything. `[1]`: "Losing the alternating sign when the terms alternate." — this series is all-positive (every power of 1/100 enters with a + sign), confirmed by the next step's own all-plus `(1 − x)^{−p/q}` comparison. | Delete both (this step then has zero `common_mistakes`, which is fine — `s2`–`s5` each carry their own). If one is wanted here, a genuine risk already named later in the card (`s5` `MISTAKE[1]`) fits: "Taking the common ratio as 1/10 instead of 1/100 (the powers are 1/10², 1/10⁴, 1/10⁶, i.e. steps of (1/100)ᵏ)." |
| ts_ipe_m2a_bt_series_3_5_by_5_10_sum | s1_rewrite | `common_mistakes[2]`: "Losing the alternating sign when the terms alternate." This series ((3·5)/(5·10) + (3·5·7)/(5·10·15) + …) is all-positive. | Delete `MISTAKE[2]`. |
| ts_ipe_m2a_bt_series_4_by_5_prove_9t_16 | s1_rewrite | `common_mistakes[2]`: "Losing the alternating sign when the terms alternate." This series (4/5 + (4·6)/(5·10) + …) is all-positive. | Delete `MISTAKE[2]`. |
| ts_ipe_m2a_bt_coefficient_x10_x_minus_10_ab | s7_equate | `common_mistakes[0]`: "Not noticing ¹¹C₄ = ¹¹C₇ and computing 330 = 330 the long way." Evaluating ¹¹C₄ and ¹¹C₇ separately and finding both equal 330 is mathematically correct and reaches the identical final answer — it costs no marks, so labelling it a "mistake" condemns valid working. | Replace with a genuine risk at this step, e.g.: "Losing the minus sign from (4) when equating — writing ¹¹C₄a⁷/b⁴ = ¹¹C₇a⁴/b⁷ (dropping the (−1)⁷) and reaching ab = 1 instead of ab = −1." |

## WEAK — quality, register, Rule 41

_(none — no idioms/metaphors/personification found; every `MARK_SPLIT` label matches its step label and every mark_split/margin_note numbering was checked cumulative-mark-by-cumulative-mark and is consistent)_

## Teacher-gate questions

- `ts_ipe_m2a_bt_successive_coefficients_36_84_126` and `ts_ipe_m2a_bt_coefficients_x9_x10_x11_ap` are both sourced from the book's "Star Questions Plus" p.94, which the book itself routes to from the end of the Binomial VSAQ chapter ("Few More VSAQ are in Page 94") — genuine counter-evidence against classing them as 7-mark Section-C LAQs, even though their method/length matches the chapter's own LAQ answers. Both cards already carry `needs_teacher_verification: true`, which is correct — this needs a Telangana teacher's ruling, not an examiner's.

## Cards checked clean

ts_ipe_m2a_bt_terms_240_720_1080, ts_ipe_m2a_bt_four_consecutive_coefficients, ts_ipe_m2a_bt_three_coefficients_ap_general, ts_ipe_m2a_bt_sum_cr_cr_plus_k_2ncn_plus_k, ts_ipe_m2a_bt_c0_c1x_by_2_series_sum, ts_ipe_m2a_bt_c0_c1_by_2_corollary, ts_ipe_m2a_bt_successive_coefficients_36_84_126, ts_ipe_m2a_bt_coefficients_x9_x10_x11_ap

(8 fully clean; the other 9 each carry exactly one finding above, all in `common_mistakes` prose — no card has a wrong printed `LINE`, boxed answer, `WHY`, `MEMORY_TIP`, `MARGIN_NOTE`, `INSIDER_NOTE`, or `RECALL` field.)

## Notes

- **The mathematics is airtight.** All 17 boxed answers, all 68 steps, and the two general symbolic
  identities (the `2(r+2)/(n+1)` four-coefficient identity and the `n² − (4r+1)n + 4r² − 2 = 0`
  AP-condition identity, the latter symbolically confirmed to reduce to exactly the card's boxed
  equation with zero residual) were machine-verified. `coefficients_x9_x10_x11_ap`'s boxed
  `n² − 41n + 398 = 0` is exactly the general formula from `three_coefficients_ap_general` at `r = 10` —
  the two sibling cards are mutually consistent.
- **The pattern behind all 9 findings: a shared "common_mistakes" template pasted across the whole
  8-card infinite-series family without checking which parts apply to each series.** Two boilerplate
  entries — "add the missing terms because the series starts late" and "losing the alternating sign
  when the terms alternate" — were written once and cloned onto every series card regardless of
  whether THAT series actually starts late or actually alternates. Only 5 of 8 series genuinely start
  late (`_find_3x2_6x`, `_prove_9x2_24x`, `_find_x2_4x`, `_5_10_sum`, `_prove_9t_16`) and only 1 of 8
  genuinely alternates (`_alternating_sum`) — so the "starts late" entry is false on 3 cards and the
  "alternates" entry is false on 7 cards, one of which (`_7_by_5_1_by_100_sum`) is false on BOTH of its
  only two listed mistakes at once. This is the sibling-cloning defect the brief calls out, concentrated
  almost entirely in one step id (`s1_rewrite`) across one card family.
- **`mark_split` structure is clean across the board.** All 17 cards sum to `marks_total = 7`
  (confirmed: this is the post-reform Maths-2A 7-mark LAQ paper, not the old 8-mark split, per the
  task note). Every `MARK_SPLIT` label pairs to its step's `label` in order, every step's `marks` field
  matches the `MARK_SPLIT` number, and every step's running mark range (`(a→b)`) is echoed correctly in
  its `MARGIN_NOTE`'s "Nth mark" / "Nth and (N+1)th marks" phrasing (checked cumulative mark by
  cumulative mark for all 68 steps) — including the two harder-to-eyeball ones (`s2_ap_condition` in
  both `three_coefficients_ap_general` and `coefficients_x9_x10_x11_ap` correctly omits a leading
  "Nth mark" phrase in a way that's consistent between the two sibling cards, not a discrepancy).
- **No `FIGURE` lines exist in this group** — check 5 of the brief (figure labels/stroke directions) is
  N/A for all 17 cards.
- **No Rule 41 (plain-language) violations found** — no idioms, metaphors, or personification anywhere
  in the group; occurrences of "the question asks for n" are literal, standard mathematical register,
  not personification.
- One item considered and NOT filed: `ts_ipe_m2a_bt_successive_coefficients_36_84_126` `s1_terms`
  `MISTAKE[0]` ("Naming them ⁿCᵣ, ⁿCᵣ₊₁, ⁿCᵣ₊₂ — fine, but then the ratio formulas shift by one…") —
  this explicitly says the alternative is "fine," so it does not condemn correct work; it is honest
  about an equally valid alternative and was left alone.
- `VERIFICATION_NOTE`s across the whole group correctly state their own `[N Marks]`-tag provenance
  (book-printed vs. examiner-authored, cross-checked against each card's actual `MARK_SPLIT` sum — all
  17 matched) and none overclaim what the 2026-08-29 backtest proves.
