# M2B-05 — mathematics_2b, Unit 3 Parabola (Maths-2B) (part 1/1)

Cards audited: 20 / 20   Findings: HARMFUL 1 · WRONG 5 · WEAK 2

This unit is Parabola, not Hyperbola/Ellipse — the task's generic eccentricity/directrix/asymptote
checklist (b² = a²(e² − 1) etc.) does not apply here; verified instead against parabola-specific
identities. All 4 through-3-points systems (l, m, n) were solved independently in sympy and every
boxed final equation was substituted back through all 3 given points (residual 0 in every case); the
common-tangents quartic and both boxed tangent lines were verified by discriminant = 0 against both
curves; both triangle-area formulas (points-of-contact and tangent-intersection) were re-derived from
the shoelace formula symbolically and match the boxed (1/8a)/(1/16a) results exactly (also spot-checked
numerically); the focal-chord parameter relation t·(−1/t), the tangent condition c = a/m, and the point
of contact (a/m², 2a/m) were each verified by direct substitution. Every mark_split sum equals
marks_total, every step's running total is consistent, and every margin_note's stated mark count
matches its step's own `marks=`. Zero boxed answers, equations, or printed LINEs are wrong.

## HARMFUL — wrong content a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_par_focal_chord_other_extremity | card (insider_note) | "a focal chord's two ends are never both on the same side of the axis **unless the chord is the latus rectum**." This is false, and the exception is backwards: for ANY focal chord (points with parameters t and −1/t), y₁y₂ = (2at)(−2a/t) = −4a² < 0 always — the two ends are on opposite sides of the axis unconditionally, including the latus rectum itself (whose ends are (a,2a) and (a,−2a), also opposite signs). There is no case, latus rectum or otherwise, where both ends land on the same side. | "The negative sign in (a/t², −2a/t) is the single most commonly dropped sign in this question — a focal chord's two ends are always on opposite sides of the axis (one y-coordinate positive, one negative, for every focal chord including the latus rectum), since y₁y₂ = (2at)(−2a/t) = −4a² is always negative." |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_par_standard_form_derivation | s1_vertex_on_locus | `why`: "this same point is **the one point** where the focus-directrix ratio SP/PM equals 1". SP/PM = 1 is the defining condition of the ENTIRE parabola (every point on the curve satisfies it) — it is not unique to the vertex. As written this misstates what the ratio SP/PM = 1 means. | "…and this same point is the point on the axis SZ itself where SP = PM (by the symmetry SA = AZ) — so the midpoint of SZ satisfies the same focus–directrix condition that defines every point of the curve, and it becomes the natural place to put the origin." |
| ts_ipe_m2b_par_through_points_axis_parallel_x_v1 | card (mark_split) | `mark_split` reads "Eliminate n to get two equations in l and m (2) \| Solve for m and l (2)", but the card's own steps don't do that: s2_eliminate_n leaves only ONE surviving equation (4) — the second subtraction, (2)−(3), directly solves m = 1/2 rather than producing a second equation in l,m — and s3_solve_l then solves only for l (m is already known). Compare the sibling cards v2 and axis_parallel_y, whose step 2 genuinely produces two equations (4)/(5) in l,m and whose step 3 genuinely solves both together — this card's mark_split text was seemingly cloned from that sibling shape without adjusting for this card's own shortcut. | "Eliminate n and read off m directly (2) \| Solve for l (2)" — matching what s2/s3 actually do here (the sibling cards' "two equations…solve for m and l" phrasing does not fit this card). |
| ts_ipe_m2b_par_area_triangle_inscribed_vertices | s2_area_determinant | `memory_tip`: "Each column of this determinant is a difference of squares in disguise." The determinant's ROWS are (x-differences, x-differences) and (y-differences, y-differences); only the x-difference row (at₁²−at₂² etc.) is a literal difference of squares — the y-difference row (2at₁−2at₂ etc.) is a plain linear difference. A column mixes one entry from each row, so "each column is a difference of squares" misdescribes the structure being factored. | "Each ROW of this determinant factors: the top row is a difference of squares (t₁²−t₂² etc.), the bottom row is already a plain common factor (t₁−t₂ etc.) — pulling the shared (tᵢ−tⱼ) out of each column is what shrinks the determinant." |
| ts_ipe_m2b_par_nearest_point_to_focus_is_vertex | card (mark_split) | `mark_split` reads "Write the focal distance as a function of x₁ (1) \| Minimise it and identify the point (1)" — but the minimisation argument ("SP = x₁+a is smallest when x₁=0") is completed entirely inside step 1's own LINEs, and step 1's own margin_note confirms this ("for the focal distance formula and the reasoning that x₁ = 0 gives the minimum"). Step 2 only substitutes x₁=0 back in and names the vertex. | "Write the focal distance as a function of x₁ and show it is minimised at x₁ = 0 (1) \| Identify the minimising point as the vertex (1)" |
| ts_ipe_m2b_par_vertex_focus_directrix_axis_x2_minus4y | card (mark_split) | `mark_split` reads "Find a and the vertex (1) \| State the focus, directrix and axis (1)" — but the vertex is actually stated in step 2 ("(i) Vertex A = (0,0)"), not step 1 (step 1's LINEs only find a and the opening direction). The second item's phrase also drops "vertex" even though it's the first fact listed there. The sibling card `..._axis_y2_16x` (identical structure) gets this right: "Find a (1) \| State the vertex, focus, directrix and axis (1)". | "Find a and the direction of opening (1) \| State the vertex, focus, directrix and axis (1)" — matching the sibling card's phrasing. |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m2b_par_common_tangents_circle_2a2 | card (insider_note) | "Both roots of the quartic are expected to be considered, even though **only one is real**" — m⁴+m²−2=0 factors as (m²−1)(m²+2)=0; m² = −2 is itself a perfectly real number, it just yields no real slope m. Calling it "not real" conflates "m² is real" with "m corresponds to a real slope," which could confuse a student about what "real" means here. | "Both branches m² = 1 and m² = −2 are considered, even though only m² = 1 gives a real slope m (m² can never be negative for a real number) — stating why m² = −2 is rejected is worth the mark that a jump straight to m² = 1 would lose." |
| ts_ipe_m2b_par_area_triangle_inscribed_vertices | s1_take_vertices | `memory_tip`: "(at², 2at) is **the address** of any point on y² = 4ax." Metaphor (Rule 41a) — coordinates are not literally an address. | "One shared a, three different t's — (at², 2at) gives the coordinates of any point on y² = 4ax." |

## Teacher-gate questions

None. No convention/ambiguity in this group needs a board teacher's ruling — every finding above is either a mathematical/logical error or a structural (mark_split/step) mismatch resolvable purely by re-reading the card's own steps.

## Cards checked clean

ts_ipe_m2b_par_through_points_axis_parallel_x_v2, ts_ipe_m2b_par_through_points_axis_parallel_y, ts_ipe_m2b_par_area_triangle_tangent_points, ts_ipe_m2b_par_point_focal_distance_y2_8x, ts_ipe_m2b_par_point_focal_distance_y2_2x, ts_ipe_m2b_par_tangent_k_line_2y_5x_k, ts_ipe_m2b_par_tangent_point_of_contact_2x_minus_y_plus2, ts_ipe_m2b_par_tangent_inclined_60_degrees, ts_ipe_m2b_par_vertex_focus_upward_3_minus2_3_1, ts_ipe_m2b_par_vertex_focus_downward_1_minus2_1_minus7, ts_ipe_m2b_par_vertex_focus_directrix_axis_y2_16x, ts_ipe_m2b_par_tangent_normal_latus_rectum_y2_6x, ts_ipe_m2b_par_focus_vertex_directrix_latus_rectum_y2_minus_x_plus4y_plus5

(13 clean; the other 7 carry the findings above — one of the 7, area_triangle_inscribed_vertices, carries two findings.)

## Notes

- **Mathematically this group is in very good shape.** All 20 boxed answers/equations re-derived and
  confirmed correct (sympy: through-points systems solved and checked by substitution of all given
  points, common-tangent quartic and both tangent lines confirmed by discriminant = 0 against both the
  circle and the parabola, both area-of-triangle formulas re-derived from the shoelace formula and
  matched exactly, focal-chord/tangent/point-of-contact formulas verified by direct substitution). The
  one HARMFUL finding and all five WRONG findings are in explanatory prose (`insider_note`, `why`,
  `memory_tip`, card-level `mark_split`), never in a printed `LINE` a student would copy onto an answer
  sheet.
- **`MISTAKE` entries were checked for the bank's commonest defect (condemning correct alternative
  working) and none were found in this group.** A few entries frame a longer-but-valid method
  (brute-force determinant expansion, the point-form tangent, calculus-based optimisation of a linear
  function) as inefficient/risky rather than incorrect — these do not claim the alternative is wrong and
  were not filed.
- **No `RECALL` field appears anywhere in this group's dump** — nothing to check under that heading.
- **The chapter's "Five STAR Q's" cross-reference is internally consistent**: card 1 (answer 9) names
  answers 9, 12, 13, 14, 15 as the set; cards 4, 5, 6, 7 (answers 12, 13, 14, 15 respectively) each
  correspond correctly, and the page-boundary claims (p.22→24 jump swallowing p.23; p.24 holding both
  12 and 13; p.25 holding both 14 and 15; p.98/99 System-of-Circles→Parabola→Ellipse boundary) are all
  self-consistent across the cards that reference them.
- **Every `VERIFICATION_NOTE` carries the identical "INDEPENDENT RE-DERIVATION, 2026-08-29" boilerplate
  sentence** (per the task warning, worthless as evidence on its own) — this did not lower the bar for
  any card; every boxed result was independently re-derived regardless.
- **Mark bookkeeping is otherwise clean**: every `mark_split` sums to `marks_total` (7 for the 7 LAQ
  cards, 2 for the 13 VSAQ cards), every step's running total matches, and every `margin_note`'s stated
  mark count matches its own step's `marks=` value — the three mark_split findings above are wording
  mismatches (which step's prose describes which mark's work), not arithmetic or mark-count errors.
- **Rule 41**: only one clean idiom/metaphor found ("the address of any point," filed above); a few
  milder candidates ("n always leaves first," "handing over y," "the trip from P to the y-axis") were
  considered but left unfiled as ordinary informal register rather than genuine idiom/personification.
