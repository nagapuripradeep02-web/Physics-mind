# P2-02 — physics_2, Unit 2 Ray Optics and Optical Instruments (part 1/1)

Cards audited: 20 / 20   Findings: HARMFUL 2 · WRONG 1 · WEAK 1

Every printed number was recomputed in one Python script (Fraction/sympy-style arithmetic): mirror
formula f = −20 cm; small-angle prism μ = 1.62; concave-lens u = −270 cm, v = −27 cm; wall-image
u = −14 cm, 21 cm from the wall; spectacle lens f = +50 cm; μ = √2 ≈ 1.414. The rainbow card's angles
were checked with a Descartes minimum-deviation ray trace (k = 1, 2 internal reflections, n = 1.331
red / 1.343 violet), and the R = 2f concave-mirror proof was checked algebraically (exterior-angle
theorem, small-angle limit). Every `mark_split` sum, label pairing and `expected_time_min` convention
(VSAQ 4 min / SAQ 8 min / LAQ 15 min) was verified across all 20 cards — all consistent.

## HARMFUL — wrong content a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_ray_compound_microscope_magnification | s5_product | `RECALL must_convey`: "…m₀ = −v₀/u for the objective" and `accept[2]`: "m nought is minus v nought by u". This is the exact formula the step's own printed `LINE` gives as `m₀ = v₀/u` (no minus) and that the step's own `MISTAKE[1]` names as the wrong "MIRROR form" ("for a lens m₀ = v₀/u, and the inversion comes from u being negative"). The chatbot grading rubric requires the very formula the card teaches students not to write, and could mark down a student who correctly recites `m₀ = v₀/u`. | must_convey="That m = m₀ × mₑ and that m₀ = v₀/u for the objective (no separate minus sign — the inversion comes from u being negative)."; accept[2] → "m nought is v nought by u". |
| ts_ipe_p2_ray_compound_microscope_magnification | s6_eyepiece_m | `RECALL must_convey`: "…m = (−v₀/u)(1 + D/fₑ)" and `accept[2]`: "m equals minus v nought by u into one plus D by f e". This contradicts the step's own boxed equation (2), `m = (v₀/u)(1 + D/fₑ)` (no minus), and contradicts step `s7_near_point`'s own `WHY`, which explains that the minus sign in equation (3) comes later, from substituting u ≈ −f₀ — "not from the magnification formula." | must_convey="That the eyepiece acts as a simple microscope with mₑ = 1 + D/fₑ, giving m = (v₀/u)(1 + D/fₑ)."; accept[2] → "m equals v nought by u into one plus D by f e". |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_ray_formation_of_rainbow | s3_inside_drop | `MISTAKE[2]`: "Claiming the angle inside the drop exceeds the critical angle. It does not — about 40° against a critical angle of 48.8°." This condemns exactly what the step's own graded `LINE` says: "These colours meet the far inside surface at an angle greater than the critical angle of water, about 48°, so they are totally internally reflected" — content the step's own `WHY` says is deliberately kept as "the book's version" that a student "should write." A student who wrote the card's own boxed working would be told by `MISTAKE[2]` that it is wrong. (Machine-checked: the minimum-deviation primary-bow ray does strike the back surface at ≈40.4°, below the ≈48.75° critical angle for water — so the physics aside in `WHY` is itself accurate; the defect is only that `MISTAKE[2]` contradicts the step's own printed line.) | Drop `MISTAKE[2]`, or replace it with a genuine mistake, e.g. "Saying the reflection happens at the front (entry) surface. It happens at the far, exit-side inside surface." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_p2_ray_dispersion_and_most_dispersed_colour | s2_violet | `MEMORY_TIP`: "Short wavelength, large μ, large bend: violet leads, red trails." — "leads"/"trails" personifies the colours as competitors in a race (Rule 41). | "Short wavelength, large μ, large bend: violet bends most, red bends least." |

## Teacher-gate questions

- **Diagram-mark convention.** Across this group, a 0-mark diagram step is upgraded to a marked step
  only when the printed question uses the imperative "Draw a neat labelled diagram" — this fires for
  exactly one card, `ts_ipe_p2_ray_compound_microscope_magnification` (both diagram steps carry 1 mark
  each there). Every other card, including the two proof/derivation SAQs that ask for "a neat diagram"
  or "a neat labelled diagram" without the word "Draw" (`ts_ipe_p2_ray_focal_length_concave_mirror_r_2f`,
  `ts_ipe_p2_ray_critical_angle_total_internal_reflection`) and the 8-mark
  `ts_ipe_p2_ray_snells_law_prism_refractive_index` ("Using a neat labelled diagram"), keeps its
  diagram at 0 marks. The policy is applied consistently, but it is an invented rule — a Telangana
  IPE teacher should confirm whether diagram credit really turns on that exact wording, or whether any
  question asking for a diagram at all should carry a diagram mark.
- `ts_ipe_p2_ray_snells_law_prism_refractive_index` bundles one printed 8-mark LAQ (derivation, part a,
  6 marks across 6 blocks) with a numerical (part b, 2 marks in one block) into a single card, per the
  book's own printing. The card's own `VERIFICATION_NOTE` already flags this 6+2 split as an invented
  claim — worth a teacher's explicit confirmation.

## Cards checked clean

ts_ipe_p2_ray_focal_length_radius_concave_lens, ts_ipe_p2_ray_focus_principal_focus_lenses, ts_ipe_p2_ray_laws_reflection_curved_mirrors, ts_ipe_p2_ray_power_of_convex_lens, ts_ipe_p2_ray_pin_image_at_object_position, ts_ipe_p2_ray_small_angle_prism_refractive_index, ts_ipe_p2_ray_concave_lens_image_one_tenth, ts_ipe_p2_ray_myopia_and_correction, ts_ipe_p2_ray_hypermetropia_and_correction, ts_ipe_p2_ray_concave_mirror_wall_real_image, ts_ipe_p2_ray_reading_spectacles_focal_length, ts_ipe_p2_ray_focal_length_concave_mirror_r_2f, ts_ipe_p2_ray_critical_angle_total_internal_reflection, ts_ipe_p2_ray_formation_of_mirage, ts_ipe_p2_ray_setting_sun_appears_red, ts_ipe_p2_ray_simple_microscope_image_formation, ts_ipe_p2_ray_snells_law_prism_refractive_index

(17 clean; the other 3 carry the findings above — `ts_ipe_p2_ray_formation_of_rainbow` and
`ts_ipe_p2_ray_critical_angle_total_internal_reflection` also carry the two pre-known figure defects
that are being repaired separately and are NOT re-reported here.)

## Notes

- **Every boxed numerical answer in the group is correct** — re-derived independently and matched by
  script: f = −20 cm (pin-image concave mirror); μ = 1.62 (small-angle prism); u = −270 cm, v = −27 cm
  (concave-lens 1/10-size image); u = −14 cm, 21 cm from the wall (concave-mirror-on-wall); f = +50 cm
  (reading spectacles); μ = √2 ≈ 1.414 (Snell's-law grazing-incidence numerical). The symbolic R = 2f
  proof (`ts_ipe_p2_ray_focal_length_concave_mirror_r_2f`) checks out step by step: the exterior-angle
  argument (∠MFD = 2θ from the two θ interior angles of ΔMCF) is the standard textbook proof, and the
  small-angle substitution (CD→R, FD→f) correctly cancels MD to give R = 2f.
- **Rainbow angles independently verified by ray-tracing**: primary bow, violet 40.65° / red 42.37°
  (card claims "violet at about 40°, red at about 42°" — matches, and confirms red sits at the larger
  angle, i.e. "on top"); secondary bow, red 50.37° / violet 53.48° (card claims "between about 50° and
  53°" with colours reversed — matches). The critical angle of water, 48.75°, and the ≈40° angle of
  incidence at the second internal surface for the primary bow (both quoted in the card's `WHY`) are
  also independently confirmed — the internal reflection that forms a rainbow is genuinely NOT total in
  the strict sense, which is accurate context, not a defect; the defect is only the `MISTAKE[2]`
  self-contradiction reported above.
- **`MARK_SPLIT` sums, label pairings, and 0-mark-diagram-has-no-row exclusions all check out** across
  all 20 cards (2/2 for the 12 VSAQs, 4/4 for the 6 SAQs, 8/8 for the 2 LAQs). `expected_time_min` is
  uniform by `qtype` (VSAQ 4, SAQ 8, LAQ 15) with no outlier.
- **No other `MISTAKE`/`common_mistakes` entry in the group condemns valid alternative physics** — all
  ~80 entries across the 20 cards were checked individually against their own step's working; the one
  exception is the rainbow `MISTAKE[2]` reported above.
- **`INSIDER_NOTE`** on all 20 cards was checked against its card's actual step structure and content —
  all 20 are accurate summaries.
- **Sign conventions** (Cartesian, real/virtual, magnification sign) were checked on every numerical
  card and are internally consistent: mirror formula 1/f = 1/v + 1/u vs lens formula 1/f = 1/v − 1/u are
  used correctly and distinguished in `MISTAKE` entries throughout; concave-lens magnification is
  correctly taken positive (erect, virtual, diminished) in `ts_ipe_p2_ray_concave_lens_image_one_tenth`;
  lens magnification m = v/u (no separate minus sign) is correctly stated in
  `ts_ipe_p2_ray_compound_microscope_magnification`'s own `LINE`/`MISTAKE[1]` for step s5 — which is
  exactly why that same step's (and s6's) `RECALL` fields are flagged HARMFUL above for using the wrong
  sign.
- Figure stroke coordinates were spot-checked geometrically (e.g. the two prism-face normals in
  `ts_ipe_p2_ray_snells_law_prism_refractive_index` are perpendicular to their respective faces to
  within pixel rounding, and correctly pass through Q and R) — no new figure defects found beyond the
  two already known and excluded from this report.
- Rule 41 (plain language) was swept across all reader-facing strings in the group; the one finding is
  reported above under WEAK.
