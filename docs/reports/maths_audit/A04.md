Cards audited: 30 / 30   Findings: HARMFUL 0 · WRONG 6 · WEAK 5

Group A04 — Maths-1A: Mathematical Induction (u4, 10 cards) · Trigonometric Equations (u9, 10 cards) · Inverse Trigonometric Functions (u10, 10 cards).
Every closed form was checked numerically at n = 1, 2, 3; every general solution was checked for the `n ∈ Z` family form; every inverse-trig identity was checked numerically in degrees against the principal branch. Every `steps[].marks` sum equals `marks_total` and every `mark_split` sums to the same (30/30 correct). No induction card assumed P(k+1), and every base case is actually computed on both sides.

## HARMFUL — wrong mathematics a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| _(none found in this group)_ | | | |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_te_cos2t_cos8t_cos5t | s3_solve_cos5_0 | `common_mistakes`: "Using 2nπ ± π/2 and missing solutions", and `why`: "cos = 0 at every odd multiple of π/2, which is a **different formula** from cos = cos α. **Using the wrong one here loses half the solutions.**" Both are false. `2nπ ± π/2` over n ∈ Z is {…, −3π/2, −π/2, π/2, 3π/2, 5π/2, …} — *exactly* the set `(2n+1)π/2`. After dividing by 5, `2nπ/5 ± π/10 = (4n±1)π/10` and `(2n+1)π/10` are likewise the same set (4n±1 runs over all odd integers). A student who solves it the second way loses nothing and would be marked correct. | Drop the mistake line and rewrite the `why`: `(2n+1)π/2` is the standard form for cos θ = 0 and is quicker to divide through; `2nπ ± π/2` gives the same set of angles, so either is acceptable. |
| ts_ipe_m1a_te_root2_sin_plus_cos | s2_recognise_the_standard_ang | `memory_tip` ("…so this is the cosA cosB + sinA sinB pattern, **not a sine one**") and both `common_mistakes` ("Reading √3/2 as sin60° and finishing with a sine equation", "Using the sin(A+B) pattern") condemn a correct route. (1/√2)(sin x + cos x) **is** sin(x + π/4), so sin(x + π/4) = sin(π/3) gives x + π/4 = nπ + (−1)ⁿπ/3 and the identical solution set {π/12, 5π/12} + 2nπ. | Say the equal coefficients let the left side be read *either* as cos(x − π/4) *or* as sin(x + π/4) — both close; the cosine reading is the one shown. Remove the two mistake lines. |
| ts_ipe_m1a_it_sin_cos_cos | s2_apply_the_cos_a_b_formula | `why`: "Using the sine formula here would give **63/65** and a wrong answer." The sine formula gives sin(α+β) = (3/5)(12/13) + (4/5)(5/13) = 36/65 + 20/65 = **56/65**. 63/65 is what the *plus-sign* slip in the cosine formula gives (48/65 + 15/65 = cos(α−β)) — which is the other mistake already listed. A false number is stated to the student as fact. | "Putting a plus between the two products gives 63/65, which is cos(α − β), not cos(α + β)." |
| ts_ipe_m1a_it_sin_plus_two_tan | s1_state_the_double_angle_for | `why`: "Choosing the Cos⁻¹ form is what sets up the final identity — **the other two would not close**", plus `common_mistakes`: "Using the Sin⁻¹ or Tan⁻¹ form and being unable to finish." Both are false. Sin⁻¹ route: 2Tan⁻¹(1/3) = Sin⁻¹(2x/(1+x²)) = Sin⁻¹(3/5), and Sin⁻¹(4/5) + Sin⁻¹(3/5) closes in one line since sin of the sum = (4/5)(4/5) + (3/5)(3/5) = 1 ⇒ π/2. Tan⁻¹ route: 2Tan⁻¹(1/3) = Tan⁻¹(3/4) and Sin⁻¹(4/5) = Tan⁻¹(4/3), so Tan⁻¹a + Tan⁻¹(1/a) = π/2 closes it too. | Say the Cos⁻¹ form is chosen because it matches the 4/5 already on the page and finishes in the fewest lines; the Sin⁻¹ and Tan⁻¹ forms also reach π/2 but need one extra identity. |
| ts_ipe_m1a_mi_divisible_3_5_2n1_2_3n1 | s3_assume | `why`: "the assumption can be used **as a whole rather than rearranged**" — but s4 substitutes the REARRANGED form, `= 25(17q − 2^(3k+1)) + 8(2^(3k+1))   [from (1)]`, and the card's own `insider_note` says "Isolate 3·5^(2k+1) from the assumption first". The prose contradicts the working printed beside it, and s3's `lines` omit the `⇒` rearrangement that the sibling card (`…_2_4_2n1_3_3n1`) prints — so s4 line 3 does not follow from the last line the student has written down. | Add `⇒ 3·5^(2k+1) = 17q − 2^(3k+1)` to s3's lines and rewrite the `why` to say the assumption is rearranged to isolate the first power, matching the sibling card. |
| ts_ipe_m1a_mi_sum_cubes_over_odds | s4_inductive | `memory_tip`: "These two sides **will not factorise** into each other, so EXPAND both"; `common_mistakes`: "Trying to factorise and giving up when nothing comes out"; `why`: "This one does not [have a common factor to pull out]". The cubic does factorise: 2k³ + 15k² + 37k + 24 = (k+1)(2k² + 13k + 24) — which is precisely the factored right-hand side the card prints two lines below. A student who factors reaches the answer and is told they were on a dead end. | "The left side can be factored as (k+1)(2k² + 13k + 24), but spotting the (k+1) is not obvious — expanding both sides to the same cubic always works and is safer under time pressure." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_te_roots_tan_theta | s2_read_off_the_sum_of_roots | `margin_note`: "For ax² + bx + c = 0 the sum of the roots is −b/a." The letters a, b, c are the *question's own constants*; read literally this says the sum is −b/a, while the answer is 2b/(c+a) — and `b/a` is literally the answer to part (iii) of the same card. The step `lines` correctly use A, B, C. | Use the same capitals as the lines: "For Ax² + Bx + C = 0 the sum of the roots is −B/A. Here B = −2b and A = c + a." |
| ts_ipe_m1a_mi_sqp_gp_sum | (card `insider_note`) | "The nth term is ar^(k−1), not ar^k" — mixes the running index; the nth term is arⁿ⁻¹ (the kth is ar^(k−1)). The step lines themselves are right. | "The nth term is arⁿ⁻¹, not arⁿ." |
| ts_ipe_m1a_mi_sum_reciprocal_step_three · ts_ipe_m1a_it_tan_three_quarters | s4_inductive · s1_combine_the_first_two | Reader-facing prose cites source-book numbering a student cannot resolve: "Identical shape to **question 7**" (margin_note) and "the same first pair as **answer 75** arrived at" (why). | Name the mathematics, not the book's index: "Identical shape to the 1/(1·3) question" / "the same first pair, 3/4 and 3/5, that Cos⁻¹(4/5) + Sin⁻¹(3/√34) produced." |
| ts_ipe_m1a_te_one_plus_sin_sq · ts_ipe_m1a_te_root2_sin_plus_cos · ts_ipe_m1a_te_roots_sin_alpha_beta · ts_ipe_m1a_it_cos_two_tan_four_tan · ts_ipe_m1a_it_sin_cos_cos · ts_ipe_m1a_it_tan_three_quarters | multiple `why` / `margin_note` | "the twin" / "the twin question" / "both twins" appears in reader-facing prose on six cards with no antecedent the student can see (e.g. "unlike the twin question nothing is rejected here"; "The left side is word for word the twin question"; "cosine here, sine in the twin"). The chatbot repeats these verbatim. | Name the other question by its statement ("the version with sin(2Tan⁻¹(3/4)) on the right"), or drop the clause. |
| ts_ipe_m1a_te_sqp_cot_quadratic · ts_ipe_m1a_te_roots_sin_alpha_beta | s1_split_the_middle_term · s1_isolate_and_square | Rule 41 register + a non-justification. (a) "The question **hands over** the split rather than hiding it" — personification of the question. (b) The squaring justification does not justify anything: "it is legitimate here because the roots are being characterised, not found — any extraneous root introduced would still satisfy the squared equation." The second clause is true but irrelevant; what makes the step sound is that sinα and sinβ both satisfy the quadratic and a quadratic has only two roots. | (a) "The middle coefficient is already printed as (√3 + 1), which names the two parts of the split." (b) "sinα and sinβ both satisfy this quadratic, and a quadratic has only two roots — so they are exactly its roots." |

## Cards checked clean

ts_ipe_m1a_mi_divisible_2_4_2n1_3_3n1, ts_ipe_m1a_mi_divisible_49n_16n_minus_1, ts_ipe_m1a_mi_sqp_product_squares, ts_ipe_m1a_mi_sum_n_np1_np2, ts_ipe_m1a_mi_sum_of_squares_series, ts_ipe_m1a_mi_sum_reciprocal_odd_pairs, ts_ipe_m1a_te_root3_sin_minus_cos, ts_ipe_m1a_te_sin_sin5_sin3, ts_ipe_m1a_te_sqp_sin_plus_root3_cos, ts_ipe_m1a_te_two_cos_sq_minus_root3, ts_ipe_m1a_it_cos_sin_tan, ts_ipe_m1a_it_cos_two_tan_seventh, ts_ipe_m1a_it_sin_sin_sin, ts_ipe_m1a_it_sqp_tan_inverse_zero, ts_ipe_m1a_it_tan_half_fifth_eighth, ts_ipe_m1a_it_xy_yz_zx_one

## Notes

- **The dominant defect in this group is the confirmed group-wide shape:** four of the six WRONG findings are prose condemning a mathematically correct alternative route or form (`te_cos2t_cos8t_cos5t`, `te_root2_sin_plus_cos`, `it_sin_plus_two_tan`, `mi_sum_cubes_over_odds`). In three of those four the false claim is repeated across *two or three* fields on the same step (`why` + `memory_tip` + `common_mistakes`), so a fix must sweep all of them, not just the mistake list.
- **The mathematics itself is sound.** All 30 model answers reach the correct result by a valid route: 10/10 induction closed forms verified at n = 1, 2, 3 and every inductive step re-derived line by line (each line follows from the one above); 10/10 trig general solutions carry `n ∈ Z` and use the correct per-function form (nπ + (−1)ⁿα for sine, 2nπ ± α for cosine, nπ + α for tangent), with the one genuine extraneous root (`sinθ = −√3` in `te_two_cos_sq_minus_root3`) rejected in writing and the one interval question (`te_sin_sin5_sin3`, 0 < θ < π) listing all four values and no extras; 10/10 inverse-trig results verified numerically in degrees, and every one lands inside the principal range of the inverse function it is stated in (checked explicitly: `it_cos_sin_tan` 67.83°, `it_sin_cos_cos` 59.49°, `it_sin_sin_sin` 69.39°, `it_tan_half_fifth_eighth` 45.0°, `it_tan_three_quarters` 45.0°). **No quadrant, branch or sign error was found in this group.**
- Two standard board-level omissions were noted and deliberately NOT reported, since every printed IPE solution shares them and no examiner deducts: `te_one_plus_sin_sq` divides through by cos²θ without stating cosθ ≠ 0 (verified — cosθ = 0 is not a solution, so nothing is lost), and the three inverse-trig addition cards do not verify α + β lies in the principal range before converting back (verified numerically — all three do).
- Arithmetic spot-checks that all passed, recorded so they need not be re-run: 209 = 11·19; 391 = 17·23; 49 + 16 − 1 = 64; 16·48 = 768 = 64·12; 27 − 16 = 11; 25 − 8 = 17; 2k³+15k²+37k+24 reached from both sides; (k+1)(k+2)(k+3)(k+4)/4; (k+1)(k+2)²(k+3)/12; 65/72 ÷ 65/72 = 1; 425/425 = 1; 96/125 + 21/125 = 117/125; 48/65 − 15/65 = 33/65.
- Stale ordinal mark notes were not re-reported (per `ORDINAL_MARK_NOTES.md`). One related item is left un-reported for the same reason: `it_sin_plus_two_tan` s1 `margin_note` says "the book prints [1 Mark] for writing the formula alone", a source-book mark claim that survived the SAQ re-cut.
- No `insider_note` in this group claims exam appearance history; all 30 cards have `appearances: []` and none of the notes assert a year or a paper.
