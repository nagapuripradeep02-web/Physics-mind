Cards audited: 26 / 26   Findings: HARMFUL 0 · WRONG 2 · WEAK 2

Unit: Maths-1A Unit 1 — Sets and Relations (26 VSAQ cards, 52 steps).
Every set, complement, Cartesian product, power set and relation was enumerated by hand and
compared against the card. Every step-mark sum, mark_split sum, mark_split↔step label pairing and
mark_split↔step marks pairing was checked mechanically. No card in this group carries an
`insider_note` (correct — the chapter has no exam history).

## HARMFUL — wrong mathematics a student would copy into an exam

_(none)_

Every boxed answer in the group is correct. Spot list of the re-derivations that matter:
A − (B − C) = {1, 3}; (A×B) ∩ (A×C) = {(1,3),(2,3),(3,3)}; the 12/3/12-pair distributive card;
both De Morgan cards (U₇: A′={4,5,6,7}, B′={1,2,4,7}, (A∪B)′=A′∩B′={4,7}; U₉: (A∩B)′=A′∪B′=U);
(A∩C)∪B = (A∪B)∩(C∪B) = {1,2,3,4,5,7}; A∩B∩C = {7}; 2ⁿ = 512 ⇒ n = 9; P({−3,0,3}) all 8 subsets;
2x + y = 41 giving exactly 20 pairs with all three counterexamples genuinely inside R
((1,39) and (39,1)∉R; (12,17)∈R, (17,7)∈R, (12,7)∉R); x + y = 5 rejecting only x = 0;
x < y giving 5 pairs; A Δ B = {b,d,e,f,g,h} with the 8 − 2 = 6 check.

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_sr_define_symmetric_relation | s1_definition | `common_mistakes[2]`: "Writing the arrow the wrong way, so nothing is required of the pairs in R." This is not a mistake. With a and b universally quantified, "(b,a) ∈ R ⇒ (a,b) ∈ R" is the *same statement* as "(a,b) ∈ R ⇒ (b,a) ∈ R" (rename the two letters), so it requires exactly as much of R. The card presents a correct alternative form as an error, and its stated consequence is false. | Replace with a mistake that is real, e.g. "Writing (a,b) ∈ R ⇔ (b,a) ∈ R and treating the reverse pair as given rather than as what must be shown", or drop the item — the step already carries two genuine errors (the "and" form, and confusing symmetric with reflexive). |
| ts_ipe_m1a_sr_parallel_lines_equivalence | s2_transitive | `why` ends: "It also needs the same convention: without it, l ∥ m and m ∥ l would force l ∥ l, **which that convention denies**." The nearest antecedent of "that convention" is the one just named — the convention that a line *is* parallel to itself — which does not deny l ∥ l, it asserts it. As written the sentence contradicts itself; the reasoning intended (under the strict "distinct lines that never meet" definition, transitivity would force the false l ∥ l) is correct but is not what the words say. | "…without it, l ∥ m and m ∥ l would force l ∥ l, which the strict definition — parallel lines must be distinct — denies, so transitivity would fail." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_sr_power_set_512_elements | s2_solve | `memory_tip` "Powers of 2 worth knowing **by heart**: 2⁶ = 64, …" — "by heart" is an idiom (Rule 41a); the tip is the string the student reads directly. | "Powers of 2 worth remembering: 2⁶ = 64, 2⁷ = 128, 2⁸ = 256, 2⁹ = 512, 2¹⁰ = 1024." |
| ts_ipe_m1a_sr_de_morgan_universal_1_to_7 | s2_both_laws | Mark shape is out of line with its twin card. Here mark 1 buys only the two complements and mark 2 buys **both** full De Morgan verifications (four sets computed and two comparisons); the near-identical `..._1_to_9` card puts the complements *and* law (i) in mark 1 and law (ii) in mark 2. A student working the two cards side by side sees the same task marked two different ways, and `margin_note` here ("Both parts are needed for this mark") makes the heavier half the cheaper-looking one. | Re-cut to match `..._1_to_9`: s1 = complements + law (i), s2 = law (ii) + conclusion. Both cards are 1 + 1 = 2 either way, so nothing else moves. |

## Cards checked clean

ts_ipe_m1a_sr_a_minus_b_minus_c, ts_ipe_m1a_sr_cartesian_product_intersection, ts_ipe_m1a_sr_cartesian_product_over_union_intersection, ts_ipe_m1a_sr_de_morgan_universal_1_to_9, ts_ipe_m1a_sr_define_cartesian_product, ts_ipe_m1a_sr_define_equivalence_relation, ts_ipe_m1a_sr_define_power_set, ts_ipe_m1a_sr_define_reflexive_relation, ts_ipe_m1a_sr_define_relation, ts_ipe_m1a_sr_define_set_and_cardinality, ts_ipe_m1a_sr_define_symmetric_difference, ts_ipe_m1a_sr_define_transitive_relation, ts_ipe_m1a_sr_distributive_union_over_intersection, ts_ipe_m1a_sr_intersections_three_sets, ts_ipe_m1a_sr_power_set_minus3_0_3, ts_ipe_m1a_sr_relation_2x_plus_y_41_properties, ts_ipe_m1a_sr_relation_x_equals_y_domain_range, ts_ipe_m1a_sr_relation_x_less_than_y, ts_ipe_m1a_sr_relation_x_plus_y_5_domain_range_codomain, ts_ipe_m1a_sr_set_builder_form, ts_ipe_m1a_sr_subsets_and_power_set_of_1_2_3, ts_ipe_m1a_sr_symmetric_difference_abcde_acfgh

(22 clean; the other 4 carry the findings above.)

## Notes

- **Mathematically this group is in good shape.** 52/52 steps re-derived independently; zero wrong
  arithmetic, zero wrong set, zero wrong verdict, zero wrong count. The two findings are both in
  explanatory prose (`common_mistakes`, `why`), not in a line a student copies onto the answer sheet.
- **`insider_note`: 0 / 26** — as the unit intends. Nothing to report under check 6.
- **Marks:** every card sums 1 + 1 = 2 = `marks_total`; every `mark_split` entry matches its step's
  label and marks exactly; every `mark_note` ("Method"/"Answer"/"Definition"/"Example"/"Counts")
  describes work the step actually does.
- **No duplicated or self-repeating `why`** anywhere in the group (checked across all 52 steps and
  sentence-wise inside each). No duplicated `memory_tip`.
- **No `common_mistakes` item quotes a quantity absent from its problem.** The near-miss values that
  appear (A ∪ B = {1,2,3,4,5,6,7,8,11}; 6 + 9 = 15 pairs; A − B = {5,6}; (A − B) − C = {1};
  B × A = {(x,1),(x,2),(y,1),(y,2)}) were each recomputed and are all correct for their card.
- Minor, not filed as findings: (a) `ts_ipe_m1a_sr_define_symmetric_relation` s1 `common_mistakes[0]`
  restates the point already made in the same step's `why` (the "and" form forcing R = A × A) —
  correct, just redundant; (b) `ts_ipe_m1a_sr_define_cartesian_product` s2 uses "loses the order",
  which reads as ordinary English rather than personification; (c) the unit mixes `n(P(A))` and
  `|P(A)|` notation between cards (both standard, and `subsets_and_power_set_of_1_2_3` follows its
  own question's notation).
- The Telugu phrases in `recall.accept` (ASR acceptance strings, not rendered text) were left alone —
  they are bank-wide and outside this audit's reader-facing scope.
