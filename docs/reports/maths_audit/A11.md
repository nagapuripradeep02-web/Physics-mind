Cards audited: 23 / 23   Findings: HARMFUL 0 · WRONG 6 · WEAK 6

Unit 12, Properties of Triangles (Maths-1A). Every sine-rule, cosine-rule, projection,
half-angle, in-radius, ex-radius and circumradius identity on these 23 cards was re-derived
independently and then substituted numerically into a 3-4-5 triangle (and, where the card
supplies its own, the 13-14-15 and 12-16-20 triangles) on BOTH sides before being accepted.
Every printed line was checked to follow from the line above it, not merely to reach the
right final answer. All 23 cards pass "sum of steps[].marks == marks_total", and all 23
mark_split[] arrays match their scoring steps label-for-label and sum to marks_total.
No mathematical error was found. Everything below is prose attached to correct mathematics.

## HARMFUL — wrong mathematics a student would copy into an exam

None. No algebra error, no sign error, no quadrant/angle error, and no misquoted formula was
found on any of the 23 cards.

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_pt_a_cos_sq_half | s3_use_the_sin2a_identity | `margin_note` calls Σsin2A = 4 sinA sinB sinC "the Unit 6 identity" and `why` says "This is exactly the identity proved as Unit 6 answer 26.1." **Unit 6 of Maths-1A in this bank is "Addition of Vectors"** (verified across all 301 m1a cards). This identity belongs to Unit 8, Trigonometric Ratios and Transformations. The card sends the student — and the chatbot repeats it — to the wrong chapter. | "the Trigonometric Ratios and Transformations identity" / "This is the identity proved in the Trigonometric Transformations unit." |
| ts_ipe_m1a_pt_r1_over_bc | s3_use_the_half_angle_sum_ide | Same defect, three times in one step: `margin_note` "the Unit 6 identity, quoted", `why` "This is exactly Unit 6 answer 27.1", and `recall.must_convey` "the Unit 6 identity Σsin²(A/2) = 1 − 2sin(A/2)sin(B/2)sin(C/2)". Unit 6 is Addition of Vectors; this is a Unit 8 identity. | Name Unit 8 / "Trigonometric Ratios and Transformations", or drop the unit number and name the identity. |
| ts_ipe_m1a_pt_saq_cos_over_a | s2_add_and_conclude | `margin_note` "Same cancellation as answer 81" and `why` "The numerator is identical to answer 81 — only the denominator differs, 2abc here against 4Δ there." "answer 81" is the **Baby Bullet-Q source book's** answer number (confirmed: it is the number carried in `ts_ipe_m1a_pt_saq_cot_sum`'s own verification note). A student has no "answer 81". The substance is true; the pointer names a document the student does not hold. | "Same cancellation as the cot A + cot B + cot C question — only the denominator differs, 2abc here against 4Δ there." |
| ts_ipe_m1a_pt_a_eq_b_plus_c_cos | s3_introduce_the_half_angle | `why` ends "Its partner, 1 − cos A = 2sin²(A/2), is what answer 34.2 uses instead." Same class — a source-book answer number presented to the student as a cross-reference. | "…is what the a = (b − c) sec θ question uses instead." |
| ts_ipe_m1a_pt_sin_theta_a_over_b_plus_c | s1, s2, s3, s4 (`margin_note` ×4, `why` ×1) | Five reader-facing strings point at "answer 33", and s2 `why` states "which is why **the book** prints them side by side." The card explains itself by describing what a coaching publication does, which the student cannot check and should not be told. Note s3's `memory_tip` already does it correctly ("the same half of the pair as the a = (b+c)cos θ question") — the fix pattern exists inside the same card. | Replace every "answer 33" with "the a = (b + c) cos θ question", and delete the clause about what the book prints. |
| ts_ipe_m1a_pt_saq_r1_r2_r3_minus_r | s2_apply_the_compound_formula | `margin_note` describes working the step does not do: "…then (A+B)/2 = 90° − C/2 converts both into C, and the two terms combine." The printed lines never make that substitution — they apply sinX cosY + cosX sinY = sin(X+Y) directly to reach 4R sin((A+B+C)/2) = 4R sin 90°. The note reads as cloned from the sibling card `ts_ipe_m1a_pt_saq_r_r3_r1_minus_r2`, where that substitution IS the working. (The step's own `memory_tip` describes the real route correctly, so the two disagree.) | "The first bracket is sin((A+B)/2) and the second cos((A+B)/2); the compound formula combines them into one sine of (A+B+C)/2, which is 90°." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_pt_a_eq_b_minus_c_sec | s3_introduce_the_half_angle (cm), s4_take_the_square_root (`margin_note`, `why`, cm) | Four strings call the companion questions "the twins" — a figurative label with no antecedent anywhere on the card. Rule 41 (no metaphor) and simply unresolvable for the reader. | Name them: "the a = (b + c) cos θ and sin θ = a/(b + c) questions". |
| ts_ipe_m1a_pt_prove_345 | s1_find_and_s (`why`), s2_find_each_side_from_its_ex (`margin_note`) | "the chain is one step shorter than its twin" / "Same pairing as the twin" — same undefined "twin". | "…than the version where r is not given" / "Same pairing as the r₁ = 8, r₂ = 12, r₃ = 24 question". |
| ts_ipe_m1a_pt_saq_r_r3_r1_minus_r2 | s1_group_into_two_pairs | `common_mistakes` "Copying the twin and grouping around C." Same undefined referent, and the warning only makes sense to someone who has just read a different card. | "Grouping around C, as in the r₁ + r₂ + r₃ − r question, instead of around B." |
| ts_ipe_m1a_pt_sin_theta_a_over_b_plus_c | s1 (`why`), s2 (`why`), s4 (`why`, cm) | "this twin", "the two twins", "Both twins", "the twin" — four more of the same undefined metaphor. | Same fix: name the companion question. |
| ts_ipe_m1a_pt_sin_theta_a_over_b_plus_c | s4_take_the_square_root | `common_mistakes` reads "Assuming the answer must differ from the twin and re-checking needlessly." This marks *verifying your own answer* as a mistake — the opposite of what a mark scheme rewards, and not a mistake at all. | Drop the line, or leave only the real slip already listed second ("Writing √(4bc) as 4√(bc)"). |
| ts_ipe_m1a_pt_altitudes s3_prove_part_ii · ts_ipe_m1a_pt_find_abc_from_r s1_find_r_from_the_three_ex_r | `why` (both) | Rule 41 register: "Writing a + b − c as a + b + c − 2c **is the trick**" and "That single standard result is what **unlocks** Δ, then s, then the sides" (a formula does not unlock anything). | "…is the step that turns an awkward combination into 2s minus something" / "That single standard result is what makes Δ, then s, then the sides available." |

## Cards checked clean

ts_ipe_m1a_pt_13_14_15, ts_ipe_m1a_pt_cot_half_ratio, ts_ipe_m1a_pt_ratio_789, ts_ipe_m1a_pt_saq_a_sq_cot, ts_ipe_m1a_pt_saq_c_is_60, ts_ipe_m1a_pt_saq_cos_ab_ratio, ts_ipe_m1a_pt_saq_cot_half_sum, ts_ipe_m1a_pt_saq_cot_ratio_357, ts_ipe_m1a_pt_saq_cot_sum, ts_ipe_m1a_pt_saq_four_r1r2_sum, ts_ipe_m1a_pt_saq_reciprocal_squares, ts_ipe_m1a_pt_saq_sec_squared_equal

## Notes

- **Numeric back-substitution used.** 3-4-5 (Δ = 6, s = 6, r = 1, r₁ = 2, r₂ = 3, r₃ = 6, R = 5/2)
  was substituted into both sides of every identity card: a cos²(A/2)+… = s + Δ/R (8.4 = 8.4);
  Σ r₁/(bc) = 1/r − 1/(2R) (0.8 = 0.8); a²cotA+… = abc/R (24 = 24); Σ1/r² = Σa²/Δ² (1.3889 = 1.3889);
  4(r₁r₂+r₂r₃+r₃r₁) = (a+b+c)² (144 = 144); r₁+r₂+r₃−r = 4R (10 = 10); r+r₃+r₁−r₂ = 4R cos B (6 = 6);
  all three branches of (r₁+r₂)sec²(C/2) (10 = 10 = 10); (1+cos(A−B)cosC)/(1+cos(A−C)cosB) =
  (a²+b²)/(a²+c²) (0.7353 = 0.7353); the cot(A/2)-ratio card (2.88 = 2.88); cosA/a+… = Σa²/(2abc)
  (0.41667 = 0.41667). The 13-14-15 card was re-derived from Heron (Δ = 84, R = 65/8, r = 4,
  r₁ = 21/2, r₂ = 12, r₃ = 14 — all confirmed) and the 12-16-20 and 6-5-4 answers were re-checked by
  rebuilding s and Δ from the sides the card produces.
- **The notation traps were checked specifically.** r vs R vs r₁/r₂/r₃ pairing (r₁ with s−a, r₂ with
  s−b, r₃ with s−c) is correct on every card that uses it, including `find_abc_from_r`, `prove_345`,
  `altitudes` part (ii) (the minus correctly lands on r₃, not r₁), and `saq_r_r3_r1_minus_r2`. The
  `saq_r_r3_r1_minus_r2` sign handling is the subtlest working in the group — the minus is correctly
  factored out of (r − r₂) before the bracket is reversed — and it is right. s vs the sides is
  handled correctly everywhere; the "a + b + c = 2s, not s" point is stated on six cards and is
  never itself misapplied.
- **No card in this group quotes a formula that silently needs an acute triangle.** All half-angle,
  ex-radius, Σsin2A and Σsin²(A/2) results used here hold for any triangle.
- The `Unit 6` mis-citation is a **cross-card pattern**, not a one-off: it appears on the only two
  cards in this unit that quote a transformations identity, in five separate reader-facing strings.
  Worth a grep across the rest of the maths bank for "Unit 6" and for source-book answer-number
  pointers ("answer NN") inside `margin_note` / `why` / `memory_tip` / `recall.must_convey`.
- `ts_ipe_m1a_pt_13_14_15` s1 `margin_note` carries "and the book prints [1 Mark] for it as well" —
  the ordinal-mark class already swept in `ORDINAL_MARK_NOTES.md`, so it is not re-reported here, but
  note it is ALSO a source-book reference in reader-facing prose, the same defect as the
  "answer 33" / "answer 81" rows above.
- `ts_ipe_m1a_pt_ratio_789` verification note says "the extra mark goes to the 'Find cos B and cos C'
  step", but after the 2026-08-31 re-cut that step carries 0 marks and no `mark_split` entry. Metadata
  only, not reader-facing — recorded for completeness, not reported as a finding.
- Every `mark_note` was checked against the work its step actually does; all 23 cards pass.
- No card's `insider_note` claims exam history the card lacks; all are method summaries and all are
  true of the working printed beside them.
