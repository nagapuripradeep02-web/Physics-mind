# B09 — IPE Maths-1B, Unit 9 Differentiation (final third)

Cards audited: 24 / 24   Findings: HARMFUL 0 · WRONG 7 · WEAK 7

Every model answer in this group was re-derived independently (chain rule, implicit,
parametric, logarithmic and substitution routes each checked line by line, plus numeric
spot-checks on the substitution cards). **Every boxed final answer is correct**, every step
follows from the one above, and `steps[].marks` sums to `marks_total` and to `mark_split`
on all 24 cards. All findings below are in prose fields, not in the mathematics.

## HARMFUL — wrong mathematics a student would copy into an exam

None found in this group.

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_dif_tan_inverse_root_1_minus_x_over_1_plus_x | s1_substitute | `common_mistakes`: "Substituting x = cos θ instead of cos 2θ, which leaves half-angles and does not simplify." **False — it does simplify, completely.** With x = cos θ: (1 − cos θ)/(1 + cos θ) = tan²(θ/2), the root is tan(θ/2) for θ in (0, π), so y = θ/2 = ½cos⁻¹x — the same line the card reaches, same final answer. It is a fully correct alternative route, and it is the very identity the sibling card `..._root_1_minus_cos_over_1_plus_cos` uses. | Drop the line, or rewrite: "x = cos θ also works (the half-angle identity gives tan(θ/2)); x = cos 2θ is preferred only because it avoids half-angles." |
| ts_ipe_m1b_dif_tan_inverse_root_1_minus_cos_over_1_plus_cos | s2_answer | `common_mistakes`: "Applying the Tan⁻¹ chain rule to tan(x/2) and producing 1/(2(1 + tan²(x/2))) instead of cancelling first." **The chain rule applied correctly gives 1/2, not that expression**: [1/(1 + tan²(x/2))]·(½)sec²(x/2) = 1/2, since sec² = 1 + tan². The printed expression appears only if the inner derivative is written as ½ instead of ½sec²(x/2). As written the line condemns a valid route. | "Writing the inner derivative of tan(x/2) as ½ instead of ½sec²(x/2), which gives 1/(2(1 + tan²(x/2))) instead of 1/2." |
| ts_ipe_m1b_dif_three_tan_inverse_sum_derivative | s4_quadruple | `common_mistakes`: "Expanding (1 − x²)² as 1 − x⁴, which turns the denominator into 1 − 5x² instead of 1 − 6x² + x⁴." **The stated consequence does not follow from the stated error**: (1 − x⁴) − 4x² = 1 − 4x² − x⁴, not 1 − 5x². (1 − 5x² comes from the different slip (1 − x²)² = 1 − x².) | "…which turns the denominator into 1 − 4x² − x⁴ instead of 1 − 6x² + x⁴." |
| ts_ipe_m1b_dif_three_tan_inverse_sum_derivative | s3_triple | `common_mistakes`: "(3 tan θ + tan³θ)/(1 + 3 tan²θ), which are the signs of tan of a sum and not of tan 3θ." **False.** tan(A + B) = (tan A + tan B)/(1 − tan A tan B) — the sum formula carries a MINUS in its denominator, so the all-plus form is not "the signs of tan of a sum" (nor of tan(A − B), which is minus over plus). | "…which has the wrong sign underneath: tan 3θ carries 1 − 3tan²θ, never 1 + 3tan²θ." |
| ts_ipe_m1b_dif_x_ex_sin_x | s2_answer | `common_mistakes`: "Leaving the answer as three unfactored terms and losing the tidy form the key expects." The question is "find the derivative"; eˣ sin x + xeˣ sin x + xeˣ cos x is a complete, correct answer and earns the mark. Condemning it tells the student a correct answer is wrong. | Move it out of `common_mistakes`, e.g. into `why`: "Factoring eˣ out is not required for the mark, but it is the compact form worth writing." |
| ts_ipe_m1b_dif_tan_inverse_sec_x_plus_tan_x | s4_differentiate | `memory_tip`: "Check with two values: y(0) = π/4 and y(π/2) = π/2…". **y(π/2) does not exist** — sec x and tan x both blow up at x = π/2 — and π/2 lies outside the range −π/2 < x < π/2 the card itself imposes in s3. | Use a point inside the range: "y(0) = π/4 and y(π/3) = π/4 + π/6 — a rise of π/6 over a run of π/3, which is 1/2." |
| ts_ipe_m1b_dif_x3_plus_y3_minus_3axy | s2_collect_and_solve | `memory_tip`: "The numerator and the denominator have the same shape with x and y exchanged: ay − x² over y² − ax." **Exchanging x and y in ay − x² gives ax − y², which is minus the denominator.** A student rebuilding the answer from this chip writes (ay − x²)/(ax − y²) — the negative of the correct derivative. | "The denominator is the numerator with x and y exchanged AND the sign reversed: ay − x² on top, y² − ax below. Check the sign, not just the letters." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_dif_tan_inverse_sec_x_plus_tan_x | s1_combine_bracket | `margin_note` "A quotient-rule attempt on the original bracket reaches the same answer only after a page of algebra" (and `why`, which also calls it a quotient). It is a chain rule, and it is three lines: (sec x tan x + sec²x)/(1 + (sec x + tan x)²), denominator = 2sec x(sec x + tan x), numerator = sec x(sec x + tan x), ratio 1/2. | "Differentiating directly also works and cancels to 1/2, but simplifying the bracket first is shorter and is the method being tested." |
| ts_ipe_m1b_dif_tan_inverse_3a2x_minus_x3 | s4_differentiate | `common_mistakes`: "Leaving the answer as 3/(a(1 + x²/a²)) without clearing the inner fraction." That expression equals the boxed answer; it is untidy, not wrong. | "Stopping at 3/(a(1 + x²/a²)) — correct, but clear the inner fraction so the answer reads 3a/(a² + x²)." |
| ts_ipe_m1b_dif_x_power_y_plus_y_power_x_equals_ab | insider_note | "aᵇ is a CONSTANT, so the whole derivative is zero" reads as dy/dx = 0. The zero is the derivative of the whole left-hand side. | "aᵇ is a CONSTANT, so the derivative of the whole left side is zero — that is what makes the answer a single negative fraction." |
| ts_ipe_m1b_dif_x_power_log_y_equals_log_x | s2_differentiate | `margin_note`: "The right side needs the chain rule twice over, giving 1/(x log x)." It is one application of the chain rule to log(log x) — the card's own `memory_tip` states it correctly. | "The right side needs the chain rule on log(log x): one over the inside, times the derivative of the inside." |
| ts_ipe_m1b_dif_tan_inverse_root_sum_over_difference | s3_remove_inverse | Rule 41 register: "this is where careful students separate themselves"; `memory_tip` "Only Cos⁻¹ is the odd one out". Both figurative rather than literal. | "This is the step most often lost."  /  "Cos⁻¹ is the only one of the three with a different range." |
| ts_ipe_m1b_dif_tan_inverse_wrt_sin_inverse | s1_meaning, s4_reduce_g | Rule 41 register: "the one most often thrown away"; "looks like it was quoted from nowhere". | "the mark most often lost"  /  "the reduction to sin 2θ has no working to support it". |
| ts_ipe_m1b_dif_x_power_tan_x_plus_sin_x_power_cos_x | s3_second_term_log | Rule 41 register: "where most of this mark is won or lost". | "where this mark is most often lost". |

## Cards checked clean

ts_ipe_m1b_dif_tan_inverse_log_x, ts_ipe_m1b_dif_tan_inverse_root_1_plus_x2_minus_1_over_x, ts_ipe_m1b_dif_x2_2x_log_x, ts_ipe_m1b_dif_x_a_cos3t_y_a_sin3t, ts_ipe_m1b_dif_x_plus_tan_x_second, ts_ipe_m1b_dif_x_power_x, ts_ipe_m1b_dif_x_power_y_equals_y_power_x, ts_ipe_m1b_dif_x_root_a2_plus_x2_plus_a2_log, ts_ipe_m1b_dif_x_tan_e_minus_y, ts_ipe_m1b_dif_x_two_thirds_plus_y_two_thirds, ts_ipe_m1b_dif_xy_equals_e_x_minus_y, ts_ipe_m1b_dif_y_equals_x_power_y

(The other 12 cards each carry at least one finding above; their mathematics is clean in every case.)

## Notes

- The group list header says 24 cards / 80 steps; 24 files exist and all 24 were audited.
- **Mathematics: zero defects.** Every derivative was recomputed independently. Checked in
  particular: the a-carrying triple-angle reduction 3a/(a² + x²); −1/(2√(1 − x²)) from
  ½cos⁻¹x; 1/(2(1 + x²)) from the half-angle tan(θ/2); −x/√(1 − x⁴) from x² = cos 2θ (the
  ½ × 2x cancellation and the Cos⁻¹ minus sign both survive); df/dg = 1 on −1 < x < 1 with
  the correct −1 outside; the tan 4θ build (1 − x²)² − 4x² = 1 − 6x² + x⁴; the folium
  (ay − x²)/(y² − ax); (y/x)[(1 − log x log y)/(log x)²]; y(x log y − y)/(x(y log x − x));
  the xʸ + yˣ = aᵇ negative fraction; and 2√(a² + x²) from the two-term cancellation.
  No dropped inner derivative, no lost inverse-trig sign, no y-term differentiated without
  dy/dx, no parametric division the wrong way round.
- Domain and branch discipline is unusually good in this group: every Tan⁻¹(tan A) = A step
  states and checks the principal range, and |x| < a/√3, |x| < √2 − 1, x ≠ 0 at the
  vanishing denominator, and √(tan²) = |tan| are all handled explicitly rather than waved
  through. Substitutions are all returned to x before differentiating.
- **Boilerplate cloning (defect 3): none.** A full duplicate-string scan across every
  `why` / `memory_tip` / `margin_note` / `common_mistakes` / `insider_note` field in the
  group returned exactly one repeat — "Writing log(xʸ) as (log x)ʸ instead of y log x." on
  `..._xy_equals_e_x_minus_y` and `..._y_equals_x_power_y` — true and apt on both.
- **Cross-references to chapters or answer numbers (defect 4): none.** No card cites a unit
  number or another answer; the only reference is the generic "in this chapter".
- The dominant defect class is defect 1 again: 4 of the 7 WRONG rows are `common_mistakes`
  lines that condemn something correct, or that describe an error whose stated consequence
  is not what that error actually produces.
- Ordinal-mark margin notes ("First mark", "Second mark", …) appear throughout and were NOT
  re-reported, per `ORDINAL_MARK_NOTES.md`.
