# B01 — IPE Maths-1B examiner audit (Locus · Transformation of Axes · D.C's & D.R's)

Cards audited: 28 / 28   Findings: HARMFUL 0 · WRONG 12 · WEAK 7

Every final answer on all 28 cards was re-derived independently and confirmed against a
concrete test point or a known geometric property. **The mathematics in `answer.steps[].lines`
is correct on every card in this group, with no line that fails to follow from the one above.**
All findings live in the surrounding prose — `common_mistakes`, `memory_tip`, `margin_note`,
`insider_note`, `why` — which is exactly the pattern the earlier groups showed.

## HARMFUL — wrong mathematics a student would copy into an exam

None. No printed algebra line in this group is wrong, no squaring step gains or drops a branch,
no transform is run backwards, and every claimed direction-cosine triple satisfies BOTH of its
given constraint equations.

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_loc_right_angle_2_3_minus1_5 | s4_simplify_to_the_locus | `common_mistakes`: "Losing a sign when expanding a bracket like **(y − 4)²**." This problem is A(2,3), B(−1,5); its four brackets are (x−2)², (y−3)², (x+1)², (y−5)². **There is no (y − 4)² anywhere in the card.** Boilerplate cloned from the sibling `ts_ipe_m1b_loc_right_angle_4_0_0_4`, which genuinely does expand (y−4)². The chatbot repeats it as a warning about a bracket that does not exist in the question. | "Losing a sign when expanding (y − 5)², which gives −10y." |
| ts_ipe_m1b_loc_right_angle_hypotenuse_0_6_6_0 | s4_simplify_to_the_locus | The same cloned line: "Losing a sign when expanding a bracket like **(y − 4)²**." This card's brackets are (y−6)² and (x−6)². No (y − 4)² is present. | "Losing a sign when expanding (y − 6)², which gives −12y." |
| ts_ipe_m1b_loc_pa2_pb2_twice_pc2 | s4_simplify_to_the_locus | Printed line 3 reads `⇒ 0 = 14x − 14y + 8   ⇒   7(2x − 2y) + 8 = 14x − 14y + 8`. The second half is a tautology — both sides are identically 14x − 14y + 8. It deduces nothing and does not lead to line 4, `2(7x − 7y + 4) = 0`. A student copying the model answer writes a line an examiner cannot credit. | Delete the `7(2x−2y)+8` clause: `0 = 14x − 14y + 8 ⇒ 2(7x − 7y + 4) = 0 ⇒ 7x − 7y + 4 = 0`. |
| ts_ipe_m1b_ta_rotate_45_find_original | s4_simplify_to_the_answer | `memory_tip` and `margin_note` both say "**Multiply through by 2 at the end**", and `common_mistakes` says "**Dividing by 2 instead of multiplying at the last step**". The card's own final line is `50x² + 18y² = 450 ⇒ 25x² + 9y² = 225` — it **divides by 2** to reach the boxed answer. The card lists its own mark-earning last move as a mistake. | Tip: "Multiply by 2 to clear the /2, then divide the whole line by 2 to reach 25x² + 9y² = 225." Mistake: "Halving the 225 when clearing the denominator instead of doubling both sides." |
| ts_ipe_m1b_ta_shift_minus1_2_ellipse | insider_note | "Getting zero for them means **the 2 in front of x² was dropped** while expanding the bracket." **False diagnosis.** Dropping the 2 gives X-terms −2X − 4X = **−6X**, not zero. The linear terms vanish only if the shift was made to the curve's own centre (1, −2) instead of the given (−1, 2). | "Getting zero for the linear terms means you shifted to the ellipse's own centre (1, −2) instead of the given (−1, 2)." |
| ts_ipe_m1b_ta_sqp_shift_3_minus4_find_original | s3_substitute_into_the_equation | `common_mistakes`: "Forgetting to substitute inside a **product like XY**." This card's equation is X² + Y² − 4 = 0 — there is no XY term. Cloned from `ts_ipe_m1b_ta_shift_2_3_find_original`, which does carry 3XY. | "Substituting inside only one of the two squared brackets." |
| ts_ipe_m1b_ta_sqp_shift_3_minus4_find_original | s4_simplify_to_the_answer | `common_mistakes`: "Sign slip when expanding a **product of two brackets**." No product of two different brackets appears — only (x−3)² and (y+4)². Same clone. | "Sign slip on (y + 4)², which expands with +8y." |
| ts_ipe_m1b_dc_perpendicular_proof | s1_eliminate_l | `why`: "That is what keeps a **seven-mark** question short." This card is `marks_total: 8` (Section C moved 7 → 8 on 2026-08-28) and its own `page_header` says "8 marks". Reader-facing prose contradicting the card's own header. | "…what keeps an eight-mark question short." |
| ts_ipe_m1b_dc_perpendicular_proof | s1_eliminate_l | `common_mistakes`: "**Dividing by m² instead of n² and getting the reciprocal roots.**" Dividing 5m² + 4mn − 3n² = 0 by m² is a fully correct alternative route: it gives 3t² − 4t − 5 = 0 in t = n/m, product −5/3, i.e. the identical relation m₁m₂/3 = n₁n₂/(−5). A correct route is filed as an error. | "Reading the product of roots as −b/a; for ax² + bx + c = 0 it is c/a." |
| ts_ipe_m1b_dc_dcs_l_minus_5m_plus_3n | s5_finish | `common_mistakes`: "**Dropping the ± that both directions along a line allow.**" The card's own boxed answer is (1/√14, 2/√14, 3/√14) and (−1/√6, 1/√6, 2/√6) — with no ±. The card condemns exactly what its own model answer does. | Either write ± on the boxed d.c's, or replace the line with "Dividing both triples by the same root instead of each by its own (√14 and √6)." |
| ts_ipe_m1b_dc_dcs_l_plus_m_plus_n_mn | s5_finish | The same line: "Dropping the ± that both directions along a line allow", while the card's boxed d.c's (1/√6, 1/√6, −2/√6) and (1/√6, −2/√6, 1/√6) carry no ±. | As above. |
| ts_ipe_m1b_dc_angle_3l_plus_m_plus_5n · ts_ipe_m1b_dc_angle_l_plus_m_plus_n · ts_ipe_m1b_dc_dcs_l_minus_5m_plus_3n · ts_ipe_m1b_dc_dcs_l_plus_m_plus_n_mn | s3_first_case and s4_second_case — 8 notes across 4 cards | Each `margin_note` opens "**Two marks.**" but the step carries `marks: 1` (the split is 1+3+1+1+2), so the note contradicts the mark gutter beside it. This is a **cardinal**-count mismatch, so the bank-wide `ORDINAL_MARK_NOTES.md` sweep — which matches only `/(First\|Second\|…)\s+mark/` — could not see it. | "One mark. Take the first factor, feed it back into the linear relation…" / "One mark. The same work on the second factor…" And extend the proposed `build_answer_book.ts` gate to cardinal counts ("Two marks", "Three marks") as well as ordinals. |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_loc_diff_distances_4_0_minus4_0_four · ts_ipe_m1b_loc_sum_distances_0_2_0_minus2_six · ts_ipe_m1b_loc_distance_twice_3_0_minus3_0 · ts_ipe_m1b_ta_shift_2_3_find_original · ts_ipe_m1b_ta_sqp_shift_3_minus4_find_original · ts_ipe_m1b_ta_shift_minus1_2_ellipse | s1 / s2 / s4 / insider_note | Reader-facing prose points at a card the student cannot see: "its **partner question**", "its **facing question**", "its **circle partner**", "the sum and difference families **on the facing pages**". On `..._4_0_minus4_0_four` the claim is also **factually wrong** — its "same symmetric pair as its partner" is (±4, 0), while the partner card uses (±5, 0). | State the fact directly, with no cross-reference: "Both fixed points sit on the x-axis, symmetric about the origin, so the y-terms cancel." |
| ts_ipe_m1b_dc_four_diagonals_cos_squares | s2_four_diagonals · s3_figure | `common_mistakes` defines the mistake by another question: "Listing only two diagonals, **as the previous question needed**" and "Drawing only two diagonals, as the previous question needed." A mistake list must stand on its own. | "Listing only two diagonals — this question needs all four." |
| ts_ipe_m1b_loc_right_angle_2_3_minus1_5 · ts_ipe_m1b_loc_right_angle_4_0_0_4 · ts_ipe_m1b_loc_right_angle_hypotenuse_0_6_6_0 | s1_take_the_locus_point | Rule 41: "A and B are **nailed down**" is an idiom, not literal English, and it appears in both `memory_tip` and `margin_note`. | "A and B are fixed; P is the point that moves." |
| ts_ipe_m1b_loc_right_angle_2_3_minus1_5 · ts_ipe_m1b_loc_right_angle_4_0_0_4 · ts_ipe_m1b_loc_right_angle_hypotenuse_0_6_6_0 | s2_write_the_given_condition | `common_mistakes`: "Trying to use the slope condition **m₁m₂ = −1** and then losing the sign." m₁m₂ = −1 is a correct alternative route to the same circle; filing it under mistakes reads as if the route itself is the error. | "If you use m₁m₂ = −1 instead, keep the sign — it gives the same circle." |
| ts_ipe_m1b_dc_angle_3l_plus_m_plus_5n · ts_ipe_m1b_dc_angle_l_plus_m_plus_n | s5_finish | `common_mistakes`: "Multiplying the two roots without keeping them under one square root." √6 · √6 and √(6·6) are equal, so this names a correct equivalent form as a mistake. The intended warning (taking the root of only one of the two sums) is not what the words say. | "Taking the square root of only one of the two sums of squares." |
| ts_ipe_m1b_loc_distance_twice_3_0_minus3_0 | s4_simplify_to_the_locus | `common_mistakes`: "Collecting **9 − 36 as +27 on the wrong side**." 9 − 36 is −27, and the card's own line correctly carries it across as +27 — so as written the warning names the correct move as the error. | "Moving 9 − 36 across without changing its sign; it becomes +27 on the right." |
| ts_ipe_m1b_dc_angle_3l_plus_m_plus_5n | question_text | "Find the angle between **whose d.c's satisfy** the equations 3l+m+5n=0 and 6mn−2nl+5lm=0." The noun is missing — the sentence does not parse, and it is the first thing the student reads. (The sibling `..._l_plus_m_plus_n` has the full wording.) | "Find the angle between **the lines** whose d.c's satisfy the equations 3l + m + 5n = 0 and 6mn − 2nl + 5lm = 0." |

## Cards checked clean

ts_ipe_m1b_loc_area_2_3_minus3_4_eight_point_five, ts_ipe_m1b_loc_area_5_3_3_minus2_nine, ts_ipe_m1b_loc_diff_distances_minus5_0_5_0_eight, ts_ipe_m1b_loc_ratio_2_3_2_minus3_two_three, ts_ipe_m1b_loc_sum_distances_2_3_2_minus3_eight, ts_ipe_m1b_ta_angle_removing_xy_term, ts_ipe_m1b_ta_rotate_alpha_normal_form, ts_ipe_m1b_ta_rotate_pi_over_4, ts_ipe_m1b_ta_rotate_pi_over_6, ts_ipe_m1b_ta_shift_minus1_2_circle, ts_ipe_m1b_dc_angle_between_cube_diagonals

## Notes

**Locus (12 cards) — every final equation re-derived and confirmed by a test point.**
`area_2_3_minus3_4_eight_point_five` → (x+5y−34)(x+5y)=0 [P(5,−1) gives area 8.5 ✓];
`area_5_3_3_minus2_nine` → (5x−2y−37)(5x−2y−1)=0 [P(1,2) gives area 9 ✓];
`diff_distances_4_0_minus4_0_four` → 3x²−y²=12 [a=2, c=4, foci (±4,0), 2a=4 ✓];
`diff_distances_minus5_0_5_0_eight` → 9x²−16y²=144 [a=4, c=5, foci (±5,0), 2a=8 ✓];
`distance_twice_3_0_minus3_0` → x²+y²+10x+9=0 [P(−1,0): PA=4, PB=2 ✓];
`pa2_pb2_twice_pc2` → 7x−7y+4=0 ✓;
`ratio_2_3_2_minus3_two_three` → 5x²+5y²−20x−78y+65=0 [P(2,15): PA=12, PB=18, i.e. 2:3 ✓];
the three right-angle cards → the circle on AB as diameter in every case ✓;
`sum_distances_0_2_0_minus2_six` → 9x²+5y²=45 [foci (0,±2), 2a=6 ✓];
`sum_distances_2_3_2_minus3_eight` → 16x²+7y²−64x−48=0 [centre (2,0), foci (2,±3), 2a=8 ✓].

- No squaring step gains or drops a branch. The two `|PA−PB|` cards carry the ± correctly through the isolation and lose it only at the final squaring; both area cards keep BOTH modulus branches and report the product form, which is the branch students most often drop.
- `steps[].marks` sums to 4 and each `mark_split` sums to 4 on all 12.

**Transformation of Axes (9 cards) — every transform re-run and checked by substitution.**
`shift_minus1_2_circle` → X²+Y²−4=0 (the shift IS to the circle's own centre, so the linear terms correctly vanish ✓);
`shift_minus1_2_ellipse` → 2X²+Y²−8X+8Y+18=0 [the origin lies on the original curve; its new coordinates (1,−2) satisfy the answer ✓];
`shift_2_3_find_original` → x²+3xy−2y²+4x−y−20=0 ✓;
`sqp_shift_3_minus4_find_original` → x²+y²−6x+8y+21=0 [centre (3,−4), r=2 ✓];
`rotate_pi_over_4` → 8X²−2Y²=9 [eigenvalues of [[3,5],[5,3]] are 8 and −2 ✓];
`rotate_pi_over_6` → X²−Y²=a² [eigenvalues of [[1,√3],[√3,−1]] are ±2 ✓];
`rotate_45_find_original` → 25x²+9y²=225 ✓;
`rotate_alpha_normal_form` → X=p ✓;
`angle_removing_xy_term` → θ = ½ Tan⁻¹(2h/(a−b)), with the a = b branch correctly falling out of cos 2θ = 0 ✓.

- **Direction is correct on all nine** — the specific risk flagged for this unit. The four "find the original" cards use the reversed pair (X = x−h, Y = y−k; or X = x cosθ + y sinθ, Y = y cosθ − x sinθ) and the five forward cards use x = X+h / x = X cosθ − Y sinθ. No transform is run backwards.
- Every TA card's `mark_split` is 1+1+1+1 = 4 and `steps[].marks` sums to 4.

**D.C's & D.R's (7 cards) — both constraint equations tested on every claimed triple.**
`angle_3l_plus_m_plus_5n` → (1,2,−1) and (2,−1,−1): each satisfies 3l+m+5n=0 AND 6mn−2nl+5lm=0 ✓; cos θ = 1/6 ✓.
`angle_l_plus_m_plus_n` → (−1,0,1) and (0,−1,1): each satisfies l+m+n=0 AND l²+m²−n²=0 ✓; cos θ = 1/2, θ = 60° ✓.
`dcs_l_minus_5m_plus_3n` → (1,2,3) and (−1,1,2): each satisfies l−5m+3n=0 AND 7l²+5m²−3n²=0 ✓; normalised by √14 and √6 respectively ✓ (each by its own root, not a shared one).
`dcs_l_plus_m_plus_n_mn` → (1,1,−2) and (1,−2,1): each satisfies l+m+n=0 AND mn−2nl−2lm=0 ✓; both normalise by √6 ✓.
`angle_between_cube_diagonals` → OP=(a,a,a) and CL=(a,a,−a) are both genuine BODY diagonals ✓; cos θ = 1/3 ✓. Its claim that "any two of them make the same angle" is true — all six pairs give cos = 1/3.
`four_diagonals_cos_squares` → OP, CL, NB, AM are the four correct opposite-corner pairs ✓; the (a+b)² + (a−b)² = 2(a²+b²) identity is applied to the right two pairs, every cross term cancels, and the sum is 4/3 ✓.
`perpendicular_proof` → 5m²+4mn−3n²=0 and 3l²+10lm+2m²=0 both re-derived ✓; products of roots −3/5 and 2/3 give l₁l₂ : m₁m₂ : n₁n₂ = 2 : 3 : −5, which sums to 0 ✓.

- `steps[].marks` sums to 8 and each `mark_split` sums to 8 on all 7.

**Two documentation-only observations (not counted as findings — `verification.note` is not sent to the chatbot):**

- `ts_ipe_m1b_loc_diff_distances_4_0_minus4_0_four`'s note says "the intermediate line **−3x²+y²+16x+12=0** shown here is ours". The card's actual intermediate line is −3x²+y²+12=0, and the note's version (with +16x) is not correct algebra — the 8x cancels. The card body is right; the provenance note describes a line that is not in it.
- Six of the seven D.C. cards still carry "7 marks" / "7-mark" inside `verification.note` (paper-position prose from before the 7 → 8 re-cut). Harmless where it sits, but `dc_perpendicular_proof` let the same stale number leak into a reader-facing `why` — see the WRONG table. Worth a bank-wide grep for "seven-mark" / "7-mark" in `why` / `memory_tip` / `margin_note` / `insider_note`.

**Method note.** No card's `verification.note` was treated as evidence. Every locus equation was tested with a point constructed to satisfy the stated condition; every conic was checked against its foci and its 2a; every rotation was checked against the eigenvalues of the quadratic form; every direction-cosine triple was substituted back into BOTH given relations.
