Cards audited: 22 / 22   Findings: HARMFUL 1 · WRONG 3 · WEAK 15

Group B02 — mathematics_1b, Unit 3 "The Straight Line" (first half). Every answer was re-derived
independently from `question_text` before the card was read; every claimed point was substituted
back into the line(s) it is claimed to lie on. All 22 final answers are **numerically correct**.

## HARMFUL — wrong mathematics a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_sl_circumcentre_from_sides_x_y_2 | s3_second_equation | The expansion of (y−1)² is printed as **(y² − 1 + 2y)**. The correct expansion is y² − 2y + 1 — both the sign of the linear term and the constant are wrong. This is a line the student copies. It is provably a slip, not a convention: (a) the SAME card expands (y−1)² correctly in s2 as "(y² + 1 − 2y)"; (b) the very next line, "6x + 2x − 2y + 6y = 0", is computed from the CORRECT expansion, so the printed line does not lead to the line beneath it; (c) working the printed line through gives 8x + 8y − 2 = 0, not the 8x + 4y = 0 the card states. A student copying it writes a false expansion and then a conclusion that does not follow from it — the method mark on the step is lost, and a teacher marks the algebra wrong on sight. | `⇒ (x² + 9 + 6x) + (y² + 1 − 2y) = (x² + 1 − 2x) + (y² + 9 − 6y)` — after which 8x + 4y = 0 and 2x + y = 0 do follow, and the boxed S(−1/3, 2/3) is right. |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_sl_angle_45_find_k | s4_solve_both_branches | `why` says "Two lines through a point make two pairs of angles, 45° and 135°, and both satisfy the modulus equation". That is not why there are two roots. Because the formula carries a modulus, tanθ = 1 pins the ACUTE angle, and BOTH roots give exactly 45°, not one 45° and one 135° — verified: k = 3 → \|3.4/3.4\| = 1; k = −25/3 → \|(17/3)/(−17/3)\| = 1. The card teaches a false reason for a correct answer. | The ± produces two DIFFERENT second lines through the point — one rotated +45° from the given line, one rotated −45°. Each makes a 45° angle with 4x − y + 7 = 0, so each gives a valid k. |
| ts_ipe_m1b_sl_orthocentre_minus5_minus7_13_2_minus5_6 | s3_second_altitude | This step's entire teaching point is the SPECIAL case: AC is vertical (A and C both have x = −5), so the altitude is horizontal, y = 2, with no −1/m anywhere — and the card's own `insider_note` says "reaching for −1/m on a vertical side is where this question is lost". Yet the step's `margin_note` says "The same three moves from a different vertex", its `common_mistakes` warns of a "Sign slip when the perpendicular slope is a negative fraction" (the slope here is 0; there is no fraction), and its `memory_tip` is the generic "Repeat from another vertex". All three reader-facing strings push the student into the exact trap the card names. | Margin note and tip: "A vertical side has a HORIZONTAL altitude — write y = (the vertex's y-coordinate) straight down and use no slope formula." Mistake line: "Trying −1/m on a vertical side, whose slope is undefined." |
| ts_ipe_m1b_sl_circumcentre_1_3_0_minus2_minus3_1, ts_ipe_m1b_sl_circumcentre_minus2_3_2_minus1_4_0 | s4_solve_the_pair | `margin_note` states "the book uses the cross-multiplication rule" and `common_mistakes` warns of an "Arithmetic slip in the cross-multiplication denominators" — but neither step's working uses cross-multiplication. Both solve by elimination ((1) − (2), and (2) + 2×(1)). The note was copied from the sibling card circumcentre_1_3_minus3_5_5_minus1, which does use cross-multiplication. On the (−2,3) card the `insider_note` compounds it: "Substituting the first into the second beats elimination here" while the working shown IS elimination. | Match the note to the working: "eliminate one unknown by subtracting (or adding a multiple of) the two equations." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_sl_concurrent_find_p | s3_substitute_third | `memory_tip` "Substituting into the third gives zero, which fixes p" and the `margin_note` both promise zero, but this step writes the third line as px + 4y = 6 and reaches −p + 8 = 6 — never zero. The tip contradicts the working directly under it. | Write the third line as px + 4y − 6 = 0 so the substitution does give zero, or reword: "the point must satisfy the third line, and that equation fixes p." |
| ts_ipe_m1b_sl_concurrent_find_k | s3_substitute_third | `common_mistakes` "Sign slip when the point has a negative coordinate" quotes a quantity absent from this problem — the point is P(11, 5), both coordinates positive. (It is apt on the two sibling cards, whose points are (−1, 2) and (4, −5).) | Replace with this step's real risk: "Substituting 11 and 5 the wrong way round into 2x − 3y + k." |
| ts_ipe_m1b_sl_concurrent_point_of_concurrency | s1_solve_two_lines | `common_mistakes` "Slip in the cross-multiplication denominators" names a method this step does not use — the working is plain elimination, and the card's own insider_note says elimination was chosen instead. | "Subtracting the two equations the wrong way round, so the sign of x comes out reversed." |
| ts_ipe_m1b_sl_concurrent_find_k, ts_ipe_m1b_sl_concurrent_point_of_concurrency | s2_point_of_intersection | `common_mistakes` "Leaving the point as a pair of fractions without simplifying" is boilerplate: neither (11, 5) nor (4, −5) ever arises as a fraction in these workings. | Drop, or replace with a mistake the step can actually make. |
| ts_ipe_m1b_sl_circumcentre_from_sides_x_y_2, ts_ipe_m1b_sl_circumcentre_from_sides_3x_y_5 | s2_first_equation | `why` reads "…the book's own panel calls it PB₁". A reader-facing string that cites the source coaching book; the student has never seen "the book" or a panel labelled PB₁, and the sentence gives no reason. | "The line produced is the perpendicular bisector of AB." |
| ts_ipe_m1b_sl_orthocentre_from_sides_x_y_10, ts_ipe_m1b_sl_orthocentre_from_sides_7x_y_10 | s2_altitude_through_a | Same defect: `why` ends "…and the book uses it throughout this chapter". | Drop the clause; the real reason ("saves rearranging into y = mx + c") already stands alone. |
| ts_ipe_m1b_sl_circumcentre_1_3_0_minus2_minus3_1 | s4_solve_the_pair | `why` is entirely about obtuse triangles ("an answer far from the vertices is not automatically wrong"), but this triangle is ACUTE (all three vertex dot products positive: 14, 12, 6) and its circumcentre (−1/3, 2/3) sits inside it. Correct boilerplate on the sibling obtuse card, irrelevant here, and no reason for the step. | "Two perpendicular bisectors meet at exactly one point, so this pair has a single solution." |
| ts_ipe_m1b_sl_orthocentre_5_minus2_minus1_2_1_4, ts_ipe_m1b_sl_orthocentre_minus2_minus1_6_minus1_2_5, ts_ipe_m1b_sl_orthocentre_minus5_minus7_13_2_minus5_6 | s4_solve_the_pair | `why` for "Solve the pair" talks only about right-angled triangles ("the orthocentre sits exactly on the right-angled vertex"). True in general, but none of these three triangles is right-angled, and it is no reason for the step it is attached to. | "Two altitudes meet at exactly one point, so this pair has a single solution — and that point is the orthocentre." |
| ts_ipe_m1b_sl_parametric_distance_pi_over_6 | s1_find_the_angle | The step is labelled "Find the angle" and carries 1 mark, but nothing is found — the question hands over π/6 and the step's only line is "θ = π/6 = 30°". Its `common_mistakes` line "Using the slope directly in place of cosθ and sinθ" quotes a slope this problem never gives (apt on the sibling slope-1 card, from which it was copied). | Label it "Write down the angle"; replace the mistake line with one about π/6 → 30° and about reading cos 30° / sin 30° off correctly. |
| ts_ipe_m1b_sl_normal_form_intercepts | s3_read_off, s4_deduce | p is written as ab/√(a²+b²) and called "the perpendicular distance" with no sign discussion. Normal form requires p > 0, so strictly p = \|ab\|/√(a²+b²); with a or b negative the stated equation is not in normal form. The final result 1/p² = 1/a² + 1/b² is unaffected (squaring), so nothing is lost in the exam. | Add one line: "taking a, b > 0 (otherwise ab is replaced by \|ab\|, since p is a distance)". |
| ts_ipe_m1b_sl_circumcentre_1_3_minus3_5_5_minus1 | s1b_figure | The shared schematic shows S INSIDE an acute-looking triangle, but this card's answer is S(−8, −10), far outside — and its own insider_note makes that the headline point ("sits well outside the triangle, which is correct for an obtuse one"). The picture contradicts the card. | An obtuse variant of the figure, or a caption marking the sketch as schematic. |
| ts_ipe_m1b_sl_orthocentre_5_minus2_minus1_2_1_4, ts_ipe_m1b_sl_orthocentre_minus2_minus1_6_minus1_2_5, ts_ipe_m1b_sl_orthocentre_minus5_minus7_13_2_minus5_6 | s1b_figure (`triangle_altitudes`) | In the shared figure, altitude `alt_b` (45,190)→(190,118) is ~10° off perpendicular to side AC (132,38)→(245,190), and the plotted O dot (132,155) is ~8 px off the actual crossing of the two drawn altitudes. It is drawn WITH a right-angle mark, so the mark asserts a right angle the geometry does not show — on the very figure whose margin note says "The right-angle mark is the whole definition of an altitude". | Redraw alt_b on the true perpendicular to AC and put the O dot at the real intersection. |
| ts_ipe_m1b_sl_orthocentre_from_sides_x_y_10, ts_ipe_m1b_sl_orthocentre_from_sides_7x_y_10 | s1b_figure (`triangle_from_three_lines`) | The two drawn altitudes start 11–15 px away from the labelled vertices A and B (so they do not visibly emanate from them), and both overshoot past their feet, so no foot is marked and there is no right-angle mark anywhere. The step's stated purpose is to show "which line is opposite each vertex", which the drawing does not make legible. | Start each altitude ON its vertex, stop it at the foot on the opposite line, and mark the right angle there. |
| ts_ipe_m1b_sl_circumcentre_from_sides_3x_y_5 | s2_first_equation, s3_second_equation | Both write "SA = SB ⇒ (x−2)² + (y−1)² = …" with no square roots and no "squaring on both sides" line, while the `margin_note` and `memory_tip` both instruct the student to "square SA = SB". The sibling from-sides card does show the roots. | Show the roots and the squaring line, as every other circumcentre card in the unit does. |
| ts_ipe_m1b_sl_parametric_distance_slope_one | s3_put_on_the_line | `common_mistakes` "Forgetting to distribute a coefficient across both parts of a coordinate" — the line here is x + y − 6 = 0, both coefficients 1, so there is nothing to distribute. Copied from the π/6 sibling, where √3 and −4 make it apt. | "Forgetting that BOTH coordinates carry r/√2, so the two r-terms add." |

## Cards checked clean

ts_ipe_m1b_sl_concurrent_cubes_proof, ts_ipe_m1b_sl_foot_of_perpendicular_proof, ts_ipe_m1b_sl_image_of_point_proof, ts_ipe_m1b_sl_parallel_through_intersection, ts_ipe_m1b_sl_parametric_distance_slope_one (mathematics and notes clean; one boilerplate mistake line above), ts_ipe_m1b_sl_perpendicular_through_intersection (mathematics clean; see the AUTHOR DOUBT note below)

## Notes

**Mathematics.** All 22 boxed answers verify. Every circumcentre was re-checked by computing
SA² = SB² = SC²: (−1/3, 2/3) → 65/9; (−8, −10) → 250; (3/2, 5/2) → 25/2; (−6/7, 2/7) → 425/49.
Every orthocentre was re-checked against the THIRD (unused) altitude: (1/5, 14/5) lies on
3x − 2y + 5 = 0; (2, 5/3) on x = 2; (−3, 2) on y = −2x − 4; (−4, −6) is correctly the right-angle
vertex, since sides x + y + 10 = 0 and x − y − 2 = 0 have slopes −1 and 1; (−2/3, 4/3) on
x − 7y + 10 = 0. Both proof cards (foot of perpendicular, image of a point) derive correctly, and
the factor 2 in the image proof is correctly traced to the MIDPOINT condition rather than to Q.
All three concurrency points substitute back into all three of their given lines. Note that
circumcentre_from_sides_x_y_2 and circumcentre_1_3_0_minus2_minus3_1 are the SAME triangle
{(1,3), (0,−2), (−3,1)} reached two different ways, and both land on (−1/3, 2/3) — a useful
internal consistency check, and it holds.

**Marks.** `steps[].marks` sums to `marks_total` on all 22 cards, and every `mark_split` sums to the
same total with labels matching the marked steps. No arithmetic defect in any mark scheme. Zero-mark
steps (figures, "write the point of intersection", "write the point-slope form") are consistently
excluded from `mark_split`, which matches the convention used across the unit.

**The three AUTHOR DOUBT cards.** All three are in this group: `angle_45_find_k`,
`perpendicular_through_intersection`, `parametric_distance_pi_over_6`. **The doubt is justified on
all three — the eight marks are NOT honestly earned by the work shown.**
- `angle_45_find_k`: read two slopes, quote one formula, open a modulus, solve two one-line linear
  equations. That is four marks of work. Three marks on "solve both branches" pays 5k = 15 and
  3k = −25 at 1.5 marks each.
- `perpendicular_through_intersection`: one slope flip, one routine 2×2 solve, one point-slope
  substitution and a tidy-up. Four marks of work. The split is also weighted oddly — the heaviest
  allocation (3 marks) sits on "Solve for the point", the most mechanical step on the card, while
  the conceptual step (perpendicular means flip-and-negate) gets 2.
- `parametric_distance_pi_over_6`: the weakest of the three. Step 1 ("Find the angle", 1 mark) is a
  single line copying π/6 = 30° out of the question — a mark paid for transcription. The rest is
  one substitution and one linear solve.
The recorded repair ("EXTEND the answer, do not spread the marks thinner") is the right one. For
`parametric_distance_pi_over_6` the obvious extension is to also locate P(4√3, 5) and confirm
PQ = 6 by the distance formula — real work, and it would let the free step 1 fold into step 2.

**Defect pattern.** The dominant defect in this group is copied-across boilerplate: a `why`,
`margin_note`, `common_mistakes` entry or `memory_tip` written for a sibling card and left on a card
where it does not fit. 12 of the 15 WEAK findings and 2 of the 3 WRONG findings are this shape. Two
cross the line into actively misleading — the cross-multiplication notes on two circumcentre cards
that solve by elimination, and, more seriously, the orthocentre_minus5 vertical-side step, where the
generic strings point the student at the trap the card's own insider_note names. A reviewer sweep
should read each `margin_note` / `memory_tip` / `common_mistakes` entry **against the lines of its own
step**, not against the step's label.

**Rule 41 (plain language).** No idiom, metaphor or personification found in any reader-facing string
across the 22 cards. "The letters CROSS OVER" (normal_form_intercepts s3) and "flip and negate" (the
orthocentre cards) are literal descriptions of operations, not figures of speech, and both are true.
Register is clean throughout this group.

**`insider_note`.** All 22 are true statements about their own card's mathematics, and none claims
exam history the card's `appearances` block does not carry. Two contain internal contradictions with
their own workings, both reported above (circumcentre_minus2_3 "substituting beats elimination";
concurrent_find_p, handled at the tip level).

**`memory_tip`.** Present on every step of all 22 cards, and every tip is mathematically true as a
standalone statement. The three flagged above fail on FIT, not truth: they describe a computation the
step does not perform (concurrent_find_p, orthocentre_minus5, circumcentre_from_sides_3x_y_5).
