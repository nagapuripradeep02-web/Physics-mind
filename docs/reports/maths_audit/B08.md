Cards audited: 24 / 24   Findings: HARMFUL 1 · WRONG 5 · WEAK 5

Group B08 — mathematics_1b unit 9 Differentiation (middle third). Every function was
differentiated independently from the question stem and compared line by line against the card;
parametric, implicit, logarithmic and inverse-trig results were re-derived by a second route or
checked at a numeric point. `verification.note` claims were not treated as evidence.

## HARMFUL — wrong mathematics a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_dif_root_1_minus_x2_plus_root_1_minus_y2 | s2_sum_to_product | `common_mistakes`: "Writing sin C − sin D = 2 sin((C − D)/2) cos((C + D)/2) with the two half angles swapped, which puts the sine on the wrong half." Nothing is swapped — that expression IS the correct identity, the card's own line with the two factors written in the other order. The clause "puts the sine on the wrong half" then teaches the reverse of the truth: a student who obeys it moves the sine onto the half-sum and writes 2 sin((C + D)/2) cos((C − D)/2), which is sin C + sin D. With that form the shared factor in s3 does not cancel and the whole question fails. | Delete the line, or name the real error: "Writing sin C − sin D = 2 sin((C + D)/2) cos((C − D)/2) — that is sin C + sin D. In both sum-to-product forms the sine carries the half-difference." |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_dif_root_1_minus_x2_plus_root_1_minus_y2 | s1_substitute | `common_mistakes`: "Substituting x = cos A instead, which leaves a sine under the root and does not match the sum formulas used in the next step." x = cos A, y = cos B is a complete correct route: sin A + sin B = a(cos A − cos B) gives cot((A − B)/2) = −a, A − B is still constant, and Cos⁻¹x − Cos⁻¹y = constant differentiates to the same dy/dx. A full-mark alternative is condemned as an error. | "x = cos A also works — it uses the other pair of sum-to-product forms and reaches the same answer. This solution takes x = sin A because Sin⁻¹ is what gets differentiated at the end." |
| ts_ipe_m1b_dif_sin_inverse_and_tan_inverse_equal_derivatives | insider_note | "Both functions differ by a constant… Differentiate each separately and compare." The card proves f(x) = g(x) exactly (both reduce to the same θ), not "differ by a constant"; and its own s4 `why` says "Reaching f = g first is shorter and safer than differentiating each function separately", with a `common_mistakes` line against comparing two separately-differentiated forms. The chatbot's opening line recommends the route the answer rejects. | "Put x − β = (α − β)sin²θ; both functions reduce to the SAME θ, so f = g on (β, α) and the derivatives match. Differentiating each separately is much longer." |
| ts_ipe_m1b_dif_first_principle_tan_2x | s4_answer | `common_mistakes`: "Stopping at 2/cos²2x when the answer key expects 2 sec²2x." 2/cos²2x is the same quantity and a complete correct answer; no examiner deducts for it, and the card's own line prints it as the result one line before boxing the sec form. | Drop it, or name a real error, e.g. "Writing sec²2x without the factor 2." |
| ts_ipe_m1b_dif_log_4x2_minus_9_second | s1_split_and_first | The printed line "∴ y = log(2x − 3) + log(2x + 3),  4x² − 9 > 0" asserts the split holds wherever that domain condition holds. On x < −3/2 the condition holds but both log(2x − 3) and log(2x + 3) are undefined. The card's own verification note knows this and says absolute values are needed there. | "y = log(2x − 3) + log(2x + 3) for x > 3/2; for x < −3/2 write y = log\|2x − 3\| + log\|2x + 3\|. Either way the derivative is the same." |
| ts_ipe_m1b_dif_log_quadratic_ratio | s4_answer | `common_mistakes`: "Leaving the denominator as a product of two quadratics when the question asks for the derivative in its simplest form." The question text asks only "Find the derivative"; it never asks for simplest form, and the unexpanded denominator (x² + x + 2)(x² − x + 2) is a correct full-mark answer. | Remove the false claim about the question. If a tip is wanted: "The expanded denominator x⁴ + 3x² + 4 is neater, but the factored form is also correct." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_dif_root_1_minus_x2_plus_root_1_minus_y2 | s2_sum_to_product | `memory_tip` "In both forms the half sum comes before the half difference" teaches a writing order as though it were mathematics — and it is the belief the HARMFUL line above rests on. | "In both forms the sine carries the half-difference and the cosine the half-sum. The order they are written in does not matter." |
| ts_ipe_m1b_dif_log_sin_log_x | s2_answer | "This working needs sin(log x) > 0, that is 0 < log x < π." The "that is" claims an equivalence that is false — sin(log x) > 0 also on 2π < log x < 3π, and so on. | "…that is log x in (0, π), or any interval (2kπ, (2k+1)π)." |
| ts_ipe_m1b_dif_log_quadratic_ratio | s1_split_log | `common_mistakes` lists "Applying the quotient rule inside the logarithm first" as a mistake. It is a valid route to the same answer, only longer — the line's own wording admits it ("makes the algebra several times longer"). | Move it into `why` as a longer but valid route, the way ts_ipe_m1b_dif_sin_y_equals_x_sin_a_plus_y s1 handles the same situation ("which is correct but far longer"). |
| ts_ipe_m1b_dif_poly_1_to_x100_at_1 | s1_term_by_term | `margin_note` ends "so the derivative starts at 1, not at 0 + 1 + 1", which does not parse — no reading of the working produces "0 + 1 + 1". | "…so the derivative starts with the term 1; the constant contributes nothing." |
| ts_ipe_m1b_dif_poly_1_to_x100_at_1 | s2_answer | `why` says adding 1 to 100 by hand "cannot be finished inside the four minutes a 2-mark question allows". The card's own `expected_time_min` is 5, and a 60-mark / 3-hour paper allows about 6 minutes for 2 marks. | Drop the invented figure, or use the card's own 5 minutes. |

## Cards checked clean

ts_ipe_m1b_dif_first_principle_x_sin_x, ts_ipe_m1b_dif_log_diff_fractional_powers, ts_ipe_m1b_dif_log_sec_x_plus_tan_x, ts_ipe_m1b_dif_log_tan_e_x, ts_ipe_m1b_dif_param_1_minus_t2_over_1_plus_t2, ts_ipe_m1b_dif_param_3_cos_t_minus_2_cos3t, ts_ipe_m1b_dif_param_a_cos_t_plus_t_sin_t, ts_ipe_m1b_dif_param_cycloid_second_deriv, ts_ipe_m1b_dif_poly_1_to_xn_at_1, ts_ipe_m1b_dif_sec_root_tan_x, ts_ipe_m1b_dif_second_deriv_tan_inverse_2x_over_1_minus_x2, ts_ipe_m1b_dif_sin_inverse_2x_over_1_plus_x2, ts_ipe_m1b_dif_sin_inverse_3x_minus_4x3, ts_ipe_m1b_dif_sin_inverse_root_x, ts_ipe_m1b_dif_sin_log_x, ts_ipe_m1b_dif_sin_x_power_x_plus_x_power_sin_x, ts_ipe_m1b_dif_sin_y_equals_x_sin_a_plus_y

(17 of 24 carry no finding.)

## Notes

- **Every boxed final answer in the group is correct.** All 24 were re-derived independently:
  2 sec²2x; sin x + x cos x; y″ = −4/(2x−3)² − 4/(2x+3)² = −8(4x²+9)/(4x²−9)²; the four-log
  derivative (inner derivatives −2, 3, −6, 7 → coefficients −4/3, −9/4, +5, +6);
  2(2 − x²)/(x⁴ + 3x² + 4); sec x; cot(log x)/x; 2eˣ·cosec(2eˣ); b(t² − 1)/(2at); cot t; tan t;
  cosec⁴(t/2)/(4a); 5050; n(n+1)/2; √((1 − y²)/(1 − x²)); sec(√tan x)·tan(√tan x)·sec²x/(2√tan x);
  −4x/(1 + x²)²; 2/(1 + x²); 3/√(1 − x²); 1/(2√((x − β)(α − x))); 1/(2√(x − x²)); cos(log x)/x;
  the two-bracket u^v sum; sin²(a + y)/sin a. No dropped inner derivative, no lost inverse-trig
  sign, no parametric division the wrong way round; the cycloid card divides by dx/dt the second
  time and the log-second-derivative card differentiates the right expression.
- Two independent cross-checks were run where a second route existed: the parametric
  x = a(1 − t²)/(1 + t²) card was re-derived by the direct quotient rule in t (same
  b(t² − 1)/(2at)), and the cycloid second derivative was evaluated at t = π/2 (1/a both ways).
- `steps[].marks` sums to `marks_total` and matches `mark_split` on all 24 cards; step labels and
  mark_split labels agree everywhere. Every `mark_note` describes work its step actually does.
- Substitution validity is handled unusually well in this group: `sin_inverse_3x_minus_4x3`
  states |x| ≤ 1/2, `sin_inverse_2x_over_1_plus_x2` states |x| ≤ 1,
  `second_deriv_tan_inverse_2x_over_1_minus_x2` states |x| < 1 and explains why the derivative
  survives outside it, and `sin_inverse_and_tan_inverse_equal_derivatives` justifies dropping
  both modulus signs. Only `log_4x2_minus_9_second` (above) mis-states its domain.
- The dominant defect class is the one the brief predicted: `common_mistakes` lines that condemn
  correct work. Four of the six mathematics findings have that shape, and one of them inverts a
  standard identity. The fix pattern already exists in this same group —
  `sin_y_equals_x_sin_a_plus_y` s1 writes "which is correct but far longer" of a valid slower
  route instead of calling it a mistake.
- No cloned-boilerplate defect was found. The three parametric cards and the three three-layer
  chain-rule cards share step shape, but each `why`, `memory_tip` and mistake list is specific to
  its own function.
- Not re-reported: stale ordinal mark notes (swept in ORDINAL_MARK_NOTES.md). Also not reported:
  `log_tan_e_x` writes "Cosec" with a capital C mid-line — house typography used elsewhere in the
  bank, not a defect.
