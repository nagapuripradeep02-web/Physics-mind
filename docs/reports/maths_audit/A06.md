Cards audited: 27 / 27   Findings: HARMFUL 2 · WRONG 5 · WEAK 4

Group A06 — Maths-1A Unit 5 Matrices (second half), 27 cards / 59 steps.
Every determinant, cofactor, adjoint entry, inverse, row operation and rank claim was
re-derived from scratch; every claimed solution was substituted back into all original
equations; every `steps[].marks` sum was recomputed against `marks_total` (27/27 correct).
No stale 7-mark wording survives in this group (no LAQ cards here).

## HARMFUL — wrong mathematics a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_mat_sqp_adj_three_at | s2_find_the_adjoint | `margin_note` "The result turns out symmetric, so the transpose changes nothing here" and `memory_tip` "The result is symmetric here, so the transpose changes nothing" are both FALSE. adj A = [[−3,6,6],[−6,3,−6],[−6,−6,3]] has (1,2)=6 against (2,1)=−6, so it is not symmetric; the cofactor matrix [[−3,−6,−6],[6,3,−6],[6,−6,3]] is not symmetric either. A student who believes this hands in the cofactor matrix as adj A, which is NOT equal to 3Aᵀ, so the whole "show that adj A = 3Aᵀ" collapses and the A⁻¹ mark goes with it. The step's own `common_mistakes` ("Skipping the transpose") contradicts the tip. Reads like text cloned from a sibling adjoint card whose adjoint really is symmetric. | "Nine cofactors, then transpose — and the transpose really does move entries here: the (1,2) and (2,1) cofactors are −6 and 6, so writing the cofactor matrix instead of its transpose gives the wrong adjoint." |
| ts_ipe_m1a_mat_vsaq_reverse_law_transpose | s2_rhs | Four reader-facing strings assert that A′B′ is undefined: `why` "A′B′ is not even defined here — a 3×2 times a 2×3 gives a 3×3", `memory_tip` "Written the other way round the sizes would not even fit", `margin_note` (same sentence), and `insider_note` "never A′B′, which would not even be defined here". This is FALSE, and the card refutes itself inside one sentence: A′ is 3×2 and B′ is 2×3, so A′B′ IS defined and is a 3×3 matrix — the inner dimensions (2 and 2) match. A student taught this writes "A′B′ is not defined" as the justification and is marked wrong; worse, it plants a false conformability rule that misfires on every later product. | "The order reverses: (AB)′ = B′A′. A′B′ is also defined here, but it is a 3×3 matrix, so it cannot equal the 2×2 (AB)′ — the reversal is what makes both sides the same shape as well as the same matrix." |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_mat_sqp_transpose_inverse | (card-level `insider_note`) | "the cofactors of A′ are not the cofactors of A rearranged" is mathematically false — cof(A′) = cof(A)ᵀ always, and this very card proves it: cof(A) = [[−9,−8,−2],[8,7,2],[−5,−4,−1]] and the card's own cof(A′) = [[−9,8,−5],[−8,7,−4],[−2,2,−1]] is exactly its transpose. The note tells the student a valid identity is invalid. | "The cofactors of A′ are the cofactors of A transposed, so adj(A′) = (adj A)′ — but in the exam it is safer to transpose A first and work the nine cofactors of A′ directly." |
| ts_ipe_m1a_mat_sqp_transpose_inverse | s3_transpose_and_divide | `common_mistakes` "Dividing by \|A\| when it is not 1" states that dividing by the determinant is the error. Dividing by \|A\| is always required; here it happens to be division by 1. As written it tells a student to skip the division whenever \|A\| ≠ 1, which breaks every other inverse question. | "Forgetting to divide by \|A\| on a question where \|A\| is not 1 — here it is 1, so the adjoint is already the inverse." |
| ts_ipe_m1a_mat_sqp_omega_determinant | s2_use_1_0 | `common_mistakes` "Using ω = 1, for which the sum is 3 and the result fails." The RESULT does not fail: with ω = 1 every entry of the matrix is 1, all three rows are identical, and the determinant is still 0. What fails is only the zero-column argument. As written, a student concludes the determinant is non-zero for ω = 1. | "Using ω = 1, for which 1 + ω + ω² = 3, so column 1 is not zero and this argument does not work — that is why the question says COMPLEX cube root." |
| ts_ipe_m1a_mat_sqp_i_squared_minus_i | s1_multiply_a_by_a | `common_mistakes` "Squaring the entries without doing the multiplication" marks a correct route as an error, and contradicts the same step's `memory_tip` and `margin_note` ("A is diagonal, so each diagonal entry simply squares"). For a diagonal matrix, squaring the diagonal entries IS the multiplication. | "Squaring the entries of a matrix that is NOT diagonal — entry-by-entry squaring works here only because the off-diagonal zeros kill every cross term." |
| ts_ipe_m1a_mat_vsaq_i_squared | s1_multiply | The same cloned `common_mistakes` line, "Squaring the entries without doing the multiplication", against the same step's `memory_tip` "The matrix is diagonal, so each diagonal entry squares and the off-diagonals stay 0". | Same correction as the sibling card above. |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_mat_vsaq_det_of_squares | s1_write_out | `why` "there is no shortcut here, only careful arithmetic" and `memory_tip` "the pattern in the question is a distraction, not a shortcut" are stated as fact and are not true. R₂ → R₂ − R₁ and R₃ → R₃ − R₂ turn the last two rows into (3,5,7) and (5,7,9); one more subtraction gives (2,2,2), which factors 2 out and makes the determinant a one-line evaluation. Telling a student the pattern is a distraction closes off the standard method for this classic. | "Replace every square with its value first. Row subtractions do shorten this one, but the direct expansion is safe and is all the marks need." |
| ts_ipe_m1a_mat_vsaq_det_equals_45 | s1_expand | `why` opens "A row of zeros is a gift". The row is (1, 0, 0) — a row WITH zeros, not a row of zeros (a genuine row of zeros would force the determinant to 0). "is a gift" is also a metaphor, which Rule 41 bans in reader-facing prose. | "A row with two zeros saves work — expanding along it leaves a single 2×2 minor instead of three." |
| ts_ipe_m1a_mat_sqp_omega_determinant | s1_add_all_three_columns_into | `memory_tip` and `margin_note` both explain the equal row sums "because ω³ = 1 collapses every higher power back into the same three values". No power above ω² appears anywhere in this determinant, so nothing is collapsed. The real reason — the rows are cyclic rotations of the same three entries — is stated correctly in the same step's `why`, so the card gives two different explanations for one fact. | "Every row holds the same three entries 1, ω, ω² in a different order, so every row sums to 1 + ω + ω²." |
| ts_ipe_m1a_mat_vsaq_matrix_addition | (card-level `insider_note`) | "Two entries involve a negative, 8 − 4 and 5 − 1, and those are the ones to check" — three entries involve a negative number, and the one the card's own s2 `memory_tip` singles out (−1 + 1 = 0, in the FIRST row) is not in the insider note's pair. The two strings point the student at different entries. | "Three entries involve a negative: −1 + 1 = 0 in the first row, and 8 + (−4) and 5 + (−1) in the second. Those are the ones to check." |

## Cards checked clean

ts_ipe_m1a_mat_sqp_ab_transpose, ts_ipe_m1a_mat_sqp_construct_3x2, ts_ipe_m1a_mat_sqp_rank_test, ts_ipe_m1a_mat_sqp_rotation_inverse, ts_ipe_m1a_mat_sqp_scalar_fourth_power, ts_ipe_m1a_mat_vsaq_3b_minus_2a, ts_ipe_m1a_mat_vsaq_a_plus_transpose, ts_ipe_m1a_mat_vsaq_adj_inverse_2x2, ts_ipe_m1a_mat_vsaq_equality_2x2, ts_ipe_m1a_mat_vsaq_equality_3x3, ts_ipe_m1a_mat_vsaq_find_k_nilpotent, ts_ipe_m1a_mat_vsaq_inverse_1_2_3_minus5, ts_ipe_m1a_mat_vsaq_rank_one, ts_ipe_m1a_mat_vsaq_rank_two, ts_ipe_m1a_mat_vsaq_skew_symmetric_find_x, ts_ipe_m1a_mat_vsaq_solve_for_x, ts_ipe_m1a_mat_vsaq_symmetric_find_x, ts_ipe_m1a_mat_vsaq_trace

## Notes

- **The mathematics on the printed lines is sound across all 27 cards.** Every determinant
  (27, 1, −8, 0, −11, 24, −2, and the 3x+24 = 45 setup), every cofactor board, every adjoint,
  every inverse and every rank claim re-derived correctly, and each printed line follows from
  the one above it. All three cards carrying a system or an unknown were back-substituted into
  the ORIGINAL equations: `rank_test` (x=1, y=2, z=3 satisfies all three equations, and the
  original det A = −2 ≠ 0 confirms rank 3), `det_equals_45` (x=7 gives exactly 45),
  `find_k_nilpotent` (k=−2 zeroes all four entries of A², not only the one solved).
  Inverses were verified by multiplying back to I on `adj_three_at` (A Aᵀ = 9I confirms
  A⁻¹ = Aᵀ/9), `transpose_inverse`, `adj_inverse_2x2` and `inverse_1_2_3_minus5`.
- **Every defect found is in PROSE, not in the working** — the same pattern the earlier groups
  reported. Both HARMFUL findings are a false *reason* bolted onto correct lines, and in each
  case the card contradicts itself: `adj_three_at` calls its adjoint symmetric while its own
  `common_mistakes` warns not to skip the transpose; `reverse_law_transpose` says A′B′ is "not
  even defined" and then states its size in the same sentence.
- **Sibling cloning is visible.** The identical `common_mistakes` string "Squaring the entries
  without doing the multiplication" appears on both `i_squared` cards, where each card's own
  memory_tip endorses exactly that route. The false symmetry claim on `adj_three_at` s2 has the
  same signature — text that would be true of a different adjoint.
- **The "correct route marked as an error" shape** appears in a milder form on two further cards
  (`equality_3x3` s1 "Writing out all nine equations and losing time"; `rank_two` s2 "Checking
  every 2×2 minor and wasting time"). Both name TIME rather than correctness, so neither is
  reported above — but if this group is re-cut, they are the two nearest the line.
- `marks_total` reconciles on all 27 cards, `mark_split` labels match the step labels on all 27,
  and every `mark_note` names work the step actually does.
- Rule 41: register is plain and literal throughout except the "is a gift" metaphor noted above.
  No idioms, no personification, and no country-specific content found in this group.
