Cards audited: 29 / 29   Findings: HARMFUL 0 · WRONG 3 · WEAK 3

Group B12 — mathematics_1b, Unit 10 Applications of Derivatives (final third): Rolle/Lagrange,
approximations and errors, subtangent/subnormal, tangent & normal, maxima/minima.
Every derivative, every c, every line equation and every numeric check in all 85 steps was
re-derived independently of the cards' own `verification.note`. The mathematics in the `lines[]`
is correct on all 29 cards. Every finding below is in a prose field.

## HARMFUL — wrong mathematics a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| — | — | none found | — |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_ad_square_3_to_3_01 | s2_answer (`common_mistakes[1]`) | "Giving 9.0601 or **0.0601**, which answers a different question from the one asked." 0.0601 cm² is the *exact* increase in the area — the step's own line computes it ("Exact increase = 9.0601 − 9 = 0.0601 cm²") and the margin_note tells the student it "is worth writing beside it". The card condemns as an error the very number it instructs the student to write, and 0.0601 is in fact a correct increase in area. Only 9.0601, the new area, is a wrong answer. | "Giving 9.0601, the new area, instead of the increase." Drop 0.0601 from the mistake list — it is the exact increase and the card prints it deliberately. |
| ts_ipe_m1b_ad_rolle_sin_x_minus_sin_2x_on_0_pi | insider_note | "Only the root that lies inside (0, π) may be quoted." Both roots of 4cos²c − cos c − 2 = 0 give a c inside (0, π): cos c ≈ 0.843 → c ≈ 0.568 and cos c ≈ −0.593 → c ≈ 2.206. The card's own s2 margin_note says so ("here there are two") and the boxed answer quotes both. The chatbot's opening line sends the student hunting for a root to reject that does not exist. | "Both roots of the quadratic in cos c give a c inside (0, π); either one verifies the theorem." |
| ts_ipe_m1b_ad_rolle_x2_minus_1_x_minus_2 | insider_note | "The quadratic 3c² − 4c − 1 = 0 has two roots, and only the one inside (−1, 2) may be given." Both roots, (2 + √7)/3 ≈ 1.549 and (2 − √7)/3 ≈ −0.215, lie inside (−1, 2). The card's own s2 line states this and the boxed answer quotes both; the verification note flags it explicitly. | "The quadratic 3c² − 4c − 1 = 0 has two roots and both lie inside (−1, 2); either one verifies the theorem." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_ad_subtangent_constant_y_a_power_x | s3_in_terms_of_y (`common_mistakes[0]`) | "Leaving the slope as aˣ logₑa and dividing aˣ by aˣ later, **which works** but takes an extra line." A route the card itself calls correct is filed under `common_mistakes`, a field the student and the chatbot both read as "this is an error". | Move it out of `common_mistakes` — say in `why` or `memory_tip` that keeping the slope as aˣ logₑa reaches the same answer one line later. |
| ts_ipe_m1b_ad_sphere_radius_7_to_7_02 | s2_answer (`why`) | "The exact increase is … 3.9312π cm³, and the approximation gives 3.92π cm³. They agree to three figures." They agree to two significant figures (3.9); the third figure is where they differ. | "They agree to two figures and differ by about 0.3%, because dr = 0.02 is small compared with r = 7." |
| ts_ipe_m1b_ad_sphere_radius_7_to_7_02 | s2_answer (`common_mistakes[1]`) | "Computing the two full volumes and subtracting, **which is exact** but is not what 'approximate' asks for." An exact, correct calculation of the increase is listed as a student mistake; the card concedes in the same clause that it is right. | Keep it as an examiner note, not a mistake: "The marks here are for the differential method dV = 4πr²·dr; subtracting the two exact volumes is correct arithmetic but does not show the method the question is testing." |

## Cards checked clean

ts_ipe_m1b_ad_rolle_log_x2_plus_2_minus_log_3, ts_ipe_m1b_ad_rolle_x2_minus_1_on_minus1_1, ts_ipe_m1b_ad_rolle_x2_plus_4_on_minus3_3, ts_ipe_m1b_ad_rolle_x_x_plus_3_e_minus_x_over_2, ts_ipe_m1b_ad_slope_normal_astroid_at_pi_over_4, ts_ipe_m1b_ad_sphere_diameter_40_error, ts_ipe_m1b_ad_square_side_up_4_percent, ts_ipe_m1b_ad_state_rolle_and_lagrange, ts_ipe_m1b_ad_subnormal_constant_find_k, ts_ipe_m1b_ad_subnormal_constant_y2_4ax, ts_ipe_m1b_ad_subtangent_constant_subnormal_y2_over_a, ts_ipe_m1b_ad_subtangent_squared_varies_subnormal, ts_ipe_m1b_ad_subtangent_subnormal_b_sin_x_over_a, ts_ipe_m1b_ad_tangent_normal_2e_minus_x_over_3_y_axis, ts_ipe_m1b_ad_tangent_normal_quartic_at_0_5, ts_ipe_m1b_ad_tangent_normal_x3_plus_4x2_at_minus1_3, ts_ipe_m1b_ad_tangent_normal_xy_10_at_2_5, ts_ipe_m1b_ad_tangent_normal_y4_ax3_at_a_a, ts_ipe_m1b_ad_tangent_normal_y_5x4_at_1_5, ts_ipe_m1b_ad_tangent_parallel_chord_y_x2, ts_ipe_m1b_ad_tangent_root_x_plus_root_y_equals_root_a, ts_ipe_m1b_ad_tangent_sec_tan_curve, ts_ipe_m1b_ad_two_integers_sum_60_xy3_max, ts_ipe_m1b_ad_two_numbers_sum_15_min_sum_squares

(24 clean · 5 cards carry a finding.)

## Notes

**What was checked and passed.**

- **Rolle / Lagrange hypotheses (7 cards).** `state_rolle_and_lagrange` states Rolle with THREE conditions
  and Lagrange with TWO — the error the sibling group found in the middle third is NOT present here, and
  the boxed "Rolle is the special case f(a) = f(b)" line is correct. On every verification card the
  conditions are checked on the stated interval BEFORE the derivative is used, continuity is claimed on
  the closed interval and differentiability on the open one, and the c produced is strictly interior:
  c = 0 in (−1,1); c = 0 in (−3,3); c = −2 in (−3,0) with x = 3 rejected in writing; (2 ± √7)/3 both
  in (−1,2); Cos⁻¹((1 ± √33)/8) ≈ 0.568 and 2.206, both in (0,π) — all re-derived and confirmed
  numerically. `rolle_log_x2_plus_2` handles the printed OPEN interval (−1, 1) honestly on the page.
- **Subtangent / subnormal formulas — no swap anywhere.** Every card that states them writes
  subtangent = y/m and subnormal = y·m, and the mnemonic ("sub-NORMAL multiplies, sub-TANGENT divides")
  is the right way round on all six cards that carry it. The distractor formulas quoted in
  `common_mistakes` — length of tangent y√(1+m²)/m, length of normal y√(1+m²) — are also correct.
  Results re-derived: y² = 4ax gives 2a; y = a^(1−k)x^k gives k = 1/2 and subnormal a/2; y = be^(x/a)
  gives subtangent a and subnormal y²/a; y = aˣ gives 1/logₑa; by² = (x+a)³ gives
  (subtangent)²/subnormal = 8b/27; y = b sin(x/a) gives a tan(x/a) and (b²/2a) sin(2x/a).
- **Rates / approximations — differential and base point correct on all four.** The diameter card halves
  both the value and the error (r = 20, dr = 0.01) and gives dV = 16π cm³, dS = 1.6π cm² with the right
  units; the sphere card uses the OLD radius 7 as the base point; the percentage card uses the relative
  form dA/A = 2(dx/x) to reach 8% (exact 8.16%, correctly quoted). Units are cm³ / cm² / percent as
  appropriate throughout.
- **Maxima/minima — both cards run a real second-derivative test**, not an asserted verdict:
  f″(45) = −8100 < 0 for the maximum, S″ = 4 > 0 for the minimum. Both back-substitute and both quote
  a numeric sanity check (16×44³ and 14×46³ both below 15×45³; 7²+8² = 113 above 225/2) — all four
  arithmetic checks re-computed and correct.
- **Tangent/normal (6 cards).** Every slope, every point-slope expansion and every general-form
  rearrangement re-derived; every card's own "check the point in the boxed line" arithmetic verified
  (e.g. 3a − 4a + a = 0; −1 − 15 + 16 = 0; 1 + 100 − 101 = 0). Normal slopes are −1/m throughout,
  never −m and never 1/m.
- **Mechanical.** `steps[].marks` sums to `marks_total` on all 29 cards, and `mark_split` sums to the
  same total on all 29. 85 steps, matching the group manifest. No `mark_note` mislabels the work its
  step does. No prose field is duplicated across any two cards in the group — no cloned boilerplate.
  No cross-reference to a chapter, unit or answer number appears in any student-facing field.
  A Rule 41 sweep over every reader-facing string found no idiom, metaphor or personification.

**Pattern.** The three WRONG findings and one WEAK finding are the same defect in two costumes: a
prose field written as a *generic* piece of exam advice ("only the interior root may be quoted",
"the exact subtraction is not what was asked", "0.0601 answers a different question") that is FALSE
about the specific card it sits on, because that card is exactly the case where both roots are
interior, or where the exact value is printed on the page on purpose. Two of the three are
`insider_note`, the chatbot's opening line, and in both cases the card's own margin_note already
says the opposite. Worth a targeted sweep of the bank for insider_notes that assert "only one root"
on Rolle cards.
