Cards audited: 28 / 28   Findings: HARMFUL 0 · WRONG 7 · WEAK 9

Scope: Maths-1A Unit 5 (Matrices), first half — 4 Cramer, 6 determinant proofs, 4 Gauss-Jordan,
3 matrix-inversion, 11 SAQ. Every determinant, cofactor, adjoint, row/column operation and matrix
product was recomputed independently; every linear system's stated solution was substituted back into
all three original equations; every claimed identity was re-derived symbolically and spot-checked
numerically (a = 1, b = 2, c = 3).

## HARMFUL — wrong mathematics a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| — | — | None. All 28 cards' mathematics is correct: 4/4 systems verified by back-substitution, 6/6 determinant identities re-derived and numerically confirmed, all nine-entry cofactor/adjoint matrices recomputed entry by entry, and every A·A⁻¹ / A·(Adj A) product checked against I / (det A)I. | — |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_mat_det_1_a2_a3 | s7_expand | `why` says "Rushing this line is where **the two marks** are lost", but the step carries 3 marks and its own `margin_note` says "The last three marks together". Stale text from the pre-2026-27 7-mark cut. | "…where the three marks are lost." |
| ts_ipe_m1a_mat_det_square_cyclic | s6_conclude | `margin_note` says "No separate mark — **the seven** are already spent". The card is `marks_total: 8` and its steps sum to 8. Same stale 7-mark relic. | "…the eight are already spent…" |
| ts_ipe_m1a_mat_gauss_3x_4y_5z_18 | s5_read | `common_mistakes`: "Forgetting a row left with a −1 pivot, which needs one sign flip." No row in THIS solution is left with a −1 pivot — s3 normalises row 2 to +1 and s4 reaches a clean identity. Copied verbatim from the sibling `gauss_2x_y_3z_9`, where row 2 genuinely stays at −1. | Drop it, or replace with an error that can occur here (e.g. stopping at the triangular form and back-substituting). |
| ts_ipe_m1a_mat_inverse_2x_y_3z_8 | s4_adj_inverse | `why` contradicts itself on this card — "Forgetting the transpose gives a matrix that looks right and **is wrong**" — when A here is symmetric, its cofactor matrix is symmetric, and the card's own `insider_note` says "the transpose changes nothing". The `common_mistakes` line "Using the cofactor matrix directly as the adjoint without transposing" therefore marks as an error a step that is CORRECT on this question. | Say it the way the insider note does: here the cofactor matrix happens to be symmetric so the transpose changes nothing, which is why the habit must still be written; do not list a harmless step as a mistake on this card. |
| ts_ipe_m1a_mat_inverse_3x_4y_5z_18 | s2_det | `memory_tip` "det A = 136, non-zero and **large — which keeps the nine cofactors whole numbers**" (same claim in `margin_note`). False causality: the cofactors are integers because A's entries are integers; the size of det A has nothing to do with it. A student can carry this away as a rule. | "det A = 136 ≠ 0, so A⁻¹ exists — and it divides all three products exactly here, so no fractions survive to the last line." |
| ts_ipe_m1a_mat_saq_ai_plus_be_cubed | s1_form | `common_mistakes`: "Trying to expand (aI + bE)³ by the binomial theorem **without checking that I and E commute**", and `why`: "matrices do not generally commute, so that is dangerous". I is the identity — it commutes with every matrix, so there is nothing to check. The card's own `insider_note` names the real requirement ("would need E² = O justified separately"). | "Expanding by the binomial theorem without first justifying E² = O — the E² and E³ terms are what vanish; commuting is not the issue." |
| ts_ipe_m1a_mat_saq_a_cubed_identity | s2_a_cubed | `common_mistakes`: "Multiplying A·A² and A²·A as though they might differ; **here** they do not…" The word "here" implies powers of one matrix might fail to commute elsewhere. AᵐAⁿ = AⁿAᵐ always. | "A·A² and A²·A are always equal for powers of one matrix, so use A²·A and do not compute both." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_mat_gauss_3x_4y_5z_18 | s5_read | `why` illustrates with "A row reading 0 0 1 ⎪ 3 is literally the equation z = 3" — but row 3 on THIS card reads 0 0 1 ⎪ 1 and z = 1. The example number is carried over from the sibling card. | Use this card's own row: "0 0 1 ⎪ 1 is literally z = 1." |
| ts_ipe_m1a_mat_saq_inverse_2_1_2 | s1_det | `memory_tip` / `margin_note`: "Row 2 has a 0, so expanding along the **top row** and using it keeps the arithmetic short." Row 2's zero cannot be "used" while expanding along row 1 — the short expansion is along row 2 (or column 2). | "Row 2 is (1, 0, 1), so expand along row 2 — the 0 removes a term outright: det A = −1(1−4) − 1(4−2) = 1." |
| ts_ipe_m1a_mat_saq_inverse_equals_a_cubed | s1_rtp | `why`: "That swap is what makes this a 4-mark question instead of **a 7-mark one**." Long answers are 8 marks on the 2026-27 paper this bank is cut to. | "…instead of a full long-answer one." |
| ts_ipe_m1a_mat_inverse_2x_y_3z_9, ts_ipe_m1a_mat_inverse_3x_4y_5z_18 | s4_adj_inverse | Boilerplate `why` sentence "On a symmetric cofactor matrix the transpose changes nothing" is carried onto two cards whose cofactor matrices are NOT symmetric, where it explains nothing and invites the student to skip the transpose. | Keep that sentence only on `inverse_2x_y_3z_8`, where it is true. |
| ts_ipe_m1a_mat_det_a_minus_b_minus_c | s1_setup, s2_row_sum, s6_final | Three reader-facing strings lean on "the last question" / "the previous question's answer 2(a+b+c)³" (that is `det_sum_2c_cube`). Cards are opened individually in the player, so a student may have no previous question. | Name the contrast instead of its position: "the a+b+2c determinant has a 2 in its answer; this one does not." |
| ts_ipe_m1a_mat_gauss_no_solution | s4_contradiction | `memory_tip` / `margin_note` say "Subtracting **the identical rows**", but rows 2 and 3 are (0 0 1 ⎪ 2) and (0 0 1 ⎪ 1) — identical only to the left of the rule, which is the entire point of the step. | "The two rows are identical on the left of the rule but not on the right, so subtracting leaves 0 = −1." |
| all four Cramer cards; det_1_a2_a3; det_powers_abc; det_square_cyclic | s1/s2/s3 | Rule 41 — metaphor: "Cramer's rule is a **recipe** that reads off this matrix" (on all four Cramer cards); "this single move **buys** two zeros at no cost"; "you may **buy** zeros for free"; "this determinant is its usual **disguise**"; "Everything after it is **bookkeeping**". | "Cramer's rule is a fixed procedure applied to this matrix"; "the operation costs nothing and creates two zeros"; "…is the standard form of a³+b³+c³−3abc". |
| gauss_infinite_solutions; gauss_no_solution; gauss_2x_y_3z_9; gauss_3x_4y_5z_18; saq_inverse_2_1_2; saq_a_cubed_identity | s1/s2/s4 | Rule 41 — personification: "Gauss-Jordan … always **wants** a 1 at the pivot"; "this system **will not behave**"; "the **surprise arrives** two steps later"; "the next question **will not be so kind**"; "a dropped minus here **quietly ruins** A³". | "the method needs a 1 at the pivot"; "this system has no solution, which the working will show"; "the next question will have a determinant other than 1". |
| gauss_2x_y_3z_9; gauss_3x_4y_5z_18; saq_adj_inverse_1_0_2; saq_adj_inverse_1_3_3; saq_inverse_2_1_2; saq_nonsingular_inverse_1_2_1; saq_inverse_formula_proof | s1–s5 | Rule 41 — idiom: "**drowning** in fractions"; "It **buys you** an answer"; "the middle 0 **kills** one of the three terms" (twice); "this line is a **gate, not a warm-up**" (four cards); "makes the last step **painless**"; "That is the **heart of it**"; "The transpose is **not decoration**"; "makes the whole method **feel less like a trick**". | Literal equivalents: "the working fills with fractions"; "so the answer is read straight off"; "the 0 makes one of the three terms vanish"; "this line must be written before anything else — a singular matrix has no inverse". |

## Cards checked clean

ts_ipe_m1a_mat_cramer_2x_y_3z_9, ts_ipe_m1a_mat_cramer_3x_4y_5z_18, ts_ipe_m1a_mat_cramer_x_minus_y_3z_5, ts_ipe_m1a_mat_cramer_x_y_z_1, ts_ipe_m1a_mat_det_bc_ca_ab_twice, ts_ipe_m1a_mat_det_powers_abc, ts_ipe_m1a_mat_det_sum_2c_cube, ts_ipe_m1a_mat_gauss_2x_y_3z_9, ts_ipe_m1a_mat_gauss_infinite_solutions, ts_ipe_m1a_mat_saq_a_squared_minus_4a_minus_5i, ts_ipe_m1a_mat_saq_adj_inverse_1_0_2, ts_ipe_m1a_mat_saq_adj_inverse_1_3_3, ts_ipe_m1a_mat_saq_inverse_equals_transpose, ts_ipe_m1a_mat_saq_inverse_formula_proof, ts_ipe_m1a_mat_saq_nonsingular_inverse_1_2_1, ts_ipe_m1a_mat_saq_trig_product_zero

(16 of 28 carry no card-specific finding. The three Rule-41 register rows above are group-wide wording
issues and name several of these cards for register only, not for content.)

## Notes

- **Zero harmful mathematics — the first unit in this bank to come through an accuracy pass with no
  arithmetic or sign error at all.** Specifically verified: all four Cramer solutions substitute back into
  all three of their own equations (1,2,3 · 3,1,1 · 0,1,2 · 7,−10,4); `det_1_a2_a3`, `det_powers_abc`,
  `det_a_minus_b_minus_c`, `det_bc_ca_ab_twice`, `det_sum_2c_cube` and `det_square_cyclic` all re-derive
  exactly, including the two-sign-flip reorderings that finish the first two; every 3×3 cofactor matrix
  (five cards) is correct in all nine entries and every adjoint is its true transpose; A·A⁻¹ = I confirmed
  on `adj_inverse_1_0_2`, `adj_inverse_1_3_3`, `inverse_2_1_2` and `nonsingular_inverse_1_2_1`; A⁴ = I
  confirmed entry by entry on `inverse_equals_a_cubed`; A³ − 3A² − A − 3I = O confirmed in all nine
  entries; the row-by-row determinant multiplication in `det_square_cyclic` checked in all nine products.
- **Marks are sound everywhere.** `steps[].marks` sums to `marks_total` on all 28 cards, `mark_split` sums
  to the same total on all 28, and every `mark_note` describes work its step actually does. The running
  "First mark / Fourth mark / Last mark" commentary in `margin_note` also matches the cumulative total on
  every card — except the two stale 7-mark relics listed above.
- **The defect pattern in this group is COPYING between sibling cards, not arithmetic.** Four of the seven
  WRONG findings and two WEAK ones are text that is correct on one card and carried onto a sibling where
  it is false (`gauss_2x_y_3z_9` → `gauss_3x_4y_5z_18`; `inverse_2x_y_3z_8`'s symmetric-A remark → the two
  non-symmetric inverse cards). The four Cramer cards, the three inverse cards and the four Gauss cards
  each share a near-identical `why` / `common_mistakes` skeleton by design, so any future edit to one member
  of a family must be re-checked against the numbers of the others.
- **Two 7-mark relics survive the 2026-27 re-cut** (`det_1_a2_a3` s7, `det_square_cyclic` s6). Both sit in
  prose that names a mark count, which no gate reads. Worth grepping the whole maths bank for
  "the two marks", "the seven", "7-mark" inside `why` / `margin_note` / `memory_tip`.
- `verification.status` is `unverified` with `needs_teacher_verification: true` throughout, and the notes are
  explicit that the mark splits are ours and not the source book's. Nothing overclaims exam history — the
  `appearances` arrays match the AP/TS year tags the notes attribute to the book.
- `insider_note` is present on every card in the group and is true on every card.
