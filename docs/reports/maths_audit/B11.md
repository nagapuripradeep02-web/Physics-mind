Cards audited: 30 / 30   Findings: HARMFUL 1 · WRONG 5 · WEAK 3

Group B11 — mathematics_1b unit 10, Applications of Derivatives (middle third), 30 cards / 70 steps.
Every step was re-derived independently; `verification.note` was ignored as evidence.
Mechanical checks all pass: `steps[].marks` sums to `marks_total` on all 30 cards, `mark_split`
labels and marks match the steps one-for-one on all 30, and every step carries `mark_note`,
`margin_note`, `memory_tip`; every card carries `insider_note`.

## HARMFUL — wrong mathematics a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_ad_profit_150_minus_x_max | insider_note (contradicts s3_confirm) | "The x² coefficient is negative, so the stationary point is automatically a maximum and **no second-derivative test is needed**." The card allots a whole mark (s3, 1 of 4) to exactly that test, its s3 margin_note says "A stationary point is not a maximum until the test says so, even when the shape of the graph makes it obvious", and its s3 common_mistakes lists "Skipping the test because the answer looks obvious from the shape" as an error. A student who follows the insider_note drops the step the same card marks — and the note is spoken to them verbatim by the chatbot. | "The x² coefficient is negative, so you already know the shape — but still write P″ = −2 < 0, because that line carries its own mark." |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_ad_lmvt_log_x_on_1_2 | insider_note | "Verify all **THREE** conditions before finding c." Lagrange's MVT has TWO hypotheses (continuous on [1, 2], differentiable on (1, 2)); the third condition f(a) = f(b) belongs to ROLLE. The card's own s1 says "Both conditions hold", and here f(1) = 0 ≠ log 2 = f(2), so a student hunting a third condition would conclude the theorem fails. Boilerplate cloned from a Rolle sibling card. | "Verify BOTH conditions before finding c — continuity on [1, 2] and differentiability on (1, 2)." |
| ts_ipe_m1b_ad_dy_delta_y_ex_plus_x | insider_note (contradicts s1_dy `why`) | "Leaving the answer in terms of e is only half of what is asked." The same card's s1 `why` says the opposite: "Writing dy = (e⁵ + 1)(0.02) **earns the mark on its own**; the decimal is the reading of that exact value." dy = 0.02(e⁵ + 1) is a complete exact answer, so the insider_note marks a correct answer form as incomplete. | "Both dy and Δy are wanted. The exact forms are complete answers; evaluate e⁵ as well so the two values can be compared." |
| ts_ipe_m1b_ad_dy_delta_y_5x2_plus_6x_plus_6 | insider_note | "…they differ in the **fourth** decimal." dy = 0.026 and Δy = 0.026005 differ in the SIXTH decimal place, and the card's own s2 margin_note says exactly that ("dy and Δy differ in the sixth decimal place"). | "…and they differ only in the sixth decimal place." |
| ts_ipe_m1b_ad_particle_t3_minus_9t2_velocity_zero | s2_positions (margin_note) | "A negative S is a real position — it is 2 units on the other side of the **starting point**." S = t³ − 9t² + 24t − 18 gives S = −18 at t = 0, so the starting point is S = −18, not S = 0. At t = 4 the particle is at S = −2, which is 16 units **past** its starting point, on the same side of it. The negative sign is measured from the fixed point, not from where the particle started. | "A negative S is a real position — it is 2 units on the far side of the fixed point the distance is measured from, not a mistake. (The particle started at S = −18.)" |
| ts_ipe_m1b_ad_normal_subnormal_catenary | s4_subnormal (memory_tip) | "Subtangent divides, subnormal multiplies: y/m and ym. **The word with more letters, subnormal, is the product.**" Subnormal has 9 letters, subtangent has 10 — subtangent is the word with more letters. A student who applies the mnemonic as written picks subtangent as the product and gets both formulas backwards. | Drop the letter-count mnemonic and keep the correct half only: "Subtangent divides, subnormal multiplies: y/m and ym." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_ad_dy_delta_y_x2_plus_x | s2_delta_y (margin_note) | "Δx = 0.1 is ten times bigger than in **the previous part**" — a card is read standalone; there is no previous part on the student's screen or in the chatbot's context. | "Δx = 0.1 is large as these questions go, so dy and Δy sit further apart — 2.1 against 2.11." |
| ts_ipe_m1b_ad_find_c_ex_on_0_1 | s2_solve_for_c | Three different bogus forms of one warning, none of them a form a student would write: margin_note says logₑ(e − 1) "is not 1 − logₑ e" (that equals 0), common_mistakes says "1 − logₑ 1" and "logₑ e − logₑ 1" (that equals 1). The reader cannot tell what is being warned against. | One warning: "A logarithm of a difference does not split — logₑ(e − 1) is not logₑ e − logₑ 1." |
| ts_ipe_m1b_ad_normal_subnormal_catenary | s3_normal_length (common_mistakes) | "Writing √(y²/a²) as **y²/a**, forgetting that the root removes the squares" sits three lines above the card's own boxed answer, which IS y²/a. A student skimming common_mistakes reads the correct final answer listed as an error. | "Writing √(y²/a²) as y²/a² — the root removes the squares, so the factor is y/a; only multiplying by y afterwards gives y²/a." |

## Cards checked clean

ts_ipe_m1b_ad_dy_delta_y_one_over_x_plus_2, ts_ipe_m1b_ad_dy_delta_y_x2_plus_3x_plus_6, ts_ipe_m1b_ad_find_c_x2_minus_3x_minus_1, ts_ipe_m1b_ad_intervals_increasing_decreasing_x2_minus_3x_plus_8, ts_ipe_m1b_ad_kxn_relative_error, ts_ipe_m1b_ad_lmvt_sin_x_minus_sin_2x_on_0_pi, ts_ipe_m1b_ad_lmvt_x2_minus_1_on_2_3, ts_ipe_m1b_ad_marginal_cost_3_units, ts_ipe_m1b_ad_marginal_revenue_x_5, ts_ipe_m1b_ad_max_area_rectangle_perimeter_20, ts_ipe_m1b_ad_more_than_one_c_cubic, ts_ipe_m1b_ad_no_k_two_distinct_roots, ts_ipe_m1b_ad_normal_y_x2_minus_4x_plus_2_at_4_2, ts_ipe_m1b_ad_open_box_30_by_80_max_volume, ts_ipe_m1b_ad_particle_45t_plus_11t2_minus_t3_rest, ts_ipe_m1b_ad_particle_8t_plus_t3_velocity_acceleration, ts_ipe_m1b_ad_particle_s_4t3_minus_3t2_acceleration_zero, ts_ipe_m1b_ad_particle_s_t3_plus_2t_plus_3_at_4, ts_ipe_m1b_ad_pendulum_percentage_error, ts_ipe_m1b_ad_point_on_y_2x2_rate, ts_ipe_m1b_ad_ripple_area_rate_radius_8, ts_ipe_m1b_ad_rolle_holds_find_a_and_b

(22 cards. The eight cards carrying a finding above are otherwise correct line-for-line — every
defect in this group is in the PROSE, not in the model answer.)

## Notes

- **No line-follows-from-the-line-above defect was found in this group.** All 70 steps were
  re-derived: derivatives, factorisations, quadratic-formula discriminants, the surd algebra in
  `rolle_holds_find_a_and_b` (3c² = 13 + 4√3, b = −6, a = 11, back-checked against f(1) = f(3) = 6),
  the perfect-square identity 1 + m² = y²/a² on the catenary, and every arithmetic check line the
  cards print (P(74) = P(76) = 4024; V(6) = 7344, V(7) = 7392; f(10.01) = 136.2301;
  e^5.02 − e⁵ = 2.9981; C(3) = 589.955; R(5) = 260) all reproduce exactly.
- **Rates and units are sound throughout.** Every rate card differentiates the right quantity with
  respect to the right one and prints the unit: marginal cost 30.015 rupees per unit, marginal
  revenue 66 rupees per unit, dA/dt = 80π cm²/sec, dy/dt = 32 units/sec, and the cm/sec ÷ cm/sec²
  pair on `particle_8t_plus_t3` (correctly flagged "do not write metres" because the stem prints cm).
  `particle_s_t3_plus_2t_plus_3_at_4` correctly says the stem prints no unit and that metres/seconds
  is the standard reading, rather than asserting a unit the paper does not give.
- **Both approximation cards use the right differential at the right base point** — `kxn_relative_error`
  (dy/y = n·dx/x, k cancels) and `pendulum_percentage_error` (index ½ → 0.5%, 2π and g cancel).
- **Every maxima/minima card actually tests the verdict** rather than asserting it:
  `max_area_rectangle_perimeter_20` (A″ = −2), `open_box_30_by_80_max_volume` (V″ = −280 < 0, plus a
  reasoned domain rejection of x = 30 against 0 < x < 15), `profit_150_minus_x_max` (P″ = −2) — which
  is exactly why the `profit` insider_note telling the student to skip that test is the one HARMFUL
  finding here.
- **Every Rolle/LMVT card states the hypotheses before applying the theorem and confirms c lies in
  the OPEN interval**: c = 5/2 ∈ (2, 3); c = 1/logₑ 2 ≈ 1.443 ∈ (1, 2); c = logₑ(e − 1) ≈ 0.541 ∈ (0, 1);
  both c = Cos⁻¹((1 ± √33)/8) ≈ 0.568 and 2.206 ∈ (0, π); c = 1/7 ∈ (−11/7, 13/7);
  `no_k_two_distinct_roots` correctly places c = 3/2 OUTSIDE [0, 1] to close the contradiction, and
  `more_than_one_c_cubic` correctly splits at the middle zero and argues c₁ ≠ c₂ from disjoint
  subintervals. `lmvt_log_x_on_1_2` is the only card whose statement of the hypotheses is wrong, and
  it is wrong only in the insider_note, which contradicts the card's own step.
- **No `common_mistakes` line marks a correct alternative route as an error.** The closest calls are
  deliberate and correct: `lmvt_sin_x_minus_sin_2x` explicitly warns against REJECTING the negative
  root (both roots give a valid c), and `more_than_one_c_cubic` correctly notes that solving
  f′(x) = 3x² − 12x + 11 = 0 confirms the answer but is not the argument the question asks for.
- Rule 41 register is clean across all 70 steps. A regex sweep for idiom and personification returned
  only "the question asks / which answers nothing" constructions, where the subject is the exam
  question and not a mathematical object — normal exam English, not personification.
- Stale ordinal-mark margin notes were excluded per instruction; separately, every ordinal I did read
  in this group ("Fourth mark" on `max_area` s3 and on `intervals` s3, both correctly preceded by
  1 + 2 marks) is arithmetically right.
