# A08 — Maths-1A Unit 7 Product of Vectors (examiner accuracy audit)

Cards audited: 32 / 32   Findings: HARMFUL 0 · WRONG 10 · WEAK 17

Every dot product, cross product, scalar triple product, magnitude, normalisation and final
answer in all 103 steps was recomputed component by component, independently of the card's
`verification.note`. Cross-product row ORDER and the sign that follows were checked on every
determinant; the four vector-triple-product cards were additionally cross-checked against
a×(b×c) = b(a·c) − c(a·b). Every mark-count claim in prose was compared against `steps[].marks`.

## HARMFUL — wrong mathematics a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| — | — | None. The mathematics of this unit is clean: every printed line follows from the one above, every cross product is in the order the question names, every volume/area formula carries its modulus and its ⅙ or ½, and both unit-normal answers keep the ±. | — |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_pv_sqp_sin_theta_ij_jk | s2_use_a_b_a_b_sin | `why` says θ = 60° "is what two edge-vectors of a cube meeting at a vertex actually make". i + j and j + k are FACE DIAGONALS of a cube, not edges. The edge vectors meeting at one vertex are i, j, k, and they meet at 90°, not 60°. A student who carries this into a cube question gets the wrong angle, and Vidi repeats it as fact. | "θ = 60° — the angle between two face diagonals of a cube that share an edge." Or drop the geometry claim: a·b = 1 gives cos θ = ½ directly. |
| ts_ipe_m1a_pv_saq_cube_diagonals | s1_setup | `common_mistakes` lists "Using a cube of side a and carrying it through pointlessly." A cube of side a is a fully correct and very common proof — every a² cancels, giving cos θ = (a²+a²−a²)/(3a²) = 1/3 — and it earns full marks. Filing a correct route under `common_mistakes` tells the student their valid working is an error. | Move it out of `common_mistakes` into `memory_tip` as a time note: "a cube of side a works too, but every a² cancels — side 1 gets there faster." |
| ts_ipe_m1a_pv_two_triple_products | s4_combine | `margin_note` opens "One mark." The step earns **2** (`marks: 2`; `mark_split` "Find b×c" = 2). A student budgeting from the margin under-weights the step. Not covered by the ORDINAL_MARK_NOTES sweep, which tested ordinal words only, never mark COUNTS. | "Two more marks. Second part now — a DIFFERENT inner bracket…" |
| ts_ipe_m1a_pv_triple_product_pair | s4_combine | Same defect: `margin_note` opens "One mark." on a step whose `marks` is 2 and whose `mark_split` entry ("Find a×b") is 2. | "Two more marks. Second part now, and the bracket has moved to the LEFT…" |
| ts_ipe_m1a_pv_two_triple_products | s4_combine | `mark_note` is "Combine", but this step combines nothing — it computes a fresh inner cross product b×c to open the second half of the question. The label is cloned from the axb_* sibling cards, where s4 genuinely does combine two earlier results. `mark_note` prints in the mark gutter. | "Third product" (or "Second bracket"). |
| ts_ipe_m1a_pv_triple_product_pair | s4_combine | Same cloned `mark_note` "Combine" on a step that computes a×b from scratch. | "Third product" (or "Second bracket"). |
| ts_ipe_m1a_pv_axb_dot_bxc | s1_given | `margin_note` is un-adapted boilerplate from the four-vector sibling: "Copy the three (or four) vectors out once… so a slip here is repeated three times." This card has exactly THREE vectors (never four) and only TWO cross products — the third operation is a dot. The step's own `memory_tip` WAS adapted ("Copy the three vectors"), so only the margin note is stale. | "No mark of its own. Copy the three vectors out once, in i, j, k order. Both cross products below are read straight off these rows, so a slip here is repeated twice." |
| ts_ipe_m1a_pv_two_triple_products | s1_given | Same cloned `margin_note`: "the three (or four) vectors". This card has three vectors and never four. | Drop "(or four)". |
| ts_ipe_m1a_pv_triple_product_pair | s1_given | Same cloned `margin_note`: "(or four) vectors" on a card with exactly three. | Drop "(or four)". |
| ts_ipe_m1a_pv_distance_ab_cd | s2_lines | `why` says "The formula from **answer 23** needs a point and a direction for each line." "Answer 23" is the source coaching book's own numbering, leaked into a reader-facing field sent verbatim to Vidi. It means nothing to a student and names a competitor's book structure. The Maths-1A section of `SOURCE_BOOK_LEAKS.md` does not cover this unit, so this instance appears to be new. | "The shortest-distance formula needs a point and a direction for each line. Four points give exactly that, once the subtractions are done." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_pv_vsaq_unit_normal_two_vectors | s2_answer | The printed chain reads "± 5(−3i + 2j − 6k)/35 = ± (3i − 2j + 6k)/7" — every component silently flips sign between two consecutive lines. It is true only because of the ±, and "does this line follow from the one above?" is exactly the check a student is trained to run. | "= ± (−3i + 2j − 6k)/7". If the tidier form is wanted, say why: "the ± lets us write this as ± (3i − 2j + 6k)/7." |
| ts_ipe_m1a_pv_saq_triangle_area | s2_cross | `memory_tip` and `margin_note`: "All three components come out −3, which is a good sign the arithmetic is right." A symmetric-looking answer is not evidence of correct arithmetic — several sign slips preserve that shape. This teaches a false self-check. | Replace with a real check: "check it — the cross product must dot to zero with both AB and AC." |
| ts_ipe_m1a_pv_axb_cross_cxd | s2_first | Same false self-check: "All three components come out −5 — a useful check." | As above. |
| ts_ipe_m1a_pv_axb_cross_cxd | insider_note vs s4_combine | `insider_note` says "a×b comes out as −5(i + j + k), so take the 5 out early", but the printed working does NOT — s4 puts the raw row (−5, −5, −5) into the third determinant and its own `memory_tip` says "Take the 5 out at the end." One card, two contradictory instructions. | Match the working: "…the common factor 5 comes out of the FINAL vector, which is what makes the magnitude easy." |
| ts_ipe_m1a_pv_sqp_sin_theta_ij_jk | s1_find_the_magnitudes_and_a_ | `why` says "one column of the determinant carries a 0", while the same step's `common_mistakes` says "forgetting the 0 in the determinant ROW". The vectors are the rows; a = (1,1,0) has its zero in the k column and b = (0,1,1) in the i column — two different columns, one zero per row. | "Each vector has a zero component, so each of the two rows carries a 0 — write it in rather than shortening the row." |
| ts_ipe_m1a_pv_vsaq_parallelogram_diagonals | s1_method | `margin_note` opens "Same cross product as before." The preceding card in the unit (…vsaq_parallelogram_sides) has a completely different cross product; only the METHOD repeats. Read literally it invites reusing the previous answer. | "Same cross-product step as before — but these are DIAGONALS, not sides…" |
| ts_ipe_m1a_pv_sqp_box_product_square | s2_expand_with_the_triple_pro | `margin_note` contains a literal unfinished formula: "Use (X × Y) = ... with X = c×a treated as a single vector". The "= ..." is a drafting placeholder printed to the student. | "Use the vector triple product p × (q × r) = (p·r)q − (p·q)r, with p = c×a treated as a single vector…" |
| ts_ipe_m1a_pv_sqp_box_product_square | s3_kill_the_repeated_term (label + mark_split) | Rule 41: the step LABEL — which shows in the rail and in `mark_split` — is "Kill the repeated term". "Kill" is a violent metaphor for "is zero". | "The repeated term is zero" / "Drop the repeated term". |
| ts_ipe_m1a_pv_saq_verify_perpendicular | s2_outer | Rule 41: `memory_tip` and `margin_note` both say "The zero kills one product in each expansion." | "The zero makes one product in each expansion zero." |
| ts_ipe_m1a_pv_triple_product_identity | s4_rhs | Rule 41 metaphor: "The cancellation is the heart of the proof". | "The cancellation is the key step of the proof." |
| ts_ipe_m1a_pv_distance_ab_cd | s3_a_minus_c | Rule 41 metaphor in `common_mistakes`: "Carrying phantom i or k terms through." | "Carrying an i or k term through when both are zero." |
| ts_ipe_m1a_pv_saq_tetrahedron_vertices | s2_triple | Register: `common_mistakes` says "Panicking at the negative and re-doing the arithmetic." Emotive rather than literal, and it names a feeling rather than an error. | "Assuming the negative determinant is a mistake and re-doing correct arithmetic." |
| ts_ipe_m1a_pv_triple_product_pair | s3_second, s5_final | Rule 41a personification: "This part **wants** the VECTOR" (s3 `memory_tip`) and "this part does **want** a number" (s5 `memory_tip` and `margin_note`). The same card's s3 `margin_note` already says it correctly ("This part ASKS FOR the VECTOR"). | Use "asks for" in all three places. |
| ts_ipe_m1a_pv_skew_lines_distance | s2_identify | `common_mistakes` lists "Taking c − a, which changes the sign inside the modulus but not the answer — harmless here, but worth being deliberate about." The entry says in its own words that the alternative is harmless, i.e. correct. A correct route does not belong in `common_mistakes`. | Move to `why` or `memory_tip`: "c − a gives the same distance — the modulus removes the sign — but keep the order you chose." |
| ts_ipe_m1a_pv_triple_product_identity | s3_lhs | `common_mistakes` lists "Expanding all three components in full and running out of time and space." Writing out all three components IS the complete, fully rigorous proof and earns full marks; the only cost is time. Calling it a mistake is wrong. | Move to `memory_tip` as strategy: "all three components in full is correct but long — write i in full and say j and k follow by rotating the letters." |
| ts_ipe_m1a_pv_skew_lines_distance | s6_divide | The boxed answer reads "Shortest distance = 9" with no units, while the sibling distance card writes "4/3 units" and four cards in this same unit list "Omitting the units" under `common_mistakes`. Inconsistent within one unit. | "Shortest distance = 9 units". |
| ts_ipe_m1a_pv_skew_lines_distance · ts_ipe_m1a_pv_saq_tetrahedron_vertices | s1_formula · s2_triple | Both `margin_note`s tell the student what "the book" prints ("the book prints [1 Mark] against this line" / "the book prints [2 Marks] here"). Reader-facing and sent to Vidi. This is category (c) of `SOURCE_BOOK_LEAKS.md`, a known founder-level policy question — recorded here only so the two instances in this unit are named. | Move the provenance into `verification.note`, where it already appears, and keep the margin note about the work. |

## Cards checked clean

ts_ipe_m1a_pv_vsaq_angle_between_planes, ts_ipe_m1a_pv_vsaq_angle_between_vectors, ts_ipe_m1a_pv_vsaq_equal_sum_diff_angle, ts_ipe_m1a_pv_vsaq_find_sin_theta, ts_ipe_m1a_pv_vsaq_magnitude_cross_product, ts_ipe_m1a_pv_vsaq_parallelogram_sides, ts_ipe_m1a_pv_vsaq_perpendicular_find_lambda, ts_ipe_m1a_pv_vsaq_perpendicular_quadratic, ts_ipe_m1a_pv_vsaq_sum_diff_perpendicular, ts_ipe_m1a_pv_vsaq_triangle_area_sides, ts_ipe_m1a_pv_sqp_projection_vector, ts_ipe_m1a_pv_saq_a_cross_bxc, ts_ipe_m1a_pv_saq_unit_normal_plane, ts_ipe_m1a_pv_saq_tetrahedron_edges, ts_ipe_m1a_pv_sqp_parallelogram_diagonals, ts_ipe_m1a_pv_sqp_twice_triangle_area, ts_ipe_m1a_pv_sqp_unit_normal_pqr

## Notes

**The mathematics is the cleanest of any group so far — zero HARMFUL.** Values re-derived
independently and confirmed: (a×b)·(b×c) = −54; |(a×b)×(c×d)| = 5√114; 5√14 vs 3√6 for the two
bracketings; √174; a×(b×c) = −9i − 6j − 3k (confirmed twice — by determinant and by
b(a·c) − c(a·b)); tetrahedron volumes 1/6 and 6 from a −36 determinant; shortest distances 9 and
4/3; cos θ = 1/3 for the cube; area 5√3 from diagonals and 3 from sides; unit normals
±(3i + j)/√10 and ±(2i + j + k)/√6; sin θ = √(155/156) and √3/2. Both proof cards
((a×b)×c = (c·a)b − (c·b)a, and [b×c c×a a×b] = [a b c]²) were re-derived line by line and both
are valid, including the correct p × (q × r) = (p·r)q − (p·q)r orientation in the box-product
proof and the cyclic-shift claims that close it. Every "sides vs diagonals" halving and every
⅙ vs ½ is right, and both unit-normal cards keep the ±.

**All 32 cards pass the marks arithmetic.** `steps[].marks` sums to `marks_total` on every card,
and so does `mark_split`. The failures are in prose ABOUT marks, not in the marks.

**One new machine-detectable family:** a `margin_note` opening with a mark COUNT ("One mark.")
that disagrees with the step's `marks`. The bank-wide ORDINAL_MARK_NOTES sweep tested ordinal
words only ("Sixth mark"), so counts were never checked anywhere. Two hits in 32 cards suggests
a bank-wide sweep of the same shape is worth running.

**Cloned-boilerplate cluster.** The three multi-cross-product LAQ cards (axb_dot_bxc,
two_triple_products, triple_product_pair) share one `s1_given` `margin_note` written for the
four-vector card (axb_cross_cxd) — "the three (or four) vectors… repeated three times" — and two
of them also carry a `mark_note` of "Combine" on a step that combines nothing. The `memory_tip`
in those same steps WAS adapted per card, which is what makes the un-adapted `margin_note` visible.

**Cross-references inside the unit were verified against the real ordering** in
`answer-book/content/mathematics-7.json` and are correct: parallelogram_sides (index 24) → "the
next question" is parallelogram_diagonals (25); perpendicular_find_lambda (20) → "the next
question, where λ appears squared" is perpendicular_quadratic (21). Only the wording "Same cross
product as before" is loose.

**Three `common_mistakes` entries mark a correct route as an error** (a cube of side a; expanding
all three components; taking c − a). This is the same defect shape confirmed in four earlier
groups, which makes it look systemic to how `common_mistakes` was authored rather than incidental.
