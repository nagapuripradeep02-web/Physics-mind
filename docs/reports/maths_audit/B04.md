Cards audited: 36 / 36   Findings: HARMFUL 0 · WRONG 8 · WEAK 12

Group B04 — mathematics_1b u4 Pair of Straight Lines (15) · u5 3D-Coordinates (10) · u7 The Plane (11).
Every final answer in this group was independently re-derived and every one is CORRECT. All 36 cards
have `steps[].marks` summing to `marks_total`, and every `mark_split` sums to `marks_total` too.
No `insider_note` claims exam history the card lacks.

## HARMFUL — wrong mathematics a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| *(none)* | — | Every model answer in this group re-derives correctly: all four homogenisation answers, both Δ = 0 checks (each verified a second way by actually factorising the pair and solving for the meeting point), all four chapter proofs, all ten 3-D answers (each section-formula point re-checked by parametrising the segment) and all eleven plane answers (each plane substituted back through every point and intercept it is said to carry). | — |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_pl_homog_angle_3x_minus_y_plus_1 | s3_simplify | Sign error in the printed factorisation: `−50x² − 2y² + 28xy = 0  ⇒  2(25x² + y² − 14xy) = 0`. Expanding `2(25x² + y² − 14xy)` gives `+50x² + 2y² − 28xy`, the negative of the line above. The next line (`25x² + y² − 14xy = 0`) is computed from the correct division by −2, so the right answer hides the bad step — the exact shape already confirmed twice in this paper. | `⇒ −2(25x² + y² − 14xy) = 0  ⇒  25x² + y² − 14xy = 0` |
| ts_ipe_m1b_pl_chord_coincident_condition | s4_apply_the_condition | `Here a = (1 − a²l²), b = (1 − a²m²), 2h = −2a²lm` re-uses the letter **a** for both the circle's radius and the pair's x² coefficient, so the printed equation reads literally `a = 1 − a²l²`, which is self-contradictory. The facing card (chord_right_angle) avoids it entirely by writing "Coeff. x²". | `Here A = 1 − a²l², B = 1 − a²m², 2H = −2a²lm, and coincident lines need H² = AB` — or use the "Coeff. x² / Coeff. y²" wording the sibling card uses. |
| ts_ipe_m1b_pl_area_triangle_proof | s4_convert | The justification line `[∵ (a − b) = √((a + b)² − 4ab)]` is false without a modulus — √((a+b)²−4ab) = √((a−b)²) = **abs(a − b)**, which equals (a−b) only when a ≥ b. It also re-uses **a** and **b**, which on this very card already mean the pair's x² and y² coefficients (the next line substitutes m₁+m₂ = −2h/**b** with the other meaning). | `[∵ abs(p − q) = √((p + q)² − 4pq)]`, applied to m₁, m₂ — different letters, and the modulus kept. |
| ts_ipe_m1b_pl_check_2x2_minus_13xy | s5_angle_between_them | `common_mistakes`: "Giving the answer as Tan⁻¹(3) when the book states it as a cosine." Tan⁻¹(3) is a completely correct answer to "find the angle between them" and earns full marks; the card's own working establishes tan θ = 3 before converting. A correct alternative form is condemned as an error, on the authority of a book the student cannot see. | Delete it, or replace with a real slip: "Dropping the modulus on abs(a + b) and getting a negative tangent." |
| ts_ipe_m1b_pln_angle_x_2y_2z · ts_ipe_m1b_pln_angle_2x_minus_y_plus_z | s2_apply_the_formula | `common_mistakes`: "Multiplying the two roots separately instead of keeping them under one root." √(9 × 22) and √9·√22 are equal — this condemns a correct step, and on the `x_2y_2z` card it condemns the step the card ITSELF prints one line earlier (`= abs(3 + 6 + 4) / (√9 · √22)`). | Drop it. If a real denominator slip is wanted: "Adding the two sums of squares instead of multiplying them." |
| ts_ipe_m1b_pln_dcs_of_normal | s1_write_the_formula (`common_mistakes`), `insider_note` | "Omitting the ±, which the word 'triad' in the question is asking about" / "The word triad is asking for the ±, so both sign sets belong in the answer." A *triad* is one ordered set of three direction cosines; the ± records that a normal points both ways, not what "triad" means. A student who answers the question as asked — "find **a** triad" — with (1/3, 2/3, 2/3) is correct, and is here told that is a mistake. | "Both sign sets are valid, because a normal points both ways out of the plane; quoting one triad answers the question, and ± shows both." |
| ts_ipe_m1b_td_sqp_distance_find_x | s2_solve_for_x (`why`) | "Two points on the line through (5,−1,7) sit at distance 9 in the x-direction, one on each side." Both halves are false: the locus of the unknown point is the line {(x, 5, 1)}, which does **not** pass through (5,−1,7); and the two answers sit 3 apart in x, not 9 (the card's own line gives (x−5)² = 9). Read literally it invites x − 5 = ±9, i.e. x = 14 or −4. | "The point must lie on the line y = 5, z = 1. A sphere of radius 9 about (5,−1,7) cuts that line twice, which is why two values of x come out." |
| ts_ipe_m1b_td_collinear_1_2_3 · ts_ipe_m1b_td_collinear_5_4_2 · ts_ipe_m1b_td_equilateral_triangle · ts_ipe_m1b_td_sqp_collinear_and_ratio | s1_find_the_three_distances (`why`) | Identical boilerplate on four cards: "Both questions on this page are answered by computing the SAME three distances… which is why the book prints the note 'Questioning is different but Answering is similar' underneath." The four cards use four DIFFERENT point sets (√11, √14, √6 and √3 families), so "the SAME three distances" is false wherever the reader lands; there is no "page"; and the sentence quotes the source book's own editorial note verbatim to the student. | "Collinearity and the shape of a triangle are both settled by the same three distances — only the conclusion drawn from them differs." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_pl_check_2x2_minus_13xy · ts_ipe_m1b_pl_check_3x2_7xy_2y2 | s2_delta (`why`) | "it is the condition proved in **answer 10 of this same chapter**" — source-book answer numbering in student-facing prose. This answer book has no "answer 10"; the chatbot reads it aloud as a pointer to nothing. | "it is the Δ = 0 condition proved for the general second-degree equation." |
| ts_ipe_m1b_pl_sqp_product_perpendiculars_origin | s2 (`why`), s3 (`why`), s4 (`margin_note`) | Three references on one card to "**chapter answer 8**" — same dead numbering, three times. | "…shorter than the version measured from a general point (α, β)." |
| ts_ipe_m1b_pl_chord_right_angle_condition · ts_ipe_m1b_pl_chord_coincident_condition | s1, s4 (`margin_note`, `why`, `common_mistakes`) | Authoring-run and page-layout language a single-card reader cannot resolve: "unlike the earlier questions **in the run**", "the same one used three times earlier **in this run**", "which is why the two **sit on the same page**", "the coincident condition from **the facing answer**", "shorter than **its neighbours**". | Name the mathematics instead: "the coincident condition h² = ab", "the perpendicular condition a + b = 0". |
| ts_ipe_m1b_pl_area_triangle_proof · ts_ipe_m1b_pl_product_perpendiculars_proof · ts_ipe_m1b_pl_sqp_product_perpendiculars_origin | `question_text` | A colon stands where the fraction bar belongs: "is n²√(h²−ab) **:** abs(am²−2hlm+bl²)", "is abs(aα²+2hαβ+bβ²) **:** √((a−b)²+4h²)", "is abs(c) **:** √((a−b)²+4h²)". Read literally each states a RATIO, not the quantity asked for; every boxed answer on the same cards uses "/". | Use "/" (or a rendered fraction) in the question text so it matches the boxed line. |
| ts_ipe_m1b_pl_angular_bisectors_proof | s1_split_and_match (`common_mistakes`), `insider_note` | "Using the slope form m₁x − y, **which cannot express an angle bisector cleanly**" / "the slope form will not bisect cleanly". The slope-form derivation ((y−m₁x)/√(1+m₁²) = ±(y−m₂x)/√(1+m₂²)) is a standard and fully correct route to the same result — longer, not wrong. | Move it out of `common_mistakes`: "The slope form works but is longer, because the two denominators no longer simplify to 1." |
| ts_ipe_m1b_pl_area_triangle_proof | s4_convert (`lines`) | `= ½ · n²(2√(h²−ab)/b) / (abs(bl²−2hlm+am²)/b)` drops the modulus when taking √((4h²−4ab)/b²) = 2√(h²−ab)/**abs(b)**. The b's cancel either way so the answer is unaffected, but the printed step is only valid for b > 0. | Write abs(b) in both places, or note explicitly that the b cancels whatever its sign. |
| ts_ipe_m1b_pl_angular_bisectors_proof · pl_area_triangle_proof · pl_pair_conditions_proof · pl_parallel_lines_conditions_proof · pl_product_perpendiculars_proof · pl_sqp_product_perpendiculars_origin | s1 (`why`), s3/s4 (`why`, `memory_tip`) | Rule 41 register: the coefficient-matching step is called "**the dictionary**" on six cards, and the prose personifies it — "what every later step **spends**", "the products **the dictionary knows**". Metaphor, not mathematics vocabulary; a Class-11 reader with textbook English has to be told what a dictionary is doing in a proof. | "the list of matched coefficients" / "the coefficients matched in (1)"; "every later step uses those matches". |
| ts_ipe_m1b_td_xz_plane_ratio · ts_ipe_m1b_td_yz_plane_ratio | s2_find_the_point (`why`) | "A negative ratio … is what **makes the denominator m + n small** — which is why the coordinates can come out large." A negative ratio does not in general make m + n small (−5 : 2 gives −3); it happens to here. Stated as a general rule it is false, and it is not the reason a coordinate is large. | "A negative ratio marks external division: the cut lies outside the segment, so the point can be far from both A and B." |
| ts_ipe_m1b_td_sqp_collinear_and_ratio | `mark_split`, s2 (`label`) | The question has two named parts ("show collinear **and find the ratio**"), but both mark_split labels and the step label cover only the distances: "Find the three distances" / "Add two of them to get the third". The ratio — half of what is asked — is awarded no mark and is not named in the scheme, though the step does the work. | Re-label the second mark "Add two distances and read off the ratio", so the scheme names every part the question asks for. |
| ts_ipe_m1b_pln_intercepts_x_minus_3y_2z | s1_move_the_constant (`label`, `mark_split`, `margin_note`) | The step is labelled "Move the constant across" and its margin note says "the constant term goes across first", but on this card the constant is already on the right (x − 3y + 2z = 9) and nothing is moved — the card's own `memory_tip` says so ("The constant is already on the right"). Cloned from the two sibling intercept cards. | "Divide through by the constant" — and drop the "goes across first" sentence on this card. |
| ts_ipe_m1b_td_fourth_vertex_parallelogram | s1, s2 (`margin_note`) | "First mark, **printed in the book's margin**" / "Second mark, **also printed**". Truthful against this card's verification note, but it puts the source book into student-facing prose and tells the student where the marks came from rather than what earns them. | "First mark: the diagonals of a parallelogram bisect each other, so equate the two midpoints." |
| ts_ipe_m1b_pln_angle_x_2y_2z | `insider_note` | "**Unlike its partner question** this one has no standard angle" — a cross-card reference a student holding one card cannot resolve. | "This one has no standard angle, so leave it as θ = Cos⁻¹(13/(3√22))." |

## Cards checked clean

ts_ipe_m1b_pl_homog_find_k_perpendicular, ts_ipe_m1b_pl_homog_perpendicular_7x2_minus_4xy, ts_ipe_m1b_pl_homog_perpendicular_x2_minus_xy, ts_ipe_m1b_pl_sqp_centroid_and_area, ts_ipe_m1b_td_fourth_vertex_tetrahedron, ts_ipe_m1b_td_vertex_c_centroid_origin, ts_ipe_m1b_pln_from_intercepts_1_2_4, ts_ipe_m1b_pln_from_intercepts_2_3_4, ts_ipe_m1b_pln_intercept_form_4x_4y_2z, ts_ipe_m1b_pln_intercepts_4x_3y_2z, ts_ipe_m1b_pln_normal_form_x_2y_3z, ts_ipe_m1b_pln_parallel_through_1_1_1, ts_ipe_m1b_pln_parallel_through_1_2_minus3

## Notes

**`ts_ipe_m1b_pl_pair_conditions_proof` — the "two questions in one" split IS defensible.** The card
carries 1+1+1+1 = 4 against `marks_total` 4, all integers, and `mark_split` matches the four steps
one-to-one. Part (a) (Δ = 0) takes s2 + s3, part (b) (the three inequalities) takes s4, and s1 — the
coefficient matching — is genuinely SHARED: part (b) computes h² − ab straight out of the same
`2h = l₁m₂ + l₂m₁`, `a = l₁l₂`, `b = m₁m₂` that s1 establishes. So the effective weighting is 3 : 1
in favour of part (a), which matches the work: the Δ derivation is a six-letter expansion over two
steps, the inequality is three lines and a "similarly". I re-derived both parts in full — the
regrouping of (2h)(2g)(2f) into `a[(2f)²−2bc] + b[(2g)²−2ac] + c[(2h)²−2ab] + 2abc` is correct term
by term, and h² − ab = (l₁m₂ − l₂m₁)²/4 ≥ 0 is correct including the non-strict ≥ and the mistake
line that explains it ("strict h² > ab fails when the lines are parallel"). **No change needed to the
split.** Its one blemish is a `why` on s4 saying "which is why **the book** writes 'similarly'" — the
same source-book leak as the WEAK rows above.

**Pair-of-Straight-Lines mathematics verified, twice where possible.** `check_2x2_minus_13xy`
factorises as (2x + y − 3)(x − 7y + 2), giving (19/15, 7/15) and tan θ = 3 — both match the card by
the coefficient formulas AND by direct solution of the two lines. `check_3x2_7xy_2y2` factorises as
(3x + y + 2)(x + 2y + 1), giving (−3/5, −1/5) — matches. All four homogenisation answers were expanded
from scratch: −50x²+28xy−2y²; 2k²+2k−1 / −2k²+3k−4 / 3k²−2k−4 (sum 5k²−5, so k = ±1); −8x²+xy+8y²;
3x²+√2xy−3y². The four chapter proofs (angular bisectors, area of the triangle, the Δ conditions, the
parallel-lines conditions) and both perpendicular-product proofs each reproduce their stated result
exactly, including the (a−b)² + 4h² regrouping and the −2h / (b−a) substitution at the end of the
bisector proof.

**3-D and Plane answers verified by substitution.** Every collinearity was confirmed a second way by
direction ratios (A→B a scalar multiple of A→C), not only by the distance sum. Both section-formula
points were re-derived by parametrising the segment: XZ-plane t = 3 → (7, 0, 1); YZ-plane t = −2 →
(0, 2, 23) — both match, including the negative ratios and the −y₁ : y₂ / −x₁ : x₂ direction. Every
plane was substituted back through each point and intercept it is said to carry: 4x+2y+z=4 (1, 2, 4);
6x+4y+3z=12 (2, 3, 4); 4x−4y+2z+5=0 (−5/4, 5/4, −5/2); 4x+3y−2z+2=0 (−1/2, −2/3, 1); x−3y+2z=9
(9, −3, 9/2); x+2y+3z−6=0 through (1,1,1); 2x−3y+6z+22=0 through (1,2,−3). All correct.

**The dominant defect in this group is prose, not mathematics.** Of 20 findings, none is a wrong
answer. Six of the eight WRONG rows and most of the WEAK rows are one of two things: (i) a
`common_mistakes` line that condemns a mathematically correct alternative — Tan⁻¹(3); √9·√22;
omitting ± when asked for "a triad"; the slope-form bisector route — the same shape confirmed in every
earlier group, found here on five cards; or (ii) source-book scaffolding surviving into student-facing
prose ("answer 10", "chapter answer 8", "this page", "in the run", "the facing answer", "printed in
the book's margin", and one verbatim quotation of the book's own editorial note on four cards). Every
one of those strings is sent to the chatbot and spoken to students as fact.

**Not re-reported:** stale ordinal-mark margin notes ("First mark", "Second mark", "The last mark"),
already swept in `docs/reports/maths_audit/ORDINAL_MARK_NOTES.md`. **Observed but not filed as a
finding:** every `mark_note` in this group is the bare token "Method" or "Solution" rather than a
description of the step's work — a bank-wide convention, not a B04 defect.
