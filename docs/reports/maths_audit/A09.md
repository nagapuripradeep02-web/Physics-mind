Cards audited: 27 / 27   Findings: HARMFUL 0 · WRONG 6 · WEAK 6

Every identity in this group was re-derived from `question_text` and then independently checked by
substituting concrete angles (A=B=C=60°, A=50/B=60/C=70 with S=90°, A=30/B=50/C=−80 for the
A+B+C=0 card, and exact decimal evaluation of every π/8, π/10 and π/16 product). **All 27 final
answers and every intermediate line are mathematically correct**, including the sign-critical ones
(cos(180°−θ) = −cosθ vs sin(180°−θ) = +sinθ; sin²A − cos²B = −cos(A+B)cos(A−B); sin(−C) = −sinC).
Step marks sum to `marks_total` on all 27 cards, and `mark_split` totals agree on all 27.

## HARMFUL — wrong mathematics a student would copy into an exam

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| _(none)_ | — | No card in this group prints a wrong line, a wrong sign or a wrong final answer. | — |

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1a_tr_sin_half_squares | s2_pair | `memory_tip`: "Convert only TWO of the three terms — converting all three loses track." The model answer converts exactly **ONE** term (sin²(A/2) → 1 − cos²(A/2)); the other two are untouched. A student who follows the tip literally will not reach the printed line. The step's own `common_mistakes` ("Converting all three terms") reinforces the wrong count. | "Convert only ONE of the three terms: sin²(A/2) becomes 1 − cos²(A/2). That is where the leading 1 comes from." |
| ts_ipe_m1a_tr_cos_half_squares · ts_ipe_m1a_tr_sin_half_squares · ts_ipe_m1a_tr_sin_half_minus_c | s2_pair | Step `label` "Pair two terms", `mark_note` "Sum to product", `margin_note` ("two of them are combined into a product first") and the `mark_split` label "Pair two terms with a sum-to-product formula" all describe work the step does **not** do on these three cards — no sum-to-product formula appears in s2; the step applies the Pythagorean identity cos² = 1 − sin². (The genuine product identity cos²A − sin²B = cos(A+B)cos(A−B) is used in **s3**.) On `cos_half_squares` the `memory_tip` repeats it verbatim: "combine TWO into a product first. Quote the formula." | Name the actual work on the half-angle cards: label "Convert one square", mark_note "Pythagorean identity", tip "Write one squared term as 1 − (the other function)² so a cos² − sin² pair appears." The 8-card family template was copied without checking that s2 does the same job on the half-angle members. |
| ts_ipe_m1a_tr_cos2a_sum · cos_half_squares · cos_s_minus_a · cosa_cosb_minus_cosc · sin2a_sum · sin_half_minus_c · sin_half_squares · sin_s_minus_a · sqp_cos_s_minus (9 cards) | s4_factor / s4_take_2_cos_c_2_common | `margin_note` opens "**Sixth mark.**" but after the 7→8-mark re-cut this step is the **seventh** mark on all nine cards (1+3+2+**1**+1, and 2+2+2+**1**+1 on `sqp_cos_s_minus`). On `sqp_cos_s_minus` it also contradicts s3's own margin_note, which correctly claims "Two marks (fifth and sixth)". Stale text left over from the 1+2+2+1+1 = 7 split. | "Seventh mark." |
| ts_ipe_m1a_tr_sqp_cos_sq_tenths | s2_rewrite_the_two_larger_ang | `common_mistakes`: "Writing 108° as 180° − 72°, which works but leads to a longer route." It is not longer — cos²108° = cos²72° and cos²162° = cos²18° give 2(cos²18° + cos²72°) = 2(cos²18° + sin²18°) = 2 in **two** lines, against this card's four steps. A correct and shorter alternative is being listed as a mistake with a false justification. | Either drop the line, or state it truthfully: "180° − 72° also works; this card uses the 90° shift because it makes both Pythagorean pairs appear without needing cos 72° = sin 18°." |
| ts_ipe_m1a_tr_sqp_cos_sq_tenths | s4_pair_by_pythagoras | `common_mistakes`: "Pairing cos²18° with cos²72°." That pairing is mathematically fine — cos 72° = sin 18°, so cos²18° + cos²72° = 1 and sin²18° + sin²72° = 1, giving 2. A correct alternative form is again marked as an error. | "Pairing cos²18° with cos²72° also reaches 2, but only after using cos 72° = sin 18°. Pairing by the SAME angle needs no extra step." |
| ts_ipe_m1a_tr_sin2a_sum | s3_substitute | `common_mistakes`: "Writing sin(180° − C) = −sin C, **which is the cosine rule not the sine one**." "Cosine rule" has a fixed and different meaning in Maths-1A (a² = b² + c² − 2bc cos A, Properties of Triangles). Naming a reduction formula "the cosine rule" will confuse a student who has met the real one. | "…that is what happens to the COSINE, not the sine: cos(180° − θ) = −cos θ but sin(180° − θ) = +sin θ." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| cos_half_squares (s4 `why`) · cos_s_minus_a (s4 `why`) · sin_s_minus_a (s5 `why`) · saq_product_pi_ten (s2 `why`, s2 `margin_note`) · saq_cos_fourth_powers (s2 `why`) | as listed | Reader-facing text cross-references source-book answer numbers and the source book itself, which the student cannot see: "the same tactic as the −1 in **answer 26.2**", "the same tactic used in **answers 26.2 and 28**", "exactly as in **answer 29**", "Identical move to **answer 67**", "which is why **the book** calls the second half 'same to same'". | Name the technique or the sibling question, never the book's numbering: "the same tactic used on the cos 2A + cos 2B + cos 2C proof". No student copy contains an answer 26.2, 28, 29 or 67. |
| cos2a_sum · cos_s_minus_a · saq_cos_fourth_powers · saq_cot_minus_tan · saq_root3_csc20 · sin_half_minus_c (6 cards) | s1–s5 `why`, `memory_tip`, `margin_note`, `common_mistakes` | The word "**the twin**" is used ~15 times in reader-facing strings to mean an unnamed sibling question ("Identical opening to the twin", "the twin ended in cosec", "Copying the twin and using 30°", "Expecting a different answer from the twin and going back to hunt"). A student reading one card alone has no twin. `saq_cos_fourth_powers` s3 `common_mistakes` is entirely meta-advice about the twin rather than an error on that step. | Name the sibling explicitly ("the version with cot A + tan A") or drop the reference. Replace the meta `common_mistakes` with real Class-11 errors on that step. |
| family (≈20 cards) | `why`, `margin_note`, `memory_tip` | Rule 41 — idiom, metaphor and personification in reader-facing strings: "the angle condition **earns its place**", "the hypothesis the whole proof **leans on**", "so the examiner can see **it landed**", "the identity **closes**", "which **unlocks** the last step", "makes the numerator **attackable**", "a 30° or 60° angle **in disguise**", "the fourth powers **kill** the signs", "Nothing was added that did not have to be **paid back**", "that is where the minus **is born**", "**not luck**", "the right sign appearing **by luck**", "the phrase **does all the work**", "**out of habit**", "going back to **hunt**". | Literal wording: "the angle condition is used here", "write = R.H.S so the examiner can see the proof finished", "this line completes the identity", "the fourth power makes both minus signs positive", "the quadrant clause is what makes the answer unique". |
| ts_ipe_m1a_tr_saq_tan_plus_cot · ts_ipe_m1a_tr_saq_cot_minus_tan | s4 `why`, `common_mistakes` | "Leaving the answer as 2/sin 2A" / "Leaving the answer as a fraction" is listed as a mistake and the `why` says "an equal answer written as a fraction is **answering a different question**". 2/sin 2A is mathematically identical; in a "prove that" it is an unfinished proof, not a different answer, and the wording overstates it. | "The question asks for the cosec form, so the last line must read 2 cosec 2A — stopping at 2/sin 2A leaves the proof unfinished." |
| ts_ipe_m1a_tr_sqp_sin_minus_third | s2_find_cos_and_cot | The answers are left as √8/3 and −√8. Both are correct but not in simplest surd form; the card never mentions 2√2/3 and −2√2, which is how most IPE markers and textbooks print them, and a student is normally expected to simplify. | Add the simplified form: "cos θ = √8/3 = 2√2/3 and cot θ = −√8 = −2√2." |
| ts_ipe_m1a_tr_saq_cos_fourth_powers | s2_group_and_use_a_b | The printed line "= 2[(sin² + cos²)² − 2sin²cos²]" drops the angle argument, unlike every neighbouring line on the same card and unlike the sibling `saq_sin_fourth_powers`, which writes it out in full. A student copying it verbatim writes an incomplete expression. | "= 2[(sin²(π/8) + cos²(π/8))² − 2 sin²(π/8) cos²(π/8)]" |

## Cards checked clean

ts_ipe_m1a_tr_saq_acos2t_bsin2t, ts_ipe_m1a_tr_saq_sin_fourth_powers, ts_ipe_m1a_tr_sqp_cos_double_half_pi, ts_ipe_m1a_tr_sqp_cos_sq_eighths, ts_ipe_m1a_tr_sqp_cot_product_one

(Mathematics, marks, tips, `why`, `common_mistakes` and register all sound. The other 22 cards are also mathematically clean — their findings are the register/labelling rows above.)

## Notes

- **The named source defect is NOT present.** No card in this group states tan 70° − tan 20° in any
  form; the identity does not appear anywhere in A09 (the `tan 70° − tan 20° = 2 tan 50°` question
  must live in the second half of the chapter, group A10). Nothing here to confirm or correct —
  flagging so the check is not recorded as passed on evidence that does not exist.
- **The two confirmed bank-wide `common_mistakes` defect shapes: one found, one absent.**
  `sqp_cos_sq_tenths` marks a mathematically correct alternative route as an error twice (s2 and s4).
  No instance of the other shape (a mistake line quoting a quantity absent from the problem) was
  found in this group.
- **The 7→8-mark re-cut left one stale ordinal on nine cards.** The extra mark was added to s2, but
  the s4 `margin_note` still opens "Sixth mark." on every LAQ in the family. It is machine-checkable
  (running mark total vs. the ordinal word in `margin_note`) and no gate checks it today.
- **`insider_note` is present and true on all 27 cards** and claims no exam history the card does not
  carry. Each one names a genuine method choice (which form of cos 2θ, where the −1 comes from,
  which pairing) rather than restating the answer.
- `verification.note` on the eight-card LAQ family is explicit that the mark split is ours, not the
  book's, and that it is the SAME split reused across all eight. That is honest, but it is also why
  the s2 label/mark_note is wrong on the three half-angle members: one template, three different
  s2 jobs.
- The Telugu-Romanized strings inside `recall.accept` are speech-matching data, not rendered text,
  and were not judged under Rule 41.
