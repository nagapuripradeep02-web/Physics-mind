Cards audited: 21 / 21   Findings: HARMFUL 0 · WRONG 2 · WEAK 7

Group B06 — mathematics_1b, unit 8 Limits and Continuity (second half). Every limit was
re-evaluated independently of the card and of its `verification.note`: the indeterminate form was
confirmed before each technique was applied, every cancelled factor was checked to be non-zero on
the punctured neighbourhood, every rationalisation was checked through BOTH lines, every use of
sin θ/θ, tan θ/θ and log(1+t)/t was checked against the argument the card actually has, and every
printed line was checked to follow from the line above it. Every numeric check quoted in a
reader-facing `why` was recomputed. All 21 cards reach a correct boxed answer and all 42 steps
derive correctly. The two substantive defects are both false prose attached to right mathematics.

## HARMFUL — wrong mathematics a student would copy into an exam

None. All 42 steps derive correctly. This includes the four cases where the answer depends on a
condition the card has to state and does: `root_a_plus_2x_minus_root_3x` (states a > 0 before
splitting roots, and both conjugates are carried through), `x_minus_3_over_root_x2_minus_9`
(√(x²−9) is real only for x ≥ 3 near 3, so the approach is one-sided and the card says so),
`sin_tan_squared_over_x2_minus_a2_squared` and `tan_x_minus_a_over_x2_minus_a2` (both state a ≠ 0),
and both one-sided pairs — the floor pair (3 on the left, 4 on the right, with the jump named) and
`two_abs_x…_right` (|x| = x on the right, left-hand value −1 named).

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_lim_one_over_x_minus_2_minus_four_over_x2_minus_4 | s1_combine | Two reader-facing fields state a false claim about the behaviour. The copied line reads "Each part alone increases without bound as x → 2", and `margin_note` repeats it: "Each piece alone increases without bound as x → 2, so neither may be given a limit on its own." Neither part increases without bound as x → 2. 1/(x − 2) → +∞ from the right but → −∞ from the left, and 4/(x²−4) = 4/[(x−2)(x+2)] behaves the same way (x + 2 ≈ 4 > 0 near 2). A student who copies this line has written something false about the left side, and one who tests 1/(x−2) at x = 1.999 sees −1000, not a large positive number. The step's own `why` gets it right ("neither piece has a limit here"), so the card contradicts itself. | "Neither part has a limit as x → 2 — each is unbounded near 2, positive on one side and negative on the other — so the difference rule cannot be used and the two are combined into one fraction first." |
| ts_ipe_m1b_lim_sin_tan_squared_over_x2_minus_a2_squared | s2_apply_and_substitute | `why` quotes a numeric check that is wrong in its third significant figure: "with a = 2 the expression is 0.000625 at x = 2.01 and 0.0000625 at x = 2.001". Recomputed, the expression is 0.0006219 at x = 2.01 (0.0000625 at x = 2.001 is right). 0.000625 is the value of the APPROXIMATION (x − a)/16 that the same sentence goes on to name, not the value of the expression. A student who checks on a calculator gets 0.000622 and has to decide whether the card or the calculator is wrong. | "…the expression is 0.000622 at x = 2.01 and 0.0000625 at x = 2.001, falling by a factor of ten each time, exactly as (x − a)/(2a)² = (x − a)/16 does." |

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| ts_ipe_m1b_lim_floor_x_plus_x_right | s1_floor_above_two | `common_mistakes`: "Treating [x] as continuous and writing lim [x] = [lim x] without checking the side." On THIS card the condemned line is true: [x] is right-continuous at every integer, so lim(x → 2⁺)[x] = [2] = 2 is exactly the right value and would earn the mark. The warning belongs on the left-hand card, where lim(x → 2⁻)[x] = 1 ≠ [2] = 2 — and that card already carries the right version of it ("Using [2] = 2, the value AT the point, which the one-sided limit does not use"). | Replace with a mistake that is actually wrong on the right side, e.g. "Reading 2 < x < 3 as 2 ≤ x < 3 and quoting the value at x = 2 itself, which a one-sided limit never uses." |
| ts_ipe_m1b_lim_sin_tan_squared_over_x2_minus_a2_squared | s1_count_the_powers, s2_apply_and_substitute | The same wrong answer is listed as two different mistakes on one card: s1 has "Cancelling all three powers above against the two below and answering 1/(2a)²" and s2 has "Answering 1/(4a²) by cancelling one power of (x − a) too many". 1/(2a)² and 1/(4a²) are the same number and the same slip, described twice. | Keep one (in s1, where the power count is made) and give s2 a different real slip, e.g. "Writing (x − a)/(x + a)² → 0/0 and calling the limit indeterminate; the denominator is 4a² ≠ 0." |
| ts_ipe_m1b_lim_sin_tan_squared_over_x2_minus_a2_squared | s2_apply_and_substitute | `common_mistakes`: "Writing the answer as 1/(2a)³ by copying the pattern of the simpler tangent question." Two problems: 1/(2a)³ is not a form any plausible slip produces (the tangent question gives 1/(2a), and no miscount here yields a cube), and "the simpler tangent question" is a cross-card reference a student reading this one card cannot resolve. | Drop it, or name the mathematics rather than the other card: "Answering 1/(2a)² by counting only two powers of (x − a) on top instead of three." |
| ts_ipe_m1b_lim_x2_sin_one_over_x | s2_sandwich | `common_mistakes`: "Writing 0 without naming the theorem; the name is what the method is credited for." This contradicts s1, whose `margin_note` says of the double inequality "It is the statement the Sandwich theorem is applied to, and it carries this mark" — i.e. s1 holds the method mark, and s2's own `mark_note` is "Answer". The card tells the student two different things about where the method mark sits. | "Writing 0 straight from the inequality without saying that both outer limits are equal; that equality is what the theorem needs." |
| ts_ipe_m1b_lim_sin_ax_over_x_cos_x | s1_separate | `memory_tip`: "…so create ax below and pay for it with an a outside." "Pay for it" is a metaphor (Rule 41a) and a student reading textbook English has to guess what is being bought. The same card's `insider_note` also opens on "Separate the awkward part from the ordinary one", which names neither part. | "…so create ax below, and put an a outside so the value is unchanged." Insider: "Only sin(ax)/x is indeterminate; cos x is simply 1 at x = 0, so separate the two first." |
| ts_ipe_m1b_lim_x_sin_a_minus_a_sin_x | s2_two_limits | The step's only justification for lim(x → a)(sin x − sin a)/(x − a) = cos a is "the derivative of sin x at x = a". Derivatives are unit 9; a student answering this unit-8 question has not met them, so the load-bearing line quotes a result they cannot yet use. The in-chapter justification (sin x − sin a = 2 cos((x+a)/2) sin((x−a)/2), then sin θ/θ → 1) exists but is buried in `why`. Also s1 `why` says the manipulation "manufactures one factor of (x − a)" — a step of algebra does not manufacture (Rule 41a). | Put the sum-to-product line into `lines[]` as the reason, and keep "this is also the derivative of sin x at x = a" as the side remark. Replace "manufactures" with "produces". |
| ts_ipe_m1b_lim_root_1_plus_x_minus_1_over_x, ts_ipe_m1b_lim_tan_x_minus_a_over_x2_minus_a2 | insider_note, s2_standard_limit (`memory_tip`) | Two unresolvable cross-card references in student-facing prose: "this result 1/2 is quoted as a standard limit elsewhere in the chapter", and "Compare it with the x → 1 case, where the leftover is 2 and the answer is 1/2". A student reading one card has no "elsewhere" and no "x → 1 case" in front of them, and the chatbot speaks both lines aloud. | Name the mathematics: "…and 1/2 is worth remembering as a standard result: (√(1+x) − 1)/x → 1/2 as x → 0." / "The leftover factor at the point is x + a = 2a, so the answer is its reciprocal, 1/(2a)." |

## Cards checked clean

ts_ipe_m1b_lim_floor_x_plus_x_left, ts_ipe_m1b_lim_log_1_plus_5x_over_x, ts_ipe_m1b_lim_root_a_plus_2x_minus_root_3x, ts_ipe_m1b_lim_root_x_plus_1_minus_root_x, ts_ipe_m1b_lim_sin_a_plus_bx_minus_sin_a_minus_bx, ts_ipe_m1b_lim_sin_pi_cos2_x_over_x2, ts_ipe_m1b_lim_sin_x_minus_1_over_x2_minus_1, ts_ipe_m1b_lim_two_abs_x_over_x_plus_x_plus_1_right, ts_ipe_m1b_lim_x2_minus_8x_plus_15_over_x2_minus_9, ts_ipe_m1b_lim_x2_minus_sin_x_over_x2_minus_2, ts_ipe_m1b_lim_x2_plus_5x_plus_2_over_2x2_minus_5x_plus_1, ts_ipe_m1b_lim_x_minus_2_over_x3_minus_8, ts_ipe_m1b_lim_x_minus_3_over_root_x2_minus_9

(13 clean. The 8 cards with findings are named in the tables above; six of them carry a WEAK prose
finding only and their mathematics is sound.)

## Notes

- **Mark structure: clean on all 21.** `steps[].marks` sums to `marks_total` on every card (2 = 1+1
  throughout), every `mark_split[]` entry agrees with its step in both label and marks, and every
  `boxed_final` step carries a boxed line matching the "Box …" instruction in its `margin_note`.
  `mark_note` is "Method" / "Answer" on every step, matching the convention of the neighbouring
  groups, and each does describe the work its step actually does.
- **Boilerplate risk (defect class 3) was checked mechanically and came back nearly empty.** Only one
  prose string is shared across two cards — "Writing 0 above after cancelling and giving the answer
  as 0.", in `one_over_x_minus_2…` and `x_minus_2_over_x3_minus_8` — and it is apt on both, because
  on both the numerator really does reduce to 1 after the cancellation. The near-identical sibling
  pairs (sin/tan over x² − a², the two floor cards, the two limits at infinity of rational functions)
  each carry their own wording; the only cross-contamination found is the misplaced [x]-continuity
  mistake on the right-hand floor card, filed above.
- **No source-book numbering leaked into student-facing prose.** The only cross-references found are
  the vague ones filed under WEAK ("elsewhere in the chapter", "the x → 1 case", "the simpler tangent
  question", "a form the chapter can handle"); no card names a unit, chapter number, page or answer
  number outside `verification.note`.
- **Every numeric check quoted in a `why` was recomputed** and all but one match the digits printed:
  3.241814 (sin(a+bx) card), 2.99999997 → 3.0000 (sin ax / x cos x), 3.1415916 → 3.14159
  (sin(π cos²x)), 0.49999750 (sin(x−1)/(x²−1)), 0.24999938 → 0.2499994 (tan card), 0.49998750 →
  0.49999 (√(1+x) card), 2.6013319 → 2.60133 (x sin a − a sin x). The single mismatch is the
  0.000625 filed under WRONG.
- **Standard limits were checked against the argument each card actually has**, not the one it names:
  log(1 + 5x)/x correctly forces the denominator to 5x (and the card states that log means logₑ and
  that the standard limit fails in base 10); sin(ax)/(x cos x) and sin(a + bx) − sin(a − bx) both
  create their own angle below; sin(π cos²x)/x² correctly uses the standard limit TWICE (once with
  θ = π sin²x, once as (sin x / x)²) and sin(π − θ) = sin θ is the right reduction.
- **One presentational inconsistency, not filed as a finding:** `x2_minus_sin_x_over_x2_minus_2`
  divides by x² without first naming the ∞/∞ form, while its sibling
  `x2_plus_5x_plus_2_over_2x2_minus_5x_plus_1` names it explicitly and warns in `margin_note` that
  "writing ∞/∞ on its own earns nothing". Naming the form on the first card would match the pair.
- Stale ordinal `margin_note` openings ("First mark." / "Second mark.") are present on all 42 steps
  and are NOT reported here — already swept in `ORDINAL_MARK_NOTES.md`.
