# A02 — Maths-1A · Unit 2 Functions — examiner accuracy pass

Cards audited: 26 / 26   Findings: HARMFUL 0 · WRONG 5 · WEAK 21

Every answer was re-derived independently from `question_text` before the card's own working was
read. Every domain was tested at its boundary and at an excluded point; both inverse compositions
were checked on the stated domain; every composite given as a finite set was evaluated pair by
pair; every mark sum was recomputed mechanically (`steps[].marks` and `mark_split[]` both equal
`marks_total` on all 26 — no mark defects found).

**No card in this group states a wrong final answer.** All ten domains, all four inverses, both
set-operation cards, both surjection cards, the composite value and all six proofs are
mathematically correct as written. The findings below are wrong *instructions* and wrong *warnings*
sitting beside correct working, one under-scoped question, and register.

---

## HARMFUL — wrong mathematics a student would copy into an exam

None. No `lines[]` block in this group contains an incorrect value, interval, inverse or proof step.

---

## WRONG — incorrect but not exam-costing

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| `ts_ipe_m1a_fn_domain_rational_three_factors` | s2_domain | `margin_note` says "Write it in **interval form**, and check every bracket." The answer is `R − {1, 2, 3}`, which is not an interval — and the card's own `insider_note` says "writing an interval answers a different question." The margin note instructs the exact error the insider note warns against. | "The second mark is the answer set. Write it as R minus the excluded points — this one is not an interval." |
| `ts_ipe_m1a_fn_domain_rational_x2_minus_1_x_plus_3` | s2_domain | Same boilerplate `margin_note` ("Write it in interval form") on an answer that is `R − {1, −1, −3}`; the step's own `why` directly contradicts it — "The answer is written as the whole real line with a finite set removed, **not as intervals**." | Same fix: name the answer as R minus a finite set, not an interval. The boilerplate was cloned from the eight root/log cards without being re-read against a rational card. |
| `ts_ipe_m1a_fn_inverse_5x_plus_4` | insider_note | "swapping the letter only at the very end — swapping too early is what produces 5y + 4 instead of (x − 4)/5." Interchanging x and y **first** (y = 5x + 4 → x = 5y + 4 → y = (x − 4)/5) is the standard route taught in most texts and reaches the same correct answer. The note marks a valid method as the cause of an error; the real error is stopping before solving. | "Either route works — solve for x then rename, or interchange x and y first and then solve. What loses the mark is stopping at x = 5y + 4 without making y the subject." |
| `ts_ipe_m1a_fn_domain_one_over_root_x2_minus_a2` | s1_condition / s2_domain | The solution writes `(−∞, −a) ∪ (a, ∞)`, which is only correct when a > 0, and never says so — while the step's own `common_mistakes` lists "**Assuming a is positive without saying so**" as a student error. The card commits the very slip it warns about; for a < 0 the stated interval is empty-side-swapped and wrong. | Add "(taking a > 0)" to the condition line, or give the sign-free form `x < −|a| or x > |a|`, i.e. `(−∞, −\|a\|) ∪ (\|a\|, ∞)`. Then the `common_mistakes` entry is consistent with the working. |
| `ts_ipe_m1a_fn_domain_root_9_minus_x2` | (whole card) | **Known issue confirmed.** The card asks and answers the domain only. The source question of this shape is routinely set as "find the domain **and range**", and the range [0, 3] appears nowhere — a grep of the whole bank finds no Maths-1A Functions card that asks for a range of a function at all (`surjection_range_*` use the word only in prose about B = f(A)). A student who prepares from this card and meets the two-part version writes half the answer. | Extend the card to "Find the domain and range of √(9 − x²)" with a third mark-bearing line — domain [−3, 3], and range [0, 3] because the square root is non-negative and 9 − x² peaks at 9 when x = 0. Or add a sibling range card to the unit. |

---

## WEAK — quality, register, Rule 41

| question_id | step_id | what is wrong | what it should say |
|---|---|---|---|
| `ts_ipe_m1a_fn_domain_one_over_root_x2_minus_a2` | s2_domain | `why` ends "— the difference from **question 95.1**, where the root stood alone." That is the source book's internal numbering. A student reading this card in the app has no question 95.1 and cannot resolve the reference. | "— the difference from the plain √(x² − a²) version, where the root stands alone and zero is allowed." |
| `ts_ipe_m1a_fn_domain_root_9_minus_x2` | insider_note | "The same shape as **the 16 − x² question**" — the bank has no fixed reading order, so "the … question" points at nothing the reader can open. | Name the mathematics, not a neighbouring card: "Same shape as any a² − x² root: the answer is the closed interval between the roots." |
| `ts_ipe_m1a_fn_domain_root_x2_minus_25` | insider_note | "That sign flip is the whole difference from **the 16 − x² question**." Same unresolvable cross-reference. | "The sign of x² is the whole difference: 16 − x² gives the inside interval, x² − 25 gives the two outside rays." |
| `ts_ipe_m1a_fn_inverse_ax_plus_b` | s2 | `why`: "This is the general form of **the previous question**." There is no previous question for a student who opens this card directly. | "This is the general form of the numerical linear case f(x) = 5x + 4." |
| `ts_ipe_m1a_fn_surjection_range_cos` | s2 | `why`: "Every value is different here, so **unlike the previous question** nothing collapses". Same problem, and "collapses" is loose. | "Every cosine value here is different, so no two elements of A share an image and B has all five values." |
| `ts_ipe_m1a_fn_set_two_f_and_f_squared` | s1 | `common_mistakes[0]`: "Adding 2 instead of multiplying, **which is the previous question**." The mistake is real; the trailing cross-reference is not resolvable. | "Adding 2 instead of multiplying — that gives 2 + f, a different operation." |
| `ts_ipe_m1a_fn_domain_rational_x2_minus_1_x_plus_3` | s1_condition | Rule 41: `why` says "A rational function is defined everywhere its denominator **is alive**." Personification, and it is also imprecise. | "A rational function is defined everywhere its denominator is non-zero." |
| `ts_ipe_m1a_fn_domain_rational_x2_minus_1_x_plus_3` | insider_note | Rule 41: "or −1 is **quietly missed**" — personifies the omission. | "or −1 is missed." |
| `ts_ipe_m1a_fn_domain_one_over_root_x2_minus_a2` | s1_condition | Rule 41: `common_mistakes[0]` — "which wrongly includes ±a where the function **blows up**." Idiom. | "…where the denominator is zero and the function is undefined." |
| `ts_ipe_m1a_fn_domain_one_over_root_1_minus_x2` | s1_condition | Rule 41 / precision: `memory_tip` — "zero would **divide by nothing**." Dividing by zero is not dividing by nothing, and this is the one line the student reads on the chip. | "A root in a denominator needs > 0, not ≥ 0 — at zero the fraction is undefined." |
| `ts_ipe_m1a_fn_gof_bijective` | s2_one_one | Rule 41: `memory_tip` and `common_mistakes[0]` both say "**peel** g off first". Metaphor, on the chip a student reads. | "g is the outer function, so remove g first (using g one one), then f." |
| `ts_ipe_m1a_fn_gof_bijective` | s3_onto | Rule 41: `margin_note` and `memory_tip` both say "**walk backwards**" from c. | "Start from any c in C and go back one step at a time: g onto gives b, then f onto gives a." |
| `ts_ipe_m1a_fn_gof_inverse_reverse` | s3_values | Rule 41: `why` — "Applying f⁻¹og⁻¹ to c **walks backwards along the same path that f then g walked forwards**." Personification of the functions plus a metaphor. | "Applying f⁻¹og⁻¹ to c undoes g first and then f, which is the reverse of the order f then g was applied — that is why the order reverses." |
| `ts_ipe_m1a_fn_domain_root_9_minus_x2` | s1_condition | `common_mistakes[1]` — "Writing the answer as (−3, 3) with round brackets" — is a mistake about the **final interval**, not about the condition step it sits on, and it duplicates s2's `common_mistakes[1]` ("Using round brackets where the endpoints are allowed"). | Give s1 a condition-step mistake instead, e.g. "Writing 9 − x² > 0 as if the root were in a denominator, which loses x = ±3." |
| `ts_ipe_m1a_fn_domain_log_x2_minus_4x_plus_3` | s1_condition | `common_mistakes[1]` — "Factorising to (x − 1)(x − 4) and **moving the second root**" — is unclear. (x − 1)(x − 4) is not a factorisation of x² − 4x + 3 at all (it expands to x² − 5x + 4), and "moving the second root" explains nothing. | "Factorising as (x − 1)(x − 4), taking the 4 from the −4x term — check by expanding: the constant must come to 3, so the roots are 1 and 3." |
| `ts_ipe_m1a_fn_domain_rational_three_factors` | s1_condition | `margin_note` says "Write it before any **factorising**", but this denominator arrives already factorised and the step's own `why` says exactly that ("the denominator is already factorised, so the excluded points can be read straight off it"). Cloned boilerplate contradicting its own step. | "One of the two marks is for the condition alone. The denominator is already factorised, so read the three excluded points straight off it." |
| `ts_ipe_m1a_fn_domain_root_x2_minus_3x_plus_2` | s1_condition | `common_mistakes[0]` — "Not factorising, so there is nothing to reason about" — is a restatement of the step, not a student error, and it implies factorising is the only route (completing the square, x² − 3x + 2 = (x − 1.5)² − 0.25 ≥ 0, is equally valid). | "Solving x² − 3x + 2 ≥ 0 by testing only one point, so one of the two pieces of the answer is lost." |
| `ts_ipe_m1a_fn_set_two_plus_f_and_root_f` | s2 | The working writes `√f = {(1,√2), (2,√−3), (3,√−1)}` as an equality before dropping two pairs, so for one line the card asserts that √f contains non-real entries — the exact thing the card is set to catch. | Write it as a test, not an equality: "√2 is real; √(−3) and √(−1) are not, so only the pair at 1 survives. ∴ √f = {(1, √2)}." |
| `ts_ipe_m1a_fn_f_finverse_identity` | s3_left | The line `= f⁻¹(b) (∵ f(a) = b)` introduces b without ever declaring it in part (ii) — the part opens "For a ∈ A" only. A student copying this hands the examiner an undefined symbol. | Open the substitution with "For a ∈ A, let f(a) = b ∈ B", then the bracketed reason is justified. |
| `ts_ipe_m1a_fn_identity_composition` | s3_left_identity | Same gap: `= f(a) (∵ I_B(b) = b)` cites I_B(b) = b while the element on the page is f(a), and b is never introduced in part (ii). | "= f(a)  (∵ I_B(y) = y for every y ∈ B, and f(a) ∈ B)". |
| `ts_ipe_m1a_fn_sqp_bijection_inverse` | verification.note | The note contradicts itself on provenance inside one field: "the heading reads 'FUNCTIONS LAQ', so **the mark cut here IS the book's, not ours**" and then "⚠ **THE MARK SPLIT IS OURS, NOT THE BOOK'S** — the bank prints no [N Marks] tags anywhere." A future reviewer cannot tell which claim to trust. | Keep the second claim and reword the first to say only what it means — the book supplies the SECTION (LAQ), not the mark split. |
| `ts_ipe_m1a_fn_sqp_seven_operations` | verification.note | Same self-contradiction, same two sentences ("so the mark cut is the book's" … "⚠ THE MARK SPLIT IS OURS"), plus "the seven parts map naturally to seven marks" left standing beside an 8-mark re-cut card. | Same fix; and state the 8-mark cut in the same breath as the seven parts, so the sentence is not read as the current split. |

---

## Cards checked clean

`ts_ipe_m1a_fn_domain_root_16_minus_x2`, `ts_ipe_m1a_fn_domain_root_4x_minus_x2`,
`ts_ipe_m1a_fn_fog_at_2`, `ts_ipe_m1a_fn_gof_fog_identity`, `ts_ipe_m1a_fn_inverse_5_power_x`,
`ts_ipe_m1a_fn_inverse_log2_x`, `ts_ipe_m1a_fn_surjection_range_quadratic`

(The other 19 cards are all mathematically correct too — they carry only the instruction, warning,
cross-reference and register defects tabled above.)

---

## Notes

**What was re-derived and agreed.** log(x²−4x+3) → (−∞,1)∪(3,∞); 1/√(1−x²) → (−1,1);
1/√(x²−a²) → outside ±a (for a > 0); (2x²−5x+7)/((x−1)(x−2)(x−3)) → R−{1,2,3};
1/((x²−1)(x+3)) → R−{1,−1,−3}; √(16−x²) → [−4,4]; √(4x−x²) → [0,4]; √(9−x²) → [−3,3];
√(x²−25) → (−∞,−5]∪[5,∞); √(x²−3x+2) → (−∞,1]∪[2,∞). Inverses: 5ˣ → log₅x; 5x+4 → (x−4)/5;
ax+b → (x−b)/a; log₂x → 2ˣ — each verified by composing both ways on the stated domain.
(fog)(2) = f(5) = 14, and the insider note's contrast value gof(2) = g(5) = 26 is also correct.
2f, f², 2+f, √f on {(1,2),(2,−3),(3,−1)} all correct, including the domain shrink of √f.
Both surjection cards correct, including the collapse {3,1,1,3,7} → {1,3,7}. All six proofs
(fof⁻¹ = I_B / f⁻¹of = I_A, gof bijective, (gof)⁻¹ = f⁻¹og⁻¹, foI_A = f = I_Bof, the
gof = I_A ∧ fog = I_B converse, and the seven set operations) are logically complete and correctly
ordered — including the two places the order genuinely matters and the card gets it right:
g is undone before f in the reverse law, and it is **fog**, not gof, that supplies "f is onto"
in `sqp_bijection_inverse`.

**The insider-note check.** No `insider_note` in this group claims exam history the card does not
carry, and only one is mathematically off (`inverse_5x_plus_4`, tabled under WRONG). The
`set_two_f_and_f_squared` note's claim that 2f and f² agree only where the image is 0 or 2 is
correct (2y = y² ⇔ y ∈ {0, 2}).

**One defect class, cloned.** The two rational-domain cards and the "before any factorising" note
are the same failure: the ten domain VSAQs share one pair of `margin_note` strings, written for the
root/log shape, and never re-read against the two cards where the answer is a set rather than an
interval. A mechanical check is possible — if a card's final `lines[]` contain `R −` or `R -`, its
margin notes must not say "interval form".

**Stale but not tabled.** `ts_ipe_m1a_fn_gof_bijective`'s `verification.note` still states in the
present tense "Maths-1A Section C runs Q18-Q24 at **7 marks each**", then adds the 8-mark re-cut in
the next sentence. Metadata only, and the card itself is correctly cut to 8, so it is recorded here
rather than as a finding — but the same sentence is likely cloned across the Maths-1A LAQ fleet.

**Not re-reported.** No card in this group appears in
`docs/reports/maths_audit/ORDINAL_MARK_NOTES.md`; the ordinal margin notes that do appear here
("Fifth mark", "The last three marks", "Four of the eight marks") were each checked against the
running mark total and are all correct.

**Not verifiable from here.** Whether the source book prints "domain and range" on the √(9−x²)
question, and whether the 3+5 (one-one / onto) and 2+2+1+3 splits match a real TGBIE scheme. Both
remain `needs_teacher_verification: true`, correctly.
