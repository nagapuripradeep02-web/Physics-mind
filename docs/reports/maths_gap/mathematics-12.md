# Maths-1A Unit 12 — Properties of Triangles — coverage vs Sri Chaitanya FAST TRACK

Book questions indexed: 32   |   Our cards in this unit: 23
MATCHED 17 · MISSING 15 · ELSEWHERE 0 · PARTIAL 0 · EXTRA 6

(The book prints no LAQ block for this chapter — every item is filed as VSAQ or SAQ. Our bank
files some of these same identities as "laq" refs (`ref` prefix `laq`, `section: "LAQ"` or
`"SAQ"` per card) — a book VSAQ/SAQ landing on one of our `laq*`-ref cards is still counted
MATCHED, not ELSEWHERE, since both sides are within this same unit's own question list; only the
`ref` labeling differs, not the unit.)

## MISSING — the authoring queue (15)

| ref | section | stars | stem | note |
|---|---|---|---|---|
| vsaq3 | VSAQ | 0 | Show that in △ABC, tan((B − C)/2) = ((b − c)/(b + c)) cot(A/2). | Napier's analogy, no card |
| vsaq5i | VSAQ | 3 | In △ABC, prove that tan(A/2) + tan(B/2) + tan(C/2) = (bc + ca + ab − s²)/Δ. | book splits this from vsaq5ii (cot-half version, matched to `ts_ipe_m1a_pt_saq_cot_half_sum`) as a separate result; the tan-half identity has no card |
| vsaq9 | VSAQ | 0 | Show that in △ABC, a = b cos C + c cos B. | projection formula, no card |
| vsaq11 | VSAQ | 1 | In △ABC, show that (b − c)² cos²(A/2) + (b + c)² sin²(A/2) = a². | no card |
| vsaq12 | VSAQ | 1 | In △ABC, if cot(A/2), cot(B/2), cot(C/2) are in A.P., prove that a, b, c are in A.P. | no card |
| vsaq13 | VSAQ | 1 | If A, A₁, A₂, A₃ are the areas of the incircle and the three excircles of a triangle respectively, prove that 1/√A₁ + 1/√A₂ + 1/√A₃ = 1/√A. | no card |
| vsaq14 | VSAQ | 0 | If cos A + cos B + cos C = 3/2, show that the triangle is equilateral. | no card |
| vsaq15 | VSAQ | 0 | If tan(A/2) = 5/6 and tan(C/2) = 2/5, determine the relation between a, b and c. | no card |
| vsaq16 | VSAQ | 0 | If b + c = 3a, find the value of cot(B/2) cot(C/2). | no card |
| saq3II | SAQ | 1 | If p₁, p₂, p₃ are the altitudes of △ABC, show that 1/p₁² + 1/p₂² + 1/p₃² = (cot A + cot B + cot C)/Δ. | reuses the same p₁,p₂,p₃ as saq3I (matched to `ts_ipe_m1a_pt_altitudes`, which covers its own three sum-form identities) but this squared-reciprocal identity itself has no card; book's own note flags a print defect on the instruction wording ("centers of" for "lengths of"), unrelated to the missing coverage |
| saq5 | SAQ | 3 | In △ABC, show that cos A + cos B + cos C = 1 + r/R. | no card |
| saq8 | SAQ | 3 | In △ABC, show that r + r₁ + r₂ − r₃ = 4R cos C. | same method as `ts_ipe_m1a_pt_saq_r_r3_r1_minus_r2` (matched to saq7's cos-B cyclic variant), different cyclic combination |
| saq9ii | SAQ | 1 | In △ABC, if r₁ + r₂ = r₃ − r, show that C = 90°. | no card (book's own saq9 part i is not printed on the scanned page, so only ii exists to index) |
| saq10 | SAQ | 1 | In △ABC, if a² + b² + c² = 8R², prove that the triangle is right angled. | no card |
| saq11 | SAQ | 0 | Prove that r₁(r₂ + r₃)/√(r₁r₂ + r₂r₃ + r₃r₁) = a. | no card; distinct from our EXTRA card `ts_ipe_m1a_pt_saq_four_r1r2_sum` (4(r₁r₂+r₂r₃+r₃r₁)=(a+b+c)²) — related r₁r₂r₃ family but a different identity, not a match |

## PARTIAL (0)

None found. Every card that overlaps a book question covers it fully (MATCHED); every book
question with no card is a separate, distinctly-numbered item (MISSING).

## ELSEWHERE (0)

None found. This chapter has no sibling "triangles" unit in the given file set to cross-check
against, and no MISSING item above resembles a topic that would plausibly live in a different
unit (all are Properties-of-Triangles-specific identities involving r, r₁, r₂, r₃, R, Δ, s, or
half-angle/full-angle trig of the triangle's own angles).

## MATCHED (17)

| ref | question_id |
|---|---|
| vsaq1 | ts_ipe_m1a_pt_saq_c_is_60 |
| vsaq2i | ts_ipe_m1a_pt_a_eq_b_minus_c_sec |
| vsaq2ii | ts_ipe_m1a_pt_a_eq_b_plus_c_cos |
| vsaq2iii | ts_ipe_m1a_pt_sin_theta_a_over_b_plus_c |
| vsaq4 | ts_ipe_m1a_pt_ratio_789 |
| vsaq5ii | ts_ipe_m1a_pt_saq_cot_half_sum |
| vsaq6 | ts_ipe_m1a_pt_saq_cot_sum |
| vsaq7 | ts_ipe_m1a_pt_saq_cos_over_a |
| vsaq8 | ts_ipe_m1a_pt_saq_reciprocal_squares |
| vsaq10 | ts_ipe_m1a_pt_saq_a_sq_cot |
| saq1 | ts_ipe_m1a_pt_13_14_15 |
| saq2i | ts_ipe_m1a_pt_prove_345 |
| saq2ii | ts_ipe_m1a_pt_find_abc_from_r |
| saq3I | ts_ipe_m1a_pt_altitudes |
| saq4 | ts_ipe_m1a_pt_cot_half_ratio |
| saq6 | ts_ipe_m1a_pt_saq_r1_r2_r3_minus_r |
| saq7 | ts_ipe_m1a_pt_saq_r_r3_r1_minus_r2 |

## EXTRA — our cards with no book counterpart (6)

| question_id | one-line stem |
|---|---|
| ts_ipe_m1a_pt_a_cos_sq_half | a cos²(A/2) + b cos²(B/2) + c cos²(C/2) = s + Δ/R. |
| ts_ipe_m1a_pt_r1_over_bc | r₁/(bc) + r₂/(ca) + r₃/(ab) = 1/r − 1/(2R). |
| ts_ipe_m1a_pt_saq_cot_ratio_357 | cot(A/2):cot(B/2):cot(C/2) = 3:5:7 — show a:b:c = 6:5:4. |
| ts_ipe_m1a_pt_saq_cos_ab_ratio | (1+cos(A−B)cosC)/(1+cos(A−C)cosB) = (a²+b²)/(a²+c²). |
| ts_ipe_m1a_pt_saq_four_r1r2_sum | Prove 4(r₁r₂ + r₂r₃ + r₃r₁) = (a + b + c)². |
| ts_ipe_m1a_pt_saq_sec_squared_equal | (r₁+r₂)sec²(C/2) = (r₂+r₃)sec²(A/2) = (r₃+r₁)sec²(B/2). |

## Notes

- **`ref` prefix is not a section signal in this unit's card set.** Our bank labels 11 of the 23
  cards with `ref: "laqN"` even though their own `section` field reads `"VSAQ"`, `"SAQ"`, or
  `"LAQ"` inconsistently with the `ref` prefix (e.g. `laq4` carries `"section": "VSAQ"`,
  `laq8`/`laq9` carry `"section": "LAQ"`). The book itself prints no LAQ block for this chapter.
  Matching was done on mathematics + `question_id`, never on the `ref`/`section` label, per the
  task's instruction to match on the mathematics.
- **The two split-part book questions (`vsaq5i`/`vsaq5ii`, `saq3I`/`saq3II`) are genuinely two
  different identities each, not one question with a partial answer.** `vsaq5ii` and `saq3I` are
  each fully matched by an existing card; `vsaq5i` and `saq3II` are each a separate, unauthored
  identity, so both land in MISSING rather than PARTIAL.
- **The `r₁,r₂,r₃`-cyclic identity family (saq6/saq7/saq8) is drilled three ways in the book, only
  two of which have cards.** saq6 (`r₁+r₂+r₃−r=4R`) and saq7 (`r+r₃+r₁−r₂=4R cos B`) are both
  matched; saq8, the cos C cyclic variant (`r+r₁+r₂−r₃=4R cos C`), has no card — left MISSING with
  a cross-reference to the nearest matched sibling, per the task's rule that a same-method,
  different-configuration item is not a match.
- **`saq11`'s r₁r₂r₃ identity is a near neighbor of an EXTRA card, not a match.** Book's saq11
  (`r₁(r₂+r₃)/√(r₁r₂+r₂r₃+r₃r₁) = a`) and our EXTRA card `ts_ipe_m1a_pt_saq_four_r1r2_sum`
  (`4(r₁r₂+r₂r₃+r₃r₁) = (a+b+c)²`) both manipulate the same three products r₁r₂, r₂r₃, r₃r₁, but
  prove different results by a different route — not the same question, so saq11 stays MISSING
  and the EXTRA card stays EXTRA rather than being force-matched.
- **The 6 EXTRA cards** carry no counterpart anywhere in this scanned book chapter — consistent
  with the sibling units' own findings that this bank's vintage draws from a wider drill set than
  any single scanned FAST TRACK chapter.
- No sibling "Properties of Triangles" unit file was supplied to cross-check ELSEWHERE against;
  every MISSING item is squarely a triangle-trigonometric-identity topic with no plausible home in
  another Maths-1A/1B unit, so ELSEWHERE is reported as 0 rather than left unchecked.
