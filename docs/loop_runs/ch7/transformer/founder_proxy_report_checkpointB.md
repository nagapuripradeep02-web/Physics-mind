# FOUNDER-PROXY — Checkpoint B (build gate) — `transformer` (Ch.7 §7.9, concept 8/8, CHAPTER CLOSER) — cycle 0

> Persisted by the orchestrator (§0.2 artifacts-are-files). founder-proxy is report-only and does not
> write repo files; it returned this inline and the text below is its report, transcribed without edit
> of substance. It edited no files, applied no SQL, dispatched no agents.

## VERDICT: **FIX** — two Rule-34 legibility findings routed (one engine, one authoring). Not APPROVE.

The physics is excellent and complete across all 11 states — independently re-verified the load-bearing
numerals (S3 Iₚ = 10.0/3.0 = 3.33 A DC with Vₛ=Iₛ=0.00 and no Φ numeral; S6 ratio-2 lock Pₚ=Pₛ=16.0 W
with Iₚ=1.60 doubling Iₛ=0.80; S8 ledger 16.0+0.8=16.8, η=95%; S7 loss 3.200→0.032 ×100 at 3dp), and
the two NCERT misconception pivots (S3 DC-dead, S6 free-power) land as clean wrong-consequence-first
contrast beats. **No physics doubt → no escalation.** But this build is not yet its highest-value
version: the explore state's single formula surface is clipped behind the 9-row HUD (S11), and the
derivation state renders a dead empty bordered box for its full 22 s (S10). Both are genuine
founder-catch defects; neither is polish worth defending. Per the PRIME DIRECTIVE, the completion pull
of the chapter-closer is exactly the wrong reason to wave them through — both fixes are cheap, and one
re-review cycle buys the highest-value sim. Route both now; re-review at cycle 1.

### The contested question, adjudicated on quality grounds
The S11 collision fix is the **durable engine invariant** (`FIX(engine)` → `peter_parker:renderer_primitives`),
**not** the auditor's one-line HUD-row drop. Three reasons:
1. The engine fix keeps every readout — the authoring row-drop removes the turns readout from the one
   state where a teacher manipulates Nₛ, a real teaching loss.
2. The auditor's blast-radius argument is **factually wrong** — `tfr_formula` is a transformer-scoped
   element with its own inline CSS (`field_3d_renderer.ts:31489-31490`), so repositioning it touches
   only the brand-new `tfr_` scenario, which has **no locked baseline** and is used by one concept;
   capacitance (the regression sample) is a different `scenario_type` and is untouched.
3. HUD rows only ever accumulate toward the explore/final state, so a fixed-offset formula is a latent
   collision class for the whole field_3d fleet — the eye-walker's "position below the HUD's dynamic
   bottom, verified in the max-row state" is a ratchet worth banking.

(If the authoring lever were nonetheless preferred for speed, the auditor picked the *wrong row*: drop
`hud_show_power` — redundant with the on-canvas power-bars pane, a genuine Rule-34 de-dup — never
`hud_show_turns`, which is unique and the exact quantity the sandbox teaches.)

The S10 owner is **`alex:json_author`**, definitively: the renderer gates the band container purely on
`visible_elements` membership (`:31582`), and S10 lists `tfr_band` while `band_content:"none"` — the
toggle exists, so this is authoring, not engine (resolving eye-walker's "ambiguous" flag).

## Pass 1 — scar pre-read (the ratchet): NO recurrences

| Scar class checked | Result on transformer |
|---|---|
| HUD/chrome top-corner collision (Stage 1a, top:52px) | ✓ clear — `tfr_readout` top:52px (`:31479`), sliders bottom-right, band/gauges bottom-left; drive overlayCollisions=[] |
| Unauthored bottom-right formula echo (lc F3) | ✓ one formula surface (`tfr_formula`); `#formula_overlay` suppression joined |
| Bar-total label collision (lc F4) | ✓ S8 leak ledger 16.0+0.8=16.8 closes, labels disjoint |
| Glow focal pane-level not per-bar (lc F5) | ✓ S6/S8 `glow_pane_multiplier:true`, whole-pane confirmed |
| Rounding seam vs displayed total (lc F6) | ✓ all work-point values exact at declared precision |
| Ghost-compare slider frozen (lc F1) | ✓ S5 Nₛ ramp thumb-lockstep; drive S5 Nₛ 200→350 moved:true |
| Pivot frozen-frame precedes crossing (lc F2) | ✓ settled pins phase-pinned |
| ASCII-in-renderer-text-paths (34c) | ✓ auditor machine-scan clean; Unicode confirmed on frames (Φ ω ε η ₚ ₛ) |
| Stale caption bleed | ✓ single-latest captions |
| `field3d_dt_accumulated_motion_invisible_to_eye` | ✓ closed-form phase clock; motionProbe bytesEqual:false (explore alive) |

The Stage 8 forward-probe row `field3d_transformer_scenario_binding_invariants` is present and
schema-valid; its 7 invariants all hold on the frames. The two new bug_class names collide with no row
in this run's candidate file.

## Pass 4 — per-state review table (all 11 states)

| state | correct | order_ok | labels | reads_sound_off | clearly_diff | how a teacher points at it | problem | P |
|---|---|---|---|---|---|---|---|---|
| S1 | Y | Y | Y | Y | Y (baseline) | "Two coils, one core, touching nothing — only the flux links them; secondary open, dark" | — | — |
| S2 | Y | Y | Y | Y | Y | "Close the secondary's own switch → the lamp lights with no wire back to the source" | — | — |
| S3 | Y | Y | Y | Y | Y | "Swap to DC: one blip, then dead. Iₚ=3.33 A flows, flux is full — but dΦ/dt=0, so nothing crosses. That's why the grid is AC." | — (two-zeros holds) | — |
| S4 | Y | Y | Y | Y | Y | "Every turn earns the same 0.100 V; 100 turns → 10 V" | — | — |
| S5 | Y | Y | Y | Y | Y | "Crank Nₛ 100→200, watch Vₛ climb 10→20 while the flux never changes" | — (thumb-lockstep confirmed) | — |
| S6 | Y | Y | Y | Y | Y | "The aha: volts doubled, amps halved, Pₚ=Pₛ=16 W dead level — a trade, not a gift" | — | — |
| S7 | Y | Y | Y | Y | Y | "Send 16 W at 20 V → line burns 3.2 W; step up ×10 → loss collapses to 0.032 W (÷100)" | — | — |
| S8 | Y | Y | Y | Y | Y | "Real transformers leak: four named losses, ledger 16.0+0.8=16.8, η=95%" | — | — |
| S9 | Y | Y | Y | Y | Y | "Zoom into the core: solid → wide eddy whirlpools; laminate → slivers" | — | — |
| S10 | Y | Y | Y | Y | Y | "Derive it: ε=−N dΦ/dt on the same Φ → εₛ/εₚ=Nₛ/Nₚ → Vₛ/Vₚ=Nₛ/Nₚ → Iₚ/Iₛ=Nₛ/Nₚ" | **empty bordered band container renders bottom-left for the full 22 s** | **P2** |
| S11 | Y | Y | Y | partial | Y | "Hand it over: drag Nₛ/Vₚ/f/R_load, watch ratio/currents/lamp respond live" | **the one formula surface Vₛ/Vₚ=Nₛ/Nₚ is clipped behind the 9-row HUD (subscripts cut)** | **P2** |

## Findings

**F1 — STATE_11 (explore): formula surface clipped behind the growing HUD — P2 — `FIX(engine)` → `peter_parker:renderer_primitives`**
- Founder voice: "The explore state's formula is tucked half-behind the readout box — fix the layout so it sits below the numbers; don't delete the turns readout to make room."
- Evidence: `STATE_11__frozen.png` (clip); contrast `STATE_6__frozen.png` (7 rows, clears) → collision is the 9-row case only. Config: S11 uniquely sets both `hud_show_turns:true` + `hud_show_power:true`. Renderer: `tfr_formula` fixed `top:40%` (`:31490`) vs `tfr_readout` growing from `top:52px` (`:31479`).
- `founder:drive overlayCollisions:[]` does NOT cover this — its probe is overlay-vs-review-chrome only (confirmed in `manifest.json`).
- Not blocking in the physics sense (the sandbox teaches; the 2:1 ratio is legible from the live meters), but **must land before the concept can APPROVE/seal**. Runs THIS fix cycle alongside F2.

**F2 — STATE_10 (derivation): empty bordered band container rendered — P2 — `FIX` → `alex:json_author`**
- Founder voice: "Why is there an empty black box in the corner of the derivation slide? Kill it."
- Evidence: `STATE_10__frozen.png` + dense `t00000…t22000`. Config: `STATE_10.visible_elements` includes `"tfr_band"` while `band_content:"none"`. Renderer gate at `:31582`; container bg+border at `:31483`.
- Fix: remove `"tfr_band"` from `STATE_10.visible_elements` (zero risk; the derivation chain is `tfr_formula`, retained).

**Not findings (checked and cleared):** the bottom-left dark label strip on the EYE frames
("…Drag to rotate • Scroll to zoom") is the visual_eyes harness footer, not the sim's teaching surface —
no machine evidence of an on-canvas ASCII violation, so no finding (evidence discipline). S11 shows
power in both the HUD and the on-canvas power-bars pane — minor duplication, **P3 optional**.

## Engine queue (the loop dispatches under §3b; founder-proxy routes, never dispatches)

**F1 — `FIX(engine)`, `peter_parker:renderer_primitives`, runs THIS fix cycle (pre-approve; not physics-blocking).**
File `src/lib/renderers/field_3d_renderer.ts`, `tfr_formula` creation (`:31489-31490`) + `applyTransformerState`
(`:31584-31591`). Replace the fixed `top:40%` anchor with a position computed from
`#tfr_readout.getBoundingClientRect().bottom + margin` for the current state (drop the `translateY(-50%)`
centering), so the formula always sits below the actual HUD bottom regardless of row count. Verify probe:
at STATE_11, `#tfr_formula` must not vertically overlap `#tfr_readout`. Scoped to `tfr_` only → no
sealed-scenario regression; capacitance sample unaffected. On 2-attempt failure → degrades to the
founder's chapter-end engine queue (F2 still lands, but the concept parks, since F1 is a pre-seal fix).

## Candidate scar rows
Both appended by the orchestrator to `docs/loop_runs/ch7/_engine/scar_candidates.sql` (FILE only, trial
forbids DB writes): `explore_state_formula_surface_clips_behind_growing_hud_panel` (MODERATE,
peter_parker:renderer_primitives) and `derivation_state_leaves_empty_graph_panel_container_rendered`
(MODERATE, alex:json_author). Schema-checked; no bug_class collision.

## ≤5 key images for founder eyes
1. `.visual_runs/transformer/20260724-212106/STATE_11__frozen.png` — **F1**: `Vₛ/Vₚ=Nₛ/Nₚ` clipped behind the 9-row explore HUD.
2. `.visual_runs/transformer/20260724-212106/STATE_10__frozen.png` — **F2**: the empty dead bordered box, bottom-left.
3. `.visual_runs/transformer/20260724-212106/STATE_6__frozen.png` — the PRIMARY aha reads perfectly (7 rows, formula clears, ghost struck, level 16.0 W bars).
4. `.visual_runs/transformer/20260724-212106/STATE_3__frozen.png` — the DC-dead pivot: the hardest state to get right, and it reads exactly correct.

## Grade-drift self-review
- Every P1/P2 has founder-verifiable evidence in <1 min (named frozen frame + config/renderer line). ✓
- Each FIX finding names exactly one owner; the `peter_parker:*` finding is in the engine_queue as `FIX(engine)` and NOT in the alex routing; no one was dispatched. ✓
- Engine-vs-content checked against the PRIME DIRECTIVE: explicitly rejected the content workaround (HUD-row drop loses teaching info) in favor of the engine reposition (keeps all readouts) — and disproved the blast-radius objection from renderer source. ✓
- Both scar rows pass schema discipline (`probe_type` js_eval, `row_type` incident, `severity` MODERATE, `ARRAY[…]::text[]`, no NULL, no collision). ✓
- Rule 38 checked in FULL: 38a rings core×8/extended×2(S8,S9)/advanced×1(S10, contiguous before explore) with a coherent ring-cut; 38b explore surfaces CORE-ring formula only; 38c calculus confined to S10; 38d "battery" not "cell"; 38g CBSE `verified:true`, six others `needs_teacher_verification:true`. ✓
- Pass-1 recurrence check actually ran and is listed by class, not asserted. ✓
- Did NOT lower a P1 to reach APPROVE — the verdict is FIX; no APPROVE pressure. eye-walker's "MAJOR" graded P2 (teaching survives via the live meters) with full reasoning, not to reach a pass. ✓

## Trial constraints (binding this verdict)
Report-only: no files edited, no SQL applied, no agents dispatched, no approve/tts/deploy run.
**`visual:approve` is unavailable AND unnecessary** — `transformer` is a brand-new scenario with no
approved baseline, so both fixes break nothing and need no re-baseline; Rule 34e's "expected H2 fail →
re-baseline" does not apply (there is no baseline yet). The auditor's "re-EYE + visual:approve" is a
generic reflex. No TTS, no PILOT_CONCEPTS, no build:pilot/deploy, no merge to master, **no DB writes**.

## Chapter-end packet flags (this is the chapter closer — Ch.7 is 8/8 with this concept)
1. **Two cheap Rule-34 legibility fixes stand between this build and seal** — F1 (engine reposition, `tfr_` only) + F2 (json remove `tfr_band` from S10). Confirm both landed at Checkpoint C; neither touches physics.
2. **Quality-auditor calibration note:** on F1 the auditor recommended a *content workaround* citing a "shared field_3d layout → full regression" blast radius that the renderer source contradicts (`tfr_formula` is `tfr_`-scoped). Worth a founder glance at the auditor's engine-vs-content routing heuristic on new scenarios — it defaulted to the lower-quality authoring fix on a false regression fear.
3. **Latent fleet class banked:** fixed-offset formula vs a monotonically-growing HUD is a field_3d-wide latent collision (F1's prevention rule). The immediate fix is scoped to `tfr_`; the founder may later want it generalized so every scenario's explore/final state is safe by construction.
4. **P3 optional de-dup:** S11 shows power in both the HUD and the on-canvas power-bars pane. If the authoring lever is ever taken for the collision, drop `hud_show_power` — never `hud_show_turns`.
5. **Scar corpus is FILE-only:** Stage 8 forward-probe row + these two new incident rows await apply/edit/discard ruling.
6. **Regression-anchor caveat (inherited):** `faraday_law_induction` has no baseline in this worktree, so `capacitance` (44/44, H2 0.00%) is the sole sound field_3d regression anchor.

## "Is this the highest-value version achievable within loop authority?"
**Not yet — but it's two small, unambiguous steps away, and both are within loop authority.** The
teaching is genuinely strong: eleven distinct, non-repeating motions; both NCERT misconceptions
confronted as contrast beats; the S9 lamination cutaway is the chapter's one true Rule-33 interior; the
grid-transmission payoff (S7) cashes the seed from `ac_power_factor` exactly as designed; every locked
numeral is exact and physics-true. What is missing is legibility polish on precisely two surfaces:
(1) the explore state's formula must sit below the 9-row HUD instead of behind it (F1, engine — the fix
that keeps every readout), and (2) the derivation state must not render a dead empty box (F2, one JSON
line). Land those, re-EYE, re-review at cycle 1 → this becomes the highest-value version and a fitting
close to Ch.7. **Recommendation: route both now; do not seal on the current build.**
