# QUALITY-AUDITOR REPORT — `ac_power_factor` (Ch.7 §7.7, `ac_power` field_3d scenario, 10 states)

## VERDICT: **FAIL** — one LOW-severity finding on STATE_4; every other active gate PASS

Route to **`alex:physics_author`** `[reason: pass-2 / number-lock]`. Narrow, physics-correct-underneath numeric-consistency defect on the pivot state violating the declared A8/F7 displayed-addend closure. Founder may accept as a known rounding wart, but the auditor cannot green-light it unseen.

## The finding — S4 apparent power renders as **5.54 AND 5.55** in the same state (A8/F7 violated)
- Exact renderer arithmetic (work point vm=10, f=0.50, R=5): true `I_rms = 0.784498` → toFixed(3) = **0.784**; true `S = V_rms·I_rms = 5.547241` → toFixed(2) = **5.55**.
- HUD S3–S9 renders `I_rms = 0.784 A` (arithmetically CORRECT 3dp).
- S4 struck-chip reads **`7.07 × 0.784 = 5.54 W?`** (7.07×0.784=5.5429→5.54).
- S4 ratio chip reads **`3.08 / 5.55 = 0.555`**; S8 triangle leg reads **`S = 5.55 VA`**.
- ⇒ apparent power S shown as **5.54** (struck multiply chip) AND **5.55** (ratio chip + S8) — within-state self-contradiction on the misconception-pivot centerpiece.
- **Root cause:** the number-lock (physics_block §4.1 A8; CpA row 2) assumed `I_rms` displays as `0.785`, making 7.07×0.785=5.55 close everywhere. True 0.784498 displays 0.784, and 7.07×0.784=5.54≠5.55. The (non-rendered) JSON annotation `s4_mid_label` hard-codes the intended `"7.07 × 0.785 = 5.55 W?"`, confirming the intended-consistent design the correct 0.784 rounding broke. The "number lock re-derived exact at Checkpoint A" carried a 0.785 double-rounding slip.
- **Why LOW:** physics 100% correct; a 0.01 W artifact; pedagogy (V·I over-predicts 3.08) survives — but a teacher reading "naive 5.54… now divide 3.08 by 5.55" hits an unanswerable contradiction on the flagship pivot.
- **Fix options (physics_author decides; engine implements):** (a) present S as a single value without the literal displayed-addend multiply (e.g. `V_rms·I_rms = 5.55 W?`) — but a teacher can still compute 7.07×0.784=5.54≠5.55 from the HUD; (b) nudge one work-point default so I_rms genuinely displays 0.785 (then everything closes) — preserves the "same number" pedagogy. Correct the 0.785→0.784 slip in physics_block A8 / CpA regardless.

## Per-gate results (evidence-cited) — all PASS except A8
Gate 0 DoD ✓ (10 contiguous; symbol labels all render; S1 & S3 formula_overlay="" deliberate NONE confirmed on-canvas; conceptual-only; assessment+coverage; misconception_watch only S4/S6). Gate 1 tsc ✓ (exit 0). Gate 2 validator ✓ (ac_power_factor PASS zero-warn; 131 PASS/0 FAIL; cross-check ✓). Gate 3a ✓ (advance_mode 9×manual_click+1×interaction_complete; prims≥3; prereqs advisory). Gate 3c N/A. Gate 3d ✓. **Gate 3e ✓ (control-gating verified on-canvas: f-slider ONLY S3, R-slider ONLY S6, all 5 ONLY S10, none on S1/S2/S4/S5/S7/S8/S9; 10 distinct archetypes; S10 interaction_complete).** Gate 3f ✓ (word budget 53–55 guided / 20 explore; ≤5-word delta cues; cause-first sweeps confirmed dense; one glow_focal/state; home-pose persists). Gate 3g ✓ (macro↔representation; per-state real number; ONE Unicode formula surface/state; composed subscripts render correctly NOT literal V_rms; value-only HUD; no collisions). **Gate 3h ✓ (depth rings core×7·extended S8·advanced S9·core-explore S10, advanced contiguous; BOTH ring-cuts coherent — S10 explore CORE-only, NO Q/VAR/triangle/derivation, A2 verified on-canvas; tag honesty CBSE-only verified; ⚙ auto-inherited).** Gate 4 ✓. Gates 5/6 N/A DORMANT. Gate 7 ✓ (0 consoleErrors). Gate 8 ✓ (query no rows; ghost_compare NOT present — slider thumb+label track the scripted sweep, dense proof; the S4 finding is a new F7-family candidate). Gate 9 ✓ (no collisions; EYE 43/43 + drive 0). Gate 10 ✓ (zero {var} leaks). Gate 11 ✓ (plain English; no forbidden notation). Gate 12 ✓ (same apparatus S1→S10; camera fixed). Gate 13 ✓ (all 10 modes render). Gate 14 ✓ (Pass-1; PRIMARY S5 + SUPPORTING S6). Gate 15 ✓ (Pass-2). Gates 16–18 ✓ (2 straightforward pivots S4/S6; ≤1 new var/state; concrete before derivation). Gate 19 ✓ (Q1→S2,Q6→S1,Q2→S4,Q3→S5,Q4→S6,Q5→S7; non_assessed=[S8,S9,S10]; no orphan). Gate 20 ✓ (distractors carry misconceptions; Q3 hits aha S5). Anti-plagiarism ✓ (universal motors/grid/kW-kVA anchor).

## Requested confirmations (all honored except A8)
f→f_demo ✓ (13×, zero bare "f"). S9 in non_assessed ✓ (["STATE_8","STATE_9","STATE_10"]). S5 check in narration ✓ (tts s5_4, no renderer chip). No off-convention notes file ✓. **A2** ✓ (S8/S9 hidden-cut leaves S4 coherent; S10 core-only). **A3** ✓ (narration s6_4 verbatim "Here reactance dominates: at the home frequency, dropping resistance does the opposite"; S10 hud_show_p:true → true 25.0 W at R=2/resonance, live-drag is founder:drive territory). **A5** ✓ (on-canvas "Q = 4.62 VAR" + "reactive"; forbidden tokens ONLY in prohibition metadata line 57, zero rendered/narrated). **A8** ✗ — the finding (3dp honored 0.784, but the promised displayed-addend closure broken).

## Non-blocking advisories
1. S10 explore HUD shows `I_rms sin φ = -0.000 A` (negative-zero at resonance, X≈−0.001). Cosmetic — clamp tiny |X| to `0.000 A`. Owner peter_parker (trivial).
2. concept_panel_config row deferred (trial file-only; ship-time default_panel_count=1, the vestigial-dual-panel WP). N/A-DORMANT; silent narration EXPECTED.
3. Rule 39g ⚙ spot-check — founder/eye-walker eyeball ⚙ on localhost:8087 lists the pwr_ overlays with teacher-readable labels.
4. Wattmeter numeric ("P = 3.08 W") small/faint italic; needle+HUD carry the reading; a larger meter numeric reads better in a classroom.
5. **Design-doc correction owed:** physics_block §4.1 A8 + CpA row 2 state I_rms=0.785; true is 0.784498→0.784. Correct regardless of the S4 fix.

## engine_bug_queue candidate — FILE TEXT ONLY
`bug_class: field3d_struck_chip_displayed_addends_disagree_with_quantity_rounding` · row_type incident · severity MINOR(LOW) · status OPEN · owner_cluster alex:physics_author (number-lock; peter_parker implements) · concepts_affected [ac_power_factor] (STATE_4). Description: S4 renders S as 5.54 (struck chip "7.07×0.784=5.54 W?", displayed-addend product) while the ratio chip + S8 triangle render S=5.55 (true 5.547 → 2dp); true I_rms=0.784498 displays 0.784 not the design-assumed 0.785, so A8/F7 closure is false. Sibling/inverse of `field3d_struck_sum_rounds_full_not_displayed_addends`. prevention_rule: when a chip presents a quantity as the literal product of two DISPLAYED addends, verify that product rounds to the SAME value the quantity shows elsewhere; if not, drop the literal multiply or pick defaults so the addends close. probe_type manual: compare round(V_rms_disp×I_rms_disp,2) vs round(V_rms×I_rms,2) — must be equal to ship a displayed-addend chip.

Evidence frames: STATE_4__frozen.png (struck chip), STATE_3__dense_t01000 + STATE_6__dense_t02000 (slider lockstep + paradox), STATE_8__frozen (S=5.55), STATE_10__frozen (explore core-only).
