# eye-walker report — `ac_power_factor` (Ch.7 §7.7, `ac_power`, 10 states)

> Walked all 216 frames of run `20260724-080840` (10 contact sheets, all 10 frozen, ~35 dense at key timestamps, pixel crops/diffs for ambiguous cases). engine_bug_queue pre-check: 0 open rows for ac_power_factor --field3d.

## ⚠ Orchestrator correction to the deterministic-gate note
eye-walker flagged that `manifest.json` had no `deterministic checks` line and suggested re-running. **The deterministic gate DID run and passed** — the orchestrator's detached-run log shows `📊 43 deterministic checks · 43 passed · 0 failed · 653166ms · ✅ Deterministic gates clean`. The summary goes to stdout (captured in the detached logfile), not manifest.json. No re-run needed; this pixel-content walk is the complementary pass.

## Per-state verdict table
| State | Verdict | Evidence |
|---|---|---|
| S1 the_wattmeter_reads_ten | **PASS** | needle mid-climb P=2.59W (staged not instant), frozen P=10.00W, HUD complete, no formula (deliberate), continuous beads (797px diff t0/t1). Caption matches. |
| S2 the_product_wave | **PASS** | strip (cyan v/amber i in-step) + p-pane (twin-hump, all-positive, touching zero) dock; dashed ⟨p⟩=10.00W; formula `p(t)=v(t)·i(t)` real-Unicode; cursor-walk staged. |
| S3 off_resonance_the_wave_sinks | **CONCERN** | f-glide 0.25→0.50 confirmed (I_rms 1.414→0.784, P 10→3.08); grey "returned" fill present + correctly coloured BUT visually thin/subtle at ±4…21W — the teaching point risks being missed at a glance. No formula (deliberate, correct). |
| S4 volts_times_amps_fails | **CONCERN (numeric) + CONCERN (sequencing)** | Chip reads **"7.07 × 0.784 = 5.54 W?"** — I_rms displays 0.784 (correct 3dp of 0.784488) contradicting the spec's "0.785"/5.55; internal mismatch with S=5.55 VA quoted at S8 for the SAME quantity. Separately: ghost-swing + chip-strike + triangle-dock all already resolved at t≈0 (only ratio_reveal/naming stage ~t4000/t6000) — collapses the 16a "watch the wrong law fail live" beat. Ghost needle IS present (solid cyan, spec said "dashed" — minor). |
| S5 only_the_shadow_works (PRIMARY aha) | **CONCERN (sequencing)** | Split content correct (i∥ coral 0.435A / i⊥ violet 0.653A, rotation continues across t1000/3000/8000); no wrong S4-style chip at S5 (as expected). But fan_dock→rotation_stop→split_par→split_perp→check all resolved at t=0 — same collapse as S4. |
| S6 wattless_current (16a #2 + supporting aha) | **PASS** | R-cycle 5→2→5 EASED not snapped (t1000 R=4.8Ω); paradox numbers exact (I_rms 0.784→0.911, P 3.08→1.66); thumb moves; caption matches. |
| S7 where_the_power_goes | **CONCERN** | 3 gauges present, E_R monotonic ratchet (3.08→52.31J across dense); heater-lands-power intact. Specified close_chip **"0.785²×5.0=3.08 W" never appears** (checked t1000/4000/17000/frozen). Note (not a defect): dense 1s cadence aliases the E_L/E_C breathing period (T/2=1.0s) so breathing can't be confirmed frame-to-frame — a sampling artifact, not a bug. |
| S8 the_power_triangle | **PASS** | detach→morph→relabel→check stages (unlabeled t0–2000 → labeled+chipped t4000); P=3.08W/Q=4.62VAR/S=5.55VA present; `S²=P²+Q²` symbolic-only (correctly never chipped); "reactive power Q (VAR)" dual-labeled once, no "Q factor" anywhere (A5 satisfied). |
| S9 the_average_from_the_algebra | **PASS** | 5-line Cambria chain stages (2@t2000→4@t6000→5@t9000); apparatus+meter+p-pane dimmed (E4); all-Unicode; single glow focal. |
| S10 power_sandbox | **PASS (1 cosmetic)** | 5 sliders labeled; circuit+strip+p-pane+fan+HUD surfaced; formula core-only (no Q/VAR/triangle/derivation leak); caption matches. Cosmetic: HUD `I_rms sin φ = -0.000 A` (negative-zero artifact) instead of `0.000 A` at resonance. |

**Delta-visible (Rule 32):** confirmed — every adjacent pair swaps its panel, no two states alike, no static state.
**A4 colour-contrast:** i∥ `#FF6E40` (coral) vs solid i `#FFB300` (amber) confirmed coded (pixel-probed in STATE_5__frozen); technically distinct hues but spatially adjacent/collinear at fan-disc scale — worth a founder look at real size, not a hard fail.

## Frames for founder eyes (5)
1. `.visual_runs/ac_power_factor/20260724-080840/STATE_4__dense_t00000.png` — S4 ghost/strike/triangle already resolved at entry + the 5.54-vs-5.55 chip mismatch.
2. `.../STATE_5__dense_t00000.png` — same collapsed-cue pattern on the PRIMARY-aha state.
3. `.../STATE_7__dense_t17000.png` — the specified close_chip never renders across the full duration.
4. `.../STATE_3__frozen.png` — negative-lobe legibility (grey fill present but thin).
5. `.../STATE_5__frozen.png` — A4 colour contrast (i∥ coral vs i amber) at real scale.

## Candidate engine_bug_queue rows (FILE TEXT — not inserted)
**(1) `field3d_irms_3dp_true_rounding_breaks_authored_chip_closure` (MAJOR, owner ambiguous — spec/json):** ac_power_factor S4: I_rms displays 0.784 A (correct 3dp rounding of true 0.784488) but the physics_block/skeleton authored the F7 chip-closure assuming 0.785 A. The rendered chip "7.07 × 0.784 = 5.54 W?" disagrees with S=5.55 VA quoted identically at S8 for the SAME quantity S. Root: physics_block "0.78449→0.785" is a DOUBLE-rounding error (0.7844… single-rounds to 0.784; 4th digit 4<5). Renderer correctly displays the true rounding, exposing the spec's arithmetic error as a visible on-canvas inconsistency. Prevention: any authored "F7 chip must close" claim must be re-verified by actually rounding the true value at the stated precision (not hand-copied), and a chip from displayed-rounded operands diffed against the canonical value used elsewhere in the SAME concept for the same symbol before ship.

**(2) `field3d_multi_cue_chain_collapses_to_instant_at_state_entry` (MODERATE, owner alex:json_author):** S4 (ghost_swing + chip_strike + triangle_dock) and S5 (fan_dock + rotation_stop + split_reveal_par + split_reveal_perp + check) render their FULL end-state at t=0 in THE EYE dense capture instead of staging over the authored 0–6s / 0–3.6s windows. Later cues in the SAME states (S4 ratio_reveal/naming) and other states (S1/S2/S6/S8/S9) stage correctly → partial, state-specific collapse, not a universal defect. Suspected: the early cues' `*_at_ms` EYE-arming fallbacks were authored clustered near 0 instead of spread. Prevention: on any 16a "wrong belief first, then struck" or multi-arrow-materialize choreography, dense-capture at t=0/t=1000 must show the FIRST beat only, not the full resolved sequence. **[ORCHESTRATOR NOTE for CpB: THE EYE posts no cue times and uses `*_at_ms` fallbacks; the LIVE player fires scenario_cue on narration. Determine whether the live cue schedule staggers correctly even if the `*_at_ms` fallbacks are clustered — this may be a THE-EYE-fallback artifact, not a live-player defect.]**

**(3) `field3d_specified_chip_cue_never_renders` (MODERATE, owner alex:json_author):** S7: the physics_block choreography specifies a close_chip ("0.785²×5.0=3.08 W — the meter number a third way") firing after ~1 cycle. Checked t1000/4000/17000/frozen (E_R=52.31 J at t17000, many cycles elapsed) — the chip never appears. Root: the close_chip cue appears not wired into the S7 JSON cue list (or scenario_cue/*_at_ms never set). Prevention: cross-check every named cue in the physics_block per-state timeline against the actual JSON cue list before EYE handoff. **[ORCHESTRATOR NOTE for CpB: json_author's own report flagged that the S5 "check chip" is not a real renderer feature and was carried in NARRATION instead — determine whether the S7 close is likewise deliberately in narration (not a defect) or a genuine gap.]**

## Overall read: FINDINGS (3)
One numeric-consistency defect (S4 I_rms rounding vs S=5.55), one sequencing pattern on the two pivot/PRIMARY-aha states (S4/S5 cues resolving instantly — possibly a THE-EYE-fallback artifact), one missing specified chip (S7 — possibly deliberate-narration like S5). 7/10 states clean, all physics numbers cross-checked correct, colour semantics/Unicode/dual-labels/Q-collision-disarm/formula-ring-gating all verified present. No black/blank frames, no crash, no wrong physics. All three merit founder eyes before visual:approve.
