# Founder-proxy — Checkpoint B (Build Gate) — `series_lcr_circuit` (Ch.7 §7.6, `ac_series_lcr`, 11 states)
**Fix cycle 0 (first review). Founder asleep — proceeding autonomously.** Orchestrator-persisted verbatim (verdict + adjudication + routing; the five scar rows are filed in `docs/loop_runs/ch7/_engine/scar_candidates.sql`, severity-corrected per the known `.agents/founder_proxy/CLAUDE.md` enum bug — MINOR→MODERATE against the live CHECK).

## VERDICT: **FIX(engine)** — 3 BLOCKING + 3 RIDE-ALONG, all owner `peter_parker:renderer_primitives`

The two AI reviewers were each half-right, and neither was safe to trust. **eye-walker's CRITICAL (F1) is CONFIRMED and is a genuine core-claim contradiction** — the concept's central new quantity, the net reactance X = X_L − X_C = 7.50 Ω, is **never rendered as a number anywhere it is taught** (S6 triangle, S7 swap, S8 crossing, S11 mini-triangle); the auditor missed it by checking the formula surface `Z=√(R²+(X_L−X_C)²)` but not the triangle leg. **The auditor's F1 (S4 0.01 V seam) is CONFIRMED and eye-walker missed it** (rated S4 "clean"). **eye-walker's MAJOR on S3 (F3) is REFUTED** — the un-staggered fan is a deliberate, JSON-verified glow-tour, exactly as the auditor judged. Both reviewers made both error types; only opening the frames settled it. Because F1/F4 (missing reactance value on the PRIMARY-aha path) and F2 (a P1 **recurrence** of the Stage-1b renderer-ASCII-subscript scar) are present, **the concept cannot approve this cycle**. Run ONE bundled §3b engine dispatch (finding 1 forces it anyway), fix all six real items, then re-drive and re-dispatch me. Every fix is the engine, not a content workaround — I route zero of them to `alex:*`.

Machine corroboration that is clean and must NOT be re-litigated: founder:drive `overlayCollisions:[]`, `consoleErrors:[]`, `pageErrors:[]`, `motionProbe.bytesEqual:false` (explore alive — Rule 37 ✓), all 8 slider drags `moved:true`; THE EYE 47/47; SIM_READY fired on all 11 states (`simTimeMs` monotonic).

## Pass-1 scar recurrence check (ran BEFORE judging pixels)
| Scar class checked | Result |
|---|---|
| `field3d_rms_subscript_ascii_in_renderer_text_paths` (Stage-1b) | **RECURRED → F2** (f_0/v_m ASCII on the new `ac_series_lcr` renderer text paths). Auto-P1. |
| `field3d_new_scenario_needs_deriveStateMeta_reveal_hold` | **RECURRED → F5** (S9 reveal-hold pin lands mid-sequence). |
| `ghost_compare_cause_invisible_slider_frozen` | Satisfied — every slider drag `moved:true`, thumbs track. |
| `field3d_formula_overlay_generic_not_cambria_math` | Satisfied — slcr formula surfaces use `'Cambria Math'` (renderer :28901). |
| `field3d_createtubeline_undefined_field_lines_throws` (Stage-1b CRITICAL) | Satisfied — 0 pageErrors, SIM_READY on all 11 states. |
| particle_field chrome-collision family | N/A (field_3d; `overlayCollisions:[]`). |

## Per-state review table (all 11 states, incl. explore)
| state | correct | labels | reads_sound_off | clearly_diff | problem_or_missing | pri |
|---|---|---|---|---|---|---|
| S1 | Y | ~Y | Y | Y (baseline) | faint stray "AC source" mini-schematic leaks in the reserved-empty band (**F6**) | P3 |
| S2 | Y | Y | Y | Y | none | — |
| S3 | Y | Y | Y | Y | staggered dock replaced by glow-tour — **sound**, not a defect (**F3 REFUTED**) | P3 |
| S4 | Y | Y | Y | Y | struck sum **19.42** vs addends/narration **19.41**; "source v_m" ASCII underscore (**F7 / F2**) | P3 |
| S5 | Y | Y | Y | Y | none (`vₘ²=V_R²+(V_L−V_C)²` Unicode-clean) | — |
| **S6** | Y | **PARTIAL** | **NO** | Y | **violet X-leg (7.50 Ω) UNLABELED; no X_L/X_C chips — taught quantity invisible (F1)** | **P1** |
| **S7** | Y | **PARTIAL** | **NO** | Y | **X-leg still bare; "X chips SWAP 10.00↔2.50" never render (F1)** | **P1** |
| **S8** | Y | **PARTIAL** | ~Y | Y | **"f_0" ASCII underscore (F2); merged "X_L=X_C=5.00 Ω" crossing chip ABSENT (F4)** — PRIMARY aha | **P1** |
| S9 | Y (live) | Y | Y | Y | frozen pin captures transitional **R=7.4/Q=0.7** not settled R=10/Q=0.5 (**F5**); "f_0" (**F2**) | P2/P1 |
| S10 | Y | Y | Y | Y | derivation line 4 **"f_0"** underscore while line 3 "ω₀" correct on SAME surface (**F2**) | P1 |
| S11 | Y | Y | Y | Y | inherits "f_0" (F2); X-leg collapsed (X=0 at resonance, moot); explore CORE-ring (38b ✓); motion alive (37 ✓) | P1 (F2) |

## The 7 contested findings — adjudication
- **F1 — reactance leg X unlabeled + no X_L/X_C chips (S6/S7/S8/S11) — CONFIRM · BLOCKING · engine.** `slcrDrawTriangle` (28880–28904) draws all three leg arrows but `showChips` writes ONLY `"R="` (28902) and `"Z="` (28903) — no `"X="` line. `slcrDrawResoPlot` (28910–28954) draws the crossing marker but no equality chip. json_author correctly enables chips (R & Z render) → pure renderer omission. Narration s6_2 speaks "net reactance seven point five" with nothing on screen. Core claim contradicted → §3b NOW.
- **F2 — `f_0` ASCII underscore across HUD + canvas-graph + derivation — CONFIRM · BLOCKING (P1 recurrence) · engine.** Hardcoded ASCII literals: HUD (29059), canvas plot (28945, 28949), derivation (29102), + S4 "v_m". JSON authored `f₀` correctly everywhere; the renderer-hardcoded prefixes leak. Recurrence of `field3d_rms_subscript_ascii_in_renderer_text_paths` → forbidden to APPROVE with an unresolved Pass-1 recurrence; resolve in the same dispatch.
- **F3 — S3 fan un-staggered — REFUTE (as a defect) · not routed.** Deliberate glow-tour (per-sentence glow i→V_R→V_L→source, JSON 233–236; no dock timing, unlike S1/S5 which prove the machinery exists and was intentionally not used). Rule 32a doesn't bite (five arrows are constant offsets of one θ(t), not independent cause/effect). Reads better than sequential docking. Auditor's judgment concurred. Per-arrow fan docking = a low-value engine feature request, DECLINED.
- **F4 — S8 merged crossing chip absent — CONFIRM (genuinely absent, not a cadence miss) · BLOCKING · bundle with F1.** `slcrDrawResoPlot` has no equality-chip draw anywhere; not a sub-second flash. Same root cause as F1.
- **F5 — S9 reveal-hold pin captures transitional R=7.4/Q=0.7 — CONFIRM · RIDE-ALONG · engine.** `deriveStateMeta` reveal-hold ms < last R-step start+dur. Live teaching settles correct; no `visual:approve` this trial so no baseline harmed. Bundle for a clean re-review.
- **F6 — S1 empty band leaks "AC source" mini-schematic — CONFIRM · RIDE-ALONG · engine.** Band-container draw emits a default AC-source glyph unconditionally. Cosmetic.
- **F7 — S4 struck-sum 19.42 vs 19.41 — CONFIRM (auditor right, eye-walker missed) · RIDE-ALONG · engine.** Engine renders full-precision rounded sum (19.41587→19.42) vs displayed addends (5.55+11.09+2.77=19.41) + narration. Fix: sum the displayed rounded addends.

## The §3b dispatch (ONE bundled dispatch; all owner `peter_parker:renderer_primitives`)
| # | finding | tag | file · region | fix | proof |
|---|---|---|---|---|---|
| F1+F4 | reactance value never rendered | **BLOCKING** | `slcrDrawTriangle` 28900–28904; `slcrDrawResoPlot` 28940–28951 | add X-leg label `X = 7.50 Ω` (violet/green by winner) on S6/S7/S11; add S8 crossing merged `X_L=X_C=5.00 Ω` chip | re-drive: S6/S7 triangle → 3 labeled legs; S8 dense t≈2.9s → equality chip present |
| F2 | `f_0` ASCII underscore (4 paths) | **BLOCKING** | 28945, 28949, 29059, 29102 (+ S4 "v_m") | `"f_0"`→`"f₀"` (U+2080) on HUD+canvas+derivation; sweep ALL renderer text paths (Rule 34c) | THE EYE S8/S9/S10/S11 HUD+plot crop: no underscore; `capacitance` 44/44 incl H2 |
| F5 | reveal-hold mid-sequence | ride-along | `deriveStateMeta` S9 reveal_hold ms | bump > last R-step start+dur (~6–8s) | re-drive S9 frozen → R=10.0Ω, Q=0.5, iₘ=1.00A |
| F6 | empty-band stray schematic | ride-along | band container draw when `visible_elements` omits strip/fan | render nothing in reserved band on S1 | re-drive S1 frozen crop → empty band |
| F7 | struck-sum 19.42 vs 19.41 | ride-along | `ac_series_lcr` S4 `kvl_stack` struck-sum draw | sum displayed rounded addends → 19.41 | S4 frozen crop → "= 19.41 V?" |

Regression duties: `check:renderer-syntax` clean; `tsc` 0; `validate:concepts` PASS; THE EYE `series_lcr_circuit` (new frozen frames expected to change — no locked baseline, not a regression); H2 regression sample `capacitance` 44/44 (faraday has no baseline this worktree). If the 2-attempt budget fails on F1/F4 (blocking), the concept **parks** to the founder's chapter-end engine queue.

## ≤5 key images for the founder
1. `.visual_runs/series_lcr_circuit/20260724-031729/STATE_6__frozen.png` — F1: R and Z legs labeled, violet reactance leg (7.50 Ω) bare.
2. `.../STATE_8__frozen.png` — F2 + F4 on PRIMARY aha: HUD "f_0=0.25 Hz" underscore, no X_L=X_C crossing chip.
3. `.../STATE_10__frozen.png` — F2: derivation line 3 "ω₀" correct Unicode, line 4 "f_0" leaks (same surface).
4. `.../STATE_9__frozen.png` — F5: pin caught R=7.4Ω/Q=0.7 (transitional, not settled R=10/Q=0.5).
5. `.../STATE_4__frozen.png` — F7: struck sum "= 19.42 V?" vs 19.41 addends/narration.

## Orchestrator action
1. Do NOT approve this cycle (blocking F1/F4 core-claim + F2 P1 recurrence).
2. Dispatch ONE `peter_parker:renderer_primitives` §3b engine fix bundling all five scar rows (BLOCKING F1/F4/F2 must land + verify; ride-along F5/F6/F7 in the same pass).
3. On landing: re-run founder:drive + THE EYE, then re-dispatch Checkpoint B cycle 1. Budget: 2 further cycles before ESCALATE.
4. Route NOTHING to `alex:*`; F3 refuted.

**FIX(engine)** — BLOCKING: F1, F2, F4 · RIDE-ALONG: F5, F6, F7 · REFUTED: F3. Shipping remains founder-only (Rule 17); this is an authoring re-review gate, never a deploy trigger.
