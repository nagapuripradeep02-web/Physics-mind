# ARCHITECT SKELETON — `potential_energy_definition` — cycle 1 (Checkpoint-A revision, 2026-08-09)

> Chapter: Class 11 Ch.6 Work, Energy and Power · concept **#6** of 12 (approved teaching order, founder 2026-08-01)
> Renderer: `field_3d` / `newtons_laws_body` + SEAM K/L/M/N energy layer + the 2026-08-08/09 teaching-dwell seam. **0d pure-JSON concept — design target: ZERO renderer edits (Phase-0 alarm rule).** This is the FIRST concept to render the `U_grav` energy bar, the first to pair `energy_layer` + `work_accumulators` where the claim IS their numeric CHANGE-identity (ΔU = −W_c — route (a), cycle-1 coordinated ruling), and the first to author `capture_mode: 'first'` WITH `dwell_ms`.
> Authoritative config contracts: `docs/loop_runs/ch6_state.md` §"SEAM K/L/M/N RESULT" — those REPORTS supersede any literal here. Post-dwell-seam contracts read at source THIS session (line numbers are 2026-08-09 reads, desk `C:\Tutor\physics-mind-ch6-work-energy-power`, branch `feat/ch6-concept-6`).
> Doctrine: Rules 16a · 19 · 23 · 24 · 25 · 31 · 32 · 33 · 34 · 35 · 38 · 41. Conceptual-only (Rule 20 [D]); EPIC-C branches: none.

## CYCLE 1 CHANGE LOG (Checkpoint-A DESIGN_FIX resolved, 2026-08-09)

- **P1-1 / P1-3 / P2-4 — ROUTE (a) ADOPTED** (the dispatching session's coordinated ruling, filed at the end of #7's `founder_proxy_A.md`): ONE reference for all four states at the ramp's foot, `h_ref_m = −3.05`. The taught claim is the Δ-form ONLY (U − 49.0 = −W_gravity; the level never equals −W anywhere); S3 becomes load-bearing (stamps 88.2/127.4 vs −39.2/−78.4 — levels fail, differences work); no reference jump into explore; #7's ground-reference habit intact. No fifth state added (per ruling).
- **P1-2** — S2 names friction's destination in ONE authored clause ("Friction's joules leave the block as heat — a later lesson tracks them."); the (f-2) heat ban lifted for exactly that clause; the counter re-worded to never imply a missing destination.
- **P2-5** — §Arithmetic re-derived from scratch under route (a); every column labeled by its instant; worst-folded (R+50) column retained and populated; S2's R / R+50 column mix and S1's 17.6 → 17.4 (66.4 level) fixed; all values probe-verified by inline computation this session.
- **P2-6** — sandbox friction envelope refiled ACCEPTED-with-bounded-exposure (drag case stated, §0.B); μ sliders capped at 0.5 < tan 30° so no corner sticks (monotonicity argument corrected).
- **P2-7** — q2 re-designed as a sign-of-change question grounded in S1's descent + new (f-3) S1 descent narration duty. No state added.
- **P3-8** — multibody N/A now quotes the deciding reader (`nlbBodyLaneZ` L44503–44508); all other N/As re-audited for the same substitution (none found).
- **P3-9** — dwell-badge ~37 px panel shift dispositioned in §0.B + eye-walker comparison note.
- **P3-10** — S1 title → "Negative work becomes stored energy"; S2 cue → "No store for friction" (Rule 41).
- **P3-11** — anchors inverted: pumped-storage plant PRIMARY (38f widest-overlap), rooftop tank secondary; universality assertion dropped.
- **KEPT per review §B (do not re-litigate):** `bar_max_J = 2 × work_scale_J` pixel-pairing via the one-loop track write (`nlbFitEnergyPanel` L48860–48882, founder-proxy-proved at every reflow rung) — restated as a CHANGE invariant; the DoD (f-2) #7 forward-bridge sentence HELD; all §B-verified reads relied on unchanged.

## §0 SCAR DISPOSITION — 76 OPEN rows on `newtons_laws_body` + the directive corpus + the FIXED re-introducibles

Queries run this session: `--scenario newtons_laws_body --open` (76 rows, read in full), `--scenario newtons_laws_body` (FIXED set), `--row-type directive`, `potential_energy_definition` (0 rows — new concept). Every disposition claiming an engine behaviour cites the READER function read this session, per `architect_declares_an_engine_limit_without_checking_the_per_concept_override_path`; the inverted form (an N/A claim) is held to the same standard.

### §0.0 Session verification record — the load-bearing engine reads (all read 2026-08-09, `src/lib/renderers/field_3d_renderer.ts` unless noted)

1. **`energy_layer` gate + `U_grav` bar.** Config type: `bars` closed enum L1941; `h_ref_m` L1967. Reader `nlbEnCfg` L48578–48582 (absent/empty `bars` ⇒ not one pixel). `nlbHeightM` L48240–48244: `h = s·sin(theta_deg) − h_ref_m` (non-hanging); `b.U_grav_J = m·g·h` L48288–48290 — a pure state function of this frame's `s`, nothing accumulates.
2. **Negative U is a HARD constraint.** L48948–48955: `if (Ug < -1e-9)` fires `nlbEnWarnOnce(... NLB_ENERGY_SCALE_WARN_PREFIX ...)` — "U_grav went negative … Lower h_ref_m". THE EYE now FAILS on `[PM_*]` self-diagnostics (H4, ch6_state.md 2026-08-09). **So `h_ref_m` must sit at/below the body's lowest reachable height at EVERY instant of every loop, worst folded frame included.** Every loop below is bounded against this (§Arithmetic).
3. **Bar over-scale warns under the same EYE-asserted prefix** (L48967–48971 — clamp + warn). All scales sized ≥10% over the true loop extremes derived at ONE t (the `state_end_of_loop…` CRITICAL row's discipline).
4. **Checkpoint stamp emitter — interpolated form quoted** (`nlb_checkpoint_W_capture_renders_the_work_bar_label…`). `nlbCpStampText` L50551–50594: head = `label + (mode 'every' && _count>1 ? " (pass N)" : "") + ":  "`; `'U_grav'` emits `"U = " + nlbEnFx(ugX, prec)` (L50579); `'W'` emits ONE clause PER authored accumulator (L50584–50590). S3's stamps therefore render EXACTLY `point A:  U = 88.2 J · W gravity = −39.2 J` and `point B:  U = 127.4 J · W gravity = −78.4 J` (route-(a) levels, ~38–39 chars each; clause-boundary NBSP wrap L50596–50609). Base formula + 2 stamps = 3 lines — the shipped `work_energy_theorem` STATE_4 shape (clone evidence).
5. **Crossing-exact capture** (CRITICAL `nlb_checkpoint_capture_overshoots…` FIXED, relied on): `W` rewound `(1−f)·_dW`, `U_grav` shifted affinely to `s_m` (L50560–50568, `nlbCpFrac` L50543–50549). Round stamps are exact: W −39.2/−78.4; U 88.2/127.4 (affine in s_m, hence crossing-exact too).
6. **Dwell scheduler.** Config: `dwell_ms` L2110 (sanitised to (0, `NLB_DWELL_MAX_MS`=5000] — L49107, L51019–51020), `dwell_from_pass` L2115–2117 (default 1, L51021–51022), `marker: 'flag'|'point'` L2137. Reader L50457–50480: fires only on a STAMPED crossing — `_count++` (L50441) precedes the `'first'` early-continue (L50442), and the dwell block sits inside the stamped branch ("no dwell without a stamp", L50445–50450); never in sandbox, never under seize; window anchored at interpolated `tCross`; `max()` merge if one step carries two flags. **So `'first'` + `dwell_ms` dwells exactly once per cycle, on the stamped ascent pass — verified control flow; first authoring of this pairing.**
7. **Loop reset vs dwell**: `nlbRunLoopReset` L48440–48508 — a reset due mid-dwell DEFERS to the dwell end with a once-per-entry `[PM_NLB_DWELL]` warn (L48462–48468). THE EYE asserts zero ⇒ binding: `R ≥ T_last + D_last + ~500 ms` (honoured by hand, §0.A).
8. **Rewind trace — followed through EVERY called function** (`rewind_path_assertion_stops_at_the_function_body…`): loop reset → `nlbResetTrajectory()` L51452 (bodies re-seeded s0/v0 L51458–51459; the loop path restores the `(_tPrevMs, t_ms)` PAIR L48495–48507 — the PR-#74 clock fix) → `nlbSpringPhysReset(eng)` L48352: dwell window/label/owner/warn latch cleared L48366–48370, `E_t0` L48371, warn latches L48374–48380, loop clock L48381, **every W ledger zeroed L48405–48406**, **every checkpoint `_side=null`, `_count=0`, `text=""` L48407–48411** → `nlbCpArm(eng)` L51510 / L50405–50417 re-arms sides from AUTHORED seed data (no clock, no race). `U_grav` needs no rewind (pure function of s, §0.0.1). Terminating line cited for every write.
9. **Frozen-pin arithmetic** (no `eye_capture_ms`): `src/lib/validators/visual/deriveStateMeta.ts` L3202–3220 — offset = `clamp(0.60R, 150, R−150)`, `cycle = ceil((max(1500, candidates) − offset)/R)`, sets `phaseFound` (the SEAM-L 3000 ms energy floor L2972/L3258 defers to it). **`eye_capture_ms`**: authored per state under `field_3d_config.states.STATE_N` — `src/scripts/visual_eyes.ts` L221–235 extracts, L119–121 overrides the reveal map, L145–148 feeds BOTH the primary poll and the frozen pin. The E1 engine ask was discharged by this existing seam (PROGRESS 2026-08-08) — used on S3.
10. **Real minus on the nlb paths now.** `nlbMinus` L48605 substitutes U+2212 inside `nlbEnFx` L48608–48612 (PR #67). **Supersedes concept #5's cycle-1 F5 record ("ASCII hyphen-minus, no substitution on the nlb path") — that claim is STALE.**
11. **Work bars + energy bars share ONE panel** (SEAM M contract, ch6_state.md; header "Energy and work bars" when both authored — `work_energy_theorem` source_book, shipped clone evidence). Energy glow ids LIVE (L48551–48556); **`work_bar_*` ids INERT** (MAJOR/OPEN `nlb_work_bar_glow_ids_never_light…`) — none authored anywhere here.
12. **Bounds/inset:** home `initial_position_m = −3.6` (|−3.6| < 6.0 − 0.55, the static-bound directive's probe); max reach +2.93 m ≪ +5.4; no loop touches a bound, so the bound-arrest rows are never exercised.

### §0.A The four architect-owned OPEN directives (non-negotiable — each discharged with arithmetic)

| Row | Disposition |
|---|---|
| `nlb_dwell_state_authored_without_recomputing_loop_reset_and_eye_capture` | S3 is the ONLY dwell state. By hand: (a) T_A = 322.0 ms (crossing; physics = clock, no prior dwell); T_B = 789.7 + 2000 = **2789.7 ms** clock. (b) `loop_reset_ms = 6600 ≥ T_B + D_B + settle = 2789.7 + 2000 + 500 = 5289.7` ✓ — ZERO `[PM_NLB_DWELL]` warnings by arithmetic (defer branch L48462 unreachable). (c) `eye_capture_ms = T_B + D_B/2 = 2789.7 + 1000 ≈ **3790 ms**`, 1000 ms inside BOTH window edges. (d) Re-derive duty in DoD (d): any change to v0/m/theta/flag d/dwell re-runs this block. No dwell overlaps `idle_auto_sweep`/`param_ramp` (neither authored) or a spring window (no spring). |
| `nlb_frozen_pin_lands_within_one_frame_of_the_beat_the_dod_asserts_it_shows` | Margins in ms, discrete (h = 1/60, ≤2-step slack subtracted): S1 pin 1890 (= clamp(0.60·3150, 150, 3000)); asserted beat = near-peak mirror mid-descent; nearest discrete event = apex 1632.7 → margin **257 raw / 224 net ≥ 167** ✓. S2 pin 1770 (= 0.60·2950); apex 1016.5 → **753 / 720** ✓. S3 pin = authored 3790, mid-dwell (physics FROZEN, scene static) → window-edge margins **1000/1000** ✓. S4 sandbox — no pin assertion. No DoD sentence sits closer than 167 ms net to its event. |
| `rewind_path_assertion_stops_at_the_function_body_and_never_follows_its_calls` | Full trace §0.0.8 with the terminating line of every write in every called function (`nlbResetTrajectory` → `nlbSpringPhysReset` → `nlbCpArm`). Rewind claims made by this design: ledgers re-zero (L48406), stamps clear (L48411), sides re-arm from seed (L50411–50415), U needs no rewind (L48288–48290). |
| `architect_declares_an_engine_limit_without_checking_the_per_concept_override_path` | Every engine claim in §0.0 quotes the config-type line AND the reader line. Inverted form honoured on every N/A: multibody rows — `bodies[]` length 1 authored in every state, and the lane READER itself early-outs: `nlbBodyLaneZ` L44503–44508, `if (lanes.length < 2) return 0` (independently verified by founder-proxy cycle 0; P3-8 citation corrected — the cycle-0 disposition cited the directive TEXT, the exact substitution the architect-owned row forbids) — one declared body never enters the lane path; drift-guard N/A cites its own gate L48998–49001 ("state that shows an E_total bar" — no state authors `'E_total'`); `h_ref_m` truthiness cleared at L50937–50938 (`typeof === "number" && isFinite` — negative values bind correctly). Cycle-1 N/A re-audit (P3-8): every other N/A either quotes its deciding reader line (drift-guard L48998–49001, h_ref truthiness L50937–50938, explore-ring closed string enum L1819) or rests on config ABSENCE (no angle_arc / spring / pulley / velocity arrow / applied force authored anywhere), which has no reader to quote — no second instance of the directive-text substitution found. |

### §0.B OPEN rows that could bite this design — individual dispositions (part 1)

| Row | Disposition |
|---|---|
| `state_end_of_loop_energy_numbers_derived_at_a_different_t_than_the_travel_table` (CRITICAL) | AVOIDED: §Arithmetic derives s, v, U and W at the SAME t = R per state, substitution shown; scales set from that one set with headroom. |
| `nlb_sandbox_energy_envelope_computed_from_the_seed_lap_instead_of_the_full_wrap_span` (CRITICAL) | Two-part (P2-6 refile). FREE-RUNNING: AVOIDED — envelope over the FULL 12 m wrap span at the slider corner μ = 0.5 (cap LOWERED from 0.6 this cycle: 0.5 < tan 30° = 0.577, so the block can NEVER stick and every corner is free-running — the cycle-0 μ = 0.6 worst-corner argument was wrong in form, the block sticks at the apex there): W_fric = 0.5·33.948·12 = 203.7 J ≤ 0.9·280 = 252 ✓; W_grav 19.6·12 = 235.2 ≤ 252 ✓; U_max at s = +6: 19.6·(6 + 6.1) = 237.2 J ≤ 0.9·560 = 504 ✓; friction monotone in μ (corner 0.5); U position-only; v0 drives no shown bar. TEACHER DRAG: ACCEPTED-with-bounded-exposure — `nlbRunWorkAccum` L50319–50340 accumulates on ds = b.s − b._s_pre for ANY motion including trusted drag (teleport reject only, L50322); the wrap re-zeros the ledger, a drag does NOT, so at the μ = 0.5 corner (f = 16.97 N) the friction bar clamps + warns after 280/16.97 ≈ 16.5 m of cumulative drag between wraps — reachable in an extended demonstration, and S4's own μ discoverable invites it. Bounds on the exposure: numerals stay correct (the clamp is visual saturation of the bar), one `[PM_NLB_ENERGY_SCALE]` console warn, and THE EYE never fires trusted drags (sandbox untestable by THE EYE — recorded) so no H4 exposure; the (f-3) changes-since-the-wrap narration duty is the honesty mechanism. The cycle-0 AVOIDED filing was wrong — founder-proxy P2-6 sustained. Declared to eye-walker and the founder. |
| `nlb_frictionless_state_with_an_opposing_applied_force_reverses_and_unwinds_its_own_work_ledger` (CRITICAL) | N/A BY CONSTRUCTION + the #5 pattern: **no applied force anywhere** and **no guided state authors any slider or `trusted_drag_seizes`** — the probe gates on non-empty `controls_visible` and reaches only S4, where the wrap (not a seized loop) re-zeros ledgers and `loop_reset_ms` is inert in sandbox. The gravity ledger's unwind on the descent IS the taught physics (declared under `authored_beat_ends_by_undoing_the_state_own_claim`: S1's beat ends by returning U toward its 49.0 J opening — that return IS the claim). FLAG for quality-auditor Gate 8, #5 precedent verbatim. |
| `prose_in_a_variable_derived_field…` + `computed_output_name_encodes_a_symbol_no_instrument_paints…` + `computed_output_divides_by_a_coefficient_whose_default_is_zero…` (physics_author-owned) | Constraints handed down, DoD (f-7): `derived` = expression slots only; computed_outputs named to painted symbols (`U_J` → "U", `gravity_J`, `friction_J`); no division by μ/v0; finite at declared defaults (μ defaults 0 in S4). |
| `nlb_checkpoint_s_m_authored_as_displacement_not_track_coordinate` (DIRECTIVE) | AVOIDED: both S3 flags as home-pose arithmetic — A `s_m = initial_position_m + 2.0 = −1.6`, B `+ 4.0 = +0.4`; `initial_position_m = −3.6` stated in the home-pose paragraph; W stamps = −19.6·2.0 / −19.6·4.0; U stamps = 39.2·(0.5·s_m + 3.05) = 88.2 / 127.4 — verified. |
| `nlb_work_bar_zero_crossing_reading_is_unrenderable_at_teaching_speed` (DIRECTIVE) | AVOIDED: no narrated/captioned number is a moving-bar transient. S3's numbers live on LATCHED stamps under a physics-freezing dwell; S1/S2 claim the continuous mirror with no specific transient value; narration never asserts the bar READS 0.0 at an instant (S1's loop truncates ~115 ms before the exact recross, U resetting from 66.4 J — declared, §Arithmetic). |
| `nlb_loop_reset_clears_checkpoint_stamp_and_frozen_pin_can_photograph_an_empty_formula` (DIRECTIVE) | AVOIDED: S3's crossings (322/790 ms physics) ≪ 0.55·R; its pin is the authored mid-dwell instant where both stamps are latched-visible by construction. S1/S2/S4 author no checkpoints. |
| `nlb_static_state_authored_on_the_track_bound_fires_a_false_clamp_alarm` (DIRECTIVE) | AVOIDED: home −3.6 (inset margin 1.85 m); every loop end ≥ 0.27 m above home, far from bounds; zero `[PM_NLB_ENERGY_CLAMP]` expected. |
| Multibody family: `nlb_multibody_lane_gap…` · `the_eye_passes_a_frame_in_which_one_compared_body_is_hidden…` · `nlb_displacement_vector_is_single_body…` · `nlb_work_probe_globals_disagree…` · `nlb_multibody_sandbox_wrap_reanchors_only_the_wrapping_body` · `energy_layer_two_body_groups_stack_vertically…` · `skeleton_asserts_a_held_comparison_value_at_an_instant_the_faster_body…` · `skeleton_authors_a_multi_phase_state_on_an_engine_with_no_per_body_activation_time` · `nlb_lane_offsets_apply_to_declared_bodies…` | N/A — ONE declared body in `bodies[]` in every state (inverted-form citation §0.A row 4). Stated, not skipped. |
| `nlb_angle_arc_to_displacement_measures_net_travel…` (DIRECTIVE) | N/A — no `angle_arc` authored anywhere. |
| `nlb_motion_archetype_declared_from_a_between_state_delta_or_a_teacher_driven_control` (DIRECTIVE) | AVOIDED: §3 archetype audit — every archetype is an authored in-state motion, zero teacher input. |
| `authored_state_glow_focal_silently_voids_every_tts_sentence_glow…` + `state_glow_focal_dims_one_half_of_the_relation…` (DIRECTIVE) | AVOIDED: S1/S2/S3 are RELATION states (U bar ↔ W bar) → **no `glow_focal`**; `glowActive` stays false, per-sentence bindings LIVE. S4 authors `nlb_body_block` (single-object sandbox focal) and opts out of narration glow — declared. |
| `concept_ships_zero_narration_glow_bindings` (MAJOR) | Duty DoD (f-5): bindings on S1–S3 from the verified id whitelist; S4's zero bindings named with reason; NEVER a `work_bar_*` id. |
| `nlb_work_bar_glow_ids_never_light_behind_the_energy_prefix_gate` (MAJOR) | AVOIDED: no `work_bar_*` focal or binding anywhere. |
| `explore_state_discoverables_authored_only_as_unrendered_scene_composition_annotations` (MAJOR) | AVOIDED: S4 discoverables narration-delivered (§3); Rule-19 counts DRAWN primitives only (DoD j). |
| `shared_bar_scale_cross_state_guarantee_is_void_when_the_panel_reflow_ladder_drops_a_step` (DIRECTIVE) + `the_eye_captures_a_fixed_720px_sim_height_while_the_teacher_iframe_is_responsive` (MAJOR) | AVOIDED: ONE `bar_max_J`/`work_scale_J` pair across ALL guided states (290/145); single-token bar captions ("gravity", "friction"); panel load ≤ 1 energy bar + 2 work bars + ≤2 stamp lines (under #2's shipped 4-bar responsive evidence). Iframe stated as the RANGE ~551–911 px; THE EYE captures at the separate 720 default; no claim rides a reflow rung — the mirror is a CHANGE claim and rides NUMERALS plus the pixel-pairing invariant: one loop writes the SAME `S.trk` height to every `.nlb_en_trk` — energy AND work — (`nlbFitEnergyPanel` L48860–48882, founder-proxy-verified at every ladder step, cycle 0 — KEEP), so with bar_max = 2 × work_scale an equal joule CHANGE moves both fills the same pixels on every rung. |
| `one_measured_viewport_recorded_as_the_invariant…` (DIRECTIVE) | Honoured — range + capture size stated separately, above. |
| `energy_bar_track_renders_no_scale_ceiling_so_two_states_draw_one_value_at_two_heights` (MODERATE) | AVOIDED: scales CONSTANT across S1–S3; S4 differs — declared sandbox exemption (#5 precedent), and no numeral repeats between guided stills and sandbox. |
| `calculator_dom_harvest_needs_symbol_and_value_in_ONE_text_node…` (MAJOR) | MITIGATED: PR #47 landed the sibling-composition channel (K harvested on every WET state — clone evidence); the U bar + work bars use the same panel classes; S3's stamps are single-text-node harvestable. |
| `calculator_harvester_pairs_a_symbol_from_one_checkpoint_stamp_with_a_value_from_another` + `calculator_n3_slider_check_expects_an_instant_value_change_from_a_path_integral_readout` + `gate_headline_counts_fold_skips_into_passes` (gate-side) | ACCEPTED (OPEN, gate-owned): expect false FAILs/SKIP-noise on S3's two-stamp surface and S4's accumulated ledgers; THE CALCULATOR is ADVISORY — hand-confirm each. Declared so the auditor does not route a content fix. |
| `taught_variable_has_no_rendered_physical_correlate…` (MAJOR) | AVOIDED: U's non-text correlate = the block's HEIGHT above the dashed U = 0 line (the geometry the bar tracks) + the bar fill; W's = travel along the slope. |
| `quantitative_check_state_reuses_the_exact_numbers_and_the_delta_cue_of_the_state_it_verifies` (MAJOR) | AVOIDED: S3's stamped set {88.2, 127.4, −39.2, −78.4} at v0 = 7 is rendered as a STILL by no earlier state, and no delta cue repeats. Cross-CONCEPT freshness: m = 4 kg, θ = 30° vs #5's m = 5, θ = 25° — no stamp value collides with #4/#5. |

### §0.B continued (part 2)

| Row | Disposition |
|---|---|
| `ramp_endpoints_multiply_the_taught_variable…` + `field3d_param_ramp_authoring_contract` | N/A — no `param_ramp`. |
| `nlb_sandbox_default_mu_below_tan_theta_drifts_the_block…` + `nlb_sandbox_work_ledger_has_no_provably_safe_work_scale…` | AVOIDED: S4's standing surface is the Δ-form `ΔU = −W gravity` — true in every steady state (definitional, not a round-trip claim), and cycle 1 makes it reference-uniform with S1–S3 (one h_ref everywhere; μ sliders capped at 0.5, no stick corner); the wrap re-zeros the ledger AND re-anchors (FIXED `nlb_sandbox_wrap_remaps_s_but_not_s0…` relied on) so the Δ-form survives every lap; envelope sized over the wrap span. Narration duty: sandbox speaks in changes since the last wrap. |
| `nlb_friction_vector_first_frame_reveal_tint…` (MAJOR, engine) | N/A-with-note: NO friction arrow is drawn in any state (friction carried by bar + HUD `f` — #5-S2 precedent); the row's element never renders here. |
| `field3d_nlb_arrow_min_length_floor…` (DIRECTIVE, physics_author) | AVOIDED: the only arrow is weight, mg = 39.2 N → 39.2·0.048 = 1.88 units = 3.4× the 0.55 floor ✓ (NLB_ARROW_SCALE 0.048 read live per the row's own instruction); no length-ratio compare exists. |
| `nlb_dwell_holds_one_step_past_the_crossing_so_the_live_bars_still_trail_the_stamp` (MODERATE, OPEN) | ACCEPTED + BOUNDED: residual ≤ F·v·h — at B 19.6·3.13/60 = **1.02 J** (stamp 78.4 vs live ≈ 79.4), at A **1.77 J**. The live caption's "so far" qualifier is the honesty mechanism per the row; narration cites STAMP numbers only. Declared to eye-walker. |
| `checkpoint_flag_post_depthtest_false_occludes…` + `nlb_marker_captions_absent_from_the_body_label_obstacle_set` + `nlb_marker_caption_corner_nicks_the_body_mass_billboard…` (engine) | MITIGATED: `marker: 'point'` (track-level dot, depthTest TRUE — the PR-#74 form built to retire the post occlusion); captions "point A"/"point B" on the shipped 19 px caption lane. Residual transit-frame nicks are the OPEN engine rows — eye-walker judges from frames. |
| `field3d_dt_accumulated_motion_invisible_to_eye_timepin` (DIRECTIVE, engine) | AVOIDED: no new dt-accumulated quantity — U is a closed form of s; the W ledger is SEAM M's sanctioned accumulator rebuilt from 0 on entry/rewind (§0.0.8); latched stamps re-render under a pin. |
| `field3d_integrating_scenario_state_entry_must_rebuild_the_whole_engine_record` (engine) | Relied-on invariant (SEAM M RESET proof, ch6_state.md); this concept adds no engine record fields. |
| `frozen_frame_read_as_dense_series_continuation_on_translating_body` | Honoured: DoD (d) states each pin's expected picture so reviewers compare frozen against its own pin instant. |
| `reviewer_accepts_a_residual_on_an_inferred_visual_premise_instead_of_measuring_it` (DIRECTIVE) | **The PRIMARY aha IS a co-display claim (equal joule CHANGES, opposite signs, same frames — route (a)) ⇒ carried as THREE named Checkpoint-B measurement duties**, never closed from arithmetic: **(M-1, change form)** on S1's pin frame harvest the U numeral and the gravity-W numeral and assert U − 49.0 = −W at display precision (173.8 − 49.0 = 124.8 = |−124.8|; U₀ = 49.0 is the authored-h_ref constant, cross-checked by harvesting U on an early frame); **(M-2, pixel change)** render two synthetic joule values ΔJ apart into both bar families and assert the two fill-height DELTAS agree within 2 px (the FIXED pairing row's probe as a difference, enabled by bar_max = 2×work_scale + the one-loop track write `nlbFitEnergyPanel` L48860–48882); **(M-3, stamp difference)** harvest S3's two latched stamps and assert (127.4 − 88.2) = −(−78.4 − (−39.2)) = 39.2 at display precision — the Δ-law read directly off the record. All three measure the CHANGE, never the level (cycle-1 ruling). |
| `worked_loop_resize_patch_bounded_at_the_pin_instant_instead_of_the_loop_end` (DIRECTIVE) | Honoured: every R bounded at the LOOP END and the worst folded frame, not the pin (§Arithmetic). |
| `skeleton_pin_budget_names_the_phase_start_instead_of_the_last_asserted_reveal_inside_it` (DIRECTIVE) | Pin budgets name the LAST asserted reveal (S3: point B's stamp mid-dwell, not the state start). |
| `taught_delta_smaller_than_the_instruments_own_live_noise` (DIRECTIVE) | AVOIDED: taught deltas 39–128 J vs 0.1 J display precision. |
| `derived_readout_asserted_by_value_without_defining_its_metric` (DIRECTIVE) | Metrics in DoD (b): U in J at precision 1 from the dashed U = 0 line; W in J from home (guided) / last wrap (S4). |
| `phase0_union_table_asserted_not_walked_state_by_state` + `signed_engine_union_drops_items…` + `enum_gap_audit_walks_the_engine_ask_list_instead_of_the_dod_symbol_table` (DIRECTIVE) | ENGINE FIT CHECK walks union row #6 both directions AND walks the DoD symbol table against the closed enums. |
| `skeleton_designs_around_an_engine_limit_a_landed_build_already_removed` (DIRECTIVE) | Honoured: the dwell + `marker:'point'` + `eye_capture_ms` seams (landed 2026-08-08/09) are USED; §0.0 reads are current-tree. |
| `explore_controls_not_ring_gated_survive_the_ring_cut` (DIRECTIVE) | Discharged by analysis: the nlb `controls_visible` entry is a plain string — no per-control `min_ring` shape on this scenario (#5 §0.8 read of the same contract region, re-confirmed); all three S4 controls (v0, μₛ, μₖ) drive CORE-taught content, so every control survives every cut. |
| `state_added_at_review_outruns_the_config_contract_shape` + `prior_rulings_and_internal_ledgers_are_not_re_run_over_the_states_a_restructure_creates` (DIRECTIVE) | Standing instruction: any state added/split in review re-walks §0.0 and re-runs §Arithmetic from scratch. |
| `skeleton_anchor_specified_in_section_9_reaches_no_narration_line` (DIRECTIVE) | Duty: the §9 primary anchor is S1's hook sentence (physics-author input, DoD f-3). |
| `teach_visual_must_match_narration` + `derivation_principle_applied_to_one_beat_but_not_its_sibling` (DIRECTIVE) | Honoured: every narrated number sits on an instrument; the mirror principle applied to BOTH directions of S1's trip and re-checked on S2. |
| `state_rewrite_updates_the_state_block_and_leaves_every_other_field_asserting_the_old_behaviour` (MAJOR, json_author) | Duty handed down: any state edit greps the concept id + changed vocabulary across the JSON, 8 registration sites, classifier prompt. |
| `nlb_formula_and_readout_zones_are_fixed_css_and_collide_with_a_tall_hud` (MODERATE, engine) | MITIGATED: HUD ≤ 2 readouts/state; the 3-line surface only on S3 (WET-S4 shipped shape). |
| `field3d_focal_glow_pulse_phase_reads_absolute_time_so_frozen_h2_jitters` (MODERATE, engine) | Exposure limited: no guided focal; S4's focal is sandbox (no H2 equality assumed). Declared for eye-walker. |
| `baseline_set_whose_two_captures_of_one_pinned_instant_disagree…` (MAJOR) | Duty: before `visual:approve`, diff STATE_N vs STATE_N__frozen per the row (ship-chain step). |
| `eye_h2_frozen_frames_of_moving_elements_wobble…` (MODERATE) | Honoured: no DoD asserts H2 = 0.00% as an equality. |
| Dwell-badge panel shift (founder-proxy P3-9 / candidate row `checkpoint_dwell_badge_translates_the_shared_bar_panel…`) | ACCEPTED + DECLARED: `#nlb_slowmo` (top:52px;left:12px, L46612) and `#nlb_energy` (L48677) share the top-left corner; `nlbEnergyTopPx()` L48846–48857 re-tops the panel to badge.bottom + 8 (≈89 px) while a dwell or slow window is open, sliding the whole bar panel ~37 px down and back — the fit ladder is NOT re-run, only `top`. S3's frozen pin (3790) lands mid-dwell BY CONSTRUCTION (the mandated T + D/2 rule), so S3's frozen frame photographs the bars ~37 px lower than S1/S2's — in the state whose numerals the cross-state story leans on. Not a defect: founder-proxy measured no clipping at 720 or 551 px (step-1 bottom ≈478 < 539); precedent `work_energy_theorem` S4 shipped through it. Duty: eye-walker comparison note — the S3 panel offset is the badge, NOT a layout regression. |
| `velocity_arrows_routed_through_a_force_arrow_map…` + `architect_authors_a_force_triangle_whose_ratio_exceeds…` | N/A — no velocity arrow; one force arrow total. |
| Coupled/pulley/spring family (`nlb_coupled_sandbox_F_slider…` · `field3d_nlb_body_label_overlaps_the_pulley_mesh` · spring rows) | N/A — no coupling, pulley, spring, or F slider. |
| Engine-work-only rows (`engine_stash_on_shared_renderer…` · `two_skeletons_sharing_one_engine_build_state…` · `skeleton_authors_a_second_timed_action_after_the_engine_buy…` · `phase0_config_enum_closed_against_the_spec_driver…` · `new_scenario_buy_list_omits_the_visual_gate_platform_registration_sites` · `phase0_union_lists_capabilities_but_never_which_knobs_are_scriptable`) | N/A — this concept specifies NO engine mechanism (pure JSON, zero renderer edits); no sibling ch6 concept is concurrently in authoring. |
| Remaining OPEN rows judged irrelevant (grouped, per the brief's allowance) | Engine-internal label/ink/layout classes not triggered or not designable-around by this config: `field3d_generic_visible_elements_matcher…` (no new scenario) · `nlb_body_label_is_brighten_only…` · `field3d_arrow_label_sprite_renders_at_under_half…` · `nlb_camera_rotated_body_label_bleed…` · `field3d_sprite_label_text_and_ink_box_are_invisible_to_every_dom_probe` · `seam_r_ink_lift_reveals_sub_surface_force_arrows…` (fleet-wide founder call pending) · `seam_q_hovering_stroke…` · `nlb_overlay_ink_lift_is_bounded…` · `equal_magnitude_force_arrows_render_at_unequal_visual_weight…` (single arrow) · `nlb_force_label_font_scale…` · `nlb_angle_arc_radius…` (no arc) · `nlb_work_bar_track_tops_lose_collinearity…` (no label-constant change here) · `field3d_nlb_body_screen_anchor_lands_under_the_dom_slider_panel` (guided states author no sliders; S4 uses the fleet-standard sandbox layout) · `renderer_selfdiagnostics…`/`eye_renderer_self_diagnostics…` (H4 landed — now our guard, §0.0.2) · `spec_semi_implicit_euler…` · `derivestatemeta_new_scenario_key…` · `field3d_build_once_body_reads_a_per_state_flag…` · `explicit_linear_drag…` · `closed_enum_cannot_name_a_substance…` · `camera_solve_searched_in_one_axis…` · `engine_must_cite_a_quantity_its_own_model_misses…` · `refused_display_override…` · `hysteretic_state…` · `rule31_motion_floor_satisfied_by_a_late_label_reveal…` (every state has real motion) · `narration_timing_probe…` · `directive_no_gate_asks_whether_a_teacher_could_use_it` · `teach_*` rows for other scenario families · all PCPL/parametric/chemistry rows. |

### §0.C FIXED rows this concept could RE-INTRODUCE — the constraint that keeps each fixed

| FIXED row | Constraint honoured |
|---|---|
| `nlb_signed_bar_half_range_breaks_equal_value_pairing_with_an_unsigned_bar` (DIRECTIVE, alex:physics_author) | **The load-bearing one — this concept's claim IS the numeric CHANGE-identity between the unsigned U bar and the signed W bar (equal joule changes, opposite signs — route (a)).** Per the row's DO: **`bar_max_J = 2 × work_scale_J`** in EVERY change-claiming state: guided **290/145**, sandbox **560/280**. One joule = one pixel height on both families; the invariant is stated beside the scales in DoD (f-1) for inspection; probed at Checkpoint B (duty M-2). |
| `checkpoint_capture_mode_first_at_the_home_pose_stamps_the_departure_reading…` (CRITICAL) | Neither S3 flag sits at the home pose (−1.6/+0.4 vs −3.6), so `'first'` latches the ASCENT stamp — the intended reading. Recorded: if review ever adds a start-line mark it must be `'every'` (or `dwell_from_pass ≥ 2`) with the pass named in the label. |
| `nlb_checkpoint_at_the_body_home_pose_adopts_its_side_in_a_race` + `nlb_checkpoint_capture_overshoots_exact_crossing_value` + `nlb_checkpoint_dwell_does_not_refire_after_a_loop_rewind` | Relied on (arm-from-seed L50405–50417; interpolation L50560–50568; the `_tPrevMs` pair restore L48495–48507). Flags kept off the home pose anyway. |
| `nlb_work_bar_tracks_misalign_when_one_caption_wraps…` (CRITICAL) | Single-token captions `gravity` / `friction` — no wrap input. |
| `nlb_formula_surface_max_width_is_a_340px_literal` + `nlb_formula_surface_wraps_mid_clause…` | S3's surface = base + 2 stamps (~38 chars each) — the shipped WET-S4 shape; clause-boundary wrap verified L50596–50609. |
| `nlb_legend_renders_the_state_label_prose_on_canvas` (CRITICAL) | Engine-fixed; state labels still authored as short literal titles (rail only). |
| `nlb_sandbox_wrap_remaps_s_but_not_s0…` + `nlb_sandbox_wrap_reseeds_v0_toward_the_bound_it_just_crossed` (CRITICAL) | Relied on for S4's Δ-form claim across wraps. |
| `nlb_seized_slider_run_overruns_a_loop_sized_work_scale` (DIRECTIVE, json_author) | Guided states author ZERO sliders; S4's scales sized for the wrap span. |
| `derived_energy_sum_pairs_prestep_position_with_poststep_velocity` + `geometric_track_clamp_rendered_as_an_energy_change` + `contact_detected_slow_window…` | Spring/E_total paths — not exercised (no spring, no E_total). |
| `nlb_signed_launch_velocity_state_never_renders_K0…` (DIRECTIVE, physics_author) | N/A — K never shown; no K delta arithmetic exists to verify. |
| `nlb_hud_and_body_labels_never_show_mass…` | Single body; no unequal-mass compare exists; mass named once in narration. |
| `authored_second_language_text_dropped_for_one_state_during_json_transcription` (json_author) | Duty: `text_hi` authored structurally for every state (Rule 30i), diffed after transcription. |

## 1. Atomic claim

This concept teaches ONE idea: **potential energy U is energy stored by POSITION against a conservative force, DEFINED by ΔU = −W_conservative — when gravity does −40 J of work, the stored energy rises by exactly +40 J: the same size change, the opposite sign, in the same frames — and only a conservative force can have a U at all, because only its work is a single number per position (friction's is not, so "friction potential energy" does not exist).** It does NOT teach U = mgh or any formula for U's value, the freedom to choose the h = 0 reference, or path independence (all #7 `gravitational_potential_energy` — the dashed U = 0 line at the ramp's foot is named once as "where the count starts" and NEVER discussed as a choice); does NOT teach spring PE (#8 — no spring; `'spring'` is unrepresentable in the work enum by SEAM M design); does NOT teach K, E_total or conservation (#9 — no K bar anywhere); does NOT do the accounting of where friction's heat goes (#10 — S2 names the DESTINATION in one authored clause, nothing more; see §4); and does NOT re-teach the work meter (#1), work's sign (#2) or the round-trip test (#5 — consumed in one leaning clause, never re-run).

## 2. State count + arc — 4 states (3 guided + 1 explore)

Four, not the medium-band 5–6, and deliberately: the concept is ONE relation (the change-mirror), ONE scope condition (conservative only), and ONE quantitative form (ΔU between two marked points). A separate "stored going up" vs "returned coming down" split was drafted and rejected — both halves render the SAME round-trip picture, the #4-S5 deletion class; the storing and the returning are two phases of one continuous motion carrying one relation, so they are one state. Under route (a) — one reference at the ramp's foot, cycle-1 coordinated ruling — **S3 is genuinely load-bearing (P2-4 resolved): the U LEVEL no longer equals −W anywhere, so S3's held stamps are the only place the student can SEE that the levels differ and only the DIFFERENCES mirror.** A fifth state was considered and rejected per the ruling ("do not solve this by adding a fifth state"). Checkpoint marks appear in exactly ONE state (S3), where the claim is genuinely a difference between two places — the founder's mark rule.

| # | id | Ring | Purpose (one line) | teaching_method |
|---|---|---|---|---|
| S1 | `work_stored_as_potential_energy` | core | Launched up a frictionless slope: gravity's work bar dives while a second bar — U, the potential energy — RISES BY the same joules; on the way down gravity's work is positive, its total climbs back, and U empties by the same amount. Stored, then returned — **PRIMARY aha**; "potential energy" named here | misconception_confrontation |
| S2 | `friction_has_no_potential_energy` | core | Same trip made rough: U still moves joule-for-joule with gravity's bar, but friction's bar falls on BOTH legs and no bar rises to meet it — its joules leave the block as heat (destination named, one clause); only a conservative force's work can be stored; ΔU = −W conservative stated — **SUPPORTING aha** | misconception_confrontation |
| S3 | `delta_u_between_two_points` | extended | Two marked points; the scene HOLDS 2 s at each while the stamp reads U and W together: the LEVELS differ (88.2 vs −39.2) — only the CHANGE matches: from A to B, U rose by exactly the work gravity did, negated; one closing sentence hands U's formula and the movable zero line to #7 | (straightforward beat) |
| S4 | `explore` | core (explore) | Sandbox: launch speed, both friction dials, drag; U bar + both ledgers live, SAME dashed reference line as every guided state (no jump at the click — P1-3 resolved); every motion, up or down, rough or smooth, keeps ΔU = −W gravity | exploration_sliders |

Rule 38a: qualitative change-mirror + scope (S1–S2, core) → quantitative two-point form (S3, the sole extended state, contiguous, immediately before explore; **no advanced ring** — this concept has no derivation of its own; both preset cuts coherent with the empty block, DoD i). The hook MOVES from frame one (block already launched).

## 3. Per-state choreography + control table (Rule 31 — REQUIRED artifact)

**Home pose (Rule 32d — permanent, never rebuilt):** incline `surface.theta_deg: 30`, `length_m: 6` (track −6…+6 along the slope; +s is up-slope), ONE body (`id: "block"`, label `m`, `mass_kg: 4`, block shape), **`initial_position_m = −3.6` in ALL four states** — differentiated from #5's shipped apparatus (θ 25°, m 5 kg). g = 9.8 → mg = 39.2 N, **mg·sin 30° = 19.6 N** (the clean per-metre rate every number below derives from); μ = 0.35 in S2 → f = 11.88 N (HUD-only, no arrow). **`energy_layer.h_ref_m = −3.05` in ALL FOUR states (route (a), cycle-1 coordinated ruling)** — the ramp's foot: 0.05 m below the track's lowest point (s = −6 → h = −3.0), so U can never go negative under ANY motion, guided or dragged, and the reference NEVER changes between states (Rule 32d — the P1-3 teleport is gone by construction). The dashed line (`height_markers.show_h_ref_line`, `h_ref_label: "U = 0"`) draws at exactly that height in every state — one field, line and numeral cannot disagree (SEAM M contract). Closed form: **U = 19.6·(s + 6.1) J**, so the home pose opens at **U₀ = 19.6·2.5 = 49.0 J exactly** (the block starts 1.25 m above the line — S1 names the line and the 49 once, then never discusses it; #7 owns the choice). **The identity is now the Δ-form and ONLY the Δ-form: U − 49.0 = −W_gravity at every frame of S1/S3 (both linear in the same post-step s; the constant-force ledger is fold-exact, L50502 comment) — the U LEVEL never equals −W anywhere.** This is the law the concept claims (ΔU = −W_c), not the pinned special case; the cost — the "same numeral" read weakens to a lock-step-motion read — is the accepted trade (founder ruling 2026-08-09), and duties M-1/M-2/M-3 measure the CHANGE. Panel: LEFT edge, ONE group — the U bar (blue, engine symbol `U`) beside the signed work bar(s); header "Energy and work bars". ONE formula surface top-centre; HUD value-only. One camera S1–S3 (`block_on_incline` fleet framing); S4 sandbox camera. 32a cause-first: the block MOVES, the bars follow; 32b: only the taught pairing changes per state.

| # | Teaches | Archetype | Distinct motion (authored beat, no teacher input) | Delta cue | Controls | Ring | Words |
|---|---|---|---|---|---|---|---|
| S1 | Gravity's negative work is not lost — U rises by exactly the joules gravity's ledger falls, and returns them on the descent (ΔU = −W_gravity, both directions) | `cycle-compare` | Block launched UP at v₀ = 8 m/s, frictionless: the U bar opens at 49.0 J (block 1.25 m above the dashed U = 0 line at the ramp's foot, named once); over 1.63 s the gravity ledger dives 0 → −128.0 J while U climbs 49.0 → 177.0 J — equal joule changes, opposite directions, every frame; the block turns (ledger −128.0, U 177.0) and descends: gravity's work is now POSITIVE, its total climbs back toward zero, and U empties by the same joules; at R = 3150 ms (U at 66.4 J) the loop re-runs the trip | "Negative work, stored as U" | none | core | 45–55 (top of budget — anchor hook = ONE short clause) |
| S2 | Only a conservative force's work is stored: friction's bar has no mirror — its joules leave as heat | `cycle-compare` — **declared contrast pair with S1** (delta names the flip: same trip made rough — gravity's change-mirror survives, friction's bar gets none) | Same slope rough (μₛ = μₖ = 0.35), same 8 m/s launch: a THIRD bar joins — friction, falling on the way up (−48.3 J at the top) and STILL falling on the way down (−91.2 J by the loop end) while U keeps its joule-for-joule lock to gravity's ledger (U peak 128.7 as gravity reads −79.7 at the top; on the descent U falls and gravity climbs by the same amount). One bar's work is stored; one's leaves as heat — no store rises to meet it. HUD `f = 11.9 N` | "No store for friction" | none | core | 35–50 |
| S3 | Between two marked points, the LEVELS differ from the ledger — only the CHANGE in U equals minus the work gravity did between them | `reveal-build` — the beat CONSTRUCTS the two-line stamped record under two physics-freezing holds | Slower launch (v₀ = 7 m/s, frictionless): the block crosses **point A** (track-level dot, s = −3.6 + 2.0) at 322 ms and the WHOLE scene freezes 2 s ("paused: point A") while the stamp latches `point A:  U = 88.2 J · W gravity = −39.2 J`; motion resumes, crosses **point B** (s = −3.6 + 4.0) and freezes again: `point B:  U = 127.4 J · W gravity = −78.4 J`. Two lines now stand, and the levels visibly do NOT match their ledgers — but 127.4 − 88.2 = 39.2 IS minus the −39.2 J gravity did from A to B. The block tops out (U 147.0) and descends; at R = 6600 the record wipes and rebuilds | "ΔU equals −W gravity" | none | extended | 45–55 |
| S4 | Teacher's sandbox — the definition survives every motion | `drag-sandbox` | `mode: 'sandbox'`, `trusted_drag_seizes: true`; opens MOVING (the S1 launch, μ defaults 0) with the SAME dashed line and the same 49.0 J at the start line — continuous with every guided state. Named discoverables (narration-delivered, never annotations): drag the block anywhere — U tracks its height above the dashed line, and the gravity ledger's change since the last wrap is always its negative; raise μ — friction's bar falls whichever way the block moves, and still nothing rises to meet it; relaunch faster — U at a given height is unchanged (speed lives in the HUD, not in U) | "Change anything" | ALL: `v0` (0–8, step 0.5, default 8) · `mu_s` (0–0.5, step 0.05, default 0) · `mu_k` (0–0.5, step 0.05, default 0) + drag. **μ cap 0.5 (< tan 30° = 0.577): the block can never stick — every corner free-running (P2-6)** | core | 0 / open |

**Archetype audit** (per the `nlb_motion_archetype…` directive — each names a motion the authored beat produces with no teacher input): `cycle-compare` ×2 = S1/S2, the ONE declared contrast pair (canonical A→B→A′ round trips; the pair's delta names the flip — gravity's ledger change-mirrored by U in both, friction's mirrored by nothing in S2) · `reveal-build` ×1 (S3 — the only state constructing a persistent multi-line record, under the chapter's first `'first'`+dwell holds) · `drag-sandbox` explore-only. No static state; zero coined archetypes; #2/#3/#5's chapter coinages not reused. **Distinct-IDEA check (the founder's gate):** S1 proves the change-mirror EXISTS, both directions; S2 proves it does NOT extend to a non-conservative force (a third bar with no partner — a picture S1 cannot show); S3 proves the two-place arithmetic form AND that the levels fail where the differences work (a fact no other state can display — its stamps are the only latched co-readings of U and W in the concept); S4 is the sandbox. No state is derivable from its predecessors' pictures, and no two states differ only in numbers.

**Glow focal:** S1 = none · S2 = none · S3 = none (all three are RELATION states, U bar ↔ W bar — the relation-dimming directive; bindings live) · S4 = `nlb_body_block` (single-object sandbox focal; opts out of narration glow — declared). Binding whitelist in DoD (f-5).

**Scales (the pairing invariant, stated as a CHANGE per the cycle-1 ruling):** guided S1–S3 share `work_scale_J = 145` and `bar_max_J = 290 = 2 × 145`. One loop writes the SAME track height to every energy AND work bar (`nlbFitEnergyPanel` L48860–L48882 — founder-proxy-verified at every reflow rung, cycle 0 — KEEP), so with bar_max = 2 × work_scale **an equal joule CHANGE moves both fills by the same pixels on every rung**: S1's Δ128.0 J = 128/290 = 44.1% of the U track ≡ 128/145 = 88.3% of the W half-track = 44.1% of full-track-equivalent — the same pixel height. The mirror is pixel-true as a change even though the LEVELS differ by the 49.0 J offset. Extremes (each at its named instant, §Arithmetic): |W| max 128.0 (S1 apex, v = 0 there — no frame-slack excursion) ≤ 0.9·145 = 130.5 ✓; U max 177.0 (S1 apex) ≤ 0.9·290 = 261 ✓; S2 U peak 128.7 ✓, friction −91.2 ✓; S3 U peak 147.0 ✓. S4: `work_scale_J = 280`, `bar_max_J = 560` (envelope §0.B at the μ = 0.5 cap; declared sandbox deviation — explore exempt from cross-state comparability, #5 precedent). **Adjacent-numeral note (declared for eye-walker/auditor):** S1's 128.0 (ΔU = |W| peak) and S2's 128.7 (U level peak) are distinct at display precision 1 and are never co-rendered in one still.

## 4. Misconception confrontation plan (Rule 16a — 2 genuine pivots; other states straightforward)

| Wrong belief (real, documented) | State | `misconception_watch` beat |
|---|---|---|
| "Negative work means the energy is gone" (planted by #2's sign taxonomy and #4/#5's ever-falling ledgers) | S1 | `belief`: when a force's work is negative, that energy has been destroyed · `visual_counter`: as gravity's bar falls 0 → −128.0 J, the U bar climbs 49.0 → 177.0 J — up by the same 128.0 J in the same frames; on the descent U empties back exactly as gravity's total climbs back toward zero · `one_line_fix`: gravity's negative work is stored as potential energy and returned on the way down — moved, not destroyed |
| "Work done against ANY force is stored — friction too" | S2 | `belief`: pushing against friction stores energy the way lifting against gravity does · `visual_counter`: on the rough trip U still moves joule-for-joule with gravity's bar, while friction's bar falls on both legs and NO bar rises to meet it — friction's joules leave the block as heat, not into any stored-by-position number · `one_line_fix`: friction's work depends on the trip taken, not the position reached (the round trip from the last concept), so no single stored number per position can exist — its joules leave the block as heat (a later lesson tracks them); only conservative forces have a potential energy |

**P1-2 resolution (cycle 1):** S2 NAMES the destination in one authored narration clause — verbatim: *"Friction's joules leave the block as heat — a later lesson tracks them."* The (f-2) "heat" ban is lifted for exactly this clause and nowhere else; the destination is named, #10's ACCOUNTING is still withheld. S2's counter no longer implies absence of a destination (the cycle-0 wording "have no store anywhere on screen" is retired).

S3/S4 carry NO watch entries (founder guardrail 2026-07-04 — pivots only). Counter-evidence producibility (the evidence-confirms-belief CRITICAL class): S1's counter is the engine-true frame identity U − 49.0 = −W_gravity (§3 home-pose derivation); S2's is `b.f` flipping with velocity so the friction ledger falls both legs (#5's verified read of the same engine region) — both contradict the belief, never confirm it. EPIC-C branches: NONE (EPIC-L-first directive 2026-06-10).

## 5. `has_prebuilt_deep_dive` states (cache hints, not gates) — 3 picks

- **S1** — "where do the negative joules GO?" is the topic's documented core confusion (phrasings: "negative work destroys energy", "gravity removed the energy").
- **S2** — "why can't friction have a potential energy?" — students who accept the mirror still ask why the definition needs "conservative".
- **S3** — the two-point ΔU computation (sign errors in ΔU = −W are the classic exam failure), now including "why doesn't U equal −W?" (the level-vs-change distinction S3 displays).

Block-1 cross-reference: Pass-1 cliff states are S1/S2 — same as picks 1–2; S3 added for the quantitative abstraction. Divergence documented.

## 6. Drill-down clusters (3 candidates each)

- S1: `where_negative_work_goes` (stored vs destroyed) · `stored_energy_returns_on_descent` (the recovery half) · `sign_of_delta_u` (up means ΔU > 0, down means ΔU < 0)
- S2: `why_only_conservative_forces_have_u` (a single number per position) · `friction_work_is_not_stored` (where it differs from gravity — and where its joules go instead) · `round_trip_test_recap` (#5's test as the gatekeeper of U)
- S3: `computing_delta_u_from_work` (ΔU = −W arithmetic) · `u_level_vs_u_change` (why U ≠ −W but ΔU = −W — the stamps' own picture) · `minus_sign_in_the_definition` (why the minus is there)

## 7. `entry_state_map` (v2.2)

```
entry_state_map:
  foundational:   STATE_1 → STATE_2   # "what is potential energy" — contains PRIMARY (S1) + SUPPORTING (S2)
  two_point_form: STATE_3             # "calculate delta U", "change in potential energy between two points"
```

Default aspect `foundational`; PRIMARY aha inside it ✓ (no exit-pill needed). Cross-slice pill after foundational: "Read ΔU between two marked points?" → STATE_3.

## 8. Prerequisites (advisory — Rule 23)

Shipped: `conservative_vs_nonconservative_forces` (#5 — the round-trip test S2 leans on; its closing narration hands off to this concept by name), `positive_negative_zero_work` (#2 — signed work), `work_done_by_constant_force` (#1 — the W meter), `block_on_incline` (the slope apparatus). Forward edges: #7 `gravitational_potential_energy` (U = mgh + the movable zero — **coordination note: #7's S1/S2 sit at the ramp base h_ref = 0 and its S3 moves the line as its PRIMARY aha; #6's foot-of-ramp reference is consistent with that sequence by the 2026-08-09 coordinated ruling, and #6's S3 closing sentence is the authored bridge**), #8 (spring U), #9 (K + U conservation) all build on S1's change-mirror.

## 9. Real-world anchor (Rule 35 universal · 38f widest-overlap · 41 plain)

**Primary — a pumped-storage hydroelectric plant (the 38f widest-syllabus-overlap device for this concept).** At night, spare electricity pumps water uphill into a high reservoir; at peak hours the water runs back down through the turbines. A whole power grid stores energy by POSITION — and the energy it gets back is exactly minus the work gravity did while the water was pumped up. It is S1 at grid scale, physics-true at full depth, and a named device in energy topics across boards.

**Secondary — the rooftop water tank.** A pump pushes water up to a tank above a building; open a tap and the stored energy comes back — the water flows down on its own. Common in many parts of the world (no universality claimed — P3-11), the everyday-scale version of the same storage. (Deliberately NOT #5's shelf-bag anchor — no cross-concept reuse.)

No conservation vocabulary in either anchor (boundary with #9); the friction contrast needs no anchor of its own (S2 leans on #5's box-drag, already taught). The primary anchor is a REQUIRED S1 hook sentence, compressed to ONE short clause under S1's word budget (physics-author input — the anchor-reaches-narration directive).

## 10. Definition of Done (Gate 0 — no TBDs)

**(a) States:** the 4 states of §2 exactly as tabled in §3. `advance_mode`: `manual_click` ×3 + `interaction_complete` (Gate 12 ✓).

**(b) Symbol-label table (engine-true strings; term-introduction ledger — defining state precedes every use):**

| Narrated quantity | On-canvas rendering (engine-true) | Defined |
|---|---|---|
| potential energy U | unsigned blue energy bar, engine symbol `U` (NLB_EN_SYM L48541), value via `nlbEnFx` precision 1 with real U+2212 minus and −0.0 clamp (L48605–48612); metric: joules measured from the dashed U = 0 line at the ramp's foot (`h_ref_m = −3.05`, IDENTICAL in all four states) | **S1** (the concept's own new symbol) |
| work by gravity | signed work bar, single-token caption `gravity`, live value with the engine's "so far" qualifier; metric: joules from the home pose (guided) / last wrap (S4) | meter #1/#2 (prereq); this bar S1 |
| work by friction | signed bar, caption `friction` | #5 (prereq); this bar S2 |
| stamp lines (S3 only) | `point A:  U = 88.2 J · W gravity = −39.2 J` and `point B:  U = 127.4 J · W gravity = −78.4 J` — interpolated form quoted from `nlbCpStampText` (§0.0.4), one text node each, CALCULATOR-harvestable | S3 |
| points A / B | `marker: 'point'` track-level dots + captions `point A` / `point B` (19 px caption lane); the marker brightens only while its dwell window is open (L2066) | S3 |
| U = 0 line | dashed line at exactly `h_ref_m = −3.05` via `height_markers.show_h_ref_line`, `h_ref_label: "U = 0"` — SAME height in every state (Rule 32d) | S1 (one anchoring clause — "U counts from the dashed line at the ramp's foot"; NEVER discussed as a choice — #7 boundary). **ASSUMPTION — probe-before-authoring:** the line draws at `energy_h_ref_m · NLB_WORLD_PER_M` (L49891, founder-proxy-read); cycle 0 authored this exact value on S4, but no frame has yet proven the foot-of-ramp line sits INSIDE the guided `block_on_incline` camera frame. Confirm on the first EYE frame of S1; if clipped, route a camera-framing note — the physics is unaffected (U₀ = 49.0 is narrated) |
| weight | full arrow, engine label `mg` (39.2 N — 3.4× the length floor) | prereq; shown all states |
| speed / friction force | HUD `v = … m/s` (all states); `f = 11.9 N` (S2/S4) | prereq |
| paused badge | engine `paused: point A` / `paused: point B` (slow-mo channel) | S3 |
| formula surfaces | authored Unicode (Δ, U+2212, →): S1 `ΔU = −W gravity` · S2 `ΔU = −W conservative` · S3 `ΔU = −W gravity (A → B)` · S4 `ΔU = −W gravity` | — |

**Enum walk** (`enum_gap_audit…`): U → `bars: ['U_grav']` (L1941) ✓ · gravity/friction → `work_accumulators.force` closed enum ✓ · stamps → `capture: ['U_grav','W']` (L2084) ✓ · dots → `marker: 'point'` (L2137) ✓ · line/label → `h_ref_m` + `h_ref_label` ✓ · Δ-forms → authored formula surface (free text) ✓. Nothing named that no surface renders.

**(c) Direction-rule plan:** N/A — no right-hand rule (1-D track, scalar work). Direction content = the sign pairing itself (one bar's change up, one's down). Per-state `arrows.show: ['weight']` in ALL states — **no friction arrow anywhere** (friction carried by bar + HUD, #5-S2 precedent; keeps the first-frame-tint OPEN row unexercised) and no normal arrow (untaught; N lives only inside S2's f = μN HUD clause). No `show_components` (S1's one new thing is the U bar).

**(d) Motion plan (loops, flags, pins — arithmetic in §Arithmetic, every column labeled by its instant):**
- Loops: S1 **R = 3150** · S2 **R = 2950** · S3 **R = 6600** (wall-clock, incl. 4 s of dwell) · S4 free-runs (Rule 37).
- Flags: S3 ONLY — A `s_m = −3.6 + 2.0 = −1.6`, B `= −3.6 + 4.0 = +0.4`, both `{capture: ['U_grav','W'], capture_mode: 'first', marker: 'point', dwell_ms: 2000}` (`dwell_from_pass` default 1 — dwells on the stamped ascent pass only, §0.0.6), labels `point A` / `point B`. S1/S2/S4: ZERO checkpoints (their truths are continuous; the bars already prove them — the founder's mark rule).
- Dwell arithmetic (§0.A row 1 — UNCHANGED by route (a): crossings, dwells and pins are position/time facts, h_ref only offsets U's level): T_A = 322.0 → hold to 2322.0; T_B = 2789.7 → hold to 4789.7; R = 6600 ≥ 5289.7 ✓ zero `[PM_NLB_DWELL]`; **`eye_capture_ms: 3790` authored on STATE_3** (mid-B window, margins 1000/1000). RE-DERIVE ALL THREE on any change to v0/m/theta/flag positions/dwell lengths.
- Frozen pins + expected pictures (levels updated to route (a)): S1 **1890 ms** (descent begun: U ≈ 173.8 with ΔU 124.8 = |W| 124.8 — near-full change-mirror, block just below its top point) · S2 **1770 ms** (descent: U 118.0 falling joule-for-joule with gravity −69.0; friction −54.8 with no partner) · S3 **3790 ms** (physics frozen at point B: both stamps latched — 88.2/−39.2 and 127.4/−78.4 — badge `paused: point B`, live bars within the declared ≤1.02 J one-step residual; **panel sits ~37 px lower than S1/S2's — the dwell-badge offset, dispositioned §0.B, eye-walker comparison note**) · S4 interactive hold.
- Not used (correctly): `energy_layer` members `K`/`U_spring`/`E_total`/`E_dissipated` (boundaries #3/#8/#9/#10) · `sum_merge` · `predicted_stop`/`ghost_marker` · `displacement_vector` · `angle_arc` · `applied_force` · `param_ramp` · `push_off` · spring · `P`/`P_avg` · multi-body · `capture` members `K`/`v`/`s`/`E_total`.

**(e) Modes:** conceptual-only (Rule 20 [D]). Scenario `mode`: S1 `incline_slide` (frictionless, v0 8) · S2 `incline_slide` (mu 0.35/0.35, v0 8) · S3 `incline_slide` (frictionless, v0 7) · S4 `sandbox`.

**(f) Physics-author binding constraints:** `assessment` ≥ 6 questions, backward-designed (q1 mirror qualitative/S1 · **q2 sign-of-change (P2-7 re-designed): on the descent, is gravity's work on that leg positive or negative, and does U rise or fall? (down ⇒ W_grav > 0 on the leg ⇒ ΔU < 0) — grounded in S1's descent beat, whose narration now SAYS the ledger's climb is positive work (f-3), so the sim draws the picture the question asks about**/S1 · q3 friction-no-U with the reason AND the destination/S2 · q4 ΔU between two points from stamped W/S3 · q5 the general law ΔU = −W_c and its scope/S2 · q6 explore transfer: U at a height is speed-independent/S4) + `coverage_map`; `misconception_watch` exactly §4 (2 entries).
- (f-1) **Scale invariant beside the fields (stated as a CHANGE — cycle-1 ruling):** `bar_max_J = 2 × work_scale_J` in every state (290/145 guided, 560/280 sandbox); one loop writes one track height to every bar (`nlbFitEnergyPanel` L48860–L48882 — KEEP, founder-proxy-verified), so equal joule CHANGES move equal pixels on every reflow rung. Checkpoint-B duties **M-1 (numeric change: S1 pin, U − 49.0 = −W at display precision), M-2 (pixel change: two synthetic values ΔJ apart → fill-height DELTAS within 2 px), M-3 (stamp difference: 127.4 − 88.2 = 39.2 = −ΔW, read off S3's latched record)** — all defined in §0.B, all measure the change, never the level.
- (f-2) **Vocabulary bans (boundary + Rule 41):** no "mgh", "reference is your choice", "kinetic energy", "K", "total energy", "conserved/conservation", "spring" anywhere; **"heat" banned EXCEPT the single authored S2 clause (P1-2): "Friction's joules leave the block as heat — a later lesson tracks them."** — and the single authored S3 closing sentence (HELD per the coordinated ruling): "The next concept gives gravity's U its own formula, and lets you place the U = 0 line wherever you like." Banned register (41a, #5's list inherited): "gives back", "pays", "owes", "account", "banks", "remembers", "wants". Plain verbs allowed: stored, returned, rises, empties, counts, matches, leaves.
- (f-3) **Narration duties:** S1 hook = the §9 pumped-storage anchor, ONE short clause; S1 names the dashed line + the 49.0 J opening ONCE ("U counts from the dashed line at the ramp's foot — the block starts at 49.0 J"); **S1 descent clause (P2-7): "on the way down gravity's work is positive — its total climbs back toward zero as U empties"**; S1 never claims U reads exactly 49.0 at the loop's end (it resets from 66.4 J — the loop truncates ~115 ms before the home recross, declared); S2 delivers the heat clause verbatim (f-2); S3 narration reads the STAMP numbers only (never the live-bar transient — the ≤1.02 J residual row) and states the level-vs-change contrast ("U is not −W — the CHANGE in U is"); S4 speaks in changes-since-the-wrap and names the dashed line as where U counts from.
- (f-4) Accumulator order S2/S4: `[gravity, friction]` (gravity's mirrored ledger reads first); labels verbatim `"gravity"`, `"friction"`.
- (f-5) Sentence-glow duty on S1–S3 (no focal, bindings live); id whitelist: `energy_bar_U_grav`, `energy_panel`, `nlb_arrow_block_weight`, `nlb_body_block`, `checkpoint_1`, `checkpoint_2`, `marker_h_ref`; NEVER `work_bar_*` (inert). S4 zero bindings, declared with reason.
- (f-6) Readouts: `['v']` S1/S3; `['v','f']` S2/S4. Every narrated number sits on an instrument.
- (f-7) computed_outputs named to painted symbols (`U_J`, `gravity_J`, `friction_J`); `derived` = expressions only; finite at declared defaults (μ defaults 0 in S4).

**(g) Macro-micro plan (Rule 33):** N/A-with-rationale — the taught variable (energy stored by macroscopic position) lives at the macro level; the micro story of storage is beyond Class 11 scope here. 33d instruments ARE the spine: the U bar, the signed ledgers, the latched two-line stamp record, the live v/f HUD, the dashed U = 0 line.

**(h) Canvas budget (Rule 34):** ONE authored formula surface per state (math-serif Unicode; S3's stamps append UNDER it by engine design — base + 2 stamps = 3 lines, the shipped WET-S4 shape); caption = the ≤5-word delta cue only; prose in `#capStrip`; HUD value-only; panel left / formula top-centre / HUD clear of review chrome. Iframe: responsive ~551–911 px range, THE EYE at the separate 720 default — no claim rides a reflow rung (§0.B; the change-mirror is numeral- and delta-pixel-based on every rung).

**(i) Curriculum-flex block (Rule 38):**
- (i-1) **Cut 1** (hide advanced): identity — no advanced states. **Cut 2** (hide advanced + extended → S1, S2, S4): coherent — the definition, the change-mirror, the naming, the conservative-only scope and the sandbox are all core; nothing surviving references points A/B, the two-point form, or S3's formula. Constraint: S4's narration must not say "between two points" or reference A/B; the S3-only #7 hand-off sentence dies with the cut, acceptably. **Both directions checked (the P1-3 scar's rule): no surviving state DEPENDS on a cut state for its only explanation — the reference line is identical in all four states and is named in S1, a surviving state under every cut; S4 inherits nothing from S3.**
- (i-2) Explore surfaces CORE content only: U bar, gravity + friction ledgers, `ΔU = −W gravity` surface. No checkpoints in S4.
- (i-3) Explore-control ring audit: v0/mu_s/mu_k all core-taught (§0.B); no `min_ring` field exists on this scenario (closed string enum L1819, founder-proxy re-confirmed against the L1323 contrast) — discharged by analysis.
- (i-4) `curriculum_tags`: CBSE/NCERT Class 11 Ch.6 ("The concept of potential energy" is a named NCERT section between the work-energy theorem and conservation) — **verified** at authoring (38g; chapter-consistent per founder-proxy P3-12, chapter-wide founder call). IB DP, AP Physics 1/C, A-Level (AQA/OCR/Edexcel), JEE Main/Advanced, NEET — claims with `needs_teacher_verification: true`.
- (i-5) Presets (hide, never reorder — 38h/25d): `full` = S1–S4 · `standard` = S1–S4 · `intro` (hide extended) = S1, S2, S4 — coherent under (i-1) both directions.
- (i-6) Graph-axis convention: N/A — no graph panel (bars + stamps, not plots).

**(j) Rule 19:** every state declares ≥3 primitives actually DRAWN by `newtons_laws_body` (body, slab, weight arrow, U bar, work bar(s), dashed U = 0 line, point markers, stamps) — never unrendered `annotations`.

**(Rule 41 audit of reader-facing strings — P3-10 applied):** titles — **"Negative work becomes stored energy"** (S1 — result stated, meaning in the first words, no trailing fragment) · "Friction has no potential energy" (S2) · "ΔU between two points" (S3) · "Explore: launch speed, friction, drag" (S4). Delta cues — "Negative work, stored as U" (5) · **"No store for friction"** (4 — the store is the subject; friction no longer the agent of storing) · "ΔU equals −W gravity" (4) · "Change anything" (2). Literal, no personification (forces do not "give back" — stored energy "is returned", literal passive; friction's joules "leave", literal motion of energy); flag labels `point A`/`point B`; badge strings engine-fixed.

## ENGINE FIT CHECK (0d — union walk, BOTH directions)

Union row consumed (phase0_survey): **"ΔU = −W_c coupling — the two must be shown as the same quantity → concept 6."** (Shown as the same CHANGE — the Δ-form is the union row's own statement.)

| # | Needs | Engine block (read cycle 0, current tree; founder-proxy-verified) | Status |
|---|---|---|---|
| S1 | U bar change-mirroring the gravity ledger, both directions, from a visible U = 0 line | `energy_layer: {bars:['U_grav'], bar_max_J: 290, h_ref_m: −3.05, precision: 1}` (gate L48578; U = mg·(s·sinθ − h_ref) L48240–48244, L48288–48290) + `work_accumulators: [{force:'gravity', label:'gravity'}]` + `work_scale_J: 145` + `height_markers: {show_h_ref_line: true, h_ref_label: 'U = 0'}` (line at exactly h_ref_m — SEAM M; in-frame visibility ASSUMPTION flagged, DoD b) + `loop_reset_ms: 3150` (rewind trace §0.0.8) | ✅ built |
| S2 | a third bar with no mirror; U's change-lock surviving friction | + `{force:'friction', label:'friction'}` (live signed `b.f`, falls both legs — #5's verified read); the U/gravity Δ-identity is unaffected by friction (both pure functions of s) | ✅ built |
| S3 | two held, readable place-to-number stamps pairing U with W | `checkpoints` ×2 (`'first'` latch + `dwell_ms: 2000` + `marker:'point'` — control flow §0.0.6/7; stamp form §0.0.4; crossing-exact §0.0.5 — the U stamp is affine in s_m, hence exact 88.2/127.4) + `eye_capture_ms: 3790` (extractor §0.0.9) | ✅ built — **first `'first'`+dwell pairing; the PR-#74 seam's second consumer** |
| S4 | sandbox with U + both ledgers + wrap honesty, SAME reference as guided | `mode:'sandbox'` + `trusted_drag_seizes` + tokens `v0/mu_s/mu_k` (μ capped 0.5) + the same `h_ref_m: −3.05` (negative-U guard cleared over the full wrap span AND all guided loops — ONE bound check covers all four states, §Arithmetic) + wrap ledger/anchor re-zero (FIXED rows) | ✅ built |
| — | Reverse walk | Union row #6 → all four states consume it; no other union row is this concept's (K #3/#4, U_grav-as-mgh + zero line #7, U_spring #8, E_total #9, E_dissipated #10, P #11/#12 — absent by boundary). `deriveStateMeta` co-edit: NONE (no new scenario key; loop states keep `reveal_hold`; S3's pin is the authored `eye_capture_ms`) | ✅ walked |

**Alarm rule: NOT fired — zero renderer edits.** Accepted limitations (recorded, deliberately not routed): (1) the dwell one-step stamp-vs-bar residual ≤ 1.02 J (OPEN row, declared); (2) S1's loop truncates ~115 ms of physics before the exact home recross, so U resets from 66.4 J and never displays exactly 49.0 at the loop's end — narration worded to the process, not the instant; (3) **the LEVEL equality U = −W is gone BY DESIGN (route (a), coordinated ruling): U and the gravity ledger read different numerals everywhere (offset 49.0 J) — the taught claim, the narration, and duties M-1/M-2/M-3 are all change-form; cycle 0's "same numeral" read is deliberately traded for lock-step motion + stamp-difference arithmetic**; (4) THE CALCULATOR's cross-stamp pairing and accumulated-slider checks will emit known false FAILs (OPEN gate rows) — hand-confirm, do not tune content; (5) the foot-of-ramp U = 0 line's in-frame visibility on the guided camera is an ASSUMPTION — probe-before-authoring (DoD b); (6) S4 friction-ledger clamp under extended teacher drag — ACCEPTED-with-bounded-exposure (§0.B, P2-6).

## Two-pass cognitive lens

### Block 1 — Pass-1 strategic checklist

**Prerequisite cliff.** (1) `positive_negative_zero_work` missing → breaks at **S1** ("why is gravity's bar below zero?"): patch clause "the work meter counts with its sign — a force against the motion counts negative, exactly as before". (2) `conservative_vs_nonconservative_forces` missing → breaks at **S2** (the word "conservative" and the trip-dependence reason): patch clause "a conservative force is one whose round-trip work is zero — gravity passed that test, friction failed it" (one sentence, no re-run of the test). (3) `work_done_by_constant_force` missing → S1's joule: covered by the same S1 patch clause. Cliff states S1/S2 = deep-dive picks 1–2 ✓.

**JEE-backwards trace.** Question: *"A 2 kg block is taken 3 m up a 30° frictionless incline. (a) Work done by gravity. (b) Change in the block's potential energy. (c) The same block is instead dragged 3 m across a rough horizontal floor (μ = 0.4). Why is there no 'potential energy of friction' that stores the −23.5 J friction did?"* Pieces → states: (a) −2·9.8·3·sin 30° = −29.4 J — sign and meter are #1/#2 prereqs, exercised in S1; (b) ΔU = −W_grav = +29.4 J — S1 (definition) + S3 (the arithmetic form, INCLUDING that only the change is asked — the level would need a reference, which is #7's lesson); (c) friction's work is trip-dependent, not position-valued, so no U exists — S2. The natural follow-up ("find its speed at the top") is deliberately out of scope (#9). No missing piece within scope.

**Misconception entry mapping (16a).** (1) "negative work = energy destroyed" — arrives with the student AND is actively planted by #4/#5's monotone-falling ledgers; S1 confronts it in the concept's FIRST state, consequence-first (the U bar rises by the same joules in the same frames — no predict-pause). (2) "work against anything is stored" — planted by S1 itself (the generalization from gravity is the natural confident-wrong step); S2 breaks it ONE click later with the unmirrored friction bar AND names where those joules actually go (heat — one clause), so the correction never implies destruction (P1-2). Both watches in §4; no EPIC-C branches.

### Block 2 — Aha-moment designation

- **PRIMARY aha (S1):** gravity's negative work is not gone — the U bar rises BY exactly the joules gravity's bar falls, and empties by the same joules on the way down. The 10-year memory: *stored, not destroyed — the two bars always move by the same amount, in opposite directions.*
- **SUPPORTING aha (S2):** friction's bar has NO partner — the storage belongs to conservative forces only; friction's joules leave as heat. Exactly 1 primary + 1 supporting (the sweet spot); S3 is deliberately NOT an aha (it is the quantitative reading of S1 — and the display that the LEVELS differ where the CHANGES match).
- **Cohesion:** S2's scope condition is what makes S1's relation a DEFINITION rather than a curiosity; S3 restates S1 in the form #7 will consume (and its level-vs-change display is the doorway to #7's movable zero). Every state serves the primary.
- **Wrong-belief setup:** primary — set up by #2/#4/#5's entire history of work bars that only ever fall or vanish (the long setup; the student arrives confident, no in-concept setup state needed); supporting — set up by S1 one click earlier (the "everything stores" generalization S1 invites).
- **Foundational-coverage:** S1 ∈ foundational ✓.

## Arithmetic (RE-DERIVED cycle 1 under route (a) — every column labeled by its instant; engine step fold-exact for constant a, L51523–51526; all values probe-verified by inline computation this session, P2-5)

Constants: m = 4 kg, θ = 30°, g = 9.8 → mg·sinθ = 19.6 N; a_free = 4.9 m/s²; μ = 0.35 → f = 0.35·4·9.8·cos 30° = 11.882 N; a_up(rough) = 9.8(0.5 + 0.35·0.86603) = 7.8705; a_down(rough) = 1.9295 m/s². Home s₀ = −3.6; **h_ref = −3.05 in ALL states ⇒ U = 19.6·(s + 6.1); U₀ = U(s₀) = 49.0 J exactly; U − 49.0 = −W_gravity every frame (S1/S3)**. Guard: lowest track point s = −6 → h = −3.0 > h_ref = −3.05 ⇒ U > 0 EVERYWHERE on the track, every state, drag included — the negative-U guard (§0.0.2) provably never fires, with margin ≥ 0.98 J at the physical bound, ≥ 12.7 J at the drag inset (s_min = −(length_m − 0.55) = −5.45, founder-proxy-verified bound), and ≥ 49 J at every reachable guided-loop frame.

| State | Launch | d_up (m) | t_apex (ms) | home recross (ms) | R (ms) | s and U at t = R exactly | s and U at t = R + 50 (worst folded frame) | U/W extremes, each at its named instant (J) | pin (ms) → picture at the pin instant |
|---|---|---|---|---|---|---|---|---|---|
| S1 | 8 m/s, μ=0 | 6.5306 | 1632.7 | 3265.3 | 3150 | s(3150) = **−2.7101** (0.89 above home ✓); U(3150) = **66.4** (ΔU 17.4) | s(3200) = **−3.088** ✓ above home; U(3200) = 59.0 > 0 ✓ | U: 49.0 (t=0) → 177.0 (apex, v=0 ⇒ no frame-slack excursion) ≤ 261 ✓; W: 0 → −128.0 (apex) ≤ 130.5 ✓ | 1890 → s = 2.768, U **173.8** / W **−124.8** (ΔU 124.8 = |W|), descending |
| S2 | 8 m/s, μ=0.35 | 4.0658 | 1016.5 | 3069.3 | 2950 | s(2950) = **−3.141** (0.46 above home ✓); U(2950) = **58.0**; W_grav(2950) = −9.0; W_fric(2950) = −11.882·(4.0658 + 3.6066) = **−91.2** (distance-by-2950 = 3.6066 — SAME instant as s(2950); the cycle-0 row mixed R and R+50, P2-5 fixed) | s(3000) = **−3.330** ✓ above home; U(3000) = 54.3 ✓ | U: 49.0 → 128.7 (apex) ≤ 261 ✓; W_grav −79.7 (apex); W_fric −48.3 (apex) → −91.2 (t=R) ≤ 130.5 ✓ | 1770 → s = −0.082, U **118.0** / gravity **−69.0** (ΔU 69.0 = |W_grav|) / friction **−54.8**, no partner |
| S3 | 7 m/s, μ=0 | 5.0 | 1428.6 | 2857.1 (physics) | 6600 wall = 2600 physics | s(2600 phys) = **−1.962** (1.64 above ✓); U = 81.1 | s(2650 phys) = **−2.255** ✓; U = 75.4 | U: 49.0 → 147.0 (apex) ≤ 261 ✓; stamps at the crossings: U(A) = 19.6·4.5 = **88.2**, U(B) = 19.6·6.5 = **127.4**, W(A) = **−39.2**, W(B) = **−78.4** — all exact (§0.0.5; U affine in s_m) | 3790 → frozen mid-B-dwell, both stamps latched |
| S4 | sandbox | — | — | — | free-runs | wrap at ±6; drag inset s_min = −5.45 → U_min = **12.7 J** ✓ | h_min = −3.0 > h_ref ⇒ U > 0 at every draggable point ✓ | envelope at μ-cap 0.5: W_fric(12 m wrap) = 16.974·12 = **203.7** ≤ 252 ✓; W_grav(12 m) = 235.2 ≤ 252 ✓; U_max(s=+6) = 19.6·12.1 = **237.2** ≤ 504 ✓ | interactive |

Crossings (S3, physics clock — position facts, UNCHANGED by route (a)): A (d = 2.0): 2.45t² − 7t + 2 = 0 → t = **322.0 ms**, v_A = 5.42 m/s. B (d = 4.0): t = **789.7 ms**, v_B = 3.13 m/s. Dwell clock: T_A = 322.0 (hold → 2322.0), T_B = 789.7 + 2000 = 2789.7 (hold → 4789.7); descent re-crossings hit the `'first'` pass-2 early-continue (L50442) — no stamp change, no dwell ✓. Reset-detection worst case: one frame ≤ 50 ms (3 folded steps) past R — the R+50 column above bounds every state above home, so the negative-U guard provably never fires. Dwell-freeze residuals: ≤ F·v·h = 19.6·5.42/60 = **1.77 J** (A), 19.6·3.13/60 = **1.02 J** (B) — declared (§0.B). Pixel-pair CHANGE check at B (duty M-2's arithmetic): ΔU(A→B) = 39.2 → 39.2/290 = 13.5% of the U track ≡ ΔW = 39.2 → 39.2/145 = 27.0% of the half track = 13.5% of the full track — the same pixel delta. Cross-state numeral cross-tab (rendered stills): S1 pin {173.8, −124.8} · S2 pin {118.0, −69.0, −54.8} · S3 stamps {88.2, −39.2, 127.4, −78.4} — no repeated rendered set, no repeated delta cue; **49.0 appears as S1's opening AND S4's start-line value — the SAME quantity by design (reference continuity, the point of route (a)) — declared, not a collision**; S1's 128.0 and S2's 128.7 are adjacent-but-distinct at precision 1, never co-rendered (declared, §3 scales). S2 slide-back guarantee: tan 30° = 0.577 > μ 0.35 ✓ (margin 1.65×); breakaway at the apex: mg sinθ = 19.6 > maxStat = 11.88 ✓ no stuck dwell; S4: μ-cap 0.5 < 0.577 ⇒ NO corner of the sandbox can stick (the cycle-0 μ = 0.6 "worst free-running corner" argument was wrong in form — P2-6 corrected: every corner is now free-running by construction).

## Compliance lines

- **Source check:** NCERT Class 11 Physics Ch.6 table of contents consulted for scope only ("The concept of potential energy" sits between the work-energy theorem and conservation — confirming the #5 → #6 → #7 order). No teaching method, example problem or figure imported; anchors authored universal (Rule 35), primary = widest-overlap device (38f, P3-11 applied).
- **Engine bug queue:** all four mandated queries run (cycle 0; dispositions re-checked against the cycle-1 design). §0 covers every OPEN row, every re-introducible FIXED row, and the four architect-owned directives with hand arithmetic; cycle 1 adds the dwell-badge panel-shift disposition (P3-9) and the drag-exposure refile (P2-6). Field_3d pedagogy directives applied: concrete-before-abstract (two watched trips before the two-point form) · nothing hardcoded at `*_at_ms` (crossing/loop/dwell-driven; the one authored instant is THE EYE's own `eye_capture_ms`) · every narrated number on an instrument · don't-pre-spoil (no K bar, no E_total, no movable zero, no mgh — #7/#8/#9 material withheld).
- **Boundary reconciliation:** #1/#2 — meter and sign consumed, never re-taught; #5 — round-trip test consumed in one S2 clause, exactly as #5's closing line promised; #7 — one authored forward sentence (f-2, HELD per the coordinated ruling) + the reference designed so #7's S3 lands as a genuine first-time change (the line never moves inside #6, and never sits at #7's ground zero); #8 — no spring; #9 — no K, no totals; #10 — the DESTINATION of friction's work named in one clause (heat), the accounting withheld.
- **Cross-concept coordination (P1-1):** #7 is architected in parallel; the 2026-08-09 coordinated ruling (filed at the end of #7's founder_proxy_A.md) binds both sides — #6 takes route (a), #7 adds its bridging clause. The two skeletons are gated together on this point.

## Self-review checklist — verified (cycle 1)

Atomic claim one sentence with #1/#2/#5/#7/#8/#9/#10 boundaries, change-form ✓ · 4 states with the under-band justification written; S3 now load-bearing under route (a) (P2-4) ✓ · control table complete (teaches × archetype × distinct motion × delta × controls × ring × words); ONE declared contrast pair (S1/S2), zero coined archetypes, no static state, drag-sandbox explore-only ✓ · Rule 32 (block moves first, bars follow; one taught pairing per state; ≤5-word cues; ONE home pose −3.6 AND ONE reference −3.05 in ALL states — 32d now covers the measured-from field too, per the P1-3 scar's prevention rule; one camera S1–S3; zero guided focals, relation-state rule cited) ✓ · Rule 33 N/A-with-rationale + live instruments ✓ · Rule 34 (one formula surface per state; S3's 3-line stamp shape = shipped WET-S4 clone; Unicode incl. the real engine minus) ✓ · Rule 35/38f/41 (anchors inverted per P3-11 — widest-overlap primary, no universality assertion; titles/cues re-worded per P3-10; plain-language sweep; banned-register list inherited) ✓ · Rule 38 (rings ordered, sole extended state contiguous before explore, empty advanced declared, both cuts checked BOTH DIRECTIONS incl. the S4-narration constraint, explore core-only, control ring audit, tags as claims, presets, axes N/A) ✓ · Rule 19 drawn-primitives plan ✓ · misconception_watch at exactly 2 pivots, counter-evidence engine-producible, destination named (P1-2) ✓ · deep-dive picks ×3 with clusters + divergence note ✓ · entry_state_map with PRIMARY in foundational ✓ · prerequisites advisory + the #7 coordination note ✓ · DoD zero TBDs; ONE declared ASSUMPTION flag (foot-of-ramp line in-frame — probe-before-authoring; flagged is routing, not a defect) ✓ · Blocks 1+2 complete, aha re-worded to change-form ✓ · ENGINE FIT CHECK walked both directions, zero renderer edits, alarm rule not fired, six accepted limitations recorded ✓ · §Arithmetic RE-DERIVED from scratch under route (a), every column labeled by its instant, worst-folded-frame column retained and populated, all values probe-verified this session (P2-5) ✓ · §0: multibody citation now quotes the deciding reader + N/A re-audit recorded (P3-8); dwell-badge shift dispositioned (P3-9); drag exposure refiled ACCEPTED-with-bounds (P2-6) ✓.

**Handoff:** to founder-proxy Checkpoint A (cycle 1), then physics-author. Physics-author inputs: narration within the tabled budgets under (f-2)/(f-3) — including the S2 heat clause and the S1 descent clause VERBATIM as authored duties; stamp values verbatim (88.2 / 127.4 / −39.2 / −78.4 — never recomputed loosely); the S1 anchor-hook duty (one clause); the (f-5) glow-binding duty on S1–S3 only; `assessment` + `coverage_map` per (f) with the re-designed q2. json-author authors: home −3.6 all states; `energy_layer` per state (`bars: ['U_grav']`, `bar_max_J` 290/290/290/560, **`h_ref_m` −3.05 in ALL FOUR states**, `precision: 1`); `work_accumulators` S1/S3 `[gravity]`, S2/S4 `[gravity, friction]`; `work_scale_J` 145/145/145/280; `height_markers` with `h_ref_label: "U = 0"` in every state; S3's two checkpoints as home-pose arithmetic (+2.0/+4.0) with `'first'` / `['U_grav','W']` / `marker: 'point'` / `dwell_ms: 2000` and **`eye_capture_ms: 3790` on STATE_3**; `loop_reset_ms` 3150/2950/6600; `arrows.show: ['weight']` everywhere; readouts per (f-6); modes per (e); sliders `v0` (0–8) / `mu_s` (0–0.5) / `mu_k` (0–0.5) exposed in S4 only (key `default`, never `def`); zero `param_ramp`, zero applied force, zero spring, zero K bar, zero `angle_arc`. First EYE run: confirm the U = 0 line is in-frame on S1 (the declared ASSUMPTION).
