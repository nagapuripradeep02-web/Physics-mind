# ARCHITECT SKELETON — `gravitational_potential_energy` — cycle 1 (revised 2026-08-09 per founder_proxy_A P1-1…P3-6 + the dispatching session's coordinated ruling; cycle-0 preserved verbatim at `skeleton_cycle0.md`)

> Chapter: Class 11 Ch.6 Work, Energy and Power · concept **#7** of 12 (approved teaching order, founder 2026-08-01)
> Renderer: `field_3d` / `newtons_laws_body` + SEAM K/L/M/N energy layer. **0d pure-JSON concept — this is the FIRST concept to exercise `energy_layer.h_ref_m` and SEAM M's shared-field `h = 0` line (built in Phase 0 for exactly this concept). Design target: ZERO renderer edits — met; see ENGINE FIT CHECK.**
> Authoritative config contracts: `docs/loop_runs/ch6_state.md` §"SEAM K/L/M/N RESULT" — those REPORTS supersede any literal here.
> Doctrine: Rules 16a · 19 · 23 · 24 · 25 · 31 · 32 · 33 · 34 · 35 · 38 · 41. Conceptual-only (Rule 20 [D]); EPIC-C branches: none.
> Boundary with #6 (`potential_energy_definition`, in-flight on a parallel desk — cited by concept_id only, NEVER by its state numbers, per `paired_skeletons_cite_each_other_by_state_number_across_a_renumbering`): #6 owns the general definition and ΔU = −W_conservative; **#7 owns U = mgh, the h = 0 line as a free choice, ΔU's independence of that choice, and path independence for gravity.** S5 below CITES the #6 relation as taught and applies it to gravity; it never re-teaches it and it is not this concept's primary aha. **Cycle-1 coordination (dispatching session's ruling on collision 2): #6 takes route (a) — ONE reference at the ramp's foot across its states. Absorbed here: #7-S1/S2's line at the ramp base is visually CONTINUOUS with #6's picture, so S3's move is the first time the line moves (planting story re-pointed, §4 + Block-1); and both concepts now face the same offset-U problem and solve it the SAME way — latch the base value on screen so the subtraction is visible (#7-S5's start stamp; #6-S3's stamps).**
> Every renderer line number below was read THIS session (desk `C:\Tutor\physics-mind-ch6-work-energy-power`, 2026-08-09), never inherited.

## 0A. Session verification record — every load-bearing engine claim read at source

Per `architect_declares_an_engine_limit_without_checking_the_per_concept_override_path` (and its inverted form: every N/A disposition below quotes the READER that decides it), each mechanism was read in `src/lib/renderers/field_3d_renderer.ts` this session:

1. **The `h = 0` line and the U numeric read ONE field — verified; the dispatch's claim is true.** Config type `energy_layer.h_ref_m?: number` L1967 (comment L1958–1966: metres of height above the surface origin, default 0, **the stack is UNSIGNED — author at or below the lowest point the tracked body reaches, or U_grav goes negative, the drawn column stops equalling its own numeric, and the engine warns**). Resolver L50937–50938 writes it once into `eng.energy_h_ref_m`. Reader 1 — the U numeric: `nlbHeightM` L48240–48244, h = b.s·sin θ − ref (hanging bodies negate, L48242; none authored here). Reader 2 — the drawn line: L49884–49898, y = (eng.energy_h_ref_m || 0)·NLB_WORLD_PER_M, label `cfg.h_ref_label || "h = 0"`. Reader 3 — the predicted-stop caption's h readout: L49908 subtracts the same field. One authored value, three consumers, zero ways to disagree.
2. **The negative-U guard is a live console warning THE EYE's H4 fails on.** L48946–48955: "U_grav went negative (…): energy_layer.h_ref_m is … Lower h_ref_m to the h = 0 line the state actually shows." Every state below therefore proves h(t) ≥ h_ref for its ENTIRE loop including tails, by arithmetic in §10(d).
3. **Track bounds extend BELOW the origin**: `nlbBoundsM` L51635–51653 returns {lo: −lenM, hi: +lenM} for a non-hanging body. A body descending an incline can reach negative h — this makes the S2/S5 loop-end arithmetic (§10d) and the explore no-drag decision (§0A.9) binding, and it is why "the cart halts at the floor" is NOT an available design.
4. **Home-armed checkpoints + the return dwell are purpose-built for the round-trip beat.** Arming L50410–50415: a body seeded exactly ON a flag sets `_home = true`, `_side` from v₀'s sign; the `_home` branch L50434–50438 lets the first DEPARTURE be pass 1 without a race (the CRITICAL home-pose scars are FIXED and this contract is their fix). Firing L50440–50449; `capture_mode:'first'` early-return L50443. Dwell scheduling L50457–50480: gated on `cp._count >= cp.dwell_from_pass`, carved out of sandbox and seized scenes; config comment L2111–2117 states the use case verbatim: "dwell_from_pass: 2 puts the pause on the RETURN crossing." `NLB_DWELL_MAX_MS = 5000` (L49107) — all dwells below are 1200/1400 ms.
5. **The stamp string, quoted from its emitter** (per the `nlb_checkpoint_W_capture_renders_the_work_bar_label…` directive): `nlbCpStampText` L50551–50595. Format: `<label>:  U = <x> J`; head gains " (pass N)" only for `capture_mode:'every'` with `_count > 1` (L50594). `U_grav` interpolates to the crossing via ugX = m·G·(h_m + dh), L50566–50568 — it reads h_m, which `nlbHeightM` already measured from h_ref, so **stamps respect the authored zero line** (load-bearing for S3). `capture:['W']` emits ONE clause PER accumulator, "W <label> = <x>", rewound by (1−f)·_dW (L50585–50590). **Default capture is ['K','U_grav'] (SEAM M contract) — every checkpoint below AUTHORS `capture` explicitly, because a stamped K would import kinetic energy this concept deliberately never shows.**
6. **`eye_capture_ms` is honoured, from the state-config TOP level.** `visual_eyes.ts` L115–123 and L217–231 read `field_3d_config.states[sid].eye_capture_ms` (the "nested one level too deep" trap is a recorded scar — renderer comment L62543). Shipped clone evidence: `work_energy_theorem.json` L590/L650, `conservative_vs_nonconservative_forces.json` L666/L697/L755.
7. **Contextual-control tokens are PLAIN STRINGS on this scenario** — L1819–1824: 'm'|'m2'|'F'|'F_ang'|'theta'|'mu_s'|'mu_k'|'v0'|'R'|'R2'|'omega0'. **No per-control `min_ring` shape exists on `newtons_laws_body`** (that shape lives on other scenarios, e.g. L1323). The `explore_controls_not_ring_gated_survive_the_ring_cut` directive is discharged by design in §10(i): the explore exposes 'm' and 'theta', whose lessons (U ∝ m at fixed h; h drives U — both S1 core) survive every ring cut.
8. **`idle_auto_sweep` is CLOSED to param 'F'|'theta'|'m'** (L2195). The explore authors {param:'theta', range:[30,40]} — a member; range[0] = 30 = the authored θ, so the first frame snaps NOTHING (the engine starts the sweep at range[0] at t = 0, L47646–47649 — founder_proxy P2-5 discharged at the root).
9. **The explore state deliberately authors NO `trusted_drag_seizes` — and the engine limit is credited with removing DRAG ONLY (re-filed, founder_proxy P1-5).** Drag clamps only at `nlbBoundsM`'s lo = −length_m (L51653): a teacher could drag the cart to h = −length·sin θ, driving U below every authorable h_ref and firing the L48946 warning while the unsigned column clamps at 0 against a negative numeric (§0A.1). That blocks BODY DRAG and nothing else. The rest of the sandbox is NOT the limit's coattails: `'theta'` is a live token (`controls_visible` enum L1819; `NLB_SLIDER_SPEC.theta` L47127, per-concept overridable via `slider_controls` → `nlbSc` L47160–47181), `nlbApplyParam('theta')` writes `eng.theta_deg` AND calls `nlbApplySurface` (L47361–47363), and `nlbHeightM` reads it live — so S6 authors BOTH `m` and `theta` (bounded 15–40°): the θ sweep/slider rotates the ramp with the cart riding it. Token-by-token audit of the remaining enum members: `m2`/`R`/`R2`/`omega0` — no second body / no rotation authored, off-concept; `F` — a drive on a rest sandbox moves the cart toward the drag problem and teaches nothing about U = mgh; `F_ang` — no applied force exists; `mu_s`/`mu_k` — friction is scaffolding holding the cart still, not a term of U = mgh, and exposing it invites an uncommanded slide. Body drag stays the honest engine residual (EQ-1, ride-along — this design does not depend on it).
10. **`height_markers.predicted_stop` recomputes per frame and hides on a flat surface or unlaunched body** (SEAM M contract; drawn L49902–49913). Authored ONLY in S2 (the one launch state) — per `architect_reuses_a_marker_mechanism_without_diffing_the_side_effects_its_presence_switches_on`, the side-effects of every presence-gated block used here are diffed in the ENGINE FIT CHECK.
11. **Loop reset wipes and re-arms checkpoints and ledgers** — re-verified this session (L48409 `_home` re-arm; L50410–50415 re-adoption); SEAM M's `RESET_TRAJECTORY` DOM-diff-zero proof (ch6_state.md) is the measured evidence. Rewind-trace for every claim below terminates at: `nlbCpArm` re-adopt (L50410–50415) · ledger zeroing + `formula_base` restore (SEAM M report) · `sum_merge`/`param_ramp` N/A (none authored).
12. **Energy panel + instruments contract** (ch6_state.md SEAM L/M RESULT, authoritative): `bars` CLOSED 'K'|'U_grav'|'U_spring'|'E_total'|'E_dissipated', fixed panel order, `bar_max_J` REQUIRED shared linear scale (no rendered ceiling — scar disposition #1), `precision` 0|1|2 default 1, glow ids incl. `energy_bar_U_grav`. `work_accumulators` CLOSED 'gravity'|'friction'|'applied'|'normal'|'net', signed bars, single-token labels (CALCULATOR sibling-composition scar). Engine-fixed bar symbol is `U` — the painted caption every `computed_outputs` key must byte-match (`U_J`, `W_J` pattern per the naming scar).

**Engine bug queue consulted live this session (Bash, read-only):** `--scenario newtons_laws_body --open` (76 rows, all read) · `--scenario newtons_laws_body` (143 rows; FIXED list read) · `--row-type directive` (110 rows; headers walked, every architect-owned body read) · `gravitational_potential_energy` (1 inherited row). Coverage boundary, enumerated per `scar_audit_claims_a_coverage_boundary_it_did_not_enumerate`: those four queries — rows filed after 2026-08-09, or under other scenarios/owners only, are outside it. Rule 40a `git log -S`: N/A — zero engine mechanisms specified (pure JSON).

## 0B. SCAR DISPOSITION — 76 OPEN scenario rows + architect directives + re-introducible FIXED rows

**Legend:** AVOIDED = the design cannot exhibit it · MITIGATED = constraint stated and carried · ACCEPTED = residual named, bounded, flagged · N/A = cannot apply, with the reader/design fact that decides it.

### The two rows the dispatch aimed at this concept's instrument

1. **`energy_bar_track_renders_no_scale_ceiling_so_two_states_draw_one_value_at_two_heights` (MODERATE/OPEN)** — MITIGATED by obeying its DO clause exactly: **`bar_max_J = 60` in ALL SIX states, explore included.** This concept's whole claim ("same height ⇒ same U"; "same ΔU under a moved zero") is carried by cross-state equal NUMBERS (44.1 J at S1 point B and S4's point B; ΔU = 29.4 J in S1 and S3), and under one shared scale equal values render equal fills by construction. The explore envelope was sized DOWN to fit the shared scale (worst corner 35.3 J, §10d) rather than varying the scale. The engine-owed ceiling label is NOT requested — zero renderer edits.
2. **`nlb_overlay_ink_lift_is_bounded_to_the_families_whose_length_is_a_magnitude` (DIRECTIVE/OPEN)** — HONOURED: the h = 0 dashed line and the point markers are position-meaning ink and correctly KEEP depth testing (the body hides a point it stands on — config comment L2118–2131 says exactly this). No DoD row asserts the line or a marker draws through the cart; no ink-lift widening is requested.

### The four architect-owned OPEN directives named in the dispatch

3. **`nlb_dwell_state_authored_without_recomputing_loop_reset_and_eye_capture`** — AVOIDED: the full (a)–(d) arithmetic is done by hand per state in §10(d) (T_k with prior-dwell sums, loop_reset_ms ≥ T_last + D_last + settle, eye_capture_ms = T_P + D_P/2, re-derivation duty). Zero [PM_NLB_DWELL] console lines is a named EYE assertion.
4. **`nlb_frozen_pin_lands_within_one_frame_of_the_beat_the_dod_asserts_it_shows`** — AVOIDED: every frozen-frame DoD states its millisecond margin; all captures sit mid-dwell with ≥600 ms on BOTH sides (≥36 frames, above the 167 ms floor); §10(d) computes event times from the authored physics and notes the ≤2-step discrete-event allowance.
5. **`rewind_path_assertion_stops_at_the_function_body_and_never_follows_its_calls`** — AVOIDED: §0A.11 traces reset claims to their terminating lines; no state authors `param_ramp`, so the ramp-boundary probe is vacuous here.
6. **`architect_declares_an_engine_limit_without_checking_the_per_concept_override_path`** — SATISFIED: every declared limit quotes type + reader (§0A.1/3/7/9); every N/A names the reader or a grep-able design fact.

### OPEN rows dispositioned individually (scenario + fleet directives)

7. `nlb_checkpoint_s_m_authored_as_displacement_not_track_coordinate` — AVOIDED: every s_m in §3 is written `initial_position_m + d`, and each state's home paragraph states `initial_position_m`.
8. `nlb_static_state_authored_on_the_track_bound_fires_a_false_clamp_alarm` — AVOIDED: all seeds |s₀| ≤ 2.8 < 3.95 = length_m − 0.55.
9. `nlb_work_bar_zero_crossing_reading_is_unrenderable_at_teaching_speed` — AVOIDED: every narrated/captioned reading is a LATCHED interpolated stamp, never a moving-bar instant. Per-frame ledger step computed anyway: S5 worst |F_along|·v·h = 12.25·4.22·0.0167 = 0.86 J > 0.1 J display (v = 4.22 m/s at the shortened loop end) — exactly why no moving-bar value is asserted anywhere.
10. `nlb_loop_reset_clears_checkpoint_stamp_and_frozen_pin_can_photograph_an_empty_formula` — MITIGATED: every checkpoint state authors `eye_capture_ms` inside a dwell window whose stamp is already on screen (§10d); the 0.60R default pin is never relied on.
11. `nlb_frictionless_state_with_an_opposing_applied_force_reverses_and_unwinds_its_own_work_ledger` — AVOIDED: S1/S3/S4's applied force AIDS the motion; S2/S5 author no applied force; NO guided state exposes a slider (§3), so no seized run can outlive a loop.
12. `nlb_sandbox_energy_envelope_computed_from_the_seed_lap_instead_of_the_full_wrap_span` — AVOIDED: the explore body is friction-held at rest (μs = 0.9 > tan 30° = 0.577) with no drive and no drag → no wrap exists; envelope computed over the full slider cross-product incl. θ (§10d): U_max = 3·9.8·2.4·sin 40° = 45.4 J ≤ 0.9·60 = 54 ✓ (m 3, θ 40 corner).
13. `nlb_sandbox_default_mu_below_tan_theta_drifts_the_block…falsifies_its_own_claim` — AVOIDED: μs = 0.9 > tan 40° = 0.839 at the slider/sweep maximum (static-hold limit θ = 41.99°); the standing claim U = mgh is a state function, true at rest and in every steady state; the 20 s default-drive check is handoff probe P3.
14. `nlb_sandbox_work_ledger_has_no_provably_safe_work_scale…` — AVOIDED: the explore authors NO `work_accumulators` (Rule 38b: explore surfaces core content only; the ledger is advanced ring).
15. `state_end_of_loop_energy_numbers_derived_at_a_different_t_than_the_travel_table` — AVOIDED: §10(d) derives s, h, U (and W where authored) at the SINGLE t = R per state; scales set from that set with ≥10% headroom.
16. `worked_loop_resize_patch_bounded_at_the_pin_instant_instead_of_the_loop_end` — **RECURRED at cycle 0, now DISCHARGED (founder_proxy P1-4; recurrence CITED, no sibling row minted).** Cycle 0 bounded every extreme at t = R exactly and dropped the one-frame-slack the row's DO explicitly requires; at a 3-step folded frame S5's tail overran `work_scale_J` (+30.20 > 30), firing the very `[PM_NLB_ENERGY_*]` warn §10(d) names as an H4 assertion. Cycle 1: §10(d) re-derived in FULL with a worst-folded-frame (R + 50 ms) column; S5's loop shortened to R 2900 (W folded +12.7 ≤ 27); every state's folded values inside bounds, with S4's 53.81 J vs the 54 discipline line ACCEPTED and documented in the §10(d) note.
17. `nlb_multibody_lane_gap_is_along_z…` · `energy_layer_two_body_groups_stack_vertically…` · `the_eye_passes_a_frame_in_which_one_compared_body_is_hidden…` · `nlb_displacement_vector_is_single_body…` · `nlb_work_probe_globals_disagree_on_multibody…` · `nlb_multibody_sandbox_wrap_reanchors…` · `skeleton_authors_a_multi_phase_state_on_an_engine_with_no_per_body_activation_time` · `nlb_lane_offsets_apply_to_declared_bodies…` · `skeleton_asserts_a_held_comparison_value_at_an_instant_the_faster_body_has_already_passed` — **N/A by design commitment: every state authors exactly ONE body** (`bodies.length === 1`, a grep-able DoD row). The compare beats are sequential contrast pairs, never lanes. (The lane machinery counts DECLARED bodies — which is why this is written as a declared-count invariant, not a "never two in frame" claim.)
18. `nlb_angle_arc_to_displacement…` · `nlb_angle_arc_radius_overruns…` — N/A by design: no `angle_arc` authored (the angle in U = mgh is carried by the ramp itself).
19. `nlb_motion_archetype_declared_from_a_between_state_delta_or_a_teacher_driven_control` — AVOIDED: every archetype in §3 names an authored in-state motion needing zero teacher input; the reference-shift and slope-shift are DELTA CUES on declared contrast pairs, never archetypes.
20. `authored_beat_ends_by_undoing_the_state_own_claim` — AVOIDED: §3 declares termination per state (all guided beats LOOP; latched stamps carry each claim through its tail; S5's tail actively CONFIRMS its relation — U drains exactly as W rises).
21. `taught_delta_smaller_than_the_instruments_own_live_noise` — AVOIDED: U is a pure function of position (§0A.1 — no clock, no history), so the bar has zero live noise at fixed s; smallest taught delta = 8.6 J (S5's stamped ΔU) on a 0.1 J display.
22. `shared_bar_scale_cross_state_guarantee_is_void_when_the_panel_reflow_ladder_drops_a_step` + `the_eye_captures_a_fixed_720px_sim_height…` + `one_measured_viewport_recorded_as_the_invariant…` — MITIGATED: panel content is IDENTICAL across S1/S2/S3/S4/S6 (one U_grav bar, engine-fixed single-token caption `U`); S5 alone adds the one work bar, and NO cross-state pixel comparison involves S5 (its claim is internal + stamped). Teacher iframe height is a RANGE (~551–911 px, measured table in ch6_state.md); THE EYE captures at 720; no DoD references a reflow rung.
23. `nlb_checkpoint_W_capture_renders_the_work_bar_label_so_the_authored_stamp_string_never_ships` — MITIGATED + MEASURED: stamp strings in §3 are quoted from the emitter (§0A.5), one W clause per accumulator; two-clause stamps wrap at the CLAUSE boundary (the mid-clause-wrap row is FIXED). Busiest surfaces are now S5 (base + 1 formula line + 2 two-clause stamps — up to ~6 rendered lines) and S1/S3 (base + 1 formula line + 2 one-clause stamps); fit against the 340 px literal at each pin is probe **P7** (measured, never asserted).
24. `reviewer_accepts_a_residual_on_an_inferred_visual_premise_instead_of_measuring_it` — MITIGATED: the one "two readings at the same instant" claim (S5's stamped U and W) shares a SINGLE stamp clause group (one provenance, §0A.5); the live-bar-vs-stamp gap during dwells is a named Checkpoint-B measurement duty (P4), not closed by arithmetic.
25. `nlb_dwell_holds_one_step_past_the_crossing_so_the_live_bars_still_trail_the_stamp` (MODERATE/OPEN, engine) — **ACCEPTED, bounded per state**: during a dwell the live U bar may trail the stamp by ≤ one integrator step = m·g·sinθ·v·h → S1/S3 0.20 J · S4 0.29 J · S2 0.64 J · S5 0.35 J (v = 1.71 m/s at its s = 3.5 flag) — 1–2 display tenths worst-case except S2's ≈0.6 J ≈ 1.9% of the stamped value. Narration and captions cite STAMPED values only; residual flagged to founder-proxy with these numbers (P4 measures them).
26. `calculator_dom_harvest_needs_symbol_and_value_in_ONE_text_node…` (this concept's 1 inherited row) — ACCEPTED (engine-owed): panel readings will SKIP; the checkpoint stamps (single text node in `#nlb_formula`) are the harvestable channel and every state's key number is stamped. *A skip is not a pass* — the numeric:calc duty reads the per-state reading list, never the headline.
27. `multi_word_bar_caption_is_invisible_to_the_calculator…` — AVOIDED: the only authorable caption here is the work label — 'gravity', single token.
28. `computed_output_name_encodes_a_symbol_no_instrument_paints…` + `computed_output_divides_by_a_coefficient_whose_default_is_zero…` + `prose_in_a_variable_derived_field…` — MITIGATED: physics-author constraints in §Handoff (keys matching painted symbols; `derived` = expression slots only; every formula finite under declared defaults — no μ division anywhere).
29. `authored_state_glow_focal_silently_voids_every_tts_sentence_glow…` + `state_glow_focal_dims_one_half_of_the_relation…` + `nlb_body_label_is_brighten_only…` + `field3d_focal_glow_pulse_phase_reads_absolute_time…` — AVOIDED: **ZERO state-level `glow_focal` anywhere** (every guided state's claim is a relation — bar↔height, stamp↔stamp); per-sentence SET_GLOW bindings drive emphasis in narration order, and the dwell's own marker brightening (config comment L2066–2075) is the crossing signal. Rule 32e satisfied (≤1 focal at any instant).
30. `concept_ships_zero_narration_glow_bindings` — MITIGATED: handoff duty — every tts sentence carries a glow naming an element existing in that state (`energy_bar_U_grav`, `marker_h_ref`, `checkpoint_1..3`, `energy_panel`, `displacement_vector`).
31. `explore_state_discoverables_authored_only_as_unrendered_scene_composition_annotations` — AVOIDED: S6's discoverables live on rendered paths (state label, formula surface, slider row); Rule 19 counted against DRAWN primitives (§10a).
32. `ramp_endpoints_multiply_the_taught_variable_by_a_factor_no_rendered_string_claims` + `field3d_param_ramp_authoring_contract` — N/A by design: no `param_ramp` authored.
33. `taught_variable_has_no_rendered_physical_correlate…` — AVOIDED for S1–S5 (U's rendered correlates: the cart's HEIGHT above the drawn line + the bar fill, both monotone in it). **Cycle 0's blanket claim was FALSE for S6** (mass-only sandbox: the only geometric response was an arrow length — founder_proxy P1-5); cycle 1 fixes it at the root: the θ slider/sweep ROTATES the ramp and the cart rides it, so the concept's middle term h has a moving physical correlate in the explore.
34. `quantitative_check_state_reuses_the_exact_numbers_and_the_delta_cue_of_the_state_it_verifies` — DECLARED EXCEPTION: S4 re-renders 44.1 J DELIBERATELY — its entire claim is that this ONE number is unchanged while θ, d and F all visibly differ (θ 30°→48.6°, d 2.4→1.4 m, F 14.7→22.05 N). Its delta cue shares no phrase with any sibling; no (m, v, U) triple is duplicated. Flagged for founder-proxy. **Cycle-1 addendum — the full cross-state NUMERAL cross-tab the triple-check cannot do (P2-1):** every authored/latched rendered numeral, with its (m, height-above-ref) tuple: 8.8 J (S1 open, 3/0.3 — unique) · 14.7 J (S1-A, 3/0.5 — unique as a stamp; the F = 14.7 N value is never rendered as a numeral, arrows are symbol-labelled per §10b) · **44.1 J (S1-B 3/1.5 and S4 3/1.5 — SAME tuple, the declared teaching identity)** · **29.4 J (S1 + S3 formula line — same quantity mgΔh, 3 kg/Δh 1.0, the held invariant)** · 5.9 / 35.3 J (S3, 3/0.2 and 3/1.2 — unique as stamps; 35.3 is reachable in S6's live dial at m 3, θ 30 = the SAME 3 kg/1.2 m tuple, benign) · **34.3 J (S2 both passes 2.5/1.4 and S5 start 2.5/1.4 — same tuple, benign and declared)** · 13.2 J (S4 open, 3/0.45 — unique) · **42.9 / −8.6 / 8.6 J (S5 — the cycle-0 flag at s 3.6 stamped 44.1 J at (2.5 kg, 1.8 m), COLLIDING with the taught 44.1 ≡ (3 kg, 1.5 m); the flag is MOVED to s 3.5, killing the collision)** · 1.90 m / 2.40 m / 1.40 m (S2 predicted stop; S1/S3 d; S4 d — distinct meanings, distinct values) · 23.5→30.2 J (S6 sweep — a live continuum, exempt by nature and labelled by its own sliders). No numeral is shared by two states whose tuples differ.
35. `frozen_pin_unbudgeted_on_a_sequential_misconception_state…` — N/A by design: both 16a confrontations are BETWEEN-state contrast pairs; no state contains a wrong-picture-then-right-picture sequence, so no pin can archive a wrong half.
36. `skeleton_anchor_specified_in_section_9_reaches_no_narration_line` — AVOIDED: anchor sentences quoted in §9 AND inside the S1/S4 narration constraints, within budget.
37. `teach_do_not_prespoil_a_later_reveal` — AVOIDED: the ΔU form first appears in S3; the work ledger in S5; `predicted_stop` only in S2; the explore surfaces only what core states taught.
38. `teach_concrete_before_abstract_compare` — AVOIDED: S1 is the concrete single case; every compare arrives after its simple case ran alone.
39. `teach_visual_must_match_narration` · `teach_show_quantity_live_when_named` · `teach_reveal_synced_to_narration` · `teach_color_each_element_by_its_own_sign` — physics-author duties restated in §Handoff (every narrated agent/quantity is a rendered object/number; the S5 signed bar already colours by sign).
40. `prior_rulings_and_internal_ledgers_are_not_re_run_over_the_states_a_restructure_creates` — carried: any Checkpoint-A restructure re-runs §0B and the §10 ledgers over the NEW state list, stated here so the duty survives the cycle.
41. `close_camera_framed_extent_is_authored_as_the_run_length_and_omits_the_body_radius` — MITIGATED: framed extent per state = run + 0.55 m half-width each side (§10d); camera = the fleet `block_on_incline`/#5 convention (clone evidence), held fixed S1–S6.
42. `architect_authors_a_force_triangle_whose_ratio_exceeds_the_renderer_arrow_maps_dynamic_range` + `field3d_nlb_arrow_min_length_floor…` — AVOIDED: only two arrows ever drawn (F 14.7–22.05 N, mg 24.5–29.4 N); max ratio 2.0, min magnitude 14.7 N — inside the band #1–#5 shipped at.
43. `velocity_arrows_routed_through_a_force_arrow_map…` — N/A by design: no velocity arrow authored.
44. `nlb_friction_vector_first_frame_reveal_tint…` — AVOIDED: guided states are frictionless; the explore's static-friction arrow is NOT drawn (arrows: mg only there).
45. `checkpoint_flag_post_depthtest_false_occludes_the_tracked_body…` + `nlb_marker_captions_absent_from_the_body_label_obstacle_set` + `nlb_marker_caption_corner_nicks…` + `field3d_sprite_label_text_and_ink_box_are_invisible_to_every_dom_probe` + `field3d_arrow_label_sprite_renders_at_under_half…` — MITIGATED: every checkpoint authors `marker: 'point'` (the founder-preferred depth-tested form); captions short ("point A", "point B", "start height"); transit-frame corner nicks are the queue's accepted bounded residual (engine-owed); sprite-geometry verification is the projection-probe class, named for the review chain.
46. `skeleton_pin_budget_names_the_phase_start_instead_of_the_last_asserted_reveal_inside_it` — AVOIDED: every `eye_capture_ms` anchors on the LAST asserted reveal inside its window — the stamp (S2/S4) or the later formula LINE (S1/S3: line 4400, pin 4800; S5: line 600, pin 989) — mid-dwell, margins in §10(d).
47. `derived_readout_asserted_by_value_without_defining_its_metric` — AVOIDED: every asserted number carries unit + display precision (J at 0.1; m at 0.01; ms integer).
48. `gate_headline_counts_fold_skips_into_passes…` · `baseline_set_whose_two_captures_of_one_pinned_instant_disagree…` · `eye_h2_frozen_frames…wobble…` · `frozen_frame_read_as_dense_series_continuation…` · `state_rewrite_updates_the_state_block_and_leaves_every_other_field…` · `review_site_build_is_stale…` — review/ship-chain duties, named in §Handoff so they are inherited, not rediscovered.
49. `field3d_dt_accumulated_motion_invisible_to_eye_timepin` + `field3d_integrating_scenario_state_entry_must_rebuild_the_whole_engine_record` — ACCEPTED (engine-verified): U is closed-form in s (§0A.1); the S5 ledger is SEAM M's own accumulator whose entry-rebuild + reset determinism carries a measured DOM-diff-zero proof (ch6_state.md SEAM M RESULT); no NEW accumulator is authored.
50. `skeleton_designs_around_an_engine_limit_a_landed_build_already_removed` — AVOIDED: every limit cited was read in the CURRENT tree this session (§0A), none inherited from seam reports alone.
51. `state_survives_a_downstream_author_withdrawing_the_rationale_it_was_added_for` — carried: each §3 row names its rationale; if physics-author withdraws one, the state falls with it (report back, never keep the state).
52. `uniformity_fix_moved_every_overlay_across_a_documented_chrome_clearance_threshold` + `nlb_formula_and_readout_zones_are_fixed_css_and_collide_with_a_tall_hud` — MITIGATED: no `readouts` authored on any state (the panel, formula surface, markers and h-line are the instruments); §10(h) reserves zones per Rule 34d.
53. `derivation_principle_applied_to_one_beat_but_not_its_sibling` — AVOIDED: the checkpoint-stamp treatment and the marks-earn-their-place rule are applied symmetrically to every measured beat (§3 note).

**Grouped as genuinely irrelevant** (single line, per the dispatch): all `pcpl_*`/parametric rows · `drill_down_*` · chemistry-scenario rows · rotmech/rolling rows beyond the directives dispositioned above · spring rows (no spring) · pulley/coupled/train/hanging rows · `momentum_bench`/`force_rig`/EM/`kt_*` rows · new-scenario bring-up rows (`field3d_generic_visible_elements_matcher…`, `derivestatemeta_new_scenario_key_absent…`, `new_scenario_buy_list…`, `named_primitive_declared_without_the_surface…`, `closed_enum_cannot_name_a_substance…`, `phase0_*`, `enum_gap_audit…`, `signed_engine_union…`, `two_skeletons_sharing_one_engine_build…`, `skeleton_authors_a_second_timed_action…`, `engine_extension_replaces_a_shared_constant…`, `state_added_at_review_outruns_the_config_contract_shape`, `skeleton_authors_a_timed_reveal_chain…`, `call_site_enumeration…` — this concept buys NOTHING from the engine) · `teach_*` rows bound to other scenarios' geometry (`…grab_and_move_field_point`, `…inverts_cutline_flags`, `…distinct_reference_lines_for_two_radii`, `teach_coordinate_sim_with_graph` — no graph panel exists in this concept) · validator-owned gate rows not touched by authoring (`rule31_motion_floor_satisfied_by_a_late_label_reveal…` — every state here moves a BODY · `narration_timing_probe…` · `teach_auditor_reads_field3d_sliders…` · `teach_read_dense_ramp_frames…`) · `engine_stash_on_shared_renderer…` · `optional_config_field_with_a_legal_zero_value…` · `refused_display_override…` · `camera_solve_searched_in_one_axis…` · `explicit_linear_drag…` · `spec_semi_implicit_euler…` · `field3d_build_once_body_reads_a_per_state_flag…` · `seam_q_hovering_stroke…` · `seam_r_ink_lift…` · `equal_magnitude_force_arrows…` (no equal-magnitude pair authored) · `nlb_camera_rotated_body_label_bleed…` (no camera drag exposed) · `calculator_n3_slider_check…` (no accumulated readout under a slider) · `calculator_harvester_pairs_a_symbol_from_one_checkpoint_stamp_with_another` (single-clause provenance here) · `nlb_work_bar_glow_ids_never_light…` (no work_bar_* focal authored) · `field3d_energy_group_comment…` (doc row) · `ramp_threshold_asserted_on_the_unrounded_value…` (no threshold ramp) · `authored_lumped_constant…` (none) · `nlb_work_bar_track_tops_lose_collinearity…` (single U bar; S5's one work bar has no sibling track to misalign against) · `nlb_body_screen_anchor_lands_under_the_dom_slider_panel` (guided states show no sliders; S6's cart sits mid-track) · `directive_no_gate_asks_whether_a_teacher_could_use_it` (meta row).

### FIXED rows this concept could RE-INTRODUCE — the constraint that keeps each fixed

| FIXED row | Authoring constraint honoured here |
|---|---|
| `checkpoint_capture_mode_first_at_the_home_pose_stamps_the_departure_reading…` (CRITICAL) + `nlb_checkpoint_at_the_body_home_pose_adopts_its_side_in_a_race` | TWO home-pose flags, both on launched bodies (v₀ = +3.13 — never author a home flag with v₀ = 0): S2's is `'every'` + `dwell_from_pass: 2` (the fix's own stated use case); S5's is `'first'` + `capture:['U_grav','W']` and RELIES on the fixed contract deliberately — departure = pass 1, so it latches U₀ = 34.3 / W = 0.0 from the first frames, and the `'first'` early-continue (L50443) makes every later recross (cp descent recross wall 2389, home recross wall 2678) a no-op. Probe P1 measures both flags. |
| `nlb_checkpoint_capture_overshoots_exact_crossing_value` (CRITICAL) | All stamped quantities (U_grav, W) are on the interpolated path (§0A.5); display-exact stamps are therefore claimable and ARE claimed. |
| `nlb_latched_checkpoint_stamp_and_the_live_work_bar_report_one_symbol_at_two_values_with_no_time_qualifier` | The live-qualifier caption ships with the panel; narration cites stamps only (disposition #25). |
| `nlb_word_body_label_overprints_the_state_focal` (CRITICAL) | Body label is the SYMBOL form `m = 2.5 kg` / `m = 3 kg` (also re-satisfies `nlb_hud_and_body_labels_never_show_mass`), never a word. |
| `nlb_legend_renders_the_state_label_prose_on_canvas` | No legend/prose on canvas; caption = the ≤5-word delta cue only (§10h). |
| `geometric_track_clamp_rendered_as_an_energy_change` + `nlb_uncoupled_readouts_flip_to_static_on_bound_halt` | No authored loop ever touches a bound (§10d margins), so the clamp path is never exercised as an energy event. |
| `nlb_work_bar_tracks_misalign_when_one_caption_wraps…` + `nlb_formula_surface_max_width_is_a_340px_literal…` + `nlb_formula_surface_wraps_mid_clause…` | Single-token bar captions; longest stamp ≈38 chars; max 2 stamps per state (S1/S3), each its own clause line. |
| `nlb_seized_slider_run_overruns_a_loop_sized_work_scale` (json_author) | No guided state authors a slider; the explore authors no ledger. Nothing can seize past a scale. |
| `nlb_signed_bar_half_range_breaks_equal_value_pairing_with_an_unsigned_bar` (physics_author) | S5 pairs U (unsigned, 60 J) with W (signed, ±30 J) but authors NO equal-height claim between them — the ΔU = −W equality lives in one stamp clause. physics-author must not add a bar-height pairing sentence. |
| `nlb_signed_launch_velocity_state_never_renders_K0…` (physics_author) | The delta arithmetic's baseline (U₀) is on screen from frame 1 (the bar reads 34.3 J at t = 0); K is never rendered or stamped anywhere (capture always explicit, §0A.5). |
| `nlb_checkpoint_dwell_does_not_refire_after_a_loop_rewind` | Relied on as fixed: every loop re-dwells; zero [PM_NLB_DWELL] in THE EYE console audit is the regression tell. |
| `checkpoint_marker_labels_collide_when_two_checkpoints_sit_near_in_world_space` | S1/S3's two points sit 2.0 m apart (world) — the fixed de-collision's easy case. |
| `nlb_sandbox_wrap_remaps_s_but_not_s0…` + `nlb_sandbox_wrap_reseeds_v0_toward_the_bound…` | Explore has no motion and no wrap (rest + slider-only); the fixed paths are never exercised. |
| `confusion_cluster_registry_unseeded_for_concept` | Known auditor false-FAIL on new conceptual-only concepts — N/A-DORMANT; do not route json-author to "fix" it. |
| `classifier_prompt_drift…` / `production_routing_disconnect…` / `stale_fingerprintkey…` / `default_variables_only_first_var_merged` / `fetch_technology_config…` / `review_site_build_is_stale…` | Registration-site + rebuild duties restated in §Handoff (8 sites; cache re-seed before THE EYE; rebuild review site before reading frames). |

## 1. Atomic claim

This concept teaches ONE idea: **near the Earth's surface a body's gravitational potential energy is U = mgh measured from a height you CHOOSE to call zero — the choice moves every U value but never the difference ΔU = mgΔh, and that difference depends only on the start and end heights, not on the path between them.** It does NOT teach the general definition of potential energy or ΔU = −W_conservative (that is `potential_energy_definition`, #6 — S5 cites it as taught), does NOT teach the spring form (#8), does NOT convert U to K or track a moving total (#9), does NOT treat friction losses (#10), and does NOT touch the general −GMm/r form (a later chapter; the h ≪ R_E caveat is one advanced-ring narration clause, never a formula).

## 2. State count + arc — 6 states (5 guided + 1 explore)

Medium concept (§5 calibration 5–6) — justified at 6: one canonical picture (a cart on a ramp with a U bar and a zero line) plus THREE independent invariances that each need their own beat — same place ⇒ same U (state function), moved zero ⇒ same ΔU (the PRIMARY aha), different slope ⇒ same U (path independence) — plus the #6-relation derivation (advanced) and the explore. Fewer fuses two invariances into one state (two ideas); more would pad (energy conversion belongs to #9).

| # | id | Ring | Purpose (one line) | teaching_method |
|---|---|---|---|---|
| S1 | `lifting_stores_energy` | core | A cart driven up a ramp at constant speed: the U bar fills exactly as the cart's height above the dashed h = 0 line grows; two point-stamps make U = mgh a number twice | (straightforward beat) |
| S2 | `same_height_same_energy` | core | A launched cart rises and comes back: the home flag stamps the SAME U at departure and at return — U belongs to the place, not to the trip | (straightforward beat) |
| S3 | `the_zero_is_your_choice` | core | **PRIMARY aha** — the identical climb with the h = 0 line MOVED to the start height: every U value changes (the bar opens EMPTY), the stamped ΔU = 29.4 J does not | misconception_confrontation |
| S4 | `different_slope_same_U` | extended | A steeper ramp to the SAME height: the measured path d is shorter, the stamped U is the identical 44.1 J — U depends on height, not on path | misconception_confrontation |
| S5 | `U_and_the_work_by_gravity` | advanced | S2's launch with gravity's work ledger and BOTH endpoints LATCHED (start 34.3/0.0 · point C 42.9/−8.6): the subtraction is on screen — ΔU = −W by gravity holds under ANY zero line, closing #6's relation through S3's choice and deriving U = mgh | derivation_first_principles |
| S6 | `explore` | core (explore) | Sandbox: mass AND angle sliders (θ idle-sweeps, ROTATING the ramp with the cart riding it, until touched) drive the U bar live; U = mgh is the standing surface | exploration_sliders |

Rule 38a ordering: qualitative→quantitative (S1–S3 core) → extended (S4) → advanced contiguous immediately before explore (S5) → explore (S6, core content only). The hook MOVES from frame 1 of S1 (the cart is already climbing). Advance modes: S1–S5 `manual_click`, S6 `interaction_complete` (Gate 12 ✓). No `wait_for_answer`, no `pause_after_ms`, no `narrative_socratic` anywhere.

## 3. Per-state choreography + control table (Rule 31 — REQUIRED artifact)

**Home pose (Rule 32d — permanent, never rebuilt):** one incline, `surface.theta_deg: 30` (S4 alone rotates it to 48.6° — that rotation IS S4's delta), `surface.length_m: 4.5`, `frictionless: true` on S1–S5; ONE body (`id: "cart"`, block shape, billboard `m = <mass> kg`). g = 9.8. The SEAM L energy panel sits at the screen left edge with the single U bar (`bars: ['U_grav']`, `bar_max_J: 60`, `precision: 1` — identical in all six states); the dashed h = 0 line + label spans the scene (`height_markers.show_h_ref_line: true` in every state); ONE formula surface top-centre; no `readouts`. Arrows: F (applied drive) + mg on S1/S3/S4; mg only on S2/S5/S6. `displacement_vector {show_value: true}` on S1/S3/S4 — the d numeral is part of the home picture the S4 compare reads against (P1-2); hides below 0.02 m, so first frames are clean. ONE camera S1–S6 (fleet `block_on_incline`/#5 framing — clone evidence; framed extent incl. the 0.55 m body half-width, §10d). Cause-before-effect (32a): the drive arrow/launch motion is visible first; the bar and stamps respond to the cart's height. **Zero state-level `glow_focal`** (§0B.29); the dwell brightens its own marker.

**Marks earn their place** (founder ruling, #4/#5): every checkpoint exists because the state's claim IS a difference (or sameness) between two places or two passes — S1/S3 (two heights), S2 (two passes of one place), S4 (one place vs S1's same-height place), S5 (TWO latched places on two instruments — the subtraction between them IS the claim). S6 authors none.

| # | Teaches | Archetype | Distinct motion (authored, no teacher input) | Delta cue (≤5 words) | Controls | Ring | EN words |
|---|---|---|---|---|---|---|---|
| S1 | Raising a body stores U = mgh; higher means more | `translate-through` | m = 3 kg cart, s₀ = 0.6 (h₀ = 0.3 m, bar opens at 8.8 J), driven up at constant 0.8 m/s (F = mg·sin 30° = 14.7 N exactly, so a = 0). `displacement_vector` (`show_value: true`) grows live from s₀. Point A at s = 0.6+0.4 = 1.0 (h = 0.5) stamps `point A:  U = 14.7 J`, dwell 1200; point B at s = 0.6+2.4 = 3.0 (h = 1.5) stamps `point B:  U = 44.1 J`, dwell 1200 — d reads **2.40 m** at B, the numeral S4's compare renders against (P1-2). At 4400 ms (mid-B-dwell) the formula surface gains its second line `ΔU = mgΔh = 29.4 J` (`formula_lines`, `at_ms` — `nlbRenderStamps` L50623–50650): 44.1 − 14.7 RENDERED, the number S3 will hold (P1-1). Bar fills in lockstep with height. Termination: LOOPS; stamps + line latch to reset | "Higher up, more energy" | none | core | 40–55 (carries anchor sentence 1) |
| S2 | U is a property of the PLACE — same height, same U | `cycle-compare` | m = 2.5 kg, s₀ = 2.8 (h₀ = 1.4, bar opens 34.3 J), launched UP at v₀ = 3.13 m/s, frictionless: rises 1.0 m along (top h = 1.9), falls back. HOME-ARMED point at s₀ (`capture_mode:'every'`, `capture:['U_grav']`, `dwell_from_pass: 2`, dwell 1400): pass 1 stamps `start height:  U = 34.3 J` at departure; pass 2 re-stamps `start height (pass 2):  U = 34.3 J` at the return — the SAME number, scene held 1400 ms. `predicted_stop` marks `highest point  h = 1.90 m` (recomputed per frame). LOOPS | "Same height, same energy" | none | core | 35–50 |
| S3 | The zero line is a CHOICE; U shifts, ΔU does not | `translate-through` — **declared contrast pair with S1** (identical motion; the delta names the flip: only the zero line moved) | The IDENTICAL S1 climb (m 3, v 0.8, same F, same points, same d vector) with ONE change: `h_ref_m: 0.3` — the dashed line (label unchanged: `h = 0`, P3-2) now sits at the cart's start height and the bar opens at exactly 0.0 J. Point A stamps `point A:  U = 5.9 J`, point B `point B:  U = 35.3 J` — every U changed. The formula surface is **BYTE-IDENTICAL to S1's**: base `U = mgh` + the SAME second line `ΔU = mgΔh = 29.4 J` at 4400 ms — the click into S3 moves the stamps and HOLDS this line (P1-1; dissolves the two-things-change note P3-6: only the line's position differs). One narration clause names #6's line position as the first worked example of the choice (P2-3, concept_id only). Only the taught variable changed (32b). LOOPS | "New zero, same ΔU" | none | core | 40–55 (carries anchor sentence 3) |
| S4 | Path independence: same height means same U on any slope | `translate-through` — **declared contrast pair with S1** (the delta names the flip: the ramp steepened, the target height did not) | Ramp rotated to θ = 48.6° (sin θ = 0.75), m = 3 kg, s₀ = 0.6 (h₀ = 0.45, bar opens 13.2 J), constant 0.8 m/s (F = 22.05 N). ONE point at s = 0.6+1.4 = 2.0 — the SAME height h = 1.5 m as S1's point B — stamps `point B:  U = 44.1 J`, dwell 1400. `displacement_vector` (`show_value: true`) draws d = 1.40 m — against the d = 2.40 m the student READ on S1/S3's own vector at this same height (P1-2: both compared numbers rendered). With d in the home picture since S1, S4's only visible change is the ramp angle (32b strengthened). LOOPS | "Shorter path, same U" | none | extended | 40–55 (carries anchor sentence 2) |
| S5 | ΔU = −W by gravity survives ANY choice of zero — closing #6's relation through S3's freedom, and deriving U = mgh | `cycle-compare` — **declared contrast pair with S2** (identical motion; the delta names the flip: the gravity ledger joins the panel and both endpoints latch) | S2's exact launch (m 2.5, s₀ 2.8, v₀ 3.13) with `work_accumulators: [{force:'gravity', label:'gravity'}]`, `work_scale_J: 30` (= bar_max/2, the honest-mirror ratio). HOME-ARMED start flag (`capture_mode:'first'`, `capture:['U_grav','W']`, no dwell) latches `start:  U = 34.3 J · W gravity = 0.0 J` at departure — **U₀ stays ON SCREEN all loop** (the offset the zero-choice creates, latched, not remembered — the same fix #6 now uses). Point C at s = 2.8+0.7 = 3.5 (h 1.75, `'first'`, `capture:['U_grav','W']`, dwell 1400, wall 289) stamps `point C:  U = 42.9 J · W gravity = −8.6 J`. Four latched numerals: 42.9 − 34.3 = 8.6 = −(−8.6) — the subtraction is VISIBLE, display-exact (24.5 and 1.75 are dyadic; the interpolated stamp is on the exact path, §0A.5). Formula line `ΔU = mgΔh = 8.6 J` at 600 ms renders the mg·Δh product; the two flag markers bracket Δh on the ramp itself. Narration cites #6's pinned-at-start configuration as the case where U₀ happened to be 0 (concept_id only). Tail (to R 2900) confirms: U drains as W climbs to +10.0 J. LOOPS | "Equal change, opposite sign" | none | advanced | 40–55 (fullest budget: derivation + #6 bridge) |
| S6 | Teacher manipulation of U = mgh | `drag-sandbox` (slider-driven; §0A.9 — body drag alone OFF, the one true engine limit) | `mode: 'sandbox'`. m = 2 kg cart at rest, s = 2.4, θ = 30° (h = 1.2 m), μs = 0.9 / μk = 0.8 (static hold to θ = 41.99° > slider max 40°). `controls_visible: ['m','theta']`; `slider_controls`: m {1–3 kg, label 'm' — kills the default `m₁` glyph, L47119 (P2-5)}, theta {15–40°}. `idle_auto_sweep {param:'theta', range:[30,40]}` — starts at range[0] = the authored 30° (no first-frame snap, P2-5) and ROTATES the ramp with the cart riding it, h 1.20→1.54 m, U 23.5→30.2 J live, until the teacher's first trusted event seizes it. Formula U = mgh stands; h line at 0. Free-runs (Rule 37 — the APPARATUS itself now moves, P1-5) | "Change mass and angle yourself" | `m` + `theta` (ALL relevant controls — the two live tokens matching U = mgh's variables) | core | 0 / open |

Narration budget: guided 25–55 EN words each; explore 0/open. Motion may outrun narration, never the reverse.

## 4. Misconception confrontation plan (Rule 16a — 2 genuine pivots, no per-state tic)

| Wrong belief (real, exam-documented) | Where confronted | `misconception_watch` beat |
|---|---|---|
| "The ground is THE zero — a body at the ground HAS no potential energy, and U is an absolute property of the body." | **S3** | `belief`: U has one true value measured from the ground. `visual_counter`: the identical climb re-runs with the zero line moved to the start height — the bar now opens EMPTY and every stamped U changes, yet ΔU stamps the same 29.4 J. `one_line_fix`: "U depends on where you put zero; only ΔU = mgΔh is physical." Contrast beat + bridge (P2-3, re-pointed for the student who arrived THROUGH #6): the prerequisite showed its zero line at the ramp's foot, unremarked, and S1/S2 CONTINUE the same placement at the base — the "line just lives at the bottom" habit is built across BOTH concepts. S3 then moves the line for the first time in the pair, and one narration clause names #6's placement as the first worked example of the choice (cited by concept_id only, never by its state numbers). |
| "Pushing a load up a LONGER ramp stores more potential energy — you pushed for longer." | **S4** | `belief`: more path means more stored energy. `visual_counter`: a steeper ramp reaches the same 1.5 m with d = 1.40 m instead of 2.40 m, and the stamp is the identical 44.1 J. `one_line_fix`: "Only the height gained sets ΔU — path length does not enter mgΔh." |

S1/S2/S5/S6 carry NO misconception_watch (straightforward teaching — founder guardrail 2026-07-04).

## 5. `has_prebuilt_deep_dive` states (cache hints, not gates)

- **S3** — the reference-choice idea is the classic confusion point ("how can the zero be arbitrary?"); highest historical stuck-rate in this concept family.
- **S4** — path independence is the piece exam questions test hardest (ramp vs ladder vs lift) and the one students argue with.
(V1 ships zero authored deep-dives; un-flagged states route the Explain button to the feedback form — Rule 18.)

## 6. Drill-down clusters (3 candidates each; physics-author writes trigger_examples)

- S3: `zero_reference_choice` (why any zero is legal) · `negative_potential_energy` (what U < 0 means below your zero — NOT renderable in the unsigned stack today, §0A.1; the deep-dive is where it would finally be drawn) · `delta_u_vs_u` (which one enters problems).
- S4: `path_independence_gravity` (same endpoints, any path) · `ramp_vs_vertical_lift_work` (the classic compare) · `longer_ramp_less_force_same_energy` (why ramps exist at all).

## 7. `entry_state_map` (v2.2)

```
entry_state_map:
  foundational:      STATE_1 -> STATE_3   # what is U, U = mgh, the zero choice — CONTAINS the PRIMARY aha (S3)
  path_independence: STATE_4 -> STATE_4   # "ramp vs lift", "does the path matter"
  relation_to_work:  STATE_5 -> STATE_5   # "why mgh", "U and the work by gravity"
```
Default aspect `foundational`. Foundational-coverage rule satisfied: the PRIMARY aha state (S3) is inside the foundational range — no exit-pill needed.
Preset consequence (P3-3): under `no_derivation` (Cut 1) the `relation_to_work` aspect's target S5 is hidden; under `essentials` (Cut 2) `path_independence` (→ S4) is too. The preset DATA must route those aspects back to `foundational` when their slice is hidden; the classifier enum is unchanged.

## 8. Prerequisites (advisory — Rule 23)

- `potential_energy_definition` (#6 — IN-FLIGHT on a parallel desk; advisory link lands when it ships) — supplies ΔU = −W_c, applied in S5.
- `conservative_vs_nonconservative_forces` (#5, shipped) — supplies "gravity is a conservative force", cited in S1 in one clause.
- `work_done_by_constant_force` (#1, shipped) — supplies the work reading in S5's ledger.

## 9. Real-world anchor (Rule 35 universal · Rule 38f widest-overlap · Rule 41 plain)

**Primary — a bag carried upstairs in any building (stairs, ramp, or elevator).** Universal: every student on any syllabus climbs stairs and rides elevators; no country, brand, festival or currency. Physics-true at every depth: the stored energy is exactly mgΔh between floors; the route genuinely does not matter; and floor NUMBERING is genuinely a choice (street level vs basement — the h = 0 aha in the student's own experience). Hook for Class 11: it turns the most abstract idea in the chapter (an arbitrary zero) into something they already accept about buildings.

Anchor sentences, quoted here AND bound into the state narrations (scar `skeleton_anchor_specified_in_section_9_reaches_no_narration_line`):
- **S1 narration must contain:** "Lifting a bag from the floor to a shelf stores energy in it — the higher the shelf, the more energy stored."
- **S4 narration must contain:** "Stairs, a ramp, or an elevator to the same floor store the same energy in the bag."
- **S3 narration must contain (P2-2 — the PRIMARY aha carries the anchor's best asset):** "Buildings number their floors from a level someone chose — call the floor you start on zero, and nothing about the building changes; only the numbers do."

**Binding Rule 35 constraint (physics-author):** floor language may only ever be "the floor you start on" / "a floor below it" — NEVER a floor number or a national naming convention (ground = 0 vs first = 1 differs by country).

Secondary (optional, S6 if room): water pumped to a rooftop tank — stored energy set only by how high the tank is. *(The source catalog's mined anchors — railway porter, Manali chairlift — are pre-Rule-35 India-specific and are NOT used; phase0_survey.md warning applied.)*

## 10. Definition of Done (Gate 0 — no TBDs)

### (a) States and rendered primitives (Rule 19 counted against what is DRAWN — field_3d never paints scene_composition annotations)

| State | Drawn primitives (≥3 each) |
|---|---|
| S1 | ramp slab · cart + `m = 3 kg` billboard · F arrow · mg arrow · d displacement arrow + live value · U bar panel · dashed h = 0 line + label · point A marker + caption · point B marker + caption · formula surface `U = mgh` (+ 2 stamps + line `ΔU = mgΔh = 29.4 J` at 4400 ms) · delta-cue caption |
| S2 | ramp · cart (`m = 2.5 kg`) · mg arrow · U bar panel · h = 0 line · home point marker ("start height") · predicted-stop marker + `h = 1.90 m` caption · formula surface (+ 2-pass stamps) |
| S3 | as S1 — d arrow included, formula surface BYTE-IDENTICAL (base `U = mgh` + the same 29.4 J line) — with the line at h_ref = 0.3 |
| S4 | rotated ramp (48.6°) · cart · F arrow · mg arrow · U bar · h = 0 line · point B marker · d displacement arrow + `d = 1.40 m` value · formula `U = mgh` (+ stamp) |
| S5 | ramp · cart · mg arrow · U bar · signed `gravity` work bar · h = 0 line · start flag marker + latched start stamp · point C marker + stamp · formula `ΔU = −W by gravity` + line `ΔU = mgΔh = 8.6 J` |
| S6 | ramp (rotating under the θ sweep) · cart · mg arrow · U bar · h = 0 line · m-slider row · θ-slider row · formula `U = mgh` |

Single-body invariant: `bodies.length === 1` in every state (grep-able; discharges §0B.17).

### (b) Symbol-label table (Rule 34c Unicode; engine strings read at source)

| Quantity | On-canvas label | Source |
|---|---|---|
| Potential energy | `U` (bar caption, engine-fixed) · stamps `U = 44.1 J` | SEAM L symbols; `nlbCpStampText` L50579 |
| Mass | `m = 3 kg` / `m = 2.5 kg` / `m = 2 kg` body billboard | body label |
| Height reference | `h = 0` dashed-line label — the SAME string in all six states (P3-2: "(moved)" dropped; the positional change is the whole delta, 32b). S1 narration carries the one label-bridging clause: the previous concept wrote its U = 0 on this same line; here it reads h = 0 (cited by concept_id) | L49898 |
| Highest point (S2) | `highest point  h = 1.90 m` | L49907–49911 |
| Formula surfaces | `U = mgh` base on S1/S3/S4/S6 — S1 and S3 BYTE-IDENTICAL incl. the 4400 ms second line `ΔU = mgΔh = 29.4 J` (P1-1) · `ΔU = −W by gravity` base + line `ΔU = mgΔh = 8.6 J` (S5 — Unicode has no subscript g, per SEAM M; "by gravity" in words, Rule 41) | authored |
| Work ledger | bar caption `gravity` (single token) · stamp clauses `W gravity = 0.0 J` (start) and `W gravity = −8.6 J` (point C) | L50585–50590 |
| Displacement (S1/S3/S4) | `d = 2.40 m` at point B (S1/S3) · `d = 1.40 m` at point B (S4) — compared values read at the dwell/pin frames; the live d keeps growing in the settle tail, and no claim rides there | SEAM N `displacement_vector`, show_value true |
| Forces | `F` (applied) · `mg` (weight) | fleet defaults |
| Angle | shown only by the ramp itself — no angle_arc, no θ numeral (off-message) | design |

All on-canvas math is real Unicode (Δ, θ, ·, ₀); no ASCII transcriptions on any of the three text paths.

### (c) Direction/sign-rule plan (RHR N/A — translational concept)

h is POSITIVE upward from the authored line; ΔU > 0 going up; W by gravity NEGATIVE going up (S5's signed bar grows DOWNWARD, red — SEAM M's own sign rendering); the U↑/W↓ mirror IS the S5 picture. No right-hand rule anywhere.

### (d) Motion plan, loop arithmetic, frozen-pin margins — ONE t per state, loop-END bounds + a worst-folded-frame column (§0B.16 recurrence, founder_proxy P1-4)

All derived at the single t = R per state AND re-checked at R + 50 ms — the 3-step folded-frame cap, the "one frame of slack" the `worked_loop_resize_patch…` DO requires (cycle 0 omitted this column; S5 overran its scale there). a = 0 exactly on driven states (F = mg sin θ: SEAM K's step is error-free for constant a — ch6_state.md SEAM K RESULT "gravity error-free"; probe P5 measures it once anyway).

| State | Home / drive | Checkpoint walltimes (Tₖ = tₖ + Σ prior dwells) | R = `loop_reset_ms` | `eye_capture_ms` (margins both sides) | At t = R (single-t set) | At R + 50 ms (worst folded frame) | Bounds |
|---|---|---|---|---|---|---|---|
| S1 | s₀ 0.6, v 0.8 const | A: T₁ 500, D 1200, win [500,1700] · B: T₂ 4200, D 1200, win [4200,5400] · formula line at 4400 = the LAST asserted reveal | **6000** ≥ 5900 | **4800** (600/600 to the dwell edges; 400 past the line reveal ≥ 167) | s 3.48 · h 1.74 · U 51.2 J | s 3.52 · h 1.76 · U **51.7 J** | 3.52 ≤ 3.95 · 51.7 ≤ 0.9·60 = 54 |
| S2 | s₀ 2.8, v₀ +3.13, a −4.9 | departure pass 1 first frame (no dwell) · return T 1278, D 1400 (pass 2), win [1278,2678] | **3178** = 2678+500 | **1978** (700/700) | s 0.62 · h 0.31 · U 7.6 J | s 0.334 · h 0.167 · U **4.1 J** — the h = 0 crossing sits 56 ms (≈3 steps) further out; the guard cannot fire | peak s 3.80 ≤ 3.95 · U ≥ 0 whole loop incl. folded |
| S3 | = S1, `h_ref_m` 0.3 | = S1 (same points, same 4400 ms line) | **6000** | **4800** | s 3.48 · h−ref 1.44 · U 42.3 J | h−ref 1.46 · U **42.9 J** | h ≥ h_ref whole loop; t = 0 equality gives U = 0.0 exactly (float-safe — founder_proxy independently confirmed) |
| S4 | θ 48.6°, s₀ 0.6, v 0.8, F 22.05 N | B: T 1750, D 1400, win [1750,3150] | **3650** ≥ 3150+500 | **2450** (700/700) | s 2.40 · h 1.80 · U 52.9 J | s 2.44 · h 1.83 · U **53.81 J** — see the acceptance note below | 2.40 / 2.44 ≤ 3.95 |
| S5 | s₀ 2.8, v₀ +3.13, a −4.9 + gravity ledger | start flag: home-armed `'first'`, stamps at departure (first frames, no dwell) · point C (s 3.5): T 289, D 1400, win [289,1689] · descent recross wall 2389 and home recross wall 2678 are `'first'` early-continue no-ops (L50443) | **2900** ≥ 1689+500 = 2189 (margin 711) | **989** (700/700; 389 past the 600 ms formula line) | s 1.98 · h 0.99 · U 24.3 J · W **+10.0 J** | s 1.77 · h 0.88 · U 21.6 J · W **+12.7 J** ≤ 0.9·30 = 27 · h = 0 at physics 1884 → 334 ms margin at R, 284 ms at folded | peak s 3.80 ≤ 3.95 · ledger extremes [−12.25, +12.7] inside ±30 |
| S6 | rest, sandbox | none | n/a (free-runs, Rule 37) | default pin — no claim rides on it | θ sweep 30→40°, cart riding the rotating ramp | n/a (no loop) | envelope over the FULL slider cross-product: U_max = 3·9.8·2.4·sin 40° = **45.4 J** ≤ 54 · static hold tan 40° = 0.839 < μₛ 0.9 (limit 41.99°) |

**S4 folded-frame acceptance note (named residual, not silent — founder_proxy's "thin neighbour"):** the folded U 53.81 J sits 0.35% under the self-imposed 0.9·bar_max = 54 discipline but **10.3% under the 60 J warn threshold the H4 assertion actually reads**. The floor is deterministic: R ≥ stamp + dwell + 500 settle forces folded s ≥ 2.0 + 0.8·0.55 = 2.44 given (m 3, target h 1.5, v 0.8) — no R choice removes it. Lowering v would add a second visible change to the S1↔S4 contrast that P1-2's fix just purified (32b). ACCEPTED with the number recorded; no claim rides on the settle tail; the re-derivation tripwire below covers it.

Re-derivation duty: any change to a mass, v₀, F, θ, dwell, flag or s value re-derives this whole table INCLUDING the folded column (§0B.3 / §0B.16). Dwells ≤ NLB_DWELL_MAX_MS 5000. Framed extent: run + 0.55 m half-width each side (max S1: 0.05–4.03 along-track m). Zero `[PM_NLB_DWELL]` / `[PM_NLB_ENERGY_*]` console lines is a named EYE H4 assertion — now proven WITH slack, not at t = R alone. **S2's two-pass stamp is ONE overwritten line** (`cp.text` is rewritten at each crossing, L50444), never two lines — stated here so eye-walker judges the frozen frame as a single re-written line (P3-4).

### (e) Modes

EPIC-L conceptual only (Rule 20 [D] — no `mode_overrides`). Deep-dive/drill-down per §5/§6 flags only.

### (f) `assessment` + `coverage_map` + `misconception_watch`

`misconception_watch`: exactly the 2 entries of §4 (S3, S4). Assessment (6 questions, `teaches_state` mapped; numbers not reused from rendered triples except where the tested idea IS the reuse):
1. Compute U for a 2 kg bag on a 3 m shelf (S1). 2. A body returns to its launch height — what is ΔU for the trip? (S2). 3. One student quotes a body's U as 98 J, another as 40 J; both are right — how? (S3). 4. Ramp vs vertical lift to the same floor: compare ΔU (S4). 5. Gravity does −58.8 J on a lifted crate; state ΔU (S5). 6. Double the mass at fixed height: what happens to U? (S6/S1). `coverage_map`: q1→S1 … q6→S6; every state carries ≥1 question; ring-cut presets keep ≥1 question per surviving state (q5 falls with S5, q4 with S4 — noted for the preset data).

### (g) Macro↔micro plan (Rule 33)

N/A with rationale: U is a configuration property of body+Earth, not a macroscopic variable hiding a microscopic mechanism at this level — there is no honest interior story to draw. Rule 33d's instrument half IS satisfied: the U bar and stamps are live numeric instruments; the h = 0 line + markers are the geometric readout.

### (h) Canvas budget (Rule 34)

Per state: ONE formula surface (§b strings, math-serif Unicode); top caption = the ≤5-word delta cue ONLY; narration prose in the strip below the canvas; no HUD readouts; panel/formula/slider zones are the fleet's fixed non-colliding zones; corners reserved (nothing new at top-right, review-chrome clearance untouched). S5 is the busiest state (2 bars + 2 latched stamps + formula base + 1 formula line) and stays inside the zones because the work bar lives INSIDE the SEAM L panel; the surface's rendered-line fit at the pin is measured by probe P7, not asserted.

### (i) Curriculum-flex block (Rule 38)

- **Cut 1 (hide advanced — drop S5):** S1–S4 + S6 remain. No surviving narration/caption/formula references the ledger or −W (the relation appears ONLY in S5). Coherent.
- **Cut 2 (hide advanced+extended — drop S4, S5):** S1–S3 + S6 remain. No surviving string references slope-compare or d. Coherent.
- **Explore = core only (38b):** S6 surfaces U = mgh, the m and θ sliders, the h line — all established in S1; both controls map to core lessons and survive every cut (§0A.7).
- **Notation ladder (38c):** algebra only, every ring — no calculus anywhere.
- **Dialect (38d):** "stored energy — potential energy U" dual-labelled once in S1 narration, then bare U.
- **Graph axes (38e):** N/A — no graph panel exists in this concept.
- **Anchor (38f):** buildings/stairs/elevator — maximal syllabus overlap (§9).
- **`curriculum_tags` (38g):** CBSE/NCERT Class 11 (Work, Energy and Power — "potential energy of an object at a height") marked `syllabus_doc_verified` at authoring — a SYLLABUS-DOCUMENT verification (NCERT chapter-index read, scope only), NOT teacher verification; teacher confirmation is still pending for this cell like every other (P3-5). Every other board cell ships `needs_teacher_verification: true`; no preset teacher-visible until confirmed.
- **Preset proposal (38h — hide, never reorder):** `full` = S1–S6 · `no_derivation` = hide S5 · `essentials` = hide S4+S5. Each reduced preset also re-routes its dead `entry_state_map` aspects to `foundational` (§7, P3-3).

### Rule 41 audit of every reader-facing string

Titles: "Lifting stores energy: U = mgh" · "Back at the same height, same U" · "The zero line is your choice" · "A steeper ramp, the same U" · "ΔU equals minus the work by gravity" · "Explore: change mass and angle". Delta cues per §3. Checkpoint labels: "point A", "point B", "start height", "start", "point C". All literal; no idioms, no personification (energy is "stored", never "waiting"; gravity "does negative work", never "fights"); first words carry the meaning (rail truncation). physics-author holds narration to the same register.

## ENGINE FIT CHECK — every state mapped to a built, contracted block (ZERO renderer edits)

| Config block | States | Contract source | Presence side-effects diffed (§0B.45 / §0A.10) |
|---|---|---|---|
| `energy_layer {bars:['U_grav'], bar_max_J:60, h_ref_m, precision:1}` | all 6 | SEAM L RESULT + §0A.1/.12 | raises the deriveStateMeta reveal floor to 3000 ms (`NLB_ENERGY_SETTLE_MS`) — all pins here are authored `eye_capture_ms`, unaffected |
| `height_markers {show_h_ref_line, h_ref_label}` | all 6 | SEAM M RESULT + §0A.1 | line reads the ONE resolved field; no marker drawn unless authored |
| `height_markers.predicted_stop {show_height:true}` | S2 only | §0A.10 | hides on flat/unlaunched — why it is authored ONLY on the launch state |
| `checkpoints` (+ dwell_ms, dwell_from_pass, `marker:'point'`, explicit `capture`) | S1 ×2 · S2 ×1 (home-armed 'every') · S3 ×2 · S4 ×1 · S5 ×2 (one home-armed 'first', one dwelled) | §0A.4/.5 | dwell freezes physics while the narration clock runs (Rule 26); the marker brightens during its own window; stamps append under `formula_base` |
| `work_accumulators [{force:'gravity', label:'gravity'}]` + `work_scale_J: 30` | S5 only | SEAM M RESULT | the ledger exists internally regardless of drawing — drawing it is S5's delta |
| `displacement_vector {show_value:true}` | S1/S3/S4 (P1-2) | SEAM N RESULT | hides below 0.02 m; measures from `initial_position_m`; compared d values are read at the dwell/pin frames — the live value keeps growing in the settle tail |
| `formula_lines` (ordered lines, `at_ms`) | S1/S3 (`ΔU = mgΔh = 29.4 J` at 4400 ms — P1-1) · S5 (`ΔU = mgΔh = 8.6 J` at 600 ms) | `nlbRenderStamps` L50623–50650 | reveals on the state wall clock (runs through dwells); each pin sits ≥ 167 ms after its line reveal |
| `applied_force` (plain number, along-surface) | S1/S3/S4 | SEAM N (legacy gate: plain number takes the bit-identical pre-seam path) | — |
| `loop_reset_ms` | S1–S5 | SEAM K RESULT | wipes stamps/ledgers per cycle (§0A.11) |
| `eye_capture_ms` (state-config TOP level) | S1–S5 | §0A.6 | EYE-only; teacher path unaffected |
| `mode:'sandbox'` + `controls_visible:['m','theta']` + `slider_controls` bounds/label overrides (`nlbSc` L47160–47181) + `idle_auto_sweep {param:'theta', range:[30,40]}` | S6 | §0A.7/.8/.9 | sandbox carve-outs: no dwell, no loop reset, Rule 37 free-run; sweep starts at range[0] = authored θ (no snap) |

Nothing outside these contracts is asked for. **Phase-0 alarm rule: PASS — zero renderer edits needed.** One near-miss recorded honestly: negative U below the reference is UNRENDERABLE in the unsigned stack (§0A.1) — its ONLY design cost is body drag removed from S6 (§0A.9; the θ and m sliders are unaffected and authored); otherwise handled by arithmetic (S2/S5 loop-end + folded margins) and one S3 narration sentence + a deep-dive cluster. NOT an engine ask here: founder_proxy filed EQ-1 (signed U_grav rendering) RIDE-ALONG, owner peter_parker:field3d_surgeon — this design does not depend on it and must not wait for it.

### Probes physics-author runs BEFORE json-author commits (probe-don't-grep; ASSUMPTION flags)

- **P1** (ASSUMPTION — home-armed flags, both modes): drive S2 on the built page; assert the pass-1 stamp appears within ~50 ms, the pass-2 stamp + 1400 ms dwell at wall-clock ≈ 2678 ms, and "(pass 2)" present in `#nlb_formula` — as ONE overwritten line, not two (L50444, P3-4). Then S5: assert the `'first'` start stamp (U 34.3 · W 0.0) appears within ~50 ms and is NEVER overwritten or removed across a full loop (the early-continue no-ops at wall 2389/2678).
- **P2** (warning-freeness): free-run S2 and S5 for 3 full loops; assert ZERO console lines beginning `[PM_NLB` (negative-U, clamp, dwell, scale).
- **P3** (explore hold + sweep): load S6, no input, 20 s: cart s unchanged along-track (static hold at every swept θ), ramp visibly rotates 30→40°, U numeric sweeps 23.5→30.2 J; assert the FIRST frame reads θ = 30° and m = 2 kg (no range[0] snap, P2-5). Then one trusted event on EACH of m and θ seizes the sweep and the standing claim still reads true.
- **P4** (dwell residual): at each `eye_capture_ms`, read the live U bar numeric vs the stamp; assert the gap ≤ the §0B.25 bound for that state; record measured gaps for founder-proxy.
- **P5** (constant-speed exactness): S1 pinned at 4800 ms — probe v = 0.800 within one display tenth; s per the §10d table.
- **P6** (ASSUMPTION — the never-rendered mechanism, P2-4: `height_markers`/`h_ref_m`/`predicted_stop` appear in ZERO shipped concept JSONs): load S3, assert `marker_h_ref` is visible, its world y = h_ref_m · NLB_WORLD_PER_M (L49890), its label `h = 0` renders, and — the part arithmetic cannot answer — that at h_ref = 0.3 m the line reads as a LEVEL clearly separate from the slab silhouette. Same probe on S2 asserts the `predicted_stop` caption renders `highest point  h = 1.90 m`.
- **P7** (formula-surface fit, §0B.23): pin S1/S3 at 4800 and S5 at 989; assert base + formula line + stamps all render with no mid-clause wrap and no clip against the 340 px surface literal (S5 worst case: up to ~6 rendered lines with two-clause wraps); screenshot for founder-proxy Checkpoint B.

## Two-pass cognitive lens

### Block 1 — Pass-1 strategic checklist

1. **Prerequisite cliff.** #6 (`potential_energy_definition`): a student arriving without it breaks at **S5** — patch sentence inside S5's budget: "From the last concept: a conservative force's work defines the change in potential energy, ΔU = −W." #5: breaks at **S1** where "stored" is asserted — patch clause: "gravity is a conservative force, so this energy is stored, not lost." #1/#2 (work sign): breaks at **S5**'s negative ledger — covered by the same S5 sentence's "−W". Each patch is one clause; none condescends to a student who has the prerequisites.
2. **JEE-backwards trace.** "A 2.5 kg block is moved from A (height 1.4 m) to B (height 1.9 m), once vertically and once along a smooth 30° incline. (i) Find ΔU for each route. (ii) Taking A as the zero level, find U at B. (iii) How much work does gravity do from A to B?" — (i) needs U = mgh + path independence → S1 + S4; (ii) needs the reference choice → S3; (iii) needs ΔU = −W_gravity → S5. Every needed piece has a delivering state; no gap. (M1–M6 carve-out N/A — not magnetism.)
3. **Misconception entry mapping (16a — re-pointed cycle 1, P2-3).** Belief 1 (absolute zero) is carried IN and REINFORCED, not planted cold: the default-path student arrives through #6 (declared prerequisite), which showed its zero line at the ramp's foot unremarked, and S1/S2 continue the same placement at the base — two concepts of silent habit. S3 breaks it, and its bridging clause names #6's placement as the first worked example of the choice. The watch entry sits on S3; S1's narration must not assert "the ground IS zero" (it silently uses it — flagged to physics-author at the planting moment). Belief 2 (path means energy) is planted by every single-slope state and by everyday effort-intuition; S4 confronts it. EPIC-C branches: none (EPIC-L-first directive 2026-06-10).

### Block 2 — Aha-moment designation

- **PRIMARY aha (the 10-year memory), S3:** "There is no true zero of height — you choose the h = 0 line, every U value moves with your choice, and nothing physical changes, because only ΔU ever mattered."
- **SUPPORTING aha (1), S4 — set up by S2:** "Gravity's stored energy depends only on WHERE you are — same height means same U, by any path." Cohesion: both ahas are one deep structure — U's individual value is bookkeeping; differences between places are the physics. The supporting aha (position-only) makes the primary (zero-shift invariance) feel inevitable rather than arbitrary. Two total — the sweet spot.
- **Wrong-belief setup.** PRIMARY: S1 and S2 both use the floor zero silently and confidently — two states of earned habit before S3 breaks it. SUPPORTING: S1's single slope plus "longer climb = more effort" intuition, pre-loosened by S2's round trip, broken by S4.
- **Foundational coverage:** S3 is inside `entry_state_map.foundational` (STATE_1→STATE_3) — satisfied without an exit-pill.
- Deep-dive flags (S3, S4) coincide with the Pass-1 stuck-points — no divergence to document.

## Handoff dependencies (constraints downstream agents inherit)

- **physics-author:** narration register per the Rule-41 audit; anchor sentences verbatim in S1/S4; every tts sentence carries a `glow` naming an existing element (§0B.30); narrated numbers = STAMPED numbers only (§0B.25); `variables[].derived` = expression slots; `computed_outputs` keys byte-match painted symbols (`U_J`; for S5 check the W key against a live `numeric:calc` reading list — a skip is not a pass); no equal-height claim between the unsigned U bar and the signed W bar; every narrated agent/quantity is a rendered object/number (`teach_visual_must_match_narration`); run P1–P7 and record results in the physics block; anchor floor-language constraint (§9 — never a floor number or naming convention); S1's label-bridge clause and S3's #6-bridge clause are verbatim duties; S1/S3 formula surfaces must stay byte-identical (P1-1).
- **json-author:** 8 registration sites + SQL migration; explicit `capture` on every checkpoint (the default silently imports K — §0A.5); `eye_capture_ms` at state-config TOP level (§0A.6); `bar_max_J: 60` in all six states; single-token work label; `marker: 'point'` everywhere; body label `m = <x> kg`; `bodies.length === 1` every state; rebuild the review site before any frame is read; re-seed the cache before THE EYE.
- **quality-auditor / eye-walker:** H4 zero `[PM_NLB_*]`; read executed/skipped/failed as three numbers; diff `STATE_N.png` vs `STATE_N__frozen.png` per state before any approval request; judge frozen frames at the authored `eye_capture_ms` beats (frozen = a re-entered state pinned at that instant, not the dense tail).
- **Checkpoint-B measurement duties (named, not closed):** the §0B.25 dwell residuals (P4 numbers) · S5's two latched stamps co-displayed with the formula line (the four-numeral subtraction frame) · S3's empty-bar opening frame (U = 0.0 exactly, not a warning) · the S1↔S3 frozen-frame diff: stamps and line positions move, the `ΔU = mgΔh = 29.4 J` line byte-holds (P1-1's click test) · S2's frozen frame judged as ONE re-written stamp line (P3-4).

## Self-review checklist — all items verified

- [x] Atomic claim one sentence; #6 boundary honoured (ΔU = −W cited as taught, never primary aha).
- [x] 6 states, medium calibration justified; control table complete (teaches × archetype × distinct motion × delta × controls × ring × words); no static state; archetype repeats ONLY as three DECLARED contrast pairs, each delta naming its flip (S1↔S3 reference, S1↔S4 slope, S2↔S5 instrument) — flagged for founder-proxy as the deliberate "one canonical picture ± one flipped variable" structure (Rule 32b applied across states; each state's IDEA is a distinct invariance, per the #4 idea-distinctness lesson).
- [x] Rule 32: cause-first, one-variable-moves, ≤5-word delta cues, home pose persistent (S4's ramp rotation IS its delta), zero glow_focal / ≤1 emphasis at any instant.
- [x] Rule 33 N/A with rationale; instruments live-numeric. Rule 34 per-state budget. Rule 35/38f/41 anchor + every string audited.
- [x] Rule 38: rings ordered; advanced contiguous before explore; BOTH cuts checked coherent; explore core-only; curriculum_tags as claims with needs_teacher_verification; presets hide-only; graph axes N/A stated.
- [x] Rule 16a: 2 genuine pivots only; between-state contrast beats; no predict-and-reveal.
- [x] 2 deep-dive flags + 3 clusters each; entry_state_map (foundational contains the PRIMARY aha); prerequisites advisory with shipped/in-flight status.
- [x] DoD complete, zero TBDs — (a) through (i) plus the Rule 41 audit.
- [x] STEP-ZERO scar analysis: §0B — the 2 instrument-aimed rows + the 4 dispatch-named architect directives + 47 further individually dispositioned rows + one grouped-irrelevant line + 16 FIXED re-introduction constraints; every N/A quotes its reader or a grep-able design fact.
- [x] Probe-don't-grep: every behavioral claim carries clone evidence (shipped sibling JSON / fleet convention), a this-session renderer read with line numbers, or an explicit ASSUMPTION flag with its probe body (P1–P5).
- [x] Loop/dwell/pin arithmetic done by hand at ONE t per state with loop-END bounds AND a worst-folded-frame (R + 50 ms) column (§0B.16 recurrence discharged); margins stated in ms.
- [x] Cycle-1 resolution of all five P1s: ΔU = 29.4 J rendered in BOTH S1 and S3 (formula_lines); d rendered in S1/S3 so S4's compare has both numbers on screen; S5 redesigned around two latched endpoint stamps (survived, not cut — see the §3 row); §10(d) folded-frame column + S5 loop shortened to R 2900; θ added to the explore. P2-1 cross-tab run (§0B.34); probes P6/P7 added; anchor reaches S3 (P2-2).
- [x] DC Pandey / NCERT check: consulted the NCERT Class 11 chapter index for scope only ("potential energy of an object at a height" confirmed under Work, Energy and Power). No teaching method, no example problem, no figure imported. Catalog anchors rejected as pre-Rule-35.
- [x] Findings NOT taken, with evidence (cycle 1): (i) founder_proxy's S5 mechanism option "offset flag at s 2.85" — rejected: it breaks display-exactness (U₀ would stamp 34.9 with W −0.6, forcing a two-step subtraction); (ii) option "home-armed 'every' + dwell_from_pass 2" — rejected: a pass-2 dwell forces physics end ≥ 1778 ms (R ≥ home-recross + D + 500 with physics end = R − ΣD), recreating the exact P1-4 overrun the same report requires fixing; chosen instead: home-armed `'first'` relying on the FIXED departure-pass contract (§0A.4, v₀ = 3.13 ≠ 0); (iii) S4's folded 53.81 J left in place rather than lowering v — see the §10(d) acceptance note.
