# SKELETON — `pure_rolling` (chapter `rotmech`, Class 11 Ch.7 — Systems of Particles & Rotational Motion) — REV 3 (fix cycle 2 response — FINAL cycle)

> **Phase-0 role:** 0b skeleton for concept **#11** of the 14-concept spine (★ Diamond, **V1**), second consumer of build **0c-2** (bounded rotational extension to `newtons_laws_body`). Sibling: `rolling_on_incline` (#12, REV 5 → REV 6 in parallel). founder-proxy Checkpoint A: REV 1 → `DESIGN_FIX`; REV 2 → `DESIGN_FIX` (cycle 2 of 2 — `founder_proxy_A_cycle2.md`). This revision is built around TWO binding founder rulings (both 2026-08-02): **(R-1)** two-phase Rule-16a contrast beats stay ONE state on per-body **`bodies[].activate_at_ms`**; three-phase states split; `lane_gap_m = 0` bought; **(R-2, resolving the sibling's escalation — binding on #11):** 0c-2 additionally buys **per-line formula reveal ONLY** — `formula_overlay` as an ordered line list with per-line `at_ms`, same discipline as `activate_at_ms`, registered in `deriveStateMeta`. It does **NOT** buy per-arrow `show_at_ms`: **arrows are static from state entry and sequence via `phases[].glow_focal` windows.** The buy is therefore **TWO timed fields, exactly** — see the restated scope guard in the union. REV 1 at `skeleton_rev1.md`; REV 2 at `skeleton_rev2.md`.
> Survey: `docs/loop_runs/rotmech/phase0_survey.md` (founder-approved 2026-08-02). The closing **"0c-2 ENGINE UNION — pure_rolling contribution"** remains the load-bearing artifact.

## FIX-CYCLE-2 RESPONSE (finding × what changed × where it now reads)

| Finding | What changed | Where |
|---|---|---|
| **P1-1** second timed action (S3 dissolve @1400) | The 1400 ms dissolve is **DELETED** from §3 and the timing table. The sibling's single-lane retirement is imported **verbatim** (ACTIVATION SEMANTICS block, §3): phase A is live from state entry until phase B's `activate_at_ms = 1500`, at which instant it **retires — hidden, un-integrated, skid trail CLEARED — as a declared HARD cut** (the narrow buy has no dissolve animation), bridged by narration. No empty-track gap exists (A visible until the very instant B appears); the pin cannot photograph a roller trailing a skid mark (trail cleared at retirement, asserted in the timing row). A classification sweep of **every authored millisecond** in this document is stated under the timing table: each is a physics event, a glow window, or one of the two bought fields — `activate_at_ms = 1500` is the only activation instant in the concept | §3 ACTIVATION SEMANTICS ¶; §3 S3 row; timing table S3 row + sweep ¶; §4 S3 beat; union (c)-5; DoD (d) |
| **P1-2** glow ruling not re-run over S2/S5 | S2 and S5 now author **NO state-level `glow_focal`**. Emphasis runs on `phases[]` windows with hand-back to full brightness on EVERY overlay the delta cue and `tts_sentences` name — S2 complete by 2618 ms (< the 3000 pin), S5 complete by 1200 ms (< the 2400 pin) — the S4/S6 pattern. The glow table now carries a **per-state relation-check column** so the ruling is re-derived row by row and cannot be missed by a future restructure; S7 is also moved to a `phases[]` window ending at capture so every narration-named readout is full-bright at its pin | §3 glow table (rewritten, + relation-check column) |
| **P1-3** ledger forbids the arrow S4/S5 draw | The (b′) row is re-scoped to the **summed VALUE, not the position**: *"the **summed** top-point arrow at **2v** \| DEFINED S6 \| must not render in S1–S3 or S8; S4 and S5 render top-point arrows at their **part** values (v and +Rω), the setup for S6's sum."* The **whole ledger was then re-read against the eight-state numbering**: one further latent contradiction found and fixed — REV 2's "point-speed arrows \| DEFINED S4" while S2 renders the centre arrow; the row is split (centre arrow DEFINED S2; the three-point FAN defined S4) | DoD (b′), rewritten rows + re-read note |
| **FOUNDER RULING R-2** (two-field buy) | §3's S1/S6 formula-line builds (`at_ms` 2300/2600 and 1800/2400) are now **BOUGHT and legal** under the per-line formula reveal — declared as union item **(c)-9** with `deriveStateMeta` registration. S4/S5/S6's former per-arrow *reveal* times are converted to **glow-walk windows over arrows static from state entry**; S2's centre arrow + contact marker are likewise static from entry. The scope guard is restated at the two-field boundary: no per-arrow reveal, no per-label reveal, no per-body show/hide timeline, no choreography DSL — **a THIRD timed class is the alarm rule firing**. Timing rows for S2/S4/S5/S6 re-derived accordingly (S4/S5/S6 numerically unchanged; S2 changes only via P3-3) | Header; §3 S2/S4/S5/S6 rows; timing table; union closure + (c)-9; engine_queue |
| **P2-1** S2 framed extent omits the radius | Extent corrected to **4.0 m ≈ [+2.9, −1.1]** and the sibling's rule adopted as #11's own: **framed along-track extent = run + one body DIAMETER of margin each end** (run [+2.4, −0.6], diameter 0.5 m). Acceptance: project the body bounding box at t = 0, at the frozen pin, and at state end — every corner inside the viewport. The rule is applied to every non-sandbox camera (per-state extents stated) | §3 S2 row; camera plan |
| **P2-2** S8 contact element unspecified for nonzero contact speed | **In S8 the contact element is a dot + its LIVE value at all times** (marker semantics — never routed through the clamped arrow channel), so the full-preset ω₀ demo's arbitrary contact speeds — including the sub-floor band (0, 0.4076) m/s — always have a specified, honest rendering. S8 added to DoD (b)'s velocity-arrow state list with the v₀-range floor check stated (centre arrow 0.46–2.30 wu over v₀ ∈ [0.5, 2.5]: min 1.84× floor, max < 2.80). E11's acceptance amended to the sibling's fleet definition | §3 S8 row; velocity map table; DoD (b); union (c)-6/E11 |
| **P2-3** S1/S6 shared motion + dead coinage | `superpose-combine` is **retired** (its rhythm died with the split). S1 and S6 are declared a **`reveal-build` contrast pair** whose delta names the flip: S1 reads v = Rω from the **GROUND** (marks + bracket, one equation); S6 reads it from the **POINTS** (the arrow set, two equations) | §2 note; §3 S6 archetype cell |
| **P2-4** S1 density, no sentence plan | Five-sentence plan with per-sentence word allocation, total **53 ≤ 55**, stated in the S1 row | §3 S1 row (sentence-plan block) |
| **P2-5** cross-document divergences | The sibling's **ACTIVATION SEMANTICS paragraph is imported verbatim** (its striking of the two stale "#11 S4" build-sheet references and the two `#11 S4` force-table rows is the sibling REV 6's edit, acknowledged here). Cross-document references in this document name **content, not bare state numbers** (or quote the number with its revision); a cross-reference check line added to the union | §3 ACTIVATION SEMANTICS ¶; union closure + cross-reference note |
| **P2-6** the `\|\|` trap | E9/E10/E11 acceptances amended: **presence is resolved by `typeof x === 'number'` / `in`, never truthiness**; the regression pair asserts authored-`0`/`false` and **absent** produce DIFFERENT rendered geometry (`lane_gap_m: 0` ⇒ both bodies z = 0; absent ⇒ ±0.425 wu; `activate_at_ms: 0` ⇒ active from entry *by authored instant*, distinct from the absent default in the meta) | engine_queue E9/E10/E11; union (b)-19 note |
| **P3-1** seize extends the mark run | Envelope stated at BOTH run lengths: unseized 4.5 m and seized-to-bound 5.4 m — R = 0.15 → 5 / **6** marks (mark 5 at −2.3124); R = 0.25 → 3 / 4 (mark 3 at −2.3124); R = 0.35 → 3 / 3. All in-bounds | §3 marks ¶; S1 row |
| **P3-2** S5 cue six words | Delta cue → **"Rim turns, centre stays"** (4 words) | §2/§3 S5 |
| **P3-3** S2 last asserted event | Re-stated as the **arch completion at 2618 ms** (top-to-top arch containing one cusp = 2πR/v), 52.4% of R 5000; pin 3000, margin **382 ms** ✓ | timing table S2 |
| **P3-4** undispositioned OPEN row | `eye_h2_frozen_frames_of_moving_elements_wobble…` explicitly dispositioned: #11 inherits the sibling's amended (b)-19 acceptance by reference (frozen captures byte-stable under the pin; comparison only at the pinned instant). Sweep now **29 of 29** | SCAR AUDIT |
| **P3-5** rounding | Mark 3 at R = 0.15 printed **−0.4274** | §3 marks ¶ |
| **P3-6** no purely qualitative opener | Acknowledged as a note; the within-state order (marks → bracket → THEN formula) is the concrete-before-abstract rescue, now also visible in the S1 sentence plan (equation arrives at sentence 4 of 5). No structural change (do-not-churn: the fold stands) | §3 S1 row |
| **P3-7** Rule 39 by construction | Stands, unchanged | — |
| *(advisory)* D9 condition-label titles | S4 → **"Sliding only: one speed everywhere"**; S5 → **"Spin in place: centre at rest"** — titles now state the result, front-loaded for rail truncation (Rule 41d). Non-blocking rubric note, taken because it is one line each | §2 table |

**Not churned (per the review's explicit list):** the fold (S1) · the split (S4/S5/S6) · the founder ruling in every part · the velocity-channel values · the marks primitive + envelope method · the S7 capture derivation · the μ_k `min_ring` paragraph (verbatim) · both ring cuts · the timing table's method · the eleven-id sweep. The six timing rows the review confirmed as printed (S1, S4, S5, S6, S7, envelope) are unchanged to the digit; only S2 (P3-3) and S3 (P1-1) rows changed.

## FIX-CYCLE-1 RESPONSE (REV 2 — preserved for the audit trail; cells superseded at REV 3 are annotated)

The full cycle-1 table stands as written in `skeleton_rev2.md` §FIX-CYCLE-1 RESPONSE, with two annotations: the **P2-3 row's** framed extent ("≈ 3.4 m ≈ [+2.6, −0.8]") is **SUPERSEDED** by this cycle's P2-1 (4.0 m ≈ [+2.9, −1.1]); the **founder-ruling row's** "no other timed actions" phrasing is **restated** at the two-field boundary by R-2 (per-line formula reveal is now inside the buy). Everything else in that table is live and re-verified against the REV 3 deltas.

> **Engine bug queue consultation (REV 3 — the REV 2 provenance stands, re-checked this cycle):** ① `grep -rl "newtons_laws_body" src/data/concepts/*.json` → the same eleven ids; ② per-id queries all read; ③ `--owner alex:architect` + `--row-type directive`; ④ `--owner alex:json_author` + `--owner alex:physics_author` (reach the id-less glow/narration rows); ⑤ `pure_rolling` / `rolling_on_incline` → 0 rows each (cycle-1's five candidates + three upserts AND cycle-2's five candidates + two upserts remain unfiled — orchestrating session's `log:lesson` duty, noted). `--field3d --open` NOT used as coverage (`query_engine_bug_queue.ts:23`). Sweep disposition count this cycle: **29 of 29** (P3-4 closed below).
> **Renderer citations:** unchanged from REV 2's corrected set (all re-confirmed by Checkpoint A cycle 2's independent re-derivation, including the seize pair `:42164–42175` → `:43022`), plus the escalation packet's machine evidence consumed by R-2: arrows built once per state apply `:44697–44712`, drawn from a static map `:40815+` with **no time argument** — the code fact that makes "arrows static from entry + glow-walk" the only honest sequencing; `formula_base` a static string today `:44801` — the code fact (c)-9 changes.

## 1. Atomic claim

This concept teaches the rolling-without-slipping condition v = Rω and its direct consequence: the contact point of a rolling body is instantaneously at rest, so the friction at a rolling contact is static, never kinetic. It does NOT teach the incline race or the shape factor I/mR² (deferred to `rolling_on_incline`), and it does NOT teach rotational kinetic energy (deferred to `rotational_work_energy`).

## 2. State count + arc

**8 states.** Calibration, stated honestly: `pure_rolling` is medium (5–6 band). It lands at 8 for two declared reasons, neither of which is padding: (i) the founder ruling splits the slide/spin/sum decomposition into three one-idea states — Rule 31's "one idea + one complete motion" applied to what genuinely is three ideas; (ii) the advanced ring carries the slipping→rolling capture as its own ring-gated state. The offset: old S1 is folded into S1 below. The core block is **3 states + explore = 4 under the deepest preset cut**, inside the band; the full-preset 8 is the JEE-complete lesson. Rings: core S1–S3, extended S4–S6, advanced S7 (contiguous, immediately before explore), explore S8.

| State | Title (Rule 41 — literal, result-stating, rail-truncation-safe) | Purpose | teaching_method | depth_ring |
|---|---|---|---|---|
| STATE_1 | One turn, one circumference | Hook + condition: the wheel rolls with v and ω both live; each full turn advances exactly 2πR ⇒ v = Rω | straightforward beat | core |
| STATE_2 | The contact point is at rest | PRIMARY aha: the rim point's cycloid comes to a cusp; contact marker holds `0.00 m/s` | straightforward beat (16a) | core |
| STATE_3 | No sliding, no kinetic friction | RM-G7 kill: a locked wheel skids, slows, stops; the rolling wheel does not — two phases, ONE state (`activate_at_ms`) | straightforward beat (16a) | core |
| STATE_4 | Sliding only: one speed everywhere | Part-motion A: every point moves at the same v | straightforward beat | extended |
| STATE_5 | Spin in place: centre at rest | Part-motion B: rim ±Rω, centre at rest | straightforward beat | extended |
| STATE_6 | Slide plus spin makes rolling | The sum: the point speeds come out 2v / v / 0 | straightforward beat | extended |
| STATE_7 | Sliding becomes rolling | A wheel launched moving but not spinning: kinetic friction spins it up until v = Rω, then rolling holds | straightforward beat | advanced |
| STATE_8 | Roll it yourself | Sandbox — controls per `min_ring`, core-ring content only | exploration_sliders | explore |

The hook MOVES (S1 rolls from its first frame). No `narrative_socratic`, no `wait_for_answer`, no `pause_after_ms`. **Archetype no-repeat check (P2-3):** the single repeat is S1/S6, a **declared `reveal-build` contrast pair** — S1 builds the relation read from the GROUND (marks + bracket), S6 builds it read from the POINTS (the arrow set); each delta cue names its side of the flip. All other archetypes distinct.

## 3. Per-state choreography + control plan (Rule 31 control table)

Every state: flat ground (`theta_deg = 0` — same code path, `:940`; no θ-arc renders, `:40071–40079`), centre lane. **Body co-presence:** S3 is the ONLY state declaring two bodies; because `nlbBodyLaneZ` (`:39992–40001`) lanes every DECLARED body regardless of visibility, S3 authors **`lane_gap_m = 0`** (single-lane semantics, sibling item (b)-10 — #11 a declared consumer) so both phase bodies sit on the centre line of the 1.6 wu slab; they are never co-visible (the retirement rule below), so z-coincidence can never occlude. Every other state declares exactly one body. Wheel: `shape: 'wheel'` (SEAM G), m = 1 kg, `radius_m = 0.25` ((b)-9), k = 0.5 authored on the body — **modelled as a uniform disc, declared in the physics block**; no k chip ever renders (do-not-prespoil — k is #12's reveal).

**ACTIVATION SEMANTICS (imported VERBATIM from the sibling — the canonical paragraph; #11's consumers noted in brackets; P1-1/P2-5):** a body may author **`activate_at_ms`** (optional; absent ⇒ active from state entry — the (b)-19 back-compat default). Before its activation instant the body is **NOT integrated** and, by default, **hidden**; AT it, the body is seeded at its authored `initial_position_m`/`v₀` (and ω₀ per (c)-2) and integrates on the state-local clock from there. Two derived rules, both expressed on the SAME authored instants — no second timed field:
- **Single-lane retirement:** in a **`lane_gap_m = 0`** state, a body is live from its own `activate_at_ms` until the NEXT body's `activate_at_ms`, at which instant it **retires** — hidden, no longer integrated, its trail cleared (A4). This is the "explicit single-lane semantics" of Ruling 3(iii): sequential presentation on one lane, phase boundaries = the activation instants themselves. The retirement cut is HARD (the narrow buy has no dissolve animation) — declared, and bridged by narration where used *(#11: S3)*.
- **Held-visible single body:** a state's ONLY body may author **`visible_before_activation: true`**: it renders at its seed pose with v = 0, a = 0 until its activation *(#11 authors NO consumer of this flag — every single-body state is active from entry; the clause is carried so the two documents state one contract)*.

**Scope guard (restated at the R-2 boundary — the buy is TWO timed fields, exactly):** `activate_at_ms` (per body) and per-line `formula_overlay[].at_ms` ((c)-9) are the ONLY timed actions bought. Retirement and `visible_before_activation` are derived semantics/flags on the activation instants. **No per-arrow reveal, no per-label reveal, no per-body show/hide timeline, no timed property changes, no general choreography DSL — arrows are STATIC from state entry and sequence via `phases[].glow_focal` windows.** If the surgeon finds a THIRD timed class needed, that is the alarm rule firing — STOP and re-scope with the survey.

**VELOCITY-ARROW MAP (the second channel):** authored **`velocity_scale = 0.92` wu per m/s · `velocity_min_len = 0.25` wu** (fleet defaults preserved when absent — presence resolved by `typeof`, never truthiness, per (b)-19 as amended).

| Arrow | value (m/s) | Rendered L (wu) | Clamp? |
|---|---|---|---|
| S2 centre | 0.6 | **0.552** | no — **2.2× the 0.25 floor** (> the scar row's 1.5×) |
| S4 top/centre/bottom | 1.0 each | 0.92 each | no — equal by physics, equal in pixels |
| S5 top / bottom | ±1.0 | 0.92 each | no |
| S6 top | 2.0 | **1.84** | no (< MAX 2.80) |
| S6 centre | 1.0 | **0.92** | no — ratio **2 : 1 rendered exactly** |
| **S8 centre (v₀ 0.5–2.5)** | 0.5–2.5 | **0.46–2.30** | no — min **1.84× the floor**, max < 2.80 (the P2-2 range check) |
| any exact zero in a GUIDED state (S2/S5/S6 contact or centre) | 0.0 | **labelled stationary MARKER: dot + `0.00 m/s`** | never a stub, never a floored arrow |
| **S8 contact — at ALL times** | 0 → arbitrary | **dot + its LIVE value** (e.g. `contact 0.37 m/s`) — marker semantics | **never routed through the arrow channel** — so the sub-floor band (0, 0.4076) m/s the channel cannot render honestly (full-preset ω₀ demo) always has a specified rendering (P2-2) |

This closes `field3d_nlb_arrow_min_length_floor_collapses_small_force_visibility_and_ratio` for #11 across the WHOLE envelope, sandbox included. #11 draws **no force arrows** — its `f_k 1.96 N` / `f 0.00 N` are readouts.

| State | Teaches | Archetype | Distinct motion | Delta cue | Live controls | Words | Ring |
|---|---|---|---|---|---|---|---|
| S1 | Rolling has both v and ω, bound by v = Rω | `reveal-build` (pair of S6 — reads the relation from the **ground**) | The wheel rolls at 0.9 m/s (readouts `v` and `ω` live from frame 1, ω dual-labelled once); **mark `0` stamps at the release point**; at the first COMPLETED turn (CAUSE: the marked spoke returns upright; EFFECT one readable beat later) mark `1` stamps and the bracket `2πR = 1.57 m` draws **mark 0 → mark 1** (turn-count physics trigger, not an authored ms); the formula surface then builds per-line **((c)-9, bought)**: `one turn → 2πR` @ 2300 → `v = Rω` @ 2600; readouts `v 0.90` and `Rω 0.90` sit equal. Concrete before abstract (the equation arrives at sentence 4 of 5). **Five-sentence plan, 53 words total (P2-4):** ① "The wheel's CENTRE moves along the road at speed v" — the Block-1 anti-planting opener (10) · ② ω dual-label + prerequisite patch: "its turn rate ω (angular velocity) counts the angle turned each second" (12) · ③ "each completed turn puts exactly one circumference, 2πR, of road behind it" — spoken over the mark-1/bracket beat (12) · ④ "so the two are locked: v = Rω" — the equal readouts (10) · ⑤ the anchor: "A bicycle wheel crosses the road exactly this way" (9). R drag = Rule-31 extra: re-lifts, re-scales, respaces marks + bracket live; **a trusted R drag SEIZES the loop (`:42171` → `:43022`) — the wheel then rolls to the −3.0 bound (run extends 4.5 → 5.4 m; marks keep stamping) and holds; `energy_active` false, so the bound stop renders no energy event — declared** | "One turn, one circumference" | R (0.15–0.35 m) | 45–55 (plan: 53) | core |
| S2 | The contact point is instantaneously at rest | `flow-along-path` | Slow roll (0.6 m/s), camera target at the **run midpoint s = +0.9**; **framed extent = run + one body DIAMETER each end (the sibling's A-11 rule, adopted): run [+2.4, −0.6] + 0.5 m margins = 4.0 m ≈ [+2.9, −1.1]** — body bounding box inside the viewport at t = 0, at the pin, and at state end (P2-1). Centre arrow (0.552 wu) and **contact zero MARKER (dot + `0.00 m/s`)** are **static from state entry (R-2 — no arrow reveal exists)**; a marked rim dot streams along its cycloid; at the ground touch the trace comes to a CUSP and the dot visibly stops while the wheel keeps moving; the arch completes top-to-top at 2618 ms. Top arrow deliberately NOT shown (S6's reveal). Body-rect ∩ `nlbPanelRects()` = ∅ under this close camera | "Contact point speed: zero" | none | 35–50 | core |
| S3 | Sliding contact = kinetic friction; rolling contact = none | `cycle-compare` — two-phase 16a contrast, ONE state (`activate_at_ms` + `lane_gap_m = 0`) | Phase A (t = 0): `nlb_wheel_locked` (`rotation_locked`) skids in at 2.0 m/s, μ_k = 0.2 → 1.96 m/s², skid trail, `f_k 1.96 N`, stops at 1020 ms (s = +1.38) and **holds. At 1500 ms — phase B's `activate_at_ms`, the concept's ONLY activation instant — phase A RETIRES under the single-lane rule: a HARD cut (hidden, un-integrated, skid trail CLEARED; no dissolve — the narrow buy has no dissolve animation), bridged by narration** ("the skidding wheel is done — now the same track, a rolling wheel"). Phase B: `nlb_wheel_roll`, **`activate_at_ms = 1500`** — hidden and un-integrated before it, seeded at s₀ = +2.4, v₀ = 2.0 on that instant — crosses at constant speed: **no trail anywhere on the track (the retired trail is cleared, so the pin cannot photograph a roller trailing a skid mark)**, `contact 0.00`, `f 0.00 N` (honest ONLY under the A1 branch-priority rule `:45497–45499`). Activation a pure function of state-local t (pin/rewind byte-stable). Wrong picture first, kill after, back-to-back, no click, no empty-track gap. Secondary anchor (≤12 words) closes | "Skid slows; roll does not" | none | 40–55 | core |
| S4 | Pure translation: every point at the same v | `translate-through` | Frictionless flag. The wheel SLIDES at 1.0 m/s with spokes visibly NOT turning; **all three velocity arrows (top/centre/bottom, 0.92 wu each — exactly equal) are static from state entry (R-2)**; the sequencing is a **glow WALK**: `phases[]` windows top 300–600 / centre 600–900 / bottom 900–1200 ms, **hand-back to all three at EQUAL brightness by 1200** (the payoff is equality). One idea: no turn ⇒ one speed everywhere | "Sliding: one speed everywhere" | none | 25–35 | extended |
| S5 | Pure rotation: rim ±Rω, centre at rest | `rotate/flip` | Frictionless flag. The wheel SPINS IN PLACE at the home pose, ω = 4 rad/s, v = 0 ((c)-2's `omega0_rad_s` with v₀ = 0): **top arrow 0.92 wu forward, bottom arrow 0.92 wu BACKWARD, centre zero marker — all static from entry (R-2)**; glow WALK: top 300–600 / bottom 600–900 (the surprising element) / centre marker 900–1200, **hand-back to ALL THREE named overlays at full brightness by 1200 < the 2400 pin (P1-2)**. One idea: pure turn ⇒ opposite rim speeds, still centre | "Rim turns, centre stays" *(4 words, P3-2)* | none | 25–35 | extended |
| S6 | The sum: 2v / v / 0 | `reveal-build` (**declared contrast pair of S1** — reads the relation from the **points**; the retired `superpose-combine` coinage no longer described this motion, P2-3) | The rolling wheel (v = 1.0, ω = 4.0, v = Rω ✓) rolls; **all three point elements (top 1.84 wu / centre 0.92 / contact zero marker) static from entry (R-2)**; glow WALK windows top 300–600 / centre 600–1000 / contact 1000–1400; then the formula surface builds per-line **((c)-9, bought)**: `v + Rω = 2v` @ 1800, `v − Rω = 0` @ 2400. **No state-level glow_focal** — hand-back complete by 2400: all three at equal brightness before the 2880 pin | "Slide plus spin: roll" | none | 40–55 | extended |
| S7 | Kinetic friction drives a slipping body TO rolling | `regime-switch` (shared coin with sibling, honestly differentiated: theirs = steady → threshold → FAILURE of rolling; this = convergence → capture → steady rolling) | Launch v₀ = 2.0, ω₀ = 0 ((c)-2) on μ_k = 0.05: contact slides (trail, spokes lag), kinetic friction slows v AND spins ω up; `v` and `Rω` readouts CONVERGE while the `contact` readout counts DOWN; capture at **t_c = v₀k/(μ_k g(1+k)) = 1361 ms** (closed-form — uniform disc, k = 0.5; at k = 1 t_c would be 2041 ms = 60.0% of R, ON the pin — the modelling declaration is load-bearing), trail stops, rolling persists (A1 branch priority: post-capture `f 0.00`). v₀ slider = Rule-31 extra (envelope below) | "Sliding becomes rolling" | v₀ (1.0–2.5 m/s) | 35–50 | advanced |
| S8 | Everything, teacher-driven | `drag-sandbox` | One wheel, sandbox wrap, trusted drag; readouts v, Rω live; **contact = dot + its LIVE value at all times (P2-2)**; rim trace on; **turns counter + revolution marks ON (core furniture from S1 — under the reduced preset the R dial visibly changes distance-per-turn)**. On every wrap: v AND **ω re-seed to the authored seeds** ((c)-7); trail/trace break at the wrap. CORE content only (38b): contact dot + centre arrow, v = Rω furniture — no top arrow, no formula surface, no k | "All controls live" | see min_ring table | 0 / open | explore |

**S8 explore controls with `min_ring` (μ_k paragraph kept verbatim per Checkpoint A ruling):**

| Control | `min_ring` | Guided state that teaches it |
|---|---|---|
| v₀ (0.5–2.5 m/s) | core | S1 (speed is S1's defined variable) |
| R (0.15–0.35 m) | core | S1 |
| ω₀ starting spin (0–12 rad/s) | **advanced** | S7 (a v–ω mismatch is S7's lesson) |
| μ_k (0.02–0.30) | **advanced** | deliberately advanced, NOT S3: with ω₀ hidden the sandbox always starts rolling, where μ_k does nothing visible on level ground (f = 0) — a dial that changes nothing fails the teacher test; its sandbox lesson (capture time) is S7's |

*Hide advanced* → v₀ + R survive; the sandbox always starts rolling (ω₀ defaults to v₀/R), every surviving readout is taught, **and the turns counter gives the R dial a visible consequence** ✓. *Hide advanced+extended* → same set ✓. Full preset: a teacher authors a v–ω mismatch and watches capture — the explaining cue (converging v / Rω / contact) is CORE furniture, so no ring-suppressed-cue defect is possible.

**Revolution marks — own primitive:** own mesh + turn-count trigger + live respace under an R drag; **no `checkpoints` reuse** (four collisions: enum `:1500`, stamp target `:1487`/`:44373`/`:44797`, `energy_active` `:42747`, `NLB_CP_MAX = 3` `:43547`). **R-slider envelope at BOTH run lengths (P3-1) — unseized run 4.5 m / seized-to-bound run 5.4 m:** at R = 0.15 (2πR = 0.9425 m) → marks 0–4, **5 marks** unseized, positions +2.4 / +1.4575 / +0.5150 / **−0.4274** / −1.3699 (P3-5); under a seize mark 5 stamps at **−2.3124** → **6 marks**. At R = 0.25 → 3 marks unseized (+2.4 / +0.829 / −0.742; mark 1 is 2.171 m from the +3.0 bound, mark 2 is 2.258 m from −3.0); seized, mark 3 at −2.3124 → **4 marks**. At R = 0.35 → 3 marks (+2.4 / +0.201 / −1.998) both ways (mark 3 would land at −4.197, out of bounds). All in-bounds across the whole envelope, seized or not.

**Home pose + track geometry:** `length_m` is a half-length (`:941`; readers `:40061–40067`, `:44176`) → **`surface.length_m = 3.0`, ONE value concept-wide**, 6.0 m track, s ∈ [−3.0, +3.0]. **`initial_position_m = +2.4`** every state (0.6 m inset ≥ 2× the 0.25 m radius). All motion toward −s; readouts display magnitudes.

**Loop-reset / frozen-pin timing (g = 9.8; last asserted event = the COMPLETION of the state's assertion chain — glow hand-backs and bought formula lines included; pin phase fixed at 0.60R, `deriveStateMeta.ts:2937–2957`; json_author re-verifies at h = 1/60; conditional on the state-local clock, (b)-11/E1):**

| State | R (ms) | Assertion chain → last asserted event | Time (% R) | Pin 0.60R | What the pin photographs · margin |
|---|---|---|---|---|---|
| S1 | 5000 | mark 0 @ 0 · mark 1 @ 1745 (physics trigger) · bracket 1745–2050 · formula L1 @ 2300 · **L2 @ 2600** ((c)-9) | 2600 (52.0%) ✓ | 3000 | wheel at s = −0.30, marks 0+1, full bracket (BOTH endpoints), full formula, equal readouts · 400 ms ✓ (mark 2 @ 3490 NOT asserted; run ends s = −2.1 ✓) |
| S2 | 5000 | arrow + marker static from entry · rim-dot glow window over the arch · first cusp @ 1309 · **arch completes top-to-top @ 2618 = glow hand-back complete** (P3-3) | 2618 (52.4%) ✓ | 3000 | one full cycloid arch + cusp, marker `0.00`, all named overlays full-bright, wheel at s = +0.6 — inside the framed extent · 382 ms ✓ (run ends s = −0.6 ✓) |
| S3 | 4000 | A: stop @ 1020 (s = +1.38), holds · **A RETIRES @ 1500 (hard cut — hidden, un-integrated, trail CLEARED) = B's `activate_at_ms`** · B activates 1500 (HELD until then), readouts settle @ 1800 | 1800 (45.0%) ✓ | 2400 | phase B ROLLING at s = +0.60 (t_B = 900 ms), `contact 0.00`, `f 0.00`, **no trail anywhere, no skidder** · 600 ms ✓ (B ends s = −2.6 @ 4000, 0.4 m inside the bound; no empty-track instant — A visible until exactly 1500) |
| S4 | 4000 | arrows static from entry · glow walk 300–600/600–900/900–1200 · **hand-back all-equal @ 1200** | 1200 (30.0%) ✓ | 2400 | mid-slide at s = 0.0, spokes still, three equal arrows at equal brightness · 1200 ms ✓ (ends s = −1.6 ✓) |
| S5 | 4000 | arrows + marker static from entry · glow walk 300–600/600–900/900–1200 · **hand-back complete @ 1200** (P1-2) | 1200 (30.0%) ✓ | 2400 | mid-spin at the home pose, ±0.92 wu arrows, centre `0.00`, all full-bright · 1200 ms ✓ (no translation — no bound) |
| S6 | 4800 | elements static from entry · glow walk 300–600/600–1000/1000–1400 · formula L1 @ 1800 · **L2 @ 2400** ((c)-9; glow hand-back complete 2400) | 2400 (50.0%) ✓ | 2880 | rolling at s = −0.48, all three arrows at EQUAL brightness + both formula lines · 480 ms ✓ (ends s = −2.4 ✓) |
| S7 | 3400 | **capture @ 1361** (closed-form; slide = 2.722 − 0.454 = 2.268 m → capture at s = +0.132; v_roll = v₀/(1+k) = 1.333 m/s); contact glow window ends AT capture | 1361 (40.0%) ✓ | 2040 | post-capture rolling at s = −0.77, trail ended, contact 0.00, **every named readout full-bright** · 679 ms ✓ (ends s = −2.586, 0.414 m inside — no clamp) |
| S8 | — | free-run sandbox (Rule 37); synchronised single-body wrap with v + ω re-seed | — | — | — |

**Authored-millisecond classification sweep (P1-1 — verified over the whole document):** every authored time above is one of exactly three classes — (i) a **physics event** (mark stamps, bracket trigger, cusp/arch, the 1020 stop, the 1361 capture — all closed-form or event-triggered, never authored ms); (ii) a **`phases[].glow_focal` window** (existing channel `:45296–45310`); (iii) one of the **two bought fields** — the single `activate_at_ms = 1500` (S3) and the per-line formula `at_ms` values (S1: 2300/2600; S6: 1800/2400, (c)-9). **No timed instant of any other class exists in this concept.** The REV 2 dissolve at 1400 was the only violation; it is deleted.

**S7 slider envelope:** **d(v₀) = v₀²k(2+k)/(2μ_k g(1+k)²) = 0.567·v₀²** at μ_k = 0.05, k = 0.5. Over v₀ ∈ [1.0, 2.5]: d_max = 3.543 m → capture completes at s ≥ −1.143, always ≥ 1.857 m before the −3.0 bound ✓. A trusted v₀ drag SEIZES the loop (`:42171` → `:43022`); the wheel then rolls to the track end and stops under the geometric bound — honest, readable, and with **no energy layer authored anywhere in this concept** no false energy event renders; declared.

**Per-state glow plan (rewritten under the P1-2 per-state ruling; Rule 32e caps at one focal, it does not require one):**

> **Relation rule, stated per state so it cannot be missed by a future restructure:** *if a state's claim is a RELATION between two or more overlays (named in its delta cue or `tts_sentences`), author NO state-level `glow_focal`; drive emphasis from `phases[]` windows with hand-back to full brightness on every named overlay, completing before the 0.60R pin.*

| State | Relation check (the per-state ruling line) | Channel | Emphasis |
|---|---|---|---|
| S1 | relation (v = Rω) — carried by windows + hand-back | `phases[]` (`nlbRunPhases` `:45296–45310`) | bracket window at its draw → formula-line windows → hand-back after 2600 |
| S2 | **RELATION** — the counter compares centre arrow AND contact marker (P1-2) | **NO state-level `glow_focal`**; `phases[]` | rim-dot window over the arch; hand-back to rim dot + centre arrow + contact marker ALL at full brightness by **2618 < the 3000 pin** |
| S3 | each phase's claim = one readout | `phases[]` | phase A: the f_k readout · phase B: the contact readout |
| S4 | relation (equality of three) | `phases[]` | per-arrow walk windows, hand-back to all three EQUAL by 1200 (no single arrow may stay focal) |
| S5 | **RELATION** — cue names rim AND centre (P1-2) | **NO state-level `glow_focal`**; `phases[]` | top → bottom → centre-marker windows; hand-back to ALL THREE named overlays at full brightness by **1200 < the 2400 pin** |
| S6 | relation (three arrows + two lines) | **NO state-level `glow_focal`**; `phases[]` | walk windows per element; hand-back complete by 2400, before the 2880 pin |
| S7 | narration names v, Rω AND contact — so no overlay may sit dimmed at the pin | `phases[]` (window, not a state-level focal) | contact-readout window 800 → **1361 (ends AT capture)**; hand-back after — every named readout full-bright at the 2040 pin |
| S8 | sandbox | none | — |

**Camera plan (per-state `camera_position`, target per (b)-13; framed-extent rule = run + one body DIAMETER (0.5 m) each end, adopted from the sibling — P2-1):** S1 near side-on (seized run 5.4 m → extent 6.4 m ≈ [+2.9, −3.5]); S2 closed, **target s = +0.9 (run midpoint), extent 4.0 m ≈ [+2.9, −1.1]**, in-frame acceptance at t = 0 / pin / state end, body-rect ∩ `nlbPanelRects()` = ∅; S3 wide (phase-B run 5.0 m → extent 6.0 m ≈ [+2.9, −3.1], containing phase A's run); S4/S5/S6 side-on medium (extents [+2.9, −2.1] / home-pose framing / [+2.9, −2.9]); S7/S8 wide, mild yaw (S7 extent ≈ [+2.9, −3.09]). S3's two declared bodies are z-coincident (`lane_gap_m = 0`) and never co-visible (retirement), so nothing can occlude; every other state is single-body.

**Rule 32 legibility:** cause before effect everywhere (S1 spoke-upright → mark a beat later; S3 skid → slow → stop → hard-cut retirement at the activation instant; S7 friction acts → readouts converge → capture). Only the taught variable moves per state. Same apparatus from the home pose; S3's phase bodies are distinct ids (`nlb_wheel_locked`, `nlb_wheel_roll`), `rotation_locked` constant per id. Single glow focal per instant via the channel table. Body labels are mass symbols or absent.

## 4. Misconception confrontation plan (Rule 16a — 2 genuine pivots)

| Wrong belief | Source | At | `misconception_watch` beat |
|---|---|---|---|
| "Every point of the wheel moves at the wheel's speed — the bottom slides along the road" | PER + catalog | STATE_2 | `belief`: the contact point moves forward at v like the rest of the wheel. `visual_counter`: the rim dot's cycloid comes to a CUSP — the dot visibly stops at each ground touch — while the centre arrow holds 0.552 wu and the contact MARKER holds `0.00 m/s` (both at full brightness at the pin — the S2 glow row). `one_line_fix`: the turn carries the bottom point backward at exactly the speed the wheel moves forward, so at the ground the two cancel to zero. **Named primitives:** rim dot + cycloid trace + zero marker |
| "A rolling wheel slides against the road — its friction is kinetic and always slows it" (RM-G7) | RM-G7 | STATE_3 | `belief`: rolling contact is sliding contact, so kinetic friction acts and drains speed. `visual_counter`: the locked wheel skids ALONE first — slows at 1.96 m/s², leaves a trail, stops — **and at the rolling wheel's activation instant it RETIRES (hard cut, trail cleared)**; the rolling wheel then crosses at constant speed, no trail anywhere, `f 0.00 N`. Wrong picture first, real physics after, back-to-back in one state. `one_line_fix`: kinetic friction needs a sliding contact; a rolling contact point is at rest, so only static friction can ever act there — and on level ground at steady speed even that is zero. **Named primitives:** `rotation_locked` + skid trail + f_k readout + `activate_at_ms` |

No other state carries a `misconception_watch` (S4–S6 are straightforward teaching). EPIC-C branches: ZERO.

## 5. `has_prebuilt_deep_dive` states

- **STATE_2** — the PRIMARY aha and the historically stickiest claim.
- **STATE_7** — the exam-heavy state (sliding-then-rolling problem class), importing the most prerequisite machinery.

Divergence note: the S1 prerequisite cliff is patched by one narration sentence and needs no deep-dive. All other states un-flagged (Rule 18).

## 6. Drill-down clusters

**STATE_2:** `why_the_contact_point_is_at_rest` · `cycloid_path_of_a_rim_point` · `backward_turn_speed_cancels_forward_speed`.
**STATE_7:** `time_for_sliding_to_become_rolling` · `friction_direction_during_slipping` · `final_speed_after_rolling_begins`.

## 7. `entry_state_map`

```
entry_state_map:
  foundational: STATE_1 → STATE_3
  point_speeds: STATE_4 → STATE_6
  slipping:     STATE_7
```
Default `foundational`. Cross-slice pill after S3: "How fast is the TOP of the wheel moving?" → STATE_4. PRIMARY aha (S2) inside foundational ✓.

## 8. Prerequisites (advisory, Rule 23)

`rotational_kinematics` (#4 — ω and v = ωr; cliff at S1) · `friction_force` (SHIPPED, same scenario — S3's skidding half IS the patch) · `tau_eq_i_alpha` (#7 — S7's spin-up; one patch sentence) · `moment_of_inertia` (#6 — S7 consumes k silently). Successor: `rolling_on_incline` (#12) recaps this concept in its v = Rω recap state, on the same wheel radius. *(Cross-document references name CONTENT, not bare state numbers — P2-5.)*

Namespace check: `pure_rolling` collides with no rostered physics or chemistry id.

## 9. Real-world anchor (Rule 35 / 38f — universal, culture-neutral)

**Primary (STATE_1, sentence ⑤ of the plan):** *"A bicycle wheel crosses the road exactly this way."* Widest-syllabus-overlap rolling device on Earth (38f); no place, brand, or culture; physics-true at every depth. **Secondary (STATE_3, ≤12 words):** *"A braked, locked wheel leaves a skid mark; a rolling wheel leaves none."* **Deliberate chapter coherence:** the bicycle wheel is #11's primary and #12's secondary BY DESIGN — the same remembered object first on the flat, then on the slope.

**DC Pandey check:** chapter table of contents only. No teaching sequence, example problem, figure, or phrasing imported. NCERT §7.14's decomposition is re-derived from first principles as S4–S6, not copied.

## 10. Definition of Done (Gate 0 — no TBDs)

**(a) States:** the 8 of §2, exactly as tabled in §3, including geometry, timing table, glow table, camera plan.

**(b) Symbol-label table:**

| Quantity | On-canvas label |
|---|---|
| Centre speed | `v 0.90 m/s` (metric: \|v\|) |
| Turn rate | `ω 3.6 rad/s` (dual-label once in S1, then bare — 38d) |
| Radius | R (slider row "Radius R"; drives lift + scale + spin + mark respace, (b)-9) |
| Rolling-condition readout | `Rω 0.90 m/s` beside v (metric: \|ω\|·R) |
| Contact-point speed | `contact 0.00 m/s` (metric: \|v − ωR\|) |
| Circumference bracket | `2πR = 1.57 m`, drawn **mark 0 → mark 1** (derived from authored R) |
| Revolution marks | ground ticks `0`, `1`, `2`, … at s_n = 2.4 − n·2πR — **own primitive**: turn-count trigger, live respace, no stamp, no `energy_active`, no cap |
| Turns counter (S8) | `turns 3` (the turn count is DEFINED by S1's marks; the counter readout re-uses it) |
| Friction readouts (S3/S7) | `f_k 1.96 N` on the skidder · `f 0.00 N` on the roller (honest via the A1 branch-priority rule) |
| Velocity arrows (**S2/S4/S5/S6/S8** — P2-2) | value-labelled arrows through the **velocity channel** (0.92 wu·s/m, floor 0.25) — computed from live (v, ω), never constants; **static from state entry, sequenced by glow walk (R-2)**; S8 range check: centre arrow 0.46–2.30 wu over v₀ ∈ [0.5, 2.5] |
| Zero-velocity marker (guided states) | dot + `0.00 m/s` — never a stub, never a floored arrow |
| **S8 contact element (P2-2)** | **dot + its LIVE value at ALL times** (marker semantics, never the arrow channel) — the specified rendering of the sandbox's arbitrary contact speeds, including the sub-floor band (0, 0.4076) m/s |
| Skid trail | drawn during \|v − ωR\| > 0 only; breaks at every wrap/reset; **cleared at a body's retirement** |
| Sliders | `Speed v₀` · `Radius R` · `Starting spin ω₀` · `Friction μ_k` |
| Formula surfaces | S1: `one turn → 2πR` ⇒ `v = Rω` · S6: `v + Rω = 2v` then `v − Rω = 0` — on `#nlb_formula` (Cambria Math), **as ordered line lists with per-line `at_ms` ((c)-9, bought; registered in `deriveStateMeta`)**; longest line verified in pixels against the 340 px max-width ((b)-14) |

All Unicode across all three text paths (Rule 34c). **k = I/mR² never renders.**

**(b′) Term-introduction ledger (re-read in full against the eight-state numbering — P1-3):**

| Symbol/term | DEFINED in | First USED in | ✓ |
|---|---|---|---|
| v, ω (dual-label), R, 2πR, marks, bracket, v = Rω, Rω readout, turn count | S1 | S1 (Rω re-used S2–S8; marks/turn count re-used S8) | ✓ |
| contact readout, rim dot, cycloid trace, zero marker | S2 | S2 (marker re-used S5/S6; S8's contact dot + live value is the marker's declared sandbox extension, P2-2) | ✓ |
| **centre velocity arrow (velocity channel)** | **S2** | S2 (re-used S4/S6/S8) — *split from the fan row: REV 2's "defined S4" contradicted S2's own render* | ✓ |
| f_k, skid trail, μ_k, `f 0.00 N` | S3 | S3 (re-used S7) | ✓ |
| **three-point arrow FAN (top/centre/bottom compared)** | S4 | S4 (re-used S5 as the rim pair, S6 as the sum set) | ✓ |
| **the SUMMED top-point arrow at 2v** | **DEFINED S6** | **must not render in S1–S3 or S8; S4 and S5 render top-point arrows at their PART values (v and +Rω), which are the setup for S6's sum** *(P1-3 — re-scoped to the summed VALUE, not the position)* | ✓ |
| ω₀ (starting spin, as a SYMBOL) | S7 | S7 + S8 slider (S5 shows spin-in-place without naming the symbol) | ✓ |

**(c) Right-hand-rule plan:** N/A — no direction rule taught. Declared deliberately.

**(d) Motion plan:** per §3; nothing static, nothing asserted-but-unrendered (**the S3 phase boundary is the hard retirement cut at the activation instant — no dissolve exists anywhere in this concept**). Spin position-driven through the body's own `radius_m` when rolling, integrator-driven when slipping. **Modelling declaration: the wheel is treated as a uniform disc, k = 0.5.** Every archetype discharged by the authored beat with zero teacher input.

**(e) Modes:** reuses sibling modes `rolling_contact` (S2) and `rolling_friction_contrast` (S3) at θ = 0; adds `rolling_intro_circumference` (S1), `rolling_slide_only` (S4), `rolling_spin_only` (S5), `rolling_sum` (S6), `rolling_capture` (S7); reuses `sandbox` (S8). deriveStateMeta co-edit at all sites — **including the (c)-9 formula-line `at_ms` registration**.

**(f)** `assessment` + `coverage_map` span: v = Rω (S1), contact at rest (S2), friction type (S3), point speeds 0/v/2v (S4–S6), capture (S7). `misconception_watch` = exactly §4's two beats.

**(g) Macro↔micro (Rule 33):** N/A — the mechanism is contact-scale and is itself the taught picture. 33d met: every instrument shows a live numeric value; every derived readout specified by METRIC.

**(h) Canvas budget (Rule 34):** ONE formula surface max per state (S1, S6 only — per-line builds, (c)-9); caption = the ≤5-word delta cue only; HUD value-only; panels at `top:52px+`.

**(i) Curriculum-flex (Rule 38):**
- **(i-1)** *Hide advanced (S1–S6 + S8):* condition → contact at rest → friction type → slide → spin → sum → sandbox (v₀, R). No surviving narration references slipping, capture, ω₀-as-symbol, or μ_k's sandbox role. *Hide advanced+extended (S1–S3 + S8):* a complete qualitative-plus-v=Rω lesson; the S8 turns counter keeps the R dial consequential.
- **(i-2)** S8 surfaces CORE content only: v, Rω readouts, contact dot + live value, centre arrow, trace, marks + turns. No explore formula surface exists.
- **(i-3) `curriculum_tags` (claims, not facts):** CBSE/NCERT §7.14 core+extended — verified at authoring; advanced S7 = JEE Main/Advanced — verified against the DCP index. AP Physics C full · AP Physics 1 core only · IB DP core+extended · A-level core only — every one `needs_teacher_verification`.
- **(i-4) Presets:** `full` = S1–S8; `mainstream` = hide S7; `qualitative` = hide S4–S7.
- **(i-5)** No graph panel — N/A, declared.
- **Notation ladder (38c):** all surfaces algebra-only. **Dialect (38d):** dual-label once, then bare.

**(j) Teacher-walk answers:** (1) S1 states v = Rω and shows the equal readouts + bracket; S2 shows the cusp + `0.00`; S4–S6 build the 0/v/2v figure; S7 shows the capture. (2) Drag R in S1 and watch marks + bracket respace; in S8 at full preset set ω₀ = 0 and watch capture (the contact dot's live value counting down is now the specified rendering, P2-2); under the reduced preset drag R and watch distance-per-turn. All in range, envelopes stated at both run lengths. (3) Term ledger (b′). Declared omissions: k and the incline are roster design (#12); rotational KE is #8; the ω-vector is #9.

---

## Two-pass cognitive lens

### Block 1 — Pass-1 strategic checklist

1. **Prerequisite cliff.** `rotational_kinematics` → **S1**: patch sentence ② of the plan. `friction_force` → **S3**: the skidding phase-A IS the patch. `tau_eq_i_alpha` → **S7**: one sentence — "friction's turning effect on the rim speeds the spin up, a torque doing to ω what a force does to v." `moment_of_inertia` → S7 consumes k = 0.5 silently; the modelling line covers it.
2. **JEE-backwards trace.** *"A wheel is projected along a level floor at v₀ with no spin; friction coefficient μ_k. Find (i) the time when pure rolling begins, (ii) the speed then, (iii) the speed of the highest point at that moment."* v = Rω as the capture condition → S1; contact-at-rest legitimising "pure rolling" → S2; kinetic while sliding, static after → S3; spin-up + t_c + v_roll → S7; top point 2v → S6. No missing piece.
3. **Misconception entry mapping.** Contact-moves-at-v → S2. *Planting risk:* S1's sentence ① says "the wheel's CENTRE moves at v" — the over-generalisation is never planted. RM-G7 → S3. *Planting risk:* S1–S2 stay friction-neutral.

### Block 2 — Aha-moment designation

- **PRIMARY aha, at STATE_2:** "the point where a rolling wheel touches the road is, at that instant, NOT MOVING." The 10-year memory.
- **SUPPORTING aha, at STATE_3:** "because the contact never slides, rolling does not slow the way skidding does" — the primary's payoff, and the reason wheels and ball bearings exist.
- **Wrong-belief setup:** primary — S1 builds a confident "the wheel moves at v" picture; S2 breaks it at the cusp. Supporting — everyday "friction slows moving things" plus S2's fresh contact story sets up "so friction must act down there"; S3's `f 0.00 N` breaks it.
- **Foundational coverage:** S2 ∈ foundational (S1–S3) ✓.

---

## SCAR AUDIT (the eleven-concept sweep; REV 2's disposition table stands with these deltas — **29 of 29 rows dispositioned**)

- `state_glow_focal_dims_one_half_of_the_relation_the_state_exists_to_teach` (MAJOR/OPEN) — **now applied to ALL EIGHT states via the per-state relation-check column** (P1-2): S2/S5/S6 author NO state-level focal; S7 moved to a window ending at capture; S4's hand-back ends equal. UPSERT note (widen to `pure_rolling`) carried.
- `eye_h2_frozen_frames_of_moving_elements_wobble…` (the P3-4 gap) — **dispositioned:** #11 inherits the sibling's amended (b)-19 acceptance by reference — frozen captures are byte-stable under the pin (Rule 36 fixed-step + `SET_TIME_FREEZE`); moving-element baselines are compared only at the pinned instant, never as a dense-series continuation.
- `hysteretic_state_cannot_be_latched_under_a_time_pin` — now binds **all three** timed/derived mechanisms: `activate_at_ms` + the retirement instant, the (c)-9 formula-line `at_ms` values, and (c)-3's closed-form capture.
- `nlb_friction_vector_first_frame_reveal_tint…` — **narrowed by R-2:** #11's velocity arrows are static from state ENTRY; the contrast-floor duty folds into E14/E8 sampling at state entry and, for S3 phase B, at the activation frame.
- All other REV 2 dispositions **stand unchanged**, re-checked against the REV 3 deltas — the only mechanics that moved are S3's retirement, the glow channels, and the arrow-static/glow-walk conversion.

---

# 0c-2 ENGINE UNION — pure_rolling contribution (REV 3 — reflects BOTH founder rulings)

> **Union closure statement:** the union is measured over BOTH concepts. Per the founder rulings: **A5 is DELETED** (three-phase states split; sequential-phase maximum TWO bodies per state); **the buy is TWO timed fields, exactly**: **(1) per-body `activate_at_ms`** ((c)-5 — with the derived single-lane retirement + `visible_before_activation`, per the imported ACTIVATION SEMANTICS) and **(2) per-line formula reveal** ((c)-9 — `formula_overlay` as an ordered line list with per-line `at_ms`, registered in `deriveStateMeta`). **It is NOT a choreography DSL: no per-arrow reveal, no per-label reveal, no per-body show/hide timeline, no timed property changes — arrows are static from state entry and sequence via `phases[].glow_focal`. A THIRD timed class is the alarm rule firing — STOP and re-scope with the survey.** **#11 is a declared consumer of (b)-10.** **Cross-document references name CONTENT, never bare state numbers** (P2-5); the sibling's REV 6 strikes its two stale "#11 S4" build-sheet references and its two #11 force-table rows — #11 draws no force arrows.

## (a) Already exists today (verified in code, file:line)

1. Flat surface — `theta_deg = 0` same code path (`:940`); θ-arc collapses and hides (`:40071–40079`).
2. Bodies with mass / `initial_position_m` / initial velocity; `v0` token (`:1340`); `mu_s`/`mu_k` per body + `surface.frictionless`.
3. Wheel mesh, tyre + hub + crossed spokes (SEAM G); lift `NLB_BODY_SIZE/2` (`:40015`) and spin divisor `NLB_WHEEL_R` (`:40053`) both replaced by (b)-9.
4. SEAM M readouts + `#nlb_formula`; scenario_cue channel + ghost/dissolve **(the dissolve is NOT used by #11 — the S3 phase boundary is the hard retirement cut)**; per-state `camera_position`; fixed-step integrator + `loop_reset_ms`; sandbox wrap + `trusted_drag_seizes`.
5. Checkpoint marker machinery — **explicitly NOT the substrate for revolution marks** (closed `capture` enum `:1500`; stamps target the ONE formula surface `:1487`; `energy_active` `:42747`; `NLB_CP_MAX = 3` `:43547`).

## (b) In the sibling's union (shared — consumed by pure_rolling)

| Sibling item | pure_rolling consumption |
|---|---|
| (b)-2 rolling acceleration branch, θ = 0 first-class (A1 as restated) | S1–S6's substrate; S3/S7's `f 0.00 N` depends on the branch-priority rule |
| (b)-3 contact-point picture (arrows computed from live (v, ω) — A2; cycloid/rim trace; contact readout) | S2 (whole state), S4–S6, S7, S8 — through the velocity channel (c)-6 |
| (b)-4 static/kinetic friction call-out | S3, S7 |
| (b)-5 slip regime (independent ω integration, α from friction torque, skid trail, spin lag) | S5 (spin-in-place via ω₀ + frictionless), S7's slide phase |
| (b)-7 `rotation_locked` per-body flag + sequential contrast cueing | S3 phase A |
| (b)-8 `controls_visible` token `R` | S1, S8 |
| (b)-9 per-body `radius_m` | every state; S1's R slider is the live-re-lift consumer |
| **(b)-10 lane geometry — CONSUMED** | S3: `lane_gap_m = 0` / explicit single-lane — both declared phase bodies at z = 0 |
| (b)-11 state-local physics clock (CRITICAL; → pcpl-surgeon) | precondition of the entire timing table AND of both bought timed fields |
| (b)-13 camera target authoring | S2's run-midpoint target (+ extent rule + in-frame acceptance at t = 0/pin/end) |
| (b)-14 overlay verifications in pixels (sprite projection probe, never DOM) | formula surfaces, label decollision, readout zone |
| (b)-19 back-compat clause | governs `velocity_scale`/`velocity_min_len`, `activate_at_ms`, `lane_gap_m`, formula-line `at_ms`, marks — absent ⇒ today's behaviour byte-identically; **presence resolved by `typeof`/`in`, never truthiness (P2-6)** |
| Explicitly NOT consumed | (b)-1 k chips · (b)-6 KE readouts · (b)-12 occlusion warning · (b)-15 centre markers · (b)-16/17/18 finish/restart/force-map (#12-only) · F8 non-wheel meshes · `visible_before_activation` (carried in the semantics, no #11 consumer) |

## (c) NEW — needed by `pure_rolling`

1. **Revolution marks + circumference bracket — OWN primitive.** Ground mark at each completed revolution (mark 0 at the release point; s_n = initial − n·2πR), labelled bracket mark 0 → mark 1, marks AND bracket respacing live under an R drag, plus a turns-counter readout. Own mesh, own turn-count trigger, **no `checkpoints` reuse, no formula-surface stamp, no `energy_active` side effect, no `NLB_CP_MAX` cap**. Envelope stated at both run lengths (P3-1). Consumers: S1, S8.
2. **Authorable initial spin, decoupled from v.** `bodies[].omega0_rad_s` (default v₀/R) + `controls_visible` token `'omega0'`. Consumers: S5, S7, S8.
3. **Slip-to-roll CAPTURE** — closed-form in state-local t (t_c = v₀k/(μ_k g(1+k))), never a per-frame latch. **Plus the A1 branch-priority rule: while the rolling condition holds, the rolling branch SUPERSEDES Branch A's `f = −sign(v)·μ_k·N` (`:45497–45499`).** The (c) centerpiece; S7/S8 payoff.
4. **Bare-ω readout token** (+ the turns token from (c)-1) — enum-diff honesty on `readouts` `:1336`. Consumer: S1 (and S8).
5. **Per-body `activate_at_ms` (founder-bought, NARROW) — with the imported ACTIVATION SEMANTICS (P1-1):** hidden and un-integrated before the instant; seeded at authored s₀/v₀/ω₀ on it; a pure function of state-local t; **single-lane retirement derived from the SAME instants** (a `lane_gap_m = 0` body is live from its own activation until the NEXT body's, at which instant it retires — hidden, un-integrated, **trail cleared**; the cut is HARD, no dissolve). Consumers: #11's 16a contrast state (S3); the sibling's two consumers per its REV 6.
6. **Two-channel vector map — the velocity channel.** `velocity_scale`/`velocity_min_len` independent of the sibling's force channel, defaults = today's constants (presence by `typeof`); authored 0.92 / 0.25. **The marker is a dot + its LIVE value — so it is also the correct rendering of a NONZERO contact speed in the sandbox; no point speed is ever routed through a clamped arrow (P2-2).** Consumers: S2/S4/S5/S6/S8.
7. **ω re-seed on the sandbox wrap.** On wrap: v AND ω re-seed to the authored seeds; trail/trace break at wrap and reset. Consumer: S8.
8. **Visible-elements matcher registration** for every new element type: marks, bracket, velocity arrows, zero/live markers, cycloid trace, skid trail, turns counter.
9. **Per-line formula reveal (FOUNDER RULING R-2 — the second and LAST timed field).** `formula_overlay` becomes an ordered line list with per-line `at_ms` (today a static string, `:44801`); same discipline as `activate_at_ms` (state-local t, byte-stable under the pin); **registered in `deriveStateMeta`** so the reveal-completion times feed the pin budget. Explicitly NOT per-arrow `show_at_ms` — arrows stay static from entry. Consumers: #11 S1 (2300/2600) + S6 (1800/2400); the sibling's derivation state.

## Survey/build-sheet AMENDMENT FLAGS

- **A1 (restated)** — (b)-2 states θ = 0 first-class AND the branch-priority rule (`:45497–45499`). Not "formula-automatic".
- **A2 (stands, widened)** — point-speed arrows computed from live (v, ω); carried WITH the two-channel map + marker ((c)-6).
- **A3 (stands)** — sibling-only. #11 needs nothing (wheel only).
- **A4 (stands, widened)** — trail/trace break at every wrap/reset/halt **and at every retirement** + ω re-seed on wrap ((c)-7).
- **A5 — DELETED (founder ruling).** Sequential-phase maximum TWO bodies per state, served by (c)-5 + (b)-10.

**Union WALK (state × row, both directions):**

| State | Rows consumed |
|---|---|
| S1 | (a)1,2,3,4 · (b)2@θ0,8,9,11,14 · (c)1,4,**9** |
| S2 | (a)1,2,3,4 · (b)2,3,9,11,13,14 · (c)6 |
| S3 | (a)1,2,3,4 · (b)2,4,5,7,**10(gap 0)**,9,11 · (c)3(branch priority),**5(activation + retirement)** |
| S4 | (a)1(frictionless),2,3,4 · (b)3,9,11,14 · (c)6 |
| S5 | (a)1(frictionless),2,3,4 · (b)3,5,9,11,14 · (c)2,6 |
| S6 | (a)1,2,3,4 · (b)3,9,11,14 · (c)6,**9** · A2 |
| S7 | (a)1,2,3,4 · (b)2,3,4,5,9,11 · (c)2,3 · A2 |
| S8 | (a)1,2,3,4 · (b)2,3,5,8,9 · (c)1,2,3,4,6,7 · A4 |

Reverse: every (a), consumed-(b), and (c) row is claimed by ≥1 state; every state claims ≥1 row. Both directions closed. Nothing else: no energy bars, no graph panel, no new meshes, no RHR hand, no finish lines (#12-only), **no timed mechanism beyond the two bought fields**. If the surgeon finds any FURTHER capability needed, that is the alarm rule firing — STOP and re-scope with the survey.

**`engine_queue` mapping (numbering per Checkpoint A; acceptances as amended there):**
- **E9** `activate_at_ms` (blocking) — acceptance: single-lane retirement derived from the same instants (hidden, un-integrated, trail cleared); presence by `typeof`, never truthiness; **the classification-sweep grep**: no authored ms in either skeleton that is not a reveal-window, a physics event, or one of the two bought fields.
- **E10** `lane_gap_m` authorable including 0 (blocking) — acceptance: the regression pair asserts authored-`0` and absent produce **different** geometry (`0` ⇒ both bodies z = 0; absent ⇒ ±0.425 wu).
- **E11** velocity channel + marker (blocking) — acceptance: the marker is a dot + its **live** value, correct for zero AND nonzero contact speeds; no point speed ever routed through a clamped arrow.
- **E12** marks-own-primitive (blocking; both-run-length envelope). **E13** ω re-seed + trail/retirement break (ride-along). **E14** visible-elements registration (ride-along, pcpl-surgeon). **E1** state-local clock (`peter_parker:renderer_primitives` → pcpl-surgeon, blocking, shared).
- **E15–E17** (minted by the sibling's REV 5: U1 rolling branch + branch priority · U2 contact picture · U4 apparatus set) — **carried into #11's dispatch list**: #11 consumes them as (b)-2/(c)-3, (b)-3/(c)-6, and the shared apparatus respectively; the surgeon's list is ONE list.
- **NEW: per-line formula reveal ((c)-9)** — proposed tag **E18**, blocking, `peter_parker:field3d_surgeon`; acceptance: ordered lines render cumulatively at their `at_ms` on state-local t; byte-stable under the pin; absent `at_ms` ⇒ today's static-string behaviour (presence by `typeof`); registered in `deriveStateMeta` (`:2739–2994` region). **Numbering reconciliation duty (P2-5):** if the sibling's REV 6 mints a different number for the same content, the CONTENT reference governs and both documents adopt one tag before dispatch.

---

**Files referenced:** `founder_proxy_A_cycle2.md` (the fix list) · `skeleton_rev1.md` / `skeleton_rev2.md` (preserved) · `rolling_on_incline/skeleton.md` (ACTIVATION SEMANTICS imported verbatim) · `rolling_on_incline/founder_proxy_A_cycle2_final.md` (R-2's machine evidence) · `src/lib/renderers/field_3d_renderer.ts` · `src/scripts/query_engine_bug_queue.ts` (line 23 — the `--field3d` gap, again not used as coverage).
