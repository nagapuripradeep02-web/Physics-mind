# Checkpoint A — `pure_rolling` REV 2 (fix cycle 2 of 2 — the last)

**VERDICT: `DESIGN_FIX` → `alex:architect` (cycle 2 of 2; there is no cycle 3 — a residual failure at REV 3 is `ESCALATE`, not another fix round)** · founder-proxy, 2026-08-02

REV 2 is a genuinely good revision. It landed the hard half of cycle 1: the lane discharge is retracted against the reader, the marks are their own primitive with the four collisions quoted, the velocity channel is authored with real arithmetic and a defined zero marker, every timing row is re-derived against reveal-chain **completion**, the fold and the split were executed honestly, and the eleven-id sweep is now the sweep (P1-F closed in substance — re-run here, same 29 OPEN rows). The credited material survived: A2, A3, the μ_k `min_ring` paragraph (verbatim modulo renumbering), the θ = 0 reading, the closed-enum diffs (now widened to three enums), the one-`length_m` discipline, and the S7 capture derivation — re-derived to the digit again, including the corrected envelope coefficient 0.567.

But the restructure was not re-run over its own prior rulings, and that is the shape of all three blocking findings. **S3 authors a dissolve at 1400 ms — a second timed instant with an animation, 100 ms before the single bought `activate_at_ms`** — inside a document whose union section repeats the founder's scope guard verbatim ("no other timed actions, no timed property changes"). **The P1-E glow ruling was applied to the two states cycle 1 named and not to the two new/renumbered states whose claims are also relations (S2, S5)** — a MAJOR/OPEN scar row whose own probe would fail on both. **And the term ledger — a cycle-1 credit item — now forbids in S4/S5 exactly the top-point arrow that §3 authors there**, which if resolved the ledger's way voids both states' entire claims. Each is a one-line, mechanically checkable correction; none touches the founder ruling; none requires redesign.

No physics-correctness doubt anywhere: v = Rω, the cusp, the superposition field, t_c = v₀k/(μ_k g(1+k)), v_roll = v₀/(1+k) all reproduce. Hence `DESIGN_FIX`, not `ESCALATE`.

**Do not churn on REV 3:** the fold (S1), the split (S4/S5/S6), the founder ruling in any part, the velocity-channel values, the marks primitive + envelope, the S7 derivation, the μ_k argument, the ring cuts, the timing table's *method*. Six rows of the timing table are correct as printed.

---

## Pass 1 — scar consultation (re-run live, by the eleven ids)

`grep -rl "newtons_laws_body" src/data/concepts/*.json` → the same eleven ids; `query_engine_bug_queue.ts <id> --open` per id → **29 distinct OPEN rows** (identical to the cycle-1 count). `pure_rolling` / `rolling_on_incline` → **0 rows** — cycle 1's five candidates and three upserts are still unfiled.

| Class | Status in REV 2 |
|---|---|
| `architect_scar_audit_claims_completeness_while_skipping_open_rows_on_the_scenario_it_extends` | **Closed in substance.** The sweep is the eleven-id grep, run live; 28 of 29 OPEN rows dispositioned. One missing — `eye_h2_frozen_frames_of_moving_elements_wobble…` — which the sibling surfaced at its REV-5 sweep and used to amend (b)-19's acceptance; #11 inherits the amended clause by reference → **P3-4**, not a P1 recurrence. |
| `architect_declares_an_engine_limit_without_checking_the_per_concept_override_path` | **Closed.** Lane N/A retracted and re-decided by quoting `nlbBodyLaneZ` `:39992–40001`; marks reuse re-decided by the four-part diff. Both re-read and correct. |
| `closed_enum_cannot_name_a_substance_the_design_teaches` | **Closed, widened** to `controls_visible` `:1340`, `readouts` `:1336`, `checkpoints.capture` `:1500` — all three confirmed verbatim in code. |
| `state_glow_focal_dims_one_half_of_the_relation_the_state_exists_to_teach` (MAJOR/OPEN) | **RECURS in S2 and S5** → **P1-2**. Applied correctly to S4 and S6; not re-run over the states the restructure created. |
| `field3d_nlb_arrow_min_length_floor_collapses_small_force_visibility_and_ratio` | **Closed for S2/S4/S5/S6** by the velocity channel. **Open at S8** under the full preset → P2-2. |

**Verified clean:** `nlb_checkpoint_s_m_authored_as_displacement…` · `nlb_static_state_authored_on_the_track_bound…` · `nlb_angle_arc_radius_overruns…` (N/A at θ = 0 — `arc.visible = Math.abs(thetaDeg) > 0.5`, `:40079`) · `nlb_loop_reset_clears_checkpoint_stamp…` + `nlb_frozen_pin_lands_within_one_frame…` · `nlb_multibody_lane_gap_is_along_z…` · `field3d_param_ramp_authoring_contract` · `nlb_coupled_sandbox_F_slider…` · `nlb_motion_archetype_declared…` · `field3d_sprite_label_text_and_ink_box…` · `nlb_friction_vector_first_frame_reveal_tint…` · `concept_ships_zero_narration_glow_bindings` (routed ✓) · `authored_state_glow_focal_silently_voids…` (correctly not chased) · `field3d_generic_visible_elements_matcher…` (E14 ✓).

---

## Independently re-derived (every number)

**Renderer citations — P3-1 corrections verified exact.** `theta_deg` **`:940`** ✓ · `length_m` "visible half-length" **`:941`** ✓ · θ-arc collapse **`:40073–40079`** (cited range contains it ✓) · `NLB_ARROW_SCALE 0.048` / `MIN 0.55` / `MAX 2.80` / `EPS 0.05` at **`:39661–39664`** ✓ · `readouts` `:1336` ✓ · `controls_visible` `:1340` ✓ · `nlbBodyLaneZ` `:39992–40001` ✓ · `o.visible = !!bd` once per apply `:44768` ✓.

**Seize claims — verified in code, both correct.** `nlbWireSlider` sets `PM_nlbSweepSeized = true` on any *trusted* input in **any** state (`:42164–42175`), and loop-reset early-returns on that latch (`:43022`). S1's R-drag seize and S7's v₀-drag seize are real, not assumed. Credit.

**Timing table — all eight rows recomputed.**

| State | Check | Result |
|---|---|---|
| S1 | turn = 1.5708/0.9 = **1745 ms** ✓ · L2 2600 = **52.0%** ✓ · pin 3000, margin **400** ✓ · s(pin) = **−0.30** ✓ · end −2.1 ✓ · bracket has BOTH endpoints ✓ **P1-C landed** | ✓ |
| S2 | cusp = πR/v = **1309 ms** ✓ · pin 3000, s = **+0.6** ✓ · end −0.6 ✓ | ✓ (camera → P2-1) |
| S3 | a = 1.96 ✓ stop 1020 at **+1.3796** ✓ · pin 2400 ⇒ t_B = 900, s = **+0.60** ✓ · B ends **−2.6** at 4000, 0.4 m inside ✓ · R = 4000 correctly chosen over 4200 ✓ | ✓ physics (→ **P1-1** on the mechanism) |
| S4 | 1200 = 30% ✓ pin 2400 ⇒ s = **0.0** ✓ end **−1.6** ✓ | ✓ |
| S5 | 1200 = 30% ✓ pin 2400 ✓ no translation ✓ | ✓ |
| S6 | L2 2400 = **50.0%** ✓ pin 2880, margin **480** ✓ s = **−0.48** ✓ end **−2.4** ✓ hand-back before the pin ✓ | ✓ |
| S7 | t_c = **1360.5 ms** ✓ 40.0% ✓ slide **2.267573 m** ✓ capture **+0.1324** ✓ v_roll **1.3333** ✓ pin 2040 ⇒ **−0.7729** ✓ end **−2.5863** ✓ · k = 1 ⇒ t_c 2041 = **60.0% of R** exactly — the modelling declaration is load-bearing as claimed ✓ | ✓ |
| envelope | d = 1.25/2.205 = **0.56689** ✓ d_max(2.5) = **3.5431** ✓ — stray `5` gone ✓ | ✓ |

**Velocity channel — arithmetic confirmed.** `velocity_scale = 0.92`, floor `0.25` wu ⇒ the floor corresponds to **0.2717 m/s**; the 1.5× margin requires every drawn nonzero speed ≥ **0.4076 m/s**. S2 centre 0.6 → **0.552 wu = 2.208× floor** ✓ · S4/S5 1.0 → 0.92 ✓ · S6 top 2.0 → **1.84** ✓ centre 0.92 ⇒ ratio **2 : 1 exact** ✓ · every exact zero a labelled marker ✓. The guided set is not the whole envelope — see **P2-2**.

**Mark envelope (P1-D).** R = 0.15 ⇒ marks 0–4 at +2.4 / +1.4575 / +0.5150 / **−0.4274** / −1.3699 = **5 marks** ✓. R = 0.35 ⇒ +2.4 / +0.2009 / −1.9982 = **3 marks** ✓. Authored R = 0.25 ⇒ mark 1 **2.171 m** from +3.0, mark 2 **2.258 m** from −3.0 ✓ (P3-2 corrected). One edge case → P3-1.

---

## P1 findings — block the design

### P1-1 · S3 authors a second timed action (a 1400 ms dissolve) inside a buy the founder scoped to one instant — and the retirement semantics the sibling defines are absent here · `[owner: alex:architect]`

§3's S3 row: *"stops at 1020 ms (s = +1.38), **holds, DISSOLVES at 1400**"*, repeated in the timing table. The union section of the same document states the guard verbatim: *"**no other timed actions, no timed property changes, no per-body timelines beyond the single activation instant**."* A dissolve at 1400 is (a) a second timed instant and (b) an animation. The sibling's REV 5 rules both out: *"the retirement cut is **HARD** (the narrow buy has no dissolve animation)."*

Three consequences:

1. **The surgeon has two wrong choices.** Build it → the buy grows past a binding founder ruling. Ignore it → document and sim disagree at a 16a phase boundary.
2. **If 1400 is honoured literally, the track is empty for 100 ms** mid-misconception-beat (A gone at 1400, B not seeded until 1500) — six frames of nothing at 60 fps.
3. **The retired body's skid trail is never said to clear.** Phase A's trail spans +2.4 → +1.38; the roller crosses that region 1500–2010 ms and is at **+0.60 at the pin (2400)**. Unless the trail clears at retirement, `STATE_3__frozen.png` shows *a rolling wheel with a skid trail behind it* under the caption **"Skid slows; roll does not"** — the misconception's own picture in the archived frame of the state that kills it. The sibling states the clause; #11 does not.

**Concrete correction (three sentences):** delete the 1400 dissolve from §3 and the timing table; adopt the sibling's single-lane retirement verbatim — *a body in a `lane_gap_m = 0` state is live from its own `activate_at_ms` (absent ⇒ 0) until the NEXT body's, at which instant it retires: hidden, un-integrated, trail cleared* — so phase A retires at **1500** as a declared hard cut, bridged by narration; and state that no beat in this concept requires any timed instant other than the single `activate_at_ms = 1500`. *(Every timed beat in the document was swept to check that last clause: S1's marks are a turn-count trigger on s(t); S1/S2/S4/S5/S6's overlay times are ordinary reveals; S3/S6's glow windows are existing `phases[].glow_focal`; S7's capture and trail-stop are closed-form physics; S8 is the wrap. **The 1400 dissolve is the only one.**)*

### P1-2 · The P1-E glow ruling was not re-run over the states the restructure created — S2 and S5 both author a state-level focal on a relation · `[owner: alex:architect]`

`state_glow_focal_dims_one_half_of_the_relation_the_state_exists_to_teach` (MAJOR/OPEN) — live text: *"if it is a RELATION between two overlays, author no glow_focal"*, with a probe that samples every overlay named in the state's **delta cue and tts_sentences** and *"FAILs when a named overlay is a dimmed peer and falls under the floor."*

The glow table applies this correctly to **S4** and **S6**. It does not apply it to:

- **S5**, delta cue **"Turning: the rim, not the centre"** — a relation named across two overlays in the cue itself — with a state-level focal on *"the bottom (backward) arrow"*. At the 2400 pin the top arrow **and** the centre zero marker are dimmed peers. Two thirds of the claim, dim, in the archived frame.
- **S2**, state-level focal on the rim dot, while §4's own `visual_counter` states the claim as a two-overlay comparison: *"the centre arrow holds 0.552 wu **and** the contact MARKER holds 0.00 m/s."* Both are dimmed peers at the pin.

**Concrete correction:** for S2 and S5, either author **no state-level `glow_focal`** and drive emphasis from `phases[]` windows with hand-back to full brightness on every overlay the delta cue and `tts_sentences` name, completing before 0.60R (S2: before 3000; S5: before 2400) — the pattern S4 and S6 already use — or show, by the row's own metric, that each non-focal named overlay clears 3:1 / 4.5:1 as a dimmed peer. Add the ruling as a *per-state* line so it cannot be missed again.

### P1-3 · The term ledger was not re-run over the split, and now forbids the top arrow that S4 and S5 authoritatively draw · `[owner: alex:architect]`

DoD (b′): *"top-point arrow, 2v | S6 | **must not render in S1–S5 or S8**"*. §3: **S4** draws *"velocity arrows … at top/centre/bottom — all exactly equal"*; **S5** draws *"**top arrow 0.92 wu forward**, bottom arrow 0.92 wu BACKWARD"*. The DoD forbids in two states the element those states exist to show.

The intent was right at REV 1 (don't pre-spoil **2v**). The split made top-point arrows at the *part* values the whole content of S4 and S5. A json_author following the ledger literally deletes S4's top arrow — destroying "one speed everywhere", which requires three arrows seen as equal — and S5's, destroying "±Rω". Gate 0 requires the DoD to be internally consistent.

**Concrete correction (one row):** re-scope to the summed value, not the position — *"the **summed** top-point arrow at **2v** | DEFINED S6 | must not render in S1–S3 or S8; S4 and S5 render top-point arrows at their **part** values (v and +Rω), which are the setup for S6's sum"*. Then re-read the whole ledger against the eight-state numbering.

---

## P2 findings

**P2-1 · S2's framed extent omits the wheel radius, so the wheel clips at both ends of its own run.** §3 S2: target s = +0.9, *"framed extent ≈ 3.4 m ≈ [+2.6, −0.8] — the wheel never leaves frame"*. The run is [+2.4, −0.6] and the radius 0.25 m, so the body spans **[+2.15, +2.65] at t = 0** and **[−0.85, −0.35]** at state end — **0.05 m outside the stated frame at each end**. Falsified by the document's own arithmetic. The sibling states the correct rule (A-11): *"framed along-track extent = run + one body-diameter margin each end"*. Correction: extent **4.0 m ≈ [+2.9, −1.1]**, plus the sibling's acceptance sentence. `[alex:architect]`

**P2-2 · S8 is the one place a drawn point-speed can land between zero and the floor, and its velocity elements are specified two ways.** DoD (b) scopes velocity arrows to *"(S2/S4/S5/S6)"* — S8 absent; §3's S8 row authors *"contact marker + centre arrow"*. (i) The centre arrow rides v₀ **0.5–2.5 m/s**; at 0.5 it renders 0.46 wu = 1.84× floor — clears, but only because the slider floor is 0.5, and the envelope was never stated. (ii) Under the **full** preset, ω₀ (0–12 rad/s) makes the contact speed |v − ωR| a live arbitrary value — including the whole band (0, 0.4076) m/s the channel cannot render honestly. Since the marker is defined as *"the specified rendering of every **exact-zero** point speed"*, the sandbox's headline demo (*"set ω₀ = 0 and watch capture"*) has no specified rendering for the contact between launch and capture. Correction: **in S8 the contact element is a dot + its LIVE value at all times (marker semantics, never routed through the arrow channel)** — the sibling's fleet definition — and add S8 to DoD (b)'s arrow-state list with the v₀-range floor check stated. `[alex:architect]`

**P2-3 · S1 and S6 share a motion and a rhythm; `superpose-combine`'s own definition no longer describes S6.** REV 1 coined it as *"part-motion A alone → part-motion B alone → their SUM as one motion"* — a rhythm the split removed. S6's motion is now "a wheel rolls at constant speed while overlays reveal in sequence and a two-line formula builds", which is S1's motion and S1's rhythm (`reveal-build`), differing only in what the overlays are — both side-on cameras, both ~1 m/s. Rule 31 permits a repeat only as a **declared contrast pair**. Correction: declare S1 and S6 as that pair in one line (S1 reads the relation from the **ground**; S6 from the **points**), or re-coin S6 against its own motion. `[alex:architect]`

**P2-4 · S1 carries the concept's densest content at the top of the word budget, with no sentence plan.** The merged S1 must deliver: "the wheel's CENTRE moves at v" (the anti-planting phrasing Block 1 requires from sentence one), the ω dual-label + patch, one turn = 2πR, v = Rω, and the 8-word anchor — in **45–55 EN words**. Gate 3f counts `text_en` words; >55 is "two ideas, split". Correction: state the five-sentence plan with a word allocation inside 55. `[alex:architect]`

**P2-5 · The two documents do not yet describe one contract — three divergences.** (i) The sibling's build sheet names **"#11 S4"** as the `activate_at_ms` consumer in two places — in REV 2 that state is **S3**; a surgeon verifying E9 against #11 would land on the frictionless slide state. (ii) The sibling's **force**-channel table carries two `#11 S4` rows while #11 states *"#11 draws **no force arrows**"*. (iii) **Retirement semantics + `visible_before_activation` + the ω₀ seed** are defined only in the sibling (this is P1-1's root). Correction: carry the sibling's ACTIVATION SEMANTICS paragraph into #11 by reference or verbatim, and strike the two stale `#11 S4` references and the two force-table rows from the build sheet. `[alex:architect]`

**P2-6 · Every new optional field in this buy has a legal falsy value, and the back-compat clause's phrasing invites `||`.** (b)-19 reads *"absent ⇒ today's constant/behaviour"* while U8/E10 declares **`lane_gap_m = 0` a legal value**. The natural `var gap = nn.lane_gap_m || NLB_LANE_GAP` silently restores **0.85** — reproducing cycle-1's P1-A(b) exactly (two wheels 1.7 m apart on the 16a state) with the design believing it authored 0. Same trap for `activate_at_ms = 0` and `visible_before_activation: false`. Correction: add to E9/E10/E11's acceptance — *presence is resolved by `typeof x === 'number'` / `in`, never by truthiness; the regression pair asserts authored-0 and absent produce **different** geometry*. `[peter_parker:field3d_surgeon]`

---

## P3 notes

- **P3-1 · The mark envelope is computed against the unseized run, but only a seize can reach its endpoints.** An R drag necessarily seizes (`:42171` → `:43022`), extending the run 4.5 → 5.4 m — at R = 0.15 that adds mark 5 at −2.312, i.e. **6 marks, not 5**. No failure consequence (no cap), but the document's currency is arithmetic.
- **P3-2 · S5's delta cue is six words.** "Turning: the rim, not the centre" — cap is ≤5. "Rim turns, centre stays" (4) carries the same claim.
- **P3-3 · S2's "last asserted event" again names an earlier instant than the pin's asserted content.** The row names the cusp (1309); the pin asserts *"one full cycloid arch + cusp"*, and a top-to-top arch containing one cusp completes at **2618 ms**. No consequence (2618 < 55% = 2750, margin still 382 ms), but stating 2618 costs nothing.
- **P3-4 · One OPEN row undispositioned:** `eye_h2_frozen_frames…` — bites (b)-19's acceptance, which the sibling amended and #11 inherits by reference. Listed for the ratchet only.
- **P3-5 · Mark position rounding:** R = 0.15, n = 3 is **−0.4274**, printed −0.428.
- **P3-6 · After the fold there is no purely qualitative opening state** — the first state ends on an equation, so the *hide advanced+extended* lesson opens on one (38a ordering). The within-state order rescues it (mark 0 → mark 1 → bracket 1745–2050 **then** formula 2300/2600 — concrete before abstract ✓), hence a note not a finding.
- **P3-7 · Rule 39 is satisfied by construction** and correctly not discussed: marks, bracket, arrows and markers are 3D meshes; `turns` and `ω` are `readouts` tokens on the existing SEAM-M surface; the R/v₀ rows ride `<prefix>_<name>_row`.

---

## Per-state table (design-level — no frames exist at Checkpoint A)

| state | correct | order_ok | labels_present | reads_sound_off | clearly_different | how_i_would_use | problem_or_missing | pri |
|---|---|---|---|---|---|---|---|---|
| S1 | Y | Y | Y (v, ω dual-labelled, marks, bracket, formula) | Y | Y (marks + bracket stamp) | "One full turn puts exactly one circumference of road behind it." | Densest state at the top of the 55-word ceiling with no sentence plan; motion + rhythm shared with S6 | P2-3, P2-4 |
| S2 | Y | Y | Y (centre arrow, contact marker) | Y | Y (the cusp) | "Put your finger on the bottom of the wheel — it is not moving." | State-level focal dims the two overlays §4's counter compares; framed extent clips the wheel 0.05 m at both ends | **P1-2**, P2-1 |
| S3 | Y (physics re-verified) | Y | Y (f_k, f, trail) | Y | Y (two pictures, one state) | "The skid mark is the proof: a rolling tyre leaves none." | Dissolve at 1400 is outside the buy; retirement undefined; retired trail never cleared → the pin can show a roller trailing a skid mark | **P1-1** |
| S4 | Y | Y | Y (three equal arrows) | Y | Y (spokes visibly still) | "No turn at all — so every point of it moves at the same speed." | Ledger forbids the top arrow this state must draw; thinnest state (its picture already appeared in S3 phase A) | **P1-3** |
| S5 | Y | Y | Y (±0.92 wu arrows, centre marker) | Y | Y (only spin-in-place state) | "Spin it on the spot: the top goes forward, the bottom goes back, the centre stays." | Same ledger issue; state-level focal dims two thirds of the claim; 6-word delta cue | **P1-2, P1-3**, P3-2 |
| S6 | Y | Y | Y (1.84 / 0.92 / marker, two formula lines) | Y | Y (the sum) | "Add the two: the top gets 2v, the bottom cancels to zero." | Archetype coinage no longer matches its own definition; motion + rhythm shared with S1 | P2-3 |
| S7 | Y (t_c, v_roll, envelope reproduce) | Y | Y (v, Rω, contact converging) | Y | Y (capture) | "Watch the two numbers meet — that instant is when it starts rolling." | none new — branch priority correctly restated; modelling declaration load-bearing and made | — |
| S8 | Y | Y | Y (v, Rω, contact, turns) | Y | Y (teacher-driven) | "Give it spin, take spin away, watch it settle into rolling." | Contact element unspecified for nonzero contact speed — the exact case the full-preset demo creates | P2-2 |

---

## `engine_queue` (numbering unchanged; **acceptances added**, no new E numbers)

| # | Item | Owner | Tag | Added this cycle |
|---|---|---|---|---|
| E1 | State-local physics clock / `RESET_TRAJECTORY` rebase | **`peter_parker:renderer_primitives`** → pcpl-surgeon | blocking | unchanged; precondition of every timing row **and** of the activation instant |
| E9 | Per-body `activate_at_ms` (narrow) | `peter_parker:field3d_surgeon` | blocking | **Acceptance +=** single-lane retirement derived from the same instants (live from own activation until the next body's; hidden, un-integrated, **trail cleared**); presence resolved by `typeof`, never truthiness (P2-6); **no second timed instant exists in either concept** — verify by grepping both skeletons for any authored ms that is not a reveal, a physics event or an activation |
| E10 | `lane_gap_m` authorable **including 0** / single-lane | `peter_parker:field3d_surgeon` | blocking | **Acceptance +=** the regression pair must assert authored-`0` and **absent** produce *different* geometry (`0` ⇒ both bodies z = 0; absent ⇒ ±0.425 wu) — the `||` trap |
| E11 | Two-channel vector map + zero-vector marker | `peter_parker:field3d_surgeon` | blocking | **Acceptance +=** the marker is *dot + its **live** value*, so it is also the correct rendering of a **nonzero** contact speed in the sandbox — no point speed ever routed through a clamped arrow |
| E12 | Revolution-mark primitive independent of `checkpoints` | `peter_parker:field3d_surgeon` | blocking | unchanged; envelope now stated at both ends ✓ |
| E13 | ω re-seed on the sandbox wrap; trail/trace break | `peter_parker:field3d_surgeon` | ride-along | unchanged |
| E14 | Register every new element type with the visible-elements matcher | **`peter_parker:renderer_primitives`** | ride-along | registration set enumerated ✓ |
| E15–E17 | U1 rolling branch + branch priority · U2 contact picture · U4 apparatus set | `peter_parker:field3d_surgeon` | blocking | minted by the sibling's REV 5; #11 consumes all three but never lists them — carry them into #11's E-map so the surgeon's dispatch list is one list |

No item forces a new `scenario_type`; nothing leaves the `newtons_laws_body` family; **the buy is still one timed field** — provided P1-1 lands.

---

## Candidate scar rows (report-only; no collision with cycle 1's five unfiled classes)

```sql
-- 1
('skeleton_authors_a_second_timed_action_after_the_engine_buy_was_scoped_to_one_instant',
 'A revision built on a narrowly-scoped engine buy still authors a beat at a second timed instant, with an animation the buy does not include',
 'MAJOR','alex:architect',
 'The founder scoped the choreography buy to a single per-body activation instant, and the revision restates that scope guard verbatim in its union section - while its 16a state still authors a dissolve 100 ms before the activation. The surgeon can only grow the buy past the ruling or ignore the document, and the un-named retirement leaves the retired body''s trail on the ground in the archived frame of the state that exists to say the trail is absent.',
 'When an engine buy is scoped to ONE mechanism, sweep every authored millisecond in the document and classify each as a reveal, a physics event, or the bought instant. Anything left over is out of scope: delete it and express the beat on the bought mechanism, or take it back to the founder. State the derived semantics (retirement, hold, trail clearing) explicitly in the same document, never by reference to a sibling only.',
 'js_eval',
 'For every state declaring two or more bodies: assert each body is hidden and its position unchanged before its own activation instant, assert the previous body is hidden at exactly that same instant, and assert no trail or trace geometry belonging to a retired body survives one frame past it.',
 'OPEN', ARRAY['pure_rolling','rolling_on_incline']::text[], ARRAY[]::text[], 'founder_proxy Checkpoint A rotmech cycle2 2026-08-02', 'directive'),

-- 2
('prior_rulings_and_internal_ledgers_are_not_re_run_over_the_states_a_restructure_creates',
 'A fix cycle that splits, folds or renumbers states applies each landed ruling only to the states the reviewer named, leaving the new states unruled and the document''s own ledgers contradicting its state table',
 'MAJOR','alex:architect',
 'A revision split one three-phase state into three and folded two others. The glow ruling was applied to the two states the review named and not to the two states whose claims are also relations; the term-introduction ledger still forbade an element that two of the new states must draw, which if honoured voids both states'' entire claims. Both defects were created by the restructure, not present before it.',
 'After any restructure, re-run EVERY prior ruling and EVERY internal ledger (term introduction, do-not-prespoil, glow channel, control min_ring, camera, misconception placement) over the NEW state list from scratch, state by state, and say in the response table that you did. A ruling recorded against a state number is not a ruling until it is re-derived against the states that now exist.',
 'js_eval',
 'Cross-check the term-introduction ledger against the per-state element list: every element a state renders must be introduced in that state or an earlier one, and no ledger row may forbid an element the state table authors. Independently, for every state authoring a state-level glow_focal, assert no overlay named in that state''s delta cue or narration is a dimmed peer below the contrast floor.',
 'OPEN', ARRAY['pure_rolling']::text[], ARRAY[]::text[], 'founder_proxy Checkpoint A rotmech cycle2 2026-08-02', 'directive'),

-- 3
('optional_config_field_with_a_legal_zero_value_is_resolved_by_truthiness',
 'A new optional field whose zero is a meaningful authored value is defaulted with a falsy test, so the authored zero silently becomes the fleet default',
 'MAJOR','peter_parker:field3d_surgeon',
 'The back-compat clause is written as "absent implies today''s constant", which invites a truthiness default. Where zero is itself a legal authored value - a zero lane gap meaning single lane, a zero activation instant meaning active from entry, a false visibility flag - a truthiness default restores the constant and reproduces the very defect the field was bought to fix, with the design believing it authored zero.',
 'Resolve optional config presence with typeof or an in-check, never with a falsy test. For every new optional field, state in the acceptance whether zero and false are legal authored values, and make the regression pair assert that authored-zero and absent produce DIFFERENT rendered geometry.',
 'js_eval',
 'For each newly added optional field with a legal falsy value: run the scenario twice, once with the field absent and once with it authored zero or false, and assert the rendered geometry differs in the way the design states.',
 'OPEN', ARRAY['pure_rolling','rolling_on_incline']::text[], ARRAY[]::text[], 'founder_proxy Checkpoint A rotmech cycle2 2026-08-02', 'directive'),

-- 4
('close_camera_framed_extent_is_authored_as_the_run_length_and_omits_the_body_radius',
 'A close-camera state states a framed extent equal to its run plus a small margin, so the body clips at the frame edge at the first and last frames of its own run',
 'MODERATE','alex:architect',
 'A state authored a 3.4 m framed extent for a 3.0 m run of a 0.5 m diameter body, leaving 0.2 m of margin at each end - less than the body radius - and then asserted in the same sentence that the body never leaves frame. The sibling concept, on the same shared camera-target surface, states the correct rule as run plus one body diameter of margin at each end; the two documents diverged on one contract.',
 'A framed along-track extent is the run PLUS one body diameter at each end, computed from the body radius, not the run alone. State the extent in metres with both endpoints, and pair it with the in-frame acceptance at t = 0 and every sample to the state end - the far end of an approaching run is the worst case and the first sample the probe takes.',
 'js_eval',
 'For every state authoring a camera target: project the body bounding box at t = 0, at the frozen pin and at state end, and assert every corner lies inside the viewport.',
 'OPEN', ARRAY['pure_rolling']::text[], ARRAY[]::text[], 'founder_proxy Checkpoint A rotmech cycle2 2026-08-02', 'directive'),

-- 5
('paired_skeletons_cite_each_other_by_state_number_across_a_renumbering',
 'Two skeletons sharing one build sheet reference each other''s states by number, and a fold or split in one leaves the other''s consumer lists and verification expectations pointing at the wrong state',
 'MODERATE','alex:architect',
 'The shared build sheet names the sibling''s S4 as the consumer of the bought activation field and carries two force-arrow rows for that state, while the sibling''s current revision moved that content to S3 and declares it draws no force arrows at all. A surgeon verifying the engine item against the named state finds a single-body frictionless state and no arrows.',
 'Cross-document references name the CONTENT, not the state number ("the sibling''s two-phase 16a contrast state"), or quote the number with the revision it belongs to. When either document renumbers, diff both consumer lists and both expectation tables in the same edit and say so in the response table.',
 'manual',
 'Before dispatching a shared build sheet, read every cross-concept state reference in it against the other document''s current state table and confirm the referenced state carries the named content.',
 'OPEN', ARRAY['pure_rolling','rolling_on_incline']::text[], ARRAY[]::text[], 'founder_proxy Checkpoint A rotmech cycle2 2026-08-02', 'directive')
```

**UPSERT, do not re-mint:** `state_glow_focal_dims_one_half_of_the_relation_the_state_exists_to_teach` — widen `concepts_affected` to include `pure_rolling`. `field3d_nlb_arrow_min_length_floor_collapses_small_force_visibility_and_ratio` — widen to both rolling concepts and note the stale `NLB_ARROW_SCALE = 0.030` in its DO (live code is **0.048**, `:39661`). Cycle 1's five candidates and three upserts remain **unfiled**.

---

## Key artefacts (the five reads that decide the fix)

1. `docs/loop_runs/rotmech/pure_rolling/skeleton.md` §3 **S3 row + its timing-table row** — "holds, DISSOLVES at 1400" sitting four lines from the union section's own "no other timed actions" guard. P1-1 in one screen.
2. `docs/loop_runs/rotmech/rolling_on_incline/skeleton.md` §3 **ACTIVATION SEMANTICS paragraph** — the retirement rule, the hard-cut sentence and the trail-clearing clause #11 needs verbatim. The fix for P1-1 is already written, in the sibling.
3. `docs/loop_runs/rotmech/pure_rolling/skeleton.md` **§3 glow table vs DoD (b′)** — S4/S6 ruled, S2/S5 not (P1-2); and the ledger row forbidding the arrow S4/S5 draw (P1-3). Both visible in one comparison.
4. `src/lib/renderers/field_3d_renderer.ts:39992–40001` + `:44768` — lane z over **declared** bodies and visibility set once per apply: the two facts the activation buy exists to work around, and the site where P2-6's `||` trap will be introduced.
5. `src/lib/renderers/field_3d_renderer.ts:42164–42175` + `:43022` — `nlbWireSlider`'s trusted-input latch and loop-reset's early return: the code that makes S1's and S7's seize declarations correct rather than assumed.

---

```
RUBRIC (advisory, unratified — docs/EXEMPLAR_RUBRIC.md; did not affect the verdict)
  Checkpoint A subset:  D1 1 · D2 2 · D8 2 · D9 1 · D10 2   = 8/10   (REV 1: 8/10)
  weakest: D9 title as a teaching claim — six of eight titles state a result, but two do
           not: "Sliding without turning" and "Turning without moving" name the CONDITION
           being examined, not what the student learns from it (the results live in the
           delta cues). Rule 41 register is clean throughout; this is the claim-vs-label
           axis only.
           D1 information gain — S4 is the thin state: a non-turning wheel translating is
           a picture already on screen in S3 phase A, and S4's residue is the
           three-equal-arrows claim; S6 is by construction the addition of S4 and S5.
  D2 (2): the order IS the build — v = Rω → contact at rest → friction type → translation
          field → rotation field → sum → capture; the aha lands at state 2 of 8.
  D8 (2): exactly two beats, S2 and S3, both at genuine pivots, both naming primitives.
  D10 (2): the P2-6 fix landed — the turns counter gives the R dial a visible consequence
          under the reduced preset.
  Movement since REV 1: D10 rose 1 → 2 (turns counter), D9 fell 2 → 1 (the two new
  condition-label titles + the six-word cue). The restructure moved value between
  dimensions without changing the subset total.
```

**Fix cycle 2 of 2 — the last.** `physics_author` and the 0c-2 `field3d-surgeon` dispatch remain **unauthorised** until this returns `DESIGN_OK`. REV 3 needs three edits — delete the 1400 dissolve and import the sibling's retirement semantics (P1-1); re-run the glow ruling over S2 and S5 (P1-2); re-scope the ledger's top-arrow row to the summed 2v (P1-3) — plus the six P2 one-liners. All are local; none touches the founder ruling, the fold, the split, the timing method or any verified number. If REV 3 returns with any of the three P1s unlanded, the correct next verdict is `ESCALATE` (fix-cycle budget exceeded), not a cycle 3.
