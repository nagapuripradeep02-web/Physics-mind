# SKELETON — `rolling_on_incline` (chapter `rotmech`, Class 11 Ch.7 — Systems of Particles & Rotational Motion) — REV 5 (fix cycle 2 response — FINAL cycle; supersedes the unsubmitted REV 4)

> **Phase-0 role:** 0b spec driver for build **0c-2** (bounded rotational extension to `newtons_laws_body`). founder-proxy Checkpoint A: REV 2 → `DESIGN_FIX` (`founder_proxy_A.md`), REV 3 → `DESIGN_FIX` (`founder_proxy_A_cycle2.md`, all seventeen cycle-1 fixes verified landed). **REV 4 was authored as the cycle-2 response but never submitted:** the sibling's Checkpoint A (`../pure_rolling/founder_proxy_A.md`) proved, hours later, that `newtons_laws_body` has **no per-body activation time** (visibility set once per state apply, `:44766–44771`; `phases[]` drives only `glow_focal` — `eng.phase_action` is written at `:45031`/`:45304` and never read; **no `scenario_cue` exists in the nlb block**) and that **lane z derives from the state's DECLARED body list, never co-presence** (`nlbBodyLaneZ`, `:39992–40001`, with a `fixed`-body `return 0` early-out) — falsifying REV 4's S3 and S6 mechanisms. REV 5 is REV 4 plus the founder's ruling on that seam and the sibling-review items that reach into this document. **This is the cycle-2 revision that goes to verification. There is no cycle 3.** REV 3 preserved at `skeleton_rev3.md`; REV 4 at `skeleton_rev4.md`.
> Survey: `docs/loop_runs/rotmech/phase0_survey.md`. Concept #12 of 14, ★ Diamond, V1. Sibling: `pure_rolling` (#11 — REV 1 reviewed `DESIGN_FIX`, revised to REV 2 in parallel against the same ruling), so the 0c-2 union is measured over BOTH consumers.
> **FOUNDER RULING 1 (2026-08-02, binding):** the survey's 0c-2 row amendment is SIGNED-in-principle — the union is enumerated honestly and completely below (closing build sheet), not shrunk. **FOUNDER RULING 2 (2026-08-02, binding):** P1-B resolves by option (iii) — the arrow map becomes authorable per-concept, defaults preserved; widened to TWO channels by Ruling 3's companion items (see the arrow-map ¶). **FOUNDER RULING 3 (2026-08-02, binding — identical contract for both rolling concepts):** (i) two-phase Rule-16a contrast beats stay ONE state, on a new per-body **`bodies[].activate_at_ms`** (hidden and NOT integrated before that instant; seeded at authored s₀/v₀ and integrated from it) — the wrong picture and its kill land back-to-back with no teacher click between; (ii) **three-phase states SPLIT into separate states** — bites #11's S5, not this concept; **A5 (the three-phase config shape) is DELETED from the union**; (iii) **`lane_gap_m = 0` is a legal authored value** with explicit single-lane semantics — S3 authors it, and #11 is now a consumer of U8; (iv) S6's hold-then-release is the single-body case of the same need, expressed on the same mechanism (below); (v) the engine buy is **narrowly scoped: `activate_at_ms` only** — no general choreography DSL, no other timed actions.
> **Routing correction (carried):** `field3d_nlb_physics_clock_not_state_local` is owned `peter_parker:renderer_primitives` → dispatches the **pcpl-surgeon** agent (2026-07-31 rename table), NOT field3d-surgeon. E1 must be dispatched on that tag.

## AMENDMENT-TO-REV-4 (what changed and why → where it now reads)

| # | Ruling item / sibling finding | What changed vs REV 4 | Where it now reads |
|---|---|---|---|
| A-1 | RULING 3(i) — `bodies[].activate_at_ms` exists as a bought mechanism (the sibling's P1-A proved no such surface exists today: `:44766–44771`, `:1583`/`:45290–45311`, `phase_action` write-only) | New **ACTIVATION SEMANTICS** ¶ in §3 defines the mechanism this document authors against: activation instant, seed-at-activation, default-hidden-before, **single-lane retirement** (in a `lane_gap_m = 0` state a body is live from its own `activate_at_ms` until the NEXT body's — retirement is derived from the same authored instants, not a second timed field), and the **`visible_before_activation`** boolean for the single-body held case. Scope-guard sentence included so the buy cannot grow by default | §3 ACTIVATION SEMANTICS ¶; U10/E9 |
| A-2 | S3 was authored against a dissolve cue that does not exist ("Cues on the scenario_cue channel" — there is NO `scenario_cue` in the nlb block; the `:919` field belongs to a different scenario) | S3 re-choreographed on the bought mechanism: phase A is now the **full 0–1500 ms continuous skid** (block +2.4 → −0.76; the REV 4 "1200–1500 hold + DISSOLVE" beat is deleted — nothing in the narrow buy can hold a moving block, and nothing needs to); at 1500 ms the disc ACTIVATES and the block RETIRES (single-lane rule) — a hard cut, declared; the retired body's skid trail clears with it (A4 widened). All downstream numbers re-verified: pin 1920 = 420 ms after activation ✓, disc at s = +2.156 at the pin, loop end s = −1.59 (1.41 m clear); block never reaches the bound (clamp would be 1961 ms > 1500 retirement) | §3 S3 row; timing table; A4 clause |
| A-3 | RULING 3(iv) — S6's held disc: REV 4's "v₀ = 0 + release cue, never `fixed`" named a cue that does not exist | S6's hold is expressed on the SAME bought instant: `activate_at_ms = 2500` + **`visible_before_activation: true`** — the disc renders at its seed pose (s = +2.4, v = 0, a = 0), arrows and formula build on it, and it begins integrating at its activation. This IS the ruling's "explicit release cue on a held body," expressed as one boolean on `activate_at_ms` — no second timed action. Timing re-derived against the genuine hold: release 2500 → pin 2760 (disc at +2.307, live a = 2.76) margin 260 ms ✓; finish halt 4305; loop end 4600 (295 ms held finish, declared housekeeping). All REV 4 numbers stand — now against a real mechanism | §3 S6 row; timing table; U10/E9 |
| A-4 | RULING 3(iii) — `lane_gap_m = 0` legal; S3 a consumer; #11 a consumer of U8 | U8 amended: 0 is an explicitly legal value with single-lane semantics; S3 authors `lane_gap_m = 0` (at the default `NLB_LANE_GAP = 0.85` its two declared bodies would render **1.7 m apart across the track** — the sibling's P1-A(b) defect, on a 16a state); #11 added to U8's consumer list | §3 S3 row + framing ¶; U8 |
| A-5 | RULING 3(ii) — three-phase states split; **A5 deleted from the union** | E8's config-shape note rewritten: the shape requirement is **up to TWO sequential activation bodies per state** (#12 S3; #11 S4); no three-phase shape exists anywhere in the union. The FIX-CYCLE-2 table's "#11 A5" row is preserved below for the reviewer but annotated DELETED | Build sheet E8 shape note; FIX-CYCLE-2 table annotation |
| A-6 | Sibling P1-B — the arrow map is TWO channels (#11's velocity arrows through the force clamp collapse 2 : 1 : 0 → 1 : 1 : nothing), plus a defined zero-vector marker | U7 rewritten: `force_scale`/`force_min_len` **and** `velocity_scale`/`velocity_min_len`, each defaulting to today's constants (`0.048`/`0.55`, `:39661–39662`), plus a **zero-vector marker** (labelled dot + value — never a stub, never a floored arrow; extends the `NLB_ARROW_EPS` hide rule). The authored FORCE channel (0.30 wu/N, 0.25 wu floor) and its rendered-length table are **verbatim unchanged**. #12 draws NO point-speed velocity arrows anywhere (S2's recap carries the contact story by trace + readouts); the velocity-channel values are authored by #11's REV 2 and, if #12 ever adds point-speed arrows, it binds to those same values (one chapter value — Rule 32d) | §3 arrow-map ¶; U7/E11 (supersedes cycle-2 E5) |
| A-7 | Sibling P1-D — revolution marks cannot ride `checkpoints` (closed `capture` enum `:1500`; stamps → the ONE formula surface `:1487`; `eng.energy_active` `:42747`; `NLB_CP_MAX = 3` `:43547`) | Revolution marks + circumference bracket REMOVED from U4's apparatus set → their own primitive, **U11/E12** (#11-only consumer): own mesh, turn-count trigger, live respace under an R drag, no capture-enum member, no formula-surface stamp, no `energy_active` side effect, no `NLB_CP_MAX` cap. U4's remainder (four meshes + rotation markers + k chips + KE readouts + bare-ω token + `controls_visible` extension) stands | U4 (trimmed); U11/E12 |
| A-8 | Sibling P2-4 — the sandbox wrap re-seeds `v1 = b.v0` ONLY (`:45571–45572`, re-verified in code this session); ω carries across the wrap | Two consumers named: the per-body default wrap gets an **ω re-seed to the authored ω₀** (U12/E13, shared, #11 S7's lesson-replay case), and #12's S8 **synchronised race restart (U6/E4) re-seeds ω for ALL bodies** alongside `_dsp0`/seeds — every lap replays from the authored (v₀, ω₀ = v₀/R) | §3 S8 row; U6; U12/E13 |
| A-9 | Sibling P2-7 — visible-elements matcher registration for every NEW element type | E8's bring-up probe duty now ENUMERATES the registration set: finish line + finish chips, rotation markers, centre markers, k chips, KE readouts, skid trail, cycloid trace, revolution marks, circumference bracket, velocity arrows, zero-vector marker. The matcher row itself is a separate item E14 on its own owner tag (`peter_parker:renderer_primitives` → pcpl-surgeon) | E8; E14 |
| A-10 | Sibling P2-1 — A1 is understated: θ = 0 being formula-automatic is not the requirement | U1 restated in **branch-priority** terms: today's Branch A applies `f = −sign(v)·μ_k·N` to ANY moving body with μ_k > 0 (`:45497–45499`, re-verified — `f = -vSign * b.mu_k * N`); the rolling branch must **SUPERSEDE** that path while the rolling condition holds. This binds every #12 rolling state whose body carries a μ_k (S7's ring above all), not only #11's flat states; θ = 0 ⇒ a = 0, f_s = 0 remains the surgeon's TEST case, not the requirement | U1 |
| A-11 | Sibling P2-3 — my S2/S6/S7 close cameras were accepted at cycle 2 without checking the framed extent against the run; a static target at the home-pose contact lets the body roll out of frame | Framing plan rewritten for the close-camera states: **static target at the RUN MIDPOINT's contact height, framed along-track extent = run + one body-diameter margin each end**, tabled per state in metres (S2: target s = +1.4, extent 3.0 m; S6: target s = +0.15, extent 5.4 m; S7: target s = +0.15, extent 5.4 m). Arrow legibility re-checked at S6's wider frame (~180 px/m estimate: smallest arrow f_s 0.414 wu ≈ 149 px). Probe disjointness + body-rect ∩ `nlbPanelRects()` = ∅ remain the acceptance | §3 framing plan |
| A-12 | Scar sweep re-run (this session) + rows the sibling's review named | Eleven-id sweep re-run live — same row set as REV 4 **plus one row REV 4 never held**: `eye_h2_frozen_frames_of_moving_elements_wobble_sub_perceptually_so_zero_percent_is_not_a_valid_gate` (MODERATE/OPEN) — it bites **(b)-19's acceptance**, which is re-worded (same-session A/B pre-/post-build EYE runs, diff judged per the row's wobble band — never raw zero-percent against aged baselines). Also explicitly dispositioned: `concept_ships_zero_narration_glow_bindings` (MAJOR/OPEN — routed to physics_author: a `glow` on every `tts_sentences[]` entry naming exactly one on-canvas element). `rolling_on_incline`/`pure_rolling` return 0 rows — **the sibling review's five candidate scar rows + three upserts are NOT yet filed**; dispositioned here from the review text; the dispatching session should file them before the 0c-2 dispatch | SCAR AUDIT; (b)-19 |
| A-13 | Housekeeping | REV 4's HOLD note removed (the ruling it waited for is Ruling 3 above); the orchestrating-session italic notes inside the arrow-map ¶ and after the build sheet are removed (their content is now the document body); the **E-number collision** between REV 4 (which minted E9–E11 for U1/U2/U4) and the sibling's review (which minted E9–E14 for its additions) is resolved: **the sibling review's E9–E14 stand** (they are in the report the dispatching session reads), and U1/U2/U4 re-tag as **E15/E16/E17** | Header; build sheet numbering note |

## FIX-CYCLE-2 RESPONSE (preserved verbatim from REV 4 so the cycle-2 reviewer can verify the original fix set; finding × what changed × where)

| Finding | What changed | Where |
|---|---|---|
| **P1-A(1)** finish semantics | Authored explicitly: **per-body HALT-AND-LATCH at its own CoM crossing of `s_finish`** — the body halts AT the line (position pinned to `s_finish`, v→0, a→0), and its compared readouts + chips LATCH at the crossing-interpolated values. One mechanism, both sub-needs: S1's lineup and S5's honest split. S5's held frame now reads sphere `7.0 / 2.8` beside ring `4.9 / 4.9` — the 14.0 J frame is impossible by construction | §3 finish-semantics ¶; S1/S2/S5/S6/S7 rows; (b)-16 |
| **P1-A(2)** checkpoints enum diff | `checkpoints.capture` (`:1494–1500`) diffed: closed, lacks `KE_trans`/`KE_rot`; stamps target the ONE formula surface (`:1487`) which S1/S5 don't author; `NLB_CP_MAX = 3` (`:43547`) < 4 bodies; `eng.energy_active` coupling (`:42747`). Decision: F9/finish **reuses the crossing interpolator, bypasses the stamp rendering** — reasons with line numbers stated | §3 finish-semantics ¶; (b)-16 with the four line-numbered reasons |
| **P1-A(3)** S8 per-body wrap | S8 stays `mode:'sandbox'` but the build adds a **synchronised all-body race restart** ((b)-17/E4): all four re-anchor to the start line together each lap; Rule 37 free-run preserved; DoD (j)(2)'s marble-vs-ring demo now achievable | §3 S8 row; (b)-17; DoD (j)(2) |
| **P1-A(4)** loop_reset re-derivation | Full timing table re-derived against halted geometry for ALL EIGHT states (S2/S3/S6 added per P1-D): S1 R 4000/pin 2400 (315 ms after ring's crossing — a finished race, not a heap); S4 3500/2100; S5 4500/2700 (photographs the latched splits); S7 4000/2400 | §3 timing table |
| **P1-B / RULING 2** arrow map | Option (iii) taken: authored **`arrow_scale = 0.30` wu/N, `min_len = 0.25` wu** for BOTH rolling concepts (defaults preserved fleet-wide). Three S6 lengths tabled at m = 1 kg: 1.243 / 2.665 / 0.414 wu — all UNCLAMPED, smallest = 1.66× floor (> the scar row's 1.5×), largest < MAX 2.80; true ratios 6.43:1 and 3.00:1 rendered exactly. #11's `f_k 1.96 N` → 0.588 wu (2.35× floor); `f 0.00 N` ≤ `NLB_ARROW_EPS = 0.05` → arrow HIDES (`:39664–39665`, honest zero; the readout carries it). S6's θ slider narrowed to 20°–40° so no arrow clamps anywhere in the drag range; S3's near-equal f_k/f_s lengths declared honest-by-physics *(REV 5: this becomes the FORCE channel of the two-channel map — A-6)* | §3 arrow-map ¶ + table; S6 row; min_ring table; (b)-18/E11 |
| **P1-C** glow ruling | Per-state glow **channel table** added. S4 and S5 author **NO state-level `glow_focal`** — emphasis runs on `phases[].glow_focal` (`nlbRunPhases`, `:45296–45310`), named explicitly, windowed with hand-back; `nlb_body_label` brighten-only removal is a named blocking precondition for S4 (E6); the pending-founder SET_GLOW row explicitly NOT chased (nlb's phases channel is independent of SET_GLOW). Joint with #11 (its sequenced-arrow states use the same channel) | §3 glow table; (b)-14 precondition note; E6 |
| **P1-D** S2/S3/S6 timing | All three added to the timing table with sub-beat schedules. **S3:** *(REV 5: sub-beat schedule superseded by A-2 — the hold+dissolve beat is deleted; boundary moves 1200→1500; pin margin 420 ms and all disc numbers unchanged.)* **S2:** "rolls slowly"/"visible pause" claims DELETED; recap re-authored on a disc of **R = 0.25 m — matching #11's wheel radius** so the recap picture IS #11's picture, now inclined; 1.27 revolutions, one cusp (754 ms), halt-latch at 1204 ms; evidence = trace-cusp shape + `contact 0.00` + latched equal readouts. **S6:** arrows 0/450/900–1350, formula 1350–2500, release 2500 (54.3% R), pin 2760 (a-match live, margin 260 ms), finish halt 4305 *(REV 5: the release is now `activate_at_ms = 2500` — A-3)* | §3 S2/S3/S6 rows + timing table |
| **P1-E** back-compat clause | One clause, cross-cutting ((b)-19): **every field 0c-2 adds to the shared scenario is OPTIONAL; absent ⇒ today's constant/behaviour byte-identically** — covering `radius_m`, `lane_gap_m` + assignment, the arrow map, `finish_line`, `race_restart`, `omega0_rad_s`, all mode additions *(REV 5: + `activate_at_ms`, `visible_before_activation` — A-1)*. Acceptance: THE EYE regression pair on `rolling_friction` (`shape:'wheel'` path) and `work_done_by_constant_force` (two-body lane compare) *(REV 5: acceptance wording amended per the eye_h2 wobble row — A-12)* | (b)-19; every affected build row cross-references it |
| **P2-1** scar sweep | Hand-list replaced by the eleven-concept sweep (`grep -rl "newtons_laws_body" src/data/concepts/*.json` — 11 ids, run live) + per-id queue queries; the five named OPEN rows dispositioned, plus four further OPEN rows the sweep surfaced that REV 3 never held | SCAR AUDIT (rebuilt; re-run again at REV 5) |
| **P2-2** radius clause | `one_line_fix` now answers BOTH: "…and making the body bigger raises the turning push and the turning resistance by the same amount, so R drops out the same way — only the shape factor k survives." Still formula-free (core state), Rule-41 literal | §4 row 2 |
| **P2-3** framing worst case | Estimates re-quoted at **t = 0** (the far end — the worst case for an approaching race, and the probe's first sample): s ≈ 187 px/m; S1 clearance ≈ 66 px, S4 ≈ 109 px. Criterion stated: disjoint at t = 0 and every 100 ms sample | §3 framing plan |
| **P2-4** frame-fit bound | 6 sin ψ restated as a conservative UPPER BOUND (omits the cos 25° compression; true horizontal along-track extent 5.44 m ⇒ smaller) — a bound, not a value | §3 framing plan |
| **P2-5** union scope | SUPERSEDED by Ruling 1 + #11's skeleton: union now enumerated over both consumers; #11's (c)-1…4 and A1–A4 incorporated; the survey amendment is the closing build sheet | Closing build sheet |
| **P3-1** invisible wall | Every translating state now ends at an authored finish halt (S1/S2/S4/S5/S6/S7), a synchronised restart (S8), or mid-track (S3) — **the bound clamp (`:45582–45591`) is never the on-screen stop in any state**; the FIXED `nlb_uncoupled_readouts_flip_to_static_on_bound_halt` principle (a geometric stop never upgrades friction TYPE) is carried into the halt spec for S7 | §3 finish-semantics ¶; timing table |
| **P3-2** S1 title | Retitled to state a result: **"Four shapes, one finish order"** (first words carry the meaning; Rule 41d) | §2 table |
| **#11 A1** | AGREE — (b)-2 now states θ = 0 as a first-class case with a surgeon test duty *(REV 5: restated as branch PRIORITY per the sibling's P2-1 — A-10)* | (b)-2 / U1 |
| **#11 A2** | AGREE — (b)-3 amended: point-speed arrows **computed from live (v, ω) as v ± ωR and v**, never hardcoded 0/v/2v | (b)-3 / U2 |
| **#11 A3** | AGREE — F8 amended: every non-wheel mesh carries a **rotation marker** (contrasting meridian stripe on solid/hollow spheres, radial face-stripe on the disc, one contrasting arc segment on the ring). Markers are geometry, not emphasis (Rule 29 untouched) | F8 / U4 |
| **#11 A4** | AGREE — trail/trace wrap discipline adopted: skid trails and rim traces break at every finish halt, loop reset and synchronised restart; the teleport segment is never drawn *(REV 5: widened — a retired body's trail clears with it, A-2; + the ω re-seed companion, A-8)* | (b)-3 constraint; S8 row |
| **#11 A5** | ~~AGREE/NOTED — config shape must express up to THREE sequential phase bodies in one state~~ **[DELETED at REV 5 — Ruling 3(ii): three-phase states SPLIT into separate states; A5 is removed from the union. The surviving shape requirement is TWO sequential activation bodies per state — A-5]** | Build sheet E8 shape note |
| Proxy's 5-item additions list | 1→(b)-16/E3 · 2→(b)-17/E4 · 3→(b)-18 (Ruling 2; now E11 two-channel) · 4→E6 precondition · 5→(b)-19 — all carried | Build sheet |

**Engine bug queue consultation (REV 5 — provenance stated):** the **eleven-concept sweep re-ran live this session** — `grep -rl "newtons_laws_body" src/data/concepts/*.json` → the same 11 ids — followed by `query_engine_bug_queue.ts <id>` per id (all row headers read; full text read for every OPEN row newly dispositioned), plus `rolling_on_incline` and `pure_rolling` (**0 rows each** — the sibling review's candidate rows are NOT yet filed; see A-12), plus `--owner alex:architect` (32 rows — unchanged from REV 4) and `--row-type directive` (47 — unchanged). Renderer citations re-read in code THIS session: per-apply visibility `o.visible = !!bd` (`:44766–44771` region); `nlbRunPhases` + write-only `phase_action` (`:45031`, `:45290–45311`); `nlbBodyLaneZ` declared-body derivation + `fixed` early-out (`:39992–40001`); Branch A kinetic path `f = -vSign * b.mu_k * N` (`:45497–45499`); sandbox wrap `v1 = b.v0` only (`:45569–45577`); bound clamp + friction-type-latch comment (`:45582–45594`); `NLB_LANE_GAP = 0.85` (`:39610`). All rolling motions remain tier **[NEEDS-SCENARIO]**.

## 1. Atomic claim

This concept teaches that a body rolling without slipping down an incline accelerates at a rate set ONLY by the dimensionless shape factor k = I/mR² — so four shapes released together always finish in the fixed order solid sphere, disc, hollow sphere, ring, regardless of mass or radius. It does NOT teach the rolling constraint itself or contact-point kinematics in depth (`pure_rolling` #11, which precedes it; here one compact recap beat), and it does NOT teach rotational kinetic energy as a topic (`rotational_work_energy`); the energy split appears only as the extended-ring explanation of the race.

## 2. State count + arc

**8 states** (complex — 7–9 band). Rings: core S1–S4, extended S5, advanced S6–S7 (contiguous, immediately before explore), explore S8.

| State | Title (Rule 41 — literal, rail-truncation-safe) | Purpose | teaching_method | depth_ring |
|---|---|---|---|---|
| STATE_1 | **Four shapes, one finish order** | Hook — four bodies released together finish in a fixed order, every time | straightforward beat | core |
| STATE_2 | Rolling links v and ω | Recap of #11: v = Rω on screen — cycloid cusp + equal readouts, on #11's own wheel radius | straightforward beat | core |
| STATE_3 | The friction is static | RM-G7 kill: contact speed 0.00 ⇒ static, not kinetic — SEQUENTIAL contrast with a skidding block | straightforward beat (16a) | core |
| STATE_4 | Mass and radius cancel | Second kill: a heavy large sphere and a light small sphere TIE — only k = I/mR² matters | straightforward beat (16a) | core |
| STATE_5 | The same energy, split two ways | WHY (AP/IB/NCERT route): same drop = same total KE; the ring puts more into spinning | straightforward beat | extended |
| STATE_6 | One formula ranks all four | a = g sin θ/(1 + I/mR²) built from the CoM equations | derivation_first_principles | advanced |
| STATE_7 | Low friction: rolling becomes slipping | Regime switch: μ_s below (k/(1+k))·tan θ → contact slides, friction flips kinetic | straightforward beat | advanced |
| STATE_8 | Try every variable | Sandbox — controls per `min_ring`, core-ring readouts only; synchronised race restart | exploration_sliders | explore |

The hook MOVES (S1 is the race itself). No `narrative_socratic`, no `wait_for_answer`, no `pause_after_ms`.

## 3. Per-state choreography + control plan (Rule 31 control table)

**FINISH-LINE SEMANTICS (P1-A — unchanged from REV 4):** any state may author a `finish_line` at a track coordinate `s_finish`. When a body's **CoM track coordinate crosses `s_finish`** (crossing-interpolated — the FIXED `nlb_checkpoint_capture_overshoots_exact_crossing_value` machinery, `:44240–44262`), that body **HALTS at the line** (position pinned to `s_finish`, v → 0, a → 0) **and its compared readouts, chips and labels LATCH at their crossing-instant values** and hold to state end. The halt is a FINISH, not a wall: the friction TYPE and every label latch as they were at crossing, never re-derived at rest (the FIXED `nlb_uncoupled_readouts_flip_to_static_on_bound_halt` principle, comment at `:45584–45590` — S7's ring holds `f_k`). Trails and traces break at the halt and at every reset (A4). **No state ever reaches the track-bound clamp (`:45582–45591`) on screen** (P3-1). Mechanism = (b)-16/E3; its relationship to `checkpoints` is specified there with line numbers.

**ACTIVATION SEMANTICS (RULING 3 — the mechanism this document authors against; U10/E9):** a body may author **`activate_at_ms`** (optional; absent ⇒ active from state entry — the (b)-19 back-compat default). Before its activation instant the body is **NOT integrated** and, by default, **hidden**; AT it, the body is seeded at its authored `initial_position_m`/`v₀` (and ω₀ per U1) and integrates on the state-local clock from there. Two derived rules, both expressed on the SAME authored instants — no second timed field:
- **Single-lane retirement:** in a **`lane_gap_m = 0`** state, a body is live from its own `activate_at_ms` until the NEXT body's `activate_at_ms`, at which instant it **retires** — hidden, no longer integrated, its trail cleared (A4). This is the "explicit single-lane semantics" of Ruling 3(iii): sequential presentation on one lane, phase boundaries = the activation instants themselves. The retirement cut is HARD (the narrow buy has no dissolve animation) — declared, and bridged by narration where used (S3).
- **Held-visible single body:** a state's ONLY body may author **`visible_before_activation: true`**: it renders at its seed pose with v = 0, a = 0 (arrows, labels and the formula surface live on it) until its activation — this IS Ruling 3(iv)'s "explicit release cue on a held body," as one boolean on the bought instant (S6).

**Scope guard (Ruling 3(v)):** `activate_at_ms` is the ONLY timed action bought. Retirement and `visible_before_activation` are derived semantics/flags on that same instant — no `phases[].action` revival, no per-body show/hide timeline, no general choreography DSL. If the surgeon finds a second timed field needed, that is the alarm rule firing — STOP and re-scope with the survey.

**ARROW MAP (RULING 2, widened to TWO CHANNELS by the sibling's P1-B — binding both rolling concepts; U7/E11):** the map becomes per-concept-authorable in two independent channels, each defaulting to today's constants (`0.048`/`0.55`, `:39661–39662`; clamp site `:40602–40607`; `NLB_ARROW_MAX_LEN = 2.80`, `NLB_ARROW_EPS = 0.05` unchanged, `:39663–39665`):
- **Force channel — authored here: `force_scale = 0.30` wu/N · `force_min_len = 0.25` wu** (the REV 4 values, verbatim). Rendered lengths at the authored poses:

| State | Arrow | F (N) | Rendered L (wu) | Clamp? |
|---|---|---|---|---|
| S6 (disc, m = 1 kg, θ = 25°) | mg sin θ | 4.1417 | **1.243** | no |
| S6 | N = mg cos θ | 8.8818 | **2.665** | no (< 2.80, margin 0.135) |
| S6 | f_s = k·mg sin θ/(1+k) | 1.3806 | **0.414** | no — **1.66× the 0.25 floor** (> the scar row's 1.5× margin) |
| S3 | f_k = μ_k·mg cos θ | 1.3323 | 0.400 | no |
| S3 | f_s (rolling half) | 1.3806 | 0.414 | no |
| #11 S4 | f_k | 1.96 | 0.588 (2.35× floor) | no |
| #11 S4 | f (roller) | 0.00 | **arrow HIDES** (≤ EPS `:39664–39665`) | honest zero; readout `f 0.00 N` carries it |

  True ratios rendered exactly: N : f_s = **6.43 : 1**, mg sin θ : f_s = **3.00 : 1** — no clamp anywhere. **Drag-range honesty:** S6's θ slider is authored **20°–40°** (S8's θ row matches) — across that whole range no S6 arrow clamps (at 20°: N = 2.763 ≤ 2.80, f_s = 0.335 > 0.25; at 40°: N = 2.252, f_s = 0.630). **S3's f_k (1.33 N) and f_s (1.38 N) render at near-equal length because the forces ARE near-equal — declared honest: S3's taught contrast is the friction TYPE, never a length ratio.** Closes `field3d_nlb_arrow_min_length_floor_collapses_small_force_visibility_and_ratio` (MAJOR/OPEN) for both concepts.
- **Velocity channel — `velocity_scale`/`velocity_min_len`** (wu per m/s): consumed by #11's point-speed states, where routing v ± ωR through the force clamp collapses 2 : 1 : 0 → 1 : 1 : nothing. **#12 draws NO point-speed velocity arrows anywhere** (S2's recap carries the contact story by the rim-dot trace + the v/Rω/contact readouts); the channel VALUES are authored by #11's REV 2 (its states own v_min), and if #12 ever adds point-speed arrows it binds to those same values — one chapter value, Rule 32d across the pair.
- **Zero-vector marker (fleet definition):** an exactly-zero vector renders as a **labelled dot + its value** (e.g. `contact 0.00 m/s`) — never a stub, never a floored arrow; the force channel's ≤ EPS hide + readout rule stands for forces.

| State | Teaches | Archetype | Distinct motion | Delta cue | Live controls | Words | Ring |
|---|---|---|---|---|---|---|---|
| S1 | Shape decides the finish order | `translate-through` | Four visibly different bodies (equal m = 1 kg, R = 0.15 m; rotation markers per F8/A3) roll from a common start line, released simultaneously, approaching the camera; they separate; **each body HALTS AT the finish line as its CoM crosses `s_finish = −2.1`** (crossings 1744 / 1805 / 1903 / 2085 ms) and its chip stamps 1-2-3-4 at the crossing; by 2085 ms the frame is a finish-line LINEUP; loop resets, same order every time | "Four shapes, one ramp" | none | 40–50 (incl. ≤12-word anchor) | core |
| S2 | v = Rω in action (recap of #11) | `flow-along-path` | ONE recap disc at **R = 0.25 m — #11's own wheel radius** (distinct body id `nlb_disc_recap`, radius per (b)-9); released from rest, rolls 2.0 m (+2.4 → +0.4) in 1204 ms (1.27 revolutions — the cycloid draws one full arch); the marked rim dot's trace comes to a **cusp** at the ground touch (754 ms) while `contact 0.00 m/s` holds; **halt-latches at s = +0.4** with v and Rω readouts equal (3.32 = 3.32). No point-speed arrows (the trace + readouts carry it). S2's evidence is the trace's cusp SHAPE + the readouts | "v equals R ω" | none | 30–45 | core |
| S3 | The contact friction is STATIC | `null-result-hold` | **SEQUENTIAL on the activation mechanism, `lane_gap_m = 0` authored (both bodies z = 0; the default 0.85 gap would split them 1.7 m across the track — the sibling's P1-A(b)):** 0–1500 ms the locked block ALONE skids (active from entry; μ_k = 0.15, a = 2.809 m/s², 3.16 m of visible skid +2.4 → −0.76) — contact slides, f_k label + arrow (0.400 wu), skid trail, contact readout ≠ 0; **at 1500 ms the disc ACTIVATES (`activate_at_ms = 1500`, seeded s₀ = +2.4, v₀ = 0) and the block RETIRES** (single-lane rule — a hard cut, declared; narration bridges it; the block's trail clears with it, A4); the disc descends rolling (μ_s = 0.50) — `contact 0.00`, f_s label + arrow, no trail; loop ends with the disc mid-track at s = −1.59 (1.41 m clear of the bound). The block never reaches the bound (it would clamp at 1961 ms > its 1500 ms retirement) | "Contact point speed: zero" | none | 40–55 | core |
| S4 | Only k matters — mass and radius cancel | `translate-through` — declared contrast pair with S1 | A large heavy sphere (5 kg, R = 0.30) races a small light one (0.5 kg, R = 0.10), released simultaneously, approaching the camera; centres stay exactly abreast (centre markers, CoM tie metric — P1-8); **both halt at the line in the SAME frame (1745 ms) and both chips stamp "TIE" at the crossing**; the small sphere visibly spins 3× faster (per-body radius + rotation markers — (b)-9, A3); both k chips read 0.40; m₂/R₂ re-drag re-runs and it still ties | "Mass and radius cancel" | m₂, R₂ | 35–50 | core |
| S5 | WHY: the energy split | `cycle-compare` — count-up, arrive, HOLD | Solid sphere beside ring, same 1.00 m drop (d = 2.366 m): release → value-only readouts count up, DERIVED live (KE_trans = ½mv², KE_rot = ½k·mv²) → **the sphere halt-latches at ITS crossing (1265 ms): `KE_trans 7.0 J · KE_rot 2.8 J`; the ring at 1512 ms: `4.9 J · 4.9 J`** → from 1512 ms the frame is the held side-by-side: both bodies AT the line, totals identical (mgh = 9.8 J), splits different, sphere's chip first. The 14.0 J frame is impossible: the sphere cannot run past its finish. NO energy bars — SEAM-M readouts only | "Same energy, different split" | none | 40–55 | extended |
| S6 | a = g sin θ/(1 + I/mR²) | `reveal-build` | **Disc visible AND held at home from t = 0: `activate_at_ms = 2500` + `visible_before_activation: true`** (Ruling 3(iv)'s release-cue-on-a-held-body case, on the bought mechanism — the disc renders at s = +2.4 with v = 0, a = 0, genuinely un-integrated); arrows draw in sequence — mg sin θ (450 ms), N (900), f_s (1350) — at the force-channel lengths tabled above; the surface builds f·R = I_cm·α → f = k·m·a → a = g sin θ/(1+k) (1350–2500 ms, CoM route, f_s arrow consumed, no parallel-axis); **activation = release at 2500 ms** — live a readout 2.76 matches the formula; **finish halt at 4305 ms**. θ slider **20°–40°** | "One formula ranks all" | θ (20°–40°) | 45–55 | advanced |
| S7 | The slipping condition | `regime-switch` (coined, REV 3 justification stands) | Ring rolls at μ_s = 0.50; authored `param_ramp` 0.50 → 0.05 over 600–1600 ms; crosses μ_min = 0.233 at ≈1193 ms (s ≈ +0.93) → contact jumps off zero, label flips f_s → f_k, skid trail, spin lags; skids 3.026 m at 3.698 m/s²; **halt-latches at the finish (≈1968 ms) holding the slip picture — `f_k` label LATCHED as-at-crossing** — until reset. μ_min tick rides the μ_s row | "Too little friction: slipping" | μ_s | 35–50 | advanced |
| S8 | Everything, teacher-driven | `drag-sandbox` | Teacher drives the ring-gated set; the four-body race re-runs live under a **SYNCHRONISED restart ((b)-17/E4): each lap ends when the LAST body crosses the finish; after a short hold ALL bodies re-anchor to the start line together — position, `_dsp0`/seeds, v₀ **and ω re-seeded to the authored ω₀ = v₀/R for every body** (A-8) — and chips re-stamp: a start line and a finish order exist on every lap** (the per-body wrap at `:45569–45577` re-seeds `v1 = b.v0` only and desynchronises permanently; replaced for race states). Rule 37: the clock free-runs; the race loops forever. Trails/traces break at each restart (A4). Core-ring content only: v = Rω, contact, k chips, centre markers, finish chips | "All controls live" | see min_ring table | 0 / open | explore |

**S8 explore controls with `min_ring`:**

| Control | `min_ring` | Guided state that teaches it |
|---|---|---|
| shape (per lane) | core | S1, S4 |
| m (selected body) | core | S4 |
| R (selected body) | core | S4 |
| θ (**20°–40°** — matched to S6's arrow-honest range) | **advanced** | S6 |
| μ_s (0.05–1.00, μ_min tick riding the row) | **advanced** | S7 |

*Hide advanced* → shape + m + R ✓. *Hide advanced+extended* → same ✓. No control survives whose lesson is hidden.

**Slip envelope (P1-6, unchanged):** μ_min = (k/(1+k))·tan θ. At 25°: 0.133 / 0.155 / 0.187 / 0.233 — authored μ_s = 0.50 clears every shape in every guided state. Full-product maximum: ring at 40° = **0.420** > the 0.05 floor — slip reachable in the full-preset sandbox by design, cue riding the row. Reduced presets (fixed θ = 25°, μ_s = 0.50): worst case 0.233 < 0.50 — provably slip-free.

**Per-state glow plan (P1-C — channel named per state; Rule 32e caps at one, it does not require one):**

| State | Channel | Emphasis |
|---|---|---|
| S1 | `phases[].glow_focal` (`nlbRunPhases`, `:45296–45310`) | each finish chip glows in a window at its body's crossing (1744/1805/1903/2085 ms), hand-back after |
| S2 | state-level | the rim dot |
| S3 | `phases[]` | phase A (0–1500): the f_k readout · phase B (1500→): the contact readout — windows aligned to the activation boundary |
| S4 | **NO state-level `glow_focal`** — a relation between two k chips | `phases[]`: chip A window → chip B window → hand-back (both full-bright for the tie run + TIE stamp; mass labels visible but DIMMED as emphasis peers — **precondition E6**) |
| S5 | **NO state-level `glow_focal`** — a relation between two KE pairs | `phases[]` during the hold: sphere pair window → ring pair window → hand-back (both pairs full-bright for the pinned frame at 2700 ms) |
| S6 | `phases[]` | each arrow as it draws → each formula line as it builds → the a readout at release |
| S7 | state-level | the friction label (one id; flips f_s → f_k at onset) |
| S8 | none | sandbox |

The pending-founder row `authored_state_glow_focal_silently_voids_every_tts_sentence_glow_in_that_state` is deliberately NOT chased: nlb's `phases[]` channel is independent of `SET_GLOW`. Joint with #11.

**Multi-body framing plan (P2-3/P2-4 as amended at REV 4; close-camera correction A-11 added):** ψ ≡ camera-view-axis-vs-track-axis, authored ψ = 35°, elevation 22°; races approach the camera (+s up-slope, `:45093–45096`), so **t = 0 is the far end and the worst case — where the probe samples first**. Race estimates at t = 0 (s ≈ 187 px/m): S1 adjacent separation ≈ 123 px vs body diameter ≈ 56 px → clearance ≈ **66 px**; S4 ≈ 184 px vs 75 px → clearance ≈ **109 px**. Acceptance = bbox disjointness under the projection probe at t = 0 and every 100 ms sample, gated by (b)-12 (`PM_NLB_LANE_OCCLUSION`). Frame-fit: "6 sin ψ + 2.4 cos ψ" remains a conservative UPPER BOUND (true horizontal along-track extent 5.44 m). Lanes: S1 `lane_gap_m` = 0.8, speed-ordered in the drift direction; S4/S5 = 1.2; **S3 `lane_gap_m` = 0** (single-lane, A-4); S2/S6/S7 single body. **Close-camera states (A-11 — a static target at the home-pose contact would let the body roll out of frame; the target is the RUN MIDPOINT's contact, and the framed along-track extent = run + one body-diameter margin each end):**

| State | Run (track s) | Static target | Framed along-track extent |
|---|---|---|---|
| S2 | +2.4 → +0.4 (2.0 m) | contact at **s = +1.4** | **3.0 m** ([−0.1, +2.9]) — the 0.5 m-diameter recap wheel's cusp legible at this scale (the sibling's P2-3 guidance: ~3–3.5 m for a 0.5 m wheel) |
| S6 | +2.4 → −2.1 (4.5 m) | contact at **s = +0.15** | **5.4 m** ([−2.55, +2.85]) — arrow legibility re-checked at this wider frame: ~180 px/m estimate ⇒ smallest arrow f_s 0.414 wu ≈ 149 px, N ≈ 959 px < frame ✓ |
| S7 | +2.4 → −2.1 (4.5 m) | contact at **s = +0.15** | **5.4 m** — same frame as S6 (same run; Rule 32d continuity across the advanced pair) |

These are design values; the acceptance remains the projection probe (body in frame at t = 0 and every 100 ms sample to its halt) **plus** body-mesh rects ∩ `nlbPanelRects()` = ∅ under every authored camera (`field3d_nlb_body_screen_anchor_lands_under_the_dom_slider_panel`, MAJOR/OPEN — framing-level acceptance, (b)-13/(b)-14). No release stagger.

**Home pose + track geometry (unchanged):** `surface.length_m = 3.0` (half-length, `:941`; readers `:40060–40067`/`:44176`) → 6.0 m plank, s ∈ [−3.0, +3.0]; θ default 25°; home pose `initial_position_m = +2.4` every state; `s_finish = −2.1` (S1/S4/S6/S7/S8), `+0.034` (S5), `+0.4` (S2); all crossings on the CoM track coordinate.

**Loop-reset / frozen-pin timing (ALL EIGHT states, derived against the halted geometry AND the genuine activation holds; g sin θ = 4.1417; a = 2.958 / 2.761 / 2.485 / 2.071; json_author re-verifies at h = 1/60; the table is CONDITIONAL on the state-local clock, (b)-11/E1):**

| State | R (ms) | Sub-beats / last asserted event | Event time (% R) | Pin 0.60R | What the pin photographs · margin |
|---|---|---|---|---|---|
| S1 | 4000 | crossings + chip stamps 1744 / 1805 / 1903 / 2085; all four HALTED at the line from 2085 | 2085 (52.1%) ✓ | 2400 | the finished race: four bodies AT the line, chips 1-2-3-4 · 315 ms ✓; hold 1915 ms — a lineup, never a heap |
| S2 | 3000 | cusp 754; halt-latch at s = +0.4 at 1204 (v = Rω = 3.32 latched) | 1204 (40.1%) ✓ | 1800 | full one-arch cycloid + cusp + equal latched readouts · 596 ms ✓ |
| S3 | 3200 | **A: block skids alone 0–1500 (+2.4 → −0.76, never clamps — bound would be 1961 ms > retirement) · disc ACTIVATES / block RETIRES at 1500** | 1500 (46.9%) ✓ | 1920 | the ROLLING half — disc descending at s = +2.156, `contact 0.00` + f_s, **no block, no trail** (both retired/cleared at 1500) · **420 ms after activation ✓ (≥167)**; loop ends disc mid-track s = −1.59, 1.41 m clear — no clamp |
| S4 | 3500 | tie crossing + double-TIE stamp + halt 1745 | 1745 (49.9%) ✓ | 2100 | both bodies halted abreast at the line, TIE chips · 355 ms ✓ |
| S5 | 4500 | sphere halt-latch 1265 (7.0/2.8); ring 1512 (4.9/4.9); held side-by-side from 1512 | 1512 (33.6%) ✓ | 2700 | the held split: 7.0/2.8 beside 4.9/4.9, both at the line · 1188 ms ✓ — **the frame the state exists for** |
| S6 | 4600 | arrows 450/900/1350 · formula 1350–2500 · **activation (release) 2500** — disc genuinely held (un-integrated) before it; live a = 2.76 matches on release; finish halt 4305 (295 ms held finish before reset, declared) | 2500 (54.3%) ✓ | 2760 | disc just released and rolling at s = +2.307, full formula + arrows + matching a readout · 260 ms after release ✓ |
| S7 | 4000 | slip onset 1193 (29.8%); halt-latch at finish 1968 (49.2%) holding the slip picture, f_k latched | 1968 (49.2%) ✓ | 2400 | the held slip picture · 432 ms ✓ |
| S8 | — | free-run sandbox; synchronised restart each lap incl. the all-body ω re-seed (Rule 37) | — | — | — |

**Rule 32 legibility:** unchanged from REV 3/4 (cause before effect; one taught variable per state; union-built body set with distinct ids — `nlb_block`, `nlb_disc_recap` etc.; home pose persists; the glow table supplies the single-focal discipline). S3's activation cut is the one declared exception to visual continuity within a state — it is the 16a phase boundary itself, and the disc appears AT the home pose (+2.4), so the state re-establishes the home picture rather than teleporting a body.

## 4. Misconception confrontation plan (Rule 16a — 2 genuine pivots)

| Wrong belief | Source | At | `misconception_watch` beat |
|---|---|---|---|
| "Rolling friction is kinetic friction" | RM-G7 | STATE_3 | `belief`: the contact scrubs like a sliding block. `visual_counter`: the skidding locked block (f_k + trail) FIRST and ALONE for the full first phase, retiring at the instant the rolling disc activates at the same home pose. `one_line_fix`: the contact point is instantaneously at rest, so the friction is static; kinetic appears only when the body slips (S7 closes the loop). Named primitives: `rotation_locked` + skid trail + f_k label/arrow + the activation boundary itself |
| "The heavier (or bigger) body wins" | PER + catalog | STATE_4 | `belief`: 5 kg beats 0.5 kg downhill. `visual_counter`: centre markers exactly abreast for the full descent — a dead tie, double-stamped "TIE", re-runnable at any m₂/R₂. `one_line_fix` (mass AND radius, ring-safe, formula-free): "doubling the mass doubles both the pull down the slope and the resistance to speeding up, and making the body bigger raises the turning push and the turning resistance by the same amount — so mass and radius both drop out, and only the shape factor k survives." (The formula appears first in S6, advanced.) |

No other state carries a `misconception_watch`. EPIC-C branches: ZERO.

## 5. `has_prebuilt_deep_dive` states

- **STATE_4** — the PRIMARY aha and the stickiest point. — **STATE_6** — the derivation; home of the ALTERNATIVE contact-point route (parallel-axis lives there, not the main state). All others un-flagged (Rule 18).

## 6. Drill-down clusters

**STATE_4:** `why_mass_cancels` · `shape_factor_table` · `same_shape_always_ties`. **STATE_6:** `torque_about_contact_point` · `why_one_plus_k` · `rolling_vs_frictionless_slider`.

## 7. `entry_state_map`

```
entry_state_map:
  foundational: STATE_1 → STATE_4
  energy:       STATE_5
  derivation:   STATE_6 → STATE_7
```
Default `foundational`. Cross-slice pill: "See WHY the sphere wins? (energy)" → STATE_5. PRIMARY aha (S4) inside foundational ✓.

## 8. Prerequisites (advisory, Rule 23)

`pure_rolling` (#11 — S2 is its compact recap at its own wheel radius) · `moment_of_inertia` (#6 — S4's cliff) · `tau_eq_i_alpha` (#7 — S6's cliff) · `friction_force` (SHIPPED, same scenario) · `rotational_work_energy` (#8 — advisory for S5). Namespace check: no collision (unchanged).

## 9. Real-world anchor (Rule 35 / 38f — unchanged, verified clean by Checkpoint A)

**Primary (STATE_1, ≤12 words):** *"Try it at home: a food can beats a roll of tape."* **Secondary (S8 opening caption option):** a bicycle wheel on a sloped path (38f — the SAME device as #11's primary anchor, deliberate cross-concept continuity). **DC Pandey check:** chapter table of contents only; nothing imported. NCERT §7.14 noted (energy route → S5 ringed extended).

## 10. Definition of Done (Gate 0 — no TBDs)

**(a) States:** the 8 of §2, exactly as tabled in §3 — including the finish-semantics rule, the ACTIVATION semantics, the two-channel arrow map, the glow channel table, the close-camera table, and the eight-state timing table.

**(b) Symbol-label table:** as REV 3, with these amendments: **Finish chips** row — "`1` `2` `3` `4` and `TIE` — stamped crossing-interpolated at the body's own CoM crossing; body halts at the line; chip and readouts latch"; **Rotation markers (A3):** contrasting meridian stripe (solid + hollow sphere), radial face-stripe (disc), one contrasting arc segment (ring) — geometry, not emphasis; **Energy readouts (S5):** "count up live, LATCH at the body's own crossing"; **S6 formula surface** unchanged (pixel-verified at the longest line, (b)-14). All Unicode across all three text paths (Rule 34c).

**(b′) Term-introduction ledger:** unchanged from REV 3 (S2's recap disc introduces nothing new; k chips still first render in S4).

**(c) RHR plan:** N/A — declared deliberately (unchanged).

**(d) Motion plan:** per §3; nothing static, nothing asserted-but-unrendered. Spin driven by position through the body's OWN `radius_m`; rotation legible on every mesh via the A3 markers; halts are finishes with latched labels; **holds and phase boundaries run on `activate_at_ms` — a held body is genuinely un-integrated, never a narrated fiction**; retirement is the single-lane rule, never a nonexistent dissolve cue. Every archetype discharged by the authored beat with zero teacher input.

**(e) Modes:** 0c-2 adds `rolling_race`, `rolling_contact`, `rolling_friction_contrast`, `rolling_energy_split`, `rolling_derive`, `rolling_slip`, reusing `sandbox` (+ the S8 `race_restart: 'synchronized'` flag, (b)-17). S3's contrast mode is built on the activation mechanism (U10) — NOT on `scenario_cue`, which does not exist in the nlb block. deriveStateMeta co-edit at all three sites (F10).

**(f)** `assessment` + `coverage_map`: unchanged span; `misconception_watch` = exactly §4's two beats.

**(g) Macro↔micro (Rule 33):** unchanged — every readout metric-defined (contact = |v − ωR|; KE_trans = ½mv²; KE_rot = ½k·mv²; latched values = the crossing-interpolated metrics); document numbers are CHECK values.

**(h) Canvas budget (Rule 34):** unchanged (S2 `v = Rω`; S5 none — its latched pairs are SEAM-M readouts; S6 the formula; S7 the μ_min inequality; S1/S3/S4/S8 none).

**(i) Curriculum-flex (Rule 38):** (i-1)–(i-5) unchanged from REV 3, re-checked against the REV 5 deltas: the activation mechanism and the camera table are ring-neutral apparatus behaviour; the narrowed θ range appears only in advanced-ring rows; S8 still surfaces core content only.

**(j) Teacher-walk answers:** (1) unchanged. (2) Re-run S4 at the extremes — all tie; then S8 pit a marble against a huge ring — achievable: the synchronised restart (now with the all-body ω re-seed) gives every lap a start line and a finish order. (3) Ledger (b′); declared omissions unchanged — decisions, not exemptions.

---

## Two-pass cognitive lens

### Block 1 — Pass-1 strategic checklist

1. **Prerequisite cliff.** Unchanged from REV 4: the `pure_rolling` → S2 patch rides the SAME wheel radius as #11 (R = 0.25 m), so the recap is visually the remembered object.
2. **JEE-backwards trace.** Unchanged (verified complete by Checkpoint A cycle 1): constraint → S2; KE split → S5; formula → S6; k per shape → S4 + S1; μ_min → S7; friction-static legitimises the energy route → S3.
3. **Misconception entry mapping.** Unchanged: RM-G7 → S3 (S1 phrased friction-neutrally); heavier-wins → S4 (S1 shows four EQUAL m, R bodies).

### Block 2 — Aha-moment designation

Unchanged from REV 3/4 (verified by Checkpoint A): PRIMARY at S4 (mass and radius cancel — only shape decides), SUPPORTING at S3 (rolling friction is static), wrong-belief setups S1/S2 and S1 respectively, S4 ∈ foundational ✓. The finish-semantics fix keeps the TIE stamped at a latched, crossing-exact instant; the activation fix keeps S3's contrast pair genuinely back-to-back with no fictional hold.

---

## SCAR AUDIT (rebuilt on the eleven-concept sweep — RE-RUN LIVE at REV 5)

**Queries (provenance):** ① `grep -rl "newtons_laws_body" src/data/concepts/*.json` → the eleven ids — **re-run this session, identical set**; ② `query_engine_bug_queue.ts <id>` for EACH of the eleven — **re-run this session** (headers read for all; full row text read for every OPEN row dispositioned); ③ `--owner alex:architect` (32) + `--row-type directive` (47) — **re-run this session, counts unchanged from REV 4 ⇒ nothing new filed**; ④ `rolling_on_incline` + `pure_rolling` → **0 rows each** — the sibling review's five candidate scar rows and three upserts are NOT yet in the DB; they are dispositioned in this document from the review text, and **the dispatching session should file them (`npm run log:lesson`) before the 0c-2 dispatch**; ⑤ scenario-scoped rows not tagged to the eleven ids (clock, occlusion, camera target, glow-relation, per-body-wrap) — carried as re-verified by both Checkpoint A reports (2026-08-02) and the code reads listed in the header. `--field3d --open` NOT used as coverage (script line 23's list contains zero nlb ids — the recorded root cause). A row not reached by ①–⑤ is not dispositioned.

**OPEN rows — dispositions (REV 4's set, re-confirmed, plus the two REV 4 never explicitly held):**

| bug_class (status/owner) | Verdict |
|---|---|
| `nlb_body_label_is_brighten_only_so_static_text_outranks_the_state_focal` (MAJOR/OPEN, field3d_surgeon) | **build precondition E6, blocking for S4** — labels become emphasis peers and dim |
| `field3d_nlb_arrow_min_length_floor_collapses_small_force_visibility_and_ratio` (MAJOR/OPEN, alex:physics_author) | **CLOSED for this design by the two-channel U7/E11** — every compared force arrow unclamped with ≥1.5× floor margin at the authored pose and across the whole S6 drag range; #11's velocity fan gets its own channel; the row's DO quotes a stale `NLB_ARROW_SCALE = 0.030` — current code is 0.048 (`:39661`), applied |
| `field3d_nlb_body_screen_anchor_lands_under_the_dom_slider_panel` (MAJOR/OPEN, field3d_surgeon) | **applied** — S2/S6/S7's close cameras carry the row's acceptance (body rects ∩ `nlbPanelRects()` = ∅) AND, at REV 5, explicit framed extents + run-midpoint targets (A-11) so the body stays in frame for its whole run |
| `frozen_frame_read_as_dense_series_continuation_on_translating_body` (MODERATE/OPEN, ambiguous) | **routed to eye_walker/quality_auditor as a READING directive** — all 8 states translate; frozen = the state re-entered and pinned at maxRevealMs, compared only against the dense frame at that time |
| `eye_h2_frozen_frames_of_moving_elements_wobble_sub_perceptually_so_zero_percent_is_not_a_valid_gate` (MODERATE/OPEN, field3d_surgeon) — **NEW at REV 5 (surfaced on connected_bodies/free_body_diagram)** | **applied to (b)-19's acceptance** — the back-compat regression check is re-worded: **same-session A/B runs** (pre-build vs post-build THE EYE on `rolling_friction` + `work_done_by_constant_force`), diff judged within the row's recorded sub-perceptual wobble band — never a raw zero-percent assertion against aged baselines |
| `concept_ships_zero_narration_glow_bindings` (MAJOR/OPEN, alex:physics_author) — **explicitly dispositioned at REV 5 (the sibling's P2-8)** | **routed to physics_author** — every `tts_sentences[]` entry authors a `glow` naming exactly one on-canvas element, consistent with the §3 glow channel table (state-level vs `phases[]`) |
| `field3d_generic_visible_elements_matcher_blanks_new_scenario_apparatus` (DIRECTIVE/OPEN, renderer_primitives) | **surgeon duty, E14 + E8's bring-up probe** — registration set ENUMERATED at REV 5 (A-9): finish line + chips, rotation markers, centre markers, k chips, KE readouts, skid trail, cycloid trace, revolution marks, bracket, velocity arrows, zero-vector marker; the probe asserts apparatus EXISTENCE + visibility, never just dynamics |
| `field3d_sprite_label_text_and_ink_box_are_invisible_to_every_dom_probe` (MODERATE/OPEN, field3d_surgeon) | **applied to (b)-14** — every label-decollision acceptance uses the sprite projection probe (`_pmText` + measureText ink rects), never a DOM probe |
| `nlb_friction_vector_first_frame_reveal_tint_bypasses_seam_q_ink_fix` (MODERATE/OPEN, field3d_surgeon) | **surgeon duty, ride-along (E8)** — S3/S6/S7 friction arrows' t = 0 reveal ink must clear the same contrast floor as the settled colour; **note S3's phase-B and S6's arrows now first render at an ACTIVATION instant, not state t = 0 — the row's probe must sample at each body's activation frame** |
| `nlb_multibody_lane_gap_is_along_z_so_a_head_on_camera_stacks_the_compared_bodies` (DIRECTIVE/OPEN, alex:architect) | **satisfied** — framing plan: ψ = 35°, approach-camera, t = 0 worst-case estimates, probe acceptance, (b)-12 gate; S3 now explicitly single-lane (gap 0) |
| `nlb_checkpoint_s_m_authored_as_displacement_not_track_coordinate` (DIRECTIVE/OPEN, alex:architect) | **satisfied** — every `s_finish` is a track coordinate on the corrected model |
| `field3d_param_ramp_authoring_contract` (DIRECTIVE/OPEN, field3d_surgeon) | **satisfied** — S7's ramp authored per contract (closed-form t_ms, thumb + label lockstep, `from` = authored value) |

**Scenario-scoped rows (query path ⑤):** `field3d_nlb_physics_clock_not_state_local` (CRITICAL/OPEN) → **(b)-11/E1, precondition of the entire timing table AND of every `activate_at_ms` instant — owner `peter_parker:renderer_primitives` → pcpl-surgeon** (`RESET_TRAJECTORY` exists `:45002–45018`; the gap is THE EYE's `SET_STATE` path) · `the_eye_passes_a_frame_in_which_one_compared_body_is_hidden_behind_another` (MAJOR/OPEN) → (b)-12/E2 · `field3d_scenario_renders_offcentre_because_camera_target_is_not_authorable` (MAJOR/OPEN) → (b)-13, now with the A-11 target table · `state_glow_focal_dims_one_half_of_the_relation_the_state_exists_to_teach` (MAJOR/OPEN, alex:json_author) → **applied**: S4/S5 author no state-level focal · `authored_state_glow_focal_silently_voids_every_tts_sentence_glow` (MAJOR/OPEN, FOUNDER PENDING) → **not chased**, phases channel independent · `nlb_multibody_sandbox_wrap_reanchors_only_the_wrapping_body` (MODERATE/OPEN) → **(b)-17/E4** (re-anchors `_dsp0`/seeds AND ω for ALL bodies — A-8).

**Sibling-review candidate rows (not yet filed — dispositioned from the review text):** `skeleton_authors_a_multi_phase_state_on_an_engine_with_no_per_body_activation_time` → **the defect this REV exists to fix**; satisfied by the ACTIVATION SEMANTICS ¶ + U10/E9 (mechanism named, config shape stated, both consumers listed) · `nlb_lane_offsets_apply_to_declared_bodies_not_co_present_ones_so_sequential_phases_split_laterally` → **satisfied** — S3 authors `lane_gap_m = 0` with the lane function's line numbers quoted (`:39992–40001`) · `velocity_arrows_routed_through_a_force_arrow_map...` → **satisfied by U7/E11** (two channels + zero marker; #12 draws no velocity arrows) · `skeleton_pin_budget_names_the_phase_start_instead_of_the_last_asserted_reveal` → **checked against this concept**: S3's pin content is the rolling picture, complete at activation + one readable beat (disc + f_s label + contact readout render at 1500; pin 1920 = 420 ms later — no multi-element reveal chain inside phase B); S6's pin content is "released + formula + arrows," whose last reveal (the formula's final line) completes at 2500 = the activation instant itself, pin 2760 · `architect_reuses_a_marker_mechanism_without_diffing_the_side_effects...` → **satisfied** — (b)-16's four-part checkpoints diff (enum/target/cap/energy_active) stands, and the revolution marks are now their own primitive (U11).

**FIXED rows cited as design principles:** `nlb_checkpoint_capture_overshoots_exact_crossing_value` (crossing interpolation — reused by (b)-16) · `nlb_uncoupled_readouts_flip_to_static_on_bound_halt` (a finish stop never upgrades friction type — S7's latch spec) · `geometric_track_clamp_rendered_as_an_energy_change` (moot: no state reaches the clamp; no energy bars).

**All other REV 3/REV 4 dispositions** (the corrected-verdict table, the N/A-with-reason set, the routed physics_author directives): **unchanged**, re-checked against the REV 5 deltas — the only mechanics that moved are S3/S6's hold surfaces and the camera targets, and every affected row above covers them.

---

# THE 0c-2 BUILD SHEET — as signed (Ruling 1 + Ruling 3; the survey's 0c-2 row amendment, enumerated honestly over BOTH consumers)

> Consumers: `rolling_on_incline` (#12, this document) + `pure_rolling` (#11, REV 2 against the same ruling). No new `scenario_type`; everything below extends `newtons_laws_body`. **Cross-cutting clause (b)-19 governs every item:** every added config field is OPTIONAL; absent ⇒ today's constant/behaviour **byte-identically** — acceptance: **same-session A/B THE EYE runs** (pre-build vs post-build) on `rolling_friction` (`shape:'wheel'` code path) and `work_done_by_constant_force` (two-body lane compare), diff judged within the `eye_h2...wobble` row's recorded band — never raw zero-percent against aged baselines. If the surgeon finds any FURTHER capability needed, that is the alarm rule firing — STOP and re-scope with the survey.
>
> **E-numbering note (A-13):** the sibling review's **E9–E14** stand as minted there; REV 4's colliding E9–E11 are re-tagged **E15–E17** below. E1–E8 are the cycle-2 report's, with E5 superseded by E11.

**The signed union:**

| # | Union item | Consumed by | Engine queue | Owner | Tag |
|---|---|---|---|---|---|
| U1 | **Rolling physics branch**: per-body k = I/mR² (2/5, 1/2, 2/3, 1); a = g sin θ/(1+k) with f_s = k·m·a; **BRANCH PRIORITY (A1 restated — A-10):** today's Branch A applies `f = −sign(v)·μ_k·N` to ANY moving body with μ_k > 0 (`:45497–45499`); the rolling branch must **SUPERSEDE** that path while the rolling condition holds (binds #12's S7 ring and every rolling body carrying μ_k, not only #11's flat states); θ = 0 ⇒ a = 0, f_s = 0 is the surgeon's TEST case; slip in BOTH directions — rolling→slipping past μ_min ((b)-5) AND slipping→rolling capture, closed-form in state-local t, never a per-frame latch (#11 (c)-3); independent ω integrator with `omega0_rad_s` authoring + `'omega0'` control token (#11 (c)-2); dt-fold exactness | #12 all states; #11 all states | **E15** | field3d_surgeon | **blocking** |
| U2 | **Contact-point picture**: rim dot + cycloid trace + skid trail (replayable pure functions of state-local t; wrap/halt/reset/retirement break discipline, A4 as widened); point-speed arrows **computed from live (v, ω) as v ± ωR and v — never hardcoded (A2)**, drawn through the VELOCITY channel of U7; contact-speed readout; static/kinetic call-out ((b)-3/(b)-4) | #12 S2/S3/S7/S8; #11 S3/S5/S6/S7 | **E16** | field3d_surgeon | **blocking** |
| U3 | **Per-body `radius_m`** [optional, default = today's `NLB_BODY_SIZE/2` lift `:40015` + `NLB_WHEEL_R` divisor `:40053`]: mesh scale, contact lift, spin ω = v/R, live re-lift under R/R₂ drag; Rule-29 ruling as stated in (b)-9 | both, every state | E7a | field3d_surgeon | **blocking** |
| U4 | **Rolling apparatus set** *(trimmed at REV 5 — A-7)*: four meshes `solid_sphere/disc/hollow_sphere/ring` (silhouette + colour distinctness, ≥60 px at reference scale) **each with a rotation marker (A3)**; k chips; KE_trans/KE_rot as SEAM-M value-only readouts, count-up, **no SEAM-L change ever** ((b)-1/6, F8); bare-ω readout token (#11 (c)-4); `controls_visible` token extension `R/R2/omega0/shape` ((b)-8, `:1340`). *(Revolution marks + bracket moved OUT → U11.)* | #12 S1/S4/S5/S8; #11 S1/S2/S7 | **E17** | field3d_surgeon | **blocking** |
| U5 | **Finish-line halt-and-latch ((b)-16)**: per-state `finish_line {s_m, bodies, halt, stamp}`; body halts AT `s_finish` on its own CoM crossing; readouts/chips/labels latch at crossing-interpolated values (reuses the `:44240–44262` interpolator); stamps render to **finish chips + per-body readout rows, NOT the formula surface**. Bypasses `checkpoints` rendering because: `capture` enum closed without `KE_trans/KE_rot` (`:1494–1500`); stamps target the ONE formula surface (`:1487`) which S1/S5 author none of; `NLB_CP_MAX = 3` (`:43547`) < 4 bodies; `checkpoint_state` flips `eng.energy_active` (`:42747`) against the no-bars ruling. Expectation: at t = 1512 ms in S5 the sphere reads 7.0/2.8. Friction type latches as-at-crossing | #12 S1/S2/S4/S5/S6/S7 | E3 | field3d_surgeon | **blocking** |
| U6 | **Synchronised all-body race restart ((b)-17)** for `mode:'sandbox'` race states (`race_restart:'synchronized'`): lap ends at the LAST body's crossing; hold; ALL bodies re-anchor to the start line together — position, `_dsp0`/seeds, v₀ **and ω re-seeded to the authored ω₀ = v₀/R for every body** (A-8; the `nlb_sandbox_wrap_remaps_s_but_not_s0` lesson + the sibling's P2-4 applied to the synchronised form); chips re-stamp per lap; Rule 37 free-run preserved. The per-body wrap (`:45569–45577`) remains the default for non-race sandboxes ((b)-19) | #12 S8 | E4 | field3d_surgeon | **blocking** |
| U7 | **TWO-CHANNEL authorable vector map ((b)-18; supersedes cycle-2 E5 — A-6)**: `force_scale`/`force_min_len` AND `velocity_scale`/`velocity_min_len`, each optional, defaults = today's `0.048`/`0.55` (`:39661–39662`; clamp `:40602–40607`); plus the **zero-vector marker** (labelled dot + value; never a stub, never a floored arrow; the ≤ `NLB_ARROW_EPS` hide + readout rule stands for forces). Authored: force channel **0.30/0.25** (both rolling concepts — table in §3); velocity channel values authored by #11 REV 2, one chapter value. Expectation: #12 S6 renders N : f_s = 6.43 : 1 honestly; #11 S5 renders top : centre = 2 : 1 with the contact as a labelled marker | #12 S3/S6 (force); #11 S4 (force) + S3/S5/S6/S7 (velocity) | **E11** | field3d_surgeon | **blocking** |
| U8 | **Race framing surfaces**: authorable `lane_gap_m` + explicit speed-ordered lane assignment [optional, default `NLB_LANE_GAP = 0.85` `:39610`/`:39998–40001`] — **0 is a LEGAL value with single-lane semantics (Ruling 3(iii))**; `PM_NLB_LANE_OCCLUSION` → `manifest.warnings` ((b)-12); camera **target** authoring per state ((b)-13, with the A-11 run-midpoint targets) + body-rect-vs-DOM-overlay acceptance | #12 S1/S3(gap 0)/S4/S5/S8; **#11 (now a consumer — its single-lane sequential states + S3 target)** | E7b + E2 + **E10** + E8a | field3d_surgeon | E2/E7b/E10 **blocking**; target ride-along per its row |
| U9 | **State-local physics clock rebase ((b)-11)** — `RESET_TRAJECTORY` exists (`:45002–45018`); fix the `SET_STATE`-without-reset path THE EYE uses; probe: `PM_nlbTimeMs` = 0 at reveal start on two pinned states. **Precondition of every `activate_at_ms` instant as well as every pin** | both, every timed state | E1 | **`peter_parker:renderer_primitives` → pcpl-surgeon** | **blocking** |
| U10 | **Per-body `activate_at_ms` (RULING 3 — A-1)**: hidden and NOT integrated before its instant; seeded at authored s₀/v₀ (and ω₀) at it; **single-lane retirement** in `lane_gap_m = 0` states (live from own activation until the NEXT body's — derived from the same instants, no second timed field); **`visible_before_activation: true`** for the single-body held case (renders at seed pose, v = 0, a = 0, overlays live). **Scope guard: this is the ONLY timed action in 0c-2 — no `phases[].action` revival, no show/hide timeline, no choreography DSL; a second timed field = the alarm rule.** Expectations: #12 S3 — the disc sits unrendered and un-integrated until 1500 ms, then descends from +2.4 while the block retires; #12 S6 — the disc renders held at +2.4 with arrows/formula until 2500 ms, then integrates; #11 S4 — per its REV 2 | #12 S3/S6; #11 S4 (its split former-S5 states need NONE — single-body per the split ruling) | **E9** | field3d_surgeon | **blocking** |
| U11 | **Revolution marks + circumference bracket as their OWN primitive (A-7)**: own mesh, turn-count trigger, live respace under an R drag; **no** `checkpoints` reuse, **no** `capture`-enum member, **no** formula-surface stamp, **no** `energy_active` side effect (`:42747`), **no** `NLB_CP_MAX` cap (`:43547`) | #11 S1/S8 only | **E12** | field3d_surgeon | **blocking** (for #11) |
| U12 | **ω re-seed on the per-body sandbox wrap (A-8)**: `:45571–45572` re-seeds `v1 = b.v0` only; re-seed ω to the authored ω₀ so each lap replays the same lesson; trail/trace break rides A4 | #11 S8; #12 S8 inherits via U6's synchronised form | **E13** | field3d_surgeon | ride-along |
| U13 | **Visible-elements matcher registration (A-9)**: every new element type registered — finish line + chips, rotation markers, centre markers, k chips, KE readouts, skid trail, cycloid trace, revolution marks, bracket, velocity arrows, zero-vector marker | both | **E14** | **`peter_parker:renderer_primitives` → pcpl-surgeon** (the row's own tag) | ride-along |

**Ride-along duties (E8, field3d_surgeon — one dispatch, non-blocking):** `nlb_body_label` out of the brighten-only set (**E6 — blocking for S4**, listed here for locality); θ-arc clamp vs outer lane; `#nlb_formula` pixel check at each concept's longest line (340 px); readout zone sized off actual rendered neighbour height; **label decollision via the sprite projection probe** (`_pmText` + measureText — never DOM); friction-arrow reveal-ink floor **sampled at each body's ACTIVATION frame, not only state t = 0**; new-mode apparatus-visibility bring-up probe (existence + visibility, joint with E14's registration); new modes + `deriveStateMeta` co-edit at all three sites, proven against both config shapes — **shape requirement: up to TWO sequential activation bodies per state (#12 S3; #11 S4). No three-phase shape exists anywhere in the union (Ruling 3(ii); A5 DELETED).**

**Registration duty (json_author, per concept):** `concept_panel_config` insert in the same session; motion declared consistently in `epic_l_path` AND `field_3d_config` (F14 — unchanged).

**What is deliberately NOT in the union:** no energy bars / SEAM-L change; no zoom inset; no graph panel; no RHR hand; no new `scenario_type`; **no choreography DSL — `activate_at_ms` is the whole timed surface (Ruling 3(v))**; no three-phase config shape (A5 deleted). #11 consumes U1–U4 + U7(velocity) + U8(gap-0 + target) + U9 + U10 + U11 + U12 + shared ride-alongs; U5–U6 are #12-only; both facts recorded so the Phase-0 success test (zero renderer edits after 0c-2 lands) is judged against this exact list.
