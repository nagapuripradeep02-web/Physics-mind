> # ⚠ INCOMPLETE — DO NOT READ AS REV 2, DO NOT FEED TO ANY AGENT OR TO DESK E
>
> **What this is:** the fix-cycle-1 rewrite of `rotational_kinematics`, abandoned part-written on
> 2026-08-04 when the `alex:architect` dispatch died on an account-level API error
> ("organization has disabled Claude subscription access for Claude Code"). Not a work failure —
> an auth failure, mid-write.
>
> **Why it is not REV 2:** it is truncated before its SCAR AUDIT section, while its own status
> block (line 5 below) claims 157 bug-queue rows are "dispositioned VERBATIM in the SCAR AUDIT".
> The document asserts a section it does not contain.
>
> **What IS good in it** (kept because the cycle was expensive, and this is the record of it):
> the FIX-CYCLE-1 RESPONSE table, all ten sections, Blocks 1–2, and the ENGINE REQUIREMENTS
> rewrite. The desk's reconciled shared contract landed: α as the per-step finite difference,
> the signed-torque semantics consumed BY REFERENCE from `_engine/findings_d.md` §1, the
> zero-consumer back-compat argument, and the "authored ω₀ = 0 already works" correction.
>
> **The live document is `skeleton.md`, restored to REV 1** (gated `DESIGN_FIX`, see
> `founder_proxy_A.md`). Re-run fix cycle 1 from REV 1 once API access is restored; this file is
> reference material for that re-run, not its input.

# Skeleton — `rotational_kinematics` (rotmech · Class 11 Ch.7 · concept #4 · Desk D 0b design pass) — REV 2

> **Status:** Phase-0b design, **fix cycle 1 of 2** (responds to `founder_proxy_A.md`, verdict `DESIGN_FIX`). REV 1 preserved at `skeleton_rev1.md`. This concept is **BLOCKED on build 0c-3**; no concept JSON may be authored until the 0c-3 engine PR merges. The lesson design (nine-state arc, numbers, rings, anchors, Rule-25 answer) is UNCHANGED per the gate ("do not redesign the lesson"); this revision rewrites the engine section, adopts the desk's reconciled shared contract, and fixes the specific P1/P2 defects. **Desk E freezes 0c-3's scope from `_engine/findings_d.md` PASS 2 (the merged union), not from either skeleton alone.**
> **Renderer-readiness declaration (scar `archetype_live_tier_unverified_against_renderer`):** every `[LIVE]` claim cites file:line verified against renderer CODE (REV-1 session + the cycle-1 orchestrator verifications adopted below). Every `[NEEDS-0c-3]` motion transfers its verification obligation to the 0c-3 build and its bring-up probes.
> **Bug-queue consultation (2026-08-04, LIVE table via Bash):** `--owner alex:architect` · `--row-type directive` · `--field3d --open` · `rotational_kinematics` (0 rows — concept not yet authored, expected). Union extracted mechanically: **157 unique `bug_class` strings**, every one dispositioned VERBATIM in the SCAR AUDIT.
> **DC Pandey check:** chapter table of contents only (rotational-motion chapter scope confirmed). No teaching method, example problem, or figure imported. NCERT = syllabus backbone; HC Verma sequence consulted for ordering only (angle → rate → rate-of-rate → equations → v = ωr).
> **Namespace check:** `rotational_kinematics` appears in neither `src/data/concepts/` nor `chemistry/` — no collision (ids pre-registered on master in `4b289d4`; registration files READ-ONLY on this desk).
> **Apparatus contract:** `APPARATUS_CONTRACT.md` §1 binding — home pose r = 0.80 m, ω = +1.50 rad/s, m = 2.0 kg, I_frame 0.50, rod_half 1.00, drum 0.55, rod height 0.25, r ∈ [0.15, 0.90]. S1 opens EXACTLY at the home pose. Requested deviations are in §"Apparatus-contract items for the office" — none is taken unilaterally.

---

## FIX-CYCLE-1 RESPONSE (finding → what changed → where it now reads)

| Finding | What changed | Where |
|---|---|---|
| **Contract-3 / P1-1** | The canonical signed-torque semantics MOVED OUT of this document to `findings_d.md` §1 — this skeleton (like the sibling) consumes them **by reference**, restates nothing, and yields all ownership claims. The α metric is the reconciled shared sentence (finite difference), string-diffable with the sibling's D3 | K1, K2, §3 metrics, scar rows 27/155 |
| **Contract-1 / P1-1** | K2's α is the **per-step finite difference of ω**, published from the SAME post-step snapshot as I, ω, L and KE (`rbrWriteReadouts`, `field_3d_renderer.ts:50219`), blanked across re-pins exactly as the other rows are. The analytic τ_signed/I definition is DELETED | K2, §3 metrics |
| **P1-2** | With finite-difference α, the readout returns to 0.00 the step after the rest clamp binds. S5's held claim decided explicitly: `release_at_ms` authored at the stop instant (5.0 s) — the pad withdraws, parked by 5.9 s; the pin (6.6 s) photographs *stopped table, α 0.00, pad parked* | S5 row, §3 pin table, §4 |
| **P1-3** | K5's shape is now `time_ticks: { start_ms?, start_cue?, every_ms, count }`, positions `rbrThetaAt(start_ms + k·every_ms)`; S7 authors `start_ms: 2000` (the engage instant) — ticks land at 0.30/1.20/2.70 rad as claimed | K5, S7 row, scriptability walk |
| **P1-4** | Option (a) chosen — zero engine cost: S1's start line, existing stripe, arc and θ readout are ALL present and counting from t = 0; narration sync is glow/label emphasis only (LIVE sentence-glow channel); no reveal cue and no θ-zero field bought. S1 wording + pin budget re-derived from the 4.19 s full turn | S1 row, DoD (d), pin table |
| **P1-5** | K3 shrunk to (a) the fixed base reference ray + (c) the swept arc. The body mark is the **existing** `rbr_drum_marker` (`:50322-50327`, `RBR_ALWAYS_ON` `:50585`) — findings_d §3: Desk E builds no second one. ONE reference radius declared for line, arc AND ticks: **r_ref = 0.50 m on the drum top face** (the stripe's outer end, ≈ 0.506 m) | K3, K5, §3 |
| **P1-6** | K10 rewritten: rbr IS declared (`deriveStateMeta.ts:496-512`) — the superseded "confirm it exists" ask removed. The real gap is findings_d §6b, carried verbatim: motion declared from the seed alone, so rest-seeded S3/S7/S8 fall through `undefined` and D5 skips; the fix ships in the SAME change as K1 | K10 |
| **P1-7** | S5's slider **dropped** (no controls); ONE concept-wide α range [−0.60, +0.60] on the per-concept `slider_controls` path (`:50005` — the only path that exists). S9's sandbox engagement authored as `engage_at_ms: 0` for the whole state; K7's buy names the live-drag write path (τ re-anchor at the drag instant, `:49911-49925` pattern). No per-state range override is bought or assumed | K7, S5/S9 rows |
| **P1-8** | The Δθ/Δt fallback DELETED — it needed an unbought third row and lost the state's idea. Declared honestly: descoping K6 **drops S8** (8 states; advanced ring empty; extended (S7) becomes the last ring before explore); the ring-cut walk is re-run for the 8-state case; `entry_state_map.calculus_graphs` is removed in that case | K6, DoD (i-1), §7 note |
| **P2-1 / Contract-2** | K8 tag corrected: torque timing LIVE (`:50533`) but the actuator TRAVEL is unwired for the applied source — `padEngageMs` is assigned only in the brake branch (`:50521-50526`) and the travel render is gated on the `rbr_brake_pad` mesh (`:50729-50744`), so `eMs` = Infinity and nothing travels. Desk E lifts the travel logic onto the actuator the state's torque source names (findings_d §4b). The sibling consumes THIS SAME wheel mesh and layers its rim force arrow on it | K8 |
| **P2-2** | The unbuildable 0.7 s post-contact beat deleted. The Rule-32a cause-before-effect beat IS the travel window: `pad_travel_ms: 900` authored; torque begins exactly at contact. S3/S4/S7/S8 wording updated | S3/S4/S7/S8 rows |
| **P2-3** | K1 states the redefinition plainly: `applied_torque_Nm` is an existing field with **zero consumers** (`git log --all -S` returns only the 0c-1 commit; nothing under `src/data/`), so the regression argument is the zero-consumer check + a byte-identical `brake` branch — not the absence clause, which now covers only the genuinely NEW fields | K1, ENGINE REQUIREMENTS preamble |
| **P2-4** | K2 carries findings_d §2's loud `console.warn` on unknown readout tokens | K2 |
| **P2-5** | K1 closes the source-enum mismatch: `'applied_torque'` added to the declared enum (`:1000` vs `:50518`; findings_d §7) | K1 |
| **P2-6** | Office item 3 re-worded: HUD order is the AUTHORED array order (`:50158-50162`), so this concept simply authors θ · ω · α; the office ask is the `RBR_RO_META` **insertion position** (chapter-wide, APPARATUS_CONTRACT §3). Units/dp noted as NOT a fork (the sibling's alpha row is identical) | Office item 3 |
| **P2-7 / Contract-4** | Office item 2 is the reconciled single ask: lower the ω₀ floor to 0 at **both** sites — `RBR_SLIDER_SPEC.omega0.min` (`:49999`) AND the live-write guard `if (!(value > 0)) return;` (`:50075`). Explore state only. The sibling is aligned to identical text | Office items 1-2 |
| **P2-8** | S6's inner point layering declared: the r = 0.40 paint mark, its radius line and its v arrow live at ROD height (0.25 m) above the drum face; the always-on stripe is at drum-face height and colour-distinct — vertical + colour separation stated, not assumed | S6 row, K4 |
| **P2-9** | K4 registers the group token **`rbr_v_arrows`** so the single-token `glow_focal` (`:1053`) lights both arrows as one focal | K4, S6 row |
| **P2-10** | K6's graph zone named: right-anchored panel, `top:52px+`, right half — clear of the engine-fixed left-middle formula band (`:50448`; citation corrected — `:50570-50574` is the rebuild call site) | K6, DoD (h) |
| **P2-11** | Handoff rewritten: `findings_d.md` PASS 2 is the ONE merged union Desk E freezes from; this document's K-numbering is local and non-canonical; ownership of every shared paragraph yielded to findings_d | Handoff |
| P3-1 | Considered, no change (lesson frozen this cycle): the S3↔S4 redundancy is real and recorded as the arc's thinnest link for any future cut | this row |
| P3-2 | §2 marks S5 `misconception_confrontation` (matching §4) | §2 table |
| P3-3 | S2 drops THREE ticks (1.50 / 3.00 / 4.50 rad) — the near-closure fourth tick removed | S2 row, K5 |
| P3-4 | LIVE visibility flags this concept authors OFF are now declared (`show_l_arrow` would print an L the §10b ledger forbids) | Explicitly NOT required |
| P3-5 | Handoff instructs PASS 2 to downgrade findings_d §8 item 5 (θ/α mark surfaces — out of scope, no consumer) | Handoff |
| P3-6 | S9's wheel visibility stated: in contact the whole state (engage 0), turning when α is nonzero, idle at α = 0; per-state visibility flag ON | S9 row |
| Verified-fact (orchestrator) | An **authored** `omega0_rad_s: 0` already works today (`:50497` resolves through `rbrNum` `:49828`, typeof/isFinite — a literal 0 is honoured). Guided rest states S3/S7/S8 need NO new field, only K1's signed torque; only the SLIDER is floor-blocked. Office item 1 rescoped accordingly | K1 probes, Office item 1 |

Ledger re-run note (scar `prior_rulings_and_internal_ledgers_are_not_re_run_over_the_states_a_restructure_creates`): the state LIST is unchanged (9 states, same ids, same rings); the §10b ledger, prespoil ledger, glow plan, min_ring gating, camera plan and both watches were re-walked over the edited beats — the only ledger deltas are the ones named above (S5 controls removed; S2 tick count; S1 all-visible-from-t0).

---

## 1. Atomic claim

This concept teaches ONE thing: **rotation is described by three quantities — angle turned θ, turning rate ω, and rate of change of turning α — that obey the SAME equations as straight-line motion (ω = ω₀ + αt is v = u + at with new letters), and v = ωr ties each point's linear speed to the one shared turning rate.** It does not cover what makes the spin change (`torque`, #5), rotational mass (`moment_of_inertia`, #6), the τ = Iα law (`tau_eq_i_alpha`, #7), or ω as a vector along the axis (`angular_momentum`, #9 — this concept treats ω and α as signed rates only, no right-hand rule).

**The Rule-25 problem, answered here (the sharpest design constraint):** this concept sits BEFORE torque and moment of inertia, so whatever produces α on screen must be introducible without dynamics vocabulary. The design: α is produced by two familiar rendered objects — a **motor drive wheel** that presses the drum and turns it (spin-up), and the existing **brake pad** (slow-down) — narrated purely kinematically: "the motor wheel presses the drum; the spin rate rises steadily — that steady rise is α." The words τ, torque, I, and inertia appear in NO reader-facing string, no formula surface, no label. The authored `applied_torque_Nm`/`tau_brake_Nm` values are internal implementation numbers physics_author computes from the target α (τ = 3.06·α at the fixed home r), exactly as `pend_k` is internal to the dipole scenarios. The HUD shows ONLY θ, ω, α — never I, L, or KE (do-not-prespoil: those belong to #6-#10). A further benefit of the reconciled α metric: α is read off the MOTION (the finite difference of ω), so even its definition never routes through τ or I.

## 2. State count + arc — 9 states (8 guided + 1 explore)

Complexity call: **complex (7-9 band)**. The concept carries three new quantities (θ, ω, α), two equations (ω = ω₀ + αt core; θ = ω₀t + ½αt² extended), one link law (v = ωr), a sign story (negative α), and an advanced calculus/graph ring. Nine states, none padding: each grades against "what does this state prove the previous ones don't?"

The apparatus is the SHARED chapter turntable (Rule 32d): axle + drum (R 0.55) + rod (half-length 1.00) + two symmetric masses **fixed at r = 0.80 for the whole concept** — no radial slide anywhere; this concept's motion vocabulary is purely rotational, visibly distinct from the radial slides of `conservation_of_angular_momentum` on the same machine. All states author `theta0_rad: 0` (the EXISTING drum stripe — `rbr_drum_marker`, `:50322-50327`, always-on — starts ON the start line at every entry re-pose). State entry is an instantaneous single-frame re-pose. Camera: ONE home framing for all nine states (slightly elevated three-quarter view showing the drum face, rod, start line, and HUD) — no reframe anywhere (Rule 32d; scar `camera_solve_searched_in_one_axis_hides_the_feasible_region_in_the_axis_held_fixed` trivially discharged by having no camera solve).

**Authored numeric ground truth (all 2 dp; physics_author recomputes at the engine's 16 ms grid):**
I at r = 0.80 is 3.06 kg·m² (internal only, never rendered). Home spin ω = 1.50 rad/s → period T = 2π/1.5 = **4.19 s**; one full turn = **6.28 rad**. S3: from rest, α = **+0.60 rad/s²** for 4.0 s → ω = **2.40** (internal τ_app = 1.84 N·m). S4: from ω₀ = 1.50, α = +0.60 for 3.0 s → predicted ω = **3.30**. S5: from ω₀ = 1.50, α = **−0.50** (internal τ_brake = 1.53 N·m, inside the [0, 2.0] contract range) → rest at **3.00 s** after engagement. S6: at ω = 1.50 — v(0.40) = **0.60 m/s**, v(0.80) = **1.20 m/s**, ratio exactly **2.00** (= 0.80/0.40; scar `ramp_endpoints_multiply_the_taught_variable_by_a_factor_no_rendered_string_claims`: the rendered factor equals the factor the strings claim). S7: from rest at α = 0.60 — θ at 1, 2, 3 s AFTER ENGAGEMENT = **0.30, 1.20, 2.70 rad**; per-second increments **0.30 : 0.90 : 1.50 = 1 : 3 : 5**. No numeral triple repeats an earlier state's (scar `quantitative_check_state_reuses_the_exact_numbers_and_the_delta_cue_of_the_state_it_verifies` — cross-tabulated: S3 (0→2.40, 4.0 s), S4 (1.50→3.30, 3.0 s), S5 (1.50→0, 3.0 s, α −0.50) all distinct). **Metric note:** in every steady drive window the finite-difference α equals the internal τ/I exactly (the closed form is linear in t), so every displayed number above is unchanged by the reconciled α metric; the ONLY behavioural difference is at clamps and edges — α reads 0.00 the step after the S5 rest clamp binds (the P1-2 fix), and blanks across re-pins with the other rows.

| State | Title (Rule 41 — literal, first words carry meaning) | Purpose | teaching_method | Ring |
|---|---|---|---|---|
| S1 | The angle turned is θ | θ defined and measured: start line, drum stripe, growing arc; 2π rad = one full turn | *(straightforward beat)* | core (qualitative) |
| S2 | ω is radians turned per second | ω = θ/t for steady turning; equal-time ticks land equally spaced | *(straightforward beat)* | core (quantitative) |
| S3 | α is how fast ω changes | Spin-up from rest under a visible motor wheel; ω climbs steadily; α readout | *(straightforward beat)* | core |
| S4 | The same equation as straight-line motion | THE PRIMARY AHA: ω = ω₀ + αt predicts 3.30 BEFORE the run; the live readout meets the chip | *(straightforward beat)* | core |
| S5 | Slowing down is negative α | Brake beat: ω falls in a straight line to rest while α sits at −0.50 | `misconception_confrontation` | core |
| S6 | One turning rate, many speeds | v = ωr: two points, one ω, tangent arrows in ratio 2:1 | `misconception_confrontation` | core |
| S7 | The angle grows as t squared | θ = ω₀t + ½αt²: equal-time ticks now land 1 : 3 : 5 apart | *(straightforward beat)* | extended |
| S8 | ω is the slope of the θ graph | Calculus forms ω = dθ/dt, α = dω/dt on live θ(t)/ω(t) curves | `derivation_first_principles` | advanced |
| S9 | Try it yourself | Sandbox: ω₀ and α sliders, all readouts and arrows live | `exploration_sliders` | *(explore — core-ring content)* |

**Rule 38a — both clauses:** ladder = qualitative (S1) → quantitative (S2-S6) → extended (S7) → derivation (S8); rings monotone; the advanced ring (S8) is a contiguous block immediately before the explore state ✓. `advance_mode`: S1-S8 `manual_click`, S9 `interaction_complete` (Gate 12: ≥2 distinct modes ✓).

## 3. Per-state choreography + control plan (Rule 31 control table)

**Beat-termination contract (scar `authored_beat_ends_by_undoing_the_state_own_claim` — declared per state, no third shape invented):** S1, S2, S6 are **steady-continuous** — ω is constant (no source), the closed-form spin runs for the whole state, and nothing ever changes back (a steady spin cannot undo a claim; each revolution completes the motion cycle Rule 31 requires). S3, S4, S5, S7, S8 are **one-shot-hold**: the drive/brake engages at an authored instant, releases at an authored instant, the actuator's withdrawal completes BEFORE the pin, and ω then HOLDS at its end value for the remainder of the state (no source after release, so ω is exactly constant by the closed form `field_3d_renderer.ts:49933-49943`; ticks and marks persist). **No `param_ramp` is authored anywhere in this concept** (scar `field3d_param_ramp_authoring_contract` N/A-by-design — the timed surface used is the engagement window `engage_at_ms`/`release_at_ms`/`pad_travel_ms`, `:1002-1006`, read for the applied source at `:50533`). S9 free-runs (Rule 37). Entry configs are stated per row; every state entry is a single-frame re-pose.

**Angular reference geometry (decided once — P1-5):** the body mark is the EXISTING `rbr_drum_marker` stripe on the DRUM top face (radial extent axle → ≈ 0.506 m, `:50322-50327`, always-on `:50585`). ALL angular references share ONE radius: **r_ref = 0.50 m on the drum top plane** — the K3 start line is a base-fixed ray drawn to r_ref, the K3 swept arc draws at r_ref from line to stripe, and the K5 ticks land on the r_ref circle. The S6 point marks are a SEPARATE system at ROD height (0.25 m above the drum face): the r = 0.40 paint mark on the rod, the r = 0.80 mass, their radius lines and v arrows all draw at rod height, colour-distinct from the drum-face stripe — vertical + colour separation, no overdraw (P2-8).

**Coined archetypes (four, each justified once):**
- `equal-time-ticks` — marks dropped on the reference circle at equal time intervals make the growth law visible as SPACING. Declared by S2/S7 as a contrast pair (even spacing = constant ω; widening 1:3:5 = constant α).
- `converge-on-mark` — a live readout sweeps toward a pre-stated prediction chip and the match cue fires when it arrives. The distinct picture is the arrival itself (the inverse of the sibling skeleton's `diverge-from-mark`).
- `paired-tangent-ride` — two tangent arrows ride their points around one shared revolution; their lengths differ in exact proportion to radius. The distinct picture is the riding pair.
- `slope-trace` — a live dot draws a curve on a graph while a tangent-slope indicator rides it; the indicator's steepness IS the derived quantity.

**Archetype-discharge rule:** every archetype is discharged by motion the AUTHORED beat produces with no teacher input, within the state duration. (S5 no longer carries a slider — its beat is fully authored.)

| State | Teaches (one idea) | Archetype | ENTRY CONFIG (r · ω₀ · source, t = 0) | Authored beat (cause → effect) | Delta cue | Live controls | Words | Ring |
|---|---|---|---|---|---|---|---|---|
| S1 | θ = the angle turned, measured in radians from a start line | `reveal-build` | r 0.80 · ω₀ +1.50 (HOME POSE) · no source | Turntable spins steadily. The start line, the drum stripe, the θ arc and the θ readout are ALL present and live from t = 0 (P1-4 option a): the stripe starts ON the line (theta0 0), the arc grows from zero, the readout counts up from 0.00. Narration sync is glow/label emphasis ONLY (sentence-glow bindings, in ledger §10b order: line → stripe → arc → readout). At one full turn (4.19 s) the readout shows **6.28 rad** and narration names it: 2π radians = one full turn. Steady-continuous | **"θ: the angle turned"** | none | 30-45 | core |
| S2 | ω = radians turned per second — the steady rate the angle grows | `equal-time-ticks` | r 0.80 · ω₀ +1.50 · no source | Same steady spin. Every 1.0 s a **tick** drops onto the r_ref circle at the stripe's position — THREE ticks land **equally spaced** (at 1.50, 3.00, 4.50 rad — 1.50 rad apart, none near the start line; P3-3). After the third tick the **ω readout** reveals: 1.50 rad/s. Formula surface `ω = θ / t`. Steady-continuous | **"ω: radians per second"** | none | 30-50 | core |
| S3 | α = how fast ω itself changes; a steady spin-up means constant α | `translate-through` | r 0.80 · ω₀ 0 (from rest — authorable TODAY, `:50497`/`:49828`) · drive engages at 2.0 s | CAUSE first — the **motor drive wheel** translates in over the travel window `[1.1 s, 2.0 s]` (`pad_travel_ms: 900` — the travel IS the Rule-32a beat, P2-2), visibly turning, and makes contact at 2.0 s. The EFFECT begins at contact: the table starts from rest and the **ω readout climbs steadily** 0.00 → 2.40 over 4.0 s while the **α readout** sits pinned at **0.60 rad/s²**. At release (6.0 s) the wheel withdraws (parked by 6.9 s); ω HOLDS at 2.40. One-shot-hold. Narration notes in passing: just after contact ω has barely left 0.00 while α already reads its full 0.60 — a rate can be changing even when it is momentarily near zero | **"α: the rate ω changes"** | none | 35-55 | core |
| S4 | ω = ω₀ + αt — the straight-line equation with new letters, used to PREDICT | `converge-on-mark` | r 0.80 · ω₀ +1.50 · drive engages at 3.5 s | The formula surface shows **ω = ω₀ + αt**; narration computes the prediction aloud: 1.50 + 0.60 × 3.0 = 3.30 — and the **chip `predicted ω = 3.30`** prints beside the live ω readout at 2.0 s `[LIVE]`, BEFORE the wheel starts moving. THEN the wheel travels in `[2.6 s, 3.5 s]`, contacts, and drives for 3.0 s: ω sweeps 1.50 → 3.30 and **the match cue fires** at 6.5 s — chip and readout hold-glow together `[LIVE]`. Wheel withdraws (parked by 7.4 s); ω holds at 3.30. One-shot-hold. Bridge clause (prerequisite patch): "exactly like a car speeding up steadily — v = u + at, same equation, new letters" | **"Same equation, new letters"** | none | 40-55 | core |
| S5 | Slowing down is also α — with a negative sign | `translate-through` (declared contrast pair of S3 — the delta names the flip: drive in → α +0.60; brake in → α −0.50) | r 0.80 · ω₀ +1.50 · brake engages at 2.0 s, releases at 5.0 s | CAUSE — the **brake pad** travels in `[1.1 s, 2.0 s]` and contacts the drum `[LIVE]`. EFFECT from contact: ω falls in a straight line 1.50 → 0.00 over 3.0 s while the **α readout sits at −0.50** the whole way. The table stops at 5.0 s (rest clamp, `:49933-49943`); **α returns to 0.00 the step after the clamp binds** (finite-difference metric — the readout is honest: no change in ω, no α). `release_at_ms: 5000` — the pad withdraws at the stop, parked by 5.9 s. **Held claim: stopped table, ω 0.00, α 0.00, pad parked** — the pin photographs exactly this (P1-2). One-shot-hold. Anchor sentence (§9, ~9 words, verbatim in this state's narration): **"like a ceiling fan after the switch is turned off."** | **"Slowing down: negative α"** | none | 35-55 | core |
| S6 | v = ωr — one shared ω, but each point's speed grows with its radius | `paired-tangent-ride` | r 0.80 · ω₀ +1.50 · no source | Two **radius lines** draw at ROD height from the axle collar, distinctly labelled r = 0.40 m (to a paint mark on the rod) and r = 0.80 m (to the mass) — two radii, two labelled lines (scar `teach_distinct_reference_lines_for_two_radii`); both sit 0.25 m ABOVE the drum face, colour-distinct from the drum stripe (P2-8). Then two **tangent arrows** reveal and RIDE their points through one full shared revolution: the outer arrow exactly **twice** the inner (labels `v = 1.20 m/s`, `v = 0.60 m/s`), while both points stay on one straight rod line and finish the turn together. The axle centre carries a labelled **v = 0 dot** (never a floored arrow — K4). Formula surface `v = ωr`. Steady-continuous. Anchor sentence (§9, ~11 words, verbatim here): **"on a bicycle wheel, the rim moves faster than points near the hub."** Focal = the group token **`rbr_v_arrows`** (K4) — one registered id lighting BOTH arrows, so the single-token `glow_focal` (`:1053`) resolves it (P2-9; scar `state_glow_focal_dims_one_half_of_the_relation_the_state_exists_to_teach`) | **"One ω, many speeds"** | none | 40-55 | core |
| S7 | θ = ω₀t + ½αt² — from rest, the angle grows as t² | `equal-time-ticks` (declared contrast pair of S2 — the delta names the flip: even spacing under constant ω → widening 1:3:5 under constant α) | r 0.80 · ω₀ 0 · drive engages at 2.0 s | Wheel travels in `[1.1 s, 2.0 s]`, contacts (cause, familiar from S3); the table spins up from rest and ticks drop at 1, 2, 3 s AFTER ENGAGEMENT (K5 `start_ms: 2000` — P1-3) — landing at θ = 0.30, 1.20, 2.70 rad: increments **0.30, then 0.90, then 1.50 rad: 1 : 3 : 5**, visibly widening around the r_ref circle, against the remembered even spacing of S2. θ readout confirms 0.30 → 1.20 → 2.70. Release at 5.0 s; wheel parked by 5.9 s; holds. One-shot-hold. Formula surface `θ = ω₀t + ½αt²` | **"Angle grows as t²"** | none | 35-55 | extended |
| S8 | ω = dθ/dt and α = dω/dt — rate IS slope | `slope-trace` | r 0.80 · ω₀ 0 · drive engages at 1.5 s | The **graph panel** (K6) shows empty θ-t and ω-t axes; the same spin-up replays (wheel travels `[0.6 s, 1.5 s]`, drives 4.0 s) while a live dot **draws θ(t) as a curving-upward parabola** and a second dot draws ω(t) as a straight rising line — one state clock drives table, dots, and curves together (scar `teach_coordinate_sim_with_graph`). A **tangent-slope indicator** rides the θ curve, steepening as ω grows; narration: the slope of θ is ω; the slope of ω is α — that is all "d/dt" says. Calculus notation allowed here ONLY (38c). Release 5.5 s; wheel parked by 6.4 s; curves persist. One-shot-hold. **If Desk E descopes K6, this state is DROPPED — see K6 (P1-8); no fallback is authored** | **"Slope of θ is ω"** | none | 35-55 | advanced |
| S9 | Sandbox | `drag-sandbox` | r 0.80 · ω₀ +1.50 · applied source engaged from t = 0 (`engage_at_ms: 0`, no release), τ 0 | Free-running (Rule 37). Teacher sets **ω₀** (restart-seed, re-pin cue on change `[LIVE]`) and drags the **α slider** [−0.60, +0.60] — the slider writes τ_signed = 3.06·α live via the trusted-drag re-anchor path (K7); at α = 0 the engaged source carries zero τ (steady spin). The **drive wheel stays in contact for the whole state** — turning while α is nonzero, idle at α = 0; per-state visibility flag ON (P3-6). θ/ω/α readouts, the stripe/arc, and the S6 tangent arrows all live (ticks off). **Declared semantics:** a sustained negative α CAN take ω through zero and reverse the spin — real physics for a driven wheel (unlike the friction brake), the θ arc runs backwards, and the v arrows flip; declared intended, never clamped (findings_d §1). Long-drive speeds are honest: v arrows clamp at the K4 map max with exact labels | **"Try it yourself"** | ALL: ω₀ *(core; slider floor → 0 = office item 2)* · α drive *(core; concept-wide range)* | 0 / open | *(explore)* |

**Archetype audit:** reveal-build (S1), equal-time-ticks ×2 (S2/S7 declared pair), translate-through ×2 (S3/S5 declared pair), converge-on-mark (S4), paired-tangent-ride (S6), slope-trace (S8), drag-sandbox (S9). No repeat outside the two declared pairs; no static state; every guided beat auto-plays on the state clock.

**Explore controls — ring-gated (scar `explore_controls_not_ring_gated_survive_the_ring_cut`):** both S9 controls (ω₀, α) are core-ring, taught by S2-S5 — they survive BOTH preset cuts ✓. The S9 formula surface `ω = ω₀ + αt` is stated and performed by S4 (core), surviving every preset (scar `explore_state_formula_surface_asserts_a_relation_no_state_derives` ✓).

**Readout metrics (scar `derived_readout_asserted_by_value_without_defining_its_metric`):** `θ` = `rbrThetaAt(tMs)` — the engine's own fixed-16 ms-grid integral of ω, already computed and published as `PM_rbrTheta` (`:49952-49966`, `:50232`); displays the accumulated value (beyond 2π), 2 dp; the arc draws θ mod 2π. `ω` = L/I as today (`:49945-49948`). `α` (new, K2) — **the reconciled desk definition, one sentence, shared verbatim with the sibling (string-diffable):** *α is the per-step finite difference of ω, published from the SAME post-step snapshot as I, ω, L and KE (`rbrWriteReadouts`, `field_3d_renderer.ts:50219`), and blanked across re-pins exactly as the other rows are.* It stays true at the rest clamp (reads 0.00 on a stopped table), under a live dI/dt, and across every engage edge — and it keeps τ and I out of this concept's definitions entirely (Rule 25). In steady drive it equals τ/I exactly (the closed form is linear in t), so all §2 values are unchanged. `v` labels (S6) = ω·r_point from the same snapshot. All values closed-form and noise-free — the taught deltas (0.60 rad/s² steps, 2:1 arrow ratio) dwarf display resolution (scar `taught_delta_smaller_than_the_instruments_own_live_noise` ✓). Every taught variable has a geometric correlate, never digits alone: θ = the arc + stripe; ω = the visible spin rate + tick spacing; α = the actuator + the steady climb; v = arrow length (scar `taught_variable_has_no_rendered_physical_correlate_so_a_text_label_is_the_only_cause` ✓).

**Rule 33 macro↔micro:** N/A-with-justification — θ, ω, α ARE the visible motion of the macro apparatus itself; there is no hidden micro mechanism. Instruments per 33d: value-only HUD, live 2-dp numbers.

**Rule 34 canvas budget:** top caption = the ≤5-word delta cue only. ONE formula surface per state (the engine-fixed Cambria-Math band, styling `:50448`; `:50570-50574` is the rebuild call site — citation corrected per P2-10): S1 none · S2 `ω = θ / t` · S3 `α = Δω / Δt` · S4 `ω = ω₀ + αt` · S5 `ω = ω₀ + αt  (α < 0)` · S6 `v = ωr` · S7 `θ = ω₀t + ½αt²` · S8 `ω = dθ/dt · α = dω/dt` · S9 `ω = ω₀ + αt`. All math real Unicode (θ ω α ω₀ ½ π ² Δ). Algebra only outside S8 (38c — Δ-notation is NCERT pre-calculus; d/dt confined to the advanced ring). All surfaces symbolic; numerals live only in the HUD, the chip, and the tick labels.

**Pin-margin discipline (margins in ms; pin = clamp(0.60·R, 150, R−150); the budgeted instant is the COMPLETION of the last asserted reveal — for one-shot-hold states that is the actuator PARKED in the held end pose, per scar `skeleton_pin_budget_names_the_phase_start_instead_of_the_last_asserted_reveal_inside_it`):**

| State | Last asserted reveal completes (design est.) | State duration R (min) | Pin at 0.60R | Margin |
|---|---|---|---|---|
| S1 | full turn — θ readout reaches 6.28 at 4.19 s (all elements live from t = 0, P1-4) | ≥ 10 s | 6.0 s | 1810 ms ✓ |
| S2 | 3rd tick 3.0 s + ω reveal ≈ 4.0 s | ≥ 10 s | 6.0 s | 2000 ms ✓ |
| S3 | run ends 6.0 s; wheel parked 6.9 s | ≥ 12 s | 7.2 s | 300 ms ✓ |
| S4 | match cue 6.5 s; wheel parked 7.4 s | ≥ 13 s | 7.8 s | 400 ms ✓ |
| S5 | stop 5.0 s (rest clamp within one 16 ms step, scar `nlb_frozen_pin_lands_within_one_frame_of_the_beat_the_dod_asserts_it_shows`); α reads 0.00 from ≈ 5.016 s; pad parked 5.9 s | ≥ 11 s | 6.6 s | 700 ms ✓ |
| S6 | arrows ≈ 2.5 s + one shared turn 4.19 s ≈ 6.7 s | ≥ 12 s | 7.2 s | 500 ms ✓ |
| S7 | 3rd tick 5.0 s; wheel parked 5.9 s | ≥ 11 s | 6.6 s | 700 ms ✓ |
| S8 | curves complete 5.5 s; wheel parked 6.4 s (curves persist) | ≥ 12 s | 7.2 s | 800 ms ✓ |

All margins ≥ 167 ms ✓. One-shot-hold states hold their end configuration (actuator parked) from park time to R, so the pin photographs the held claim by construction — including S5's *stopped, α 0.00, pad parked* frame (P1-2). THE EYE must read DENSE frames across the S3/S4/S5/S7/S8 drive windows, not only frozen frames (scar `teach_read_dense_ramp_frames_not_just_frozen`) — S5's α-at-−0.50-while-falling picture lives in the dense frames; the frozen pin shows the honest end state.

## 4. Misconception confrontation plan (Rule 16a — 2 genuine pivots)

| Wrong belief | At | `misconception_watch` beat |
|---|---|---|
| "When the switch goes off, the story is over — while it slows there is no acceleration" | **S5** | belief: slowing is just 'stopping', not acceleration · visual_counter: the α readout sits at **−0.50** for the entire slow-down while ω falls in a straight line through 1.00, 0.50, to 0.00 — and the moment ω stops changing, α returns to 0.00 (the readout follows the MOTION) · one_line_fix: slowing down IS angular acceleration, with a negative sign — and it lasts exactly as long as the slowing does |
| "Every point of a spinning body moves at the same speed" | **S6** | belief: one body, one speed · visual_counter: both marked points stay on ONE straight rod line and finish the revolution together (one shared ω) — while the outer tangent arrow is exactly twice the inner (1.20 vs 0.60 m/s) · one_line_fix: one turning rate ω for the whole body, but each point's speed is v = ωr |
| | | Named primitives for each wrong picture (scar `field3d_rule16a_belief_unbuildable_for_want_of_a_rod_primitive`): S5 needs the α readout row (**K2**) + the brake pad `[LIVE]`; S6 needs the two radius lines + paired tangent arrows + v labels + the `rbr_v_arrows` group token (**K4**) |

S1-S4, S7-S9 carry NO misconception_watch (founder guardrail 2026-07-04). Both watches are straightforward contrast beats — the correct physics runs continuously; no wrong-physics sub-beat is ever rendered, so any pin instant photographs correct physics (scar `frozen_pin_unbudgeted_on_a_sequential_misconception_state_can_archive_the_wrong_picture` discharged: no sequential wrong-picture half exists; the S5 pin now photographs α 0.00 at rest — consistent physics, P1-2). EPIC-C branches: **zero**.

## 5. `has_prebuilt_deep_dive` states (2)

**S4** (the equation-use skill — where exam work concentrates, and where rad-vs-rev unit confusions surface) and **S6** (v vs ω conflation — the most documented rotational-kinematics confusion). V1.0 ships zero authored deep-dives (Rule 18); the flag marks investment priority.

## 6. Drill-down clusters

**S4:** `radians_vs_revolutions` (θ in rad vs turns; the 2π conversion) · `sign_of_alpha` (speeding vs slowing; deceleration as negative α) · `linear_angular_mapping` (x↔θ, v↔ω, a↔α — which linear equation becomes which).
**S6:** `same_omega_different_v` (one turning rate, radius-dependent speed) · `which_r_in_v_omega_r` (r is the point's distance from the AXIS, not the body's size) · `omega_vs_v_confusion` (rad/s vs m/s — units untangle the two).

## 7. `entry_state_map`

```
entry_state_map:
  foundational:    STATE_1 → STATE_5   # θ, ω, α, the equation, negative α — the exam core
  v_and_omega:     STATE_6             # "how are v and ω related" routes straight to v = ωr
  angle_equation:  STATE_7             # θ = ω₀t + ½αt² questions
  calculus_graphs: STATE_8             # (removed entirely in the K6-descope 8-state case — P1-8)
```

Default `foundational`. PRIMARY aha (S4) inside the foundational range ✓ — the silent student meets the same-equation aha and the negative-α confrontation on the default slice.

## 8. Prerequisites (advisory — Rule 23)

- `rigid_body_rotation` (#3, in-chapter, precedes in the approved order — will exist at 0d): every point traces a circle; outer points travel farther in the same time. This concept quantifies that picture.
- Linear kinematics (v = u + at; Class 11 straight-line motion): **no new-architecture concept id exists** (the ~60 legacy kinematics JSONs are old architecture, not product) — advisory prose note only, patched inside S4 (Block 1).

## 9. Real-world anchor (Rule 35 / 38f — universal, culture-neutral)

**Primary: a ceiling fan after the switch is turned off — it keeps turning and slows at a steady rate.** Assigned to **S5**, ~9 words; draft sentence quoted verbatim in the S5 row and required verbatim in the S5 narration draft (scar `skeleton_anchor_specified_in_section_9_reaches_no_narration_line`): *"like a ceiling fan after the switch is turned off."* A ceiling fan is a widest-overlap household device (38f), recognisable in any country, and physically exact: near-uniform angular deceleration to rest.
**Secondary: a bicycle wheel — the rim moves faster than points near the hub.** Assigned to **S6**, ~11 words; draft sentence, verbatim in the S6 narration draft: *"on a bicycle wheel, the rim moves faster than points near the hub."* Physically exact v = ωr, universal.
No region constants, no brands, no places. The India-specific catalog anchors (Vande Bharat, cricket spin) are NOT imported (survey Rule-35 table).

## 10. Definition of Done (Gate 0 — zero TBDs)

**(a) States:** the 9 of §2, exactly as tabled in §3.

**(b) Symbol-label table + term-introduction ledger (I, L, KE, τ are NEVER printed anywhere in this concept):**

| Quantity / element | Label | DEFINED at | First PRINTED at | ✓ |
|---|---|---|---|---|
| Angle turned | `θ` (arc + HUD `6.28 rad`) | S1 sentence 1-2 | S1, live from t = 0 | ✓ |
| Radian / start line / drum stripe | start-line label; the existing stripe | S1 ("measured from this start line") | S1 | ✓ |
| Time | `t` (in formula surfaces) | S2 | S2 | ✓ |
| Angular speed | `ω` (HUD `1.50 rad/s`) | S2, after the ticks | S2 | ✓ |
| Equal-time ticks | tick marks on the r_ref circle | S2 ("a mark every second") | S2 (reused S7) | ✓ |
| Angular acceleration | `α` (HUD `0.60 rad/s²`) | S3, as the climb is seen | S3 | ✓ |
| Motor drive wheel | plain visual (no symbol) | S3 ("a motor wheel presses the drum") | S3 | ✓ |
| Initial angular speed | `ω₀` | S4, one clause | S4 | ✓ |
| Prediction chip | `predicted ω = 3.30` | S4, the prediction sentence | S4 | ✓ |
| Brake pad | plain visual (no symbol) | S5 ("a brake pad presses the drum") | S5 | ✓ |
| Point radius | `r` (two labelled radius lines) | S6, first sentence | S6 — never earlier | ✓ |
| Linear speed of a point | `v` (tangent arrows + `1.20 m/s`) | S6 | S6 | ✓ |
| Graph axes θ-t, ω-t | axis labels (Unicode) | S8 | S8 only | ✓ |

json_author note: every glow target names a primitive the state builds (glow-target set ⊆ built ids, including the `rbr_v_arrows` group token; focal on physics primitives, never titles).

**(c) Right-hand-rule plan: N/A-with-justification.** This concept teaches θ, ω, α as signed scalar rates; ω as a VECTOR along the axis (grip rule) is the payload of `angular_momentum` (#9) and is deliberately not pre-spoiled. No hand primitive is consumed.

**(d) Motion plan:** S1 steady spin, all references live from t = 0, glow-synced naming (line → stripe → arc → readout) · S2 steady spin + three even ticks + ω reveal · S3 wheel travel-in (the cause beat) → contact → steady climb from rest → release + withdrawal, held · S4 chip prints (2.0 s) → wheel travels + contacts → readout sweeps to meet chip, match cue → withdrawal, held · S5 pad travel-in → straight-line fall to rest, α pinned negative → clamp, α to 0.00, pad withdraws → held at rest · S6 radius lines → paired tangent arrows ride one shared revolution, 2:1 · S7 wheel engages → ticks land 1:3:5 from the engage instant, widening → withdrawal, held · S8 spin-up replay + live parabola/line drawing + riding slope indicator, curves persist · S9 free-running sandbox, wheel in contact throughout, live sliders. No passive state; every stated agent (wheel, pad) is a rendered object; every stated number is produced by the §3 metrics.

**(e) Modes:** conceptual-only (Rule 20 [D]); no `mode_overrides`.

**(f)** `assessment` + `coverage_map` authored at 0d; `misconception_watch` exactly the 2 of §4.

**(g) Macro↔micro:** N/A-with-justification per §3.

**(h) Canvas budget:** per §3. New DOM panels at `top:52px+` both edges (scars `field3d_sliders_panel_top12_vs_fsbtn_top10`, `cyclotron_timers_sliders_fullscreen_button_corner_collision`); zones: the formula band is engine-fixed left-middle (`:50448`), the **K6 graph panel is right-anchored, right half, `top:52px+`** (P2-10), the HUD is 3 rows max, caption top-centre, sliders bottom — five distinct zones, nothing clips (`caption_clipped_by_adjacent_stat_box`, `nlb_formula_and_readout_zones_are_fixed_css_and_collide_with_a_tall_hud`).

**(i) Curriculum-flex (Rule 38):**
- **(i-1) Preset-cut coherence:** *Hide advanced (drop S8):* S1-S7 + S9 coherent; no surviving state references d/dt or the graph. *Hide advanced+extended (drop S7-S8):* S1-S6 + S9 coherent; nothing surviving references t², 1:3:5, or calculus; S9's controls (ω₀, α) map to surviving states S2-S5 ✓. **K6-descope case (P1-8): the concept ships 8 states (S8 dropped entirely) — S1-S7 + S9 coherent (identical to the hide-advanced cut); the advanced ring is EMPTY; extended (S7) is the last ring before explore, still contiguous (38a holds vacuously for advanced); `entry_state_map.calculus_graphs` removed; the `no_calculus` and `full` presets coincide.**
- **(i-2)** Explore = core content only: surface `ω = ω₀ + αt`, stated and performed by S4, surviving every preset ✓.
- **(i-3) `curriculum_tags` (claims, not facts):** CBSE/NCERT — covered, marked verified (angular kinematic variables + equations are core NCERT Ch.7). JEE Main core+extended+advanced · NEET core+extended · IB DP / A-level / AP Physics 1 and C — every non-CBSE cell `needs_teacher_verification: true`.
- **(i-4) Presets (hide, never reorder):** `full` = S1-S9 · `no_calculus` = hide S8 · `core_only` = hide S7-S8. Controls auto-survive (both core).
- **(i-5) Graph axes (S8):** t on the horizontal axis, θ and ω on the vertical — the universal convention for motion-time graphs across every surveyed board; no genuine conflict exists, so NO axis-swap toggle (decided, not deferred). Graph title/caption z-order named as a K6 build obligation (scar `graph_title_caption_zorder_overlap`).

**Teacher-usability walk (scar `directive_no_gate_asks_whether_a_teacher_could_use_it`):** (1) *Does anything state the law and show it in the assessed representation?* Yes — S4 states ω = ω₀ + αt AND performs the exam's exact skill (compute the prediction, watch it verified); S7 adds the second equation; S8 formalizes. (2) *First thing a teacher tries after the aha?* "What if it slows instead?" — S5's authored beat shows it; then S9's full-range α slider (both signs) re-runs slow-down at any strength, with from-rest starts once office item 2 lands. (3) *Definition precedes use?* Yes — ledger §10(b); τ/I/L/KE never appear.

---

## Block 1 — Pass-1 strategic checklist

**Prerequisite cliffs.** `rigid_body_rotation` → **S1**: a student who missed it may not accept that every point turns through the same angle; patch = one S1 sentence, "the table turns as one piece — every point turns through the same angle together" (no condescension: it doubles as the setup for θ being a single number). Linear kinematics → **S4**: the aha assumes v = u + at is familiar; patch = the one bridge clause already in the S4 row ("exactly like a car speeding up steadily — v = u + at"), naming the IDEA only, no off-screen apparatus (scar `narration_references_a_prerequisite_concepts_apparatus_that_is_not_on_screen`).

**JEE-backwards trace.** *"A fan rotating at 3.00 rad/s is switched off and comes to rest in 6.0 s under uniform angular deceleration. Find (i) α, (ii) the number of revolutions before it stops, (iii) the speed of a blade tip at r = 0.60 m at the instant of switch-off."* (i) α = −ω₀/t via ω = ω₀ + αt → S3 (α defined) + S4 (equation) + S5 (negative sign). (ii) θ = ω₀t + ½αt², then rev = θ/2π → S7 (equation) + S1 (2π rad = one turn). (iii) v = ωr → S6. No missing piece. Note: item (ii) needs the extended ring — under the `core_only` preset that item is out of scope by design (recorded in i-3).

**Misconception entry mapping.** Both confronted proactively per §4. Planting risk 1: S1-S5 must say "turning rate", never "speed", for ω — so S6 breaks a belief the narration never endorsed. Planting risk 2: S3's "the motor makes it speed up" must not become "α needs a motor" — S5 immediately shows α from a brake, and S3's narration says "whatever changes the spin, the RATE of change is α".

## Block 2 — Aha-moment designation

- **PRIMARY aha, at S4:** *rotation needs no new mathematics — ω = ω₀ + αt IS v = u + at with new letters, and the readout lands exactly on the number the old equation predicted.*
- **SUPPORTING aha, at S6:** *one ω, many speeds — the whole body shares a single turning rate, yet every point has its own speed, tied by v = ωr.* Total = 2.
- **Cohesion check:** the v = ωr law of S6 is precisely the physical bridge that makes the linear↔angular identification of S4 more than notation — each point's linear motion literally obeys the linear equations at its own radius. The supporting aha serves the primary ✓.
- **Wrong-belief setup.** Primary: S1-S3 introduce three unfamiliar Greek-lettered quantities in a row, deliberately building "this is a new subject with its own strange rules" — S4 breaks it with the old equation predicting the new machine. Supporting: S1-S2 speak of THE turning rate (one number describes the whole body), building "one number, one motion" — S6 keeps the ω half and breaks the speed half.
- **Foundational coverage:** S4 ∈ foundational (S1-S5) ✓.

---

## ENGINE REQUIREMENTS (for Desk E, build 0c-3) — this concept's demand list

**NOT the freeze source: Desk E freezes 0c-3's scope from `findings_d.md` PASS 2, the single merged union of BOTH Desk-D skeletons (P2-11). The K-numbering below is local to this document and non-canonical.** Gap classes: PHYSICS · DISPLAY · VOCABULARY · CONTROL. Ranked. **Back-compat, stated precisely (P2-3):** the genuinely NEW fields (K3 references config, K4 `point_markers`, K5 `time_ticks`, K6 graph, K7 `alpha` token) are OPTIONAL with absence reproducing today's behavior byte-identically (scar `engine_extension_replaces_a_shared_constant_without_an_absent_field_fallback_clause`). K1 is different in kind — a **semantic redefinition of an existing field with ZERO consumers** (verified: `git log --all -S "applied_torque_Nm"` returns only the 0c-1 build commit and this desk's docs commits; `grep -rl applied_torque_Nm src/data/concepts/` returns nothing), so its regression argument is the zero-consumer check plus a byte-identical `brake` branch, not the absence clause. Regression pair after 0c-3: `conservation_of_angular_momentum` (rbr path, `source: 'brake'` only) + this desk's samples `normal_force`, `inductance` — EYE runs must show no pixel regression.

1. **K1 `[NEEDS-0c-3 · PHYSICS · structural but cheap — rank 1, nothing works without it]` Signed applied torque (spin-up).** Verified gap: the single closed-form integrator computes `mag = |L0| − tau·engaged_seconds`, clamped at 0 (`field_3d_renderer.ts:49933-49943`), and the `applied_torque` source takes `Math.abs(...)` (`:50532`) — every source can only REDUCE |L|; from rest the body never moves. **The canonical semantics live in `findings_d.md` §1 and are consumed here BY REFERENCE — this document defines nothing** (Contract-3; scar `two_skeletons_sharing_one_engine_build_state_different_semantics_for_the_same_bought_field`): brake = frictional, opposes the spin, keeps the rest clamp, never reverses; applied = signed driven torque, closed form `L(t) = L_anchor + τ_signed·engaged_seconds(t)`, no rest clamp, may carry L through zero; both sources ride the existing engagement surface (`:1002-1006`, applied read at `:50533`); accumulator-free, Rule 36/`:969-976` preserved. **Redefinition stated plainly (P2-3):** today an authored `+1.84` decays the spin; after K1 it accelerates — a redefinition of a dead field (zero consumers, above), safe iff the `brake` branch is byte-identical. **Enum close (P2-5, findings_d §7):** `'applied_torque'` is added to the declared `external_torque.source` enum (`:1000`; the implementation already resolves it at `:50518`). **Bring-up probes:** (i) authored `omega0_rad_s: 0` honoured — VERIFIED already true today (`:50497` → `rbrNum` `:49828`, typeof/isFinite), so this probe is a regression guard, not a new capability; (ii) τ_signed = +1.84 from rest for 4.0 s → ω = 2.40 within 1e-9; (iii) `brake` branch outputs bit-identical to today on the regression pair. No GUIDED state crosses zero; the S9 reversal is emergent, declared physics (findings_d §1). Fail-without: S3, S4, S7, S8, S9.
2. **K2 `[NEEDS-0c-3 · DISPLAY · cheap — rank 2, the concept's named quantities]` θ and α readout rows + loud warn.** Verified gap: `RBR_RO_META` (`:50147-50154`) implements exactly six rows `I · omega · L · KE · dLdt · F_pull` — no `theta`, no `alpha` — and `rbrRebuildReadout` (`:50162-50163`) plus `rbrWriteReadouts` (`:50236-50237`) skip unknown tokens in TOTAL SILENCE. θ is already computed and published (`rbrThetaAt` `:49952-49966`; `PM_rbrTheta` `:50232`) — a pure display gap. Required: add `theta: { label: "θ", unit: " rad", dp: 2 }` and `alpha: { label: "α", unit: " rad/s²", dp: 2 }` to `RBR_RO_META` and the `readouts` doc-enum (`:1043`). **α metric — the reconciled desk definition (Contract-1, one sentence, shared verbatim with the sibling, string-diffable):** *α is the per-step finite difference of ω, published from the SAME post-step snapshot as I, ω, L and KE (`rbrWriteReadouts`, `field_3d_renderer.ts:50219`), and blanked across re-pins exactly as the other rows are.* (Same construction as the existing `dLdt` row, `:49975+` — the honest finite difference of the engine's own integrated quantity.) **Loud warn (P2-4, findings_d §2 — "worth more than the rows themselves"):** an unknown readout token `console.warn`s once per state, converting the chapter-wide silent-seal trap into a visible failure for every future rbr concept. **Enum hygiene (scar `deferred_enum_members_must_be_declared_not_merely_unimplemented`):** `reference_marks[].surface` (`:1023`) is NOT extended — `'theta'`/`'alpha'` mark surfaces are declared out-of-scope for 0c-3 (no state needs them; the only chip rides the existing `'omega'` surface). Fail-without: every state (θ); S3-S5, S8, S9 (α).
3. **K3 `[NEEDS-0c-3 · DISPLAY · small — rank 3]` Angular reference: fixed base ray + swept θ arc — the body mark ALREADY EXISTS.** Per findings_d §3 (and P1-5): the body-fixed mark is the existing `rbr_drum_marker` — a radial stripe on the DRUM top face, axle → ≈ 0.506 m, built at `:50322-50327`, in `RBR_ALWAYS_ON` (`:50585`) — **Desk E builds NO second mark.** Required, exactly two items: (a) a space-fixed **start line** — a base-frame ray on the drum top plane drawn to **r_ref = 0.50 m**; (c) a **θ arc** at r_ref from the ray to the stripe, drawing θ mod 2π (the readout carries the full accumulated value). ONE radius — r_ref = 0.50 m, the stripe's outer end — serves the ray, the arc AND the K5 ticks (stated once, consumed by both rows). This is what `theta0_rad` (wired: `:50499` → `rbrThetaReset` `:49967-49971`) needs to be measurable: the entry re-pose places the stripe at θ₀ from the ray. This concept authors `theta0_rad: 0` everywhere. Both new elements register in the exact-token element gate (`:50581+`). No reveal cues bought — S1 shows everything from t = 0 (P1-4 option a). Fail-without: S1, S2, S7 (and every θ claim).
4. **K4 `[NEEDS-0c-3 · VOCABULARY · small — rank 4, the stated payload]` Tangential velocity arrows at marked points + the `rbr_v_arrows` group token.** Verified gap: `show_pull_arrows` draws the radial arrow only; no tangential velocity arrow and no per-point marker exists — v = ωr has no rendering path. Required config shape: `point_markers: [{ id, r_m, show_v_arrow, show_radius_line, show_circle_trace }]` — a paint-mark at an authorable radius on the rod plus the mass point; tangent arrows ride the rotation with length ∝ ω·r_m on **their OWN magnitude-to-length map** (scar `velocity_arrows_routed_through_a_force_arrow_map_collapse_their_ratio_and_cannot_draw_a_true_zero`, remedy adopted verbatim): the smallest authored nonzero v (0.60 m/s) clears the floor with margin; map sized for v up to ~3.5 m/s, clamped above with EXACT labels; **exact-zero renders as a labelled dot at the axle, never a stub or floored arrow**. Ratio probe: rendered length ratio = speed ratio within 10% for every pair. **Layering (P2-8):** marks, radius lines and arrows draw at ROD height (0.25 m above the drum face), colour-distinct from the drum-face stripe — vertical + colour separation from the always-on `rbr_drum_marker`. **Group focal (P2-9):** K4 registers the group token **`rbr_v_arrows`** in the exact-token element/glow registry so the single-token `glow_focal` (`:1053`) lights BOTH arrows as one focal — without it the S6 grouped focal is unbuildable. Labels decollided/kerned (scars `field3d_label_sprite_overlap`, `radius_scenario_F_r_label_kerning_collision`, hysteretic decollision). Optional `show_circle_trace` is shared machinery with concept #3's per-point circular traces (survey 0c-1 row 3) — build once. Fail-without: S6 (and S9's arrows).
5. **K5 `[NEEDS-0c-3 · DISPLAY · small — rank 5]` Equal-time ticks with an explicit time origin.** Config shape (P1-3): `time_ticks: { start_ms?, start_cue?, every_ms, count }` — at each `start_ms + k·every_ms` a persistent tick lands on the **r_ref = 0.50 m circle** (the K3 radius) at the stripe's position. Positions are CLOSED-FORM: `rbrThetaAt(start_ms + k·every_ms)` — re-derivable under a time pin, no accumulator (preserves `:969-976`; a rewind reproduces the tick set exactly; scar `rewind_path_assertion_stops_at_the_function_body_and_never_follows_its_calls`: the rebuild-from-zero branch `:49958` and `rbrThetaReset` `:49967-49971` are the terminating lines of the trace). **Origin discipline:** absent `start_ms`/`start_cue` defaults to 0 (state entry). Consumed: S2 (`start_ms: 0`, count 3 — even spacing) + S7 (`start_ms: 2000` = the engage instant, count 3 — 1:3:5). Origin-evaluation check (the new scar's probe): at S7's first tick (engine t 3.0 s) θ = 0.30 rad — the value the state table claims ✓. Ticks register in the exact-token gate. Fail-without: the S2/S7 archetype.
6. **K6 `[NEEDS-0c-3 · DISPLAY · moderate — rank 6, the ONE declared descope candidate]` θ(t)/ω(t) graph panel.** **SURVEY CORRECTION (against `phase0_survey.md` row #4):** the survey's claim *"θ(t)/ω(t) graph panel already exists in field_3d"* is FALSE for rbr — no graph token appears in the rbr config contract (`:977-1059`) or implementation region (`:49700-50700` swept); other scenarios' graphs are scenario-local canvas code. Required: a compact 2-curve panel (θ-t parabola, ω-t line) in its OWN reserved zone — **right-anchored, right half, `top:52px+`, clear of the engine-fixed left-middle formula band (`:50448`)** (P2-10) — driven by the SAME state clock as the 3D scene (scar `teach_coordinate_sim_with_graph`), with a live dot per curve + a tangent-slope indicator on the θ curve; axes labelled in Unicode; title/caption z-order explicitly stacked (scar `graph_title_caption_zorder_overlap` — this row IS that scar's remedy); redrawn from closed forms each frame (time-pin safe). **Descope consequence (P1-8, decided): if Desk E descopes K6, S8 is DROPPED.** The concept ships 8 states; the advanced ring is empty; the ring-cut walk for the 8-state case is in DoD (i-1); `entry_state_map.calculus_graphs` is removed. No fallback readout is authored — the REV-1 Δθ/Δt fallback needed an unbought third derived row and collapsed S8's idea into S2's, so it was deleted rather than priced. Fail-without: S8 (dropped, by declaration — the derivative-as-slope picture is lost and the concept survives at 8 states).
7. **K7 `[NEEDS-0c-3 · CONTROL · small — rank 7]` `alpha` control token + slider row (concept-wide range; explore only).** Verified gap: `controls_visible` is the closed set `'r'|'m'|'omega0'|'tau_brake'|'spin_dir'` (`:1051`, spec `:49995-50003`) — the explore state cannot expose this concept's taught variable. Required: token `alpha` with slider row (glyph `α`, unit ` rad/s²`, dp 2, **range [−0.60, +0.60] — ONE concept-wide range on the per-concept `slider_controls` path** (`rbrSc` reads `(config.slider_controls || {})[token]` at `:50005` — a top-level block, typed at `:2181`; NO per-state path exists and none is assumed — P1-7), step 0.05, default 0). **Semantics (S9 only exposes it):** S9 authors the applied source with `engage_at_ms: 0` and no release (engaged the whole state; τ 0 at slider 0 is an identity); the slider write maps τ_signed = I·α_slider = 3.06·α (r and m hidden in every state, so I is constant and the α promise exact) and **re-anchors L at the drag instant via the existing trusted-drag/event-anchor pattern (`:49911-49925`) — the write path for the new token is part of this buy.** Thumb + numeric label in lockstep (`:50563` pattern). The `tau_brake` N·m row and `spin_dir` are NOT exposed anywhere in this concept (τ untaught — Rule 25; direction-as-vector untaught — deferred to #9). Fail-without: S9 (Rule 31 explore-exposes-taught-variable).
8. **K8 `[NEEDS-0c-3 · DISPLAY · small — rank 8]` Visible motor drive wheel — ONE actuator mesh, shared with the sibling.** A drive wheel that translates in to contact the drum and visibly TURNS while engaged (cause before effect, Rule 32a), withdrawing on release. **Timing status corrected (P2-1, findings_d §4b): `[LIVE torque timing (`:50533`); ACTUATOR TRAVEL UNWIRED for this source]` —** the travel animation (`:50729-50744`) is gated on the `rbr_brake_pad` mesh and driven by `eng.padEngageMs`, assigned ONLY in the `brake` branch (`:50521-50526`); the `applied_torque` branch leaves it null, so `eMs` = Infinity and nothing travels. Desk E **lifts the travel logic off the brake-pad mesh onto whichever actuator the state's torque source names** and wires `padEngageMs`/`padReleaseMs` in the applied branch — the timing does NOT come free. **Reconciled with the sibling (Contract-2): this is the ONE torque-agent mesh for both Desk-D concepts** — `tau_eq_i_alpha` drops its floating tangential arrow and layers its rim force arrow on THIS wheel. Built once at scene build; per-state visibility only (scar `field3d_build_once_body_reads_a_per_state_flag_from_the_union_def_and_mis_renders_silently`); registers in the exact-token gate. This mesh is the whole Rule-25 answer: a motor wheel is narratable in plain kinematic words. Fail-without: S3, S4, S7, S8 have no rendered cause (scar `narration_attributes_an_effect_to_a_cause_the_model_does_not_contain`).
9. **K9 `[LIVE]` — existing rows this concept consumes (verified at file:line):** brake pad + drum + drawn drum line (`:50518-50527`) · `reference_marks` chip form on the `omega` surface with match cue + co-hold-glow (`:1021-1030`, `:50164-50176`; surface enum includes `'omega'` ✓) with per-frame churn-guarded re-measure for mid-state chips (S4) · `readout_at_ms` per-row reveal (`:1047`, `:50234+`) · `hold_glow` instrument channel (`:50206-50219`) · the ONE Cambria-Math formula surface (styling `:50448`; rebuild call site `:50570-50574`) · re-pin cue with readout blanking (`:49896-49903`) and restart-on-seed-change (`:50544-50550`) · accumulator-free state clock + time-pin exactness (`:969-976`) · slider row slots with visibility-hidden reservation (`:49988-49995`) · `glow_focal`/`phases` (`:1053-1058`) · exact-token `visible_elements` gate (`:50581-50587`) · the existing `rbr_drum_marker` stripe (`:50322-50327`, `RBR_ALWAYS_ON` `:50585`) · authored `omega0_rad_s: 0` honoured (`:50497`, `rbrNum` `:49828`) · fresh engine-record rebuild on entry (`:50490+`, fresh object literal — scar `field3d_integrating_scenario_state_entry_must_rebuild_the_whole_engine_record` satisfied by construction; rbr poses from closed forms anyway) · sentence-glow bindings (the S1 narration sync channel).
10. **K10 `[NEEDS-0c-3 · co-edit]` `deriveStateMeta.ts` in the SAME change as K1 — findings_d §6b consumed by reference.** rbr **is** declared in `deriveMotionExpectations` (`deriveStateMeta.ts:496-512`) — no confirmation needed (P1-6). The REAL obligation is findings_d §6b: the branch declares motion **from the seed alone** (a rest-seeded state falls through `undefined` and THE EYE's D5 gate SKIPS). This concept seeds THREE states at rest — **S3, S7, S8** — and after K1 they are the states that move the most; without the co-edit, D5 goes silent on precisely the states whose entire content is "it starts moving". Required, verbatim from §6b: *declare motion when |ω₀| ≥ 0.05 **or** a signed applied torque is engaged during the state* — shipping in the SAME change as K1, never after. Plus the standard three-site registration for every new timed key (K5 tick instants incl. `start_ms`, K4 arrow reveals, K6 graph window): `F3D_REVEAL_KEYS` · reveal-ms in `maxRevealForField3dState` · hold classification in `deriveHoldExpectations`; steady-spin states classified so the always-turning apparatus never reads as a frozen tail (scar `derivestatemeta_new_scenario_key_absent_from_f3d_reveal_keys_falls_through_to_pcpl`).

### Apparatus-contract items for the office (contract §4 — requested, never taken unilaterally)

1. **Per-state entry ω₀ ≠ 1.50 (S3/S7/S8 enter at ω₀ = 0) — RESCOPED to a contract confirmation only.** An authored `omega0_rad_s: 0` is honoured by the engine TODAY (`:50497` → `rbrNum` `:49828`, typeof/isFinite — verified this cycle); no engine work is requested. The office is asked only to confirm the contract pins the CONCEPT-OPENING pose (S1 = home pose exactly, which this design honours), not every state's entry. Precedent: the sibling's S6 authors ω₀ = −1.50.
2. **ω₀ slider floor → 0, at BOTH sites (the reconciled desk ask — Contract-4/P2-7).** The floor lives at `RBR_SLIDER_SPEC.omega0.min = 0.5` (`:49999`) **and** at the live-write guard `if (!(value > 0)) return;` (`:50075` — which rejects a written 0 even with the min lowered). The single ask: lower it to 0 at both sites together. Affects the **explore state only** (guided rest states author their 0 and need no slider). The sibling `tau_eq_i_alpha` is aligned to this identical text — the contradictory REV-1 requests are superseded on both sides.
3. **`RBR_RO_META` insertion position for `theta`/`alpha` (re-worded — P2-6).** The HUD renders rows in the AUTHORED array order (`rbrRebuildReadout` iterates `rb.readouts`, `:50158-50162`), so this concept simply authors `readouts: ["theta", "omega", "alpha"]` — no engine ordering ask. What IS chapter-wide (APPARATUS_CONTRACT §3: one label/order per quantity across all turntable concepts) is where `theta` and `alpha` are INSERTED into `RBR_RO_META` and what canonical position they take when other concepts show them beside I/L/KE — an office decision, requested here, not taken. Units/dp are NOT a fork: the sibling independently specifies `alpha: { label: "α", unit: " rad/s²", dp: 2 }`, identical to K2.

### Scriptability walk (scar `phase0_union_lists_capabilities_but_never_which_knobs_are_scriptable`; every authored ms classified per scars `skeleton_authors_a_second_timed_action_after_the_engine_buy_was_scoped_to_one_instant`, `skeleton_authors_a_timed_reveal_chain_of_overlays_on_a_scenario_whose_overlay_config_is_static`)

| State | Animated knob / timed item | Cue surface | Class |
|---|---|---|---|
| S1 | narration-synced glow/label emphasis ONLY (all elements live from t = 0 — P1-4) | sentence-glow bindings `[LIVE]` | glow (no timed reveal authored) |
| S2 | tick drops at 1-3 s; ω-row reveal | K5 `time_ticks` (`start_ms: 0`) + `readout_at_ms` `[LIVE]` | bought field / reveal |
| S3, S4, S7, S8 | drive engage/release; actuator travel | `engage_at_ms`/`release_at_ms`/`pad_travel_ms` `[LIVE torque timing :1002-1006, :50533; travel wiring = K8]` | physics event |
| S4 | chip print (2.0 s, before wheel motion) + match cue | `reference_marks.at_ms` + match `[LIVE]` | reveal / physics event |
| S5 | brake engage 2.0 s; rest-clamp instant 5.0 s; release 5.0 s (pad withdraws) | `engage_at_ms`/`release_at_ms` `[LIVE]`; clamp = closed-form event | physics event |
| S6 | radius lines + arrow reveals | K4 reveal fields (named) | reveal window |
| S8 | graph draw window | K6 (bought field; state clock) | bought field |
| S9 | none authored (`engage_at_ms: 0` is the entry config, not a timed beat); live drags | K7 trusted-drag re-anchor | live control |

No authored millisecond falls outside these classes; no overlay is scheduled on a surface without a time input.

**Per-state × engine-row WALK (both directions):**

| State | Consumes |
|---|---|
| S1 | K2 (θ row), K3, K9 (clock, sentence glow), K10 |
| S2 | K2 (θ, ω), K3, K5 (start_ms 0, even), K9 (formula, readout_at_ms), K10 |
| S3 | K1, K2 (ω, α), K8, K9 (cues, HUD), K10 |
| S4 | K1, K2, K8, K9 (chip + match cue, formula), K10 |
| S5 | K2 (ω, α), K9 (brake + release `[LIVE]`, formula), K10 |
| S6 | K2 (ω), K4 (incl. `rbr_v_arrows`), K9 (formula), K10 |
| S7 | K1, K2 (θ, ω, α), K5 (start_ms 2000, 1:3:5), K8, K9, K10 |
| S8 | K1, K2, K6, K8, K9, K10 |
| S9 | K1, K2, K3, K4 (arrows live), K7, K9 (re-pin, sliders, Rule 37), K10 |

Reverse: K1 → S3/S4/S7/S8/S9 ✓ · K2 → all ✓ · K3 → S1/S2/S9 ✓ · K4 → S6/S9 ✓ · K5 → S2/S7 ✓ · K6 → S8 ✓ · K7 → S9 ✓ · K8 → S3/S4/S7/S8 (+S9 contact pose) ✓ · K9/K10 → all ✓. Every K-row claimed by ≥1 state; every state claims ≥1 row; every primitive named in §3 maps to a K-row (scars `phase0_union_table_asserted_not_walked_state_by_state`, `signed_engine_union_drops_items_its_own_state_table_still_consumes` — the REV-1→REV-2 row deltas are exactly the FIX-CYCLE table's, none dropped silently — `named_primitive_declared_without_the_surface_that_can_render_it` ✓).

**Explicitly NOT required:** any `param_ramp` use · radial mass motion · `r`/`m`/`tau_brake`/`spin_dir` control rows · I/L/KE/dLdt/F_pull readouts · `reference_marks` surfaces beyond `'omega'` · per-state slider-range overrides (none exist, none assumed — P1-7) · a second body mark (the stripe exists — P1-5) · energy bars · a human/fan/bicycle mesh (anchors are narration-only) · through-zero choreography in any GUIDED state (the S9 reversal is emergent, declared per findings_d §1) · precession, vectors, cross products. **LIVE visibility flags authored OFF in ALL states (P3-4):** `show_l_arrow` (would print an L the §10b ledger forbids), `show_pull_arrows`, `show_r_line`, `show_grip_hand`, `ke_bar`; `show_drum_line` ON only in the actuator-contact states (S3-S5, S7, S8), OFF elsewhere.

---
