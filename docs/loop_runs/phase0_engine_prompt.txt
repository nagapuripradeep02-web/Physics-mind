PHASE 0 - build the newtons_laws_body field_3d engine. This is a ONE-TIME engine build, not concept authoring. Do NOT author any concept JSON in this session beyond the minimum smoke fixtures the spec's build order calls for.

Read docs/NEWTONS_LAWS_BODY_ENGINE_SPEC.md IN FULL first. It is founder-approved and is the contract for this session. Then read CLAUDE.md sections 1, 5 and 6 for the operating rules it cites (especially Rules 19, 24, 27, 29, 31, 32, 33, 34, 36, 37).

Your job:

1. Dispatch the field3d-surgeon agent to implement the spec's section 5 integration checklist - all 11 sites in src/lib/renderers/field_3d_renderer.ts plus the 2 required sites in src/lib/validators/visual/deriveStateMeta.ts. Give it the spec file path and tell it to read the spec itself; do not paste the whole spec into the dispatch prompt.

2. AMENDMENT 4 DISCIPLINE IS IN FORCE and it is the single most important cost control in this session. A bundled multi-bug dispatch on Ch.7 burned 156M tokens in 91 minutes. Therefore:
   - Split the build across SEPARATE field3d-surgeon dispatches along natural seams, for example: (a) config type + union + scene build + dispatch case, (b) the integrator and the frame-update call site, (c) the force-arrow overlay, (d) the pulley/rope geometry, (e) sliders/explorer/PARAM_UPDATE + the #sliders exclusion chain, (f) deriveStateMeta. Each dispatch does ONE seam.
   - Ceiling per dispatch: roughly 100 tool calls or 45 minutes. If a dispatch approaches it, have it write docs/loop_runs/engine_handoff.md and exit; the next dispatch reads that file and continues.
   - Never re-dispatch an agent to "re-derive" something already diagnosed. Name the fix.
   - You are the orchestrator: do NOT read field_3d_renderer.ts yourself (it is ~2.3 MB). Dispatch, read the agent's report, reconcile. Keep your own context small.

3. After each dispatch, run the verify chain and fix before moving on:
   npm run check:renderer-syntax
   npx tsc --noEmit          (must be 0 errors)
   npm run validate:concepts (must pass)

4. When all sites are in, prove the engine works on the two structural extremes named in the spec's section 7 - free_body_diagram (arrow overlay + ghost, no integrator) and connected_bodies (Branch B + pulley geometry). A minimal smoke fixture is enough here; full concept authoring happens later in the chapter loop.

5. Regression: run THE EYE on this branch's regression sample to prove no existing field_3d scenario broke:
   npm run cache:clear:scoped -- electric_flux
   npm run visual:eyes -- electric_flux
   npm run cache:clear:scoped -- magnetic_flux
   npm run visual:eyes -- magnetic_flux
   Dispatch the eye-walker agent to read the frame dumps - do NOT load the PNGs into your own context.

6. Commit to this branch (feat/lom-a) when the verify chain is green. Use one commit per seam, message form:
   feat(engine): newtons_laws_body <seam> - Laws of Motion chapter engine
   Record every commit SHA in docs/loop_runs/lom_a_state.md under engine_commits:.

HARD CONSTRAINTS:
- Never merge to master. Never touch any other worktree or branch.
- No DB writes to engine_bug_queue. Scar candidates go to docs/loop_runs/lom/_engine/scar_candidates.sql for founder review.
- No tts, no build:pilot, no deploy, no PILOT_CONCEPTS edits.
- Rule 36: the integrator must be called ONCE per frame with the combined dtStep and must be linear in dt. Never write a literal per-frame 0.016 clock constant and never assume 60 Hz.
- Rule 34c: all on-canvas math is real Unicode, never ASCII transcription.

When the engine is committed and verified, write a short summary to docs/loop_runs/phase0_engine_report.md (what landed, commit SHAs, verify-chain results, anything deferred) and EXIT the session. Do not start authoring the chapter.
