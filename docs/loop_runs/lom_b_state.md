# lom-b loop state

updated: 2026-07-25 (newton_third_law SEALED - CHAPTER MAP FOR THIS WORKTREE IS COMPLETE, 3 of 3)
review_port: 8090
regression_sample: gauss_law_sphere, gauss_law_solid_sphere
next: (none - this worktree's map is DONE. All three concepts SEALED. Next action is the CHAPTER-END step, section 4: regression EYE across the regression_sample, then founder review, then merge feat/lom-b into feat/lom-a. Do NOT start another concept here.)
done: newton_first_law (SEALED 2026-07-25; visual_baselines/newton_first_law locked, 4 states), newton_second_law (SEALED 2026-07-25; visual_baselines/newton_second_law locked, 4 states), newton_third_law (SEALED 2026-07-25; visual_baselines/newton_third_law locked, 4 states; commit aaa50e2)
parked: (none)
in_flight: (none)
engine_commits: 3e1b159 nlb state-local clock (cherry-picked from lom-a cd8fe67); 3a576ea nlb two-body lane offset (cherry-picked from lom-a ff408ed)
chapter_map (founder-approved 2026-07-25): newton_first_law, newton_second_law, newton_third_law

notes: This worktree is HALF of the Laws of Motion chapter. The other half (free_body_diagram, connected_bodies, block_on_incline) runs concurrently in C:\Tutor\physics-mind-lom-a on branch feat/lom-a. Two worktrees because git cannot check out one branch twice and two sessions must never race the same working tree.

notes: These three concepts are deliberately the SIMPLEST of the six - every one of them is a strict subset of what worktree A's free_body_diagram and connected_bodies already exercise. All three are flat ground (theta = 0), frictionless (mu = 0), no pulley. newton_third_law uses the action_reaction block; the other two are plain single/two-body cases. If any of them appears to need a renderer change, that is a RED FLAG that the Phase 0 engine under-generalized: park it with an engine_gap.md and stop, do not extend the engine here.

notes: The newtons_laws_body engine is INHERITED, not built here. Engine fixes should be rare-to-none in this worktree. If engine_commits: reaches 3 in this branch, pause and notify the founder.

notes: Agent roster is 11 - the base 10 plus field3d-surgeon. founder-proxy is deliberately ABSENT (traditional pipeline, founder-approved 2026-07-25).

notes: Auto-approve is ON. quality-auditor PASS plus eye-walker clean (zero new engine_bug_queue rows) triggers npm run visual:approve automatically. TTS, PILOT_CONCEPTS, build:pilot and deploy remain FOUNDER-GATED.

notes: Merge order at chapter end is feat/lom-b into feat/lom-a, then the founder reviews and merges onward. The only expected conflict surface is three append-style registration lists (panelConfig.ts, intentClassifier.ts, aiSimulationGenerator.ts).

notes (2026-07-25, concept 1 newton_first_law): FOUNDER ATTENTION - two things worth a look. (1) The blocking defect was INVISIBLE to all 19 deterministic EYE gates: it manifests as an HUD-numeric + position-vs-caption contradiction, not a pixel regression, and a NEW concept has no prior baseline to diff against. Only the eye-walker frame read caught it. That is an argument for a deterministic probe asserting "v at the first captured frame equals the authored initial_velocity_mps" on any scenario carrying its own integrator. (2) engine_commits is still 0, but this is the first data point against the Phase 0 "concepts 2-6 need ZERO renderer edits" success criterion - it is a DEFECT in the built engine rather than an under-generalization (no config knob was missing; both upstream agents reported ENGINE GAP: none), so it does not by itself invalidate the one-scenario design.

notes (2026-07-25): DISPATCH-REGISTRY GOTCHA that will bite the next session too - field3d-surgeon exists on disk and check:agents reports 11/11, but a newly added agent type only becomes dispatchable in a session started AFTER it landed. Any session that needs field3d-surgeon must be a fresh one. The second, MODERATE finding (field3d_nlb_phase_glow_handoff_not_visible, STATE_3 weight->normal glow handoff not perceptible) is deliberately NOT dispatched - owner is ambiguous between renderer_primitives and json_author and needs founder triage; Amendment 4 also caps one bug_class per engine dispatch. Both rows are in docs/loop_runs/lom/_engine/scar_candidates.sql (report only, no DB writes).

notes: 2026-07-25 CROSS-BRANCH ENGINE FIX. newton_first_law parked on field3d_nlb_physics_clock_not_state_local - the nlb integrator accumulated wall-clock from SET_STATE instead of the reveal window, so a body moved before anyone saw it (STATE_1 froze at v=0.00 under a 'never slows' caption; STATE_2's whole deceleration finished pre-reveal). Worktree A independently hit the SAME root cause on free_body_diagram STATE_3 and fixed it properly in cd8fe67: a real nlbResetTrajectory() that rewinds s/v from new s0/v0 seeds and rebases eng.t_ms, while deliberately preserving live teacher controls (m, F, theta, mu). Cherry-picked here as 3e1b159 rather than fixing twice - two divergent fixes to the same function would have conflicted at merge. Verified after the pick: check:renderer-syntax OK, tsc 0, validate:concepts 126 PASS / 0 FAIL.

notes: The park note's instruction to 'follow the kt / kinematics_1d_track RESET_TRAJECTORY convention' was WRONG for these branches - that scenario does not exist here (grep returns 0); it was uncommitted work in another worktree when feat/lom-a was cut from ec09b28. The conventions that DO exist here are stateStartTime (~118 sites) and RESET_TRAJECTORY (~9). Do not go looking for kt.

notes (2026-07-25, concept 1 SEALED): newton_first_law re-verified and sealed. Chain: check:renderer-syntax OK, tsc 0, validate:concepts 126 PASS / 0 FAIL, cache:clear:scoped + RE-SEED (mandatory - the sim HTML is assembled from the renderer and cached, so a renderer fix does not reach THE EYE without it), visual:eyes 19/19 passed $0, build:review HTTP 200 on 8090. quality-auditor VERDICT PASS (re-ran gate 4 live walk + HUD/caption numerics + gate 7 + gate 8; carried the purely-authorial gates forward since the JSON is byte-unchanged). eye-walker CLEAN, ZERO NEW candidates. Amendment 6 auto-approve fired: visual:approve locked 4 baselines. Both pre-fix symptoms are gone in pixels - STATE_1 frozen now reads v=1.00 (was 0.00 at the motion bound under a 'never slows' caption), STATE_2 first dense frame reads v=1.00 and the ~4s decel (1.00 -> 0.76 -> 0.51 -> 0.27 -> 0.03 -> 0.00) now plays inside the reveal window, so the declared S1/S2 contrast pair - the PRIMARY aha - is finally visible. Full reports: docs/loop_runs/lom/newton_first_law/reverify_reports.md. engine_commits stays at 1 (the cherry-pick); no engine work was needed this session.

notes (2026-07-25, FOUNDER ATTENTION - two carried items, neither blocking): (1) field3d_nlb_phase_glow_handoff_not_visible is STILL reproducing - STATE_3's authored weight->normal glow handoff at 4000ms fires in code but produces no perceptible brightness delta. MODERATE, owner still AMBIGUOUS between peter_parker:renderer_primitives and alex:json_author, so it was deliberately not dispatched (Amendment 4 also caps one bug_class per engine dispatch). It is in docs/loop_runs/lom/_engine/scar_candidates.sql, report only, never inserted into the DB - it needs a founder ownership call. (2) PROCESS GAP found by eye-walker: visual_eyes.ts does not persist its deterministic summary line (D1p/D5/D6/D7/H1/H2, '19 checks / 19 passed / 0 failed') into manifest.json, and writes no console log to disk - so a later reader of the artifacts alone cannot cite the gate result. The orchestrator captured it from stdout this time. Worth making visual_eyes.ts write that line into manifest.json. Also still open from the prior session: a deterministic probe asserting 'v at the first captured frame equals the authored initial_velocity_mps' for any scenario carrying its own integrator - the clock defect was invisible to all 19 gates and only the frame read caught it.

notes (2026-07-25, concept 2 newton_second_law PARKED): Pipeline ran clean end to end - architect (ENGINE GAP: none), physics-author (ENGINE GAP: none), json-author (JSON + 4 registration sites + SQL migration; PCPL_CONCEPTS correctly untouched). tsc 0, validate:concepts 127 PASS / 0 FAIL, THE EYE 19 checks / 19 passed / 0 failed / $0, build:review HTTP 200 on 8090. quality-auditor VERDICT PASS. eye-walker returned FINDINGS (2), so Amendment 6 auto-approve correctly did NOT fire and visual:approve was NOT run - this concept has NO locked baselines. Finding 2 (field3d_nlb_arrow_min_length_floor_collapses_small_force_visibility_and_ratio, MAJOR) was FIXED this session as CONTENT, not engine: the arrow-length clamp is designed behavior per engine spec section 3, the defect was authoring 0.2 N / 0.4 N forces which floor-clamp to an invisible stub - killing STATE_1's sole glow_focal (the Rule 32a cause element) and destroying STATE_3's entire pedagogical payload (both arrows clamped to the same length, so the authored 1:2 ratio did not exist on screen). eye-walker filed it against peter_parker:renderer_primitives; the orchestrator RE-ROUTED it to alex:physics_author. Fix = a uniform x75 scale of every force AND every mass, so every acceleration / distance / velocity / clamp margin is numerically IDENTICAL and only the N/kg units moved (smallest on-screen force now 15 N = 1.5x the 0.30 floor; STATE_3 pair 15/30 N = 0.45/0.90 world units, a visible 1:2; masses 75/150 kg match the authored loaded-luggage-cart anchor). Applied and re-verified tsc 0 / validator 127 PASS. NOT re-verified in pixels - the JSON changed after the last EYE run, so the resuming session must re-run visual:eyes AND re-run BOTH gate agents in full (every prior pixel-level and numeric verdict is stale, not merely deltas).

notes (2026-07-25, FOUNDER ATTENTION - the blocker + a wrong prior conclusion): Finding 1 field3d_nlb_two_body_lane_offset_missing_causes_full_occlusion (CRITICAL) is the park cause. nlbSetBodyPosition() (field_3d_renderer.ts ~L29497-29513) hardcodes z=0 for EVERY non-hanging body, but engine spec section 1 explicitly promises "Two bodies with NO pulley = independent, side-by-side". Two bodies authored at the same initial_position_m - the intended same-start-line compare pattern - render FULLY COINCIDENT: STATE_2/STATE_3 show only one block at t=0 with the two labels as an illegible merged m1m2 blob for ~1/3 of the run, and the H2 SET_TIME_FREEZE reveal baseline lands INSIDE the overlap window, so the frozen reference frame is illegible by construction. There is NO authoring workaround (staggering initial_position_m keeps both bodies in one lane, so the faster body interpenetrates the slower one - worse, and it destroys the 2:1 distance comparison). This is a DEFECT, not an ENGINE GAP - no config knob is missing and no author-facing surface changes - so it runs section 3b, NOT the park-with-engine_gap.md path, and no engine_gap.md was written (same reasoning the founder accepted for concept 1's clock defect). IMPORTANT: concept 3 newton_third_law is ALSO a two-body concept and WILL hit this same defect - fixing it is effectively a prerequisite for finishing this worktree. THE DISPATCH-REGISTRY CONCLUSION FROM LAST SESSION IS WRONG: the prior note said "fresh sessions from now on do have field3d-surgeon". This WAS a fresh session and the dispatch still failed with "Agent type 'field3d-surgeon' not found"; renderer-primitives, runtime-generation and shipper are ALSO missing from the registry (the whole peter_parker cluster plus the release agent), even though check:agents reports "OK - all 11 emissions are up-to-date". So session freshness is NOT the condition, or not the only one - the obvious suspect is agent-registry loading inside a git WORKTREE versus the main checkout. Two concepts have now parked on this same gap; worth diagnosing directly before the next loop session runs. The session correctly refused both banned fallbacks (Amendment 4 bans general-purpose for field_3d work; section 0.1 bans the orchestrator editing the renderer) and spent ZERO of its 2-attempt repair budget. engine_commits stays at 1.

notes: Root cause of the parked-not-fixed outcome was a dispatch-registry gap: field3d-surgeon exists on disk and check:agents reports 11/11, but it was absent from that session's registry (new agent types only dispatch from a session started after they were added). The session correctly refused both fallbacks - general-purpose is banned for field_3d work by Amendment 4, and the orchestrator editing the renderer is banned by section 0.1 - and spent zero of its 2-attempt repair budget. Fresh sessions from now on do have it.

notes: 2026-07-25 concept 2 - CROSS-BRANCH ENGINE FIX #2. newton_second_law parked on field3d_nlb_two_body_lane_offset_missing_causes_full_occlusion: nlbSetBodyPosition pinned every non-hanging body to z=0, so two bodies at the same initial_position_m rendered as ONE block with merged labels, and the H2 frozen baseline landed inside the overlap window. Fixed on feat/lom-a as ff408ed (nlbBodyLaneZ - only independent non-hanging non-ghost bodies are laned; coupled/single/ghost all return 0) and cherry-picked here. Proven non-regressive: free_body_diagram was re-seeded and re-run through THE EYE at 38/38 after the change, so its locked baselines did not move.

notes: DISPATCH-REGISTRY ASYMMETRY - the real reason this branch keeps parking. field3d-surgeon is byte-identical in both worktrees (CRLF vs LF only) and check:agents reports 11/11 here, yet lom-a sessions have dispatched it 9 times while lom-b sessions have dispatched it ZERO. The error a lom-b session sees also omits renderer-primitives, runtime-generation and shipper, all of which exist here - so lom-b sessions get a PARTIAL agent registry, not a missing file. Consequence for this branch: it authors content well but cannot self-service a field_3d engine defect. Standing workaround - engine fixes are made on feat/lom-a and cherry-picked here. Do NOT fall back to a general-purpose dispatch (Amendment 4) and do NOT let the orchestrator edit the renderer (section 0.1); park and let the founder cherry-pick, which is what happened twice now.

notes (2026-07-25, concept 2 SEALED): newton_second_law re-verified and sealed. This was a RE-VERIFY-ONLY session per the prior next: line - no re-authoring. Chain: tsc 0, validate:concepts 127 PASS / 0 FAIL, cache:clear:scoped + RE-SEED (mandatory - the sim HTML is assembled from the renderer and cached, so the cherry-picked engine fix does not reach THE EYE without it; 2,268,319 chars), visual:eyes 19 checks / 19 passed / 0 failed / $0, build:review HTTP 200 on 8090. One content fix-cycle was needed (1 of the allowed 3), then the WHOLE visual chain was re-run from cache-clear before re-gating. Final: quality-auditor VERDICT PASS, eye-walker CLEAN with ZERO candidates. Amendment 6 auto-approve fired: visual:approve locked 4 baselines. Commit 788a7c9. engine_commits stays at 2 (both cherry-picks); NO engine work was needed this session and none was attempted, so the dispatch-registry asymmetry never came into play.

notes (2026-07-25, concept 2 - THE FIX-CYCLE, a silent-failure class worth remembering): quality-auditor FAILed the first re-audit on [reason: config-rescale-incomplete] and named the fix; json-author applied exactly it. The x75 force/mass rescale from the prior session had landed in physics_engine_config.variables but NOT in field_3d_config.slider_controls - and slider_controls is the block the renderer actually reads to build sliders (nlbSc() at field_3d_renderer.ts ~L30402-30416). nlbSyncSliderRow (~L30562) wrote el.value = 150 onto an input whose max was 5, so the browser clamped the thumb to the rail while the numeric label printed 150.0. Impact was real, not cosmetic: STATE_2's m2 is the ONE live control in any guided state, and dragging it would have emitted a value in [0.5,5], collapsing body B from 150 kg to lighter than the 75 kg body A and INVERTING the "heavier lags" lesson; STATE_4's sandbox was internally two-scale. Fix = rescale slider_controls to match (m/m2 -> {50,300,step 10,default 150}, F -> {-40,40,step 2}; v0 untouched because velocity was never rescaled). AUTHORING RULE: any force/mass rescale on this engine must move BOTH physics_engine_config.variables AND field_3d_config.slider_controls - they are two separate blocks and the validator does not cross-check them. PROCESS NOTE: eye-walker returned CLEAN on the pre-fix frames and under-called this - a rail-pinned thumb next to a correct-looking numeric label is easy to miss in a static frame; the second dispatch was told the exact failure signature and confirmed it in pixels. Worth a deterministic gate: assert every slider_controls range CONTAINS the value the renderer will write for it.

notes: Concept 2 content lesson worth keeping. eye-walker flagged the arrow MIN_LEN floor (len = clamp(0.30, 4.0, N x 0.030), so anything under ~10 N collapses to an identical stub) as a renderer defect; the orchestrator correctly RE-ROUTED it to content, because the clamp is designed behaviour per engine spec section 3 and the real error was authoring a 0.2 N push. physics-author rescaled every force AND mass by a uniform x75, so every acceleration, distance and velocity is numerically identical while the smallest on-screen force became 15 N (1.5x the floor) and STATE_3's pair became a genuinely visible 1:2. Authoring rule for this engine: keep the smallest on-screen force at or above ~15 N.

notes (2026-07-25, concept 3 newton_third_law SEALED): Full pipeline ran end to end in one session - architect (4 states, ENGINE GAP: none), physics-author (ENGINE GAP: none), json-author (JSON + 4 registration sites + SQL migration; PCPL_CONCEPTS correctly untouched). tsc 0, validate:concepts 128 PASS / 0 FAIL, THE EYE 19 checks / 19 passed / 0 failed / $0 on every one of four runs, build:review HTTP 200 on 8090. THREE content fix-cycles (the full allowance) were spent, all of them on ONE root cause class: camera projection. Final gate: quality-auditor VERDICT PASS + eye-walker CLEAN with ZERO candidates -> Amendment 6 auto-approve fired -> visual:approve locked 4 baselines. Commit aaa50e2. NO engine work was needed or attempted; engine_commits stays at 2, so the dispatch-registry asymmetry never came into play this session.

notes (2026-07-25, concept 3 - THE LESSON, and it is a REUSABLE AUTHORING RULE for every two-body concept on this engine): the two-body lane offset is only half a fix - THE CAMERA MUST BE OFF THE LANE AXIS FOR IT TO BE VISIBLE. eye-walker opened with the SAME CRITICAL slug that parked concept 2 (bodies ~85% overlapped, labels stacked, at t=0 AND at the H2 freeze pin). It filed it against peter_parker:renderer_primitives. The orchestrator RE-ROUTED it to alex:json_author as CONTENT after reading the renderer geometry directly: nlbBodyLaneZ applies NLB_LANE_GAP = 0.85 world units on the **z** axis against a mass-independent NLB_BODY_SIZE = 0.55, i.e. a real 0.30-unit gap between block faces - the engine and commit 3a576ea are CORRECT. But all three two-body states had authored camera_position [0, y, z], putting the camera exactly ON the z axis, looking straight down the lane-separation axis, so a genuine 3D separation projected to pure depth and one block sat behind the other. The architect's own skeleton had required "a near side-on camera framing BOTH lanes (projection scar)" and the authored cameras did not satisfy it. AUTHORING RULE: on any newtons_laws_body concept with two independent bodies, camera_position must open the z lane on screen; the geometric test is 0.85 * sin(theta) > 0.55 where theta = atan(Y/Z), i.e. elevation must exceed ~40deg, and 55deg (giving ~0.70 against a 0.55 block) is a comfortable working value.

notes (2026-07-25, concept 3 - WHY THE OBVIOUS FIX WAS WRONG, worth remembering before someone re-does it): cycle 1 opened the lanes with a LATERAL swing (camera x = 8..9). It resolved the CRITICAL, and quality-auditor PASSed it - but eye-walker then caught a new MODERATE that the deterministic gates and the auditor both missed: with equal 300/300 kg masses the recoil is physically symmetric, yet on screen m1 ended ~28% farther from the start line than m2, contradicting STATE_1's own "recoil symmetrically" caption. Cause: ANY nonzero camera x breaks the mirror symmetry about x = 0 - the body recoiling toward the camera is magnified, the one recoiling away is compressed. Since the lane gap is on z, ELEVATION opens it just as well as a lateral swing while preserving +/-x symmetry EXACTLY. Cycle 2 moved S1/S2/S4 to [0, Y, Z] at theta ~55deg; measured result, equal masses within ~2% of equidistant (was ~28%). RULE: on a concept whose payload is a symmetry or an equal-and-opposite claim, open the lane with elevation (y), never with lateral offset (x).

notes (2026-07-25, concept 3 - the third cycle, a Rule 32d tradeoff resolved rather than waived): after cycle 2, S1/S2/S4 sat at theta ~55deg while STATE_3 remained at its original ~15deg front-on shot, a ~40deg camera jump at the S2->S3 and S3->S4 cuts. eye-walker held this open as MODERATE field3d_two_body_camera_discontinuity_S3_isolation_shot with owner AMBIGUOUS ("design tradeoff, founder sign-off needed"); quality-auditor had accepted it as content-justified. The tempting "make them all match" fix was REJECTED on purpose: lowering S1/S2/S4 toward S3 thins the lane margin toward the 0.55 body size (at 45deg it is 0.60 vs 0.55) and would risk regressing the CRITICAL that parked concept 2 - trading a real CRITICAL for an aesthetic MODERATE. Instead cycle 3 raised STATE_3 ONLY, 14.9deg -> 37.1deg at PRESERVED camera distance (9.31 -> 9.28, so framing and zoom are unchanged and only the tilt moved), narrowing the cut to ~18deg. S1/S2/S4 stayed byte-unchanged. eye-walker then called the finding RESOLVED and returned CLEAN. RULE: when two states have genuinely competing legibility requirements, move the state that is NOT protecting a CRITICAL.

notes (2026-07-25, concept 3 - FOUNDER ATTENTION, one non-blocking disagreement between the two gate agents): at STATE_3's theta = 37deg the two gate agents read the vertical FBD pair slightly differently. quality-auditor logged a [judgment] note that on-screen N appears ~1.4-1.5x mg due to perspective foreshortening (it called this inherent to an elevated 3D camera, not a physics error, cancellation still legible). eye-walker, judging at the reveal-complete frozen frame - the correct graded instant - called them crisp vertical equal-and-opposite twins with no regression, and separately noted a cosmetic ~8deg lean that both arrows develop together only in the late free-run tail (t >= 10000ms, well past the narrated moment) as m1 translates off-centre, which it attributed to ordinary wide-camera perspective convergence present at any camera position. Neither agent treated it as blocking and it is NOT in scar_candidates.sql. Worth the founder's eyes at review since STATE_3's payload IS that mg and N read as equal: frames .visual_runs/newton_third_law/20260725-225740/STATE_3__frozen.png and STATE_3__dense_t12000.png. If the founder wants it tighter, the named delta is "lower STATE_3's elevation back toward ~25-30deg" - it costs only continuity, never the CRITICAL, because STATE_3 has one real body and no lane requirement.

notes (2026-07-25, CHAPTER END for this worktree): all three lom-b concepts are SEALED and baseline-locked (newton_first_law, newton_second_law, newton_third_law - 4 states each, 12 baselines total). engine_commits stayed at 2 for the whole worktree and BOTH were cherry-picks from feat/lom-a, never original engine work here - consistent with the standing workaround for the dispatch-registry asymmetry. Remaining chapter-end steps per CHAPTER_LOOP section 4, NOT done by this session (one concept per session, exit on seal): (1) regression EYE across the regression_sample gauss_law_sphere + gauss_law_solid_sphere; (2) build:review each sealed concept and verify HTTP 200 on 8090; (3) founder review of all three; (4) merge feat/lom-b into feat/lom-a. The expected conflict surface on that merge is unchanged - the three append-style registration lists in panelConfig.ts, intentClassifier.ts and aiSimulationGenerator.ts. TTS, PILOT_CONCEPTS, build:pilot and deploy all remain FOUNDER-GATED and were never touched.
