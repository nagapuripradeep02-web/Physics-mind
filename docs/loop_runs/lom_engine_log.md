# Laws of Motion — chapter engine log

One line per verified engine fix (one commit each). Rollback point for cycle 1: `04aa6fa`.

- 2026-07-25 · `coast_body_halts_mid_state_despite_authored_length_m` (free_body_diagram STATE_3) → root cause was NOT the surface bound: `newtons_laws_body` integrates, so the shared `RESET_TRAJECTORY` handler's `stateStartTime` rebase never rewound `b.s`/`b.v`/`eng.t_ms`; every EYE capture (and every teacher replay) started downrange from the previous one and hit the +22 m clamp mid-series. Fixed by implementing `nlbResetTrajectory()` (+ `s0`/`v0` seeds, `v0` slider writes the seed) in `field_3d_renderer.ts` (+54/−1). EYE: free_body_diagram 27/27, electric_flux 62/62 (H2 0.00% all 20), magnetic_flux 38/38. Scar candidate: `field3d_integrating_scenario_ignores_reset_trajectory_and_carries_stale_accumulator`.

- 2026-07-25 - `nlb_two_body_lane_offset` (free_body_diagram) -> committed `ff408ed`. (Entry backfilled 2026-07-26; see the commit message for detail.)

- 2026-07-25 - `nlb_coupled_initial_velocity_never_seeded` (connected_bodies STATE_1/STATE_2) -> the states rendered fully STATIC despite an authored 0.35 m/s glide. TWO defects on one seeding path: Branch B integrates ONE scalar along the string and overwrites per-body `v` from it every tick, so seeding `b.v` was a no-op; and the string's all-or-nothing bounds veto then zeroed any seed because the hanging body started below `NLB_HANG_MIN_M` = 1.15 m. Fixed by `nlbSeedKinematics()` - clamps each coupled seed into its band (`s` and `s0`), seeds the shared scalar on entry and on `RESET_TRAJECTORY` (+99/-2). Commit `5a07aa9`. EYE: connected_bodies 31/31; regression free_body_diagram 38/38, electric_flux 62/62, magnetic_flux 38/38, all H2 0.00%. Lesson: seed the CONSTRAINT, not its projections. Scar: `nlb_coupled_initial_velocity_never_seeded`.

- 2026-07-25 - `nlb_coupled_readouts_revert_to_rest_values_on_bound_halt` (connected_bodies STATE_3/STATE_6) -> the string-bound veto zeroed `aStr` along with `sAdv`/`vs1`, collapsing the dynamic solution to the rest state once a body halted. STATE_3 - the PRIMARY aha, captioned "T is not m2g" - then displayed `T = 19.60 N = m2g` for ~19 of its 20 s, including the frozen frame a teacher pauses on. Fixed by deleting one assignment: the veto now zeros MOTION only, so the achieved solution is held by recompute (stays live under slider drags, adds no history to rewind) (+27/-1). Commit `bc649d4`. EYE: 31/31; same three regressions clean. Runtime probe: STATE_3 holds 13.07 N late, STATE_6 holds 20.08 N, STATE_7 live under a drag. Lesson: a geometry veto must zero motion, never the force solution. Scar: same bug_class.

- 2026-07-26 - `nlb_pulley_group_hidden_with_surface_in_atwood_mode` (connected_bodies STATE_6) -> the pulley bracket is parented to the SURFACE group (deliberately - one theta rotation stands the post on the incline at any angle), but `surface.hidden` was applied to that group, so the one state authoring it hid the slab AND the pulley: both ropes terminated in open space and the declared `glow_focal: nlb_pulley_wheel` did not exist on screen. Fixed by scoping the hide to the slab mesh; parenting and every transform unchanged (+14/-4). Commit `aa7daf5`. EYE: 31/31; free_body_diagram 38/38, electric_flux 62/62, magnetic_flux 38/38, all H2 0.00%. Isolation: post-fix frozen frames match the run-to-run jitter floor to 4 dp on every state except STATE_6, whose diff bbox is exactly the bracket footprint. Lesson: effective visibility is the AND over the ancestor chain. Scar: `field3d_hide_flag_applied_to_a_group_that_also_parents_unrelated_apparatus`.

> **RUNAWAY GUARD TRIPPED at 5 of 5 loop-phase engine commits (2026-07-26).** Per `CHAPTER_LOOP.md` §3b the loop is PAUSED pending founder review. See `docs/loop_runs/lom/connected_bodies/engine_gap.md` for the analysis and `docs/loop_runs/lom_a_state.md` for the pause notice.

- 2026-07-26 - `nlb_param_ramp_missing_monotonic_reveal` (ENGINE ADDITION, PRE-APPROVED per `CHAPTER_LOOP.md` §7.1, does NOT count against block_on_incline's fresh 3-commit budget) -> `newtons_laws_body` had no way to sweep a physical parameter MONOTONICALLY across a guided state's reveal window (`idle_auto_sweep` is a repeating triangle; `phases[]` drives glow only) — block_on_incline's central beat, "tilt the ramp until the block breaks away at tan θ = μs", could not be authored. Added `param_ramp: { param, from, to, start_ms?, end_ms }` — one-shot, holds at `to` after `end_ms`, writes through the SAME `nlbApplyParam()` path a trusted slider drag uses (so N/drive/arrows/HUD/incline geometry all move together), reads only `eng.t_ms` (Rule 36 closed form — byte-stable under `SET_TIME_FREEZE`, exact under dt-folding), never runs in `mode: 'sandbox'` (Rule 37), and is seized by a trusted slider/drag exactly like the sweep. `deriveStateMeta.ts` now also considers `param_ramp.end_ms` when computing a state's max reveal ms (+12). Runtime probe (Playwright, real renderer): monotonic non-decreasing θ, holds at 35° past end_ms, body stuck (v=0) below tan⁻¹(0.45)≈24.23°, moves after, byte-identical under a time-freeze pin, and N×0.016s ≡ 1×(0.016×N)s to machine precision; also proved the ramp never runs in a sandbox state. EYE regression: electric_flux 62/62, magnetic_flux 38/38, both clean. Scar candidate (directive, not incident): `field3d_param_ramp_authoring_contract` — a `param_ramp` state must author its own surface/body value for the ramped param equal to `from`, or state entry visibly jumps before the ramp starts.
